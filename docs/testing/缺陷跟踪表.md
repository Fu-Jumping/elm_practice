# 缺陷跟踪表

日期：2026-09-04（2026-09-09 补负责人认可备注，见 §2）  
维护人：测试联调同学（王馨琪）  
依据：`docs/project/小组协作与提交流程.md`、`docs/project/TDD实施规划.md`

> **定位（2026-09-08 负责人拍板）**：本表是缺陷跟踪的**唯一正式台账**，纳入 Git 跟踪，写权归测试联调同学（王馨琪）。明细与证据放 `docs/testing/问题记录/YYYY-MM-DD-<主题>.md`，两者用编号互链；规则口径见 `docs/project/项目规则.md` §1.1。

## 1. 使用规则

- 编号递增且不复用：接口/业务缺陷使用 `BUG-YYYYMMDD-001`；展示问题可使用 `SHOW-QA-001`。
- **落盘要求（2026-09-08 新增）**：发现即在本表登记编号，并把明细落 `docs/testing/问题记录/YYYY-MM-DD-<主题>.md`；不得只发群聊、共享文档或本地归档（`docs/_archive/**` 已 gitignore）。群聊与口头只作通知，不作证据。
- **来源互链**：每条缺陷在"来源问题记录"与"证据/备注"字段填写对应文件名，问题记录文件头注明台账编号。
- 发现即登记，至少填写复现环境、前置数据、步骤、实际结果、期望结果和证据；没有截图时填写请求/响应摘要或日志位置。
- 严重程度：`P0` 主链路阻塞、白屏、金额/权限/数据严重错误；`P1` 核心功能可绕过或结果错误；`P2` 非主链路问题；`P3` 体验或文案建议。
- 状态流转：`新建 → 已确认 → 修复中 → 待回归 → 已关闭`；无法复现为 `待补充`，暂不处理为 `延期`，重复问题标记 `重复`。
- 缺陷修复若需要改代码，必须先补能失败的回归测试，提交顺序严格为 `test:` → `fix:`；不得删除、跳过或放宽原测试。
- 关闭条件：修复提交可定位、原步骤回归通过、相关模块和 P0 主链路回归通过；未关闭缺陷不得写成已完成。
- 明细落盘口径（2026-09-08，PR #36 评审跟进）：本批 BUG-001~012 明细暂以本表第 3 节详情区块为唯一载体，未拆分 `docs/testing/问题记录/**` 独立文件；是否对后续新缺陷启用问题记录明细文件，由测试联调同学（王馨琪）确认口径后回填本条。SHOW-QA 类展示问题沿用 main 上既有问题记录明细模式。

## 2. 缺陷总表

| 编号 | 发现日期 | 模块/接口 | 标题 | 严重程度 | 状态 | 责任人 | 修复提交 | 回归日期 | 证据/备注 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| BUG-20260908-001 | 2026-09-08 | 用户端部署/环境 | 5174 部署实际运行 mock 模式，真实联调未生效 | P0 | 已关闭 | 前端负责人 | `.env.development.local` 修复已生效（文档固化随 docs: 提交） | 2026-09-08 | real 注入实测；登录后刷新会话保持（真实模式）实测通过 |
| BUG-20260908-002 | 2026-09-08 | 后端 CORS/登录 | 部署来源不在 CORS 白名单，真实模式登录等写请求全部 403 | P0 | 已关闭 | 后端负责人 | `575c428`（部署 `147221`） | 2026-09-08 | 部署后带 Origin 登录 200；浏览器真实登录进入商家后台/用户端订单页 |
| BUG-20260908-003 | 2026-09-08 | 商家注册 `POST /merchants` | 字段名不一致：契约/前端 `contactPhone` vs 后端 `phone`，注册必然失败 | P0 | 已关闭 | 后端+前端负责人 | `575c428`（部署 `147221`） | 2026-09-08 | 部署后 `contactPhone` 注册 200 建店 m1033、重复账号 409 |
| BUG-20260908-004 | 2026-09-08 | 商家分类 `POST/PATCH /merchant/categories` | 字段名不一致：前端 `sort` vs 后端 `sortOrder`，排序值被丢弃 | P1 | 已关闭 | 后端+前端负责人 | `5b8c6d8` | 2026-09-08（接口级通过） | 红测试 54fd710 转绿；真实后端 sortOrder:9 保留 |
| BUG-20260908-005 | 2026-09-08 | 商家订单 `GET /merchant/orders` | `PENDING_PAYMENT` 订单对商家不可见，与 IC-MORD-01 用例冲突 | P1 | 已确认 | 需求/后端负责人 | 用例修订随 docs 提交 | 2026-09-08 | 口径已定：以契约 §5 为准（支付前不可见、支付后出现）；IC-MORD-01/MA-06 用例已修订 |
| BUG-20260908-006 | 2026-09-08 | 商家端部署完整性 | merchant-admin 未部署且部署目录代码不完整，MA-01~07 无法执行 | P1 | 已关闭 | 部署负责人 | 部署同步（pm2 `elm-merchant` :**4001**） | 2026-09-08 | 外网 `http://82.157.137.114:4001/` 实测 200；登录/分类实测通过；MA-01~07 按新地址执行 |
| BUG-20260908-007 | 2026-09-08 | 环境口径/文档 | 后端实际端口 4000 与文档/示例配置 8080 不一致，且 8080 被其他应用占用 | P2 | 已确认 | 部署+文档负责人 | - | - | 8080 现为 gunicorn（SlideWeaver）；按 `.env.example` 配置会打到错误服务 |
| BUG-20260908-008 | 2026-09-08 | 用户端登录后路由切换 | 登录成功后白屏、刷新才渲染（历史缺陷，两项根因均已修复） | P0 | 已关闭 | 用户端负责人 | `c84c4b6`（随合并 `c338325` 入库） | 2026-09-08 | 真实模式浏览器回归：登录后直接渲染订单页无白屏、刷新会话保持；回归锁 120/120 绿 |
| BUG-20260908-010 | 2026-09-08 | 商家端订单列表/详情 | 订单归一化未对齐真实形状：金额 ¥0.00、顾客缺失、状态英文原样 | P1 | 已关闭 | 商家端负责人（王馨琪） | `62a77a7`（红 `7b4a6e8`） | 2026-09-08 | 浏览器实测 ¥31.00/张同学/已完成；红→绿 4/4，全量 10/10。注：22:15 正式构建产物缺备注展示（构建源未含 62a77a7），待 PR #36 合并后重新构建闭环 |
| BUG-20260908-011 | 2026-09-08 | 后端营业状态 `PATCH /merchant/store/status` | 状态修改只改内存对象未写库，用户端查询始终 OPEN | P0 | 已关闭 | 后端负责人 | 部署 jar `147221`（含修复，22:24 重启） | 2026-09-08 | 正式链路回归：切 CLOSED 后用户端 /stores/m002 同步 CLOSED，恢复 OPEN 亦同步 |
| BUG-20260908-012 | 2026-09-08 | 商家端店铺设置联系电话 | 前端表单字段后端不支持：保存丢失、回显为空 | P2 | 新建 | 后端+前端负责人 | - | - | `StorePatch` 无 contactPhone；需求归口待确认 |
| BUG-20260908-009 | 2026-09-08 | 用户端源码版本管理 | user-h5 全部源码未纳入任何 Git 仓库，唯一副本在部署目录 | P1 | 已关闭 | 用户端负责人+组长 | 随 main 入库（合并 `c338325`） | 2026-09-08 | 主仓库已含 user-h5 133 个文件；部署目录改为 git 同步的建议并入 BUG-006 |
| SHOW-QA-001 | 2026-09-08 | 用户端/商家详情页-购物车抽屉 | 展开态抽屉未贴底，底边与购物车栏之间留 8px 透视缝（1280 视口放大到 26.3px） | P1（待确认） | 待回归 | 用户端负责人 | `fix:` 386a0d0（`test:` 4352ba1） | 2026-09-08（开发侧） | `docs/testing/问题记录/2026-09-08-商家详情页购物车抽屉未贴底.md`；负责人 2026-09-09 认可严重程度建议（001=P1），正式生效以测试联调同学确认为准 |
| SHOW-QA-002 | 2026-09-08 | 用户端/全局页面壳 | `.app-shell` 的 `max-width: 430px` 被 px→vw 换算成 110.256vw，桌面预览居中约束失效 | P3（待确认） | 新建 | 用户端负责人（待指派） | - | - | `docs/testing/问题记录/2026-09-08-用户端页面壳430px居中失效.md`；按问题记录口径纳入本期需求变更窗口统一评估；负责人 2026-09-09 认可严重程度建议（002=P3），002 纳入本期需求变更窗口，正式生效以测试联调同学确认为准 |
| SHOW-QA-003 | 2026-09-08 | 商家端登录/注册卡片 | 矮视口（现场演示 iframe 100% 缩放）下卡片底部被裁切 | P3 | 已关闭 | 商家端负责人（王馨琪） | `733a02f`（定稿，演进 7e50425/7e4e18f/e7afee8） | 2026-09-08 | 900 视口实测：注册按钮底 649，距块底余量 131px |

> 编号、严重程度与状态由测试联调同学（王馨琪）确认后生效（`docs/project/项目规则.md` §1.1 第 8 条：AI 可代拟条目，编号分配与状态结论由测试联调同学确认）。

## 3. 缺陷详情

复制以下区块新增记录，并同步更新第 2 节总表。

### BUG-20260908-001

| 字段 | 内容 |
| --- | --- |
| 发现日期/发现人 | 2026-09-08 / 王馨琪（AI 辅助执行浏览器与接口测试） |
| 模块/接口/页面 | 用户端 H5 部署（http://82.157.137.114:5174/）环境配置 |
| 环境与版本 | 前端 vite dev（pm2 `elm-h5`）@ `frontend/user-h5`；后端 `127.0.0.1:4000`；提交 3dbf1e0（部署副本带未提交改动） |
| 前置数据 | 无 |
| 复现步骤 | 1. 读取 vite 正在服务的模块 `GET :5174/src/services/http/index.ts`；2. 观察 `import.meta.env` 注入值；3. 登录后进入 /orders，按 F5 刷新 |
| 实际结果 | 注入值为 `VITE_API_MODE=mock`、`VITE_API_BASE_URL=http://127.0.0.1:8080/api/v1`，整站使用 Mock 演示数据（订单页显示 mock 的麦当劳 ¥27.50/肯德基 ¥41.00，真实库中无麦当劳订单）；登录会话仅存内存，刷新后被踢回 `/login?redirect=/orders` |
| 期望结果 | 依据测试计划 §4 与 `.env.local`（2026-09-08 04:10）部署意图，应运行 `real` 模式 + 同源代理转发 `/api/v1 → 127.0.0.1:4000`；登录会话刷新后保持（MA-01 同口径） |
| 严重程度 | P0 |
| 是否阻塞 P0 | 是（真实接口联调完全未发生） |
| 证据 | `curl :5174/src/services/http/index.ts` 注入值；`.env.development`（mock）与 `.env.local`（real）内容；刷新丢会话 DOM 快照 |
| 初步原因 | Vite 环境文件优先级：mode 专属 `.env.development` 高于通用 `.env.local`，real 配置写错文件被 mock 压制 |
| 责任人 | 前端负责人 |
| 修复方案 | 临时修复：部署机 `.env.development.local`（不入库）+ 重启，已于 2026-09-08 实测生效；正式修复由用户端负责人（江涵）将 real 模式配置修正合入 main，合入后服务器临时文件作废、以 main 为准 |
| 测试提交 | 待补（建议先补「real 模式注入断言」失败测试） |
| 修复提交 | 待提交（测试当日已按上述方案临时修复并重启生效，注入值已变为 real；待负责人补提交并回归） |
| 回归结果 | 通过（2026-09-08）：注入值实测 real；真实模式浏览器登录后刷新会话保持（订单页直接渲染） |
| 当前状态 | 已关闭 |

### BUG-20260908-002

| 字段 | 内容 |
| --- | --- |
| 发现日期/发现人 | 2026-09-08 / 王馨琪（AI 辅助执行） |
| 模块/接口/页面 | 后端 `WebConfig` CORS 配置；用户端登录页 `POST /auth/login` |
| 环境与版本 | 后端 `127.0.0.1:4000`（elm-practice-backend-0.1.0-SNAPSHOT，pm2 `elm-backend`）；前端 `http://82.157.137.114:5174` real 模式；提交 3dbf1e0 |
| 前置数据 | 固定账号 `13800000001 / 123456` |
| 复现步骤 | 1. 用户端切到 real 模式；2. 打开登录页输入正确账号密码；3. 点击登录；4. 观察网络请求 |
| 实际结果 | 页面提示「请求失败（HTTP 403）」，无法登录；浏览器直发 `POST /api/v1/auth/login` 返回 `403`，响应体为纯文本 `Invalid CORS request`（非 JSON） |
| 期望结果 | 登录成功进入后台（MA-01）。依据 IC-X-01，失败响应也必须为统一 JSON；合法来源不应被 CORS 拦截 |
| 严重程度 | P0 |
| 是否阻塞 P0 | 是（真实模式下登录后所有 P0 场景全部被阻塞） |
| 证据 | 页面内 `fetch('/api/v1/auth/login', {method:'POST'})` → `403 :: Invalid CORS request`；同请求经 curl（无 Origin 头）→ 200，证明是 Origin 校验拦截 |
| 初步原因 | `WebConfig.addCorsMappings` 的 `allowedOriginPatterns` 仅允许 `http://localhost:*`、`http://127.0.0.1:*`；代理转发保留浏览器 `Origin: http://82.157.137.114:5174`，与 Host（被 changeOrigin 改写为 127.0.0.1:4000）不同源，命中 CORS 校验且不在白名单 |
| 责任人 | 后端负责人 |
| 修复方案 | 白名单追加部署来源（如 `http://82.157.137.114:*`）；或在 vite 代理/网关层重写 Origin；注意 GET 不带 Origin 头所以浏览类接口未暴露此问题 |
| 测试提交 | `a4b5a0e` test: 补 BUG-002/BUG-003 失败测试锁定浏览器 403 与商家注册 400（红端） |
| 修复提交 | `575c428` fix: 修复 BUG-002 浏览器跨源 403 与 BUG-003 商家注册不认契约字段（后端 22/22 绿）；已随合并 `c338325` 进入当前分支 |
| 回归结果 | 通过（2026-09-08）：新 jar 部署后带 Origin 头 `POST /auth/login` 返回 200；浏览器真实登录进入商家后台与用户端订单页 |
| 当前状态 | 已关闭 |

### BUG-20260908-003

| 字段 | 内容 |
| --- | --- |
| 发现日期/发现人 | 2026-09-08 / 王馨琪（AI 辅助执行） |
| 模块/接口/页面 | 商家注册 `POST /api/v1/merchants`（IC-MER-02）；后端 `AuthService.registerMerchant`、`Requests.MerchantRegister`；前端 merchant-admin `register()` |
| 环境与版本 | 后端 `127.0.0.1:4000`；提交 3dbf1e0 |
| 前置数据 | 无（使用一次性测试账号） |
| 复现步骤 | 1. `POST /merchants`，请求体按契约与前端实现发送 `{account, password, storeName, contactPhone}`，`contactPhone` 为合法 11 位手机号 `13912340111` |
| 实际结果 | `400 {code:40001, message:"联系电话必须是 11 位手机号"}`，注册必然失败；改用字段名 `phone` 后同一数据返回 200 并建店（ma1012/m1013） |
| 期望结果 | 依据 IC-MER-02：合法请求 `{account,password,storeName,contactPhone}` 应成功建店；重复账号 409、空名称/非法电话 400 |
| 严重程度 | P0 |
| 是否阻塞 P0 | 是（商家注册主链路在真实模式下不可用） |
| 证据 | 后端 DTO `Requests.MerchantRegister` 字段为 `account, password, storeName, phone, description`；merchant-admin `register()` 原样发送含 `contactPhone` 的 input；两组请求/响应摘要见回归记录 |
| 初步原因 | 契约字段 `contactPhone` 与实现字段 `phone` 不一致，前端按契约实现 |
| 责任人 | 后端+前端负责人 |
| 修复方案 | 二选一并同步契约：后端 DTO 兼容 `contactPhone`（推荐，契约/用户端地址域已用该命名），或契约、前端统一改 `phone` |
| 测试提交 | `a4b5a0e`（与 BUG-002 同批红端测试） |
| 修复提交 | `575c428`（DTO 增加 `contactPhone`，AuthController 兼容两者；已随合并 `c338325` 进入当前分支） |
| 回归结果 | 通过（2026-09-08）：新 jar 部署后按契约 `contactPhone` 注册 200 建店（m1033）；重复账号 409、空店名 400 复测通过 |
| 当前状态 | 已关闭 |

### BUG-20260908-004

| 字段 | 内容 |
| --- | --- |
| 发现日期/发现人 | 2026-09-08 / 王馨琪（AI 辅助执行） |
| 模块/接口/页面 | 商家分类创建/修改 `POST/PATCH /merchant/categories`；merchant-admin `CategoryDraft` |
| 环境与版本 | 后端 `127.0.0.1:4000`；提交 3dbf1e0 |
| 前置数据 | 商家 merchant-a 登录 |
| 复现步骤 | 1. `POST /merchant/categories` 提交 `{"name":"QA-test-cat-A","sort":9}`；2. 查看返回与列表排序 |
| 实际结果 | 返回 `sortOrder:1`，`sort:9` 被静默丢弃，排序值永远无法保存 |
| 期望结果 | 分类排序值按提交保存并生效 |
| 严重程度 | P1 |
| 是否阻塞 P0 | 否 |
| 证据 | 前端 `CategoryDraft {name, sort?}` 且 `createCategory` 原样发送；后端 `Requests.CategoryCreate` 仅绑定 `name, sortOrder`；响应 `{"categoryId":"c1010","name":"QA-test-cat-A","sortOrder":1}` |
| 初步原因 | 前后端字段命名不一致（`sort` vs `sortOrder`），与 BUG-20260908-003 同类 |
| 责任人 | 后端+前端负责人 |
| 修复方案 | 统一为 `sortOrder`（推荐）并补契约说明；前端提交与回显字段同步修改 |
| 测试提交 | `54fd710` test: 分类排序字段契约红测试：创建分类应保留 sortOrder（预期红）；首次运行 2/2 失败（expected undefined to be 9/7），红证据已记入 `docs/record/daily/2026-09-08.md` |
| 修复提交 | `5b8c6d8` fix: 统一分类排序字段为 sortOrder（services 层 + App.tsx 表单/表格列/表单项，共 5 处） |
| 回归结果 | 通过（2026-09-08）：接口级（真实后端创建 sortOrder:9 保留、列表回显 9，数据已清理）+ 浏览器 UI 实测（分类管理新建排序值 9 端到端保留，测试数据已清理）；54fd710 转绿，全量测试与 build 通过 |
| 当前状态 | 已关闭 |

### BUG-20260908-005

| 字段 | 内容 |
| --- | --- |
| 发现日期/发现人 | 2026-09-08 / 王馨琪（AI 辅助执行） |
| 模块/接口/页面 | 商家订单列表/详情 `GET /merchant/orders`、`GET /merchant/orders/{orderId}`（IC-MORD-01/02） |
| 环境与版本 | 后端 `127.0.0.1:4000`；提交 3dbf1e0 |
| 前置数据 | 用户 u001 在店铺 m002 创建订单 `o1007`（PENDING_PAYMENT，实付 68.00） |
| 复现步骤 | 1. 用户端下单（成功，订单存在于用户订单列表）；2. 商家 merchant-a 调用 `GET /merchant/orders`；3. `GET /merchant/orders/o1007` |
| 实际结果 | 列表仅返回 `o1001(PROCESSING)、o1002(COMPLETED)`，不含 `o1007/o1003`（PENDING_PAYMENT）；订单详情返回 `404 订单不存在` |
| 期望结果 | 依据 IC-MORD-01：用户创建 m002 订单后应出现在商家列表；MA-06 要求新订单可见 |
| 严重程度 | P1（口径冲突，待需求澄清后可能调整级别） |
| 是否阻塞 P0 | 待定（若按用例原文验收则阻塞 IC-MORD-01/MA-06） |
| 证据 | `OrderMapper.listForMerchant` SQL `AND status != 'PENDING_PAYMENT'`，注释「商家端口径：本店铺且未支付订单不可见（契约 §5/PRD 7.6）」；`OrderService.merchantGet` 对 PENDING_PAYMENT 直接 404 |
| 初步原因 | 实现按契约 §5/PRD 7.6 有意过滤，与测试用例 IC-MORD-01 文本冲突，属需求口径未固化 |
| 责任人 | 需求/SRS 负责人确认，后端配合 |
| 修复方案 | 已定：维持现口径（支付前不可见），不修改代码与 PRD；IC-MORD-01/MA-06 用例已按契约 §5 修订（支付前不出现、支付后出现，步骤补支付动作），随 docs 提交 020cf45 |
| 测试提交 | 不适用（用例修订） |
| 修复提交 | 用例修订随 `020cf45` docs: 提交 |
| 回归结果 | 不适用（口径确认类）；后续按修订后用例执行回归 |
| 当前状态 | 已确认（已按修订用例闭环，无需代码修复） |

### BUG-20260908-006

| 字段 | 内容 |
| --- | --- |
| 发现日期/发现人 | 2026-09-08 / 王馨琪（AI 辅助执行） |
| 模块/接口/页面 | 商家端部署（merchant-admin） |
| 环境与版本 | 部署机 `/home/ubuntu/workspace/elm_practice`（3dbf1e0 + 未提交改动）；主仓库 `/home/wxq/elm_practice` |
| 前置数据 | 无 |
| 复现步骤 | 1. 检查服务器进程：仅有 user-h5 vite（5174）与后端（4000）在跑，无 merchant-admin 实例；2. 检查部署副本 `frontend/merchant-admin/src/` |
| 实际结果 | merchant-admin 无任何部署实例；部署副本 `src/` 仅 8 个文件，缺 `services/`、`tests` 依赖文件等（git status 显示大量修改/删除未提交），与主仓库 3dbf1e0 内容不一致 |
| 期望结果 | 商家端按 MA-01~07 验收前提可访问、代码与主仓库一致 |
| 严重程度 | P1 |
| 是否阻塞 P0 | 是（商家端页面验收场景全部无法执行） |
| 证据 | `pm2 list`、`find .../merchant-admin/src -type f`（8 个文件）、`git status --short` 输出 |
| 初步原因 | 部署目录与主仓库不同步，拷贝不完整；且部署目录并非 git 仓库（纯目录拷贝，见 BUG-20260908-009），无法用版本号核对一致性；无商家端部署计划记录 |
| 责任人 | 部署负责人 |
| 修复方案 | 以主仓库同步部署目录并起 merchant-admin 实例（最终端口 4001，安全组已放行） |
| 测试提交 | 不适用（部署问题） |
| 修复提交 | 部署同步（pm2 `elm-merchant`，非代码提交；配置入库 `b407d27`/`38a26fc`/`e5bef50`） |
| 回归结果 | 通过（2026-09-08）：外网 `http://82.157.137.114:4001/` 实测 200，登录/订单/分类/商品全流程实测通过（MA-01~07 证据见接口级联调用例 §4） |
| 当前状态 | 已关闭 |

### BUG-20260908-008

| 字段 | 内容 |
| --- | --- |
| 发现日期/发现人 | 历史缺陷（2026-09-07 首次实测发现）；2026-09-08 复盘登记 / 王馨琪（AI 辅助调查） |
| 模块/接口/页面 | 用户端登录 → 路由切换渲染（OrderListView / App 级 Transition） |
| 环境与版本 | 用户端 H5（部署副本 `frontend/user-h5`，vite dev 5174）；留痕 `docs/record/raw/2026-09-07/用户端-1418.md` |
| 前置数据 | 固定账号 `13800000001 / 123456` |
| 复现步骤（历史） | 1. 打开登录页输入正确账号密码；2. 点击登录；3. 登录成功跳转后页面白屏；4. 手动刷新后页面才正常渲染 |
| 实际结果（根因，共两项，均已修复） | ① 9/7 形状不一致：真实后端 `GET /orders` 为扁平字段（`itemSubtotal/packagingFee/total`、`address` 对象、无 `storeName`），前端按嵌套 `amounts.payableAmount` 渲染 → OrderListView TypeError 崩溃 → 白屏；② 9/8 部署实测：App 级 `<Transition mode="out-in">` 在登录页（BlankLayout）→ 主页（MainLayout）懒加载路由切换时丢失 enter 阶段 → 白屏（Vue3 已知竞态，显式 duration 亦无效） |
| 期望结果 | 登录成功后直接渲染目标页（订单页/主页），无白屏、无需手动刷新（MA-01 同口径） |
| 严重程度 | P0（历史阻塞主链路） |
| 是否阻塞 P0 | 修复前是；当前代码已带修复 |
| 证据（修复现状核查，2026-09-08） | ① `normalizers.ts` 已实现扁平字段→视图模型映射（`itemSubtotal/total → itemsTotal/payableAmount`），`normalizers.spec.ts` 用真实后端 JSON 作回归锁；② `App.vue` 已去掉 `mode="out-in"` 保留 opacity 过渡（注释留痕 2026-09-08）；③ user-h5 全量测试 19 文件 120 用例全部通过（含 M1-M12 mock/真实形状一致性锁） |
| 初步原因 | 联调期 mock 与真实后端响应形状不一致（①）；Vue out-in 过渡与懒加载路由组件的竞态（②） |
| 责任人 | 用户端负责人 |
| 修复方案 | 已实施并入库：①真实形状归一化（N1-N3）+ 真实形状 fixture 回归锁；②移除 out-in 过渡模式（`c84c4b6`，随 main 入库，user-h5 源码已全部入库） |
| 测试提交 | 回归锁测试已随 user-h5 入库（main 上共 133 文件，2026-09-08 合并 `c338325` 进入当前分支） |
| 修复提交 | `c84c4b6` fix: 登录后白板——App 根 Transition 去除 out-in 模式（main 已入库） |
| 回归结果 | 通过（2026-09-08）：单元回归锁 120/120 绿；真实模式浏览器回归「登录→直接渲染订单页无白屏、刷新会话保持」通过 |
| 当前状态 | 已关闭 |

### BUG-20260908-010

| 字段 | 内容 |
| --- | --- |
| 发现日期/发现人 | 2026-09-08 / 王馨琪（AI 辅助部署回归时发现） |
| 模块/接口/页面 | 商家端订单列表/详情（`normalizeOrder`、`orderStatusLabel`） |
| 环境与版本 | 商家端 5175（新部署）+ 后端 4000（MySQL 迁移后 jar）；提交 `7b4a6e8`（红）/`62a77a7`（绿） |
| 前置数据 | 商家 merchant-a 登录，店铺 m002 存在订单 o1001/o1002 |
| 复现步骤 | 1. 登录商家后台进入订单管理；2. 观察金额、顾客、状态列 |
| 实际结果（修复前） | 金额 ¥0.00、顾客「暂无」、o1002 状态显示原始 `COMPLETED`；详情页地址为 `[object Object]` |
| 期望结果 | 展示后端真实金额（¥31.00/¥21.00）、顾客（张同学）与中文状态（进行中/已完成） |
| 严重程度 | P1（金额展示错误，按小组分级口径属必修） |
| 是否阻塞 P0 | 否（订单主链路可用，展示错误） |
| 证据 | 红测试 `7b4a6e8` 首次运行 4/4 失败（expected +0 to be 29 / undefined to be 张同学 / '[object Object]' / 'COMPLETED'）；修复后浏览器实测 ¥31.00/张同学/已完成 |
| 初步原因 | 后端 MySQL 迁移后为扁平金额字段（itemSubtotal/total），商家端 `normalizeOrder` 别名未覆盖；顾客字段嵌套于 address 快照未展平；与用户端 9/7 白屏（BUG-008 根因①）同类 |
| 责任人 | 商家端负责人（王馨琪） |
| 修复方案 | `normalizeOrder` 补 `itemSubtotal/total` 别名、从 address 快照展平顾客/电话/地址文本；`orderStatusLabel` 补完整中文映射 |
| 测试提交 | `7b4a6e8`（真实形状 fixture 回归锁，4 用例） |
| 修复提交 | `62a77a7` |
| 回归结果 | 通过（2026-09-08）：红测试转绿 4/4、全量 10/10、build 无 TS 错误；浏览器实测订单页 ¥31.00/张同学/已完成 |
| 当前状态 | 已关闭 |

### BUG-20260908-011

| 字段 | 内容 |
| --- | --- |
| 发现日期/发现人 | 2026-09-08 / 王馨琪（AI 辅助 MA-03 验收发现） |
| 模块/接口/页面 | 后端 `PATCH /merchant/store/status`（MerchantController.status） |
| 环境与版本 | 后端 4000，MySQL 迁移版（main 合并 `c338325` 后重建部署） |
| 前置数据 | 商家 merchant-a 绑定店铺 m002 |
| 复现步骤 | 1. `PATCH /merchant/store/status` `{"status":"CLOSED"}` → 200 且响应显示 CLOSED；2. `GET /stores/m002`（用户端视角）→ 仍 OPEN；3. `TEMPORARILY_CLOSED` 同样不生效 |
| 实际结果 | 状态不落库：控制器只修改内存对象 `st.status`，无任何 mapper/持久化调用；重启或公开查询均回 OPEN |
| 期望结果 | 依据 MA-03：状态修改保存后用户端查询同步（三种状态生效） |
| 严重程度 | P0（状态明显错误，MA-03 验收不通过，直接影响用户端可见性） |
| 是否阻塞 P0 | 是（MA-03 不通过） |
| 证据 | 控制器源码：`st.status = Domain.StoreStatus.valueOf(...)` 后无持久化调用；双视角对比：商家视角 CLOSED / 公开视角 OPEN |
| 初步原因 | 内存 Repository → MyBatis/MySQL 迁移时，该端点遗漏持久化写库（旧实现靠内存对象即生效） |
| 责任人 | 后端负责人 |
| 修复方案 | 状态校验后调用 store mapper 更新落库；先补红端集成测试（PATCH 后 `GET /stores/{id}` 断言 CLOSED，当前必红） |
| 测试提交 | 待后端补（建议保留：PATCH 后 GET /stores/{id} 断言 CLOSED 的集成用例） |
| 修复提交 | 22:24 重构部署 jar 已含修复（后端修复提交号由后端补录） |
| 回归结果 | 通过（2026-09-08 正式链路）：切 CLOSED → 用户端 /stores/m002 同步 CLOSED；恢复 OPEN → 同步 OPEN；非法值 400 维持 |
| 当前状态 | 已关闭 |

### BUG-20260908-012

| 字段 | 内容 |
| --- | --- |
| 发现日期/发现人 | 2026-09-08 / 王馨琪（AI 辅助 MA-03 验收发现） |
| 模块/接口/页面 | 商家端店铺设置联系电话（前端 `StoreDraft.contactPhone` vs 后端 `Requests.StorePatch`） |
| 环境与版本 | 后端 4000；商家端 4001；提交 `c338325` |
| 前置数据 | 商家 merchant-a 打开店铺设置 |
| 复现步骤 | 1. 店铺设置填写联系电话保存（表单必填）；2. 刷新页面 |
| 实际结果 | 保存后电话不回显（后端 `StorePatch` 字段仅 name/description/image/startPrice/deliveryFee，无 contactPhone，保存值被丢弃）；该必填项回显为空还会阻断同表单的状态保存提交 |
| 期望结果 | 联系电话按契约保存并回显 |
| 严重程度 | P2 |
| 是否阻塞 P0 | 否（绕过方式：每次填写即可提交其他字段） |
| 证据 | `Requests.StorePatch` 字段清单；UI 回显快照；`GET /merchant/store` 无 contactPhone 返回 |
| 初步原因 | 前端表单字段超出契约/后端实现范围，两侧未对齐 |
| 责任人 | 后端+前端负责人 |
| 修复方案 | 契约对齐：`StorePatch` 增加 `contactPhone`（推荐）或前端移除该字段并调整必填规则 |
| 测试提交 | 待补 |
| 修复提交 | - |
| 回归结果 | 待执行 |
| 当前状态 | 新建 |

### BUG-20260908-009

| 字段 | 内容 |
| --- | --- |
| 发现日期/发现人 | 2026-09-08 / 王馨琪（AI 辅助调查白屏时发现） |
| 模块/接口/页面 | 用户端源码版本管理（`frontend/user-h5` 全部源码、测试、mocks、留痕文档） |
| 环境与版本 | 主仓库 `~/elm_practice`（分支 codex/merchant-p0-closeout，HEAD a3e771e）；部署副本 `/home/ubuntu/workspace/elm_practice` |
| 前置数据 | 无 |
| 复现步骤 | 1. 主仓库执行 `git ls-tree -r HEAD --name-only \| grep -c user-h5` → 0；`ls frontend/` → 仅 merchant-admin 与 README；2. 部署副本目录执行 `git status` → `fatal: not a git repository` |
| 实际结果 | user-h5 整个工程（含 120 个单测、mocks、原始留痕 `docs/record/raw/`）只存在于部署目录的普通文件夹中，无任何 Git 历史其为版本控制 |
| 全分支复核（2026-09-08） | `git log --all -- '*user-h5*'` 无任何提交；`main`、`feat/merchant-admin-init`、`origin/main`、`origin/feat/merchant-admin-init` 四个分支 ls-tree 计数均为 0；全盘搜索 `/home/ubuntu`、`/tmp` 下名为 user-h5 的目录仅部署副本一处——即部署副本是**全服务器唯一副本**（66 个文件在 9/7 后修改，最近为 9/8 04:30 的 App.vue 白屏修复） |
| 期望结果 | 依据课程要求「必须持续提交…老师会通过仓库记录观察项目过程」与小组协作规范：所有源码与留痕应入库并有提交历史 |
| 严重程度 | P1（唯一副本损毁即丢失全部用户端代码与联调证据，风险极高） |
| 是否阻塞 P0 | 否（不影响当前运行，但阻塞 BUG-20260908-008 的修复提交闭环） |
| 证据 | 上述两条命令输出；部署副本 `.env*`/`src/` 均无 `.git` 目录 |
| 初步原因 | 用户端工程未按单仓库口径迁入主仓库，部署时直接以目录拷贝形式上线 |
| 责任人 | 用户端负责人+组长 |
| 修复方案 | 将部署副本的 user-h5（含 `docs/record/raw/` 留痕）迁入主仓库：确认与主仓库无冲突后整目录入库，按 `feat:`（或 `chore:`）提交并注明来源；部署目录改为 `git clone`/`git archive` 同步，杜绝裸目录部署 |
| 测试提交 | 不适用（流程缺陷） |
| 修复提交 | user-h5 已随 main 入库（合并 `c338325` 后 `git ls-tree -r HEAD \| grep -c user-h5` = 133） |
| 回归结果 | 通过（2026-09-08）：源码已在版本控制内且可检出；遗留改进「部署目录改为 git 同步」并入 BUG-006 跟踪 |
| 当前状态 | 已关闭 |

### BUG-20260908-007

| 字段 | 内容 |
| --- | --- |
| 发现日期/发现人 | 2026-09-08 / 王馨琪（AI 辅助执行） |
| 模块/接口/页面 | 环境口径：后端端口、`.env.example`、测试计划 §4 |
| 环境与版本 | 部署机 82.157.137.114 |
| 前置数据 | 无 |
| 复现步骤 | 1. `curl http://127.0.0.1:8080/api/v1/shops`；2. 查看响应头与响应体；3. 对照 `application.properties`、`.env.example` 与测试计划文档 |
| 实际结果 | 8080 由 gunicorn（其他项目 SlideWeaver）监听，返回 `text/html` 的 404 页面；elm 后端实际挂 4000（`application.properties` 仍写 8080）；后端 4000 与前端 dev server 5174 均直接暴露公网 |
| 期望结果 | 联调口径一致：文档/示例配置指向真实后端地址；错误响应为本项目统一 JSON（避免误配后误判为后端缺陷）；生产部署不用 dev server |
| 严重程度 | P2 |
| 是否阻塞 P0 | 否（本次实测已按真实端口执行） |
| 证据 | `Server: gunicorn` 响应头 + HTML 404；`ss -tlnp` 端口归属；`application.properties` `server.port=8080` |
| 初步原因 | 8080 被其他应用占用后后端临时改挂 4000，文档与示例配置未同步 |
| 责任人 | 部署+文档负责人 |
| 修复方案 | 更新测试计划 §4、`.env.example`、`application.properties` 注释为实际端口；安全组收敛 4000/5174 直连暴露；正式部署改用构建产物 + 反向代理 |
| 测试提交 | 不适用 |
| 修复提交 | - |
| 回归结果 | 待执行 |
| 当前状态 | 已确认 |
### SHOW-QA-001

| 字段 | 内容 |
| --- | --- |
| 发现日期/发现人 | 2026-09-08 / 负责人（用户本人反馈；AI 代拟条目与证据整理） |
| 模块/接口/页面 | 用户端商家详情页——点餐区底部购物车抽屉（纯前端定位） |
| 环境与版本 | 前端 `http://localhost:5173`（mock 模式）；视口 390x844 / 390x568 / 430x900 / 1280x800；本机 Chrome；缺陷引入 `f1243a7`，基点 `782ebd2` |
| 前置数据 | 未登录亦可复现；店铺 `m002`（肯德基宅急送） |
| 复现步骤 | 1. 打开 `/stores/m002` 等商品列表渲染；2. 点底部购物车栏展开抽屉；3. 观察抽屉底边与购物车栏之间区域 |
| 实际结果 | 抽屉底边距视口底 72px、底栏顶边距视口底 64px，中间 8px 可透视缝；缝隙最上层为 `.cart-popup-mask`（其下是商品列表）；遮罩（z-index 30）压在底栏（20）之上，底栏整体被压暗；1280x800 缝高放大到 26.3px |
| 期望结果 | PRD 点餐内容区：底栏固定、点击展开弹层（`docs/PRD.md`:579、:827）；负责人拍板口径＝抽屉贴视口底端并盖住底栏（方案②） |
| 严重程度 | P1（待确认） |
| 是否阻塞 P0 | 否 |
| 证据 | `test-results/cart-popup-diag/{diag.cjs,stack.cjs,report.json}`（不入库）；红端 `Expected: <= 1 / Received: 72`（`test:` 4352ba1）；绿端 `1 passed`（`fix:` 386a0d0）；负责人 2026-09-09 认可严重程度建议（001=P1），正式生效以测试联调同学确认为准 |
| 来源问题记录 | `docs/testing/问题记录/2026-09-08-商家详情页购物车抽屉未贴底.md` |
| 初步原因 | `.cart-popup` 的 `bottom` 为「底栏 64px + 8px 间距」，抽屉浮在底栏上方而非贴底 |
| 责任人 | 用户端负责人 |
| 修复方案 | `bottom: 0` + 底部内边距 `calc(12px + env(safe-area-inset-bottom))`；底栏/遮罩/z-index 不动 |
| 测试提交 | `4352ba1`（新增 `e2e/store-detail-cart-drawer.spec.ts` + `playwright.config.ts`） |
| 修复提交 | `386a0d0` |
| 回归结果 | 开发侧通过（2026-09-08）：原步骤 E2E 绿；四视口底边间距 0px；`vitest run` 22 files/144 tests 绿；`type-check` 通过。真机安全区未实测 |
| 当前状态 | 待回归 |

### SHOW-QA-002

| 字段 | 内容 |
| --- | --- |
| 发现日期/发现人 | 2026-09-08 / AI 排查副产品（负责人尚未确认优先级） |
| 模块/接口/页面 | 用户端全局页面壳 `frontend/user-h5/src/layouts/MainLayout.vue` `.app-shell` |
| 环境与版本 | 前端 `http://localhost:5173`；视口 390x844 / 430x900 / 1280x800；本机 Chrome；基点 `782ebd2` |
| 前置数据 | 无（页面能渲染即可） |
| 复现步骤 | 1. 桌面视口打开任意用户端页面；2. 读取 `.app-shell` 的 `maxWidth` 计算值与实际宽度 |
| 实际结果 | `max-width: 430px` 被 px→vw 换算为 `110.256vw`：390 视口 = 429.998px、430 视口 = 474.101px、1280 视口 = 1411.28px，居中约束实际失效；`.cart-bar` 同样受影响 |
| 期望结果 | `.app-shell` 注释与 UI 复刻约定：H5 桌面预览时居中约束，验收视口 320–430 |
| 严重程度 | P3（待确认） |
| 是否阻塞 P0 | 否 |
| 证据 | `test-results/cart-popup-diag/report.json`（四视口 `壳.maxWidth`）；计算口径 430/390×100 = 110.256vw；负责人 2026-09-09 认可严重程度建议（002=P3），002 纳入本期需求变更窗口，正式生效以测试联调同学确认为准 |
| 来源问题记录 | `docs/testing/问题记录/2026-09-08-用户端页面壳430px居中失效.md` |
| 初步原因 | px→vw 插件对所有 >1px 的 px 值统一换算，`max-width` 也在其中 |
| 责任人 | 用户端负责人（待指派） |
| 修复方案 | 待定（未选定，勿直接改数值）：让该条 `max-width` 不参与换算或改由媒体查询/运行时约束 |
| 测试提交 | - |
| 修复提交 | - |
| 回归结果 | 待执行 |
| 当前状态 | 新建 |

### BUG-YYYYMMDD-001

| 字段 | 内容 |
| --- | --- |
| 发现日期/发现人 | 待填 |
| 模块/接口/页面 | 待填 |
| 环境与版本 | 后端地址、前端地址、浏览器/设备、提交号 |
| 前置数据 | 账号角色、店铺/商品/订单标识；不记录密码或密钥 |
| 复现步骤 | 1. 待填；2. 待填；3. 待填 |
| 实际结果 | 待填 |
| 期望结果 | 依据 PRD、SRS 或接口契约填写 |
| 严重程度 | P0 / P1 / P2 / P3 |
| 是否阻塞 P0 | 是 / 否 |
| 证据 | 截图、录屏、请求响应、日志路径 |
| 来源问题记录 | `docs/testing/问题记录/YYYY-MM-DD-<主题>.md`（明细与复现证据；文件头注明本表编号） |
| 初步原因 | 待开发负责人确认 |
| 责任人 | 待填 |
| 修复方案 | 待填 |
| 测试提交 | `test:` 提交号，必须早于修复提交 |
| 修复提交 | `fix:` 或其他实际提交号 |
| 回归结果 | 通过 / 失败 / 待执行；填写日期与用例编号 |
| 当前状态 | 新建 / 已确认 / 修复中 / 待回归 / 已关闭 / 待补充 / 延期 / 重复 |

## 4. 每轮发布前清单

- [ ] P0 缺陷为 0，或已由负责人明确记录延期原因和演示规避方案。
- [ ] 所有 `待回归` 缺陷已执行原复现步骤。
- [ ] 相关接口集成用例和 P0 主链路已重跑。
- [ ] 统一响应、权限归属、金额/库存/状态和真实模式回退均复核。
- [ ] `test:` 提交早于 `fix:` 提交，且 Git 提交号已记录。
- [ ] 缺陷结果已同步到当日 `docs/record/daily/YYYY-MM-DD.md`。


### SHOW-QA-003

| 字段 | 内容 |
| --- | --- |
| 发现日期/发现人 | 2026-09-08 / 王馨琪（现场演示发现） |
| 模块/接口/页面 | 商家端登录/注册卡片（`.auth-page`/`.auth-card`） |
| 环境与版本 | 现场演示页 iframe 嵌入 `4001`（overflow:hidden），100% 缩放 |
| 前置数据 | 无 |
| 复现步骤 | 1. 现场演示页打开商家端登录卡片；2. 切到注册（表单高于可视区）；3. 观察「注册并创建店铺」按钮 |
| 实际结果（修复前） | 卡片被 `place-items:center` 强制垂直居中，高于可视区时上下溢出且不可滚动，底部按钮被裁切 |
| 期望结果 | 空间足够时居中；不足时从顶部排布并可在容器内滚动到底部 |
| 严重程度 | P3（展示问题） |
| 是否阻塞 P0 | 否 |
| 证据 | 修复前截图（按钮贴边被裁）；修复后 500px 视口实测 scrollable=true、按钮完整可见（截图留痕） |
| 初步原因 | grid `place-items:center` 的溢出行为 + 嵌入容器 overflow:hidden，min-height 随内容长高时外层裁切 |
| 责任人 | 商家端负责人（王馨琪） |
| 修复方案 | `.auth-page` 改为 `height:100dvh + overflow-y:auto`（内部滚动），卡片从顶部排布；`7e50425` 一轮（flex 防裁）、`7e4e18f` 二轮（兼容 overflow:hidden 的 iframe 嵌入） |
| 测试提交 | 不适用（纯展示缺陷，jsdom 无法断言布局；回归留人工点验） |
| 修复提交 | `7e50425` / `7e4e18f` |
| 回归结果 | 通过（2026-09-08）：500px 视口内部滚动到底后注册按钮完整可见（截图证据） |
| 当前状态 | 已关闭 |