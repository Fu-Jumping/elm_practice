# 接口级联调用例

日期：2026-09-04  
用途：从接口契约生成可执行的接口/集成联调清单。测试数据每轮执行前重置；实际 HTTP 状态、响应体、数据库结果和页面证据填写到联调记录，不以 Mock 结果代替。

## 1. 执行约定

- 服务地址：`http://127.0.0.1:8080/api/v1`；Session Cookie 必须在同一角色链路中持续携带。
- 真实模式：`VITE_API_MODE=real`、`VITE_API_BASE_URL=http://127.0.0.1:8080/api/v1`、`VITE_MOCK_FALLBACK=false`。
- 每条用例记录：请求前置 → 请求方法/路径/体 → HTTP 状态 → JSON `code/message/data` → 数据库影响 → 页面结果 → 清理方式。
- 失败响应必须是 JSON；出现 HTML、静默 Mock 回退、跨用户/跨店数据泄露、半成功事务均判失败。
- 契约尚未固化的具体状态码按“拒绝请求 + 无脏数据 + 可理解提示”验收，SRS 定稿后补精确码。

## 2. 用户端级联用例

### 2.1 账号与会话（IC-ACC）

| 编号 | 前置与操作 | 请求链 | 关键断言 |
| --- | --- | --- | --- |
| IC-ACC-01 | 新手机号注册 | `POST /users` `{account,password,nickname}` | `2xx`、`code=0`；返回用户摘要，不含密码；可登录 |
| IC-ACC-02 | 使用 10 位/12 位/含字母手机号注册 | 分别 `POST /users` | `400` JSON，指出账号格式；数据库不新增 |
| IC-ACC-03 | 使用已注册账号注册 | `POST /users` | `409`；原账号数据不变 |
| IC-ACC-04 | 使用少于 6 位或超过 20 位密码注册 | `POST /users` | `400`；不落库 |
| IC-ACC-05 | 合法用户登录 | `POST /auth/login` `{account,password,role:user}` | `2xx`、`code=0`、`Set-Cookie`；随后 `GET /me` 返回当前用户 |
| IC-ACC-06 | 错误密码、未知账号、role 非 user | `POST /auth/login` | 错误密码/未知账号 `401`；非法字段按契约为 `400`；不泄露账号存在性 |
| IC-ACC-07 | 注销并访问受保护资源 | `POST /auth/logout`；再 `GET /me`、`GET /cart` | 注销成功；后续均 `401` |
| IC-ACC-08 | 用户调用商家接口 | 用户 Session 请求 `GET /merchant/me` | `403`，不返回商家数据 |
| IC-ACC-09 | 修改当前用户资料 | `GET /me` → `PATCH /me` → `GET /me` | 只修改允许字段；返回值与再次查询一致；伪造 `userId` 不改变归属 |

### 2.2 店铺、商品与购物车（IC-BROWSE/IC-CART）

| 编号 | 前置与操作 | 请求链 | 关键断言 |
| --- | --- | --- | --- |
| IC-BROWSE-01 | 查询首页店铺 | `GET /stores` | `code=0`；无结果为 `data: []`；过滤/排序参数生效 |
| IC-BROWSE-02 | 查询 m002 详情、分类、商品 | `GET /stores/m002` → `/stores/m002/categories` → `/stores/m002/products` | 数据均属于 m002；店铺 status 合法；用户端下架商品不可购买 |
| IC-BROWSE-03 | 查询不存在店铺/商品 | `GET /stores/unknown`、`GET /products/unknown` | `404` JSON；页面清空旧详情并展示空态 |
| IC-BROWSE-04 | 合法商品加入购物车并重复加入 | `POST /cart/items` 两次 → `GET /cart?storeId=m002` | 同一用户/店铺/商品只有一行，数量合并；价格以后端商品价为准 |
| IC-CART-01 | 修改数量、删除购物车行 | `PATCH /cart/items/{lineId}` → `DELETE .../{lineId}` → `GET /cart` | 数量和删除结果一致；数量为 0/负数请求 `400` 或按前端转删除 |
| IC-CART-02 | 超库存、下架商品、闭店加购 | 分别 `POST /cart/items` | 请求被拒绝，状态码按 SRS 固化；无新增/超量行；提示含处理建议 |
| IC-CART-03 | 未登录或操作他人行 | 无 Session 请求；用户 A 操作用户 B 的 `cartLineId` | 分别 `401`、`404`；不泄露行内容 |

### 2.3 地址与订单（IC-ADR/IC-ORD）

| 编号 | 前置与操作 | 请求链 | 关键断言 |
| --- | --- | --- | --- |
| IC-ADR-01 | 查询、创建、修改地址 | `GET /me/addresses` → `POST /me/addresses` → `PATCH /me/addresses/{id}` | 只返回当前用户；必填/电话非法为 `400`；默认地址最多一条 |
| IC-ADR-02 | 查询地址详情、删除默认地址和他人地址 | `GET /me/addresses/{id}` → `DELETE /me/addresses/{id}` | 详情只属于当前用户；删除默认后最近更新地址成为默认；无剩余则无默认；他人地址 `404` |
| IC-ORD-01 | 用户 m002 有购物车和地址，创建订单 | `POST /orders` `{storeId,addressId,remark,expectedTotal}` | 成功状态 `PROCESSING`；返回订单、商品/地址/金额快照；实付 = 商品小计 + `packagingFee`（2.00） |
| IC-ORD-02 | 创建后查询订单 | `GET /orders` → `GET /orders/{orderId}` → `GET /orders/{orderId}/items` | 列表倒序；详情与快照一致；购物车已清空 |
| IC-ORD-03 | 地址越权、店铺关闭、商品下架、库存不足、起送不足 | 分别 `POST /orders` | `404` 或 `409`/`400` 按契约/SRS；不生成半成品订单，购物车保持原状 |
| IC-ORD-04 | 前端金额被篡改、商品已改价 | 提交错误 `expectedTotal`；创建前修改商品价 | 以后端计算为准；旧订单金额快照不变 |
| IC-ORD-05 | 查询不存在/他人订单 | `GET /orders/{id}`、`GET /orders/{otherId}` | 均 `404` JSON；不返回订单信息 |
| IC-ORD-06 | 模拟事务失败 | 构造任一明细校验失败后查询订单、库存、购物车 | 订单、库存、购物车无半成功变化 |

## 3. 商家端级联用例

### 3.1 商家会话与店铺（IC-MER）

| 编号 | 前置与操作 | 请求链 | 关键断言 |
| --- | --- | --- | --- |
| IC-MER-01 | 合法商家登录 | `POST /merchant/auth/login` `{account,password,role:merchant}` → `GET /merchant/me` → `GET /merchant/store` | 建立商家 Session；绑定店铺正确；店铺初始注册状态为 `CLOSED` |
| IC-MER-02 | 商家注册 | `POST /merchants` `{account,password,storeName,contactPhone}` | 成功即建店；重复账号 `409`；空名称/非法电话 `400` |
| IC-MER-03 | 商家读取/修改店铺资料和状态 | `GET /merchant/store` → `PATCH /merchant/store` → `PATCH /merchant/store/status` | 仅当前店铺生效；状态仅 `OPEN/CLOSED/TEMPORARILY_CLOSED`；非法值 `400` |
| IC-MER-04 | 未登录或用户 Session 修改店铺 | 请求 `PATCH /merchant/store/status` | 分别 `401`、`403`；数据库不变 |
| IC-MER-05 | 请求体伪造 `storeId` | 商家 A 带商家 B 的 `storeId` 请求管理接口 | 以 Session 归属为准；若目标资源不属于当前店铺返回 `404`；不得改 B |

### 3.2 分类与商品管理（IC-CAT/IC-MPRD）

| 编号 | 前置与操作 | 请求链 | 关键断言 |
| --- | --- | --- | --- |
| IC-CAT-01 | 新增、修改、删除空分类 | `POST /merchant/categories` → `PATCH .../{id}` → `DELETE .../{id}` | 成功；同店重名 `409`；空/超长名称 `400`；不存在 `404` |
| IC-CAT-02 | 删除有商品分类、操作他店分类 | `DELETE /merchant/categories/{id}` | 有商品 `409`；他店资源 `404`；商品不被静默删除 |
| IC-MPRD-01 | 新增、查询、修改商品 | `POST /merchant/products` → `GET .../{id}` → `PATCH .../{id}` | 成功；名称/分类缺失、负价格/库存非整数 `400`；归属不符 `404` |
| IC-MPRD-05 | 查询当前店铺商品列表 | `GET /merchant/products` | 只返回当前店铺商品；支持契约约定筛选；无数据返回空数组 |
| IC-MPRD-02 | 上下架、库存置 0 | `PATCH /merchant/products/{id}/availability` | 成功；用户端同步不可购买/售罄；用户端列表不展示为可购买 |
| IC-MPRD-03 | 删除下架商品/删除在售商品 | 先确保状态下架，再 `DELETE /merchant/products/{id}`；另测在售删除 | 下架可删；在售 `409`；历史订单商品快照仍可查 |
| IC-MPRD-04 | 商家 A 访问商家 B 商品 | A Session 请求 B 的查询/修改/删除 | `404`；B 数据不变 |

## 3.4 P1 扩展接口（P0 全绿后执行）

以下用例属于契约已列出的 P1 接口。若本阶段未选定对应扩展，结果标记为“未启用”，不计入 P0 失败；一旦启用，必须补真实数据和执行证据。

| 编号 | 前置与操作 | 请求链 | 关键断言 |
| --- | --- | --- | --- |
| IC-P1-01 | 待支付订单模拟支付 | `POST /orders/{orderId}/payment` | 仅当前用户自己的待支付订单可支付；成功进入 `PROCESSING`；重复请求幂等；超时支付 `409` |
| IC-P1-02 | 用户查询会话和消息 | `GET /conversations` → `GET /conversations/{id}` | 只返回当前角色且关联当前订单/商家；不存在或越权 `404` |
| IC-P1-03 | 发送和重试消息、标记已读 | `POST /conversations/{id}/messages` → `PATCH /conversations/{id}/read` | 空消息/超长消息 `400`；失败保留输入；已读状态只影响当前角色 |
| IC-P1-04 | 查询店铺评价、提交评价 | `GET /stores/{storeId}/reviews` → `POST /orders/{orderId}/review` | 只有已完成且属于当前用户的订单可评价；同订单重复评价被拒绝；内容/评分边界 `400` |
| IC-P1-05 | 商家查询和回复评价 | `GET /merchant/reviews` → `PATCH /merchant/reviews/{reviewId}/reply` | 只返回本店评价；他店评价 `404`；重复回复更新原回复，不新增记录 |
| IC-P1-06 | 商家查询和保存优惠 | `GET /merchant/promotions` → `PUT /merchant/promotions` | 只作用于当前店铺；非法优惠配置被拒绝；计算结果以后端规则为准 |
| IC-P1-07 | 商家概览与统计 | `GET /merchant/overview` → `GET /merchant/analytics?range=week` | 只返回当前店铺；无真实数据返回 0/空数组；非法范围按契约返回 `400` |
| IC-P1-08 | 商品规格扩展 | `PUT /merchant/products/{productId}/specifications` → 用户查询/加购 | 仅选定规格扩展后启用；不同规格不能错误合并；商品归属不符 `404` |

### 3.3 商家订单（IC-MORD，P0）

| 编号 | 前置与操作 | 请求链 | 关键断言 |
| --- | --- | --- | --- |
| IC-MORD-01 | 用户创建 m002 订单后商家查询列表 | 用户链路 `POST /orders` → 商家 `GET /merchant/orders` | 订单出现在 m002 列表；不出现其他店铺订单；金额含商品合计、打包费、实付 |
| IC-MORD-02 | 商家查看订单详情 | `GET /merchant/orders/{orderId}` | 返回本店订单商品/地址允许展示字段和金额快照；不返回无关用户私人数据 |
| IC-MORD-03 | 商家查询他店订单/不存在订单 | 商家 A 请求 B 的订单和未知订单 | `404`；不得泄露订单存在性或详情 |
| IC-MORD-04 | 用户访问商家订单接口 | 用户 Session 请求 `GET /merchant/orders` | `403` |

## 4. 商家端页面验收场景

执行前提：商家端使用桌面视口（建议宽度不小于 1280px），关闭 Mock 回退，使用固定商家账号登录；每个场景同时核对页面反馈、网络请求和接口响应，不以页面静态展示作为通过依据。

| 编号 | 页面/场景 | 操作步骤 | 关联接口 | 通过标准 |
| --- | --- | --- | --- | --- |
| MA-01 | 商家登录成功 | 打开登录页 → 输入 `merchant-a` 和正确密码 → 点击登录 → 刷新页面 | `POST /merchant/auth/login`、`GET /merchant/me` | 登录成功进入后台；刷新后仍保持会话；显示绑定店铺；密码不出现在页面或请求日志展示中 |
| MA-02 | 商家登录异常与权限拦截 | 输入错误密码；退出后直接访问后台地址；使用用户 Session 访问商家页 | `POST /merchant/auth/login`、`POST /auth/logout`、商家受保护接口 | 错误密码 `401` 并提示；未登录跳登录；用户身份 `403`；不展示旧店铺数据 |
| MA-03 | 店铺设置与营业状态 | 打开店铺设置 → 修改基础资料并保存 → 依次切换 `OPEN`、`CLOSED`、`TEMPORARILY_CLOSED` → 刷新 | `GET/PATCH /merchant/store`、`PATCH /merchant/store/status` | 保存成功后回显最新值；三种状态文案/颜色正确；失败不提前显示成功；状态改变后用户端查询同步 |
| MA-04 | 分类管理正常与异常 | 新增分类 → 修改名称 → 删除空分类；再新增同名分类、删除有商品分类、操作不存在分类 | `GET/POST /merchant/categories`、`PATCH/DELETE /merchant/categories/{categoryId}` | 正常操作列表即时更新；同店重名 `409`；有商品分类阻断删除；不存在资源 `404`；失败不改变列表 |
| MA-05 | 商品管理正常与异常 | 新增商品 → 查询编辑 → 修改价格/库存 → 下架 → 删除；分别提交缺名称、负价格、负库存、在售删除 | `GET/POST/PATCH/DELETE /merchant/products`、`PATCH /merchant/products/{productId}/availability` | 合法数据保存并回显；非法字段 `400` 且不发或不落非法数据；在售商品删除 `409`；下架后允许删除；用户端不可购买下架/售罄商品 |
| MA-06 | 商家订单列表和详情 | 先由用户端创建 m002 订单 → 商家刷新订单列表 → 打开订单详情 | `GET /merchant/orders`、`GET /merchant/orders/{orderId}` | 订单出现在所属店铺；列表和详情金额一致，展示商品合计/打包费/实付；不能查看其他店铺订单；刷新不丢数据 |
| MA-07 | 商家端失败与重试 | 断开后端或制造接口失败 → 执行保存/切换/删除 → 恢复服务 → 点击重试或重新加载 | 对应写接口 | 显示可理解错误；不静默切回 Mock；输入或未保存内容按页面规则保留；恢复后可重试 |

商家端验收结论填写：

| 日期 | 场景编号 | 浏览器/视口 | 页面结果 | 接口结果 | 截图/录屏 | 缺陷编号 | 结论 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 2026-09-08 | MA-01 | Chrome 内核 IAB 1366×900 | 登录进入后台；刷新保持会话；显示绑定店铺肯德基宅急送；密码框不回显明文 | `POST /merchant/auth/login` 200、`GET /merchant/me` 200 | DOM 快照留痕 raw/2026-09-08 | - | 通过 |
| 2026-09-08 | MA-02 | 同上 | 退出后直访 #orders 渲染登录表单；错误密码提示「账号或密码错误」 | 错误密码 HTTP 401 统一 JSON；用户 Session 访问商家接口 403（IC-ACC-08 同批证据） | 同上 | - | 通过 |
| 2026-09-08 | MA-03 | 同上 | 简介修改保存刷新回显 ✓；状态三种选项可选；联系电话保存后不回显 | `PATCH /merchant/store` 持久化 ✓；`PATCH /merchant/store/status` 返回 200 但状态不落库，公开接口始终 OPEN；非法值 400 | 同上 | BUG-20260908-011 / 012 | **不通过（011 修复后回归）** |
| 2026-09-08 | MA-04 | 同上 | 新增/改名/删除空分类即时更新；重名提示「同一店铺分类名称不能重复」；删有商品分类弹确认且阻断 | 重名 409；有商品删除 409；不存在 404（API 证据） | 同上 | - | 通过 |
| 2026-09-08 | MA-05 | 同上 | 空表单必填校验「请输入商品名称」；创建/下架/删除流程 ✓；在售删除提示「在售商品不能删除，请先下架」 | 负价格 400（接口证据）；在售删除 409；下架后删除 200 | 同上 | - | 通过 |
| 2026-09-08 | MA-06 | 同上 + 用户端 5174 | 用户支付后商家列表出现 o1059（¥31.00/进行中）；详情金额一致（29+2=31） | 用户建单支付 API：itemSubtotal 29.00/packagingFee 2.00/total 31.00；他店订单 404（IC-MORD-03 证据） | 同上 | - | 通过 |
| 2026-09-08 | MA-07 | 同上 | 停后端刷新出现「加载失败/请求失败(502)」+ 重新加载按钮，无 Mock 回退；恢复后重试触发新请求，重登后数据完整加载 | 停服时 vite 代理 502；重启清内存会话返回 401「请先登录」属预期 | 同上 | - | 通过 |

## 5. 跨模块兜底与交付判定

| 编号 | 检查点 | 通过条件 |
| --- | --- | --- |
| IC-X-01 | 统一错误响应 | `400/401/403/404/409/500` 均为 JSON，含业务 `code`、`message`、`data:null`；不含密码、SQL、堆栈 |
| IC-X-02 | 真实模式无回退 | `VITE_MOCK_FALLBACK=false` 时后端故障直接显示错误/空态，不出现演示数据 |
| IC-X-03 | 重复提交 | 连续点击加购/创建订单/状态操作，后端不重复写入；订单创建需幂等或状态条件保护 |
| IC-X-04 | 并发冲突 | 商品、店铺状态或订单状态被改后再次提交，返回 `409`，提示刷新；页面重新读取 |
| IC-X-05 | P1 扩展 | P0 全绿后再执行支付超时/重复支付、订单状态跳级/重复推进、库存条件扣减；未启动时不作为 P0 失败 |

## 6. 契约接口覆盖核对表

核对状态只表示“已有用例设计”，不表示接口已经实现或测试通过。执行后应把“已设计”更新为实际结果，并在第 6 节记录证据。

| 契约章节 | 接口范围 | 覆盖用例 | 状态 |
| --- | --- | --- | --- |
| 3.1 | 用户注册、登录、注销、当前用户、资料修改 | IC-ACC-01~09 | 已设计 |
| 3.2 | 店铺列表/详情/分类/商品、商品详情 | IC-BROWSE-01~03 | 已设计 |
| 3.3 | 地址列表/新增/详情/修改/删除 | IC-ADR-01~02 | 已设计 |
| 3.4 | 购物车查询/新增/修改/删除 | IC-BROWSE-04、IC-CART-01~03 | 已设计 |
| 3.5 | 用户订单创建/列表/详情/明细 | IC-ORD-01~06 | 已设计 |
| 4.1 | 商家注册、登录、当前商家、店铺查询/修改/状态 | IC-MER-01~05 | 已设计 |
| 4.2 | 商家分类和商品全量管理接口 | IC-CAT-01~02、IC-MPRD-01~05 | 已设计 |
| 5.1 | 商家订单列表/详情（P0） | IC-MORD-01~04 | 已设计 |
| 5.2、6 | 状态推进、支付、消息、评价、优惠、概览统计、规格（P1） | IC-P1-01~08 | 已设计/待选定 |
| 7、8 | 事务、并发、幂等、统一响应、真实模式 | IC-X-01~05、IC-ORD-06 | 已设计 |

## 7. 执行记录模板

| 日期 | 用例 | 环境/版本 | 实际请求与响应摘要 | 数据库核对 | 页面证据 | 结果 | 缺陷编号 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 待执行 | 待填 | 待填 | 待填 | 待填 | 待填 | 未执行 | - |
