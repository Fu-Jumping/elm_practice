# 2026-09-14 商家端二阶段验收接口异常（分类详情 500、消息发送 500）

> **台账编号**：`BUG-20260914-001`（分类详情 GET 500）、`BUG-20260914-002`（消息发送 500）
> 台账入口：`docs/testing/缺陷跟踪表.md` §2 / §3。本文件由测试联调同学（王馨琪）在 9/14 商家端总回归时落盘。

## 问题 1：分类详情查询接口返回 500

```text
编号：BUG-20260914-001
页面 / 接口：GET /api/v1/merchant/categories/{categoryId}
路由：商家端 分类管理页（编辑/删除路径）
视口 / 环境（后端地址、前端地址、浏览器或设备、提交号）：
  后端 http://127.0.0.1:4000（线上 jar，main@707f234 发布 20260913-707f234）
  前端 http://82.157.137.114:4001（构建产物同发布版本）
  提交号：main fe0cf53 对应发布；乙方仓库检出 test/merchant-total-regression-0914
前置数据（账号角色、店铺/商品/订单标识；不记录密码或密钥）：
  商家 merchant-a（店铺 m002）；分类 c102「招牌汉堡」、c101
操作步骤：
  1. 以商家 Session 调用 GET /api/v1/merchant/categories/c102；
  2. 换成已存在其他分类 c101 复测；
  3. 换成不存在的 c99999 复测。
实际结果：
  1. 三种输入均返回 HTTP 500 + {"code":50000,"message":"服务暂时不可用，请稍后重试"}；
  2. 对照：GET /api/v1/merchant/products/{id}/specifications 对未实现路径返回 404「接口不存在」，
     说明本接口不是"未实现"，而是已映射但执行异常。
期望结果（依据 PRD / SRS / 接口契约，尽量给出行号）：
  按契约 §4.2，分类资源查询应对合法 id 返回 200 分类对象；不存在资源返回 404；
  未实现/未映射路径返回 404「接口不存在」。任何情况下不得返回无上下文的 500。
证据（截图、录屏、请求响应摘要、日志位置）：
  curl 响应摘要见 raw/2026-09-14/王馨琪.md；本接口三次响应均为 code=50000。
严重程度：P2
是否阻塞 P0：否（前端仅对该路径使用 PATCH/DELETE，未调用 GET，用户不可见；但接口异常已确认）
发现日期 / 发现人：2026-09-14 / 王馨琪（AI 辅助探测，人工确认）
责任人：后端负责人（王嘉悦 / 林晨，按 4.2 归属确认）
状态：已确认
```

## 问题 2：会话发送消息接口返回 500

```text
编号：BUG-20260914-002
页面 / 接口：POST /api/v1/conversations/{conversationId}/messages
路由：商家端 消息页（聊天详情发送消息）
视口 / 环境（后端地址、前端地址、浏览器或设备、提交号）：
  同上（线上 jar 与 4001 构建产物）
前置数据（账号角色、店铺/商品/订单标识；不记录密码或密钥）：
  商家 merchant-a；会话 cv1008（关联订单 o1007）
操作步骤：
  1. 以商家 Session 取会话列表 GET /conversations（返回 17 条）；
  2. 对首条会话 POST /api/v1/conversations/cv1008/messages，body {"content":"MER-004验收：您好，您的订单正在准备中"}；
  3. 对照：PATCH /api/v1/conversations/cv1008/read 标记已读。
实际结果：
  1. 发送消息返回 HTTP 500 + {"code":50000,"message":"服务暂时不可用，请稍后重试"}；
  2. 同一会话的标记已读 PATCH 返回 200 且 merchantRead 置 true（读侧与已读侧正常）；
  3. 会话列表与详情（GET /conversations、GET /conversations/{id}）均 200，字段含 orderId/merchantRead/messages。
期望结果（依据 PRD / SRS / 接口契约，尽量给出行号）：
  按契约 §6.1 与验收用例 §3 第 9 条：正常应发送成功并使对方会话出现消息；
  异常路径应为空消息/超长消息 400、非本人会话 404，不得返回无上下文的 500。
证据（截图、录屏、请求响应摘要、日志位置）：
  请求响应摘要见 raw/2026-09-14/王馨琪.md；商家端消息页实测可渲染真实会话列表。
严重程度：P2（模块本身仍在「本期实现（待开发）」批次④范围内，非 P0 主链路）
是否阻塞 P0：否
发现日期 / 发现人：2026-09-14 / 王馨琪（AI 辅助探测，人工确认）
责任人：后端负责人（批次④ 消息与通知，TODO-BE-006）
状态：已确认
```

## 关联口径说明

- 本文两条均为 9/14 商家端总回归时的真实链路探测结果，未修改任何业务代码。
- 统计口径（`avgOrderAmount` / `trend` / `channelDistribution` / `statusDistribution`）缺字段属契约已标注的
  「本期实现（待开发）」（后端批次⑨ `TODO-BE-011`），不另立缺陷，仅在 `docs/todo/商家端.md` 的
  `TODO-MER-007` 行与 raw 片段记录。
