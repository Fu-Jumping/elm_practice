# 三端部署工程检查问题单（AI 代拟）

> 发现日期：2026-09-08；发现人：当前 AI 工程检查。正式编号、严重程度和状态尚待王馨琪确认。没有 commit / push，不能写为正式关闭。报告详见 `../../presentation/engineering-audit.md`。

## 待编号 A：店铺营业状态未落库

- 环境：main 508889e + 恢复后的商家端，服务器 4001 / 后端 4000，真实 MySQL。
- 前置：登录绑定店铺的商家，读取店铺原状态。
- 复现：PATCH `/api/v1/merchant/store/status` 为 CLOSED；随后从 StoreMapper 查询相同行，并检查用户公开店铺列表。最初真实接口尝试为新建店 m1084 设置 OPEN，但用户列表仍找不到；专门红测反向将 OPEN 改为 CLOSED。
- 实际：HTTP 200，Controller 返回对象已变，但数据库仍为原值；红测期望 CLOSED、实际 OPEN。
- 期望：状态落库且下一次请求可见；CLOSED 店不进入公开列表。依据后端店铺接口与营业状态规则。
- 建议严重程度：P1（核心状态结果错误，待确认）；是否阻塞：影响商家开关店与跨端演示。
- 原因：仅 `st.status = value`，MyBatis 查询对象不会自动脏检查持久化。
- 工作区修复：StoreMapper.updateStatus + StoreService.updateStatus（事务）+ MerchantController 调用服务。
- 红绿：StoreStatusIntegrationTest 2 项中 1 项失败 → 后端全量 25 passed；真实 API 开关店与公开列表断言通过。
- 证据：服务器备份目录 `status-red.log` / `backend-green.log`；公开摘要 `docs/presentation/evidence/api-smoke.json`。
- 测试/修复提交：均未提交；责任人建议后端 A/B 共同复核；建议当前状态：待负责人复核开发侧结果。

## 待编号 B：商家部署代码和 main 漂移

- main 商家端为静态骨架，部署目录有未提交业务源码；直接覆盖会丢掉唯一完整副本。
- 先备份旧源码、PM2 与数据库，恢复服务器商家源码到工作分支并修正真实契约。
- 新生产构建服务于 4001；源码与运行产物进入指定 release。恢复不构成个人作者证明。
- 建议 P1，代码归属与 Git 证据待补齐；本轮未提交，正式状态待确认。

## 待编号 C：商家字段、会话与库存请求

- 分类排序使用错误键、订单快照适配不完整、店铺编辑包含后端不支持字段、上下架携带旧库存；另有注册成功后无登录会话的问题。
- 修复 sortOrder、订单金额/地址/明细归一化；删除不受支持的店铺电话编辑；上下架只发送 onSale；注册后显式登录。
- 用户与商家共用 JSESSIONID 导致双窗口登录覆盖：Nginx 按 `/user/api/` 与 `/api/` 分路径设置 Cookie。
- 开发验证：商家 10 测试与构建通过；真实 API 同 CookieJar 身份隔离、下单扣库存后切上下架不覆盖库存通过；浏览器执行证据见报告。
- 建议 P1，正式编号/状态待确认；未伪造测试提交。main 原先缺失 merchantApi 模块导致首轮 suite 导入失败，与业务断言失败作区分。

## 留存事项

既有 SHOW-QA-002 桌面壳仍未修复；计价展示文案与配送费口径需要继续统一；P1 并发/超时专项、真机投屏和人工交叉验收在后续计划中。原 SHOW-QA-001/002 的负责人状态未被改写。
