# 回归/联调执行记录（2026-09-08）

执行人：测试联调同学（王馨琪），AI 辅助执行浏览器与接口测试，结果待人工复核  
依据：`docs/testing/测试计划.md`、`docs/testing/接口级联调用例.md`、`测试文档工作说明.md`  
执行环境：前端 http://82.157.137.114:5174（vite dev，pm2 `elm-h5`）；后端 `http://127.0.0.1:4000`（elm-practice-backend-0.1.0-SNAPSHOT，pm2 `elm-backend`）；提交 3dbf1e0（部署副本带未提交改动）；MySQL 真实库

## 1. 结论速览

- **真实模式下浏览器 P0 全链路：未通过（阻塞）**。登录即被 CORS 拦截（BUG-20260908-002），登录后的所有页面场景无法执行。
- **接口级（后端直连）用户端 P0 级联：通过**。账号、店铺浏览、购物车、地址、下单、订单查询、异常语义、权限隔离全部符合契约。
- **接口级商家端 P0 级联：大体通过**，发现 2 个字段名不一致缺陷（注册 `contactPhone/phone`、分类 `sort/sortOrder`）与 1 个待澄清口径（待支付订单商家不可见）。
- **部署与环境发现 3 项问题**（BUG-001/006/007），其中部署 mock 模式已临时修复为 real 模式。
- 新增缺陷 7 个（1 个 P0 待回归、2 个 P0 已确认、3 个 P1、1 个 P2），详见 `缺陷跟踪表.md`。

## 2. 环境变更留痕（本次执行中产生）

1. 发现部署运行在 mock 模式（BUG-20260908-001）。按 `.env.local` 中的部署意图，新建 `frontend/user-h5/.env.development.local`（real + `/api/v1` + 无回退；该文件不入库），`pm2 restart elm-h5` 后注入值已验证为 real。
2. 如需临时恢复 mock 演示：删除该文件并 `pm2 restart elm-h5`。
3. 未修改任何源代码；CORS、字段名等代码缺陷待负责人按 `test:` → `fix:` 流程修复。

## 3. 接口级联用例执行结果（后端直连 `127.0.0.1:4000`）

### 3.1 用户端（IC-ACC / IC-BROWSE / 购物车 / 地址 / 订单）

| 用例 | 结果 | 证据摘要 |
| --- | --- | --- |
| IC-ACC-01 登录 | 通过 | `POST /auth/login` 200，返回 u001；`GET /me` 200（Session 有效） |
| 登录异常（错误密码） | 通过 | HTTP 401 + `{"code":40101,"message":"账号或密码错误"}` |
| 未登录访问受保护接口 | 通过 | `GET /orders`、`POST /cart/items` 均 HTTP 401 + 统一 JSON |
| IC-BROWSE-01/02 店铺列表/详情 | 通过 | 5 家店铺；m002 详情字段完整（startPrice 20.00 / deliveryFee 3.00 / OPEN） |
| m002 分类/商品 | 通过 | 1 个分类；5 个在售商品，库存为真实数值 |
| 购物车添加 | 通过 | 2×p101（58.00）+1×p104（8.00），小计与单价一致 |
| 购物车列表 | 通过 | `GET /cart?storeId=m002` 返回 2 行；注：必带 `storeId`，缺省报 40001（与前端调用方式一致） |
| 地址列表/新增 | 通过 | 新增 da1006（region+detail 分字段，契约 §3.3） |
| 创建订单 | 通过 | o1007：商品合计 66.00 + 打包费 2.00 = 实付 68.00（服务端计算正确）；时间 `2026-09-08 06:26:08` 东八区格式 |
| 订单列表/详情 | 通过 | 列表含 o1007 且按时间倒序；详情含商品行快照 |
| 跨用户隔离 | 通过 | 用户 B 读 A 订单 `404 订单不存在`；B 的订单列表为空 |
| 用户访问商家接口（IC-ACC-08） | 通过 | HTTP 403 `{"code":40301,"message":"当前会话角色无权访问该资源"}` |
| 加购边界 | 通过 | quantity=0 → 400；quantity=999 → 409 `商品库存不足`（details 精确提示「库存最多为 18」，已扣除购物车占用）；不存在商品 → 404；跨店商品 → 404 |

### 3.2 商家端（IC-MER / IC-CAT / IC-MPRD / IC-MORD）

| 用例 | 结果 | 证据摘要 |
| --- | --- | --- |
| 商家登录（`/auth/login` 带 role=merchant） | 通过（拦截正确） | 返回 40301「用户登录 role 必须为 user」，商家必须走 `/merchant/auth/login`（与契约一致） |
| IC-MER-01 商家登录/me/store | 通过 | `/merchant/auth/login` 200；绑定店铺 m002 正确 |
| IC-MER-03 店铺资料修改 | 通过 | PATCH 200，回显一致 |
| IC-MER-03 营业状态切换 | 通过 | OPEN/CLOSED/TEMPORARILY_CLOSED 均 200；非法 `PAUSED` → 400「status 必须为 OPEN、CLOSED 或 TEMPORARILY_CLOSED」 |
| IC-CAT 分类新增/重名/改名/删除 | 通过 | 新增 200；同店重名 409；改名 200；删有商品分类 409「该分类下存在商品」；删空分类 200 |
| IC-MPRD 商品新增/改价改库存/删除 | 通过 | 新增 200；PATCH 200；在售删除 409「在售商品不能删除，请先下架」；下架后删除 200；负价格 400「price必须为非负金额」 |
| IC-MER-02 商家注册 | **失败**（BUG-20260908-003） | 按契约 `contactPhone` → 400；按实现 `phone` → 200 并建店；重复账号 409；空店名 400（后两种在 `phone` 字段下验证通过） |
| IC-MER-05/IC-MPRD-04/IC-MORD-03 越权 | 通过 | 商家 B 读 A 的订单/商品均 404 且文案不泄露存在性；伪造 storeId 以 Session 归属为准 |
| IC-MORD-01 商家订单列表 | **不通过**（BUG-20260908-005，待澄清） | 用户新建 PENDING_PAYMENT 订单不出现在商家列表；详情 404（实现有意过滤，注释引契约 §5/PRD 7.6） |
| 分类排序字段 | **失败**（BUG-20260908-004） | 前端 `sort` vs 后端 `sortOrder`，排序值被丢弃 |

## 4. 浏览器端执行结果（http://82.157.137.114:5174/，视口 390×844）

| 场景 | 结果 | 说明 |
| --- | --- | --- |
| 未登录点击「订单」 | 通过 | 正确跳转 `/login?redirect=/orders`，带回跳地址 |
| mock 模式登录/错误提示 | 通过（但属 mock 行为） | 错误密码提示「账号或密码错误」；正确密码登录后跳转 /orders |
| mock 模式刷新保持会话 | **不通过** | 刷新后被踢回登录页（BUG-20260908-001 的表现之一，根因为 mock 会话存内存 + 部署模式错误） |
| real 模式登录 | **不通过** | 页面提示「请求失败（HTTP 403）」；浏览器 `POST /auth/login` 被后端 CORS 拦截（BUG-20260908-002），P0 全链路在登录处阻塞 |
| real 模式游客浏览 | 通过 | 首页店铺列表、店铺详情可正常加载（GET 不受 CORS 影响） |

## 5. 测试数据留痕

- 用户：u1009（13900001111 / test1234）
- 商家/店铺：ma1012 / m1013（merchant-qaB / qa123456，店名 QA-test-B，初始 CLOSED）
- 订单：o1007（u001，m002，PENDING_PAYMENT，实付 68.00）；地址 da1006；购物车行 cl1004/cl1005
- QA 分类/商品（c1010、p1011）已在用例内删除，无残留脏数据

## 6. 待执行清单（下一步）

1. 修复 BUG-20260908-002（CORS）后，重跑 real 模式浏览器 P0 全链路（登录 → 浏览 → 加购 → 地址 → 下单 → 查单），并顺带完成 BUG-20260908-008 的最终浏览器回归（登录后不白屏）。
2. 修复 BUG-20260908-003/004 后重跑商家注册与分类排序用例。
3. SRS 定稿后回填 BUG-20260908-005 口径，重跑 IC-MORD-01/MA-06。
4. user-h5 源码入库（BUG-20260908-009），之后同步部署目录（BUG-20260908-006）并执行商家端页面验收 MA-01~MA-07 回填验收结论表。
5. 未覆盖：IC-X-03 重复提交幂等、IC-X-04 并发冲突、IC-X-02 真实模式无回退的完整回归（本次已在 real 模式下部分验证无静默回退）。

## 7. 补充调查：登录后白屏问题（2026-09-08 追加）

针对成员反馈的「登录之后是白板，刷新出来才是主页」，调查结论：**该症状历史上发生过两次，根因不同，当前部署代码均已修复，登记为 BUG-20260908-008（待回归）**。

| # | 时间 | 根因 | 修复 | 现状核查 |
| --- | --- | --- | --- | --- |
| ① | 9/7 | 真实后端 `GET /orders` 扁平字段（itemSubtotal/packagingFee/total），前端按嵌套 `amounts.payableAmount` 渲染 → OrderListView TypeError → 白屏（留痕：`docs/record/raw/2026-09-07/用户端-1418.md`） | 真实形状归一化（`normalizers.ts`：itemSubtotal/total → itemsTotal/payableAmount）+ 真实形状 fixture 回归锁 | `normalizers.spec.ts` 在位；全量测试 19 文件 **120/120 通过** |
| ② | 9/8（部署时） | App 级 `<Transition mode="out-in">` 在登录页(BlankLayout)→主页(MainLayout) 懒加载切换时丢失 enter 阶段 → 白屏 | 去掉 `mode` 保留 opacity 过渡（App.vue 注释留痕） | App.vue 已无 mode 属性 ✓ |

- 「刷新出来才是主页」与 ② 吻合（整页重载绕过过渡竞态）；「白板不跳转」与 ① 吻合。
- **最终浏览器回归被 BUG-20260908-002（CORS）阻塞**：真实模式登录无法完成，登录后渲染待 CORS 修复后一并回归。
- 调查中新发现 **BUG-20260908-009**：user-h5 全部源码（含 120 个单测与原始留痕）不在任何 Git 仓库——主仓库 `git ls-tree` 无 user-h5 文件，部署目录非 git 仓库，唯一副本在 `/home/ubuntu/workspace/elm_practice`。已按 P1 登记。
