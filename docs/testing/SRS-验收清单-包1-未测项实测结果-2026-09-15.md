# SRS 验收清单·包① 未测项实测结果（2026-09-15）

> 对照物：`docs/testing/SRS-验收清单-包1-用户端交易链路.html`（79 条，其中 40 条已由负责人测过，本轮补测剩余 **39 条**）
> 实测对象：线上用户端 `http://82.157.137.114:4001/user/`（构建 `index-CRJEM2CO.js`，服务器 `Last-Modified: 2026-09-15 08:56:52 GMT`）
> 执行方式：iframe 无关的真实浏览器走查（390×844 视口）+ 接口级取证（curl + 会话 Cookie）
> 口径提醒：本轮**不改**清单文件与清单记录，结论见本文；清单勾选仍由持单人在其浏览器内维护（清单进度存 `localStorage: srs-p1-checklist-v1`）

## 0. 两条影响判读的环境事实（先看）

1. **线上构建已比清单预检备注新**：清单里三条「线上未发布」的预检备注已过期——服务器 2026-09-15 16:56 重新发布过，27 个分包齐全，实测已含
   - 商品规格弹层与会员价（清单 532/534 备注称"线上未发布"→ **已发布**，本轮按已发布实测）；
   - 订单详情商品行缩略图（`od-dish-thumb`，TODO-USER-124）→ **已发布**（清单 571 备注"线上商品行仍是空占位无图"→ 过期）。
   仍未发布的是本仓 main 上的 **SHOW-QA-007（店铺状态英文枚举）** 修复。
2. **演示账号 `13800000001` 正被并发使用（已取证，非推测）**：① 该账号购物车里**出现了我没有加入的商品**（黄金鸡块 ×2）——我读取到的购物车在两次调用之间由「仅劲脆鸡腿堡 ×2」变为含 3 行；② **订单数对账**：做 824 测试时（17:47 前后）该账号订单数为 **51**（已含我建的 `o1508`），到 18:1x 复查为 **54**，其间我只建了 `o1515`（17:48），多出的 **`o1525`（17:50:37, ¥79.35）与 `o1529`（17:53:24, ¥32.65）不是我创建的**；③ 店内库存亦在两次读取之间被他人订单扣减（p101 −2 / p104 −2）。
   → 凡依赖购物车内容或库存逐笔对账的判读都受此影响，本文相关条目已单独标注；建议验收窗口内固定一人使用演示账号。
   （本轮后半程改用**新建的独立账号** `13900000002 / test123456` 完成确认订单→支付→订单→地址链路，避免污染。）

## 1. 结论总览（39 条）

| 结论 | 条数 | 说明 |
| --- | --- | --- |
| 通过 | 25 | §8×3、§5×3（含 `537`，负责人 2026-09-15 裁定）、§6.2.1×18、§8.3×1 |
| 部分通过 | 11 | 有明确缺口或存在未覆盖子项（子项逐条写在 §4/§5 证据列） |
| 不通过 | 2 | `b070` 备注弹层未实现、`b840` 订单列表顶栏缺消息图标与未读数 |
| 无法验证（环境/真机限制） | 1 | `825` 访问形态需真机 |

> `537`（FR-SKU-01-4）原判「部分通过」，**2026-09-15 经负责人裁定改为通过**：数量下限无提示接受现状；超库存/下架属缺测试数据，不作为产品缺口。
> 其余子项缺口（`554` 保存中未观察到、`831` 重复支付与超时不回补未构造）仍计入「部分通过」，不单列「未验证」。

**需要处理的三条硬缺口**（已登记 TODO-USER-126/127/128）：

| 编号 | 条目 | 问题 | 建议等级 |
| --- | --- | --- | --- |
| b070 | 订单备注弹层 | PRD 884 行与设计真源 `docs/design/exports/用户端/06-订单/05-订单备注/` 要求**弹层 + 快捷标签（不要辣/多点辣/不吃香菜/多点葱/放门口/米饭多点）**；实现在确认订单页做成**内联文本框**，无弹层无快捷标签（已对**全部 27 个部署分包**搜索确认，见 §4 证据列），且该缺口**未登记**在 `docs/todo/用户端.md` 与 `docs/project/未完成模块与如实标注清单.md`（代码注释写了"本期内联输入"，属已知但未落账） | P2 |
| b060 | 确认订单页-底部结算栏 | 结算栏金额**不是后端试算**：实测确认页「预估实付 ¥46.00」→ 下单后后端实付 **¥38.00**（差 **¥8 = 新客立减 ¥3 + 配送费优惠 ¥5**）；另一单（老账号，无新客立减）「预估 ¥64.00」→ 实付 **¥53.00**（差 **¥11 = 会员价 ¥6 + 配送费优惠 ¥5**）。**契约无试算接口**（`docs/backend/后端接口契约.md` 全文无"试算"），前端注释也写明"用户端没有满减/新客/免配送费查询接口，故标注为预估"——属**契约缺口**，需 R7 决定补接口还是改 SRS 口径 | P2（口径） |
| b840 | 订单列表页-顶部栏 | PRD/SRS 要求顶部栏含**消息图标与未读数**（"未读数来自消息接口或默认为 0"）；实测顶部栏只有标题「我的订单」，**无消息图标、无未读数**；同时设计真源 `06-订单/01-订单列表` 的顶部栏（定位 + 品牌 + 搜索图标）也未实现 | P3 |

**另有两条低等级观察**（未登记，供判断是否值得改）：

- 规格弹层数量减到下限 1 后**继续点「减少数量」不置灰也不给原因**（SRS FR-SKU-01-4 写"数量小于 1 …时提示"；设计系统"不可用操作置灰且点击给出原因"口径）。
- 新增地址页「设为默认地址」开关在**用户当前无任何地址时仍为关闭**（SRS b350 要求"默认开关按当前是否存在默认地址决定"）；后端会把首个地址自动置为默认，功能结果正确，仅开关初值不符。

## 2. §8 章节（P0 主链路与通过门槛）

| 条目 | 结论 | 证据 |
| --- | --- | --- |
| 8.1-异常1 `819` 越权访问按「内容不存在」处理 | **通过** | 新建账号 `13900000002`（=用户 u1512）持有效会话访问他人数据：`GET /orders/o1508` → **404** `{"code":40401,"message":"订单不存在"}`；`GET /me/addresses/da001` → **404** `地址不存在`；`DELETE /me/addresses/da001` → **404**（无副作用）。无会话访问同一接口 → **401** `请先登录`。u2 自己的订单列表为 `[]` |
| 8.2-3 `823` 异常场景有操作记录 | **通过** | 本轮异常条目均留接口级操作记录（非截图）：闭店下单 **409** `店铺当前未营业`；空购物车下单 **400** `购物车为空`；越权 **404**（见上）；手机号格式前端拦截 `手机号格式错误`；必填拦截 4 条（联系人/手机号/所在地区/详细地址不能为空）；支付失败后 `status=PENDING_PAYMENT`、`paidAt=null` |
| 8.2-4 `824` 无半笔订单、越权不可见 | **通过** | 闭店 m004 下单被拒前后 `GET /orders` 条数均为 **51**（不产生半笔订单）；越权请求 404 且无数据泄露（见 819）。另一佐证：`POST /orders` 请求体校验失败/业务拒绝时均不留订单 |
| 8.2-5 `825` 访问形态（用户端） | **无法验证（需真机）** | 本环境只能做到：以 **390×844** 移动视口实载全部页面且链路可用；线上入口为公网 `82.157.137.114:4001`（非"局域网"形态）。**需持单人用手机访问一次**方可判定 |
| `823` 补充限制 | — | 演示账号并发使用导致库存/购物车无法逐笔对账（见 §0 第 2 条） |

## 3. §5.3 / §5.5 / §5.6 / §5.7 零散条目

| 条目 | 结论 | 证据 |
| --- | --- | --- |
| `537` FR-SKU-01-4 异常：必选/数量/库存/下架 | **通过**（负责人 2026-09-15 裁定） | ✓ 必选规格未选 → 弹层「加入购物车」**disabled**；✓ 连点两次「加入购物车」购物车只 +1（2×大份 ¥40 → 加标准 ×1 → 3 件 ¥57，非 +2）；✓ 数量下限停在 1。**两条原拟记为缺口的子项经负责人裁定不再计为问题**：① 数量到下限时按钮不置灰、不提示原因——接受现状；② 超库存/下架未取到样本（线上仅 m1053 `p1126` 库存 4；全量 14 个商品 `onSale` 均为 true）——属**缺测试数据**而非产品缺口，不作为待处理项 |
| `554` FR-ADR-01-4 异常：校验/保存中/越权 | **部分通过** | ✓ 空表单提交 → 4 条必填提示且停留本页；✓ 手机号 `123` → `手机号格式错误`；✓ 越权读写他人地址 404；△「保存中」防重复提交未观察到提示文案（保存很快返回） |
| `565` FR-PAY-01-1 支付页与倒计时 | **通过** | 支付页显示订单号 `o1508`、商品清单、金额明细、收货地址；倒计时实测 **14:46 → 14:42 真走秒**；订单列表对待支付超时单显示「已失效」且「去支付」按钮 **disabled**（不得支付） |
| `572` FR-ORD-02-2 订单留档 | **通过** | 下单后用自家账号把该单快照的收货地址由 `诚园8B 101` 改为 `留档验证已改 NEW-999`，回看旧订单 `o1523` 地址快照仍为 **`诚园8B 101`**，金额 `total=38.00` 不变 → 订单按下单当时快照留档 |

## 4. §6.2.1 原型说明·交易链路 28 行

> 每行按 SRS 三列原文（字段与数据来源 / 点击与状态变化 / 空态·加载·错误·检查）核验；「未单独点验」的子项已逐条写明。

### 商家详情与规格

| 条目 | 结论 | 证据 |
| --- | --- | --- |
| `b780` 商家详情页-顶部栏 | **部分通过** | ✓ 页面进入先校验 storeId 再请求详情；✓ 详情成功后 `document.title` = `肯德基宅急送 · 轻量外卖`；✓ 点返回回上一页。**口径差异**：SRS 字段列写"返回、**分享/更多**为页面控件"、点击列写"分享/更多未入选时只显示'暂未开放'"，而实现为**返回 + 搜索**（点搜索提示"暂未开放"）——**已核对设计真源**：`docs/design/exports/用户端/04-商家详情/01-商家详情-店铺主页/code.html` 顶栏图标为 `arrow_back_ios` + `search`，**全文 `share`/`more_vert` 命中数 = 0** → 实现与设计真源一致，属 **SRS 文案与设计真源不一致**，需 R7 澄清措辞（不要按 SRS 字面去补"分享/更多"） |
| `b790` 商家详情页-商家信息横幅 | **通过** | 横幅字段全部来自详情接口：店名/`4.8`/`月售3500+`/`约25分钟`/`起送 ¥20.00`/`配送 ¥5.00`/促销标签「领券」；点餐/评价为页面临时选项且默认点餐；**点「领券」提示「暂未开放」且不调领取接口**（无网络请求、无优惠状态变化）；营业状态标签仅在非 OPEN 时渲染（OPEN 店不显示，符合设计稿） |
| `b800` 点餐内容区 | **通过** | 左侧分类栏（主食/小食/饮品）、右侧商品列表（名/描述/价格/会员价/规格入口）、底部购物车栏（件数/合计/去结算）；加购后底栏即时刷新（1 件 ¥19.50 → 2 件 ¥39.00）；金额两位小数。**未单独点验**：点击分类切换与滚动联动（本轮未点验，既有用例 T13/T68/T70 覆盖） |
| `b830` 商品规格弹层 | **通过** | 有规格商品显示「可选规格 N 项」入口，点加号打开弹层；弹层内容：商品名、规格（必选）、数量（默认 1）、合计、加入购物车；**必选未选时按钮 disabled**；选「大份 +¥3.00」→ 合计 ¥17.00→¥20.00，数量 ×2 → **¥40.00**（价格由后端单价+价差算，页面只展示）；加入成功后弹层关闭、购物车刷新、轻提示「已加入购物车」；连点两次只生效一次。**未点验**：网络失败保留选择、遮罩/关闭放弃输入（弹层有独立关闭按钮） |

### 购物车（§5.4 四条已由持单人测过）与确认订单

| 条目 | 结论 | 证据 |
| --- | --- | --- |
| `b040` 确认订单页-顶部栏 | **通过** | 标题固定「确认订单」；路由携带 `storeId=m002`；点返回回上一页且**不创建订单**（返回后订单数不变）；重新进入重新读取购物车与地址 |
| `b050` 确认订单页-订单内容区 | **部分通过** | ✓ 商家、商品、数量、单价来自接口；✓ 默认选中默认地址（`诚园7A / 戴（女） 13800000001`）；✓ 送达时间默认「尽快送达」；✓ 商品行按规格分行（大份 ×2 @¥20.00 与 标准 ×1 @¥17.00 各一行）；✓ **无地址时不显示假地址**，改为「请先添加收货地址 › 去添加」引导（FR-ADR 口径）；△ SRS 写"优惠入口只有选定优惠扩展后显示"，线上恒定显示「红包 N 张可用」（红包为本期实现，属预期，但措辞需对齐） |
| `b060` 确认订单页-底部结算栏 | **部分通过** | ✓ 无地址时「去支付」**disabled**，补齐地址后才可用；✓ 请求中按钮显示 **「提交中」并禁用**；✓ 连点两次「去支付」只发出 **1 次 `POST /api/v1/orders`**，只生成一个订单；✓ 成功前不清空购物车。✗ **合计金额不是后端试算**：确认页「预估实付 ¥46.00」→ 后端实付 **¥38.00**（差 ¥8 = 新客立减 ¥3 + 配送费优惠 ¥5）；另一单「预估 ¥64.00」→ 实付 **¥53.00**（差 ¥11 = 会员价 ¥6 + 配送费优惠 ¥5）。契约无试算接口（见 §1 硬缺口表） |
| `b070` 订单备注弹层-备注弹层 | **不通过** | 线上确认订单页为**内联文本框**（`maxlength=50`），**无快捷标签、无弹层**。**证据覆盖全量部署包**：在浏览器内同源抓取全部 **27 个分包**逐一搜索 `快捷标签/不要辣/不吃香菜/多点葱/放门口/米饭多点`，仅 `ConfirmOrderView` 命中「订单备注」文案、其余关键词 **0 命中**。PRD 884 行与设计真源 `06-订单/05-订单备注`（弹层 + 6 个快捷标签 + 自定义备注计数）均要求弹层；代码注释自述"本期内联输入"，但**未登记**在 TODO 台账与如实标注清单（已核对：`未完成模块与如实标注清单.md` 无"确认订单/备注"条目；`docs/todo/用户端.md` 仅在 §4 设计保真自检说明里提到"备注输入"四字，属描述现状而非登记缺口） |

### 订单列表与详情

| 条目 | 结论 | 证据 |
| --- | --- | --- |
| `b840` 订单列表页-顶部栏 | **不通过** | 标题固定「我的订单」✓；但 SRS/PRD 要求的**消息图标与未读数未实现**（页面顶部无任何按钮，`OrderListView.vue` 头部仅 `<span class="ol-title">我的订单</span>`）；设计真源顶栏的定位与搜索图标亦未实现 |
| `b850` 状态筛选与订单列表 | **通过** | 筛选为 全部/待支付/进行中/已完成/待评价/已取消，默认「全部」选中；卡片字段（商家名/状态/下单时间/实付）来自接口；待支付超时单显示「已失效」且去支付禁用；已完成单显示「再来一单」；点卡片进详情；「待支付」筛选下为空态时不残留旧列表（未单独点验空态截图） |
| `b860` 订单列表页-底部导航 | **通过** | 底部导航固定四项 首页/消息/订单/我的；在 `/user/orders` 时「订单」项带 `tab-item--active` 高亮；其余项无激活态 |
| `b870` 订单详情页-顶部栏 | **通过** | 订单号来自路由（`/user/orders/o1508`）；标题固定「订单详情」；顶部为返回箭头 + 标题，**无关闭按钮**；点返回回订单列表；页面提供「刷新」 |
| `b880` 订单详情页-状态头与时间线 | **部分通过** | ✓ 状态头大字「待接单」+「预计 18:24 送达」；✓ 五节点时间线（已下单/已支付/商家接单/配送中/已完成）齐全；✓ 无骑手位置/地图/实时定位。△ **未目视核验**当前节点品牌橙实心+光环、未达节点灰（本轮浏览器截图层被 in-app 浏览器渲染缩放干扰，视觉项以 DOM 事实为准，未做像素级核对）；未点验"商家端推进后刷新即更新"与"已取消订单时间线置灰" |
| `b890` 订单详情页-订单详情内容 | **通过** | 商家名可点进商家详情；「外卖自取/送达」纯展示标签；商品明细按"商品+规格组合"分行并带缩略图；金额明细为「商品小计 ¥51.00 / 打包费 ¥2.00 / 配送费 ¥5.00 / 配送费优惠 −¥5.00 / 实付金额 ¥53.00」（基础四行 + 优惠项按实际发生）；「配送信息」卡=配送地址+收货人+配送服务「课程演示配送」（**无"蜂鸟专送"等第三方字样**）；「订单信息」卡=订单号+复制/下单时间/支付方式「模拟支付」（**无"微信支付"**） |
| `b8a0` 订单详情页-底部操作区 | **通过** | PENDING（待接单）状态下「取消订单」**enabled**（符合"仅待支付与待接单可用"）；已支付单显示「联系商家」；**无「查看配送进度」按钮**；无退款/售后/资金文案 |

### 支付页与支付结果

| 条目 | 结论 | 证据 |
| --- | --- | --- |
| `b080` 支付页-顶部栏 | **通过** | 返回箭头 + 应用标题「轻量外卖」，**无关闭按钮**（已按 CHG-003 去掉） |
| `b090` 支付页-支付内容区 | **通过** | 倒计时卡仅「支付剩余时间」+ 橙色大字；订单详情卡标题行右侧为**文字入口「取消订单」**（不与底部按钮并排）；卡内：商品清单（名+数量+单价）→ 金额明细 → 商家 → 订单编号（含「复制订单号」）→ 下单时间 → 收货地址；底部固定「立即支付 ¥53.00」；页面无真实支付方式、退款与资金文案（另一按钮为「演示支付失败」） |
| `b100` 支付成功页-成功横幅 | **通过** | 「支付成功 / ¥53.00 / 订单编号 o1508」来自接口（金额与订单实付一致，非写死）；页面不再发起支付 |
| `b110` 支付成功页-订单摘要卡 | **通过** | 支付方式「模拟支付」、支付时间 `2026-09-15 17:45:15`、订单编号来自订单详情；无第三方渠道 |
| `b120` 支付成功页-会员提示 | **通过** | 显示「会员红包已同步 / 下次下单可使用」固定课程文案，纯展示，无积分余额与兑换入口 |
| `b130` 支付成功页-底部操作 | **通过** | 「查看订单」与「回到首页」；点「查看订单」进入 `/user/orders/o1508` 详情 |
| `b140` 支付失败页-顶部栏 | **部分通过** | 页面存在且可返回（`/user/orders/o1515/pay-fail`）；**未点验**"已支付订单时不再显示失败页"与"编号缺失返回订单列表" |
| `b150` 支付失败页-失败内容区 | **通过** | 显示「支付失败 / 模拟支付失败 / 待支付金额 ¥57.50 / 订单号 o1515」；按钮「重新支付」「返回订单列表」；**无联系客服入口**；后端该单仍 `status=PENDING_PAYMENT`、`paidAt=null`（失败不生效） |

### 地址簿

| 条目 | 结论 | 证据 |
| --- | --- | --- |
| `b320` 地址列表页-顶部栏 | **通过** | 标题固定「收货地址」；进入即请求当前用户地址；「新增地址」入口；**无地址时显示空态「暂无收货地址」并给出新增入口**；不同账号只能读到自己的地址（越权 404，见 819） |
| `b330` 地址列表页-地址列表 | **部分通过** | ✓ 字段齐全（联系人/性别/电话/地区/详细地址/标签/默认标识）；✓ 默认地址置顶并带「默认」标识；✓ 删除有二次确认（实现为 `window.confirm('确定删除该收货地址吗？')`；**本环境自动确认**，故那次点击直接删除成功——已按原字段新建一条补回）；✓ 空列表给出新增入口；△ **未验证**：设为默认后其他地址取消默认（避免改动共享演示数据）；△ **未验证**：删除默认后由后端自动把剩余最近更新一条设为默认 |
| `b340` 新增地址页-顶部栏 | **通过** | 新增模式标题「新增地址」；点地址卡进入编辑模式，标题变「编辑地址」且路由携带地址编号（`/user/addresses/da1522/edit`） |
| `b350` 新增地址页-地址表单 | **部分通过** | ✓ 字段齐全（联系人/性别/手机号/所在地区/详细地址/标签/设为默认）；✓ 必填校验（4 条）与手机号格式校验（`手机号格式错误`）；✓ 保存后返回并回填到确认订单页；△ **「设为默认地址」开关初值不符口径**（用户无任何地址时仍为关闭；SRS 要求按"当前是否存在默认地址"决定）；后端会把首个地址自动置默认，功能结果正确 |
| `b360` 新增地址页-保存按钮 | **部分通过** | ✓ 按钮默认可见；✓ 校验不过不提交、停留本页并保留输入；✓ 保存成功后回到列表/确认页并以后端返回地址刷新；△「保存中禁用」未观察到（保存很快返回） |

## 5. §8.3 本期扩展模块（取 3 条）

| 条目 | 结论 | 证据 |
| --- | --- | --- |
| `831` 模拟支付与库存一致性 | **部分通过** | ✓ **库存只在创建订单时扣减一次**：下单前 `p103 stock=73` → 建单后 `70`（该单 2×大份+1×标准=3 件，精确相等）；✓ 支付失败不改变订单状态与已扣库存（`o1515` 失败后仍 `PENDING_PAYMENT`、`paidAt=null`）；✗ **重复支付只生效一次**未构造；✗ **支付超时不回补库存**未构造（课程简化规则，需另单验证）；⚠ 演示账号并发使用导致库存含他人订单（p101 −2 / p104 −2），无法逐笔对账 |
| `832` 优惠与统一计价 | **部分通过** | ✓ 会员价参与实付（老北京鸡肉卷标准 ¥15.00、大份 ¥18.00；劲脆鸡腿堡/香辣鸡腿堡 ¥17.50）；✓ 免配送费门槛生效（商品小计满 30 → `deliveryFeeDiscount=5`）；✓ 打包费固定 ¥2.00；✓ 实付非负且以后端为准（`51.00+2.00+5.00−5.00=53.00`）；✓ 实付逐项对账通过：新客立减 **只对新账号生效**（u2 首单 `newCustomerAmount=3`、老账号 u1 同店再下单为 0），配送费优惠满 30 触发、打包费固定 2.00，`Σ` 与 `total` 分毫不差；✗ **满减/红包叠加**未构造；⚠ **确认页预估与后端实付不一致**（预估 ¥46.00 vs 实付 ¥38.00，差 新客立减 3 + 配送费优惠 5；预估 ¥64.00 vs 实付 ¥53.00，差 会员价 6 + 配送费优惠 5），根因是契约无试算接口（见 §1） |
| `833` 商品规格（用户端侧：FR-SKU-01） | **通过** | 规格入口（"可选规格 N 项"）→ 弹层单选必选 → 价差计价（含会员价叠加：¥15.00+¥3.00=¥18.00）→ 购物车按"商品+规格组合"分行 → 订单明细留档规格与单价（`o1508` 明细两行：大份 ×2 @¥18.00、标准 ×1 @¥15.00）；必选未选禁止提交；重复提交只生效一次 |

## 6. 复现与取证命令（可复核）

```bash
# 会话（用户端为服务端 Session Cookie）
curl -s -c u1.txt -X POST http://82.157.137.114:4001/api/v1/auth/login \
  -H 'Content-Type: application/json' \
  -d '{"account":"13800000001","password":"123456","role":"user"}'

# 越权（819/824）：另一账号读他人订单/地址 → 404
curl -s -b u2.txt http://82.157.137.114:4001/api/v1/orders/o1508      # {"code":40401,"message":"订单不存在"}
curl -s -b u2.txt http://82.157.137.114:4001/api/v1/me/addresses/da001 # {"code":40401,"message":"地址不存在"}

# 半笔订单（824）：闭店下单 409，前后订单数不变
curl -s -b u1.txt -X POST http://82.157.137.114:4001/api/v1/orders \
  -H 'Content-Type: application/json' -d '{"storeId":"m004","addressId":"da1327"}'

# 金额口径（b060/832）：确认页预估 vs 后端实付
curl -s -b u2.txt http://82.157.137.114:4001/api/v1/orders/o1523   # total=38.00（确认页显示 46.00）

# 订单留档（572）：改地址后旧订单快照不变
curl -s -b u2.txt http://82.157.137.114:4001/api/v1/orders/o1523   # address.detail 仍为 "诚园8B 101"
```

> 注：带中文的请求体在 Git Bash 下会因编码被服务端判 `invalid_json`，需用 `--data-binary @file`（UTF-8 文件）提交。

## 7. 本轮对线上数据的影响（如实记录）

| 数据 | 影响 | 处理 |
| --- | --- | --- |
| 用户地址（u001） | 误删 1 条（「11 / 男 / 11111111111 / 1 / 1 11」）——点「删除」时 `window.confirm` 被 in-app 浏览器自动确认 | **已按原字段新建补回**（新 `addressId`，展示字段一致） |
| 订单（u001） | 新增 `o1508`（已支付成功，¥53.00）、`o1515`（支付失败，仍待支付 ¥57.50） | 保留（演示订单，不影响后续） |
| 用户（新增账号） | 新建 `13900000002 / test123456`（昵称 crosscheck）与其 1 条地址 | 保留（可作后续隔离测试用；如需清理请告知） |
| 库存 | 因并发使用，p101/p102/p103/p104 均有扣减（含他人订单） | 不做回补（促销/演示数据可接受） |
| 购物车（u001） | 含本人与他人混入的商品行 | **未清理**（避免影响并发使用者） |

## 8. 未覆盖 / 建议下一轮补的项

1. `825` 访问形态：需持单人真机访问一次。
2. ~~`537` 超库存与下架~~：已按负责人裁定不计为待处理项；若后续要补测，需要一份低库存（≤3）与一份 `onSale=false` 的演示数据。
3. `831` 重复支付、支付超时不回补：需专门构造两笔待支付单。
4. `b330` 设为默认 / 删除默认后自动补默认：需在无并发使用时操作演示账号。
5. `b880` 时间线节点配色、当前节点光环：需**真机或窄窗口**目视（in-app 浏览器在桌面窗口下按 vw 放大约 3 倍，截图不可用于视觉判定，见 TODO-USER-109/112）。
