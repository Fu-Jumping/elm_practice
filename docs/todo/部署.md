# 部署待办（deploy / 环境 / CI）

> 覆盖：发布与回滚、服务器与数据库环境、CI、演示数据、构建验证。主责人：**林晨（后端 B）**，组长可维护结构。
> 状态取值 `进行中 / 已完成 / 已冻结`；每行必须有且仅有一名主责人。

## 1. 进行中

| ID | 任务 | 类型 | 出处 | 负责人 | 状态 | 证据 | 更新 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| TODO-OPS-001 | `deploy/smoke.py` 支付断言由 `PROCESSING` 改为 `PENDING`（与 PRD 7.6 定稿一致），并在批次② 落地后复跑 | 打磨 | PRD 7.6、契约 §3.5 | 林晨 | 已完成 | 2026-09-13 随本次发布一并修正并复跑：① 支付后状态断言 `PROCESSING` → **`PENDING`**（依据契约 §3.5 定稿与后端 PR #51 状态机修正）；② **金额断言由写死 `total == 60` 改为按契约 §3.5 公式核对**（实付 = 小计 − 满减 − 新客 − 红包 − 会员折扣 + 配送费 − 配送费优惠 + 打包费，且不小于 0）——批次① 七步计价接入后旧硬编码值必然失效（首发冒烟即因此失败） **证据**：修正后 `python3 deploy/smoke.py` = **50/50 通过**（`businessAssertionsPassed=true`、`testStoreClosed=true`，合成订单 `o1198`/临时店 `m1192`）；发布记录见 `运行说明` §4.4（2026-09-13 段） | 2026-09-13 |
| TODO-OPS-002 | 每次发布后用 `deploy/release.py --source ... --tag <日期-短SHA> --release-sha <短SHA>` 发布，并把发布记录追加到 `运行说明` §4.4（在线版本、构建与测试输出、冒烟结果、回滚点） | 打磨 | 运行说明 §4.4 | 林晨 | 进行中 | 2026-09-13 **完成一次（发布 `20260913-707f234`）**：记录已按新格式（按时间倒序）写入 `运行说明` §4.4，含在线版本 `707f234`、构建与测试证据（用户端 47 文件 / 商家端 9 文件 29 用例 / 后端 29M jar）、冒烟 **50/50**、外网与端到端核对、**回滚点** `/srv/elm-releases/20260910-99a646d` 与回滚资产目录；本行保持「进行中」——属每次发布的常态化动作 | 2026-09-13 |
| TODO-OPS-003 | **BUG-20260908-007** 端口口径统一（**2026-09-11 按负责人指示落到开发同学**）：① `backend/src/main/resources/application.properties` 的 `server.port=8080` → **4000**；② `frontend/user-h5/vite.config.ts` 代理默认 `http://127.0.0.1:8080` → **4000**；③ `backend/README.md`「默认监听 `127.0.0.1:8080`」→ **4000**；改完按 R1 跑 `mvn -B -DskipTests package` 与用户端 `npm run build` + 冒烟回归后关闭缺陷。（若负责人改为统一 8080，则反向同步文档，需另行拍板） | 缺陷 | 缺陷跟踪表 BUG-20260908-007、PRD 7.18、运行说明 §4 | 林晨 | 进行中 | 2026-09-12 三处已改 + 现行口径文件同步（`图片清单.md`、`docs/maps/PRD与SRS思维导图.html`），分支 `fix/port-unify-4000` 待 PR；R1 已过：`mvn -DskipTests package` 成功（29M jar）、用户端 `npm run build`（含 tsc）成功；grep 复查 8080 仅余历史记录/解释性注释/CORS 测试用例；服务器由 `--server.port=4000` 同值覆盖，部署无影响；待本地 4000 冒烟与合并后发布回归 | 2026-09-12 |
| TODO-OPS-004 | 新表上线后同步演示数据重置脚本（`promotion_tiers`、`delivery_fee` 等），保证"重置后演示数据可复现" | 未完成模块 | 运行说明 §4.3、PRD 7.18 | 林晨 | 进行中 | | 2026-09-10 |
| TODO-OPS-005 | 图片上传目录 `backend/uploads/` 在服务器上的**持久化与可写**（部署时确认目录存在、权限正确、静态映射生效） | 未完成模块 | 契约 §10.1、运行说明 §4.3 | 林晨 | 进行中 | 2026-09-15 **配置侧已补齐**：`deploy/nginx.conf` 新增 `location /uploads/` 反代到 `127.0.0.1:4000/uploads/`（原缺失 → 浏览器请求落到 SPA 回退返回 HTML，图片必然打不开，见 `docs/todo/用户端.md` TODO-USER-007）；同一处 `client_max_body_size` 由 `2m` 放宽到 `4m`。**剩余＝发布后确认服务器目录存在且可写** | 2026-09-15 |
| TODO-OPS-006 | 扩充 CI：把新模块的后端测试与前端测试纳入（随批次推进自然覆盖），并观察 `.github/workflows/ci.yml` 的运行时长 | 技术债 | 小组协作流程 §7（R5） | 林晨 | 进行中 | | 2026-09-10 |
| TODO-OPS-007 | 后端测试统一走服务器测试库：在服务器 release 源码目录执行 `DB_NAME=elm_practice_test mvn -B test`，禁止对业务库执行重置类测试 | 技术债 | 运行说明 §4.5（R8） | 林晨 | 进行中 | | 2026-09-10 |
| TODO-OPS-008 | **演示入口恢复 4001（已生效）+ 移除 5174 旧跳转块**：原 4001 自 9/10 15:39 起被同服务器另一项目 `classroom-vote` 占用、本项目站点配置被改名禁用 → 对外全 404。处置（2026-09-13）：经负责人确认后**停用对方站点**（配置改名保留、一条 `mv` 可回退）、恢复本项目配置到 4001，并**移除配置中过时的 5174 旧地址跳转块**（5174 现由同服务器项目 `banwei-vote` 的 node 服务占用，该块 `bind()` 失败会导致**整次 nginx reload 中止**——现象是「配置改了但线上行为不变」，本次实测踩到并修复）；仓库侧 `deploy/nginx.conf`（含 5174 块删除）、`release.py`、`smoke.py`、`catalog-check.py`、`运行说明` §4.2/§4.4、演示材料两份、VIS-001 图与 PNG 已同步 | 缺陷 | 负责人 2026-09-13 决定（`运行说明` §4.4 注记） | 林晨 | 已完成 | 2026-09-13 **服务器已生效**：nginx reload 于 21:44 成功（worker 重启时间已核），本机与外网 `4001/`、`4001/user/`、`4001/user/api/v1/stores`、`4001/demo/` 均 **200**（页面标题确认为本项目）；配置备份 `/home/ubuntu/backups/nginx-20260913-4001switch/`；**整包发布亦已完成**（`20260913-707f234`：RELEASE_OK + 冒烟 50/50 + 端到端红包接口返回真实数据） | 2026-09-13 |

## 2. 已完成

| ID | 任务 | 类型 | 出处 | 负责人 | 状态 | 证据 | 更新 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| TODO-OPS-101 | **9/10 发布合并后 main 到演示环境**（`/srv/elm-current → 20260910-99a646d`） | 未完成模块 | 运行说明 §4.4 | 林晨 | 已完成 | commit ed38825/99a646d；RELEASE_OK；冒烟 50/50；浏览器双端核对 | 2026-09-10 |
| TODO-OPS-102 | `deploy/release.py` 参数化（`--source/--tag/--release-sha/--backup`）+ 发布前置校验 | 技术债 | deploy/README.md | 林晨 | 已完成 | commit ed38825 | 2026-09-10 |
| TODO-OPS-103 | 接入 CI（三端构建+单测、后端集成测试、提交规范检查），并把后端集成测试提升为必需任务 | 技术债 | 小组协作流程 §7（R5/R6） | 林晨 | 已完成 | `.github/workflows/ci.yml`；首次成功运行 34429908687（23 条后端测试） | 2026-09-10 |
| TODO-OPS-104 | 远端分支清理（删除 7 条已并入/废弃分支，未合并分支清零） | 技术债 | 项目规则 §5（2026-09-10） | 付赞平 | 已完成 | 远端 31 条分支、无未合并分支 | 2026-09-10 |
| TODO-OPS-105 | 演示服务器 SSH 通道打通（本机公钥登记），并实测工具链与回滚资产 | 技术债 | 运行说明 §4.4 | 付赞平 | 已完成 | Node 22.23.2 / Java 17.0.20 / Maven 3.6.3；pm2-before 等备份齐全 | 2026-09-10 |

## 3. 阻塞与待确认

| ID | 事项 | 阻塞原因 | 负责人 | 更新 |
| --- | --- | --- | --- | --- |
| TODO-OPS-006 | 分支保护/必需状态检查无法配置 | GitHub 私有仓库 + 免费账号（API 403）。**已知限制，非待办**：以"PR 自检 + 组长核对 CI 全绿"替代；若后续把仓库设为公开则可启用 | 付赞平 | 2026-09-10 |
| TODO-OPS-004 | 演示数据重置脚本依赖新表结构 | 依赖批次① 建表（TODO-BE-001） | 林晨 | 2026-09-10 |
