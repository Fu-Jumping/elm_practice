# 后端待办（backend）

> 覆盖：接口、业务规则、事务与并发、数据库脚本、后端测试。主责人：**王嘉悦（后端 A：架构/契约/计价）** 与 **林晨（后端 B：业务/数据库/部署）**。
> 状态取值 `进行中 / 已完成 / 已冻结`；每行必须有且仅有一名主责人（协作人写在任务描述里）。

## 1. 进行中

| ID | 任务 | 类型 | 出处 | 负责人 | 状态 | 证据 | 更新 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| TODO-BE-001 | 批次① **优惠计价七步接入下单** + `promotions` 扩列（`new_user_amount`/`free_delivery_threshold`/`member_discount`）+ 新表 `promotion_tiers(store_id,threshold,amount,sort_order)` + 金额快照新增 `deliveryFee` + 旧单档数据重置与迁移脚本 | 已完成 | PRD 7.4、契约 §3.5/§6.3、TC-PRV-001~011 | 林晨（协作：王嘉悦·计价与契约） | 已完成 | PR #48（merge cec86da，CI clean）；37/37 绿（elm_practice_test）；红端 6F+2E；服务器部署升级与冒烟待执行 | 2026-09-10 |
| TODO-BE-002 | **订单状态机修正**：支付成功写 `PENDING`（原实现 `OrderMapper.markPaid` 写 `PROCESSING`），`PROCESSING` 仅 P0 基线 | 缺陷 | PRD 7.6、契约 §3.5、TC-ORD 组 | 王嘉悦 | 已完成 | TDD 红绿闭环（2026-09-12）：红端 OrderServiceTest 支付后状态断言改 `PENDING` 复跑 1F（expected PENDING but was PROCESSING）；绿端 `markPaid` 写 `PENDING`、`OrderService.pay` 同步、`advance` 删除 `PROCESSING`→`PENDING` 流转、种子 `o1001` 改 `PENDING`；全量回归 55/55 绿（elm_practice_test）+ `package` 构建通过；契约 §3.5/§9/§10.4 三处回写；**PR #51 已合并 main（merge 71db6d5，CI 5/5 绿）** | 2026-09-12 |
| TODO-BE-003 | 批次② **用户取消订单**：`POST /api/v1/orders/{orderId}/cancel`、`CANCELLED` 状态、取消原因必填、**事务内回补库存**、重复取消幂等 | 未完成模块 | 契约 §3.5、TC-CAN-001~010 | 王嘉悦（协作：林晨·回补事务） | 已完成 | 服务器测试库 elm_practice_test 全量 `mvn test` 105/105 绿（2026-09-15）；OrderCancellationStage2IntegrationTest 4 用例绿（条件更新 + 库存回补 + 越权 404）；契约 §3.5 三处与 §9 状态清单 2026-09-15 回写 | 2026-09-15 |
| TODO-BE-004 | ~~批次② **配送单**三接口与状态~~（**已砍掉，CHG-002**：配送进度由订单状态表达，不建配送单） | 未完成模块 | 负责人 2026-09-11 拍板（CHG-002） | 林晨 | 已冻结 | 范围变更：配送单表、三接口与 TC-DLV 均不实现 | 2026-09-11 |
| TODO-BE-005 | 批次③ **评价**：`tags`/`images` 字段、一单一评、商家回复更新原记录 | 未完成模块 | 契约 §6.2、TC-REV 组 | 林晨 | 已完成 | 服务器测试库 elm_practice_test 全量 `mvn test` 105/105 绿（2026-09-15）；ReviewStage2IntegrationTest 3 用例绿（提交/一单一评/回复更新原记录）；图片与标签字段随批次⑩落库 | 2026-09-15 |
| TODO-BE-006 | 批次④ **消息与通知**：会话/消息/已读、双端未读分离、三类通知 | 未完成模块 | 契约 §6.1/§3.9、TC-MSG 组 | 王嘉悦 | 已完成 | 会话与消息、双端未读分离已交付（ConversationStage2IntegrationTest 3 用例绿）；**通知四接口 2026-09-15 补齐**：`GET /me/notifications`、`PATCH /me/notifications/{id}/read`、`PATCH /me/notifications/read`、`GET /me/notifications/unread-count`，触发时机（支付进 PENDING / 接单 / 出餐配送 / 完成 / 取消写 ORDER，红包到账写 COUPON，会员提醒写 MEMBER）落地；NotificationIntegrationTest 8 用例绿；服务器测试库 elm_practice_test 全量 `mvn test` 105/105 绿（2026-09-15）；契约 §3.9 已回写 | 2026-09-15 |
| TODO-BE-007 | 批次⑤ **搜索**：商家名/商品名/分类条件、排序（综合=销量优先评分次之，距离取种子 `distanceKm`）、分页 `page`/`size` 默认 1/10 | 未完成模块 | 契约 §3.6、TC-SRC-001~009 | 王嘉悦 | 已完成 | **2026-09-15 实现**：`GET /api/v1/search` 返回 `merchants`/`products` 两组分页对象；`stores` 扩列 `distance_km` 并新增 `distanceText`/`couponTags`；非法 sort 与非正整数 page/size 返回 400，分类无匹配返回空列表；SearchIntegrationTest 8 用例绿；服务器测试库 elm_practice_test 全量 `mvn test` 105/105 绿（2026-09-15）；契约 §3.6 已回写 | 2026-09-15 |
| TODO-BE-008 | 批次⑥ **收藏、会员与会员价、红包**（一单一红包、门槛与适用范围、并发锁定） | 已完成 | 契约 §3.7/§3.8、TC-FAV/MBR/CPN 组 | 林晨（协作：王嘉悦·会员价计价） | 已完成 | 红包子项 2026-09-12 已交付（CouponIntegrationTest 15 用例绿）；**2026-09-15 补齐收藏与会员**：`GET/POST /me/favorites`、`DELETE /me/favorites/{storeId}`（幂等、关闭店铺回显最新 storeStatus、couponTags 取自真实促销配置，FavoriteIntegrationTest 6 用例绿）与 `GET /me/member`（users 扩列 member_opened/member_activated_at，演示用户 u001 标记为会员，MemberIntegrationTest 6 用例绿）；PRD 7.4 第⑤步会员价与会员折扣不叠加落地（PricingService 新增 memberDiscountBase 入参）；服务器测试库 elm_practice_test 全量 `mvn test` 105/105 绿（2026-09-15）；契约 §3.7/§3.8 已回写 | 2026-09-15 |
| TODO-BE-009 | 批次⑦ **规格启用**：`PUT /merchant/products/{id}/specifications`、`cartLineId` 行唯一范围（用户+店铺+商品+规格） | 未完成模块 | 契约 §3.4/§4.2、TC-SPC 组 | 王嘉悦 | 已完成 | 服务器测试库 elm_practice_test 全量 `mvn test` 105/105 绿（2026-09-15）；MerchantCatalogStage2IntegrationTest 5 用例绿（规格保存、不同规格产生不同 cartLineId 与订单快照、越权 403）；reset.sql 与 upgrade-stage3.sql 补 p103/p106 规格种子 | 2026-09-15 |
| TODO-BE-010 | 批次⑧ **图片上传**：`backend/uploads/` + `/uploads/**` 静态映射、商品 1 张/评价 3 张、≤2MB、类型限制、越权 403 | 未完成模块 | 契约 §10.1、TC-IMG-001~007 | 王嘉悦 | 已完成 | 服务器测试库 elm_practice_test 全量 `mvn test` 105/105 绿（2026-09-15）；MerchantCatalogStage2IntegrationTest 图片用例绿（超 2MB 400、非商家 403）；**部署侧配套 2026-09-15 补齐**：deploy/nginx.conf 新增 location /uploads/ 反代并在同一 PR 把 client_max_body_size 放宽到 4m（原 2m 会把恰好 2MB 的图片挡成 413） | 2026-09-15 |
| TODO-BE-011 | 批次⑨ **统计聚合**：`overview`/`analytics` 字段与"有效订单"口径，空数据返回 0/空数组 | 未完成模块 | 契约 §6.3、TC-STA 组 | 王嘉悦 | 已完成 | 服务器测试库 elm_practice_test 全量 `mvn test` 105/105 绿（2026-09-15）；MerchantAnalyticsStage2IntegrationTest 3 用例绿（概览与区间统计字段、空数据 0/空数组） | 2026-09-15 |
| TODO-BE-012 | 契约 **§10.4 六项决议**在实现完成后回写对应章节（状态机、金额快照、图片参数、搜索分页、红包/通知种子、规格命名） | 技术债 | 契约 §10.4 | 王嘉悦 | 进行中 | 2026-09-10 复核：六项定稿值已落到 §3.4/§3.5/§3.6/§3.8/§3.9/§4.2/§10.1（快照字段含 `couponAmount`）；仅 §9 现状说明的“实现后回写”随批次实现推进 | 2026-09-10 |
| TODO-BE-013 | **BUG-20260908-005** `PENDING_PAYMENT` 订单对商家不可见：口径已定（支付前不可见、支付后出现），补齐证据并关闭台账条目 | 缺陷 | 缺陷跟踪表 BUG-20260908-005 | 王嘉悦 | 进行中 | | 2026-09-10 |
| TODO-BE-014 | 新增表的建表/种子/迁移脚本可重复执行，并在 `database/` 与 `resources/db/` 两处保持同步（现文件头已注明需同步） | 技术债 | 运行说明 §4.3 | 林晨 | 进行中 | | 2026-09-10 |
| TODO-BE-015 | **新增模块错误码细分**：取消状态冲突、红包已被占用、订单状态跳级/非法流转等场景的具体 code 补充并回写契约 §8（未补前按通用码） | 技术债 | 契约 §8（2026-09-10 登记）、PRD 7.6/7.17 | 王嘉悦（协作：林晨） | 已完成 | 2026-09-12 由林晨实现并合并（PR #49，CI 全绿）：两接口全部口径落地，10 档权重 BlastTierPool 与 §10.5 双写、随机种子可注入（BlastTierPoolTest 固定种子）；免费爆条件 UPDATE 占用、替换式原地更新、东八区当天 23:59:59；TC 由 CouponIntegrationTest 15 用例覆盖（含 409/404/400），55/55 绿（UTC 时区验证） | 2026-09-10 |
| TODO-BE-016 | **统计数据口径收口**：① “有效订单”定义**已在契约 §6.3 写明**（已支付且未取消：`PENDING`/`COOKING`/`DELIVERING`/`COMPLETED`，兼容 P0 历史 `PROCESSING`，排除 `PENDING_PAYMENT` 与 `CANCELLED`），无需重新拍板，按此实现即可；② “访客”字段已按 2026-09-10 拍板删除，PRD 7.14、契约 §6.3 与 TC-STA-001 已同步；本任务只剩批次⑨实现与证据 | 未完成模块 | 契约 §6.3、§9“有效订单定义”待实现项；`后端联调验收用例.md` 第 14 项；PRD 7.14 | 王嘉悦（协作：林晨） | 进行中 | 2026-09-10 口径已收口：契约 §6.3 已定义“已支付且未取消”；PRD 7.14 与 TC-STA-001 的“访客”已按负责人拍板删除；剩余为批次⑨ 实现与证据 | 2026-09-10 |
| TODO-BE-019 | 店铺设置补 `contactPhone`：字段、校验、回写契约 §4.1（归口已定：**后端补**） | 缺陷 | 缺陷跟踪表 BUG-20260908-012；`docs/todo/商家端.md` TODO-MER-010；负责人 2026-09-10 拍板 | 林晨（协作：王嘉悦·契约 §4.1 回写） | 进行中 | 2026-09-12 开发完成待 PR：分支 `fix/merchant-store-contact-phone`；TDD 红 3 failed（`No value at JSON path contactPhone`）→ 绿 28/28（服务器测试库 elm_practice_test）；StorePatch 加字段、落库 merchants.phone、GET/PATCH 回显、11 位手机号校验、契约 §4.1 已回写；待测试同学回归后关闭缺陷单 | 2026-09-12 |
| TODO-BE-020 | **VIS-002 系统技术架构图**（含三端、分层、MySQL、Session、Mock/real 分支，不画未实施基础设施） | 未完成模块 | `docs/project/用户待补充资料与原型清单.md` VIS-002；负责人 2026-09-10 拍板全补 | 王嘉悦 | 进行中 | | 2026-09-10 |
| TODO-BE-021 | **VIS-003 数据库 ER 图**（横向 ≥1600px，与最终建表脚本完全一致） | 未完成模块 | `docs/project/用户待补充资料与原型清单.md` VIS-003；负责人 2026-09-10 拍板全补 | 林晨 | 进行中 | | 2026-09-10 |
| TODO-BE-022 | **CHG-001 爆红包建表（二修）**：`coupons` 扩列 `source`（PACK/BLAST_OUT）、`can_blast`、`pack_id`；新增 `coupon_packs`（购买批次）；`users` 扩列 `free_blast_date`（每日免费爆）；**与批次⑥ 的 `coupons` 建表一次设计到位**；脚本可重复执行并在 `database/` 与 `resources/db/` 同步 | 已完成 | CHG-001 §1.7、契约 §3.10 | 林晨（协作：王嘉悦） | 已完成 | 2026-09-12 落盘并合并（PR #49）：schema 两处 + 迁移 `20260912-coupons-blast.sql` + 运行时 `upgrade-coupons-blast.sql`（DatabaseInitializer 启动幂等执行），含 cp001/cp002 演示券 | 2026-09-10 |
| TODO-BE-023 | **CHG-001 买套餐与爆红包接口（二修）**：`POST /me/coupon-packs`（pack49/pack99）与 `POST /me/coupons/blast`（不传 `couponId` 用当日免费次数、传则消耗并替换；**10 档权重纯随机**、**可注入随机种子**；爆出来的券 `validTo` = 当天 23:59:59、购买券 7 天；无免费次数且无可爆券 `409`） | 未完成模块 | CHG-001 §1.7、契约 §3.10/§10.5、TC-RBP-001~011 | 王嘉悦（协作：林晨） | 已完成 | 2026-09-12 由林晨实现并合并（PR #49，CI 全绿）：两接口全部口径落地，10 档权重 BlastTierPool 与 §10.5 双写、随机种子可注入（BlastTierPoolTest 固定种子）；免费爆条件 UPDATE 占用、替换式原地更新、东八区当天 23:59:59；TC 由 CouponIntegrationTest 15 用例覆盖（含 409/404/400），55/55 绿（UTC 时区验证） | 2026-09-10 |
| TODO-BE-024 | **AI 点餐助手**：LangChain4j + DeepSeek + MySQL 会话记忆 + 静态知识库注入（方案 B，零 Function Calling），`POST /api/v1/ai/chat` 与 `/stream-chat`，只做推荐查询不下单 | 新功能 | 契约 §10.6 | 林晨（协作：王嘉悦） | 已完成 | 2026-09-14 实现（方案 B）：pom 加 langchain4j 1.1.0+open-ai 1.0.1+reactor；新增 ai_chat_sessions/ai_chat_messages 表；MysqlChatMemoryStore（MySQL 持久化会话记忆，20 条窗口）；ProductKnowledgeService（启动时从业务库加载全部在售商家+商品生成知识库文本，动态拼入系统提示词）；AiChat/AiChatConfig/AiChatController；去掉 Function Calling 工具集，大模型直接从上下文检索；冒烟全通过：肯德基推荐真实数据、多轮记忆正常、口味推荐跨店检索、知识库没有的如实说没有；DeepSeek key 经 DEEPSEEK_API_KEY 注入 | 2026-09-14 |
| TODO-BE-025 | **合并 `feat/remaining-gaps-tdd` 后的后端回归与实现取舍确认**：整分支合并时后端整体采用分支实现（通知/搜索/平台级分类 + `users.member_opened` 收藏/会员变体），main 侧 BE-008 变体（`is_member` + `MemberService` + favorites/member 迁移脚本）被取代并删除 | 技术债 | 合并提交 `298ba08`、`docs/backend/后端接口契约.md` §3.6/§3.7/§3.8/§3.9 | 王嘉悦（协作：林晨） | 进行中 | 2026-09-15 合并取向：`mvn -B -DskipTests clean package` 通过；**`mvn test` 需在服务器测试库 `elm_practice_test` 重跑确认（本机无 MySQL，R8）**；被删除文件：`backend/database/migrations/20260915-favorites.sql`、`20260915-member.sql`、`backend/src/main/resources/db/upgrade-favorites.sql`、`upgrade-member.sql`、`service/MemberService.java`；如需恢复 main 侧变体，请在本行讨论后决定 | 2026-09-15 |

## 2. 已完成

| ID | 任务 | 类型 | 出处 | 负责人 | 状态 | 证据 | 更新 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| TODO-BE-101 | 营业状态修改**落库**（修复只改内存导致用户端始终 OPEN） | 缺陷 | 缺陷跟踪表 BUG-20260908-011 | 林晨 | 已完成 | PR #39、ccef40b；CI 后端测试 23 条绿 | 2026-09-10 |
| TODO-BE-102 | 演示目录种子对齐脚本 `database/seed/catalog-alignment.sql` | 未完成模块 | 演示环境需要 | 林晨 | 已完成 | PR #39 | 2026-09-10 |
| TODO-BE-103 | P0 六类能力接口与 MyBatis 持久化（账号/商家/店铺/商品/购物车/地址/订单/支付/推进；协作：林晨） | 未完成模块 | PRD §6.1.1 P0 行 | **王嘉悦** | 已完成 | main 历史提交；CI `backend-test` 23 条全绿 | 2026-09-07 |
| TODO-BE-104 | 契约 §3.5/§3.6/§3.7/§3.8/§3.9/§10 新增章节与 §10.4 决议表 | 技术债 | 2026-09-09 范围决议 | 王嘉悦 | 已完成 | PR #37 合并前回写（553→559 行） | 2026-09-09 |

## 3. 阻塞与待确认

| ID | 事项 | 阻塞原因 | 负责人 | 更新 |
| --- | --- | --- | --- | --- |
| TODO-BE-001 | 旧单档 `promotions` 数据兼容 | 已解除：迁移脚本 `20260912-promotion-tiers.sql` 落盘且 `DatabaseInitializer` 启动幂等执行同等升级（2026-09-12） | 林晨 | 2026-09-12 |
| TODO-BE-008 | 红包种子数据字段（有效期/适用范围） | 已解除：reset.sql 4 张（全场/STORE/已用/过期）+ 两处 seed.sql 与运行时升级脚本各 2 张演示券（2026-09-12） | 林晨 | 2026-09-10 |
