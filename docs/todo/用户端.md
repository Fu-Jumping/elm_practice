# 用户端待办（frontend/user-h5）

> 覆盖：Vue 3 移动 H5 的页面、交互、复刻与前端测试。主责人：**戴宇涵**（组长可维护结构）。
> 更新规则见 `docs/todo/README.md`；状态取值 `进行中 / 已完成 / 已冻结`，每行必须有且仅有一名主责人。 编号说明：`1xx` 段自 `TODO-USER-104` 起为批次⑩及以后新增任务，与已完成 P0 基线（`TODO-USER-101`–`103`）无固定对应。

## 1. 进行中

| ID | 任务 | 类型 | 出处 | 负责人 | 状态 | 证据 | 更新 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| TODO-USER-107 | **搜索页（输入页）与分类商家列表页**：① 搜索页按 `exports/用户端/03-搜索与商家列表/01-搜索页` 复刻——关键词输入 + **搜索历史** + **热门词**（PRD 7.16.1「首页-搜索框」行：搜索历史与热门词本期实现）；② 分类商家列表页按同目录两行逐区行实现——顶部栏（返回 + 页面标题）+ 筛选/排序栏（销量·距离·评分）+ 商家列表，数据走 `GET /stores?categoryId=&page=&size=`（**分类来源为店铺自身 `categories`；平台级 10 类本期不做**），接口返回空数组即空态、不得用演示数据伪装 | 未完成模块 | PRD 7.16.1「首页-搜索框」「分类商家列表页-顶部栏与筛选栏/商家列表内容区」、契约 §3.2/§3.6、设计真源 `docs/design/exports/用户端/03-搜索与商家列表/` | 戴宇涵 | 进行中 | 2026-09-13 **① 搜索页已完成（分支 `test/design-fidelity-gate`）**：新增 `views/user/SearchEntryView.vue`（搜索栏占位「搜索商家、商品名称」+ 搜索按钮、**最近搜索**（无历史整区隐藏）、**热门搜索** 8 词前 3 词带 HOT、底部促销条纯展示）+ `utils/searchHistory.ts`（localStorage 承载：trim、去重提前、上限 8 条、隐私模式/损坏内容静默降级；**契约无历史接口**故标注为本机用户侧状态）+ 首页搜索框改为进搜索页；三条提交路径（输入/历史词/热门词）统一「写历史 + 进结果页」，空词提示不跳转 **证据**：SH 6 + SE 6 + HomeView 18 通过，红 **11 failed/19 passed** → 绿全量 vitest **356 passed**、`npm run build` ✓、E2E **21 项全绿** **② 分类商家列表页已落地（2026-09-14，分支 `feat/category-store-list-tdd`，已提交并推送至 PR #63）**：新增 `views/user/CategoryStoreListView.vue`（顶部栏返回 + 标题、定位行、排序栏、商家卡：名/评分/月售/时长/距离/起送/配送费/促销标签、**休息中「不可购买」提示**）与路由 `category/:categoryId`（name `category-store-list`）；替身层 `mocks/store.ts` 的 `GET /stores` 补齐 `keyword`/`categoryId`/`sort`（排序实现抽到 `mocks/storeSort.ts`，与搜索域**共用同一份**口径），**不传参数时保持种子顺序**以免改变首页现状。**证据**：CL 组 13 条 + SLM 组 6 条，红（`CategoryStoreListView.vue` 未解析 + 替身 5 failed）→ 绿 19/19；全量 vitest **49 文件全绿**、`npm run build`（含 vue-tsc）✓、E2E **21 项全绿**。**② 入口已按方案 2 接线（2026-09-14，用户端决定；同日按负责人要求调整位置）**：商家详情页**店名右侧**新增「同类商家 ›」入口（`data-testid="store-similar-btn"`，位于 `store-banner` 的店名行内），参数取**当前选中分类**（默认第一个有商品的分类），携带 `categoryId` 与分类名进入 `category-store-list`；**原「商品分区标题行右侧」的 `section-more-{categoryId}` 入口已移除**（负责人 2026-09-14：入口不放在具体食物旁）；**左分类栏保持「点击滚动定位到该商品分区」不变**（项目规则 §5 的 P0 联动），并新增 **T76 守卫用例**锁住"点分类栏不发生路由跳转"；吸顶恢复为分区标题自身（`.product-list-title` sticky），与本次调整前一致。**证据**：T75 红（入口不存在）→ 实现后 T75/T76 绿；全量 vitest **49 文件全绿**、`npm run build`（含 vue-tsc）✓、E2E **21 项全绿**（含 `store-detail-scroll-handoff` 滚动交接与保真度巡检，验证吸顶结构改动无回归）。**未收口**：① **入口位置与 PRD 807 不一致**——PRD「首页-分类宫格」行写的是"点击分类进入分类商家列表"，但 CHG-001/PRD 817 定平台级「10 类」不做、分类来源改为店铺自身 `categories`，宫格无真实分类可传，故改由商家详情页分类区进入，**PRD 该行需按 R7 裁定后同步**（见 §3）；② **「筛选」按钮与分页未实现**——PRD 817/818 要求「排序与分页参数按契约 §3.2」「下拉加载下一页」，但契约 §1.3 明写「分页接口（§3.6 搜索）是列表约定的**唯一例外**」、§3.2 的 `GET /stores` 无 `page`/`size` 且返回裸数组，本页按**契约**实现（不分页、无筛选），冲突见 §3；③ 排序取值按契约 §3.2 实现为**综合/销量/距离**，PRD 817 字段列写的是「销量、距离、评分」，冲突见 §3；④ PR #63 已开（含本页与入口接线，CI 五项全绿，待组长 review 合并）；⑤ 复刻对照截图已补（`docs/prd-assets/user/replica-compare-2026-09-14/`：`03-分类商家列表页.png`，由 `frontend/user-h5/scripts/shot-replica-compare.cjs` 在 mock 模式生成）；⑥ 演示数据下每个分类只对应 1 家店（各店 `categories` 编号互不相同），页面可用但演示观感有限。**⑦ 线上真后端复验通过（2026-09-14 22:00 发布 `20260914-a2f15b1`，PR #63 已随发布上线）**：商家详情店名旁「同类商家 ›」→ `/user/category/c101?name=主食`（标题「主食」）→ 真实返回 1 家店（肯德基宅急送 4.8 / 月售 3500 / 25 分钟 / 起送 ¥20.00 / 配送 ¥5.00，与店铺自身 `categories` 一致）；排序切换带参重发（`categoryId=c101&sort=销量 / 距离`）；`/category/c999`（不存在分类）→ 空态「该分类暂无商家」+ 返回（接口返回空数组，符契约）。排序实测结论见 §3「分类列表页真实后端复验」行 | 2026-09-14 |
| TODO-USER-110 | **替身与契约 §3.10 响应形状对齐**：`mocks/coupon.ts` 的爆一次响应仍返回嵌套 `{ coupon, tierIndex, free }`，与契约 §3.10 的扁平形状不一致（本轮已在 api 层做归一化兼容两种形状，故不影响功能） | 技术债 | 契约 §3.10、`docs/frontend/前端架构设计与工程约定.md` | 戴宇涵 | 进行中 | 建议在冻结后与 `mocks/**` 其余域一起对齐；改替身会牵动 RB/RBM 组用例断言，冻结前不动 | 2026-09-14 |
| TODO-USER-112 | **桌面宽视口下的 vw 缩放上限（技术债，冻结后处理）**：px→vw 按 390 设计宽换算，`.app-shell` 只限制 430px 宽度、未限制缩放 → 宽窗口下整页放大 ~3 倍（购物车栏 317px 盖住半屏、遮挡加购按钮）。需整体定方案（如给缩放设上限或改用容器单位）并同步回归全部视口 | 技术债 | 负责人 2026-09-14 裁定（`TODO-USER-109`）、`docs/presentation/engineering-audit.md` 第 66 行 | 戴宇涵 | 进行中 | 冻结前不做（涉及全局样式策略与全量回归）；本行仅登记，避免遗忘 | 2026-09-14 |
| TODO-USER-114 | **BUG-20260914-003** 公开页整页刷新后客户端会话未恢复：点「+」加购被误判未登录、跳登录页且**不发请求**（服务端会话其实有效） | 缺陷 | 缺陷跟踪表 BUG-20260914-003、`docs/testing/问题记录/2026-09-14-用户端真后端联调发现.md` §1、PRD 7.3 加购 | 戴宇涵 | 进行中 | 2026-09-14 线上 `20260914-a2f15b1` 真后端复验发现：登录 → **整页刷新**商家详情（或直接打开深链 `/user/stores/m002`）→ 点加购 → 提示「请先登录」并跳 `/login?redirect=/stores/m002`；**该次点击前后无任何 `/api/` 请求发出**，同刻 `GET /cart?storeId=m002` 返回 **200**（会话有效），且商家详情页加载期无 `/me` 探活。根因：`StoreDetailView.vue:381`（加购）、`:178`（收藏）、`:435`（去结算）只读 `sessionStore.isLoggedIn`，而该状态仅在 `meta.auth` 路由由守卫 `checkLogin()`（`GET /me`）恢复，公开页整页加载后恒为「未登录」（`CouponView`/`OrderListView` 等页面均已先 `checkLogin`，本页未做）。待修：三处先 `await sessionStore.checkLogin()`；修复须 `test:` 先行（用例：公开页整页加载后加购不被拦）。**2026-09-14 已修（工作区，待提交）**：`onAdd` / `onToggleFavorite` / `onCheckout` 三处改为「**未登录才探活**」两步式（`await sessionStore.checkLogin()` 后再判定）；**关键取舍**：已登录路径完全同步、不引入 await 前置，避免破坏加购抛球动画的即时反馈（T73 依赖该同步性）。**红端**：新增 T77/T79 失败（`2 failed / 25 passed`，复现「服务端会话有效仍被跳登录页」）；T78 为回归锁（真未登录仍引导登录带 redirect，修复前即通过，不放宽口径）。**绿端**：`StoreDetailView.spec.ts` **27/27 通过**；受影响的既有用例 T45/T18 因该路径变为异步探活，断言改为 `vi.waitFor` 等待重定向（语义未变）；全量 vitest **55 文件 / 433 用例全绿**、`npm run type-check` ✓、`npm run build` ✓、E2E **21 项全绿**。**未收口**：提交/PR 待负责人允许；缺陷台账 BUG-20260914-003 状态流转归测试联调同学 | 2026-09-14 |
| TODO-USER-115 | **BUG-20260914-004 / 005** 消息中心在真后端不可用：① 通知接口 404 时 `Promise.all` 连带丢弃 **19 条真实会话**（页面显示「暂无消息」）；② 会话对象缺 `storeId`（线上返回 `merchantId`/`updatedAt`/`unreadCount`）→ 渲染抛 TypeError **整页白屏** | 缺陷 | 缺陷跟踪表 BUG-20260914-004 / BUG-20260914-005、问题记录 §2/§3、契约 §6.1 | 戴宇涵（协作：后端 A 王嘉悦·契约 §6.1 字段回写） | 进行中 | 2026-09-14 线上真后端复验：`GET /conversations` **200 / 19 条**、`/me/notifications` 与 `/unread-count` **404** → 页面「暂无消息」（对照实验：把通知接口拦截为 200 空列表后 `isEmpty` 变 false，证明会话数据被连带丢弃）；会话参与渲染时整页崩（控制台 `TypeError: Cannot read properties of undefined (reading 'slice')` @`MessageCenterView.vue:262`）。**字段口径**：契约 §6.1 只列 4 个接口与 `orderId` 参数，**未定义会话列表对象字段**；商家端已按 `unreadCount`/`userNickname` 归一化（`merchant-admin/.../merchantApi.ts:336`），用户端按自拟 `storeId`/`lastMessageAt`/`unread` 编写且 `listConversations` 未走归一化（架构约定 §3.3）。待修：① `loadAll` 改 `Promise.allSettled`/独立 try-catch；② 契约 §6.1 补字段定义（R7 同 PR）+ 用户端新增 `normalizeConversation` 且缺字段降级不崩。**2026-09-14 已修（工作区，待提交）**：① `loadAll` 改为 `Promise.allSettled([loadNotifications(), loadConversations()])`，两个数据源各自独立失败标记 + 通知区/会话区**局部失败态与「重试」入口**（`notification-error`/`notification-retry`），空态口径同步收紧（有失败时不显示「暂无消息」）；② 新增 `normalizers.ts` 的 `normalizeConversation`（架构约定 §3.3「归一化唯一出口」）：`storeId` 缺失退回 `merchantId`、`lastMessageAt` 退回 `updatedAt`、`unread` 退回 `unreadCount`，全缺给确定默认值；头像首字改用 `avatarText()`（恒返回字符串）、店名缺失给「未知商家」占位——**禁止 undefined 上屏**。**红端（新增 `MessageCenterView.realShape.spec.ts`）**：MC-R1（通知 404 时会话被整体丢弃，`conversation-item` 渲染 0 条）与 MC-R2/MC-R3 **在测试里复现了线上同一条报错** `TypeError: Cannot read properties of undefined (reading 'slice')` → `3 failed / 1 passed`；MC-R4 为标准形状回归锁（修复前即通过）。**绿端**：该文件 4/4 通过、`MessageCenterView.spec.ts`（TW 组）不变全绿、全量 vitest **55 文件 / 433 用例全绿**、`npm run type-check` ✓、`npm run build` ✓、E2E **21 项全绿**。**未收口**：契约 §6.1 会话字段口径仍待 R7 裁定（本修复是**别名兼容**、非口径定稿，裁定后可能需收敛实现）；提交/PR 待负责人允许 | 2026-09-14 |
| TODO-USER-116 | **AI 点餐助手前端页面**（P2 本期）：① 三处入口（首页右下角悬浮球 / 首页搜索框「AI 推荐」/ 我的页「AI 点餐助手」条目）；② 对话页（欢迎语 + 5 条快捷提问、流式逐字、Markdown 轻渲染、商家编号跳商家详情、会话保持、清空换会话、异常分流）；③ 数据层（`aiApi` 按契约 §10.6 + 离线替身） | 未完成模块 | `docs/frontend/AI点餐助手前端PRD.md`（PRD 6.11.1 / SRS 5.15 / 契约 §10.6）、验收项 AI-FE-01~12 | 戴宇涵 | 进行中 | 2026-09-14 **已落地（分支 `feat/ai-chat-page-tdd`，从 main `660dfbc` 起）**：新增 `views/user/AiChatView.vue`（顶部栏返回/标题/副标题/清空、消息区用户右橙气泡 + AI 左灰气泡带 🤖、快捷提问栏 5 词、输入框空则禁用 + 回车发送、网络异常提示）、`utils/aiChatSession.ts`（`ai_session_id` 与 `ai_messages_{sessionId}`，上限 50 条、损坏/隐私模式静默降级）、`utils/aiReply.ts`（Markdown 轻解析为**结构化块**：加粗/列表/商家编号，渲染层插值无 v-html → 无 XSS 面）、`services/api/ai.ts`（`chat` 非流式 + `streamChat` SSE 分片解析，30s 超时、503→`AiUnavailableError`、超时→`AiTimeoutError`）、`mocks/ai.ts`（知识库直接由演示种子 STORES/ALL_PRODUCTS/CATEGORIES_BY_STORE 构成，按店铺名/口味/价格/饮品/优惠分流，多轮记忆按 sessionId，知识库没有如实说「暂时没有」，另提供 `setAiMockUnavailable` 复现 503 错误态）、路由 `ai-chat`（无底部导航、未登录可用）。**红端**：5 个新测试文件（AS/AR/AM/AA/AI 组）+ HomeView 2 条 + MineView 1 条 → `5 failed (no tests)`（模块未创建）+ HomeView **2 failed** + MineView **1 failed**；**绿端**：AI 相关用例全绿（AS 6 + AR 7 + AM 7 + AA 6 + AI 15 + 入口 3），全量 vitest **54 文件 / 426 用例全绿**、`npm run type-check` ✓、`npm run build` ✓、E2E **21 项全绿**。**浏览器实测（mock 模式 5173，Playwright）**：首页悬浮球→`/ai-chat`；点「我想吃辣的」→ 流式分片渲染出「香辣鸡腿堡 ¥19.50 · 肯德基宅急送 [m002]」；点 `[m002]` → `/stores/m002`；整页刷新后 3 条消息与 `sessionId` 保持、不重复欢迎语；清空 → 仅欢迎语 + 新 `sessionId`；搜索框入口 `?prompt=帮我推荐今天吃什么` 只预填不自动发送；无控制台错误 **未收口**：① **线上 `POST /api/v1/ai/chat` 实测 503**——服务器未注入 `DEEPSEEK_API_KEY`（契约 §10.6 要求环境变量注入），real 模式暂不可用，属部署项（见 §3）；② PRD §5.1 与 §4.2 两处口径差异待 R7（见 §3）；③ 未补复刻对照截图（本页无设计稿，按 PRD §8 视觉规范实现）；④ 未加 E2E 用例（工程约定 E2E 只保留 3–5 条关键路径，本页由单测覆盖；是否纳入由负责人定）；⑤ 未开 PR（按 Git 纪律等负责人指示）。**2026-09-14 接库核查（负责人指示「AI 没接数据库，你接上」）**：① **写入链路核实通过**——后端 `MysqlChatMemoryStore` 按 sessionId 落库 `ai_chat_sessions` / `ai_chat_messages`，线上业务库实测 **13 会话 / 49 条消息**（`SHOW TABLES` + `COUNT(*)`），前端每条消息均经 `/ai/chat` 或 `/stream-chat` 上报，故会话内容确实在数据库中，前端 localStorage 只是展示缓存（PRD §5.2 即如此规定）；② **前端补「会话以后端为准」**：`/chat` 返回的 sessionId 与本地不一致时切到后端 id 并在新 key 下保存当前消息（`utils/aiChatSession.writeAiSessionId` + 视图 `adoptSessionId`），避免「数据库一段会话、本地展示另一段」分叉；用例 AS-7、AI-15（红端：`writeAiSessionId is not a function` / 未采用后端 id → 绿端 23/23）。③ **两处真实缺口另登记**（非本板块可修）：`ProductKnowledgeService.refresh()` 全库无调用点、契约 §10.6 无历史查询接口，分别见 §3 的 ⑤ 与 ④。PR #65（`feat/ai-chat-page-tdd`）。**2026-09-14 负责人走查（对话页顶部太靠上）**：`.ai-page` 增加 `padding-top: max(12px, env(safe-area-inset-top))`——桌面/普通浏览器 12px（与设计令牌 `--spacing-page` 同值），刘海屏独立打开取安全区高度（`index.html` 已 `viewport-fit=cover`，与底部栏 `env(safe-area-inset-bottom)` 同口径）；顶部栏自身仍按 PRD §3.2 保持 48px。实测几何：改前 appbar 顶端贴 y=0，改后 y=12；回归 vitest **54 文件 / 426 用例**全绿、`npm run build` ✓。**2026-09-14 负责人走查（顶部栏与底部输入框要固定、只滚动中间聊天记录）**：`.ai-page` 由 `min-height: 100vh`（整页在 `.app-main` 里滚）改为 `height: 100%; min-height: 0; overflow: hidden`，`.ai-list` 成为**唯一滚动容器**（`flex: 1 1 auto; min-height: 0; overflow-y: auto; overscroll-behavior: contain`），顶部栏/快捷提问栏/输入区 `flex: none`；顺带按设计稿口径隐藏滚动条（与 `.app-main` 同口径）。**浏览器实测**（mock 5173，390×844，41 条长对话）：顶部栏 `top=12`、输入区底边 `bottom=844`（贴视口底）、快捷提问栏底 `788` 三者在「列表滚到顶 / 滚到底」两次测量中**完全不变**，外层 `.app-main` 可滚高度 `0`（页面自身不滚），仅 `.ai-list` 可滚（3490px）且进入即自动贴到最新消息；无控制台错误。回归 vitest **54 文件 / 426 用例**全绿、`npm run type-check` ✓、`npm run build` ✓。**2026-09-14 负责人走查（副标题「智能推荐 · 只做建议不下单」太靠下）**：根因是 `.ai-title`/`.ai-subtitle` 未清 `<p>` 默认外边距（默认上下 1em）→ 标题块被撑到 **83.2px** 塞进 48px 顶栏，副标题落到栏底之下（top 52.9 > 栏底 60 的下半段）；已补 `margin: 0`（并一并重置 `.ai-thinking`/`.ai-network-tip` 同类隐患）。实测：副标题 top **52.9 → 37.9**（上移 15px）、标题块高 **83.2 → 31.2px** 并在顶栏内垂直居中（上间隙 7.9 / 下间隙 8.9），顶部栏仍 48px；回归 vitest **54 文件 / 426 用例**全绿、`npm run build` ✓。**同类页面提醒**：其它页面顶栏标题类均写了 `margin: 0`（如 `MessageCenterView .msg-title`），本页实现时漏了——新增页面自检项。 | 2026-09-14 |

## 2. 已完成

| ID | 任务 | 类型 | 出处 | 负责人 | 状态 | 证据 | 更新 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| TODO-USER-101 | 模拟支付动作 `PaymentActions.vue`（去支付/失败重试） | 未完成模块 | PRD 7.5、TC-PAY | 戴宇涵 | 已完成 | PR #38、feat 5d0c63b（test 30831ab 红→绿 148/148） | 2026-09-10 |
| TODO-USER-102 | 订单状态归一化补 `PENDING/COOKING/DELIVERING` 中文映射 | 打磨 | PRD 7.6 | 戴宇涵 | 已完成 | PR #38、5d0c63b；线上用户端订单页已显示"待接单" | 2026-09-10 |
| TODO-USER-103 | 首页/商家详情/购物车/地址/订单等 P0 页面与复刻（9/7 前完成） | 未完成模块 | PRD 7.2–7.4、7.16.1 | 戴宇涵 | 已完成 | main 历史提交；vitest 148 条全绿 | 2026-09-07 |
| TODO-USER-031 | **CHG-001 红框图生产（PRD 用）**：已产出 4 张红框逐区标注图并回填 PRD 7.16.1 | 未完成模块 | 清单 §6.2/§7、PRD 7.16.1 | 戴宇涵（执行：组长 AI 协作） | 已完成 | 4 张落 `docs/prd-assets/user/prototype-chg001-redpacket/`（`23_2-01-promoBarAndBlastCard`、`24_190-01-blastOverlay`、`24_441-01-blastResultCard`、`24_676-01-buyPackSheet`）；PRD 7.16.1 五行的「红框图待补」已替换为图片引用 | 2026-09-10 |
| TODO-USER-104 | 批次⑩ **订单详情页（含订单进度时间线；原订单跟踪页已并入）**按 `docs/design/exports/用户端/12-订单与支付/01-订单详情-含订单跟踪/` exact 复刻：状态头 + **五节点时间线** + 商品清单 + **金额明细（基础四行 + 优惠项按实际发生展示）** + 配送信息卡 + 订单信息卡 + 底部操作区（**「取消订单」在商家接单后保留但置灰、点击提示**；**不提供「查看配送进度」按钮**） | 未完成模块 | PRD 7.6（时间线文案↔状态值映射表）/7.16.1、契约 §5.1、TC-ORD 组、CHG-003/CHG-004 | 戴宇涵 | 已完成 | 设计稿与红框图已入库（`prototype-chg003-orderpay/26_2-01-statusBlock`、`26_2-02-mainContent`、`26_2-03-footerActions`）；**实现已落地（分支 `feat/order-detail-timeline-tdd`，待 push/PR）**：OD 组红 15 failed/16 passed → 绿 31 passed，全量 vitest 163 passed，`npm run build`（含 vue-tsc）✓；实现后补复刻对照截图；**已合并：PR #43（main `e9f7d3b`，2026-09-11）**；复刻对照截图已补（`docs/prd-assets/user/replica-compare-2026-09-14/`：09-订单详情页.png） | 2026-09-11 |
| TODO-USER-105 | 批次⑩ **支付页（待支付收银台）**按 `docs/design/exports/用户端/12-订单与支付/02-支付页/` exact 复刻：**倒计时卡**（`payDeadline`，仅支付剩余时间）+ **订单详情卡**（商品清单 + **金额明细（基础四行 + 优惠项按实际发生展示，CHG-004）**：商品小计/打包费/配送费/实付金额为恒显示的基础四行，满减优惠、红包优惠等优惠项在金额非 0 时各占一行、未发生不显示 + 商家/订单编号/下单时间/收货地址）+ **卡内取消订单入口** + 底部**「立即支付 ¥实付金额」**；含支付成功/失败跳转与倒计时失效禁用 | 未完成模块 | PRD 7.5/7.16.1、契约 §3.5/§5.2、TC-ORD 与 TC-CAN 组、CHG-003/CHG-004 | 戴宇涵 | 已完成 | 设计稿与红框图已入库（`28_2-01-payContent`、`28_2-02-itemsAndAmount`）；金额明细口径 CHG-004；依赖后端 `payDeadline` 与金额快照字段 **105a 已落地（分支 `feat/order-pay-chg003-tdd` 已 push，待 PR）**：支付页 exact 复刻 + 三处「去支付」入口接线（下单成功/详情页/列表页）+ mock 支付接口与创建状态对齐契约 **105b 已落地（同分支，待提交/推送）**：支付成功页与失败页真稿（口径见 `07-支付/03`、`04`；设计稿的「积分/支付宝/联系客服」按 PRD 明示条款替换为课程口径，失败页次按钮改为「返回订单列表」）**证据**：105a 红 17 failed/45 passed、105b 红 11 failed → 绿全量 vitest **190 passed**、`npm run build`（含 vue-tsc）✓；**待补**：三页复刻对照截图；**已合并：PR #43（main `e9f7d3b`，2026-09-11）**；复刻对照截图已补（`docs/prd-assets/user/replica-compare-2026-09-14/`：06-支付页.png） | 2026-09-11 |
| TODO-USER-002 | 批次② **取消订单确认弹层**（仅未支付/未接单可取消、取消原因必填 1–50 字、二次确认；**用户端取消入口两处**：① 支付页订单详情卡标题行右侧（文字入口，不与底部按钮并排，待支付可用）；② **订单详情页底部「取消订单」按钮**（待支付/待接单可用，商家接单后保留但置灰并提示）；**订单列表页不提供**） | 未完成模块 | PRD 7.6、契约 §3.5、TC-CAN 组 | 戴宇涵 | 已完成 | 2026-09-11 入口口径更新：取消入口统一在支付页内，列表页与详情页不提供；设计稿已明确入口在支付页卡内，弹窗设计稿已入库（`12-订单与支付/03-取消确认弹窗`，底部弹层 + 原因 chips 必填 + 0/50）；设计稿中「优惠券将原路退回」按负责人拍板删除；**已合并：PR #43（main `e9f7d3b`，2026-09-11）**；复刻对照截图已补（`docs/prd-assets/user/replica-compare-2026-09-14/`：07-取消确认弹层.png） | 2026-09-11 |
| TODO-USER-008 | **待支付倒计时**（payDeadline）与**再来一单** | 未完成模块 | PRD 6.7、契约 §3.5、TC-ORD-023/024 | 戴宇涵 | 已完成 |；**已合并：PR #43（main `e9f7d3b`，2026-09-11）**；复刻对照截图已补（`docs/prd-assets/user/replica-compare-2026-09-14/`：06-支付页.png、08-订单列表.png） | 2026-09-11 |
| TODO-USER-003 | 批次③ **评价提交页**（星级/标签/文字/图片可选）与评价 Tab 真实化 | 未完成模块 | PRD 7.7、契约 §6.2、TC-REV 组 | 戴宇涵 | 已完成 |；**已合并：PR #43（main `e9f7d3b`，2026-09-11）**；复刻对照截图待补（未覆盖：mock 种子只有 PROCESSING 订单，评价页需已完成订单 → 待真实环境补拍） | 2026-09-11 |
| TODO-USER-009 | 状态文案映射补 `CANCELLED → 已取消`，并回归全部订单状态展示 | 打磨 | PRD 7.6 | 戴宇涵 | 已完成 |； **已收口（PR #43 已实现 CANCELLED 映射；本批补全状态回归用例 T09 于 `feat/polish-batch-tdd`）**：7 个状态值中文映射 + 「已完成未评价 → 待评价」全量断言，锁住不回退 | 2026-09-11 |
| TODO-USER-015 | 商品分类列表**选中态加外圆角** | 打磨 | 负责人 2026-09-10 走查；PRD 7.3、设计系统-用户端 | 戴宇涵 | 已完成 |； **已落地（分支 `feat/polish-batch-tdd`，待 push/PR）**：分类选中态加 8px 外圆角（设计系统圆角阶梯）；纯样式项验证以复刻对照/E2E 为准 | 2026-09-11 |
| TODO-USER-017 | **支付页版式调整**：金额（价钱）移到上方通栏、不在底部栏；支付按钮铺满左右（对齐真实饿了么） | 打磨 | 负责人 2026-09-10 走查；PRD 7.5、docs/design/exports/用户端/12-订单与支付/02-支付页/ | 戴宇涵 | 已完成 |； **已落地（PR #43 的 105a 已按设计真源完成版式：金额在倒计时卡、支付按钮通栏、取消入口在卡内不与按钮并排）**；本批收口证据 | 2026-09-11 |
| TODO-USER-019 | 首页商家卡片增加**装饰性标签文字**（纯占位，不代表真实内容，不得显示为真实经营数据） | 打磨 | 负责人 2026-09-10 走查；PRD 7.2 | 戴宇涵 | 已完成 |； **已落地（分支 `feat/polish-batch-tdd`）**：商家卡装饰性标签（`品质优选`）带 `data-decorative` 标记与注释，纯占位、不得当作经营数据展示 | 2026-09-11 |
| TODO-USER-020 | 商家详情页底部"**去支付**"不用胶囊按钮（改为通栏/直角形态） | 打磨 | 负责人 2026-09-10 走查；PRD 7.3/7.4、设计系统-用户端 | 戴宇涵 | 已完成 |； **已落地（分支 `feat/polish-batch-tdd`）**：商家详情「去支付」由胶囊（20px）改直角（4px）+ 通栏形态；纯样式项验证以 E2E/截图为准 | 2026-09-11 |
| TODO-USER-021 | "**预估价**"三字发生换行、观感不佳，需处理不换行或调整文案长度 | 打磨 | 负责人 2026-09-10 走查；PRD 7.3 购物车栏 | 戴宇涵 | 已完成 |； **已落地（分支 `feat/polish-batch-tdd`）**：`.product-price-est` 加 `white-space: nowrap`，不再换行 | 2026-09-11 |
| TODO-USER-018 | **购物车抽屉加高**（展开态可视高度不足） | 打磨 | 负责人 2026-09-10 走查；PRD 7.3 购物车弹层 | 戴宇涵 | 已完成 |；**已落地（分支 `feat/polish-batch-tdd`，待 push/PR）**：购物车抽屉展开态 `max-height` 40vh → 56vh（保留内部滚动）；纯样式项验证以 E2E/截图为准 | 2026-09-11 |

| TODO-USER-016 | 加购动画：从商品"加号"到购物车栏的**抛物线抛球过渡** | 打磨 | 负责人 2026-09-10 走查；PRD 7.3 加购交互 | 戴宇涵 | 已完成 | 2026-09-13 **已落地（分支 `test/design-fidelity-gate`）**：新增 `utils/flyToCart.ts`（小球挂 `document.body` + 内联样式；Web Animations 三关键帧起→抬升中点→终点缩小成抛物线；finish/cancel 与 `duration+100ms` 双保险清理；同一次加购只留一个小球；无 WAAPI 环境静默跳过不残留）+ StoreDetailView 两处「加号」点击即抛（即时反馈、不等接口）；终点取购物车栏用**模板 ref**（`document.querySelector` 在游离挂载下单测取不到、会静默跳过） **证据**：flyToCart 5 条 + StoreDetailView T73，红（骨架 4 failed / 加实现后 T73 红）→ 绿；全量 vitest **343 passed**、`npm run build`（含 vue-tsc）✓、全量 E2E **21 项全绿** **未收口**：PR 待开；真机观感与「抛球终点落在购物车图标区」的观感确认仍建议在演示前瞄一眼（纯动画，无接口与状态变更） | 2026-09-13 |

| TODO-USER-005 | 批次⑤ **搜索结果页（**PRD 7.16.1 已于 2026-09-11 补「分类商家列表页」两行逐区行，复用本页红框图；分类来源为店铺自身 `categories`，平台级 10 类本期不做**）**（商家/商品分组、排序 综合·销量·距离、分页） | 未完成模块 | PRD 6.3/7.17、契约 §3.6、TC-SRC 组 | 戴宇涵 | 已完成 | 2026-09-13 **搜索结果页已落地（分支 `test/design-fidelity-gate`）**：新增 `views/user/SearchResultView.vue`（搜索头部：关键词来自页面参数并保留、回车/按钮提交、空关键词只提示不请求；定位文案；排序筛选栏默认综合、切换即带参重搜并高亮；商家结果列表含店名/评分/月售/配送时长/距离/起送价/配送费与促销标签，金额两位小数、字段缺失只隐藏；加载骨架 / 空态含回首页 / 失败保留关键词可重试；**请求序号守卫**保证慢响应不覆盖后发结果）与搜索域替身 `mocks/search.ts`（契约 §3.6 逐条：keyword 命中商家名或商品名、categoryId 命中范围内收窄且无匹配不 404、排序三值与分页 1/10、非法入参 400、空关键词与无命中返回空列表）；首页搜索框由占位改为真入口（PRD：点击搜索框进入搜索结果页）；新增 `utils/location.ts` 提取演示定位常量 **证据**：SR 10 条 + SM 8 条，红 **15 failed/20 passed** → 绿全量 vitest **322 passed**、`npm run build`（含 vue-tsc）✓、全量 E2E **21 项全绿**（构建曾拦下 3 处测试文件严格类型错误，见 `test:` 修正提交） **未收口**：② **搜索页（输入页：搜索历史与热门词）与分类商家列表页（`GET /stores?categoryId=` 分页/排序）本批未做**——PRD 7.16.1「首页-搜索框」行明确「搜索历史与热门词本期实现」，分类商家列表页两行亦为交付范围，已新开 `TODO-USER-107` 承接；③ 商品结果按 PRD 无逐区行故未渲染（契约仍返回 products，SR-10 以负向断言锁定）；④ 真实后端批次⑤（`TODO-BE-007`）未实现，当前走替身；⑤ 复刻对照截图已补（`docs/prd-assets/user/replica-compare-2026-09-14/`：02-搜索结果页.png）  ****已合并：PR #59（main `d034c48`，2026-09-13）**；合并后 main 复跑 R1 = **356 passed** + `npm run build`（含 vue-tsc）✓、全量 E2E **21 项全绿**** | 2026-09-13 |
| TODO-USER-007 | 批次⑧ **评价图片上传**（最多 3 张、单张 ≤2MB、失败重试；**内嵌评价订单页，不新增弹窗或页面**） | 未完成模块 | PRD 7.7/7.12、契约 §10.1、TC-IMG 组 | 戴宇涵 | 已完成 | 2026-09-13 **已落地（分支 `test/design-fidelity-gate`）**：图片区内嵌评价订单页（不新增弹窗或页面）——本地选择 → 逐张上传 → 缩略图三态（上传中/成功/失败）；**失败保留本地预览并可重试**、可单项删除、**达 3 张后添加入口消失**；选择时逐张本地校验（`utils/reviewImage` 纯函数：类型白名单、≤2MB、0 字节、张数上限）不合格立即提示且不发请求；提交时**只带上传成功的 url**、有图仍在上传则阻止提交（避免静默丢图）；替身 `mocks/file.ts` 按契约 §10.1 校验并生成文件名（不用用户原始名，防路径穿越）、`mocks/review.ts` 兜底拒绝第 4 张 **证据**：RI 4 + FM 4 + IMG 7（TV-7 占位口径演进），实现 stash 后在 `test:` 提交复验红 **10 failed/13 passed** → 绿三个文件 23 passed、全量 vitest **336 passed**、`npm run build`（含 vue-tsc）✓、全量 E2E **21 项全绿**；构建拦下 2 处严格类型错误已修 **未收口**：② 权限类（未登录 401/越权 403/脚本类型拒绝）属后端职责，替身无会话态不模拟 → 留待 real 模式联调（TC-IMG-005）；③ 真实后端批次⑧（`TODO-BE-010`）未实现，当前走替身；④ 复刻对照截图待补（未覆盖：同上，评价页图片区需已完成订单 → 待真实环境补拍）  ****已合并：PR #59（main `d034c48`，2026-09-13）**；合并后 main 复跑 R1 = **356 passed** + `npm run build`（含 vue-tsc）✓、全量 E2E **21 项全绿**** | 2026-09-13 |
| TODO-USER-011 | **SHOW-QA-001** 购物车抽屉贴底回归确认（fix 386a0d0 已在库，待真机安全区确认） | 缺陷 | 缺陷跟踪表 SHOW-QA-001 | 戴宇涵 | 已完成 | 2026-09-13 **桌面侧完成 + 修掉一处安全区隐患**：① 既有贴底断言因缺登录前置长期失效，已修并复跑通过（见 `test:` 提交）；② **代码级核对安全区**发现 `.cart-bar` 是唯一用「固定 `height: 64px` + `padding-bottom: env(safe-area-inset-bottom)`」的底栏，`border-box` 下安全区会压扁内容（iPhone 底部指示条约 34px → 内容仅余 30px），TabBar 与下单页底栏都是「安全区加在总高/padding 上」→ 已改 `min-height` + 合并 padding，并把左分类栏与商品列表底部留白改为 `calc(64px + env(safe-area-inset-bottom))`（桌面 `env()=0`，几何与此前一致） **证据**：E2E 抽屉用例通过、全量 E2E **21 项全绿**、全量 vitest **343 passed**、`npm run build` ✓ **未收口（真机清单，需人工）**：① 带底部指示条的真机上打开商家详情，确认购物车栏内容不被压扁、抽屉底边仍贴视口底、列表最后一项不被底栏遮挡；② 缺陷台账 `SHOW-QA-001` 状态流转写权归测试联调同学  ****已合并：PR #59（main `d034c48`，2026-09-13）**；合并后 main 复跑 R1 = **356 passed** + `npm run build`（含 vue-tsc）✓、全量 E2E **21 项全绿**** | 2026-09-13 |
| TODO-USER-012 | **SHOW-QA-002** `.app-shell` 430px 居中失效（px→vw 换算）修复 | 缺陷 | 缺陷跟踪表 SHOW-QA-002 | 戴宇涵 | 已完成 | 2026-09-13 **修复已落地（分支 `test/design-fidelity-gate`）**：`vite.config.ts` 的 pxToViewport 增 `propList: ['*', '!max-width']`，让 max-width 保持 px **修复方向依据**：问题记录 §3 的候选①（让该条不参与换算）——先清点全库 px `max-width` 得**仅 5 处且同为 430px**（MainLayout/BlankLayout/AddressList/AddressEdit/StoreDetail，语义都是桌面预览上限），属性级排除一次覆盖全部且行为唯一，优于候选②的媒体查询；**未改任何数值** **证据**：新增桌面视口断言（1280 下壳宽 430 且居中）红 → 绿；保真度巡检 **18 项全绿**、全量 vitest **304 passed**、`npm run build` ✓、构建产物 `dist/assets/*.css` 复查 `max-width:430px` 保持 px **未收口**：② 桌面观感人工确认；③ 缺陷台账 SHOW-QA-002 状态流转写权归测试联调同学  ****已合并：PR #59（main `d034c48`，2026-09-13）**；合并后 main 复跑 R1 = **356 passed** + `npm run build`（含 vue-tsc）✓、全量 E2E **21 项全绿**** | 2026-09-13 |
| TODO-USER-013 | **SHOW-QA-004** 点餐区吸顶分类条目栏与上方"点餐/评价"Tab 栏之间存在缝隙，一定条件下抖动（滚动交接区域） | 缺陷 | 缺陷跟踪表 SHOW-QA-004；项目规则 §5 滚动交接、PRD 7.3 | 戴宇涵 | 已完成 | 2026-09-13 **抖动已修（迟滞），死区取值待确认**：`updateOuterPinned` 由单阈值改为带滞后的双阈值（越过吸顶线才锁定；锁定后须回退超过死区才解除），消除「Tab 栏 top 在吸顶线附近来回 → 锁定态反复翻转 → 同时切换右侧列表可滚状态 → 滚轮时而作用外层时而作用内层」的抖动（机制见本文件 15:40 实测）；死区 `PIN_RELEASE_DEADBAND = 8px`（问题记录建议 8–16px）**取值属交互口径、暂定 8px 待负责人/测试联调确认** **证据**：新增 T74（带 `.app-main` 宿主挂载，经「点击分类」路径驱动刷新）断言「回退 5px 不翻转、超过死区才解除」，红 → 绿；全量 vitest **343 passed**、`npm run build` ✓、E2E **21 项全绿** **未收口**：① 死区取值待确认；③ 缺陷台账 SHOW-QA-004 状态流转写权归测试联调同学  ****已合并：PR #59（main `d034c48`，2026-09-13）**；合并后 main 复跑 R1 = **356 passed** + `npm run build`（含 vue-tsc）✓、全量 E2E **21 项全绿**** | 2026-09-13 |
| TODO-USER-023 | **重建"保真度巡检"断言脚本（可复跑门禁）**：结构断言 + 多视口断言，见本文件 §4.1 实施说明 | 技术债 | 负责人 2026-09-10 决议（结课汇报"重建后再讲"）；`docs/frontend/UI复刻经验与执行约定.md` §2.2/§3.3；`docs/record/raw/2026-09-08/组长-1847.md`（原 23/23 口径） | 戴宇涵 | 已完成 | 2026-09-13 **脚本已重建并复跑全绿（分支 `test/design-fidelity-gate`）**：新增 `e2e/design-fidelity.spec.ts`（原 `test-results/design-fidelity/verify.cjs` 因落 gitignore 目录已丢失）；结构断言改为**按区块分层判定表**并逐项标出处（A 类通栏无壳 9 区、B 类通栏且按导出稿保留局部圆角/阴影 2 区、C 类白底卡片 1 类 3 项——CHG-003 起订单详情页为 8px 圆角卡片，旧口径「一律满宽+零圆角」对 3 处已不成立，故不再照搬）；多视口 320/360/375/390/430 各断言无横向溢出、壳宽=视口宽、底栏贴底与最底像素归属、抽样文字不裁切；圆角比较带 0.5px 容差（px→vw 使 8px 实测 7.9989px） **证据**：本脚本 **17 项全绿**（打印「通过 N / 受检 N」）、全量 E2E **20 项全绿**（原 3 条未受影响）、`npm run build`（含 vue-tsc）✓ **附带修复**：脚本页快照发现真实缺陷——商家详情页顶部栏仍渲染设计稿第三方品牌名，已按课程口径改为「轻量外卖」，红 1 failed/19 passed → 绿 302 passed（`test:` `666bfb3` → `fix:` `007befc`） **未收口**：② §4.1 验收要求「PR 描述贴出命令/通过数/各视口结论/截图路径」待建 PR 时补齐；③ 截图落 `test-results/`（不入库）  ****已合并：PR #59（main `d034c48`，2026-09-13）**；合并后 main 复跑 R1 = **356 passed** + `npm run build`（含 vue-tsc）✓、全量 E2E **21 项全绿**** | 2026-09-13 |
| TODO-USER-024 | **设计稿像素比对脚本与一次性取证**：掩膜数据区 + 校正设计稿状态栏偏移，见本文件 §4.2 实施说明 | 技术债 | 负责人 2026-09-10 决议（结课汇报需要"可复跑"的像素取证）；结论口径见本文件 §4.2 | 戴宇涵 | 已完成 | 2026-09-13 **脚本已落地并跑出结果（分支 `test/design-fidelity-gate`）**：新增截图脚本 `frontend/user-h5/scripts/shot-design-compare.cjs`（393×852@2x、关动画、等字体就绪 + 静置 1.5s，同出 bands.json）与比对脚本 `tools/design-diff/pixel_diff.py`（Pillow + numpy，零 npm 新增依赖）；三条口径按 §4.2 落实并注释：53px 状态栏偏移校正（@2x 106px）、商家卡等数据驱动区必须掩膜、整图百分比只对画布 393 精细版有意义；脚本内明确「取证用、不作 CI 门禁」 **复现结果与参考数字一致**：整图原始 **30.14%**（参考 30.1%）、53px 校正后 **18.6%**（参考 19.1%，差 0.5pp 在容差内）、最优位移 dx=1/dy=0、掩膜商家卡后 **10.96%**（参考静态外框 10.6%） **未收口**：① §4.2 要求的参考实现目录 `docs/_archive/presentation/**/原型-截图比对/` 在**本机不存在**（`docs/_archive/**` 已 gitignore），故「与参考实现一致」只能与 §4.2 记录的历史数字比对（已达成），无法与脚本本身对拍；② 分带取实现页面的实测 y 区间（非设计稿 CSS 切带），已在脚本注释中说明该实现差异；③ PR 待开  ****已合并：PR #59（main `d034c48`，2026-09-13）**；合并后 main 复跑 R1 = **356 passed** + `npm run build`（含 vue-tsc）✓、全量 E2E **21 项全绿**** | 2026-09-13 |

| TODO-USER-113 | **`payDeadline` 缺失时不得阻断支付（口径修订）** | 缺陷 | 契约 §3.5（2026-09-14 实现回写）、PRD 7.5、负责人 2026-09-14 裁定 | 戴宇涵 | 已完成 | 2026-09-14：前端原口径「`payDeadline` 未返回则禁用支付」在后端缺该字段期间导致**线上支付页两个按钮全禁用、整页点不动**（P0 支付链路断死）。修订为：字段缺失只显示 `--:--`、**不禁用支付**，仅「已过期」才禁用；同步修替身（超时判定改由 `createdAt` + 15 分钟派生，与真实后端一致，原按字段缺失误判超时）。**证据**：TD-3 改写后红（缺字段仍被禁用）→ 绿（可支付并到支付成功页）；全量 vitest **49 文件全绿**、`npm run type-check` ✓、`npm run build` ✓。后端侧见「后端已实现」说明与 `TODO-BE` 侧登记。**2026-09-14 真后端复验（本机 real 模式 5174 代理至线上后端，后端仍为 9/13 版本、尚未返回 `payDeadline`）**：支付页倒计时显示 `--:--`，**「立即支付」与「演示支付失败」两个按钮均可用**（修复前双双禁用、整页点不动）；点「立即支付」→ 支付成功页（¥97.00 / 模拟支付 / 真实时间戳）→ 订单列表该单变为「待接单」，P0 支付链路恢复可用；倒计时将在后端 `payDeadline` 随发布上线后自动显示。**2026-09-14 22:00 发布（`20260914-a2f15b1`）后线上复验通过**：新单 `o1257` 支付页倒计时 **14:58 → 14:53 真实走秒**（`payDeadline` 已生效），「立即支付 ¥41.00」「演示支付失败」均可用；旧待支付单已超时则显示「已失效」且按钮禁用（口径正确）。同轮验证取消链路：`o1257` → `CANCELLED` / `cancelReason=不想要了` / `cancelledBy=USER` / `cancelledAt=2026-09-14 22:17:59`，订单列表显示「已取消」（详情见 `docs/testing/问题记录/2026-09-14-用户端真后端联调发现.md`） | 2026-09-14 |

| TODO-USER-014 | **SHOW-QA-005** 首页商家卡片商品图仅 2 张（不足 3 张）时右侧仍渲染羽化，视觉像被裁切 | 缺陷 | 缺陷跟踪表 SHOW-QA-005；PRD 7.2 | 戴宇涵 | 已完成 | 2026-09-13 **修复已落地（PR #58，main `ff35d7d`）**：根因为预览区右缘渐隐 `product-fade` 无条件渲染（与商品数量无关），而容器 `width: max-content` + 负右外边距只在展示 3 张时真正外溢——改为 `v-if="previewsFor(store).length >= 3"`，不足三张不再渲染；新增 T72（3 张时渲染，锁既有正确行为）与 T72b（仅 2 张时不渲染，复现缺陷）+ `dispatchTwoProducts` 替身（截断 m002 商品为 2 个） **证据**：红 1 failed/16 passed（`test:` `23a88ac`）→ 绿 `HomeView.spec.ts` 17 passed、全量 vitest **303 passed**、`npm run build`（含 vue-tsc）✓（`fix:` `b7da45e`） **未收口**：② 视觉确认待补——几何/视觉类缺陷按 `前端架构设计与工程约定` §6 以真机或 E2E 截图对照为准（2 张 vs 3 张店铺），问题记录亦提示需确认种子中是否有「2 张商品图」的店铺（本次以替身构造）；③ 缺陷台账 `SHOW-QA-005` 状态流转（新建→待回归→已关闭）写权归测试联调同学，本轮未改台账  **已合并：PR #58（main `ff35d7d`，2026-09-13）**；合并后 main 复跑 R1 通过；本行随 #59 批次一并收口 | 2026-09-13 |
| TODO-USER-001 | 批次① 确认订单页**金额明细（基础四行 + 优惠项按实际发生展示）**（商品小计/打包费/配送费/实付；满减、红包等优惠项按实际发生）并展示 `deliveryFee` 快照 | 未完成模块 | PRD 7.4、契约 §3.5、TC-ORD-022 | 戴宇涵 | 已完成 | 2026-09-12 **已落地（分支 `feat/confirm-amount-tdd`，待 push/PR）**：确认订单页接 `normalizers.buildAmountLines`（与订单详情页、支付页同一唯一出口，基础四行 + 优惠项非 0 才出行）、实付兜底公式补配送费（`deliveryFee` 取店铺配置，未配置按 0）、订单 mock 按定稿公式计价并把 `deliveryFee` 写入金额快照（前端替身先行，真实计价归 `TODO-BE-001`）；**优惠项行待计价与红包选择接入后按实际发生传入**（当前无数据源，未发生不渲染）**证据**：TA 组红 9 failed/34 passed → 绿全量 vitest **247 passed**、`npm run build`（含 vue-tsc）✓；**待补**：复刻对照截图 2026-09-12 **已合并：PR #50（main `f4a3a9c`，2026-09-12）**——CI 五项全绿（提交规范检查 / 用户端构建+单测 / 商家端 / 后端构建 / 后端集成测试），合并后 main 复跑 R1 = 263 passed + `npm run build` ✓ 2026-09-12 **替身已镜像七步计价（负责人指示「按组长规定的文档做，和真实的后端对接起来」）**：`mocks/order.ts` 新增 `priceOrder` **逐行镜像后端 `PricingService`（PR #48）**——① 小计 → ② 满减取最大满足档 → ③ 新客立减（该店首单） → ④ 免配送费门槛（基数=原始小计）→ ⑤ 会员折扣（本期非会员）→ ⑥ 红包（前端选择未接入暂为 0）→ ⑦ 实付不小于 0；优惠配置按 `backend/database/seed/seed.sql` 镜像（仅 m002：满 20 减 2、满 40 减 5、新客 3、满 30 免配送费）；订单快照写入 `fullReductionAmount`/`newCustomerAmount`/`memberDiscountAmount`/`couponAmount`/`deliveryFeeDiscount`；新增 PR-1~PR-5 用例锁七步（最大档不叠加 / 未达门槛无优惠 / 门槛基数为原始小计 / 首单立减 / 无配置店铺无优惠行） **证据**：PR 组红→绿，全量 vitest **269 passed**、`npm run build` ✓。**对接核对（对后端代码）**：接口路径与请求体一致（`POST /orders` 含 `couponId`/`idempotencyKey`，前端暂未用）；订单响应字段与前端类型一致；发现并修正一处真实模式缺陷——`reviewed` 缺失时不再补 false（原会把已评价订单误标「待评价」并给出必然 409 的去评价入口），补用例 TV-13；另发现 `payDeadline`/`reviewed` 后端未返回且未登记（已登记本文件 §3 阻塞区）  **收口说明（2026-09-13）**：代码已合入 main（PR #50（main `f4a3a9c`））；按 9/11 既有做法移入已完成区，**遗留项如实保留在证据列**（复刻对照截图、真实后端联调——后者依赖 4001 演示环境恢复与后端批次就绪） | 2026-09-13 |
| TODO-USER-004 | 批次④ **消息列表 + 聊天详情 + 订单详情"联系商家"入口**（双端未读） | 未完成模块 | PRD 7.8、契约 §6.1、TC-MSG 组 | 戴宇涵 | 已完成 | 2026-09-10 **004a 已落地（分支 `feat/messages-msg-tdd`，从 main 起，待 push/PR）**：消息中心页（通知三类 + 商家会话列表 + 相对时间 + 会话未读角标 + 全部已读/单条已读 + 空态）、`messageApi` 与消息/通知替身（契约 §6.1/§3.9，含 `orderId` 过滤、空白与超长消息 400、标记已读幂等、未知会话 404）、底部导航消息红点接真实通知未读数（失败降级）；契约 §6.1 同步补 `orderId` 查询参数口径（R7） **证据**：TW 组红 10 failed/1 passed → 绿全量 vitest **233 passed**、`npm run build`（含 vue-tsc）✓；**004b（聊天详情 + 发送 + 联系商家入口）未做**  **004b 已落地（分支 `feat/chat-detail-msg-tdd`，叠加在 004a 分支上，待 push/PR）**：聊天详情页（会话头部 + 订单状态卡（状态/编号/查看订单）+ 消息时间线双侧气泡与时间 + 底部快捷回复与输入发送，进入即标记已读、发送成功置底清空、失败保留输入可重试；设计稿「联系骑手」按 PRD 不实现）+ 订单详情「联系商家」按 `orderId` 取会话进入（无会话给提示）+ `chat-detail` 路由 **证据**：TX 组红 8 failed/17 passed → 绿全量 vitest **241 passed**、`npm run build`（含 vue-tsc）✓；**待收口**：通知点击跳转 `relatedId` 的交互规则待确认、真实后端联调  **收口说明（2026-09-13）**：代码已合入 main（PR #45 / #46（main `7cd4476` / `714cfb2`））；按 9/11 既有做法移入已完成区，**遗留项如实保留在证据列**（复刻对照截图、真实后端联调——后者依赖 4001 演示环境恢复与后端批次就绪）。**2026-09-14 真后端复验：消息中心在线上不可用**——`GET /conversations` 200/19 条但页面未渲染（通知接口 404 连带丢弃全部会话 + 会话缺 `storeId` 时整页崩），已登记 `TODO-USER-115`（缺陷 BUG-20260914-004 / BUG-20260914-005，明细见 `docs/testing/问题记录/2026-09-14-用户端真后端联调发现.md` §2/§3）；聊天详情与发送链路待 `TODO-USER-115` 修复后再验 | 2026-09-13 |
| TODO-USER-006 | 批次⑥ **收藏页、会员权益页、确认订单红包选择**（收藏页/会员权益页/红包页按导出稿复刻；**确认订单页红包选择区在 `06-订单/04-确认订单` 导出稿中不存在，需按 `设计系统-用户端.md` 新建**，与取消确认页同口径） | 未完成模块 | PRD 6.11/7.10、TC-FAV/MBR/CPN 组；`UI复刻经验与执行约定.md` §4.1/4.2（§4.2 的配送页部分已随 CHG-002 失效，待对方文件同步） | 戴宇涵 | 已完成 | **交付**：收藏页、会员权益页、确认订单页红包选择区（后者的设计稿无该区块，按 `设计系统-用户端.md` 新建）。**证据**：新增 `favorite`/`member` 两域 api 与替身（契约 §3.7：倒序、重复收藏幂等、缺参 400、门店不存在 404、取消未收藏幂等 200）、`favorites`/`member` 路由、我的页两入口与商家详情收藏 icon、TabBar 归属高亮；红包选择区走 `GET /me/coupons/available?storeId=&amount=`，选中后经 `buildAmountLines` 多一行「红包优惠」并随 `couponId` 下单（七步第 ⑥ 步）。红绿：FA/FAM/MB 红 **15 failed** → 绿 262 passed；CPN/CPM 红 **6 failed** → 绿 **301 passed**。**合并 PR #50（main `f4a3a9c`）与 PR #56（main `c9376c9`）**，CI 五项全绿，合并后 main 复跑 263 passed + `npm run build` ✓。**待拍板（已提请复核）**：① PRD 871「续费按钮给演示反馈」与 TC-MBR-005「页面不出现开通/续费入口」冲突——本实现按 PRD（保留按钮、零接口调用、提示含「演示」），待裁定后按 R7 同步；② 契约 §3.7 收藏对象缺 PRD 873 要求的促销标签/配送时长/距离——按「返回才展示、缺失即隐藏」实现并在 `types.ts` 就近登记缺口，待后端 A 确认字段命名；③ 三处课程口径替换（第三方品牌名→「轻量外卖」、权益项不出现积分、会员有效期无契约字段故不渲染）。**遗留**：复刻对照截图已补（`docs/prd-assets/user/replica-compare-2026-09-14/`：10-收藏页.png、11-会员权益页.png、05-确认订单页-红包选择.png）；真实后端联调（依赖 `TODO-OPS-009` 发布）。（2026-09-14 依 `TODO-USER-106 ③` 整理证据列，未改内容与状态） | 2026-09-14 |
| TODO-USER-028 | **CHG-001 红包页增量改造**（**不重做**：既有可用红包列表与底部导航保持不动）：新增顶部「加量红包省更多／去购买」通栏（打开买红包浮窗）+「天天必爆」活动卡（奖池预览 +「免费爆1次」按钮）；标题按设计稿为「天天红包」；**含 2 张纯展示占位券**（不可点用） | 未完成模块 | CHG-001 二修、PRD 7.10/7.16.1、契约 §3.10、设计真源 `docs/design/exports/用户端/11-天天必爆/01-红包页/` | 戴宇涵 | 已完成 | **交付**：红包页增量改造（顶部「加量红包省更多／去购买」通栏与买红包浮窗、天天必爆活动卡与 18.8 主视觉、2 张纯展示占位券），按导出稿 `11-天天必爆/01-红包页` 复刻，列表走 `GET /me/coupons`（金额/门槛/范围徽标按接口返回，缺名隐藏；过期券灰化）。**证据**：替身实现契约 §3.8 列表与 §3.10 买套餐（pack49/pack99 面额与 7 天有效期）；入口为我的页「红包卡券」+ TabBar 归属高亮；红绿：CP/CPM 红 **12 failed** → 绿 **283 passed**；**合并 PR #54（main `638eeea`）**。**交叉引用**：本页「天天必爆」的过渡态/结果卡由 `TODO-USER-029` 承接、首页红包入口由 `TODO-USER-030` 承接（均已完成）。**遗留**：复刻对照截图已补（`docs/prd-assets/user/replica-compare-2026-09-14/`：12-红包页.png）；真实后端联调（依赖 `TODO-OPS-009` 发布）。（2026-09-14 依 `TODO-USER-106 ③` 整理证据列，未改内容与状态） | 2026-09-14 |
| TODO-USER-029 | **CHG-001 爆红包全屏动效 + 结果卡**：点「免费爆1次」/消耗券爆 → 全屏覆盖动效（横幅 + 「爆」按钮 + 欧气进度）→ 结果卡（金额 + 满减 + 「限今天23:59前使用」+「去使用，立即抵扣」）；≤3.5 秒可跳过；失败原地重试；**音效接入与静默降级**；无免费次数且无可爆券时提示去购买 | 未完成模块 | CHG-001 二修、PRD 7.10、契约 §3.10、TC-RBP-003/004/009/013、设计真源 `docs/design/exports/用户端/11-天天必爆/02、03/` | 戴宇涵 | 已完成 | **交付**：爆红包全屏浮层三态（过渡态金光放射 + 金色「爆」按钮 + 欧气进度、结果卡、阻塞态与失败原地重试），按导出稿 `11-天天必爆/02、03` 复刻；音效以 Web Audio 合成并 **try/catch 静默降级**。**证据**：替身实现契约 §3.10 爆一次（免费爆新增／消耗券爆替换式／爆出券当天 23:59:59 到期／按 §10.5 第 2 条 10 档权重命中、随机源可注入种子）；红绿：RB/RBM 红 **11 failed** → 绿 **294 passed**；**合并 PR #55（main `879ee01`）**。**遗留**：复刻对照截图已补（`docs/prd-assets/user/replica-compare-2026-09-14/`：13-爆红包浮层.png）；「今日免费爆是否可用」仍无查询来源（归口后端，见本文件 §3 阻塞区）。（2026-09-14 依 `TODO-USER-106 ③` 整理证据列，未改内容与状态） | 2026-09-14 |
| TODO-USER-030 | **CHG-001 首页红包入口改真入口**：「天天爆红包」由占位装饰改为跳红包页；**平台级分类与宫格分类过滤不做** | 未完成模块 | CHG-001、PRD 6.1/7.10、契约 §10.5 第 9 条 | 戴宇涵 | 已完成 | **交付**：首页「天天爆红包」由占位装饰改为真入口（`GridCell.entry` 标记并接线跳红包页，未登录由红包页自身与路由守卫引导）；平台级「点餐分类 10 类」与宫格分类过滤按 CHG-001 本期不做。**证据**：T71 红→绿，全量 vitest **301 passed**、`npm run build` ✓；**合并 PR #56（main `c9376c9`）**。**遗留**：复刻对照截图已补（`docs/prd-assets/user/replica-compare-2026-09-14/`：01-首页.png）；真实后端联调（依赖 `TODO-OPS-009` 发布）。（2026-09-14 依 `TODO-USER-106 ③` 整理证据列，未改内容与状态） | 2026-09-14 |
| TODO-USER-106 | **用户端板块整理**（复核报告 verify-03 的 F-05/F-07/F-10）：① 补 `TODO-USER-025` 证据（取消确认弹层设计稿与红框图已入库 `12-订单与支付/03-取消确认弹窗`、`prototype-chg003-orderpay/27_2-01-cancelSheet.png`）；② 把**已冻结**行（`TODO-USER-010` 订单跟踪页、`TODO-USER-026` 配送原型）从「进行中」区移入「阻塞与待确认」区；③ 个别行「更新」列的多日期粘连与叙述改回日期字段 | 打磨 | 待办规则 `docs/todo/README.md`、复核报告 `docs/_archive/doc-audit-2026-09-11/verify-03-todo.md` | 戴宇涵 | 已完成 | 2026-09-13 **① 完成**：`TODO-USER-025` 已补证据（设计稿 + 红框图入库路径 + 清单 §6.2「已提供（本期）」），并顺带为 `TODO-USER-027` 补「口径调整、不需独立原型」的说明；**② 完成**：`TODO-USER-010`、`TODO-USER-026` 两条已冻结行由「进行中」区移入「阻塞与待确认」区（本区新增 2 行）；**③ 完成（2026-09-14）**：`TODO-USER-004` 的叙述已从「更新」列移回「证据」列；`006/028/029/030` 四行已整理——删除已失效的中间态描述（「分支 X，待 push/PR」等，代码均已合并入 main）、证据列改为「交付 / 证据（PR 与提交号、红绿数字、设计真源）/ 遗留项」结构，更新列统一为单日期。整理用脚本按「分区 + 列数」双条件定位（限定在 §2 与 §3 之间）、**守卫不通过即中止且不写入**，避免 9/13 那次按 ID 前缀误改阻塞区同 ID 行的教训重演；整理后 §1/§2/§3 三区结构自检全部通过 | 2026-09-14 |
| TODO-USER-027 | **PROTO-P2-004 图片上传原型**（上传、预览、失败重试、删除/替换） | 未完成模块 | 同上 §6.2 PROTO-P2-004；负责人 2026-09-10 拍板全补 | 戴宇涵 | 已完成 | **交付（按口径）**：依 2026-09-11 负责人拍板，用户端上传态**内嵌「评价订单页」、不新增弹窗或页面、按设计系统实现；商家端商品图上传不需设计稿** → 本行「出图」交付物按口径取消，无需设计资产，清单 §6.2 该行备注已同步该口径。**功能实现与证据**：见 `TODO-USER-007`（已完成并合并）——`ReviewOrderView.spec.ts` **IMG 组 7 条全绿**（IMG-1 上传并在页面预览、提交带上传后 url；IMG-2b 仍在上传时阻止提交；IMG-3a/3b 类型与 2MB 校验且不发请求；IMG-4 失败保留本地预览并可重试；IMG-6 删除为本地操作、不调接口；IMG-7 三张上限后添加入口消失）；2026-09-14 复跑该文件全绿。**一处口径说明**：原范围写作「删除/替换」，本期**替换＝先删后加**（契约 §10.1 只提供上传、无删除接口，删除为本地操作）；商家端商品图上传由 `TODO-MER-006` 覆盖。 | 2026-09-14 |
| TODO-USER-025 | **PROTO-P2-001 取消订单原型**（用户取消确认、取消成功/失败） | 未完成模块 | `docs/project/用户待补充资料与原型清单.md` §6.2 PROTO-P2-001；负责人 2026-09-10 拍板全补 | 戴宇涵 | 已完成 | **交付**：取消确认弹层设计稿与红框图已入库（`docs/design/exports/用户端/12-订单与支付/03-取消确认弹窗/`、`docs/prd-assets/user/prototype-chg003-orderpay/27_2-01-cancelSheet.png`）；清单 §6.2 该行状态为「已提供（本期）」。**范围收口（2026-09-14 主责人确认）**：原范围「取消成功/失败」**不以独立页面表达**——PRD 7.6 无该页要求、设计稿亦无独立成功/失败屏；实现为「提交成功 → 提示并回订单列表；被拒（已接单/已取消，契约 §3.5 返回 409）→ 弹层内提示原因并通知页面刷新」。**证据（2026-09-14 复跑）**：`CancelOrderSheet.spec.ts` **TP-1~TP-7 全绿**（含 TP-4 提交成功、TP-5 被拒 409 emit rejected）；`OrderPayView.spec.ts` TP-8（卡内入口取消成功后回订单列表）/TP-9（已取消订单进支付页转详情）；`OrderDetailView.spec.ts` OD-3（商家接单后取消按钮置灰并提示「商家已接单，无法取消」）/OD-6（已取消状态头 + 原因 + 时间线置灰）。**归属说明**：原范围中的「商家端取消状态」属 `TODO-MER-002`（商家端板块），不在本行。 | 2026-09-14 |
| TODO-USER-108 | **爆红包在真实模式爆不出数额**：`POST /me/coupons/blast` 响应为契约 §3.10 的**扁平对象**（券字段 + `tierIndex` + `freeBlast`），而前端类型与替身按 `{ coupon, tierIndex, free }` 嵌套解析 → 真实模式下结果卡拿不到金额、直接落到失败态（用户侧表现为「购买后也爆不出具体红包数额」） | 缺陷 | 契约 §3.10、PRD 7.10、TC-RBP 组 | 戴宇涵 | 已完成 | 2026-09-14 定位与修复：新增 `normalizeBlastResult`（`services/normalizers.ts`，唯一出口适配，已是嵌套形态则原样透传）并在 `services/api/coupon.ts:blastCoupon` 接入；新增类型 `CouponBlastRecord`（契约扁平形状）。**证据**：BL-1~BL-3 红（`normalizeBlastResult is not a function`）→ 绿；全量 vitest **49 文件全绿**、`npm run type-check` ✓、`npm run build` ✓；**真后端端到端**：本地 real 模式（5174 代理至 `82.157.137.114:4001`）点「消耗一张红包再爆」结果卡显示 `¥15 / 满25元可用 / 限今天 23:59 前使用`（修复前为失败态）| 2026-09-14 |
| TODO-USER-111 | **爆红包把「业务拒绝」显示成「网络异常，请重试」**：`burst()` 的 catch 只把 409 归为阻塞态、其余一切归为失败态；而契约 §3.10 下 404「红包不存在」/「他人券」、400 等同样是**业务拒绝**（本次爆不成，但券列表/可爆性已变），被误报为网络异常且**不刷新券列表** → 用户只能拿同一张已失效的券反复重试（2026-09-14 线上复现） | 缺陷 | 契约 §3.10、TC-RBP-013 错误列、PRD 7.16.1 爆红包结果行 | 戴宇涵 | 已完成 | 2026-09-14 修复：catch 改为**按状态码分类**——4xx（409/404/400）→ 阻塞态并 `emit('done')` 让红包页重读列表；仅网络异常/5xx 落失败态可重试。证据：RB-8 红（`expected true to be false`：应无失败态）→ 绿；RB-1~RB-8 全绿、全量 vitest **49 文件全绿**、`npm run type-check` ✓、`npm run build` ✓ | 2026-09-14 |
## 3. 阻塞与待确认

| ID | 事项 | 阻塞原因 | 负责人 | 更新 |
| --- | --- | --- | --- | --- |
| TODO-USER-107 ② | **分类商家列表页入口位置与 PRD 807 不一致（需 R7 同步）**：PRD 7.16.1「首页-分类宫格」行写「点击分类携带分类编号进入分类商家列表（6.3）」，但同页「分类商家列表页」行与 CHG-001 定「平台级 10 类不做、分类来源为店铺自身 `categories`」→ 宫格现有栏目均为超范围占位或「天天爆红包」入口，**没有可传真实 `categoryId` 的入口** | **2026-09-14 用户端按方案 2 处置**：入口改由**商家详情页商品分区标题行的「同类商家 ›」**进入（不影响左分类栏的滚动定位），已实现并配 T75/T76 用例；**剩余动作＝裁定后按 R7 同步 PRD 807 该行（写明入口实际位置：商家详情页店名旁的「同类商家」）**，或反过来要求改回首页宫格入口（那需要先解决"宫格无真实分类"与其占位口径） | 负责人/组长 | 2026-09-14 |
| TODO-USER-107 ② | **分类列表页分页与筛选口径冲突**：PRD 817 字段列「排序与分页参数按契约 §3.2」+ PRD 818 点击列「下拉加载下一页」+ 设计稿含「筛选」按钮 **vs** 契约 §1.3「分页接口（§3.6 搜索）是列表约定的唯一例外」、§3.2 `GET /stores` 可选参数仅 `keyword`/`categoryId`/`sort`、无结果返回 `data: []` | 两套口径不能并存（R7）：要么改契约（§3.2 增 `page`/`size` 并同步 §1.3 例外说明、替身与页面一并改），要么改 PRD 两行（去掉分页与筛选）。本页**暂按契约实现**（不分页、无筛选），待裁定后同一 PR 同步文档 | 负责人 + 后端 A（王嘉悦） | 2026-09-14 |
| TODO-USER-107 ② | **排序取值冲突**：PRD 817 字段列写「支持销量、距离、**评分**切换」，契约 §3.2 写「`sort` 最小取值为 `综合`、`销量`、`距离`，默认 `综合`」 | 本页按**契约**实现（与搜索结果页同一组选项），需确认 PRD 817 的「评分」是否笔误；若确需评分排序，须先改契约与后端 | 负责人 | 2026-09-14 |
| TODO-USER-007 | **评价图片在部署环境显示不出来（部署侧缺口，非前端缺陷）**：`deploy/nginx.conf` 只有 `/api/`、`/user/api/`、`/demo-images/`、`/design-assets/` 四类映射，**缺少 `/uploads/`**；后端静态映射为 `/uploads/**` 且只监听 `127.0.0.1:4000` → 浏览器请求 `/uploads/xxx.png` 落到 `location /` 的 SPA 回退（返回 HTML），**图片必然打不开**。修法：nginx 加 `location /uploads/ { proxy_pass http://127.0.0.1:4000/uploads/; }`（同一处另有 `client_max_body_size 2m` 偏紧问题：恰好 2MB 的图可能被 413 拒掉，建议放宽到 3–4m） | 前端已按契约提交 `url` 引用，问题在部署配置；**未修复前 `TODO-USER-007` 的真实联调只能验到「上传成功」，验不到「图片能显示」** | 部署（林晨）/ 组长路由 | 2026-09-14 |
| TODO-USER-003/007 | 评价图片依赖上传接口 | 契约 §10.1 已定稿、后端待实现（批次⑧）。**2026-09-14 进展**：批次⑧ 图片上传（新增 `FileController` + `/uploads/**` 静态映射 + `backend/uploads/` 入库）已随 PR #61 合并入 main，**待发布（`TODO-OPS-009`）后切 real 复验评价图片上传三态与失败重试** | 戴宇涵 | 2026-09-14 |
| TODO-USER-008 / TODO-USER-003 | **前端已实现、真实后端未返回字段**：① `payDeadline`（待支付倒计时，契约 §3.5，`TODO-USER-008` 已合并）；② `reviewed`（「待评价」判定依据，契约 §3.5 口径补充，`TODO-USER-003` 已合并） | 2026-09-12 对照后端代码核对（`ViewMapper.order`）确认**真实后端不返回这两个字段**，且 `docs/todo/后端.md` **未登记**这两项：真实模式下待支付倒计时会失效、已完成订单无法判定「待评价」；前端已按「字段缺失只隐藏对应字段」降级（`reviewed` 缺失不再误标「待评价」，已补用例 TV-13） | **归口后端**（王嘉悦/林晨），需负责人路由；契约 §3.5 已定稿，属实现缺口。**2026-09-14 进展**：`reviewed` 已随 PR #61 合并入 main（`ViewMapper.java:59` 返回、`OrderMapper.java:20` 以 `EXISTS(SELECT 1 FROM reviews …)` 派生）→ **待一次重新发布（`TODO-OPS-009`）后即可切 real 复验「已完成未评价 → 待评价」**；`payDeadline` **2026-09-14 已修复**（由用户端推动、后端 `ViewMapper.order` 按契约 §3.5 派生 `createdAt` + 15 分钟返回；前端同时改为「字段缺失只不显示倒计时、**不再禁用支付**」，避免再次把支付链路整页锁死）；契约 §3.5 的「（本期实现（待开发））」标注已同步去掉（R7）。**剩余＝发布后复验**；**2026-09-14 22:00 发布（`20260914-a2f15b1`）后已解除**：线上 `GET /orders` 已返回 `payDeadline`，支付页倒计时实测 **14:58 → 14:53 真走秒**、两个按钮均可用，旧待支付单超时正确显示「已失效」（见 `TODO-USER-113` 证据列与 `docs/testing/问题记录/2026-09-14-用户端真后端联调发现.md`）→ **本行可移出阻塞区** | 2026-09-14 |
| TODO-USER-002 | **取消订单接口未实现**（`POST /orders/{orderId}/cancel` 与 `cancelReason/cancelledAt/cancelledBy`） | 契约 §3.5 已标注「本期实现（待开发）」，后端已登记 `TODO-BE-003`（批次②，进行中）→ 非缺口，登记于此仅作前端依赖可见性；前端弹层与两处入口已合并，等后端接口就绪后联调。**2026-09-14 进展**：取消接口已随 PR #61 合并入 main（含 `CANCELLED`、取消原因、事务内回补库存、重复取消幂等），**待发布（`TODO-OPS-009`）后在 real 模式复验** | 王嘉悦（后端）/ 戴宇涵（前端联调）；**2026-09-14 22:00 发布后已解除**：线上实测取消可用——新单 `o1257` → `CANCELLED` / `cancelReason=不想要了` / `cancelledBy=USER` / `cancelledAt=2026-09-14 22:17:59`，订单列表显示「已取消」→ **本行可移出阻塞区** | 2026-09-14 |
| TODO-USER-010 | 订单跟踪页不单独实现（**已冻结**，2026-09-11 CHG-003）：跟踪页并入订单详情页，时间线作为订单详情的一部分实现 | 范围决议项，非阻塞；由本区承载以修正「已冻结行仍在进行中区」的板块不一致（TODO-USER-106 ②） | 戴宇涵 | 2026-09-13 |
| TODO-USER-026 | PROTO-P2-002 配送状态原型不制作（**已冻结**，2026-09-11 CHG-002）：用户端不设独立配送页，配送状态由订单状态在订单详情表达 | 范围决议项（已砍），非阻塞；红框图清单相应减 1 张；由本区承载以修正板块不一致（TODO-USER-106 ②） | 戴宇涵 | 2026-09-13 |

| TODO-USER-022 | 启动页（开屏）商家商品展示窗口改为可水平滑动 | **前提不成立、需负责人指明**：2026-09-13 全仓核查确认**启动页没有任何实现**（无 view/路由/组件，`index.html` 无相关标记，演示页无引用），只有设计导出 `docs/design/exports/用户端/00-启动页/{启动页,02-启动页-精细}`（后者为含 5 张浮动装饰图 + logo 的启动屏，`width: 390px; height: 884px; overflow: hidden`），且 PRD 与设计系统**均无「启动页/开屏」行**。故「当前不可水平滑动」无代码可改：请指明您看到的位置（部署版 / 设计稿预览 / 其他材料）；若确需实现，则它是一条**未开工整页**（比「打磨」大），需先定 PRD 口径（是否入交付范围、展示窗口接真实商品还是纯装饰、启动时序与路由位置） | 戴宇涵（待负责人澄清） | 2026-09-13 |
| TODO-USER-005 | 搜索结果页的**真实后端**：`GET /api/v1/search`（关键词/分类/排序/分页） | 契约 §3.6 已定稿、后端接口待实现（批次⑤ `TODO-BE-007`）；**前端已按契约 + 替身实现并合并（PR #59）**，等后端就绪后切 real 模式联调；**2026-09-14 线上真后端复验**：`GET /api/v1/search?keyword=肯德基&sort=综合` → **404 `40401 接口不存在`**（线上 `20260914-a2f15b1`，main 亦无该实现，批次⑤ `TODO-BE-007` 仍未开工）；搜索结果页在线上显示失败态「接口不存在 + 重试」并保留关键词——**前端降级行为正确**，接口就绪前本页在 real 模式不可用（mock 模式与 E2E 不受影响） | 戴宇涵（前端）/ 后端 A（王嘉悦） | 2026-09-14 |
| TODO-USER-028 | **「今日免费爆是否可用」无查询来源**：前端需据此把活动卡主按钮改为「去购买」（PRD 7.16.1 行 878 空态口径） | 契约 §3.8/§3.10 未提供该字段或接口（后端以 `users.free_blast_date` 内部判定，仅在爆接口 `409` 时告知）→ 本页暂按「列表内是否含 `canBlast` 券」判断可爆性，免费次数用尽由 `POST /me/coupons/blast` 的 `409` 告知后切换按钮 | **归口后端**（王嘉悦/林晨），提请确认是否在券列表响应或新增只读字段返回；需负责人路由 | 2026-09-13 |
| TODO-USER-109 | **桌面宽度下整页按 vw 放大 ~3 倍（已定：不改代码，按演示口径规避）** | **2026-09-14 负责人裁定选 1**：演示/验收时把用户端窗口保持在 **390–430px**（或移动视口/已内嵌的 390px iframe），即不触发该缩放；口径已写入 `docs/presentation/README.md` 与演示材料，与既有记录一致（`docs/presentation/engineering-audit.md` 第 66 行：SHOW-QA-002 的 px→vw 缩放问题仍需整体评估 viewport 缩放后再修）。**不修代码**：改动面涉及全局 vw 策略、需全量回归与全视口保真巡检，冻结前不做 → 修复转为技术债 `TODO-USER-112`（冻结后处理）。实测依据：窗口 1163px 宽时购物车栏 317px、盖住半屏且遮挡加购按钮；430px 宽时 71px 正常贴底 | 负责人 / 组长（口径） | 2026-09-14 |
| TODO-USER-107 ② | **分类列表页的排序在真实后端「等于没排」**：`GET /stores` 的 `sort` 契约取值是 `综合`/`销量`/`距离`（§3.2），但后端 `StoreMapper.listVisible` 只为 `销量` 写了 `ORDER BY`，`综合` 与 `距离` 落到默认序（`store_id`）；且**库里根本没有距离字段**（`stores` 表无 distance 列、种子与代码全无命中），而契约 §3.6 要求「距离按种子 `distanceKm` 升序」、§3.3 收藏对象也要 `distanceText` → 后两项在数据库层缺字段，无法实现 | 影响：本页在 real 模式下切「综合/距离」不会真的重排；**需后端补**（加距离列 + 迁移 + 种子值，并补齐综合/距离排序），属批次⑤ 范围；本页前端已按契约传参，无需改动 | 后端（王嘉悦/林晨）+ 组长路由 | 2026-09-14 |
| TODO-USER-107 ② | **分类列表页真实后端复验（2026-09-14 线上 `20260914-a2f15b1`）**：① **`sort=销量` 实测生效**——全量 6 家店返回 `m002(3500)→m003(2800)→m001(1200)→m005(950)→m004(800)→m1053(0)`，与 `monthly_sales` 倒序一致（此前台账只按源码推断，本轮有实测）；② **`sort=距离` 实测无效**——返回顺序与默认 `store_id` 序**完全一致**，且 `/stores` 响应**无任何距离字段** → 页面上距离文案恒不显示（前端按「缺失即隐藏」正确降级）、「距离」排序点了无反应；③ `sort=综合`（默认）同默认序、`sort=评分`（PRD 817 提法）亦为默认序。 | **结论不变**：距离列 + 种子值 + 综合/距离排序仍需后端补（属批次⑤），本页前端无需改动 | 后端（王嘉悦/林晨）+ 组长路由 | 2026-09-14 |

| TODO-USER-116 ① | **PRD §5.1 会话标识口径与实测不符**：PRD 写「首次进入对话页时，调用 `/chat` 不传 `sessionId`，后端返回新的 `sessionId`」；但契约 §10.6 的 `/stream-chat` 只回文本分片、**不回 `sessionId`**（线上实测），按字面实现则首次回复无法走流式（与 §4.3 优先流式、AI-FE-06 冲突） | **本轮实现按「前端生成 sessionId 并随每次请求携带」**（契约 §10.6 明确「不传则后端基于 HttpSession 生成」，传即采用）→ 首次回复即可流式；需负责人裁定后回写 PRD §5.1 | 负责人 / 组长（口径） | 2026-09-14 |
| TODO-USER-116 ② | **PRD §4.2 跳转路径与工程路由不一致**：PRD 写「点击跳转到对应商家详情页（`/store/m002`）」，应用实际路由为 `/stores/:storeId` | 本轮实现按实际路由 `router.push({name:'store-detail',params:{storeId}})` → `/stores/m002`；需同步 PRD §4.2 该行（或反向改路由，不建议） | 负责人 / 组长（口径） | 2026-09-14 |
| TODO-USER-116 ③ | **线上 AI 接口 503：服务器未注入 `DEEPSEEK_API_KEY`**（契约 §10.6：key 经环境变量注入、不入仓库）→ 线上 real 模式 `POST /api/v1/ai/chat` 返回 `503 {code:50301}`，前端按 PRD §6.3 显示「AI 助手暂不可用，请稍后再试」（错误态本身已验证正确） | 需部署侧在服务器注入环境变量并重启 `elm-backend`（演示前必须完成，否则线上演示只能用 mock 模式）；归部署（林晨）/ 后端 A | 部署（林晨）+ 组长路由 | 2026-09-14 |

| TODO-USER-116 ④ | **AI 会话历史无法从数据库读取（只能本地展示）**：契约 §10.6 只提供 `/ai/chat` 与 `/stream-chat`（**后端只存不查**），PRD §5.2 亦写「不依赖后端查询历史消息，前端自管理展示」——消息确实落库，但前端无从读回（清 localStorage / 换设备即看不到旧对话） | 若验收口径要求「对话历史从数据库读取」，需后端新增会话/消息查询接口（建议 `GET /ai/sessions/{sessionId}/messages`）并同步 PRD 与契约（R7）；当前实现按 PRD/契约用 localStorage 承载展示 | 后端（林晨，`TODO-BE-024`）+ 负责人（口径） | 2026-09-14 |
| TODO-USER-116 ⑤ | **AI 知识库不随数据变更刷新**：`ProductKnowledgeService.refresh()` 在 main 全库**无调用点**（首次调用时缓存），商品/商家变更后 AI 仍按启动时快照回答，与契约 §10.6「商品/商家变更后可调用 `refresh()` 强制刷新」不符 | 归后端：在商家商品/店铺变更接口后触发 refresh，或加 TTL 自动刷新；演示前若改过商品建议重启后端或触发刷新 | 后端（王嘉悦/林晨，`TODO-BE-024`） | 2026-09-14 |

## 4. 任务实施说明（附：给执行者与 AI 代理的直接指令）

> 本节只为 §1 中的 **TODO-USER-023 / TODO-USER-024** 提供可执行细节；其余任务按「出处」列回读原文即可。
> **通用纪律（两条都适用）**：走特性分支 + PR；按 **R1 合并前必跑矩阵** 执行 `npm test` **和** `npm run build`（只跑单测不算通过），命令与结果写进 PR 描述（模板 `.github/pull_request_template.md`）；脚本类提交按 `test:`（脚本与断言）→ 必要时 `fix:`（先补红测）的顺序，遵守 TDD 提交纪律；截图与报告一律落 `test-results/`（已 gitignore），不入库；完成时回填本文件「证据」列并写 raw 片段（写明更新了哪些 TODO 编号）。

### 4.1 TODO-USER-023｜保真度巡检断言脚本（结构 + 多视口）

**目标**：把 2026-09-08 那次"先跑出 23/23 全红、改完 23/23 全绿"的通栏结构断言，恢复成**仓库内可复跑**的脚本；并补上多视口断言。结课汇报要用"可复跑门禁"口径讲这件事，脚本必须能在本机一条命令跑出结果。

**放哪（推荐）**
- 新增 Playwright 用例：`frontend/user-h5/e2e/design-fidelity.spec.ts`；
- 复用现有配置 `frontend/user-h5/playwright.config.ts`（`testDir: './e2e'`、`channel: 'chrome'`、`webServer` 自动拉起 dev；**先确认 dev 跑在 mock 模式**，否则数据源不同会让断言不稳）；
- 运行：`cd frontend/user-h5; npx playwright test e2e/design-fidelity.spec.ts`；
- 报告与截图落 `test-results/`（不入库）。

**断言 A｜结构断言（判定口径不得自创）**
- 判定式（三个条件同时满足才算通过）：**① 满宽 = 壳宽（区块左右边界等于 `.app-shell` 内容宽度）∧ ② `border-radius` = 0 ∧ ③ `box-shadow` = none**。
- 出处：`docs/frontend/UI复刻经验与执行约定.md` §2.2——"设计稿没有卡片壳就不要加壳；判断是否卡片必须看圆角 + 阴影 + 边框 + 背景色 + 父级 padding 的组合，不能只看宽度"。
- 受检区块：原先共 **23 项 / 8 类**（**先 grep 核对类名是否仍在；若已改名，按"语义等价区块"重新定位，并在脚本里用注释写出"旧类名 → 新选择器"映射**）：
  1. 确认订单页 `.co-card` × 4（收货地址区、送达时间、商品明细、备注输入）；
  2. 订单列表页 `.ol-card` × 10；
  3. 订单详情页 `.od-card` × 3（状态区、收货信息区、商品与金额明细）；
  4. 我的页 `.mn-profile`、`.mn-menu`；
  5. 地址列表页 `.al-card` × 2；
  6. 新增/编辑地址页 `.ae-card`；
  7. 商家详情页 `.store-tabs`（点餐/评价 Tab：必须与横幅同底通栏，不能是独立白条）；
  8. 其余按当时留痕补齐到 23 项。
- 结果口径：打印"通过 N / 受检 N"；失败时输出选择器、实测 `border-radius` 与 `box-shadow`，便于定位。

**断言 B｜多视口断言（320 / 360 / 375 / 390 / 430）**
- 视口：320×568、360×640、375×667、390×844、430×932（同一用例参数化）。
- 每个视口至少断言：
  1. **无横向溢出**：`document.documentElement.scrollWidth <= 视口宽`，且不存在 `getBoundingClientRect().right > 视口宽 + 1` 的元素（装饰性元素单独列白名单并注释原因）；
  2. **页面壳宽度 = 视口宽**（`.app-shell`）；
  3. **固定栏**：底部导航/购物车栏贴底可视、未被内容遮挡（`elementFromPoint` 取底部中心像素归属验证）；
  4. **文字不裁切**：关键词抽样（商家名、价格、按钮文案）满足 `scrollWidth <= clientWidth + 1`。
- 依据：约定 §2.12（"多视口验收必须包含 320px"）与 `docs/frontend/前端架构设计与工程约定.md` §6（**几何类断言只能写 E2E**：jsdom 无布局引擎、`getBoundingClientRect` 恒为 0；不得用"断言 CSS 声明字符串"凑数）。

**验收标准**
1. 在**当前 main** 上一次跑通：结构断言全绿 + 5 个视口全绿；
2. 脚本内注释写明历史锚点：`docs/record/raw/2026-09-08/组长-1847.md`（原红端 23/23 → 绿端 23/23）、`…/组长-1806.md`（主链路巡检 34/34，若顺手重建则一并纳入）；
3. PR 描述贴出：命令、结构断言通过数、各视口结论、截图路径（`test-results/`）；
4. 若脚本跑出真实缺陷：**先补失败用例（`test:`）再修（`fix:`）**，并登记 `docs/testing/缺陷跟踪表.md`（写权归测试联调同学，可先起草待确认）。

### 4.2 TODO-USER-024｜设计稿像素比对脚本与一次性取证

**目标**：把"实现截图 vs 设计稿导出"的像素比对做成可复跑的一次性取证工具（**不是 CI 门禁**），用于结课汇报的"保真度分区指标"与前后对照图。

**放哪（推荐）**
- 截图脚本：`frontend/user-h5/scripts/shot-design-compare.cjs`（Playwright + 本机 Chrome：`channel: 'chrome'`、`--no-proxy-server`；视口 **393×852 @2x**；关闭 animation/transition，等 `document.fonts.ready` 后静置约 1.5s 再截图）；
- 比对脚本：`tools/design-diff/pixel_diff.py`（Python 3.12 + Pillow + numpy，**零 npm 新增依赖**，本机已验证可用）；
- 产物：`test-results/design-diff/`（截图、diff 图、分带指标 JSON），**不入库**；结论写 raw 片段与 PR 描述。

**三条必须遵守的口径（否则数字没有意义）**
1. **53px 状态栏偏移校正**：设计稿画布把 9:41 状态栏画了进去，而 H5 实现不含（见 `docs/design/exports/用户端/02-首页/02-首页-精细/README.md` §2.3）→ 比对前必须把设计稿的 y 起点下移 53px（@2x 为 106px）。
2. **数据驱动区域必须掩膜**：商家卡等区域渲染真实数据，与设计稿占位内容天然不同（实测该区域差异 33.3%）；不掩膜会把"内容不同"误报成"复刻不准"。建议用 Playwright `mask` 或按坐标裁掉后再比。
3. **整图比对只对"画布 393 的精细版"做**：`screen.png` 共 29 张、**15 种尺寸**，多数是 390 宽或被高度裁切的基线；只有首页/启动页精细版是 786×1704（393×852@2x）可直接对齐。其余页面只做**分带指标**（按设计稿 CSS 的 top/height 切带比较），不做整图百分比。

**输出内容（一次取证）**
- 整图指标：超阈像素占比、平均绝对差、最优整体位移（`dx/dy`）；
- 分带指标：定位栏 / 搜索区 / 分类宫格 / 活动区 / 筛选标签 / 商家卡（掩膜前后各一份）；
- diff 可视化图（差异标红、其余灰度）；
- 一句结论：**主门禁是几何/色值/结构断言，像素比对只用于取证**。

**参考实现（本机已有，直接改写，不要从零写）**
- `docs/_archive/presentation/调研报告/第二轮与第一轮原始报告/第二轮/原型-截图比对/`：
  - `shot.cjs`（截图；参数：base/路径/宽/高/DPR/输出）；
  - `pixel_diff.py`（整图 diff + 差异图）；
  - `align_analysis.py`（53px 偏移校正、8×8 块均值、最优位移搜索）；
  - `band_analysis.py`（按设计稿 CSS 坐标分带）；
  - 已跑出的首页截图与 diff 图（实测：全图 30.1% → 校正后 19.1% → 静态外框 10.6% / 数据区 33.3%）。

**验收标准**
1. 一条命令可复跑：截图 → 比对 → 输出指标与 diff 图；
2. 首页结果与参考实现一致（容差 ±0.5 个百分点），并在 README 或脚本注释里写明上述三条口径；
3. PR 描述写明"**不作为 CI 门禁**"的理由（见上方口径 2、3）；
4. 同样遵守 R1 必跑矩阵与 TDD 提交纪律。
---

## 4.3 原型产出提示词与验收要求（2026-09-10 新增，对接 TODO-USER-025/031；配送原型 TODO-USER-026 已随 CHG-002 冻结，不再出图）

> **用途**：为「开红包」（`PROTO-RED-01/02/03`）、「取消订单确认页」（`PROTO-P2-001`）提供可直接复制的出图提示词。
> **两条产出路径**：**路径 A（推荐）**——按设计系统实现后**实现态截图 + 红框**，只用 §4.3.7 的要求，**不需要**下面的提示词；**路径 B**——用 AI 出概念图（Stitch / 文生图），用 §4.3.1 的统一约束块 + 各屏提示词，产出**只作参考，不得作为 exact 复刻真源**。
> **真源优先级**：`docs/design/exports/用户端/**`（有导出稿的页面）> `docs/design/设计系统-用户端.md`（无导出稿的新增页/浮层）> 本节的提示词。

### 4.3.1 统一约束块（复制到每一屏提示词开头）

```
移动端外卖 App 高保真 UI 设计稿，竖版 390×844（导出 2 倍图 780×1688），简体中文，扁平简约写实风格。
色值：主色亮橙 #ff5a1f；页面底色 #f9f9f9；内容面纯白 #FFFFFF + 1px 描边 #E5E5E5；圆角 8px；不使用环境阴影（仅浮层允许轻微投影）。
文字层级：主标题 20px/28，正文 14px/20，次要说明 12px/16，金额用 20px 品牌橙加粗。
遮罩：弹层与浮窗使用 40% 黑色遮罩。
底部导航固定四项：首页 / 消息 / 订单 / 我的（当前项用品牌橙）。
语义红只用于错误、危险、未读角标，不得用于普通按钮或装饰。
不要画状态栏（时间、信号、电量图标），从页面内容顶部开始。
禁止出现：第三方品牌 logo、真实支付方式（微信/支付宝）、二维码、积分或金币、客服入口、退款与发票、骑手与地图与实时定位、物流轨迹、平台管理员界面、英文界面、阴影拟物风格。
```

**English keywords（给需要英文的工具）**：`mobile food-delivery app UI, 390x844, minimal flat realistic, primary orange #FF5A1F, background #F9F9F9, white cards with 1px #E5E5E5 border, 8px radius, Chinese text only, no logos, no payment icons, no status bar`

### 4.3.2 PROTO-RED-01「买红包」购买浮层（1 张）

```
（接统一约束块）
画面：红包页上方弹出底部购买浮层，遮罩 40% 黑；浮层为白色面板，顶部 8px 圆角，面板内边距 16px。
浮层内容自上而下：
1）标题「买红包」20px 加粗居中，标题右侧一个不显眼的关闭叉号；
2）套餐卡片：两档套餐堆叠，均为白色卡片 + 1px 描边 + 8px 圆角；`pack49` 左侧价格「¥4.9」用 20px 品牌橙加粗，右侧两行文字「4 张红包」14px 正文 与「3×满 30 减 5 + 1×无门槛减 5」12px 次要色；`pack99` 左侧价格「¥9.9」用 20px 品牌橙加粗，右侧两行文字「8 张红包」14px 正文 与「满减券组合」12px 次要色；卡片右侧一个品牌橙实心勾选圆点表示已选中；
3）说明文字 12px 次要色：「模拟付费，仅用于课程演示，不产生真实资金流水、不落支付记录、不含退款条款；购买券有效期 7 天」；
4）底部并排两个按钮：左侧次按钮「再想想」（白底 + 1px 描边，直角或 8px 圆角），右侧主按钮「确认购买」（品牌橙实心、白字、8px 圆角），主按钮占宽约 2/3。
浮层上方可露出红包页顶部区域（标题「红包」与部分红包卡片），以体现浮层关系。
```

### 4.3.3 PROTO-RED-02 红包页增量区（顶部通栏 +「天天必爆」活动卡）（1 张）

```
（接统一约束块）
画面：红包页整页（标题按设计稿为「天天红包」）。
顶部：标题「天天红包」20px 加粗；标题下方为「加量红包省更多／去购买」通栏（打开买红包浮窗）+「天天必爆」活动卡（奖池预览 +「免费爆 1 次」按钮）。
「天天必爆」活动卡样式：红金渐变底（例外配色）+ 8px 圆角；卡内展示 10 档奖池预览（满 30 减 5 / 满 30 减 8 / 满 25 减 8 / 满 40 减 10 / 满 25 减 15 / 满 40 减 20 / 满 50 减 25 / 无门槛减 5 / 满 30 减 18.8 / 满 40 减 18.8），底部一个「免费爆 1 次」按钮；每日 1 次免费爆（0 点重置）。
下方：沿用既有「可用红包 (4)」卡片列表（金额用 20px 品牌橙，金额区与信息区之间用 1px 虚线分隔、不用阴影，失效券整卡灰化），其上方不再有「待开红包」分组。
底部固定四项导航：首页 / 消息 / 订单 / 我的，当前项「我的」用品牌橙。
```


**⚠️ 增量改造口径（2026-09-10 补充，重要）**
- 红包页**不是重做、也不是重新出设计稿**——它是**在既有导出稿基础上做增量改造**，新增仅两处：① 顶部「加量红包省更多／去购买」通栏（打开买红包浮窗）；②「天天必爆」活动卡（奖池预览 +「免费爆 1 次」按钮，每日 1 次免费爆、0 点重置）。**原「待开红包」分组口径已作废（CHG-001 二修），页面不再出现待开卡片列表。**
- **既有区块保持不动**：页面标题按设计稿为「天天红包」、既有「可用红包 (4)」卡片列表（金额区与信息区虚线分隔、过期灰化）、底部四项导航，全部沿用 `docs/design/exports/用户端/10-个人中心/04-红包/` 的既有复刻结果与结论，**不得改动、不得重画**。
- 上面这段提示词要求出**整页图**，只是为了在原型里体现「新增区与既有区的关系」；**页面本身不需要重新生成**。实现阶段的结论应写成「红包页：既有区块 exact 复刻 + 新增两处按设计系统新建」，不写「红包页重新复刻完成」。
- 导出稿资产**不得覆盖**（`AGENTS.md`：PRD 引用的资产文件不得覆盖）；新原型图落 `docs/prd-assets/user/prototype-chg001-redpacket/`。
### 4.3.4 PROTO-RED-03 爆红包全屏动效与结果卡（3 张：初始态 / 结果态 / 失败态）

**03a 初始态**
```
（接统一约束块）
画面：红包页被 40% 黑遮罩覆盖，遮罩中央是一个竖版红包浮窗（宽约 260px、高约 360px、12px 圆角），红金渐变底（例外配色，允许红金而非品牌橙单色）。
浮窗自上而下：顶部横幅「天天必爆」20px 白色加粗 + 一行 12px 白字说明「每日 1 次免费爆，0 点重置」；中部一个圆形「爆」按钮（金色描边、白字「爆」）；按钮下方一条欧气进度条（细条，红色填充）；底部 12px 白色提示「点击「爆」从 10 档奖池随机开出红包」；右上角一个小白色叉号。
卡片下方可露出红包页顶部通栏，以体现浮层关系。
不要画红包雨或粒子效果（那是动效，静态稿不体现）。
```

**03b 结果态**
```
（接统一约束块）
画面：同一浮窗形态与位置，浮窗已翻开为结果卡，红金渐变底：
卡片上部 12px 白字「爆出红包」；中部两行：「满 25 可用」14px 白色（门槛），下方「减 15」用 28px 金色加粗（减免金额，视觉主体）；
卡片下部一行 12px 白字「限今天 23:59 前使用」（当天到期）；底部一个通栏主按钮「去使用，立即抵扣」（红金底白字、8px 圆角）；不再有「再开一个」或「已升级」字样。
其余保持与初始态一致（遮罩、浮窗位置、右上角叉号）。
```

**03c 失败态**
```
（接统一约束块）
画面：同一浮窗保留不关闭；浮窗中央改为一个语义红描边圆形感叹号图标，下方文字「爆红包失败，请重试」16px 加粗，再下方 12px 次要色说明「网络异常，请检查后重试」；
浮窗下方两个按钮：主按钮「重试」（红金底白字），次按钮「稍后再说」（白底 + 1px 描边）；若无免费次数且无可爆券，浮窗提示「今日免费次数已用完，去购买红包」并提供「去购买」按钮。
不使用整屏错误页，不出现客服与退款相关内容。
```

### 4.3.5 PROTO-P2-001 取消订单确认底部弹层（1 张）

```
（接统一约束块）
画面：支付页（或订单详情页）上方弹出**底部弹层**，遮罩 40% 黑；白色面板顶部 8px 圆角。
内容自上而下：标题「取消订单」20px 加粗；一行 12px 次要色说明「取消后库存将回补，订单不可恢复」；
取消原因 **chips 必填**：胶囊标签（如「不想要了」「点错了」「临时有事」「其他原因」）可换行排布，选中项品牌橙实心；下方一个 ghost 风格 1px 描边输入框（「其他原因」可补充），框右下角显示字数「0/50」（必填 1–50 字）；
底部并排按钮：主按钮「再想想」（品牌橙实心白字 8px 圆角），次按钮「确定取消」（白底 + 1px 描边）。
禁止出现退款、售后、资金处理、人工客服相关内容；**不出现「优惠券将原路退回」等表述**。
```


### 4.3.7 产出与验收要求（路径 A 与路径 B 共用）

1. **文件名**：`proto-red-01-buy-sheet-v1.png`、`proto-red-02-promo-blast-v1.png`、`proto-red-03a-open-initial-v1.png`、`proto-red-03b-open-result-v1.png`、`proto-red-03c-open-fail-v1.png`、`proto-p2-001-cancel-sheet-v1.png`。
2. **落位**：开红包系列放 `docs/prd-assets/user/prototype-chg001-redpacket/`；取消订单确认弹层放 `docs/prd-assets/user/prototype-chg003-orderpay/`。**不覆盖**既有 `prototype-redbox-v1/**`。
3. **每张随附**（见 `docs/project/用户待补充资料与原型清单.md` §7）：无红框完整 PNG、可编辑源文件或设计链接说明、页面名 + 功能 ID（`FR-RED-02` / `FR-CXL-01`）+ 状态名、基于哪份导出稿（红包页基于 `10-个人中心/04-红包`）、简体中文无「可能/暂定」类占位词。
4. **人工核对**：与 `docs/design/设计系统-用户端.md` 的对应行逐条比对（红包页、取消订单确认页、新增的购买浮层与爆红包浮窗），并确认与红包页导出稿不冲突。
5. **路径 B 的额外限制**：AI 出图只作参考与答辩素材，**不得**写进 PRD 充当 exact 复刻真源；若与导出稿冲突，以导出稿为准。
6. **红框标注**：按 `docs/prd-assets/user/**` 既有风格加红框与编号，便于 PRD 7.16.1 引用。
7. **回填**：出图后把 `docs/PRD.md` 7.16.1 中对应行的「红框图待补」替换为图片引用（同一 PR 内完成，遵守 R7）。
8. **纪律**：原型产出属文档/资产变更，按 R1 改文档做口径 grep 复查；不涉及代码则无需跑构建与单测。


