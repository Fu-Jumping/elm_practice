# 2026-09-15 文档与交付留痕（A2–A5 口径裁定回写）

## 12:01 （本轮：A2–A5 四处文档口径裁定回写，纯文档）

- **来源与边界**：负责人（用户本人）2026-09-15 对 4 处文档口径冲突的裁定，由开发子代理在分支 `feat/remaining-gaps-tdd`（HEAD=471408b）执行。**只改文档，未改任何代码**（`frontend/**`、`backend/**`、`deploy/**`、`database/**` 零改动）；**未改 `docs/backend/后端接口契约.md`**（其中 §3.6 关于 `/stores` 的分页行按分工留主代理处理）；**未改 `docs/record/daily/**`**；未执行 `git commit`/`push`。
- **A2 注册页「性别」**：删掉「注册含性别」要求，注册字段统一为 账号/密码/昵称。改 `docs/PRD.md` §6.2「用户注册」功能行（402 行）、§7.1 注册页字段行（583 行）；连带 `docs/SRS.md` FR-ACC-01 首条（243 行）。地址对象 `contactSex`（联系人性别）、个人信息「查询和修改昵称、性别」、地址表单「性别」不在本次范围，未动。
- **A3 商家详情「领券」**：删除「按优惠接口返回结果展示可领取金额」承诺，与契约 §3.8（只有会员与红包接口，无店铺券领取接口）对齐——本期不提供店铺券领取能力、页面不展示可领取金额、「领券」按钮点击提示「暂未开放」。改 PRD 849 行、SRS 671 行（「商家详情页-商家信息横幅」）的字段列与点击列。
- **A4 聊天详情会话关闭态**：删除「若会话已关闭，输入区改为只读」，原处留诚实范围说明（契约 §6.1 会话对象无 `closed`/状态字段，前端无数据源，本期不做会话关闭态）；同时收敛同页「底部输入区」行旧的「已关闭会话禁止发送」。改 PRD 868/871 行、SRS 693/696 行。
- **A5 分类商家列表页分页/筛选/排序**：PRD 改为「不做分页、不做筛选（列表为一次性返回的完整数组）；排序只保留 综合/销量/距离，去掉评分」，与契约 §1.3/§3.2 完全一致。改 PRD 837/838 行、§7.2「商家列表支持排序」规则行（593 行）；连带 SRS 659/660 行、`docs/todo/用户端.md`（TODO-USER-107 任务列改为「排序栏（综合·销量·距离，无筛选；本页不分页）+ `GET /stores?categoryId=&sort=`」；两条冲突阻塞行 73/74 追加「2026-09-15 裁定 A5 关闭」）、`docs/project/未完成模块与如实标注清单.md` 第 56 行（分页/排序 → 分类条件与排序；不分页、不筛选）。
- **口径 grep 复查（0 命中证据）**：口径文档（排除 `docs/record/**` 历史留档）中旧表述 0 命中——`账号、密码、昵称、性别` = 0；`按优惠接口返回结果展示可领取金额` / `在优惠接口有结果时打开规则` = 0；`输入区改为只读` = 0；`筛选/排序栏支持销量、距离、评分` = 0；`其他商家列表支持排序：销量、距离、评分` = 0。`下拉加载下一页` 与 `排序与分页参数按契约 §3.2` 仅剩**否定句/历史冲突描述**（「不分页、无「下拉加载下一页」」「…已按契约改齐，本条关闭」），无「同页既说下拉加载又说不分页」的两套口径。
- **判断过但未改的文档**：`docs/backend/后端接口契约.md`（分工留主代理）；`docs/project/项目规则.md` §5 长期约束台账与 `docs/record/daily/**`（写权/范围外——建议负责人或次晨汇总方登记本次 A2–A5 裁定）；`docs/SRS-工程版.md`（无注册性别/领券/会话关闭/分类分页表述，grep 0）；`docs/project/TDD实施规划.md`（TC-ACC-015「修改昵称/性别」属个人信息而非注册；TC-SRC 属搜索 §3.6，不受 A5 影响）；`docs/design/**`（视觉真源，设计稿含「筛选」按钮，按 A5 本期不实现，不改设计稿）；`docs/testing/**`、`docs/frontend/**`（grep 无冲突表述）；PRD 7.16.1「注册页-注册表单」行（840 段）本就无「性别」，其「手机号/账号、确认密码」为既有口径，不属 A2 裁定范围（避免扩范围）。
- **AI 使用点（B 表口径）**：AI 代理并行只读检索 + 全库口径 grep 定位冲突点，按负责人裁定逐条回写 PRD/SRS/todo/清单，并以「删旧 → 0 命中」作为主证据；对 PRD 7.16.1 注册表单行、地址联系人性别等**边界外内容做了显式排除判断**并留痕，裁定取舍待负责人复核。执行中观察到主代理同时改 PRD（文件行数 1196→1198），故所有回写改为「按内容定位 + 行内片段替换」，避免行号漂移写错位置。

| 项 | 内容 |
| --- | --- |
| 改动文件 | `docs/PRD.md`、`docs/SRS.md`、`docs/todo/用户端.md`、`docs/project/未完成模块与如实标注清单.md`（+ 本留痕文件） |
| 未改动 | `frontend/**`、`backend/**`、`deploy/**`、`database/**`、`docs/backend/后端接口契约.md`、`docs/record/daily/**` |
| 待办编号 | `TODO-USER-107`（任务列口径改写 + 两条冲突阻塞行关闭；**未替他人改状态/负责人/更新日期**） |
