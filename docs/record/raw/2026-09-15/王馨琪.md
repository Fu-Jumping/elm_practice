# 王馨琪留痕（2026-09-15）

## 13:15 商家端两项收尾：联系电话字段（#11）与拒单/打印小票演示入口（#12，TDD 红转绿）

### A 进度事实

- **第 11 项「店铺设置缺联系电话字段」**：经核对，后端与契约均已就绪（`StorePatch.contactPhone` 已随 PR #61 入 main；PR #60 为后端修复分支），前端缺口的恢复**已完成并位于待合并分支**——
  `test 4d85c9f`（红：找不到「联系电话」标签，3 failed）→ `feat f06ad90`（恢复输入项：非必填 + `^1\d{10}$` 校验、空值不提交、mock 种子改 11 位）。
  结论：**该前端缺口不在 main，但在本分支已闭环**，合并并按最新 main 重新部署后即可回归（`TODO-MER-010`）。
- **第 12 项「拒单 / 打印小票的课程演示提示」**：PRD 7.11 原文「拒单仅给出课程演示提示，打印小票仅模拟反馈」→ 新增 TDD 循环：
  - 红端：`b3471ae`（新增 `tests/merchant-reject-and-receipt-hint.test.tsx` 3 条：入口存在、点「拒单」只给课程演示提示且不改状态、点「打印小票」只给模拟反馈且不改状态）。首次运行 **3 failed**（`Unable to find role="button" and name /拒\s*单/`）。
  - 绿端：`2396ac7`（OrdersPage 订单详情操作区在「联系顾客」后新增两入口；处理器仅 `message.info` 弹提示，不调用任何接口、不改订单状态与金额）。
  - R1：`npm test` **12 文件 / 38 用例全绿**、`npm run build` **0 个 TS 错误**、`npm run lint` **0 警告 0 错误**。
- **红端严谨性处理**：首次红端曾出现「找不到查看详情」的假红与「AntD 两字按钮自动插空格（拒单→拒 单）」两处测试自身问题，已分别用 3000ms 超时（与仓库既有测试一致）与正则 `/拒\s*单/` 修正；并用 `git stash` 临时回退实现，确认修正后的红端**原因正确**（功能缺失）后再恢复实现转绿。
- **待办登记**：新增 `TODO-MER-013`（拒单/打印小票演示入口，已完成）于 `docs/todo/商家端.md` 已完成区。

### B 本轮 AI 使用点

| # | 任务 | 类别 | AI 做了什么 | 人工审核 | 结果 |
| --- | --- | --- | --- | --- | --- |
| 1 | 需求口径回读 | 需求对齐 | 读 PRD 7.11 原文，确认两项均为「课程演示」性质（不接真实接口） | 王馨琪确认 | 口径明确 |
| 2 | TDD 实施 | 测试+实现 | 起草 3 条红测试（含异常/边界断言「不改状态」）、实现两入口、修复测试自身问题并复现干净红端 | 王馨琪确认 | 红 3 failed → 绿 38/38 |
| 3 | 台账/待办 | 文档记录 | 登记 TODO-MER-013 与当日 raw 片段 | 王馨琪复核 | 已落盘 |

### C 本轮新增/更新的 TODO 编号

- 新增并完成：`TODO-MER-013`（拒单/打印小票课程演示入口，PRD 7.11）
- 关联既有：`TODO-MER-010`（联系电话，前端恢复在待合并分支内，待部署回归）
