# 2026-09-15 林晨（后端）raw 留痕

## 12:40 BE-008 收藏+会员收尾、BUG-20260914-002 复查闭环

### A. 进度事实

1. **TODO-BE-008 剩余两项开发完成（承接 9/14 红包子项）**
   - 商家收藏（契约 §3.7）：新增 favorites 表（UNIQUE(user_id,store_id) + INDEX(user_id,created_at)），`GET/POST/DELETE /api/v1/me/favorites` 三接口；INSERT IGNORE 保证重复收藏幂等、取消未收藏店铺 200 幂等、店铺不存在 404、用户隔离、关店收藏保留；couponTags 仅生成满减标签（满20减2，按门槛升序），distanceText 无数据源不返回。FavoriteIntegrationTest 8 用例全绿。
   - 会员/会员价（契约 §3.8）：users 扩 is_member/member_activated_at；`GET /api/v1/me/member` 返回 memberOpened/discountRate(0.95)/discountDesc/activatedAt，无开通接口；商品视图回 memberPrice；下单计价第⑤步由硬编码 false 改为 u.isMember（小计按店铺 member_discount 打折，member_price 仅展示用、订单单价仍取 price 快照）。MemberIntegrationTest 6 用例全绿。
   - 迁移：upgrade-favorites.sql / upgrade-member.sql（DatabaseInitializer 启动自动执行）+ database/migrations/20260915-*.sql 副本；upgrade-member.sql 补了存量库 m002 六个商品 member_price 的幂等回填（仅回填 member_price IS NULL 的种子商品，不覆盖商家数据）。
   - 双库种子口径：测试库 reset.sql 保持 u001 非会员（不破坏既有金额断言），新增会员用户 u002（13800000003）；生产 seed u001 为会员、member_price 仅 m002 的 p101–p106 有值。
2. **BUG-20260914-002 复查闭环**：main 上 `POST /conversations/{id}/messages` 已完整实现（ExtensionController→ExtensionService.send，空/超长 400、越权 404、已读更新），9/14 的 500 系线上版本落后 main 未部署，代码无需改动；ConversationStage2IntegrationTest 3/3 绿，本地连业务库 merchant-a 实测发送 200（测试消息已清理）；缺陷跟踪表状态改"已修复-待部署验证"，随批次④部署后线上回归。
3. **TODO 更新**：TODO-BE-008 改"已完成"（补全部证据）；缺陷表 BUG-20260914-002 表格行+详情段更新。
4. **文档同步（R7）**：契约 §3.7 改"已实现"+实现回写、§3.8 标题改"均已实现"+会员实现回写。
5. 全量回归 91 用例在服务器测试库 elm_practice_test 进行中（前两轮分别遇到 1/14 个 Hikari 连接抖动错误，定位为本机两个残留后端实例（9/14 23:23、9/15 00:02 启动的 jar）占用池连接+连续压测，杀进程后第三轮验证；0 业务断言失败）。

### B. AI 使用点

- AI 代理完成收藏/会员全部代码与测试脚手架（FavoriteMapper/Service/Controller、MemberService/Controller、两个 IntegrationTest、Domain/DTO/ViewMapper 接线、计价步⑤）；测试场景与金额断言由人按 TDD 口径设计，异常用例（幂等/隔离/404/400/401）齐备。
- AI 协助定位全量回归的连接错误根因（HikariPool total=3 active=0 idle=3 的超时特征 → 本机残留 java 进程占连），非代码缺陷。
- AI 复查 BUG-002 时纠正前序"接口未实现"判断：代码在 main 已存在，原 500 是部署落差，避免了重复造轮子。
- 迁移脚本 member_price 存量回填缺口由 AI 排查发现（seed 仅空库生效，存量业务库会缺会员价数据）并补齐幂等 UPDATE。
