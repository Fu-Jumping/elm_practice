# 2026-09-15 用户端留痕（戴宇涵）

> 命名口径：按 `docs/todo/README.md` §3 第 5 条与 `docs/project/项目规则.md` §2 第 2 条 2.1–2.3，同日同角色只写本文件，后续轮次在文件末尾追加小节。

## 11:05 （本轮：拉取最新 GitHub 后按负责人清单补齐用户端 6 项"本期承诺未落地"缺口）

### A. 进度事实

- **本轮任务来源**：负责人（用户本人）指示——拉取最新 GitHub，按其给出的清单（第 5～10 项，「用户端前端功能缺失（本期承诺未落地，与后端无关）」）逐项修完。
- **拉取结果（先做）**：`git fetch --all` → `main` 快进到 `ac2b0fb`（PR #65 合并，即上一轮 `feat/ai-chat-page-tdd` 已并入 main）；工作区分支由 `feat/ai-chat-page-tdd` 切到 main 并快进 42 个提交。**本轮新建分支 `feat/user-frontend-gaps-tdd`**（从 `ac2b0fb` 起），未在 main 上直接改代码。
- **逐项核实（先核实、再动手，不凭清单字面下手）**：6 项**全部属实**，且都能定位到具体文件与缺失点——
  1. 商品规格弹层：`StoreDetailView.vue` 点加号只有「按基础价直加」一条路径，全库无 `specOptions` 消费方（`grep specOptions src/` 只命中替身与类型注释）。
  2. 设置页 + FAQ 页：`router/index.ts` 无 `settings`/`faq` 路由，`views/user/` 无对应文件。
  3. 订单列表状态筛选：`OrderListView.vue` header 右侧是写死的 `<span class="ol-filter">全部</span>`。
  4. 搜索结果页：`SearchResultView.vue` 只消费 `result.merchants`，`result.products` 丢弃；`searchApi.search` 未传 `page`/`size`。
  5. 会员价：`grep memberPrice src/` 只命中 `types.ts` 与替身注释，无渲染。
  6. 首页宫格：`HomeView.onCellClick` 只处理 `entry === 'coupons'`，其余一律 `toast('暂未开放')`。
- **逐项实现（全部走「先写失败用例取红 → 再实现 → 转绿」，红绿两端口径见下）**：

  1. **设置页 + FAQ 页（新 `TODO-USER-117`）**：新增 `FaqView.vue`（顶部栏 + 「常见问题解答」说明 + **5 条问答默认全部收起、点击本地展开、全程不发请求** + 底部「联系在线客服」占位提示；问答文案逐字取自设计稿 `09-消息与客服/04-常见问题/code.html` 而非自拟）、`SettingsView.vue`（固定菜单五项 + 版本号 `v0.1.0` + 通知/隐私占位 + 退出登录二次确认）、`ProfileView.vue`（资料展示，来源为会话与 `GET /me`，失败可重试）；路由 `faq`/`settings`/`profile`；我的页补「联系客服」（占位）/「常见问题 (FAQ)」/「系统设置」三条入口（对齐设计稿「常用功能」分组）。
     - **两处需要说明的判断**：① 品牌名——PRD 886 行原文写「关于饿了么」，但全库既有口径是**第三方品牌名不出现在本工程**（`MineView` 页脚、`MemberView` 标题均已替换并有用例 `not.toContain('饿了么')` 锁住），故写「关于轻量外卖」并在代码注释里写明出处；② **资料编辑如实标注「本期实现（待开发）」**——契约 §3.1 未定义用户资料写接口、后端也没有，故 `ProfileView` 为只读展示，不做假保存按钮（避免把未实现写成已完成）。
  2. **订单列表状态筛选（新 `TODO-USER-118`）**：新增 `utils/orderFilters.ts` 收口三件事——六个筛选取值、**接口 `status` 参数映射**、**前端收窄判定**。这里有一个必须先解决的口径问题：契约 §3.5 的 `status` 是**单值等值查询**，而「进行中」跨 `PENDING/COOKING/DELIVERING/PROCESSING`、「待评价」＝`COMPLETED`+`reviewed=false`，**单值参数表达不了**。处理办法：能一一对应的（待支付/已取消）直接下传参数，跨状态的（进行中不带参、待评价先收窄到 `COMPLETED`）由前端按同一口径收窄——真实后端与替身都不需要新增能力，也没有改契约。另：「已完成」与「待评价」按 `reviewed` 互斥，`reviewed` 缺失时按既有 TV-13 口径归「已完成」、不误判待评价。
  3. **规格弹层 + 会员价（新 `TODO-USER-119`）**：读后端代码确认**批次⑦ 的规格能力其实已在 main**（`CartService.validatedSelection` 强制**单选**：无规格传了规格 400、有规格未选 400、多选 400、规格名不存在 400；`unitPrice = price + priceDelta`；行按 `specKey`（规格名排序拼接）区分），因此按真实契约实现、替身逐条对齐同一套校验，而不是只做视觉。前端：`Product`/`CartLine` 类型补 `specOptions`/`priceDelta`/`memberPrice`/`tags`；`StoreDetailView` 新增规格弹层（有规格商品点加号先开弹层、单选、数量默认 1、预览价＝（基础价＋价差）× 数量、遮罩/关闭放弃输入、必选未选禁止提交并提示、超库存拦截、提交带 `specOptions` 加购、成功后关闭并刷新购物车栏），购物车弹层行显示规格文案；**会员价**按「接口返回才展示、缺失整块隐藏」渲染为品牌橙高亮行。替身补演示数据：`p107`（有规格 + 会员价）、`p108`（会员价 + 标签）、`p101` 会员价——**刻意只加在新商品上**，不改 `p101`/`p102` 等既有商品的无规格形态，避免把既有加购用例的语义改掉。
  4. **搜索结果页商品组 + 分页（新 `TODO-USER-120`）**：渲染商品结果组（图片/名称/描述/价格 + 会员价高亮，点击进所属商家详情）＋ 按契约 §3.6 分页对象实现「加载更多」（`page+1` **追加**两组、取完显示「没有更多了」、换关键词/切排序回第 1 页）；空态改为**两组皆空**才显示。
     - **说明一处旧断言的处理**：原 `SR-10` 断言「本页无逐区行 → 不渲染商品区」，与 PRD 831 行交互列「请求完成后更新商家结果**和商品结果**」冲突。按 R7 用**正向用例改写**（不是删除断言）：改断言「返回 products 时渲染商品组、点击进所属商家」。
  5. **首页宫格接线（新 `TODO-USER-121`）**：新增 `utils/courseCategories.ts` 承载**课程 10 类固定分类数据**（`pc01`~`pc09`），宫格 9 个课程分类项标记 `category: true` 并点击进入 `category-store-list`（携带 `categoryId` 与分类名）；**5 个超范围占位栏目与「天天爆红包」入口行为不变**。
     - **依据**：PRD 826 行字段列原文允许「分类项由课程 10 类点餐分类数据渲染（**课程固定分类数据**或分类接口返回）」，所以用固定数据承载不是自造口径。
     - **R7 文档同步（同一改动内完成）**：PRD 7.2 第 1 条、7.16.1「首页-分类宫格」行、「天天爆红包」入口行、7.16.1 表头「已补」注，以及 `docs/SRS.md` 对应两行——共 6 处；原「**分类宫格不做按分类过滤**」收窄为「**红包入口**不做按分类过滤」，并写明平台级分类**数据**本期仍不做。改后做了口径 grep 复查（旧表述 0 命中、新表述 6 处命中）。
     - **必须如实说明的剩余缺口**：平台级课程分类**在后端尚未落库**（契约 §3.2 的分类来源是店铺自身 `categories`，种子为 `c101`/`c201` 等，无任何店铺归属课程分类）→ **真实后端下宫格入口会落到「该分类暂无商家」空态**。按 PRD 835 行「接口返回空数组即空态，**不得用演示数据伪装成功**」，**没有做兜底假数据**；该缺口已登记为 `TODO-USER-121` 阻塞行（归口负责人 + 后端），并写进 PRD/SRS 对应行。

- **红绿两端证据（TDD，红端为「移出本轮实现后复跑」的真实失败）**：
  - 红端：把本轮 5 个新增实现文件（`FaqView`/`SettingsView`/`ProfileView`/`orderFilters.ts`/`courseCategories.ts`）移出工作区、并 `git stash` 掉 10 个被改的实现文件（路由、MineView、OrderListView、SearchResultView、StoreDetailView、cartStore、mocks/cart、mocks/store、api/types、api/cart）后复跑本轮新增/改写的 5 个用例文件 → **5 个测试文件失败 / 11 条用例红**（含 `Failed to resolve import "../FaqView.vue"`、`"../SettingsView.vue"`、`"@/utils/courseCategories"` 与 SPEC 组 7 条断言失败），恢复实现后全绿。
  - 绿端：`npx vitest run` **59 文件 / 471 用例全绿**（本轮前为 47 文件 / 356 用例）；`npm run build`（含 `vue-tsc` 类型检查）✓；**Playwright E2E 21 项全绿**（含保真度巡检与多视口断言，验证首页宫格与商家详情结构改动无回归）。
  - 分项用例数：FAQ 5 + Settings 6 + MineView 新增 1 + OrderListView.filters 6 + StoreDetailSpecs 7 + SearchResultView 10→13 + HomeView 19→22。
  - **浏览器实测（mock 模式 5173，Playwright，393×852，无控制台报错）**：① 首页宫格点「汉堡西餐」→ `/category/pc07?name=汉堡西餐`、标题「汉堡西餐」、空态「该分类暂无商家」（**即上述数据侧缺口在页面上的真实表现，已如实截图留档**）；点「超市便利」→ 停留首页并提示「暂未开放」；② 商家详情点「双人分享套餐」加号 → 弹层选「加量份」→ 弹层预览 **¥53.00** 与加购后购物车栏合计 **¥53.00** 一致，购物车弹层行显示「双人分享套餐 加量份」；③ 搜索结果页（keyword=肉）渲染 **4** 条商品卡 + 「没有更多了」；④ FAQ 页 5 条问答默认收起、点击展开、底部客服按钮为占位；设置页五项菜单与版本号按设计稿渲染。
  - 本轮截图存 `/tmp/gap-shots/`（临时自查用，未入库）；如需入库为复刻对照证据，按 `scripts/shot-replica-compare.cjs` 的口径补到 `docs/prd-assets/user/replica-compare-<日期>/`。

- **未做的事（遵守规则）**：**未提交、未推送任何改动**（按 Git 纪律，等负责人明确允许；本轮所有改动都在工作区，位于新分支 `feat/user-frontend-gaps-tdd`）；未改后端代码与数据库种子（负责人本批明确"与后端无关"）；未改契约；未替他人改台账状态；未创建/修改 `docs/record/daily/**`。
- **留待负责人决策的两件事**：① 是否把「平台级课程分类」写进后端种子/迁移（写进去宫格入口才有真实结果，但那是后端 + 发布动作）；② 规格弹层与会员价在 real 模式下依赖批次⑦ 部署（`TODO-MER-005` 实测线上 `/merchant/products/{id}/specifications` 仍 404）与商家端配置数据，是否在本轮之后排一次发布。

### B. AI 使用点

| # | 任务 | 类别 | AI 做了什么 | 人工审核与修改 | 结果 |
| --- | --- | --- | --- | --- | --- |
| 1 | 清单逐项核实 | 事实核对 | 不按清单字面动手，先对 6 项逐条 grep/读码定位到具体文件与缺失点，确认全部属实后才开工 | 负责人给出清单与优先级 | 6/6 属实 |
| 2 | 口径回读与冲突识别 | 需求分析 | 回读 PRD 7.6/7.10/7.12/7.16.1、契约 §3.2/§3.4/§3.5/§3.6/§4.2 与后端 `CartService`/`OrderController` 代码，识别出三处必须先定的口径：订单筛选跨状态值无法用单值 `status` 表达、宫格接线与「平台级 10 类不做」的冲突、`SR-10` 旧断言与 PRD 831 的冲突 | 负责人指示「接线」 | 3 处按既有真源+最小改动口径解决 |
| 3 | 规格链路对齐后端 | 代码生成 | 读后端 `validatedSelection` 逐条把替身校验写齐（单选强制、四类 400、单价加价差、`specKey` 分行），使前端与替身口径一次对齐，避免后续联调返工 | 待真机/发布后复验 | 替身与后端同口径 |
| 4 | TDD 实现 | 代码生成 | 6 项均先写失败用例再实现；红端用「移出实现后复跑」的真实失败取证，而非人为制造 | 负责人需复核页面观感 | 红 5 文件/11 用例 → 绿 471/471 |
| 5 | R7 文档同步 | 文档 | 宫格接线同步改 PRD 4 处 + SRS 2 处，并做口径 grep 复查确认无两套口径并存 | 负责人复核 | 旧表述 0 命中 |
| 6 | 缺口如实登记 | 风险提示 | 未把「宫格入口在真实后端恒为空态」这一数据侧缺口藏起来：既没有用假数据兜底，也同步写进了 PRD/SRS 与 `docs/todo/用户端.md` §3 阻塞行 | 归口负责人 + 后端 | 1 处阻塞已登记 |

- **本轮更新的 TODO 编号**：新增 `TODO-USER-117`（设置页 + FAQ 页 + 个人资料页）、`TODO-USER-118`（订单列表六筛选）、`TODO-USER-119`（规格弹层 + 会员价）、`TODO-USER-120`（搜索结果页商品组 + 分页）、`TODO-USER-121`（首页宫格接线），五条均落「已完成」并带红绿证据；`TODO-USER-107 ②` 的入口位置阻塞行由「待裁定」改写为「2026-09-15 已解除」；新增阻塞行 `TODO-USER-121`（平台级课程分类数据未落库）。**未替他人改状态**。
