# 2026-09-15 组长留痕

> 命名口径：按 `docs/todo/README.md` §3 第 5 条与 `docs/project/项目规则.md` §2 第 2 条 2.1–2.3，同日同角色只写本文件，后续轮次在文件末尾追加小节。

## 10:30（本轮：把「AI 前端 SOP」演示页大纲转成 Stitch 出图提示词）

### A. 进度事实

- **本轮任务来源**：负责人要求「参考 `docs/_archive/presentation/2026-09-10-结课汇报-AI前端SOP-演示页面大纲.md` 创建一个文件，里边写用于 Stitch 生成这个页面的提示词，可以查阅 Stitch 官方文档」；中途负责人纠正口径：「**页面的风格要根据我们的演示 HTML 来，而不是我们前端项目的设计风格**」。对应大纲 §待办第 4 项（本大纲转成演示页屏幕，负责人 + 组长）。
- **产出**：新增 `docs/presentation/stitch-prompts.md`（683 行 / 48478 字节，17 个代码围栏全部配对）。内容：§0 使用方式三步 → §1 项目级 `DESIGN.md`（演示页风格令牌）+「旁路样式块」→ §1.1 屏幕通用基线（画幅、颜色语义表、字体、形态、组件对照表）→ §2 主提示词（生成屏 1）→ §3 逐屏提示词（屏 2–屏 7，每条含 PLATFORM / PAGE STRUCTURE / CONSTRAINTS）→ §4 备用页 6 条提示词骨架 → §5 生成后怎么用（落位与复核）→ 附录数字速查表 → §参考（5 条官方来源 URL）。
- **官方依据（本机实地查阅，非凭记忆）**：Stitch Prompt Guide（`discuss.ai.google.dev/t/stitch-prompt-guide/83844`）；Google Labs `stitch-skills` 的 `generate-design/SKILL.md`、`manage-design-system/SKILL.md`、`references/prompt-keywords.md`、`examples/enhanced-prompt.md`。据此确定三条关键写法：① **生成提示词不写 hex / 字体 / 圆角**，视觉主题由项目级设计系统施加，编辑提示词才允许 hex；② 设备类型只有 `MOBILE / DESKTOP / TABLET`，**没有「幻灯片」类型**，故演示页按 `DESKTOP` 单屏信息图生成，7 屏 = 7 个 screen；③ 逐屏迭代、一次只改一屏。
- **风格真源修正（本轮最重要的改动）**：首版误把用户端移动 H5 的设计系统（`docs/design/设计系统-用户端.md`、`frontend/user-h5/src/styles/tokens.css`）当成本页风格；经负责人指出后，改为**逐字读取 `docs/presentation/index.html` 的内联样式**并转写：深蓝 `#063866`、深蓝底 `#042747`、天蓝 `#006ab4`、冰蓝 `#e8f2f9`、金色 `#c9a365`（**只作细线，不做填充**）、墨色 `#17324a`、弱化 `#61778c`、描边 `#d9e3eb`、纸底 `#f5f8fb`；宋体栈作标题与大号数字、黑体栈作正文、Consolas 作路径与数字；圆角 卡片 10px / 按钮与 chip 6px / 标签 4px；阴影只允许卡片 hover 与弹窗。同步重写了旁路样式块、§5「样式同源」与屏 1–屏 7 中所有「accent / hairline / #eeeeee / text-tertiary」等沿用旧口径的措辞，并补「历史词表映射」一行说明旧叫法对应的新取值。
- **与既有约定的对齐**：`docs/todo/用户端.md` §4.3 已有一套 Stitch 提示词约定（§4.3.1 统一约束块 + 各屏提示词 + 路径 A/B）。本轮**未改动**该节，只在新文件开头写明两者关系：那套是**移动端 App 原型**口径（390×844、品牌橙 `#ff5a1f`），本文件是**桌面演示页**口径，两者不是一套；但共同遵守「AI 出图只作参考，真源优先级 导出稿 > 设计系统 > 提示词」。
- **数字保全**：大纲里所有数字（32 个导出目录 / 88 张红框图 / 120 文件 / 243 素材 / 72 区行 / 27 组 / 11 视图 13 路由 / 单测 148 / 23/23 / 78.0% / 96.1% / 24/24 / 30.1% / 19.1% / 53px / 分带 7.2–33.3% / 12+3 处偏差 / 3101 行 / 47 个对齐点 / 2.1 分钟 / 81 分钟 / ≈93 分 等）逐条抄进对应提示词，并在 CONSTRAINTS 里明令「不得四舍五入、翻译、重算或替换任何数字」，另设附录速查表供出图后逐项核对。
- **落位理由**：参考大纲在 `docs/_archive/presentation/`，但 `docs/_archive/` 已被 `.gitignore` 忽略（大纲自身未入仓）；`docs/presentation/**` 已入 main（`git ls-files` 实测 32 个文件）。故新文件落 `docs/presentation/`；Stitch 的 `.stitch/` 产物目录同样在 `.gitignore` 中，已在 §5 写明「不入库，需留档则手动拷贝 HTML」。
- **本轮未做（遵守规则）**：**未 git commit / push**（按 Git 纪律等待负责人明确允许）；未改动 `docs/record/daily/**`；未改动 `docs/todo/用户端.md` §4.3 既有口径；未新建/改动任何业务代码与设计系统文档。
- **未了事项**：`docs/presentation/stitch-prompts.md` 中少数行首缩进由编辑工具规范化后略有差异（不影响代码围栏内文本语义）；本文件尚未经负责人按最终时长与裁剪方案（4 分钟版 / 3 分钟版）确认屏序。

### B. AI 使用点

| # | 任务 | 类别 | AI 做了什么 | 人工审核与修改 | 结果 |
| --- | --- | --- | --- | --- | --- |
| 1 | 官方文档调研 | 事实核对 | 抓取并比对 Stitch Prompt Guide 与 Google Labs `stitch-skills` 四个文件，提炼出「生成不写主题令牌、编辑才写 hex」「无幻灯片设备类型」「逐屏迭代」三条可执行口径，作为全文件写法依据 | 负责人指定「可查阅官方文档」；口径由 AI 提出 | 确立三条写法依据并写入文件开头 |
| 2 | 大纲转提示词 | 内容生成 | 把 7 屏 + 6 备用页的大纲逐屏翻译成 Stitch 可粘贴提示词（目的 + PLATFORM + PAGE STRUCTURE + CONSTRAINTS），并把全部数字与五列表头等原文逐条抄入 | 负责人要求「写用于 Stitch 生成这个页面的提示词」 | 新增 `docs/presentation/stitch-prompts.md` |
| 3 | 风格真源纠错 | 自检 + 返工 | 首版按用户端移动 H5 设计系统（品牌橙 `#ff5a1f`）起草；经负责人指出后改为逐行读取 `docs/presentation/index.html` 内联样式并重写 DESIGN.md、旁路样式块、§5 与 7 条屏提示词中的全部样式措辞 | **负责人直接纠正口径**：「风格要根据我们的演示 HTML 来，而不是我们前端项目的设计风格」 | 风格令牌全部换为演示页实测取值 |
| 4 | 数字防篡改 | 风险提示 | 预判 Stitch 会改写数字（官网示例以文案润色为主），在每条 CONSTRAINTS 写明「数字权威、不得改写」并附大纲数字速查表 | 无 | 出图后可按表逐项复核 |
| 5 | 落位与边界判断 | 结构维护 | 用 `git ls-files` 与 `.gitignore` 确认 `docs/_archive/` 不入库、`docs/presentation/` 入库、`.stitch/` 忽略，据此决定新文件落位并在 §5 写明留档方式 | 无 | 落位 `docs/presentation/stitch-prompts.md` |

- **本轮更新的 TODO 编号**：无（本轮为大纲 §待办第 4 项的下游准备材料，未改动 `docs/todo/**` 任何任务行的状态；若负责人认可，可在 `docs/todo/文档与交付.md` 的 `TODO-DOC-007`（报告、PPT、截图包、演示脚本）证据列补记本文件）。

## 11:07（本轮：规划结课汇报「工作设计」板块的演示页大纲初稿）

### A. 进度事实

- **本轮任务来源**：负责人要求「为结课汇报演示 HTML 规划一段主题为小组协作 / 项目规则 / AI 智能体工作流程的**工作设计**板块；各组都会用 AI 写代码，但 **AI 与 AI 之间有差别**，分工方式与让 AI 工作的方式决定质量与效率；在归档文件夹里先创建一个初稿，提供给做演示网页的同学作为大纲，**只承担顶层结构与关键点，不细化到每个页面及其细节**」。对应 `docs/todo/文档与交付.md` 的 `TODO-DOC-007`（报告、PPT、截图包、演示脚本）。
- **产出**：新增 `docs/_archive/presentation/2026-09-15-结课汇报-工作设计板块（协作·规则·AI工作流）-演示页面大纲初稿.md`（**v0.2，298 行 / 12911 字节**）。结构：§0 动机 + 一句话主张 + 三条支撑 + 与既有 SOP 板块的边界 → §1 四层闭环图（规矩层 / 事实层 / 执行层 / 反馈层）→ §2 章节骨架 6 节（每节：论点 + 必须出现的要素 + 证据锚点）→ §3 已拍板结论 → §4 时长与裁剪 → §5 数字口径 → §6 诚实边界 → §7 证据索引 → §8 结课前准备事项。
- **负责人当场拍板的 6 项**（写入大纲 §3）：① 定位=**独立板块，硬上限 3 分钟**；② 与「AI 前端 SOP」板块边界**按初稿 §0.4 切分**（SOP 讲产品链；本板块讲组织、规则与门禁）；③ 讲法=**制度设计 + 一个微案例**（主线为 PR #9 打回 → 新规则 → R1 合并前必跑矩阵 + `commit-hygiene.sh` + CI）；④ 失误案例**保留截图与 PR 编号但不写姓名**（页面上须裁切/遮挡 GitHub 账号名）；⑤ **不做现场动作，全静态**；⑥ 数字**用 2026-09-15 本机实测值**，页面页脚注明时点与本机口径。
- **本机实测数字（已写入大纲 §5；页脚口径行 = 数据时点 2026-09-15 · 本机实测口径 · 基线 `main @ ac2b0fb`）**：提交总数 **473**；`Merge pull request` 类合并 **51**（`--merges` 总数 68，**不等于 PR 数**）；提交类型分布 docs 151 / test 96 / feat 64 / fix 52 / chore 39；`docs/todo/**` TODO 编号引用约 **215 处**（用户端 114 / 文档与交付 31 / 后端 29 / 商家端 25 / 部署 16）；`docs/record/raw/**` **83 个文件**（2026-09-05 起 11 天，09-08 单日 30 个）；`docs/testing/缺陷跟踪表.md` 570 行。
- **本轮未做（遵守规则）**：**未 git commit / push**（按 Git 纪律等待负责人明确允许）；未改动 `docs/record/daily/**`；未改动 `docs/todo/**` 任何任务行的状态；未改动任何业务代码、既有演示页与设计系统文档。

### B. AI 使用点

| # | 任务 | 类别 | AI 做了什么 | 人工审核与修改 | 结果 |
| --- | --- | --- | --- | --- | --- |
| 1 | 素材盘点 | 事实核对 | 通读 `AGENTS.md`、`docs/project/项目规则.md`、`docs/project/小组协作与提交流程.md`、`docs/todo/README.md`、`docs/presentation/README.md` 与既有两份 SOP 板块大纲，抽出可讲的组织/规则/门禁事实与数字 | 负责人给出板块主题与动机 | 形成大纲 §0–§1 的证据基础 |
| 2 | 规模数字实测 | 事实核对 | 用 git 与文件系统命令实测提交数、类型分布、PR 类合并数、TODO 引用数、raw 片段数、缺陷台账行数，并标注"引用数 ≠ 唯一编号数" | 负责人拍板「本机实测 + 页脚注明时点」 | 大纲 §5 数字口径表 |
| 3 | 初稿撰写 | 内容生成 | 按既有板块大纲体例产出 v0.1，随后按负责人 6 项拍板改写为 v0.2（7 节压成 6 节、截图去姓名纪律、全静态、页脚口径行） | 负责人通过提问工具逐项拍板 | 大纲初稿 v0.2 |
| 4 | 风险提示 | 边界判断 | 主动写出 6 条诚实边界（分支保护开不了、记录靠人、历史脚本未入库、测试口径多版本、"引用数"口径、不是全自动流水线） | 无 | 大纲 §6 |

- **本轮更新的 TODO 编号**：无（未改动 `docs/todo/**`；如负责人认可，可在 `TODO-DOC-007` 的证据列补记本文件与大纲 §8 的准备事项）。

## 11:20（本轮：规划「规则生效之后」专题的演示页大纲初稿）

### A. 进度事实

- **本轮任务来源**：负责人要求「针对『中期汇报前那两个被打回的 PR 之外，后续还有没有被审查发现并修正的类似案例』这一点，在结课汇报网页里用一个专题来讲，**重点就是归纳出的那五个方面**，参照归档文件夹里已有的展示大纲，写一个提供给做展示页面同学的大致内容」。
- **先做事实核查、再写大纲**（避免把推断写进演示材料）：
  - `gh pr list --state all --limit 100 --json number,title,reviews`：全仓库人工 `CHANGES_REQUESTED` **仅 3 次**——PR #1 `2026-09-03T14:14:44Z`、PR #3 `2026-09-04T05:13:33Z`（Feat/backend b init）、PR #9 `2026-09-06T08:17:36Z`；**9/9 之后 0 次**。
  - `gh run list`（9/9–9/15）逐条取失败任务与日志断言行，得 9 起可作为案例的失败：`34460112417`（9/10 用户端单测 `StoreDetailView.spec.ts:249 Cannot call text on an empty DOMWrapper`，1 failed | 147 passed）、`34567580642`（9/11 同一条复现）、`34577446162`（**唯一一次「提交规范检查」失败**，即 PR #41）、`34686667600`（9/12 后端集成 `Tests run: 55, Failures: 1, Errors: 1`，只在 UTC 环境暴露的时区 bug）、`34822004178`/`34863364340`/`34864527098`（9/14 `BlastOverlay.spec.ts` RB-8 `Cannot call trigger on an empty DOMWrapper`）、`34861642297`（9/14 MC-R4 店名断言偶发）、`34867327171`（9/14 main 后端集成，**未见修复记录**）。
  - 关键提交复核：`git log -1` 读 `37d6625`、`0d1381b` 全文（含「R1 必跑矩阵的第三次印证」原话）。
- **产出**：新增 `docs/_archive/presentation/2026-09-15-结课汇报-规则生效之后（被门禁抓住的错误）-演示页面大纲初稿.md`（v0.1，约 330 行）。结构：§0 动机/一句话主张/三条支撑/与既有两板块的边界 → §1「五层拦截网」顶层图（第 0 层自检 → CI 提交规范 → CI 构建与单测 → 服务器构建与真实环境 → 独立复跑与审计）→ §2 章节骨架 7 节（每节：论点 + 必须出现的要素 + 证据锚点 + 呈现纪律）→ §3 五方面→章节速查 → §4 时长与裁剪（4 分钟默认 / 3 分钟 / 2 分 30 秒与工作设计板块同场版）→ §5 数字与编号口径 → §6 诚实边界 7 条 → §7 证据索引 → §8 结课前准备事项 → 附：与既有材料的关系。
- **五个方面与章节映射（本专题骨架）**：方面① 提交/TDD 证据链被 CI 打回 → 节 3（主打，与 #9 同类但被脚本拦下）；方面② 漏跑验证矩阵被构建抓回 → 节 4（催生 R1 + 三次印证）；方面③ 本机绿、CI 红 → 节 5（时区 / 慢机 flake / 等待时机）；方面④ 流程纪律失误 + 自查纠正 → 节 6（stash 顺序、误落 main、自建守卫）；方面⑤ 审查方自身误判被纠正 → 节 7（142 条打回 5 条、差点报错、哈希口径作废），并把未收口一条挂在同一屏。
- **本轮查出的三处口径风险（已写进大纲 §6 与 §8，待负责人定）**：① 中期演示页讲「两次 PR 打回」，**实际为三次**（PR #3 也是 `CHANGES_REQUESTED`）；② 9/14 main `34867327171` 的后端集成测试至今为红（`MerchantAnalyticsStage2IntegrationTest` / `java.lang.AssertionError: JSON path "$.data.todaySalesAmount" expected:<26.5> but was:<0.0>`，响应 `startDate/endDate=2026-09-15`，run 时间 `2026-09-14T16:14Z` ≈ 北京 9/15 00:14，疑跨零点），**未见修复记录**；③ 留痕自述 PR #65 合并时「用户端任务当时在跑」，即存在一次 CI 未完全绿时的合并——与本专题「门禁接棒」的主张有张力，已要求页面主动交代。
- **沿用既有已拍板口径**：保留 PR 编号 / run 号 / 提交哈希与截图，但**不写姓名**（改用「用户端同学」「后端同学」「测试联调同学」「组长」）；**全静态不做现场动作**；数字用 2026-09-15 本机实测并在页脚注明时点与基线。
- **本轮未做（遵守规则）**：**未 git commit / push**；未改动 `docs/record/daily/**`；未改动 `docs/todo/**` 任何任务行状态；未改动业务代码、既有演示页（`docs/presentation/index.html`）与设计系统文档；未动 `docs/presentation/stitch-prompts.md`。
- **未了事项**：本专题是**独立板块**还是**并入「工作设计」板块做节 3 之后的两屏**尚未拍板（决定时长与屏序，两份大纲目前在 PR #9 上有重叠）；专题时长未定；关键短哈希与 run 号需上台前复核一次。

### B. AI 使用点

| # | 任务 | 类别 | AI 做了什么 | 人工审核与修改 | 结果 |
| --- | --- | --- | --- | --- | --- |
| 1 | 案例查证 | 事实核对 | 用 `gh pr list --json reviews` 与 `gh run list/view --log-failed` 逐条取证，把「中期后有没有类似案例」从印象变成可核对清单（3 次人工打回 + 9 起 CI 失败 + 对应提交哈希），并逐条抓取真实报错行 | 负责人提出专题需求与"五个方面"的重点 | 五方面案例清单 + 证据编号 |
| 2 | 归类与主张提炼 | 内容生成 | 把 9 起案例按"被谁抓住"归为五类，提炼出「同一条纪律从靠人记得住变成机器每次都查」「越靠前越便宜」两条主线，并设计五层拦截网图 | 无 | 大纲 §1–§3 |
| 3 | 体例对齐 | 结构维护 | 通读归档里既有两份 SOP/工作设计板块大纲，沿用其体例（主张 → 顶层图 → 逐节论点/要素/证据 → 裁剪 → 数字口径 → 边界 → 索引 → 待办），并写明本专题与两板块的边界与衔接话术 | 负责人指定"参照当前归档文件夹里已有的那些大纲" | 大纲 §0.4 / 附 |
| 4 | 口径风险提示 | 风险提示 | 主动标出三处与既有说法冲突或未收口的事实（两次打回实为三次、9/14 main 测试仍红、一次 CI 未全绿即合并），并给出"上台前修掉或在台上如实讲"的处置建议 | 待负责人拍板 | 大纲 §6 / §8 |
| 5 | 留痕与边界判断 | 结构维护 | 按 R9 判断本文件属规划/讲稿类材料，落 `docs/_archive/`（已 gitignore、不进 main），并在文件头写明存放边界；同步按规则把本轮片段追加到当日 raw 单文件 | 无 | 本小节 |

- **本轮更新的 TODO 编号**：无（未改动 `docs/todo/**`；如负责人认可，可在 `docs/todo/文档与交付.md` 的 `TODO-DOC-007`（报告、PPT、截图包、演示脚本）证据列补记本专题大纲。）


## 13:11（本轮：集中攻坚「遗留缺陷清单」——用户端 A 组 + 后端 B 组 + 数据/部署侧，随后按负责人指示转入前端）

### A. 进度事实

- **本轮任务来源**：负责人下发「修复轻量外卖项目当前遗留的全部缺陷（用户端 + 后端四缺口）」清单，并要求先问三件事（Git 授权、A2–A5 口径裁定、C1/D2 是否本轮做）再动手。中途负责人改口径：**后端交给后端同学自己修，先处理前端问题**；对已完成的后端改动裁定为「全部保留（代码 + 文档），等后端同学实现后由负责人对比取舍」。
- **开工前处置**：本地 main 停在 `ac2b0fb`、落后 origin/main 12 个提交；负责人开启代理后实测 `git ls-remote origin` 可达，`git merge --ff-only origin/main` 快进到 **`471408b`（PR #66）**，并新建分支 **`feat/remaining-gaps-tdd`**。工作区原有三处未提交内容（`.gitignore` 增 `.mimosa/`、`docs/presentation/stitch-prompts.md`、`docs/record/raw/2026-09-15/组长.md`）**全部保留并分别提交**，未丢弃他人改动。
- **负责人逐条裁定（写入对话，本轮据此执行）**：Git「可以提交、可以推送、可以开 PR、可以直接合并」；**A2 改文档**（删 PRD 6.2/7.1 的注册性别，注册字段统一账号/密码/昵称）；**A3 改 PRD 承诺**（本期不提供店铺券领取能力）；**A4 删 PRD 该句**（本期不做会话关闭态）；**A5 改 PRD**（分类商家列表页不分页、不筛选，排序去掉「评分」）；**C1 补种子/迁移**给店铺挂课程分类编号 `pc01`~`pc09`；**D2 本轮只做代码与种子**，发布由负责人安排。
- **前端 A 组交付**：**A1（RB-8 flaky）已修**——只改测试，删券前用 `vi.waitFor` 等 `loadCoupons()` 的 200~500ms 随机延迟落地，并加断言锁定「页面持有过期券快照」；**红端实测复现 1/5**（`Error: Cannot call trigger on an empty DOMWrapper.`，失败行 255，本地 Windows/Node24；CI Linux/Node22 一直绿）→ 绿端单文件五连跑全绿 + 全量 vitest **59 文件 / 471 用例全绿** + `npm run build` ✓；缺陷登记 `BUG-20260915-001`。**A2–A5 全部是「改文档」**，已按 R7 在同一 PR 内改齐 PRD/SRS/清单/台账并做 grep 复查（被删旧表述 0 命中）。
- **前端问题清零（转入前端后的成果）**：扫 `docs/todo/用户端.md` 发现 6 条「进行中」中 4 条（`TODO-USER-107/114/115/116`）其实已在 main 落地、只是台账滞后；真正剩的工程项是 `TODO-USER-110`（替身与契约 §3.10 形状不一致）与 `TODO-USER-112`（桌面宽视口按 vw 放大 ~3 倍）。两项**均已完成**：
  - `TODO-USER-110`：`mocks/coupon.ts` 爆一次响应改为契约 §3.10 **扁平形状**（券字段 + `tierIndex` + `freeBlast`），删除 `normalizeBlastResult` 的嵌套兼容分支（已无生产者，避免两套形状并存）。
  - `TODO-USER-112`：新增 `src/utils/vwScaleCap.ts`（构建期 PostCSS 插件 + 纯函数），在 `pxToViewport` 之后把每个 `N vw` 收敛为 `min(Nvw, N×4.3px)`：**≤430px 视口逐像素不变**（CE 用例锁 320/360/375/390/430 五档），>430px 冻结在 430px 口径。**实现踩坑**：首版用 PostCSS `Declaration` 访问器改写 `decl.value`，PostCSS 8 会把节点标脏并再次触发该访问器，`min()` 被层层包裹导致 `vite build` **数十分钟不收敛**（实测 5 个 vite 进程各烧掉 1000+ 秒 CPU）→ 改为 `Once` + `walkDecls` 并让正则排除已收口的 `min(…vw,…px)`；插件落位 `src/utils/` 而非 `build/`（仓库 `.gitignore:8` 忽略任意 `build/` 目录，放那里不会入库）。
  - `docs/todo/用户端.md` 的「进行中」行数由 **6 降为 0**；`TODO-USER-116 ①`（PRD §5.1 会话标识口径）按实现回写 `docs/frontend/AI点餐助手前端PRD.md` §1/§5.1。
  - 验证：全量 vitest **60 文件 / 481 用例全绿**、`npm run build` **945ms** ✓（修复前 build 无法收敛）。
- **后端 B 组（负责人后续裁定保留）**：按 TDD 先补 4 个失败集成用例（红：`Tests run: 27, Failures: 25, Errors: 1`）再实现——`GET /search`（§3.6，两组分页对象 + 三种排序 + 平台课程分类条件）、收藏三接口（§3.7）、通知四接口与触发时机（§3.9）、`GET /me/member` 与 PRD 7.4 第⑤步会员价不叠加折扣（§3.8）；`stores` 扩列 `distance_km`、`ViewMapper` 输出 `distanceText`/`couponTags`；`GlobalExceptionHandler` 补 405（此前不支持的方法落到兜底 500）。**服务器测试库 `elm_practice_test` 全量 `mvn test` = Tests run: 105, Failures: 0, Errors: 0**。口径影响：`u001` 由种子标记为会员后会员折扣真正生效，既有计价用例期望金额按 95 折重算（PricingIntegrationTest 3 处、OrderServiceTest 1 处、CouponIntegrationTest 1 处）并补 `memberDiscountAmount` 断言。
- **数据/部署侧**：C1 新增 `platform_categories` + `platform_category_stores`（`pc01`~`pc09` 与用户端 `COURSE_CATEGORIES` 一致）三处种子脚本同步、可重复执行；D2 补商品会员价（p102/p103/p106/p205/p208）与规格（p103/p106）种子；D1 `deploy/nginx.conf` 新增 `location /uploads/` 反代并把 `client_max_body_size` 由 2m 放宽到 4m。
- **本轮未做（守规则）**：**未合并 main、未直接推送 main**；未改动 `docs/record/daily/**`；未替他人改状态（`docs/testing/缺陷跟踪表.md` 只登记自己发现的缺陷、`docs/todo/后端.md` 的状态更新按负责人 B5 指示与实现证据执行）。
- **已知未收口**：① 线上仍是旧版本，以上全部**需一次重新发布**才能进入 real 复验；② `TODO-USER-022`（启动页横滑）前提不成立（仓库无启动页实现、PRD 无对应行）**仍待负责人指明**；③ `TODO-USER-028`（「今日免费爆是否可用」无查询来源）与 `TODO-USER-116 ③④⑤` 归后端/部署；④ 后端四组接口虽已实现，但后端同学表示将自行实现，负责人裁定先保留本分支对比取舍。

### B. AI 使用点

| # | 任务 | 类别 | AI 做了什么 | 人工审核与修改 | 结果 |
| --- | --- | --- | --- | --- | --- |
| 1 | 开工前事实核对 | 事实核对 | 实测本地/远端提交差、未提交内容归属、GitHub 可达性、本机与服务器工具链（Node 24 / Java 21 / Maven 3.9）、服务器测试库凭据来源（`/home/ubuntu/.pm2/dump.pm2` 的 `DB_USER`），并发现「本机连服务器库被拒（`elm_local`@本机 IP）→ 必须在服务器侧跑测试」 | 负责人提供服务器信息与授权 | 确立可复现的验证链路 |
| 2 | 口径裁定提问 | 需求澄清 | 把清单里的三件事拆成 7 条带推荐项的提问（Git 授权、A2/A3/A4/A5、C1、D2 与测试库访问），一次性收敛 | **负责人逐条选择/补充** | 全部口径落地后才动手 |
| 3 | 前端 flaky 修复 | 缺陷修复 | 按给定根因（`loadCoupons` 随机延迟 vs 实时 computed）只改测试、用 `vi.waitFor` 消除竞态，并复现红（1/5）后取证绿 | 约束由主代理给定 | `BUG-20260915-001` 关闭 |
| 4 | 桌面 vw 缩放收口 | 方案设计 + 返工 | 侦察 vw 使用面与 390 基准，否决容器查询方案（`contain: layout` 会改写 20+ 处 fixed 定位基准），落地 `min(Nvw, N×4.3px)`；首版 PostCSS 访问器写法导致 build 不收敛，由构建实测暴露后改为 `Once`+`walkDecls` 并补幂等 | 方案取舍经人工确认；卡死后由主代理介入定位进程与插件 | build 从「数十分钟不收敛」到 945ms |
| 5 | 后端四缺口实现 | 实现 | 先补失败用例（红 27/25F/1E）再实现搜索/收藏/通知/会员与计价第⑤步，并在服务器测试库跑到 105/105 绿 | 契约逐条比对，异常场景经人工确认 | 后端接口缺口清零（保留待对比） |
| 6 | 台账与文档同步 | 结构维护 | 按 R7 同 PR 改齐 PRD/SRS/契约/清单/台账并 grep 复查；把 6 条滞后「进行中」按 main 中的实现落点逐条核对后收口 | 负责人裁定「保留后端文档回写」 | 用户端「进行中」归零 |

- **本轮更新的 TODO 编号**：`TODO-USER-107`、`TODO-USER-110`、`TODO-USER-112`、`TODO-USER-114`、`TODO-USER-115`、`TODO-USER-116`（含子项 ①）、`TODO-USER-121`、`TODO-USER-005`、`TODO-USER-007`、`TODO-USER-109` 证据列；`TODO-BE-003/005/006/007/008/009/010/011` 状态与证据；`TODO-OPS-005` 证据列。`docs/testing/缺陷跟踪表.md` 新增 `BUG-20260915-001`。

## 14:20（本轮：PRD / SRS 两版 / 连带与部署文档的「交叉验收参照」专项只读审查）

### A. 进度事实

- **目的（老师口径）**：9/16 双盲交叉验收按**对方组的 SRS** 出题，交付版 `docs/SRS.md` 即验收参照物；本轮只读回答两个问题——「PRD 与 SRS 是否需要细节修改」「`docs/SRS-工程版.md` 有无保留必要、是否分散进其他文档」。**未修改任何文档**。
- **执行方式**：并行派 4 个只读子代理（PRD 线 / SRS 交付版线 / 工程版存废线 / 连带+部署线），提示词内写死只读红线（禁改文件、禁 git 写、禁触碰 `docs/record/**`）；主代理另做结构与关键口径的独立核对。
- **实测行数**（修正 `Measure-Object -Line` 低估与旧审计基准）：`docs/PRD.md` 1198、`docs/SRS.md` 893（旧审计基准 767）、`docs/SRS-工程版.md` 479（旧审计基准 478）、`docs/backend/后端接口契约.md` 653。
- **结论一（要不要改）**：要改，但全部是原位替换/加注级，不需结构重写。高危项：PRD 9 条（§4 完成状态表失实 `docs/PRD.md:306-315`、页数 6 页 vs 7 页 `:353`/`:795`、§6.1.1 已砍行两列未标 `:374`、§7.6 `PROCESSING` 偏差句 `:666`、订单筛选口径 `:855`、平台级分类四套口径、AI 点餐助手范围外夹带、§9「尚未交付」`:1133`）；SRS 5 条（`FR-AST-01` 矩阵有正文无 `docs/SRS.md:120`、`FR-AI-02/03/04` 正文有矩阵无、`FR-OST-01` 在 `:338` 与 `:382` 重复、§6.2.1 订单详情与支付页各两套行 `:680-683`/`:686-687` 与 `:704`/`:706`、`FR-DLV-01` 在 `:442` 仍挂「P2（本期）」）；契约 3 条（配送接口与错误码示例残留、§9 过期「待开发/证据待补」）。
- **结论二（工程版存废）**：**保留 + 改定位，今天不拆分、不删除**。依据：`FR-` 真源是 `docs/SRS.md`（98 次 vs 工程版 48 次），`TC-` 真源是 `docs/project/TDD实施规划.md`（215 次 vs 13 次），代码侧 `FR-` 0 次 / `TC-` 269 次（frontend 206 + backend 63）——文档自述的「FR/TC 编号真源」不成立，应改的是声明；独有内容仅 3 处（§2「FR→契约章节」列、§7 NFR-01~10 被 `docs/SRS.md:841` 硬引用、§5.2 数据字典）；§4/§8/§9/§10 属重复或过期，且文内有 4 处作者自注 `<!-- TODO(组长) -->` 自认 §8.4/§9/§10 该搬走。
- **主代理独立核实**：① `docs/backend/架构设计.md` §4 已是 20+ 实体的字段级表 → 工程版 §5.2 独有价值有限；② `deploy/*.py` 无任何脚本读取 `release-manifest.json`（grep 0 命中）→ 改动工程版不会让 CI 变红，仅清单 sha256 失配；③ 根 `README.md:12-13/46/52/56` 仍写「待初始化 / 唯一有效 PRD（v1.0）/ `docs/demo/**`」→ 外组第一入口即断链；④ 契约已回写「已实现，2026-09-15」，服务器测试库 `mvn test` = Tests run 105 / Failures 0 / Errors 0；⑤ SRS 图片与路径引用 169 处 + 41 处，**0 断链**。
- **必须今天处理的泄露面（非口径问题）**：真实公网 IP `82.157.137.114`、SSH 账号名单 `wjy/wxq/lc/fzp/dyh`、DB 账号 `elm`/`elm_local`、`deploy/smoke.py:4` 写死 base 与 `deploy/smoke.py:22-23` 固定口令 `Audit1234`（可对线上注册账号、开店、下单）均在 Git 跟踪文件内。
- **本轮未做（守规则）**：未修改任何 PRD/SRS/契约/清单文档；未改 `docs/todo/**` 状态、未替他人登记或关闭任务行；未触碰 `docs/record/daily/**`；未执行 git 提交或推送。

### B. AI 使用点

| # | 任务 | 类别 | AI 做了什么 | 人工审核与修改 | 结果 |
| --- | --- | --- | --- | --- | --- |
| 1 | 交叉验收参照物专项审查（方法设计） | 方案设计 | 把「SRS 当参照」拆成 4 条互不重叠的只读审查线，各写自包含提示词（范围口径、旧审计复核要求、输出格式、只读红线） | 负责人提出审查目标与两个问题 | 四条线报告齐备，全程未改文件 |
| 2 | 旧审计复用与现状复核 | 事实核对 | 以 `docs/_archive/doc-audit-2026-09-11/02-SRS.md`（F-01~F-23）为清单逐条复核「已修/部分修/未修」，并发现其基准行数已失效（767→893） | 负责人提供历史审计线索 | 交付版 11 条已修 / 3 条部分修 / 1 条未修；工程版 2 修 3 半 3 未修 |
| 3 | 工程版存废判定 | 方案设计 | 用引用普查（`FR-`/`TC-` 在 docs、deploy、.github、frontend、backend 的分布）证伪「FR/TC 编号真源」，给出保留 / 瘦身 / 拆分删除三方案的风险—工作量对比 | 结论待负责人拍板 | 建议方案 B 轻量版（改定位 + 修 5 处），删除推迟至 9/18 之后 |
| 4 | 泄露面扫描 | 风险识别 | 对 `deploy/**`、运行说明、后端 README/架构设计/puml/测试配置逐处定位公网 IP、SSH 账号、DB 账号与写死口令，并区分「Git 跟踪内」与「`docs/_archive/**` 未入库」 | 负责人决定脱敏范围与时机 | 28 项；其中冒烟脚本写死口令（可写线上数据）风险最高 |

- **本轮更新的 TODO 编号**：本轮**未修改任何 TODO 状态**；以下建议由组长确认后登记（编号待负责人指定，暂记）：`TODO-DOC-015` 文档口径残留清理（PRD P-01~P-20、SRS 19 条、契约 3 条）、`TODO-DOC-016` 敏感信息脱敏（真实 IP / SSH 账号 / DB 账号 / 冒烟写死口令）、`TODO-DOC-017` `docs/SRS-工程版.md` 改定位为「工程索引」并修 5 处过期项。三件均为 `TODO-DOC-006`（R7 口径同步复查）与 `TODO-DOC-005`（交叉验收包匿名化）的子项。

## 15:10（本轮：按当前代码进度执行文档整改 —— PRD/SRS/连带/deploy 四线 + SRS 工程版方案 C 拆分删除）

### A. 进度事实

- **负责人拍板（本轮 4 项）**：① 敏感信息（真实 IP、SSH 账号、DB 账号、冒烟写死口令）**由负责服务器的同学改，本轮不动**；② 其余文档**按当前真实代码进度改**；③ **AI 点餐助手正式纳入交付范围，用户端扩展服务 13 → 14 页**（含商家端扩展 6 页、商家端合计 13 页、全站合计 37 页）；④ `docs/SRS-工程版.md` 走 **方案 C：拆分进其他文档后删除**。
- **执行方式**：4 条并行改稿线（PRD+AGENTS / 连带文档 / 工程版迁移与引用 / 契约+deploy+运行说明）+ 主代理自改 `docs/SRS.md`；**文件互不重叠**；全程只改工作区，**未执行任何 git 写命令**。
- **`docs/SRS.md` 升 v1.3（主代理亲改）**：补 `FR-AI-02/03/04` 进追踪矩阵、`FR-AST-01` 注明为分组编号；删订单详情页与支付页**共 5 行重复旧行**（含已划删的订单跟踪行）；删 §5.7 重复 `FR-OST-01` 块并补全状态机链（支付成功进 `PENDING`）；`FR-DLV-01` 移出需求正文；页数 13→14；§8.1 补**固定演示数据**（`13800000001/123456`、`merchant-a/123456`、`m002`、`p101` 起）与 **4 条异常链路**；§8.3 每条**回挂 FR 编号**并补爆红包判据（权重和 100%、固定种子断言、失败不消耗次数与券）与 AI 验收条；§7 指针改指 `docs/project/项目规则.md` 附录 A；金额汇总卡删旧等式「实付 = 商品合计 + 打包费」；红包适用范围改 `ALL`/`STORE`；排序去掉「评分」；平台级分类按**代码实证**改为「`platform_categories` 已落库，不做的是管理后台」。
- **PRD + AGENTS（子代理，16 组）**：§4 完成状态表**由全「待实现」正向化**（含新增 AI 行）；`:352/:353/:357` 加 AI 行、13→14 页并给出**唯一合计 37 页**；`:798` 取消确认弹层已入库；`:378` 已砍行两列标「已砍（CHG-002）·不实现」；`:669` 删 `PROCESSING` 实现偏差句；`:862` 六项筛选；平台级分类 8 处统一；`:1140` 改 VIS-002/003 待补；`:837` 天天爆红包移出占位；`:939/:941` 取消口径统一；`:596/:597` 排序统一；**删除误写入的一行脏数据**（`[Showing lines 583-583 of 1196...]`）；`AGENTS.md:22` 同步 14 页/6 页；`:3`/`:22`/`:295` 三处工程版指针改指 `docs/backend/**` 与项目规则附录 A。
- **连带文档（子代理，8 文件 31 处）**：`docs/README.md`、`docs/project/项目目录索引.md`、`未完成模块与如实标注清单.md`（线上版本统一 `20260914-a2f15b1`）、`开工指引.md`、`docs/frontend/前端架构设计与工程约定.md`（端口统一**商家端 4001**）、`docs/frontend/README.md`、`用户端-real模式联调清单.md`、`docs/testing/README.md`（断链示例改实存 `回归记录-2026-09-08.md`）。
- **方案 C 拆分与删除（子代理迁移 + 主代理删除）**：独有内容 3 处落点已核实存在——NFR-01~10 → `docs/project/项目规则.md:173` 附录 A；36 条 FR ↔ 契约章节映射 → `docs/backend/后端任务分配与联调清单.md:103`；P0 数据字典 → `docs/backend/架构设计.md:80` §4.1。§8.4 证据清单（已过期）、§10 时间锚点（与分工与里程碑重复）、§0 分工表（已失效）**不迁移**。12 处引用同步（含 `docs/maps/**` 四个 HTML、`docs/presentation/index.html`、`docs/backend/后端B任务清单.md`、`deploy/release-manifest.json` 加 `_note` 并删工程版 sha256 条目，JSON 校验通过）。**`docs/SRS-工程版.md`（479 行）已删除**，删除后全库 grep 残留 16 处**逐条核对均为「已拆分归档」迁移说明或历史台账行，无现行指针**。
- **终检证据**：`13 页 / 7 页 / 99a646d / 707f234 / 5174 / 工程版第 2 章 / 即将拆分 / 配送单和配送状态 / 三方状态演示 / 待开红包 / 8 档` 共 11 个关键词全库 grep（排除 `docs/record/**`、`docs/_archive/**`）；`docs/PRD.md` 的「待实现」**0 命中**；7 份被改文档的 Markdown 表格列数**0 处异常**。终检另发现并修掉 3 处漏网：`docs/SRS.md` §6.1.1 同源副本「简化配送管理」行后端/证据列、同表「平台管理员」行验收证据列、`docs/presentation/index.html` P2 卡片仍写「P2 仍为候选、未承诺全部开发」。
- **四条改稿线全部收口**：契约、`deploy/README.md`、`deploy/release.py`、`运行说明` §4.4/§4.5、需求变更台账、原型清单、`docs/testing/测试计划.md` 已完成——契约删两个配送接口行、错误码示例去「配送状态跳跃」、§9 证据回填 105/105 并清过期「待开发」（§4.2 规格、§3.4、§7 库存口径、§6.3 统计）；`deploy/README.md` 更新在线版本 `20260914-a2f15b1` + `/srv/elm-current` 指向 + tag 口径统一 + 新增「证据时效」与「构建/部署前置清单」（`backend/uploads/` 可写、`DEEPSEEK_API_KEY`、DB 重置真源 vs 打包副本）；`运行说明` 新增 2026-09-14 发布段（构建冒烟标「待补」）、测试库账号口径、SQL 真源加注、JDK 占位补探测命令；台账 CHG-001 标题改「开红包 / 爆红包」并为 §1.1–§1.6 旧口径加「已废止」标记；原型清单修好 PROTO-P2-001 加粗与重复词并新增 `PROTO-AI-01`（AI 点餐助手无设计稿）。
- **终检后主代理补修的 3 处「无主」残留**：`docs/backend/后端接口契约.md:390`「图片上传本期实现（待开发）」→ 已实现；同文件 §10 章首「本章接口全部标注待开发」→ 加 2026-09-15 复核说明（§10.1/§10.6 已实现）；`docs/project/小组协作与提交流程.md` 三行 `$env:X = "y"` 空格赋值 → 改为 `$env:X="y"`。复查：`\$env:... = ` 全库 0 命中。
- **已声明延后**：SRS 术语三种叫法（天天爆红包 / 设计稿页标题「天天红包」/ 活动卡「天天必爆」）、`docs/PRD.md:793/795` 原型覆盖状态两格、`docs/backend/后端任务分配与联调清单.md` §2 缺 9/16 交叉验收行、`docs/backend/后端接口契约.md:224`「再来一单（待开发）」与 §10 章首/分批段的通用表述。
- **守规则声明**：未改 `docs/record/daily/**`；未改任何 IP/SSH 账号/DB 账号/口令；未执行 `git init/add/commit/push/checkout`。工具约束：`str_replace_editor` 写文件被沙箱拒绝，全部写入改用 `edit`；`Remove-Item` 删除工程版文件成功。

### B. AI 使用点

| # | 任务 | 类别 | AI 做了什么 | 人工审核与修改 | 结果 |
| --- | --- | --- | --- | --- | --- |
| 1 | 拍板项收敛 | 需求澄清 | 先用 grep 把两处事实查实（AI 前端 `AiChatView.vue` 已实现、`platform_categories` 已落库），再只把真正需要拍板的 2 项（AI 计入口径、工程版 B/C）用提问工具抛出 | **负责人选定「正式纳入 14 页」「方案 C」** | 5 条线改稿口径唯一，无返工 |
| 2 | 方案 C 迁移与删除 | 结构维护 | 坚持「先迁移、后删除」：3 处独有内容新落点逐一核实存在后才执行删除；删除后对 16 处残留逐条判定性质 | 负责人选 C（风险已在选项中写明） | 工程版 479 行退出仓库，manifest 同步；无内容丢失 |
| 3 | 交叉验收参照物修复 | 文档整改 | 按四线报告把 SRS 交付版升 v1.3：补编号、清重复行、给判据与固定演示数据、判据来源指向契约 | 主代理逐条复核 + grep 复查 | 参照物从「能列范围」变为「也能判 pass/fail」 |
| 4 | 口径终检 | 质量巡检 | 11 个关键词全库 grep + 表格列数脚本校验 | 无 | 修掉 3 处漏网（SRS 两格、演示页 P2 卡片），表格 0 异常 |

- **本轮更新的 TODO 编号**：本轮**未修改任何 TODO 状态**（含未登记 `TODO-DOC-015/016/017`）；工作归属 `TODO-DOC-006`（R7 口径同步复查）与 `TODO-DOC-005`（交叉验收包匿名化）；敏感信息脱敏条目归服务器同学。


## 16:40（本轮：整分支合并 main —— 13 处冲突解析、两轮远端并入与缺陷编号撞号处置）

### A. 进度事实

- **背景**：负责人指示「登记提交并推送合并」。文档整改 4 个提交（`7395cfe`/`4e07b7c`/`5fb826f`/`ffbcc9e`）已推送分支 `feat/remaining-gaps-tdd`；整分支合并时 `origin/main` 已前进 3 个提交（BE-008 收藏/会员），产生 **13 处冲突**（6 处 add/add、7 处 content，含 `schema.sql`/`reset.sql`/`Domain.java`/`UserMapper.java`/`OrderService.java`/`后端接口契约.md`/`后端.md`）。
- **关键判定（用构建事实而非猜测）**：main 与分支是**两套各自完整、互不兼容的实现**——main 有收藏/会员（`users.is_member` + `MemberService` + `ViewMapper.favorite` + `Requests.FavoriteCreate`），分支有**通知 / 搜索 / 平台级分类**（`Notification*`/`Search*`/`PlatformCategoryMapper`/`PromotionTagService`，main 完全没有）。**混搭即断裂**：实测「分支 Domain + main 的 `MemberService`/`ViewMapper`」编译失败（`Domain.Favorite.rating`/`User.isMember` 找不到）；反向混搭同样失败。
- **取向**：后端**整体采用分支实现**（在独立 worktree 中 `mvn -B -DskipTests clean package` = BUILD SUCCESS 作为依据），main 侧 BE-008 变体由分支等效实现取代；被删除文件 5 个：`backend/database/migrations/20260915-favorites.sql`、`20260915-member.sql`、`backend/src/main/resources/db/upgrade-favorites.sql`、`upgrade-member.sql`、`service/MemberService.java`。文档侧契约与后端台账同步改用分支口径，并把 §3.7/§3.8 回写改回 main 实现文字后再统一切回分支口径。
- **R1 证据（合并前必跑矩阵）**：后端 `mvn -B -DskipTests clean package` 通过；用户端 `npm run build` exit 0、`npx vitest run` **60 文件 / 481 用例全绿**；`mvn test` 本机无法执行（开发机无 MySQL，R8），已在 `docs/todo/后端.md` 登记 `TODO-BE-025` 要求在服务器测试库 `elm_practice_test` 重跑。**注**：中途一次 `mvn -q` 过滤输出得到的「BUILD SUCCESS」是**增量编译假象**，`clean` 全量构建才暴露失败——教训：**验证构建必须 `clean`，且不能用管道过滤后的 `$LASTEXITCODE` 判断**。
- **合并提交**：`298ba08`（Merge branch 'feat/remaining-gaps-tdd' into main）。push 时被拒（远端又前进 6 个提交：AI 流式换行/空格修复），遂 `merge origin/main`，冲突仅 1 处文档。
- **缺陷编号撞号处置**：`docs/testing/缺陷跟踪表.md` 两侧**同日各自登记了 `BUG-20260915-001`**（我方 RB-8 flaky / 远端 AI 流式换行）→ 保留双条目，远端那条改号为 **`BUG-20260915-002`** 并在行内注明改号原因（raw 历史片段不改写）；编号仍待测试联调同学确认。合并提交：`1e062f9`；**`main` 已推送成功：`231defa..1e062f9`**。
- **登记**：`docs/todo/后端.md` 新增 `TODO-BE-025`（合并后后端回归与实现取舍确认，进行中，王嘉悦）；`TODO-DOC-015`/`TODO-DOC-017` 证据列补「随合并 `298ba08`/`1e062f9` 进入 main」。
- **守规则**：未改 `docs/record/daily/**`；未改写任何 raw 历史片段；未改 IP/账号/口令行；所有 git 操作均在负责人「登记提交并推送合并」的明确授权内。

### B. AI 使用点

| # | 任务 | 类别 | AI 做了什么 | 人工审核与修改 | 结果 |
| --- | --- | --- | --- | --- | --- |
| 1 | 冲突取向判定 | 方案设计 | 用三方哈希比对 + 独立 worktree 全量构建，证伪「main 已含通知/搜索」的假阳性（`git cat-file -e` 失败不抛异常导致 try/catch 恒真），确认两侧为互补实现 | 负责人授权按构建事实取向 | 取向有可复现证据，非主观选择 |
| 2 | 冲突解析与合并 | 结构维护 | 13 处冲突按「同一功能二选一、以 clean 构建结果为准」解析；补齐文档口径与台账；删除 main 独有文件 5 个 | 负责人已授权整分支合并 | 后端构建通过，前端 481 用例全绿 |
| 3 | 撞号处置 | 质量巡检 | 发现两侧同日同号缺陷，保留双条目并改号 + 行内说明，避免覆盖任一方证据 | 编号归属仍交测试联调同学确认 | 台账无重复编号，证据链完整 |
| 4 | 构建验证纪律 | 教训固化 | 识别「增量编译假象」与「管道后 `$LASTEXITCODE` 不可信」，改为 `clean` + 直取退出码 | 无 | 避免把失败构建当成功合并 |
- **本轮更新的 TODO 编号**：新增 `TODO-BE-025`；更新 `TODO-DOC-015`/`TODO-DOC-017` 证据列；登记 `BUG-20260915-002`（改号）于 `docs/testing/缺陷跟踪表.md`。

## 15:24（本轮：核查「新注册账号首页仍显示天津大学北洋园校区」是否缺陷 —— 只读核查）

### A. 进度事实

- **问题来源**：负责人提问「新建账号地址为空，但首页定位仍显示『天津大学北洋园校区』，是 bug 吗？PRD/SRS 有无说明」。本轮为只读核查，**未修改任何业务代码与文档**。
- **结论：非缺陷**，是 PRD/SRS 明文规定的「无地址回退课程演示地址」口径，且实现与测试均已按该口径落地。
- **口径出处（逐条实读原文）**：`docs/PRD.md:834`（§7.16.1 用户端原型说明 → 行「首页-定位与频道栏」）字段列「右侧定位文字来自当前用户默认地址，**无地址时使用课程演示地址"天津大学北洋园校区"**，地址字段不允许由前端随意拼接」；检查列「未登录可以浏览；**读取地址失败保留默认演示地址并标记为演示数据**；地址为空进入地址管理时要求登录」。`docs/SRS.md:657`（§6.2.1 用户端原型说明，该节自述为「引自 PRD 的同源副本」）同一行同口径。旁证：`docs/backend/后端接口契约.md:93` §2 固定演示数据默认地址即北洋园 `12号楼 304室`；`docs/testing/测试计划.md:44`；`docs/frontend/首页复刻素材清单.md:61` 明确引用「PRD 7.16.1」；`docs/PRD.md:700`（§7.9 地址管理）「无地址时展示无数据提示与『新增地址』按钮」——即新账号本就应当没有地址。
- **实现对照（实读代码）**：`frontend/user-h5/src/views/user/HomeView.vue:65-67` 定义 `DEMO_LOCATION = '天津大学北洋园校区'`、`locationText = ref(DEMO_LOCATION)`、`isDemoLocation = ref(true)`；`loadLocationAddress()`（`:69-84`）仅在拿到 `def?.region || def?.detail` 时替换定位文案，**未登录 / 无地址 / 请求失败一律保留演示值**；`:255` 以 `:title="isDemoLocation ? '演示地址' : undefined"` 作演示标记。行为已被测试锁定：`frontend/user-h5/src/views/user/__tests__/HomeView.spec.ts:107-108`（用例 T1）、`:323`。同口径复用见 `frontend/user-h5/src/utils/location.ts:9`（搜索结果页、分类商家列表页）。
- **顺带发现的两处可改进项（未登记缺陷，待负责人定）**：① PRD 所述「标记为演示数据」目前仅由 `title` 属性 tooltip 承载，H5 移动端基本不可见，视觉上与新账号真实默认态无法区分；② `DEMO_LOCATION` 常量在 `frontend/user-h5/src/utils/location.ts:9` 与 `frontend/user-h5/src/views/user/HomeView.vue:65` 各存一份（该文件注释自述「HomeView 暂未改为引用本文件」，挂在 `TODO-USER-005` 下待统一）。
- **守规则声明**：未修改任何 PRD/SRS/契约/代码文件；未改 `docs/record/daily/**`；未改 `docs/testing/**` 台账（本轮结论为非缺陷，无需登记缺陷行）；未执行任何 git 写命令。

### B. AI 使用点

| # | 任务 | 类别 | AI 做了什么 | 人工审核与修改 | 结果 |
| --- | --- | --- | --- | --- | --- |
| 1 | 口径核查 | 事实核对 | 全库 grep 定位「北洋园」58 处 / 天津大学 92 处来源，回读 `docs/PRD.md:834`、`docs/SRS.md:657` 原文与 `HomeView.vue:65-84`、`HomeView.spec.ts:107-108` 实现，逐层确认行为与口径一致，并区分出「非缺陷」与「标记方式偏弱」两个不同结论 | 负责人提出问题；结论由 AI 给出 | 判定非缺陷，并留两条可改进项 |

- **本轮更新的 TODO 编号**：无（未改动 `docs/todo/**`）。

## 15:52（本轮：CHG-005 首页定位栏「无地址」口径改三态 —— 测试先行 + 三页实现 + R7 文档同步）

### A. 进度事实

- **任务来源**：负责人先问「新建账号地址为空、首页却显示天津大学北洋园校区，是不是 bug、PRD/SRS 有没有说明」，追问「没有地址首页就不应该显示地址，这不矛盾吗」；核查后负责人拍板 **「改口径」**，并选定 **方案 C（未登录=演示地址；已登录无地址=占位）+ 占位文案「选择收货地址」**。
- **口径变更（CHG-005·三态）**：① 未登录访客 → 课程演示地址「天津大学北洋园校区」+ `title="演示地址"` 标记；② 已登录有地址 → 默认地址 `region`（缺省 `detail`），不标记；③ **已登录但无任何地址 → 占位「选择收货地址」，不标记**（不得用演示地址冒充真实地址）；④ 读取地址失败 → 保留演示地址并标记（原口径不变）。
- **TDD（先红后绿）**：新增 `frontend/user-h5/src/utils/__tests__/location.spec.ts`（LC-1~LC-7）；`frontend/user-h5/src/views/user/__tests__/HomeView.spec.ts` 的 T61 由「已登录无地址 → 回退演示地址并标记」改为「→ 占位且不标记」、T1 补「未登录=演示地址并标记」断言；`SearchResultView.spec.ts` 新增 SR-14；`CategoryStoreListView.spec.ts` 新增 CL-14。**红：4 文件 10 failed | 47 passed**（`resolveLocationState` 未导出 + 三处页面仍收到 `天津大学北洋园校区`）→ **绿：4 文件 57 passed**。
- **实现落点**：`frontend/user-h5/src/utils/location.ts` 新增 `ADDRESS_PLACEHOLDER='选择收货地址'`、`resolveLocationState({ isLoggedIn, addresses })`、`LOCATION_LOAD_FAILED`，成为定位文案**唯一出口**（消除 `HomeView` 内重复的 `DEMO_LOCATION` 局部常量，收口 `TODO-USER-005` 的「待统一为唯一出口」遗留）；`frontend/user-h5/src/views/user/HomeView.vue`（`loadLocationAddress`）、`SearchResultView.vue`、`CategoryStoreListView.vue`（新增 `loadLocation()` 并在 `onMounted` 调用；**二者此前完全不读地址、恒显示演示地址**，与 PRD 841「定位来自当前默认地址」不符，本轮一并纠正）三页共用同一解析器。
- **R1 合并前必跑矩阵**（`frontend/user-h5`）：`npx vitest run` → **61 文件 / 494 用例全绿**（exit 0）；`npm run build`（含 vue-tsc 类型检查）→ **✓ built in 848ms**（exit 0）。
- **R7 同批文档同步（5 文件 11 处，全部落地）**：`docs/PRD.md` 7.16.1 行「首页-定位与频道栏」（字段列 + 检查列）与行「搜索结果页-搜索头部」（定位来源句）；`docs/SRS.md` §6.2.1 两处同源副本；`docs/frontend/首页复刻素材清单.md` 定位栏说明；`docs/frontend/用例设计决策记录.md` T61 修订 + §3「已知简化口径（答辩主动说明项）」补「定位栏未登录态」一行；`docs/project/需求变更台账.md` 新增 CHG-005（索引行 + 变更单 / 已落地 / 影响与回归三节）。
- **台账**：`docs/todo/用户端.md` §1 追增 `TODO-USER-124`（状态「已完成」，证据列含红绿数字；未收口=「real 模式新建账号三页定位栏截图待补」「提交/PR 待负责人允许」）。
- **未收口**：① real 模式（新建账号）三页定位栏截图待补；② 本轮全部为工作区修改，**未执行 git add/commit/push**（按 Git 纪律等负责人允许）；③ 未改动 `docs/record/daily/**`。

### B. AI 使用点

| # | 任务 | 类别 | AI 做了什么 | 人工审核与修改 | 结果 |
| --- | --- | --- | --- | --- | --- |
| 1 | 口径核查 | 事实核对 | 全库 grep「北洋园」58 处 /「天津大学」92 处定位来源，回读 `docs/PRD.md:834`/`:841`、`docs/SRS.md:657`/`:664`、`HomeView.vue:65-84`、`HomeView.spec.ts:104-113`/`:298-338`、`docs/frontend/用例设计决策记录.md:56`，区分「实现是否偏离文档」与「文档本身是否合理」两个不同问题 | 负责人提出两轮追问；结论由 AI 给出 | 判定「非缺陷」+ 两条可改进项 |
| 2 | 方案设计与取舍 | 方案设计 | 给出 A（只补标记）/ B（一律占位）/ **C（未登录=演示地址、已登录无地址=占位）** 三方案与两句占位文案，并说明 C 的理由：代码里 `isDemoLocation` 已存在，说明本就能区分「演示值/真实值」，缺的只是「未登录 vs 已登录无地址」这一维 | **负责人拍板「改口径」并选定 C +「选择收货地址」** | 口径定为三态 |
| 3 | 测试先行 | 测试设计 | 按新口径改写/新增 4 个测试文件共 10 条断言，先跑红取证（10 failed / 47 passed）再动实现；边界场景（无 `isDefault` 取第一条、`region` 缺失退 `detail`、字段全空退占位、未登录标记）由 AI 补齐 | 口径由负责人拍板 | 红端证据落盘 |
| 4 | 三页实现收口 | 实现 | 抽出 `resolveLocationState` 唯一出口供三页共用；搜索结果页与分类商家列表页首次真正读取默认地址 | 无 | 绿端 4 文件 57 passed；全量 494 passed、build ✓ |
| 5 | 文档 R7 同步 | 结构维护 | 长表格行不做整行替换：写**两阶段校验脚本**（11 处锚点必须各自唯一命中才写入，否则整体中止），对 5 份文档做精确子串替换 | 无 | PRD/SRS/清单/决策记录/台账同批一致 |
| 6 | 工具踩坑固化 | 教训固化 | 记录六条：① `edit` 锚点**只有最近一次 read/diff 的才有效**，且同内容行会碰撞（拿 `})` 当区间端点被解析到别处，导致新用例插到 SR-1 之后）；② 同一批 edits 中「上方插入 + 下方区间」会因位移失败，应按**自下而上**排序或一次只改一处；③ `str_replace_editor` 写文件被沙箱拒绝（改用 `edit` 或临时脚本）；④ `compress` 会报 `session event "user/message" carries an invalid replace surfaceOp` 但**实际已生效**，成败以 `acp_status` 为准；⑤ 新页面若只 `import { useSessionStore }` 而未 `const sessionStore = useSessionStore()`，`sessionStore is not defined` 会被 `try/catch` 吞掉、表现为「仍是演示地址」——**静默 catch 会把实现错误伪装成口径行为**，排查时应先怀疑 catch；⑥ PowerShell **双引号 here-string 会处理反引号转义**（`t`→Tab、`r`→CR），写含 Markdown 反引号的文本必须用单引号 here-string（本小节首版即因此损坏、已重写） | 无 | 后续轮次不再重复踩 |

- **本轮更新的 TODO 编号**：新增 `TODO-USER-124`（`docs/todo/用户端.md` §1）；`TODO-USER-005` 的「`DEMO_LOCATION` 待统一为唯一出口」子项已在本轮收口（未改该行状态）；`docs/project/需求变更台账.md` 新增 `CHG-005`。未登记缺陷（本轮为口径变更，非缺陷）。
