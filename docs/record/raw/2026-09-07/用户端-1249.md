# 2026-09-07 用户端留痕（第二批红测批次）

## A 当日进度

- PR 处理：#11（首页复刻）确认已于 9/6 合并；#12（地址订单第一批）与 #13（后端 Spring Boot 工程）经 GitHub API 合并进 main；#13 的 .gitignore 冲突以合并提交 `7ded0cd` 解除（双方忽略项保留），无 force push。后端 36 个文件随合并落位 `backend/`。
- 切出第二批分支 `feat/address-order-query-tdd`（自最新 main），完成第二批 TDD 失败测试（M5-M10 + T31-T44 共 20 条，全部先红）：
  - 地址增改删 mock（M5-M8，TC-ADR-001/003/004/007）：新增、设默认互斥、删除默认后最近更新者自动接替、删光默认为空、单查/改/删不存在统一 404；
  - 订单查询 mock（M9-M10，TC-ORD-013/015/016/022）：列表倒序与 status 筛选、详情商品快照 + 金额三件套（实付=小计+打包费）、订单不存在 404；
  - 地址列表页（T31-T35，PRD 7.9/7.16 地址列表页两行）：渲染与默认置顶标识、空态、未登录转登录、设默认、删除二次确认；
  - 新增/编辑地址页（T36-T39，TC-ADR-002/005）：非法表单逐项提示不发请求、新增成功返回列表、编辑回显保存、编辑地址不存在返回列表；
  - 订单列表页（T40-T42，PRD 7.6）：订单卡渲染（状态文案/金额两位小数/时间 yyyy-MM-dd HH:mm:ss）、空态、点击进详情；
  - 订单详情页（T43-T44，TC-ORD-016/022 前端侧）：编号/状态/时间/收货快照/商品快照/金额三件套、返回回列表、订单不存在提示返回。
- 红灯证据：`npx vitest run` → Tests 6 failed | 59 passed (65)，另 4 个视图 spec 因页面未实现整文件失败；既有 59 条全部保持绿。

## B AI 使用点

1. 用例口径：M 系列取自 TDD 规划矩阵 TC-ADR/TC-ORD，视图系列取自 PRD 7.16 地址列表/新增地址/订单列表/订单详情验收行；AI 负责场景到前端职责层的映射与脚手架。
2. 设计决策（AI 提出，待负责人复核）：订单 mock 需从"创建即返回"升级为内存持久化（`orderMockState` + `ORDER_SEED` 预置种子，与地址 mock 同风格）；mock 路由器需支持动态路径段（如 `GET /orders/:orderId`，现仅精确匹配）；编辑页回显走契约 §3.3 单条 GET；地址选择回填确认订单页留第三批（本批已能默认地址下单闭环）。
3. 假绿预防：M8/M10 的 404 断言均附加业务提示（"地址不存在"/"订单不存在"），与 mock 未注册的"mock 未定义"响应区分。
4. 无提交时点前的红线：遵守 Git 纪律，本批次红色测试经负责人确认后提交 `test:`。
