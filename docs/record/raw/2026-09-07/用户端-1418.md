# 2026-09-07 用户端留痕（白屏根因修复与真实形状对齐批次）

## A 当日进度

- **联调白屏根因定位与修复**：负责人实测"登录后白板、不跳转"。根因：真实后端 GET /orders 为扁平字段（itemSubtotal/packagingFee/total、address 对象、items 明细、无 storeName、状态 PENDING_PAYMENT），前端按嵌套 amounts.payableAmount 渲染 → OrderListView TypeError 崩溃 → 白屏（登录成功→redirect /orders→崩溃，与现象完全吻合）。
- 修复（架构约定 §3.3 normalizers 唯一出口）：
  1. types.ts 拆分：OrderRecord（后端真实形状，raw）+ OrderSummary/OrderDetail（归一化后视图模型）；
  2. normalizers 新增 normalizeOrderSummary/normalizeOrderDetail（用例 N1-N3 先红，真实 JSON 作 fixture；缺字段确定性兜底 0/空数组/空快照）；
  3. statusText 补 PENDING_PAYMENT → '待支付'（用例 B6；后端已实现支付扩展状态机，文案层兜底）；
  4. mocks/order 持久化与响应**对齐真实后端形状**（mock 是后端替身，形状必须与真后端一致，否则联调必崩）；状态保持 P0 纯度仅 PROCESSING；
  5. api/order 返回 OrderRecord；OrderListView/OrderDetailView 消费归一化模型；店名由 storeId 经店铺列表/详情映射（后端订单无 storeName，缺口已列协调清单）。
- **加购无反应修复**：上一轮 401 静默降级让未登录加购无反馈。按 PRD"登录先行"补 T45（未登录点加购 → 提示并跳登录带 redirect，不静默失败）；T15 同步演进为登录态加购。
- **局域网 CORS 修复**：后端 CORS 仅放行 localhost/127.0.0.1，手机经 172.18.238.116 访问时 API 全被浏览器拦截（"部分店没有商品"疑似该根因的空态）。改 vite server.proxy `/api → 127.0.0.1:8080` 同源转发 + VITE_API_BASE_URL=/api/v1（.env.development.local 不入库），本机与局域网探测均 200。
- 回归证据：**92 passed (15 files) 全绿**；build（type-check + vite build）通过。

## B AI 使用点

1. 根因定位方法（AI）：凭"登录成功→redirect→白屏"链路推断订单页渲染崩溃，curl 真实响应比对前端契约类型逐字段证实——**联调期字段形状差异是课程典型坑**，mock 与真实后端形状一致性应作为联调检查项（已写入本片段）。
2. 口径演进（涉及已拍板用例，需负责人知悉）：M4/M10 断言从嵌套 amounts 演进为扁平 itemSubtotal/total（业务行为不变，JSON 形状对齐真实后端）；T15 演进为登录态加购（新口径 T45：加购需登录）；T40/T43 增加店名映射的 waitFor（异步竞态，非行为变更）。
3. 流程反思（AI 自查）：本批 N/B6 用例又是"写完测试未提交即实现"——与登录页同款问题，补救方式相同（拆两笔提交保证 git 历史红绿顺序）；后续批次严格"红提交落地后再写实现"。
4. 剩余协调项（后端 A）：/stores 响应补 previewProducts（首页商家卡商品预览，契约缺口 9/6 已提请）；订单响应补 storeName（或确认前端映射方案）；状态机口径（P0 契约仅 PROCESSING vs 后端 PENDING_PAYMENT 起步——若按后端现状，前端文案已兜底，需 SRS 固化口径）。
