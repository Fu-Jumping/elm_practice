# 2026-09-07 用户端留痕（首页商品预览聚合批次）

## A 当日进度

- 负责人实测反馈"首页商家卡商品预览为空"。根因：真实后端 /stores 不返回 previewProducts（9/6 已提请的契约缺口字段），前端按 T8"缺失整块隐藏"口径隐藏预览块。
- 修复方案（T46，PRD 7.16.1 口径"预览来自商家商品接口"）：首页加载商家列表后并发聚合各店 /stores/{id}/products 取**前 3 个**作为预览；/stores 若将来返回 previewProducts 字段则直接使用（零改动切换）；单店聚合失败仅隐藏该店预览，不阻塞商家卡。
- mock 对齐真实后端：移除 mock /stores 内嵌的 5 处 previewProducts（mock 为后端替身，形状必须与真后端一致——与订单形状对齐同一口径）；types.ts Product 补 image?: string（真实接口返回该字段，空串时占位图）。
- 用例演进（已拍板框架内的口径演进）：T7 断言来源从内嵌字段改为聚合结果（家常豆腐，来源 /stores/m001/products）；T8 商品预览断言加 waitFor（聚合异步竞态）；T46 新增锁定"最多 3 个 + 占位图 + 无 undefined"。
- 回归证据：**94 passed (15 files) 全绿**；build（type-check + vite build）通过。

## B AI 使用点

1. 方案由 AI 提出并实现（前端聚合 vs 后端加字段的取舍分析已向负责人说明）：前端聚合立即可用且更贴 PRD 原文，后端加字段仍保留在协调清单作为长期正路（一次请求优于 N 次请求时切换）。
2. 批量删除 mock 字段用脚本正则完成，删除后跑用例确认仅预期断言受影响（T7/T8 红、其余绿）。
3. T8 失败为异步聚合竞态（非行为回归），waitFor 修正——异步聚合类断言应统一带 waitFor，已作为本文件经验记录。
