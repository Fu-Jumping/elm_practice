# 2026-09-07 用户端留痕（凌晨批次）

## A 当日进度

- 切出特性分支 `feat/address-order-tdd`（自 `feat/home-replica-tdd`，含 origin/main 全部历史 + 首页复刻 T1-T18；本地 main 落后远端 16 个提交，GitHub 代理连接失败暂未同步）。
- 完成用户端第三批 TDD 失败测试（T19-T30 + mock 层 M1-M4，共 16 条，全部先红）：
  - `validators.spec.ts` 追加 T19-T22：地址表单校验（联系人/电话 11 位/所在地区/详细地址，TC-ADR-002 前端体验层）；
  - `normalizers.spec.ts` 追加 T23-T25：`payableAmountText` 实付金额展示归一化（后端计价为准、缺失兜底推导、全缺给 0.00，TC-ORD-011/021 + PRD 7.4 不单列打包费行口径）；
  - 新增 `mocks/__tests__/orderAddressMocks.spec.ts` M1-M4：地址/订单域 mock 后端替身行为（地址种子查询、缺 addressId 400、地址不存在 404、创建成功后端计价 41.00 + 返回订单号 + 清空该店购物车，TC-ADR-006/TC-ORD-001/003/004/005/013/021）；
  - 新增 `views/user/__tests__/ConfirmOrderView.spec.ts` T26-T30：确认订单页渲染（默认地址卡/商品行/实付 41.00/无打包费行）、未登录转登录、无地址禁用提交、填备注提交成功清购物车跳订单列表、storeId 缺失返回商家列表（PRD 7.4/7.16）。
- 测试红灯证据：`npx vitest run` → Tests 11 failed | 43 passed (54)，ConfirmOrderView.spec.ts 因页面未实现整文件失败（预期红）。
- 关键发现（待办）：`backend/` 目录在所有远端分支（含 back-end-a-9.6、feat/backend-b-init）上仅有 README.md，后端工程尚未入仓；9/7 晚 P0 全链路联调的前置，需协调后端同学尽快推进仓库。

## B AI 使用点

1. 用例清单生成：T19-T30/M1-M4 的场景口径取自人已设计的 TDD 规划矩阵（TC-ADR/TC-ORD）与 PRD 7.4/7.9/7.16，AI 负责映射到前端职责层并生成测试脚手架；边界口径（如 T20 电话格式、T25 金额全缺兜底）沿用矩阵既有规则，未新设业务规则。
2. 假绿规避：M3 初版仅断言 404，与"mock 未注册也返回 404"撞车形成假绿；AI 自查发现后补断言 `message 含"地址不存在"` 区分真实校验路径（规避记录，供次晨汇总）。
3. 设计决策（AI 提出、待负责人确认）：确认订单页路由 `/orders/confirm?storeId=`、地址 mock 导出 `ADDRESS_SEED`/`addressMockState` 供测试隔离重灌、地址种子 region='天津大学北洋园校区' detail='12号楼 304室'（对齐契约 §2 固定演示数据）。
4. 无提交：遵循 Git 纪律未执行 commit/push，红测试落盘等待负责人确认后先提 `test:` 再转绿 `feat:`。
