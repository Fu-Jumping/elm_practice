# 2026-09-07 用户端留痕（第二批转绿批次）

## A 当日进度

- `test:` 红色提交 `97b9207`（M5-M10 + T31-T44 共 20 条）后完成转绿实现并提交 `feat:`：
  - mock 路由器升级：支持 `:param` 动态路径段（精确匹配优先，路径参数并入 ctx.params）；
  - 地址 mock 补齐增改删（POST/PATCH/DELETE/单查，TC-ADR-001/003/004/007），以 touchSeq 追踪"最近更新"支撑删除默认后自动改派；
  - 订单 mock 升级为内存持久化（orderMockState + ORDER_SEED 预置），POST /orders 保存商品/地址/金额快照与创建时间，GET /orders 倒序 + status 筛选，GET /orders/:orderId 详情（TC-ORD-013/015/016/022）；store mock 补 findMockStore 导出（订单快照店铺名）；
  - 新增契约类型 OrderStatus/OrderAmounts/OrderItemSnapshot/AddressSnapshot/OrderSummary/OrderDetail，api 层补 addAddress/getAddress/updateAddress/removeAddress/listOrders/getOrder；
  - 新增四个页面与路由：AddressListView（/addresses）、AddressEditView（/addresses/new、/addresses/:addressId/edit）、OrderListView（/orders 替换占位页）、OrderDetailView（/orders/:orderId）。
- 回归证据：全量测试 **79 passed (14 files) 全绿**；`npm run build`（type-check + vite build）通过。

## B AI 使用点

1. 转绿过程中三处缺陷修复（AI 自查自纠）：① mock 动态路由注册遗漏 handler 引用（TypeError: dyn.handler is not a function）；② AddressListView.spec 的 login() 早于 pinia 激活（测试骨架问题，用例口径未变）；③ 订单详情页 data-testid="order-detail" 应为整页容器语义而非首个卡片（实现侧修正，测试口径未变）。
2. TypeScript 严格模式一处类型收窄修正（address mock 的字段合并写入，`as unknown as` 双段转换）——业务逻辑无变化。
3. 口径留痕：订单详情页金额明细按 TC-ORD-022 展示三件套（小计/打包费/实付）；PRD 7.4"不单列打包费行"决议仅限确认订单页；地址选择回填确认订单页、状态驱动底部操作区留第三批 TDD。
4. 地址 mock 的 POST 校验（400）为后端兜底规则的最小镜像，未被用例锁定——联调期以真实后端为准。
