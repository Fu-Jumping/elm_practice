# 2026-09-07 用户端留痕（转绿批次）

## A 当日进度

- `test:` 红色提交 `8629319`（T19-T30 + M1-M4，16 条失败测试）之后完成转绿实现并提交 `feat:`：
  - validators 新增 `validateAddressForm`（T19-T22 转绿）；
  - normalizers 新增 `payableAmountText` + `PACKAGING_FEE`（T23-T25 转绿）；
  - 新增地址/订单域 mock（`mocks/address.ts`、`mocks/order.ts`，cart mock 导出 `clearMockCart`/`getMockCartSnapshot`），M1-M4 转绿；
  - 新增 `api/address.ts`、`api/order.ts` 与契约类型（Address/CreateOrderPayload/OrderCreated），endpoints 补 address/order 域；
  - 新增 `ConfirmOrderView.vue` + 路由 `/orders/confirm`（T26-T30 转绿）；
  - 商家详情页去结算接通确认订单页，T18 用例口径同步演进（原"确认订单暂未开放"弱提示过期移除）。
- 回归证据：全量测试 **59 passed (8 files) 全绿**；`npm run build`（type-check + vite build）通过。

## B AI 使用点

1. 转绿过程中测试骨架三处修正（与 feat 同提交并在提交信息披露）：① 用例间 pinia 泄漏（登录态/购物车与视图须同一实例）；② 地址/购物车 mock 模块级内存态跨用例污染——同商品加购按契约合并数量导致金额累加（39→119），beforeEach 用 `ADDRESS_SEED` 重灌 + `clearMockCart` 清理；③ T29 提交前须等地址/金额就绪（mock 延迟窗口内按钮禁用，点击空转）。
2. 口径取舍留痕：toast 文案对齐测试锁定的精确"下单成功"（订单号展示随第二批订单列表页落地）；确认订单页实付金额按 PRD 7.4 决议不单列打包费行（覆盖设计稿"包装费"行）；地址卡电话按测试口径完整展示（设计稿为脱敏展示，视觉细化任务再对齐）。
3. T18 口径演进（AI 提出并执行）：确认订单页落地使 9/6 批次锁定的"确认订单暂未开放"弱提示过期，测试改为断言跳转 order-confirm 并携带 storeId。
4. TODO(第二批 TDD)：商家关闭/起送不满足禁提交（TC-ORD-006/009）、地址管理页（列表/新增/编辑/删除/设默认）、订单列表/详情页、地址卡点击进入地址选择。
