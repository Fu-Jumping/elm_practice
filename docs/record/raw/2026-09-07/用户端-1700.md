# 2026-09-07 用户端留痕（演示图片素材接入批次）

## A 当日进度

- 组长本地提供演示图片素材包：`demo-images/`（25 张：5 店招 + 19 商品 + 1 默认头像）与 `图片清单.md`（URL 口径 `/demo-images/<文件名>`、商品对照表、三方接入说明）。**发现清单中"前端接入已完成"的描述与工作区不符**（src 无改动、public 无图片）——实际接入由本批次完成。
- 图片落位：前端 `frontend/user-h5/public/demo-images/`（页面同源伺服）+ 后端 `backend/src/main/resources/static/demo-images/`（备后端将来开启静态映射时使用）。
- 代码接入：
  - 新增 `utils/demoImages.ts`（P1-P5 用例先红）：productImageSrc/storeImageSrc 兜底链——接口 image 非空优先 → 演示映射（productId/storeId 对照清单）→ 占位图/空串；
  - mock 填图：STORES 5 家店招、全部 15 个商品按清单映射填 `/demo-images/...`；mock 演示用户补 avatar（默认头像）；
  - 视图接入：首页聚合预览、商家详情商品行、详情横幅 StoreCover（店招映射）、我的页头像（avatar 有图用图/无图昵称首字）；
  - T46 断言演进：预览图断言从占位图改为 `/demo-images/product-m002-01.jpg`（素材已接入的口径演进）。
- **口径修正（实测）**：原计划图片代理转发后端伺服（清单口径 b）——实测 404，后端 application.properties 关闭静态资源映射（add-mappings=false，API-only 设计）。改走前端 public 副本（同源），代理不配；后端若将来开启静态映射可恢复（vite.config 注释留痕）。
- 运维记录：重启后端时遇"Port 8080 already in use"——Windows 下 TaskStop 停 mvn 包装进程不杀 java 子进程，需按 netstat PID taskkill 后再启（第二次启动成功）。
- 回归证据：**110 passed (18 files) 全绿**（新增 demoImages.spec 5 条）；build 通过。

## B AI 使用点

1. md 清单与工作区状态差异由 AI 核查发现（清单声称"前端接入已完成"但代码不存在）——清单文档与实际落地的差异已对齐，接入以本批次为准。
2. mock 填图用 Python 脚本批量正则完成，两轮修正（多行/单行对象格式差异、边界行误插），最终以计数校验（店招 5、商品 15）+ 用例回归收口。
3. 素材文件为组长提供，AI 仅做接入；图片内容与版权溯源见清单"溯源"节（组长提供）。
