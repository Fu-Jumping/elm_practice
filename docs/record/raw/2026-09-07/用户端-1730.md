# 2026-09-07 用户端留痕（金额明细/滚动位置/竞态修复批次）

## A 当日进度

- 负责人两项实测反馈按 TDD 落地（红绿提交对 10ac3ff → af7d7a7，PR #24 已合）：
  1. **确认订单金额明细三行**（T64，先红）：商品小计/打包费/实付展示。口径变更：覆盖 9/1 决议"不单列打包费行"（原口径致金额构成不可见，负责人误判漏计配送费），PRD 7.4 决议行已同步修订（31e4014）。计价规则不变：实付=小计+打包费 2 元，配送费不参与计价。
  2. **返回保持滚动位置**（T65-T66，先红）：scrollBehavior 返回/前进（popstate）恢复 savedPosition，普通跳转回顶。解决"从商家详情返回列表回顶要重复下滑"。
- **竞态缺陷修复（catalogStore ERR_CANCELED，重做后入库）**：负责人报"第一次进店有商品、再次进店没了"。定位：防重拦截器取消同接口在途旧请求时，catalogStore 三个 fetch 的 catch 不分青红皂白清空数据——快速切换店铺时被取消的旧请求回调在新数据落地后执行 → 商品被清空（cartStore 早有同款防护，catalogStore 漏配）。三个 fetch 补 ERR_CANCELED 静默返回。
- 回归证据：**120 passed (19 files) 全绿**；build 通过。

## B AI 使用点

1. 流程失误与自纠（如实记录）：本日第 4 次出现"提交落在 main"（未切分支即 commit），及"reset --hard 补救时丢弃工作区未提交改动"（catalogStore 修复第一次应用后丢失，本批次重做）。两项均为 AI 操作纪律问题：补救均未影响远端（main 未推送前本地复位）；**预防措施：每次 commit 命令前先 `git branch --show-current` 校验、reset --hard 前核查工作区未提交项**。
2. "商品消失"现象未能在自动化环境复现（URL 直达/返回键/快速切换三路径均正常），竞态修复基于代码审查定位的确定性缺陷；若负责人强刷后仍复现，需 F12 Console/Network 信息进一步定位（已向负责人索取）。
3. T26 用例口径反转（原断言"不出现打包费"→ 现断言三行明细）为负责人明确决定，PRD 决议行同步修订——需求变更按"先改失败测试再改实现"流程执行。
