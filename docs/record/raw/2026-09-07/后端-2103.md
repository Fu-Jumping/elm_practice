# 2026-09-07 后端 MyBatis + 服务器 MySQL 部署留痕

成员/角色：后端 AI 协作
时间：21:03

## A 进度（当日完成事实）

1. 服务器 MySQL 8.0.46 部署完成（`82.157.137.114:3306`），监听 `0.0.0.0`，时区表已导入。
2. 开发库 `elm_practice` + 测试库 `elm_practice_test`（utf8mb4）建好，业务账号 `elm` 授权完成；密码经 `DB_PASSWORD` 环境变量注入，不入仓库。
3. 腾讯云安全组放行 TCP 3306；本机远程认证 + 查询验证通过（pymysql 连上查出 stores=5）。
4. 14 张表 + 演示种子数据导入完成（5 店铺/5 商品/3 订单/1 用户/1 地址）。

## B 进度（MyBatis 持久化改造）

1. B1 schema 补 5 张表（reviews/conversations/messages/promotions/id_sequence）+ seed 对齐，两份 schema.sql 哈希比对一致。
2. B2 pom 依赖（mybatis-spring-boot-starter 3.0.4 + mysql-connector-j）+ application.properties 环境变量注入配置完成。
3. B3 14 个 Mapper 接口（注解 SQL）+ IdGenerator（`SELECT ... FOR UPDATE` + `UPDATE`）+ HashUtil 完成。
4. B4 7 个 Service 事务化重构完成（`@Transactional` + 条件库存更新 + 行锁），InMemoryRepository 已删除；AuthController 依赖从 InMemoryRepository 换为 StoreService。
5. B5 5 个 Service 测试类重写为 `@SpringBootTest` + `@Sql(reset.sql)` + `@SqlConfig(UTF-8)`；reset.sql（每用例前 TRUNCATE + 重插）建好；集成测试补 `@Sql` 数据隔离。
6. 修复点：
   - Domain.Store/OrderItem/Conversation 补无参构造器（MyBatis 实例化需要）。
   - OrderService.create 补 `orderItems.insert(id, item)` 明细落库（原代码漏了）。
   - ExtensionService.overview 调用 `orderMapper.overviewByStore` / `productMapper.countByStore`（原调用未定义方法）。
   - GlobalExceptionHandler 补 `log.error("未处理异常", ex)`，便于排查 500。

## 验证证据

1. `mvn clean test` 全绿：`Tests run: 20, Failures: 0, Errors: 0, Skipped: 0` — BUILD SUCCESS。
2. 冒烟全链路通：登录（u001）→ 加购（p104×3）→ 下单（o1008）→ 支付（PROCESSING）→ 查库（订单/明细/库存扣减/购物车清空/会话/ID 序列全部落库）。
3. 重启持久化验证：后端进程重启后，历史订单（o1005）经 `/api/v1/orders` 仍可查到，数据未丢。
4. 冒烟脏数据已清理（o1005/o1008 + 库存/销量/序列复位到种子）。

## 文档同步

1. `backend/README.md`：分层改为 Controller/Service/Mapper/Domain，启动/测试命令改环境变量注入。
2. `docs/backend/架构设计.md`：版本升 v1.1，§1/§2/§3/§6 同步 MyBatis + 服务器 MySQL 现状。
3. `docs/project/运行说明.md`：§4.2 表填齐 S1-S6，§4.3 展开部署步骤。

## 本次工作 AI 使用点

- AI 辅助：服务器 MySQL 安装脚本、数据库初始化脚本、MyBatis Mapper SQL 设计、Service 事务化重构、测试类重写、冒烟验证脚本、架构文档同步。
- 人工判断：部署方案（共用服务器库 + 测试独立库 + 环境变量注入）、安全组放行口径、异常日志补全决策、文档同步范围。
