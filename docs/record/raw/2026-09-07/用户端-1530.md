# 2026-09-07 用户端留痕（购物车弹层与步进器批次）

## A 当日进度

- 负责人实测反馈"购物车不显示、希望商品行显示 - 1 + 步进器"。确认为 PRD 购物车弹层任务（P0）未实现：商品行只有 + 按钮、无数量指示、购物车栏点击无弹层。
- 确认后端已支持 PATCH /cart/items/{cartLineId}（quantity）与 DELETE（CartController 17-19 行），前端补齐接入：
  - mock 替身行为 M11/M12 先红：PATCH 改数量（quantity<=0 → 400，契约"数量必须大于 0"）、DELETE 删行（重复删 → 404"购物车行不存在"）；
  - api/cart 补 patchCartItem/deleteCartItem，endpoints 补 cart.byId；
  - cartStore 新增 decrementLine（减到 0 转 DELETE，契约 §3.4"前端减到 0 改删除"）/incrementLine（PATCH +1）与 isStepping 防重（XA-05 同款）；
  - StoreDetailView：商品行内步进器"- 数量 +"（未加购仅 + 按钮）+ 购物车弹层（遮罩/行列表/步进器/合计/收起），点击购物车栏展开、去结算按钮 stopPropagation 防误触弹层。
- 回归证据：**100 passed (16 files) 全绿**；build（type-check + vite build）通过。

## B AI 使用点

1. 用例 T47-T50 + M11-M12 由 AI 设计落地（沿用 1359 追认的 M/T 框架与"加购需登录"口径）：步进器 + 走 PATCH、+ 首次走 POST 合并、- 减到 0 转 DELETE，均为契约既有口径无新增业务规则。
2. 测试骨架两处修复（AI 自查）：① loginAndAdd 在 mountDetail 建 pinia 前调用 store（pinia 时序，改为先建后传入）；② cart mock 模块级内存态跨用例泄漏（T15 加购 2 件致 T47 计数 2+1=3），beforeEach 补 clearMockCart——同 ConfirmOrderView.spec 既有经验。
3. 交互细节：+ 按钮复用 add-btn-{productId} testid（未加购/已加购同键不同分支），减按钮为 minus-btn-{productId}，数量 product-qty-{productId}——命名约定随本片段留档。
