# 2026-09-08 组长留痕（商家详情页两问题：点餐区连续联动/铺满 + 换店闪屏，TDD 修复）

## A. 进度事实

- **负责人真机反馈两个页面问题，均已按 TDD 红绿修复**（分支 `fix/store-detail-scroll-flash-tdd`，未提交，待组长确认提交与拆分）：
  - **问题一：点餐区分类栏与商品列表不是连续联动，且列表未铺满底边**。
  - **问题二：进入另一家店铺详情页时，短暂闪现上一家店铺的详情页**。
- **问题记录**（负责人要求）：写入本地归档 `docs/_archive/2026-09-08-商家详情页问题记录.md`（该目录已 gitignore，属本地留档）；含三处口径对照（PRD 576/827 用词「切换」含糊、项目规则 §5 明确「不得把分类切换做成页面切换」、设计稿导出 code.html 为分段连续 + 内层滚动）、根因定位、修复口径与待确认项。
- **口径冲突与本次执行选择**（待组长确认）：本页滚动容器策略上，设计稿导出为「页面不滚动、头图/横幅/Tab 常驻、左右两栏各自滚动」，`项目规则.md` §5 描述为「外层先滚到 Tab 吸顶线、之后只滚右侧列表」。本次按**设计稿导出**实现（页面不滚，§5 的两条禁止项均满足）；若要严格按 §5 的滚动交接，需另行改造并承担 `preventDefault + scrollBy` 接管滚动的卡顿风险（复刻经验 §2.7 已警告）。
- **红绿证据**：
  - **红**（实现前复跑 `StoreDetailView.spec.ts`）：T13 失败（列表为过滤式，分区锚点缺失）／T68 失败（无 IntersectionObserver 观察，targets=0）／T69 失败（首帧渲染了旧店横幅「肯德基宅急送」）；其余 14 用例仍绿。
  - **绿**（实现后）：T13/T67/T68/T69 全绿；单文件连续 3 轮稳定；全量 **22 files 全绿**（两轮复跑）+ `npm run build`（含 type-check）通过。
- **修复内容**：
  - `catalogStore`：新增 `detailStoreId` 归属标记，换店先清 `storeDetail`（同店刷新保留缓存秒开）——T67 当时只覆盖分类/商品，漏了详情本身。
  - `StoreDetailView`：`store` 计算属性加「详情归属当前店铺」守卫（首帧即不渲染旧店，非仅事后清理）；右侧改为**分段连续渲染全部分类商品** + 分区标题 `sticky` 吸顶 + `data-section-id` 锚点；点击分类滚动定位（`scrollTo`，含程序化滚动窗口期抑制回调）；`IntersectionObserver` 滚动高亮同步；页面改为定高不滚、点餐区 `flex:1` 内层滚动、底部留白由 72px 校正为购物车栏实际高度 56px。
- **真浏览器验证**（Playwright + 系统 Chrome，脚本与截图在 `test-results/store-detail-verify/`，不入库）：
  - 铺满：m001 列表底边 = 购物车栏顶边（**间隙 0px**，修复前 320px 断口 + 16px 灰边），`.app-main` 不可整页滚动。
  - 连续：m001 招牌/配菜两分类商品同时存在于 DOM。
  - 点击定位：m002 点「饮品」→ 列表 scrollTop 0→366（= 可滚上限），高亮「饮品」。
  - 滚动高亮：滚到顶 →「主食」；滚到底 →「饮品」（修复前顶/底均为「饮品」——诊断发现 `IntersectionObserver` 只上报变化条目，实现未维护命中集合）。
  - 换店：m002 → 首页 → m001，`.store-name` 变化序列仅 `["老王小店"]`，旧店名未出现。
  - 控制台/页面错误：无。
- **测试口径变更**：T13 原断言「点第 3 个分类后列表**不含**第 1 个分类商品」硬锁过滤模型，已改为连续渲染 + 锚点 + 点击定位断言；新增 T68（滚动高亮，含「多分区同时命中取最靠上」回归断言）、T69（换店首帧不渲染旧店）。

## B. AI 使用点

1. **口径溯源**：AI 横向核对 PRD/项目规则 §5/复刻约定 §2.7/设计稿导出四处口径，发现并显式记录冲突，按 §4.3 优先级选择实现路径并标注待组长确认。
2. **TDD 红绿**：三个用例先写（红端逐条复跑取证）再实现；真浏览器验证又反查出 scroll-spy 的「命中集合」缺陷，补实现并回补单元断言。
3. **验证方式**：用 Playwright + 系统 Chrome 做几何量测（列表底边 vs 购物车栏顶边、scrollTop 上限、DOM 序列）而非仅截图目视；截图存 `test-results/`（gitignore）供组长复核。
