# 2026-09-08 用户端留痕（注册页 RegisterView TDD 落地批次）

## A. 进度事实

- **注册页（RegisterView）P0 落地**，补齐登录页"去注册"入口的路由缺口（此前 `router.push({name:'register'})` 因路由缺失失效）：
  - 新增 `src/views/user/__tests__/RegisterView.spec.ts`：R1–R9 用例（口径=PRD 7.16.1 注册页顶部栏/注册表单两行 + TC-ACC-009 前端侧），覆盖渲染、必填/格式逐字段提示、注册成功回登录不建会话、409 保留账号昵称、请求中禁用防重复（XA-05 同源）、顶部栏返回（空表单直达/有内容原生 confirm 确认放弃）、底部去登录。
  - 新增 `src/views/user/RegisterView.vue`：与登录页同族风格（tokens.css 设计令牌），注册页专属顶部栏（返回目标登录页，`router.back()` 优先、无历史兜底 `replace` 登录页）；品牌区"轻量外卖/欢迎加入，发现校园美食"；字段占位按导出稿（请输入手机号/设置你的昵称/设置6-20位密码/再次输入密码）；协议默认未勾选，协议链接为静态文本（导出稿即 span，PRD 未定义跳转目标）；校验复用 `validateRegisterForm`；失败保留全部已填内容（PRD 821，与登录页失败清密码行为不同）；已登录访问不重定向（PRD 未约定不外推）。
  - `src/router/index.ts`：新增 `/register` 路由（BlankLayout 子路由，name `register`，meta `title: 注册`，P0，公开页不设 auth）。
  - 视觉口径（负责人 2026-09-08 默认授权）：与已合并登录页同族简化风格，未按导出稿 1:1 复刻输入框内图标/灰底/明文切换；如需精修两页应一起精修（留痕待追认）。
- **红绿证据**：红=仅提交 spec 时 `RegisterView.spec.ts 1 failed（Failed to resolve import "../RegisterView.vue"）`；绿=实现后新用例 9/9 通过，全量回归 **134 passed (21 files)**（原基线 123 passed / 20 files，另含 9/8 三批 http.spec 3 条与本批 9 条中的口径差）。
- **`npm run build` 全绿**（含 vue-tsc type-check 与 vite 打包，RegisterView 4.37 kB gzip 1.76 kB）。
- **真浏览器冒烟 7/7 通过**（临时脚本 `test-results/register-smoke/smoke.cjs`，不入库）：空表单逐字段提示、注册成功 toast+回登录、409 拦截、有内容返回原生 confirm 取消/确认、登录页→去注册入口、320/390 无横向溢出；截图（390 空表/错误态/已填/320）留 `test-results/register-smoke/`。视觉与导出稿 `02-注册/screen.png` 结构逐区对应。
- **顺手修复（非本批范围，建议单独提交）**：`src/services/__tests__/http.spec.ts:18` mock 路由 `meta: { auth: true }` 缺 RouteMeta 必填 `title`——9/8 三批补录用例后未重跑 build 的遗留类型错误，补 `title: '订单'`（行为无关单行），`http.spec` 复跑 3/3 绿。

## B. AI 使用点

1. **TDD 两段**：用例清单 R1–R9 场景由 PRD 7.16.1 两行验收列逐句推导（人定口径），AI 落断言/testid/脚手架（仿 LoginView.spec 模式）；实现由 AI 生成后经全量回归+真浏览器冒烟验证，待负责人 review 追认。
2. **真浏览器取证闭环（复用 9/8 上午先例）**：ZCode IAB 输入事件管线损坏（fill/click 静默不生效，与 raw/2026-09-08/用户端-1047 一致），改用 playwright-core + 本机 chromium-1234（`--no-save` 装 `test-results/register-smoke/`，不入库）。
3. **重要环境发现——dev server 实际为 real 模式**：`.env.development.local`（不入库）覆盖 `VITE_API_MODE=real`，冒烟全程打真后端 127.0.0.1:8080。由此：
   - 冒烟脚本曾用 8 开头"11 位数字"账号，被真后端 400 `账号必须是 11 位手机号` 拦截（后端要求 1 开头）——**后端兜底校验工作正常**，页面正确 toast 并停留（即 R5 行为），排查耗时较长，最终以 console 监听定位；
   - **副作用披露**：联调真库已写入若干冒烟测试账号（13900001111/13900002222/13900003333/13800000002 及数个 138/139 开头时间戳号，昵称"冒烟同学/调试同学/对照同学"等），后端无注销接口，暂无法清理，请后端同学知悉（演示账号 13800000001 未受影响）。
4. **后续对齐建议（未动代码，待拍板）**：`validators.validateAccount` 目前 `/^\d{11}$/`，真后端要求 1 开头；建议确认口径后前端同步收紧为 `/^1\d{10}$/`（连同登录页共用与 TC-ACC-002~004 用例回归），可在需求变更批次处理。
