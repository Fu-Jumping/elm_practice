# 2026-09-08 组长留痕（通栏被卡片化：按设计稿回改 7 个页面）

## A. 进度事实

- **按负责人「直接开始改」指令，把排查出的「通栏被卡片化」按设计稿回改**（分支 `fix/design-fidelity-full-bleed`，改动未提交）。
- **红端基线**：先写通栏结构断言脚本 `test-results/design-fidelity/verify.cjs`（Playwright 真浏览器，real 模式 5174 登录态，逐区块断言「满宽 = 壳宽、radius=0、无阴影」），改动前 **23/23 全部失败**（证据：`.co-card` 左缘 12/宽 366/390、radius 7.9989px）。
- **改动内容**（5 个并行子代理各改独立文件）：
  - `ConfirmOrderView.vue`：4 个 `.co-card` 去圆角、去左右内缩，按稿改 padding（地址 24/12、送达 18/12、商品 10/12、备注 18/12）。
  - `OrderListView.vue`：`.ol-card` 去圆角、padding 16px、块间 8px 灰缝；`.ol-empty` 去圆角。
  - `OrderDetailView.vue`：3 个 `.od-card` 去圆角，状态区上下 1px、收货/商品段底部 1px（统一 `--color-border-light`）；内部分隔线 `#f3f3f3`→`#e5e5e5`。
  - `MineView.vue`：`.mn-profile` 改回通栏橙块（`--color-primary`、`border-radius: 0 0 24px 24px`、文字改白色系）、`.mn-menu` 改通栏并补「常用功能」标题行、行下边线 `#f3f3f3`→token。
  - `AddressListView.vue` / `AddressEditView.vue`：`.al-card`/`.ae-card` 去卡片壳，地址行改通栏 + 底部 1px + 右侧竖线编辑区；表单改行式（min-height 56、行下边线、输入框去描边）。
  - `StoreDetailView.vue`：`.store-tabs` 去掉 8px 灰缝、补 `border-top: 1px`（与横幅同底通栏），吸顶线与高度算式未动。
- **绿端与回归**：
  - 通栏断言 **23/23 通过**（`.co-card`×4、`.ol-card`×10、`.od-card`×3、`.mn-profile`、`.mn-menu`、`.al-card`×2、`.ae-card`、`.store-tabs` 全部满宽/零圆角）。
  - 全量单测 **22 files / 141 tests 全绿**；`npm run build`（含 type-check）通过。
  - **§5 滚动交接复跑通过**（双视口 390x844/390x568）：Tab 吸顶仍 `top=44`、外层最大滚动量 238→230（正是去掉 8px 灰缝的预期）、点击分类「先滚外层到吸顶线再定位内层」不变。
  - **主链路巡检复跑 34/34 通过**、无控制台错误、无失败请求（确认 CSS 改动未破坏功能与交互）。
  - 截图留档 `test-results/design-fidelity/01-07*.png`（不入库）。
- **未改项（子代理报告的残留差异，待决策）**：商品图圆角 8px vs 稿 2px（`--radius-sm` 已存在且零引用，建议单独一轮）；顶栏高度 44 vs 56、购物车栏 56 vs 64、字号整体偏小等尺寸类偏差；订单列表筛选栏、订单详情底部操作区、地址页两个固定底栏、我的页会员横幅/统计卡等结构缺失；地址徽标 999px 胶囊 vs 稿 2px。

## B. AI 使用点

1. **先红后绿**：改动前先跑通栏结构断言拿到 23/23 失败的基线，改完复跑转绿，避免「改完看着像好了」。
2. **并行分文件改**：7 个文件按页拆给 5 个并行子代理，各自只改 `<style scoped>` 与必要容器类名，禁止动 testid/事件/文案/逻辑；主线由组长统一跑断言、单测、§5 交接验证与主链路巡检。
3. **回归兜底**：CSS 改动后不仅跑单测，还复跑了 §5 滚动交接与主链路巡检，确认视觉改动没有破坏上一轮刚修好的滚动行为。
