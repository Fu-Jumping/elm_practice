# 2026-09-08 组长留痕（讲稿深度重写：展示页全量信息 + 9 分钟讲满，组长-2324）

## A. 进度事实

- **展示页全量读取完成**：抓取 `http://82.157.137.114:4001/demo/` 原始 HTML（35,661 字符，单文件自包含，12 屏）并解析其内嵌数据——`people` 数组（五人贡献 4,424 字符）、`bugs` 数组（6 个缺陷案例）；另抓取页面引用的 `engineering-audit.md`（《2026-09-08 三端工程检查与发布报告》90 行）、`evidence/api-smoke.json`（50/50 检查）、`contributions/{付赞平,戴宇涵,王馨琪,王嘉悦,林晨}.md` 五份个人总结。抓取产物落 `tmp/demo-src/**`（gitignore，不入库）。
- **产出**：`docs/_archive/2026-09-09-中期汇报讲稿-深度版.md`——按展示页 12 屏逐屏重写，总时长 **9:00 讲满**（10 分钟内留 1:00 给打断/提问），每屏标时长与累计；含备用弹药区（老师追问时不主动讲）、超时裁剪顺序、讲前检查 8 项、4 项待确认。替代并保留前两版（初稿、对齐展示页版）作历史。
- **展示页关键信息已全部纳入讲稿**：
  - 概览屏：三端技术栈与路径（`/user/`→`/user/api/v1`、`127.0.0.1:4000`→MySQL、`/`→`/api/v1`）；数据可信边界（真实 API/MySQL、支付为模拟支付、营销装饰与种子评分非实时）；发布基线 main `508889e` + 工作区修复；Nginx 4001 + Cookie 分路径双角色；"现场检查真实店铺接口"按钮的真实请求行为。
  - 三线屏：用户端 148 / 后端 25（独立库）/ 商家端 10 项测试；商家端"main 原为静态骨架、服务器代码未入 Git、先备份再恢复"。
  - 演示屏：六步操作、20 元起送价、小计 + 2 元打包费、配送费不参与计价、付款前订单不进商家列表、状态变化需手动刷新、操作真实写入数据库、失败重试不伪造订单、演示账号（用户 13800000001 / 商家 merchant-a）。
  - 两次打回：PR #1（`.env.*` 被忽略、AI 误判经 `git check-ignore -v` 纠正、修复 0c8af07/827eac8、合并 3fb0c2b、四条规则升级）；PR #9（`d63fe1a` 约 1,095 行实现、`b6fb523` 35 行记录、复跑 4 passed、经 PR #11 重建、四条规则升级、"工作区红绿不能冒充 Git TDD 链"）。
  - 缺陷屏 6 案例：真实订单字段致空白、页面切换白屏、main 与服务器版本漂移、营业状态 200 未落库（红测 2 项中 1 项失败→25 passed）、字段错配与库存被旧值覆盖、支付与状态未成演示链（2 failed/1 passed→3 passed）。
  - 质量屏：148/10/25/50 口径（148 = 基线 145 + 支付 3，50 为请求检查数）、从返回成功到业务结果、过程台账归属与王馨琪写权、"不是覆盖率或正式验收通过率"边界、Edge 桌面/移动视口。
  - 五人贡献与两条归属边界；9/9–9/15 五日时间线、P1 完成门槛、P2 仅为候选；交付入口与"已部署未 commit/push"声明；四个讨论重点。
- **备用弹药（讲稿 §16）**：八层检查范围与未覆盖项、50 项接口覆盖清单、合成证据（o1094/m1088/u1086、金额 60、COMPLETED）、重启持久化一致（o1094/o1097 金额 60/31、m1088 CLOSED）、Edge 彩排订单 o1097（31 = 29 + 2）、已知未修项（SHOW-QA-002、配送费文案口径、缺陷状态待确认、作者归属待补）、发布与回滚路径（releases/Nginx/PM2/备份目录）。

## B. AI 使用点

1. **全量读取与结构化解析**：AI 抓取展示页原始 HTML 并解析内嵌 `people`/`bugs` 数据、关联报告与证据 JSON，确保讲稿覆盖页面全部信息（含被文本提取遗漏的五人贡献细节与 6 个缺陷案例）。
2. **编码问题自查**：首次抓取 `engineering-audit.md` 因响应体按字节数组处理出现乱码，AI 改用 `-OutFile` + UTF-8 显式解码重取，未在乱码内容上继续加工。
3. **口径分层**：把 148/10/25/50 的口径来源（基线 145 + 支付 3；50 为请求检查数）与"非覆盖率、非验收通过率"边界写入讲稿正文，避免现场被追问时口径失守。
4. **时长工程**：按 12 屏逐屏配时并核算累计到 9:00，另设"砍时优先级"（五人贡献→缺陷案例→规划细节→演示步数），并明确两次打回与边界声明不砍。
5. **人负责判断**：演示做不做、贡献屏是否点开、商家端由谁讲、边界声明取舍均留白待负责人拍板。
