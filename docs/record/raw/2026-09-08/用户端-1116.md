# 2026-09-08 用户端留痕（首页 real 形状店招图片缺陷修复批次：T67/T68 红绿对）

## A 当日进度

- **负责人真机复核反馈"店的图片没了、食物也没了"**（real 模式，localhost:5173）。定性拆分：
  1. **店招图片空 = 前端真缺陷，已修**（红绿对 e84006a → f355e32，分支 feat/home-real-image-fallback-tdd）：HomeView.vue 店招 StoreCover 直绑 `store.image`，未走 `storeImageSrc` 兜底链；9/7 图片接入仅覆盖详情页（StoreCover/商品行），mock 数据层直接填 image 字段掩盖了首页缺口，T46 断言在 mock 下跑故绿。修复：店招改走 `storeImageSrc(store.storeId, store.image)`；**T67 先红**（real 形状断言 failed）→ 修复后**全量 122 passed (19 files) 全绿**。T68 为预览商品图对照用例（聚合层 aggregatePreviews 已有 productImageSrc 兜底，锁定性质，预期绿非红项）。
  2. **食物没了 = 后端种子数据缩水，非 bug**：真库仅 m002 肯德基 5 商品（p101-p105），其余 4 店无商品；mock 的"5 店 15 商品"是演示数据。待决策：后端补种子（商品号对齐 demoImages 映射 p101-p106/p201-p209，image 留空由前端映射自动出图）或验收演示主走肯德基。
- 页面验证：修复后 real 模式首页店招 `img "老王小店"` 恢复（修复前为首字兜底 generic"老"）。
- 测试基建：HomeView.spec 的 mockDispatch 改可编程代理（默认代理原始实现，用例内覆写剥离 image 字段对齐 real 形状），为后续 real 形状类用例提供模式。

## B AI 使用点

1. 现象拆分定性：负责人一句反馈拆为"前端缺陷（店招图）+ 数据缺口（商品种子）"两类，分别走代码修复与数据决策，避免把数据问题误当 bug 修或反之。
2. 红绿纪律完整执行：T67 红（e84006a 仅含测试）→ feat 绿（f355e32），红 X failed / 绿 122 passed 证据齐备；T68 如实标注"对照锁定、非红项"，不冒充红绿对。
3. 复用 9/7 demoImages 兜底链与测试模式（HomeView.spec T9 的 store spy、T46 的聚合断言），未新造轮子。
