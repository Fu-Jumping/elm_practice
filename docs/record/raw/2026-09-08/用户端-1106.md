# 2026-09-08 用户端留痕（real 模式全链路自测批次：DB 密码打通 + curl API 链 + 页面冒烟）

## A 当日进度

- **SSH 打通开发服务器**：本机 ~/.ssh/config 补齐 zc-prod/zc-dyh 两入口（负责人提供）；zc-prod（ubuntu）密钥认证直通，zc-dyh（dyh）未装本机公钥暂不可用。
- **DB 密码打通（经负责人逐级确认）**：负责人记忆的 111111 / 111!!!aaa 经服务器本机实测均被拒（`elm@'%'` 授权范围正常，纯密码不符）；服务器全面检索（pm2 dump、/root、/home/wjy、.my.cnf、bash history）无现行密码留存（pm2 旧值 `ElmLocal_2026!` 对应本机专用账号 elm_local@localhost，非远程账号）。**负责人确认后执行 `ALTER USER 'elm'@'%' IDENTIFIED BY '111111'`**。⚠️ 知会事项：后端同学（wjy）本机 DB_PASSWORD 旧值已失效，需同步更新。
- **本地后端启动成功**：mvn spring-boot:run 连服务器开发库（DB_HOST=82.157.137.114），Started 正常。
- **curl API 全链自测（MyBatis 迁移后契约验证）全部符合契约**：
  1. 注册新用户 ✓；重复注册 409/40901 ✓；登录/探活 ✓（演示账号 13800000001）
  2. 店铺列表过滤 CLOSED ✓（开发库发现 QA 残留店 m1013 "QA-test-B"，首页不可见）
  3. 加购（cartLineId/小计/实时库存回显）✓；下单计价快照 ✓（29×2=58 + 打包费 2 = 60.00）；支付 → PROCESSING + paidAt ✓；订单列表 status 筛选 ✓；详情 ✓；库存扣减（18→16）与销量（122→124）✓；购物车原子清空 ✓
  4. 异常口径：未登录 /cart、/orders → 401/40101 ✓（XA-06 公开页静默的真后端前提）；重复支付幂等返回当前结果（非 409）——契约 §203"重复支付请求返回当前结果"符合 ✓（超时支付才 409）
  5. 自测数据已清理：o1018 订单+明细、u1015 测试用户删除，p101 库存/销量复位（18/122）；ID 序列跳号未回拨（无害）
- **real 模式页面只读冒烟（IAB URL 直达）**：首页真数据（月售1000+、真库商品预览、5 店）✓；/stores/m002 真 5 商品全渲染 + demoImages 映射生效 ✓（/demo-images/*.jpg 同源 200）；未登录直达 /orders → 跳 /login?redirect=/orders 守卫 ✓。
- **过程踩坑**：TaskStop 停 vite 未杀 node 子进程（9/7 mvn 同款坑，netstat+taskkill //PID //F 解决）；Git Bash curl 带中文 JSON 触发后端 40001"无效 JSON"（shell 编码问题，非应用缺陷，ASCII 体正常）。
- 运行中环境：本地后端（8080，连服务器库）+ vite dev real 模式（5173），留作负责人真机复核与明日验收演示。

## B AI 使用点

1. 共享凭据变更严格走"证据先行 + 负责人逐级确认"：先服务器本机实测排除授权问题，再全面检索证明无留存，最后 AskUserQuestion 确认才 ALTER；影响面（wjy 本机环境变量失效）同步提示。
2. 服务器探索全部只读（SELECT/ls/grep/cat），唯一写操作为负责人确认后的 ALTER USER；sudo 免密权限未滥用。
3. 全链自测仿照后端同学冒烟口径做"测后清理"，恢复库状态，不留验收演示脏数据；剩余脏数据 m1013 已登记待决策（后端同学确认来源后删或留）。
4. 真机复核指引已给到负责人：5173 real 模式 + 一键登录演示账号按钮，重点三现象（卡片进店、详情点击、快速切换店铺商品消失）。
