# 2026-09-08 组长留痕（商家详情页点餐区改为严格 §5 滚动交接）

## A. 进度事实

- **负责人拍板「严格按 §5」，取代当日上午的「页面不滚」临时实现**；本次按 `docs/project/项目规则.md` §5（2026-05-29 滚动交接条）重做商家详情页点餐区，并同步补齐文档口径。分支 `fix/store-detail-scroll-flash-tdd`（改动仍未提交，待组长决定提交与拆分）。
- **落地方式（不接管滚动，用高度算式实现交接）**：
  - `.store-tabs` → `position: sticky; top: 44px`（吸顶线 = 顶部栏下沿）；顶部栏与 Tab 均为不透明背景。
  - `.order-area { height: calc(100dvh - 88px) }`（视口 − 顶部栏 − Tab 栏）→ 外层页面最大滚动量恰为「横幅高度 + Tab 上边距」，滚到吸顶线自然停住，之后只滚内层。
  - 左右两栏各自 `overflow-y: auto`，底部留白 56px 避让固定购物车栏；分区标题 sticky 吸顶、分区容器带 `data-section-id` 作为锚点。
  - 点击分类：先 `pinOuterToTabs()` 把外层滚到吸顶线，再滚内层到目标分区（顺序不可反）。
- **过程中真浏览器验证反查出三个缺陷并修复**：
  1. 观察 sticky 分区标题永远等不到「离场」→ 改为观察分区容器；
  2. 长分区内滚动、或从底部滑回顶部时交集状态不变、`IntersectionObserver` 不回调，高亮滞留在错误分类 → 改为**偏移量判定为准**（`syncActiveCategory`：取最后一个 `offsetTop <= scrollTop` 的分区，滑到底部特判最后一个），观察器仅作触发源；
  3. 分类/商品先于店铺详情返回时列表尚未渲染，`setupScrollSpy` 提前返回且此后无人触发 → 把列表元素本身纳入 watch 依赖（否则滚动高亮整体失效）。
- **红绿与回归**：新增 T70（点击分类先滚外层到吸顶线）；`StoreDetailView.spec.ts` 18/18（两轮）；全量 **22 files / 141 tests 全绿**；`npm run build`（含 type-check）通过。
- **真浏览器双视口验证**（Playwright + 系统 Chrome，390x844 与 390x568，脚本/截图 `test-results/store-detail-verify/`，不入库）：Tab 吸顶 `top=44`；外层滚到底后再滚不越线（238/238）；内层滚到底高亮「饮品」、外层不动；点击「饮品」外层 0→238 后内层定位到上限；滚到顶「主食」/滚到底「饮品」；列表底边铺满视口底部（56px 内边距避让购物车栏）；换店首帧 `.store-name` 序列仅 `["老王小店"]`；控制台无 error。
- **文档同步（负责人授权修改与明确）**：
  - `docs/project/项目规则.md` §5 追加 2026-09-08 行：写明吸顶线、高度算式、点击顺序、偏移量判定口径与「sticky 标题不可作观察目标」等执行细则。
  - `docs/frontend/UI复刻经验与执行约定.md` §2.7 追加「落地配方」：高度算式实现交接、吸顶不透明背景、观察器只作触发、sticky 标题陷阱、程序化滚动短锁、px→vw 与 calc 一致性。
  - 本地归档 `docs/_archive/2026-09-08-商家详情页问题记录.md` 更新为「严格按 §5」口径与两轮修复结果。

## B. AI 使用点

1. **按约束实现而非按直觉**：负责人拍板 §5 后，先复读 §5 原文与复刻约定 §2.7，用「高度算式 + sticky」实现交接，避免 `preventDefault + scrollBy` 接管滚动（§2.7 已警告卡顿）。
2. **验证驱动修缺陷**：真浏览器双视口量测（吸顶位置、滚动上限、内外层 scrollTop、高亮文本）逐条比对预期，反查出 3 个 IntersectionObserver/sticky 相关缺陷并回补单元断言。
3. **文档回填**：把验证得到的执行细则写回项目规则 §5 与复刻约定 §2.7，避免同类页面复发。
