# 2026-09-08 后端 BUG-002/003 修复留痕

成员/角色：后端 AI 协作
时间：10:40

## A 进度（当日完成事实）

1. BUG-002 修复：CORS 白名单补 `http://82.157.137.114:*`（WebConfig），服务器部署前端后浏览器请求不再 403。
2. BUG-003 修复：`Requests.MerchantRegister` 补契约字段 `contactPhone`，AuthController 兼容 `phone`/`contactPhone` 二者其一，商家端按契约注册不再 400。
3. TDD 红绿证据：test 提交（5e6486b）上复跑 2 个新测试必红（corsAllowsServerHostOrigin 403 / merchantRegisterAcceptsContractContactPhoneField 400）；fix 提交后全量 22/22 通过，BUILD SUCCESS。

## B 插曲：服务器 MySQL 凭据失效

1. 现象：全量测试报 `Access denied for user 'elm'@'114.246.239.125'`，pymysql 直连同样 1045。
2. 排查：SSH 登服务器查 `mysql.user`，`elm`@`%` 账号仍在、host 正常，但密码被改；服务器上新增了 `elm_local`@`localhost` 账号，判断有队友动过数据库账号。
3. 处置：按 `docs/project/运行说明.md` §4.3 文档口径重置 `elm`@`%` 密码为原值（环境变量 `DB_PASSWORD` 注入值），授权（elm_practice + elm_practice_test 全权限）验证完好，远程连通恢复。
4. 提醒：服务器 MySQL 账号密码变更须同步 `运行说明.md` §4.3 并周知全组，否则他人环境全部断连；联调同学如需独立库/账号请新增（如已有的 elm_local），不要改动公共 `elm` 账号。
5. 补充（10:55）：确认队友改密码为 `111111` 是有意的（与 SSH 密码统一口径），遂按全组口径把 `elm`@`%` 统一改为 `111111` 并双库验证连通；`运行说明.md` §4.3 已同步该口径，约束"改密码必须周知全组并更新文档"。

## 本次工作 AI 使用点

- AI 辅助：BUG 复现测试编写、修复实现、服务器排查脚本（paramiko）、红绿验证。
- 人工判断：BUG 单派发与优先级、elm 密码重置决策（按文档口径恢复而非迁就未知新值）。
