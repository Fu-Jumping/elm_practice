# 2026-09-08 组长留痕（滚动交接 + 起送拦截 + 订单列表底色，含三笔 PR 合并）

## A. 进度事实

- **负责人本轮拍板**：① 起送金额「点去结算就拦」；② 订单多状态「先不动」；③ 订单列表「可以加颜色」；④ 分类条「让分类条真粘住」；⑤ 新发现「鼠标放在商品列表里滚动不会先滚整页」要求一并修；⑥ vite 配置「保留并正规化」；⑦ PR #32 先合并。
- **合并（本轮）**：PR #32 购物车抽屉贴底（`2f8808d`）、PR #33 vite 监听配置正规化（`f31b915`）、PR #34 滚动交接（`4c4d4e0`），均 `--merge` 合入 main。
- **① 滚动交接修复（PR #34）**：根因是 `.product-list`/`.cat-rail` 为独立滚动容器、指针悬停其上时滚轮被内层直接消费；新增 `outerPinned` 状态位（监听 `.app-main` 滚动判定是否到 Tab 吸顶线），未吸顶时 `.order-area--locked` 锁定两栏 `overflow-y`。红端 `b421ea5`（未吸顶时内层滚 100px、外层 `mainScrollTop=0`）→ 绿端 `a794aac`（E2E 2 passed）。
- **② 起送拦截**：PRD 点餐内容区行修订为「登录 → 购物车非空 → 店铺营业 → 起送金额」；`onCheckout()` 在购物车非空后加差额拦截（算法与确认订单页同口径），确认订单页内兜底保留。红端 `27d5a14`（直接跳到 order-confirm）→ 绿端 `645d0d6`（StoreDetailView.spec 19 passed）。
- **③ 订单列表底色**：根因是页面底色 `#f9f9f9` 与白色仅差 6 个色阶、8px 灰缝肉眼不可见；改为 `var(--color-surface-container)`（`#eeeeee`，设计稿 `.frame` 画布色）。红端 `4f19ba7`（缝隙实测 `rgb(249,249,249)`）→ 绿端 `67a5dd7`（E2E 1 passed）。结构与间距未动，未加 `border-bottom`（那是地址页/详情页的分隔手法）。
- **回归**：单测 22 文件 / **145 passed**（新增 T71）；`npm run type-check` 通过；E2E **3 passed**（抽屉 + 滚动交接 + 订单列表分隔）。
- **未做**：订单多状态（负责人明确「先不动」；后端 6 态状态机已就绪，前端/mock 未跟上）。

## B. AI 使用点

1. **红端必须真红**：三处修复均先 stash 实现、在只含测试的提交上复跑取证红端，再恢复实现转绿，满足 TDD 两段式纪律。
2. **探针排除干扰**：滚动交接定位时先用探测脚本量出对照数据（未锁定：内层滚 100px；锁定：内层恒 0），并发现 Playwright `locator.hover()` 会把元素滚进视口（等价于直接跳到吸顶态），改用 `mouse.move` 后红绿两端才成立——该坑已写入用例文件头注释。
3. **按设计稿真源定位**：订单列表底色靠设计稿 `.frame` 与实现截图的像素取样对照（`#eeeeee` vs `#f9f9f9`）定位，而非凭观感调色。
