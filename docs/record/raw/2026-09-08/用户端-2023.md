# 2026-09-08 用户端留痕（商家详情页购物车抽屉贴视口底端：TDD 红-绿落地）

## A. 进度事实

- **分支**：`fix/store-detail-cart-drawer-flush-bottom`（未 push、未开 PR，等负责人授权）。
- **提交顺序（红在前）**：
  - `4352ba1` `test: 商家详情页购物车抽屉贴底失败测试（预期红）`——仅含测试脚手架：`e2e/store-detail-cart-drawer.spec.ts`、`playwright.config.ts`、`package.json`（`@playwright/test` devDep + `test:e2e` 脚本）、`package-lock.json`，无业务实现文件。
  - `386a0d0` `fix: 商家详情页购物车抽屉贴视口底端`——仅 `frontend/user-h5/src/views/user/StoreDetailView.vue`（`.cart-popup` 的 `bottom` 与 `padding`）。
- **红端证据**：在 `test:` 提交上复跑 `npx playwright test` → `Expected: <= 1 / Received: 72`，`1 failed`。
- **绿端证据**：在 `fix:` 提交上复跑 → `1 passed (3.4s)`。
- **修复内容**：`.cart-popup` 的 `bottom` 由 `calc(72px + env(safe-area-inset-bottom))` 改为 `0`；底部内边距由 `12px` 改为 `calc(12px + env(safe-area-inset-bottom))`（贴底后「合计」行自行避开安全区）。`.cart-bar`、遮罩、z-index 均未动。
- **多视口几何确认（Playwright + 系统 Chrome，含加购态）**：390x844 / 390x568 / 430x900 / 1280x800 四视口，展开态「弹层底边→视口底边」均为 **0px**，弹层底边下方 3px 命中元素由 `div.cart-popup-mask` 变为 `section.cart-popup`；抽屉高 ≥ 底栏高（177.4 vs 64 / 577.9 vs 210）。
- **回归**：`npx vitest run` 22 files / **144 tests 全绿**；`npm run type-check` 通过。
- **工程取舍（回答负责人「能否不加依赖」）**：`docs/frontend/前端架构设计与工程约定.md` §6 已把 E2E 工具定为 Playwright（只写 3–5 条关键路径），故依赖入库合规；`playwright.config.ts` 经实测**必须有**——不加 `testDir: './e2e'` 时默认扫描项目根，会把 `src` 下 Vitest 用例当 E2E 收集并抛 `.vue`/TS 解析错误（`npx playwright test --list` 实测）。
- **待办 / 未做**：① 缺陷台账 `docs/testing/问题记录/**` + `docs/testing/缺陷跟踪表.md` 未落（写权归测试联调同学王馨琪，待负责人定由谁写）；② PR 未开（等 push 授权）；③ 方案②的「选项 B（抽屉内加去结算入口）」未做，展开态「去结算」按钮被抽屉盖住属既定口径；④ 附带发现 `.app-shell` 的 `max-width: 430px` 被 px→vw 换算成 110.256vw（桌面预览居中失效）未修，另立问题单。
- **环境备注**：排查期间 5173/5174 dev server 已不在运行，E2E 由 `playwright.config.ts` 的 `webServer` 自动拉起（mock 模式，无需后端），跑完自动关闭。

## B. AI 使用点

1. 红端用例的**断言口径**来自负责人选定的方案②（贴视口底端）与既有几何取证；AI 负责把它落成可复跑用例，且断言只测几何结果（间距/高度/elementFromPoint），不复述 CSS 声明，避免退化成实现细节复述。
2. 依赖与配置的必要性由 AI 实测判定（`npx playwright test --list` 复现误收集；jsdom 探针复现「读不到 scoped CSS + 无布局」），未凭偏好加依赖。
3. 提交拆分按 TDD 两段式执行：`test:` 提交内无业务实现文件，`fix:` 提交仅 CSS；红绿两端输出均留痕本片段。
4. 未执行 `git push`、未开 PR、未写 `docs/record/daily/**`、未动 `docs/testing/**`（写权归测试联调同学）。
