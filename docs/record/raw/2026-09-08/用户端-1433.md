# 2026-09-08 用户端留痕（跨店切换"暂无商品"缺陷诊断与 TDD 修复批次）

## A. 进度事实

- **负责人真机反馈两现象（5174 real 模式）**，诊断结论：
  - 现象 1"点击商品没反应"：**按 PRD 属预期口径，不修**。PRD 7.16.1 商家详情页-点餐内容区行明确"点击商品加号按基础价加购（规格扩展选定后才弹出规格弹层）"，P0 商品卡为纯展示（名称/描述/月售/好评率/价格就地展示），加号是唯一交互入口；商品详情/规格弹层属 P1 未选定扩展。负责人拍板维持 P0 口径。
  - 现象 2"进店先暂无商品，退出重进才有"：**真缺陷，已修复**（分支 `fix/store-switch-category-reset-tdd`，3 提交）。
- **根因**（真浏览器 Playwright 复现取证，脚本 `test-results/register-smoke/repro-store-switch.cjs` 不入库）：
  - 复现矩阵：进 m002 正常（5 商品/高亮）→ 返回 → **首次进 m005：暂无商品=true、0 商品、分类栏无高亮、列表标题空** → 重进 m005 恢复（4 商品/高亮）。
  - 链路：`catalogStore` 为全局单例，离开店铺后旧店分类/商品残留；进新店挂载时 `watch(immediate)` 用旧店数据把 `activeCategoryId` 锁到旧店分类 id；本店数据到达后 watcher `if (activeCategoryId.value) return` 不再重算 → 新店商品挂在另一分类 id 下 → 当前分类为空 →"暂无商品"。重进时组件重建 + 缓存已属本店才恢复。每换一家店必现（逛过别店后）。
- **修复**（红绿闭环）：
  - 红：T67 共享 pinia 进 m002 再进 m005，断言本店商品可见 + 默认选中第一个有商品的分类 + 不落空态；test: 提交（1041d41）上复跑 **1 failed | 14 passed**。
  - fix（eadf4b9）：catalogStore 新增 `categoriesStoreId/productsStoreId` 归属标记，换店请求先清旧店数据（同店刷新保留缓存秒开，ERR_CANCELED 语义不变）；StoreDetailView 默认分类 watcher 双守卫（数据归属当前店铺才参与默认选中 + 商品加载完成后再按"第一个有商品"判定）。
  - 绿：T67 转绿 15/15；全量回归 **126 passed（21 files）**；build 含 type-check 全绿；真浏览器复现脚本复跑：首次进 m005 **4 商品/高亮热销推荐**。
- **随分支提交**：http.spec 路由 meta 补必填 `title`（58e37bf，与 feat/register-page-tdd 分支同内容，任一先合并即修复 main 的 type-check，无冲突）。
- **存量 lint 债登记（未动）**：`StoreDetailView.vue:22` 常量 `ASSETS` 未使用（main 上即存在，与本次改动无关），建议后续 chore 批次清理。

## B. AI 使用点

1. **取证闭环**：负责人反馈"点商品没反应/暂无商品"后，先核 PRD 口径（827 行）定性现象 1；现象 2 用 Playwright + 本机 chromium 复现出"无高亮分类栏"铁证再定位代码根因，未凭猜测改代码。
2. **TDD 红绿**：用例 T67 场景由缺陷复现链路 + PRD"默认选中第一个有商品的分类"口径推导，AI 落断言与修复代码，负责人拍板修复方案；test: 提交上 stash 源改动复跑取证红。
3. **分支基线决策**：本地 main 落后 origin/main 6 笔（纯后端提交），fix 分支先 fast-forward 到 origin/main 再开工，避免 PR 基线错位；feat/register-page-tdd 仍基于本地 main（其改动文件与 origin 6 笔无交集，PR 时如遇落后再 rebase）。
