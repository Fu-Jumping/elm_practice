# 2026-09-08 用户端留痕（地址列表页 / 新增编辑地址页外观偏差修复）

## A. 进度事实

- **范围**：只改 `frontend/user-h5/src/views/user/AddressListView.vue`、`frontend/user-h5/src/views/user/AddressEditView.vue` 两个文件；测试、testid、事件绑定、文案、校验与提交逻辑均未动。
- **地址徽标圆角（地址列表页）**：`.al-default-tag`、`.al-label` 由 `border-radius: 999px` 胶囊改为 **2px**（设计稿 `05-收货地址/index.module.scss` `.backgroundBorder` :182-201、`.background` :264-282）。
  - 默认徽标按稿补几何：`border: 1px solid #ffb59e` + `padding: 1px 5px` + `line-height: 14px` + 字号 10px；配色按项目规则 §5 品牌橙口径保留 `#ff5a1f` 系（`color: #ff5a1f` + `rgba(255,90,31,.1)` 底）。
  - 普通标签改稿值：`padding: 2px 6px`、`background: #e2e2e2`（原 `#f3f3f3`）、radius 2px。
- **地址列表固定底栏**：顶栏「新增地址」文字链接删除（设计稿顶栏仅「返回 + 标题」），改为固定底栏 `.al-footer`（fixed、白底、`border-top: 1px #e5e5e5`、padding 11/12/24、高 85px）+ 按钮 `.al-footer-btn`（高 48px、radius 8px、`#ff5a1f`）；`data-testid="add-address-btn"` 与 `@click="goNew"` 原样迁移，跳转逻辑未变。
- **新增/编辑地址固定底栏**：`.ae-save` 由流内按钮（`margin: 0 16px`、radius 12px）迁入固定底栏 `.ae-footer`（fixed、白底、`border-top: 1px #e5e5e5`、padding 15/16/16、高 77px），按钮 radius 4px、`padding: 12px 0` + `line-height: 20px`；`data-testid="save-address-btn"`、`:disabled="saving"`、`@click="save"`、文案「保存/保存中」与渲染条件（`loaded && !missing`，与改造前一致）保持原样。
- **底栏避让**：`.address-list-page` / `.address-edit-page` 底部留白 = 底栏高 + 16px 安全间距（列表 `calc(85px + 16px)` = 101px、编辑 `calc(77px + 16px)` = 93px），滚动到底时最后一行距底栏 16px 不被遮挡；底栏用 `left: 50% + translateX(-50%) + max-width: 430px` 对齐 MainLayout 430px 页面壳（参照 StoreDetailView 既有固定底栏写法），窄/宽视口均不产生横向溢出。
- **回归口径**：`.al-card` / `.ae-card` 通栏满宽、零圆角未回退；`.al-card:last-child` 无底边线规则不受底栏影响。
- **未验证项（如实登记）**：本子代理环境无浏览器（工具返回 "Browser is not available in subagent"），且任务书要求不跑 npm，故未跑单测与浏览器断言；testid 与渲染条件经静态核对未变，红绿与视口断言交组长统一验证。

## B. AI 使用点

1. 设计稿数值比对（radius/padding/height 的 scss 行号定位）由 AI 完成；三项改动口径由任务书给定，未自行扩大范围（顶栏标题色、空态按钮等 §23/§15 项未动）。
2. 徽标配色取舍：按项目规则 §5「品牌强调色统一 #ff5a1f」保留橙色系配色，只按稿改几何（radius 2px 等），未引入设计稿的 `#ffdbd0/#ae3200`。
3. 固定底栏的居中与避让策略参照仓库既有实现（StoreDetailView `.cart-bar`），避免桌面预览下 `left:0/right:0` 铺满视口导致底栏与 430px 壳错位。
