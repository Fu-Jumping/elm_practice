# 2026-09-08 组长留痕（三个用户端 PR 评审合并批次：#29/#28/#30）

## A. 进度事实

- **当日三个用户端 PR 已全部评审并合并**，均为 qianhan-111 提交、基于 45b7240、无冲突；合并顺序 #29→#28→#30（小修复→注册页→滚动管线）：
  - **PR #29**（fix 跨店切换旧店分类锁定 T67）→ merge commit `d40172e`。
  - **PR #28**（feat 用户端注册页 TDD R1-R9）→ merge commit `e8f8e71`。
  - **PR #30**（feat 返回恢复原浏览位置 S1-S3）→ merge commit `97aa83b`。
- **每个 PR 的评审证据**（本地实测，非仅看 PR 描述）：
  - TDD 纪律核查通过：三者均 `test:` 提交先于 `feat:`/`fix:` 实现，且 test 提交仅含测试代码；#30 尾部另有一笔仅测试的 `test:`（f47ee86，S3 断言时序适配，合规）。
  - 红端复跑：#29 test 提交 1041d41 上 T67 **1 failed | 14 passed**（失败形态与缺陷一致：高亮锁旧店"主食"）；#28 test 提交 22ba3e0、#30 test 提交 93cec6c 均因实现模块不存在而红（no tests）。
  - 绿端复跑：#28 分支头全量 **21 files 全绿 + build（含 type-check）通过**；#30 试合入最新 main 后多次 **22 files 全绿**；#29 分支头全量绿（首轮 1 failed 经查为下述存量 flake）。
  - 代码走查：#29 归属标记+双守卫改动最小、保留同店秒开；#28 路由公开页/校验同口径/防重复提交/成功不建会话/失败保留输入均符合 PRD 7.16.1；#30 滚动管线（navHistory 判向 + 记录/恢复 + KeepAlive include HomeView + overflow-anchor:none）含真浏览器取证注释，watch 钳制防护完整。
- **合并后 main（97aa83b）终验**：全量 **22 files 全绿** + build 通过；开放 PR 清零。
- **存量测试时序敏感债登记（非本次引入，未修）**：`StoreDetailView.spec.ts` 的 T13/T18 在全量并发负载下偶发失败（T18 购物车角标 waitFor 2s 超时；T13 第 99 行高亮断言未包 waitFor）。对照实验定性：**在含 #29、不含 #30 的 main（e8f8e71）上同样复现**（T18 与 T13 各复现一次），与本次三 PR 合并无因果；单文件运行始终通过。建议后续 test 批次为 T13 断言补 waitFor、T18 评估放宽等待条件（对齐 #30 的 f47ee86 做法）。
- 三分支各自的 http.spec meta title 单行修复内容相同，随首个合并自动消化，后两笔合并无冲突（router/index.ts 的 #28/#30 交叉改动经本地试合并验证可自动合并）。

## B. AI 使用点

1. **组长委托的评审合并流水线由 AI 代理执行**：逐 PR 完成"提交序列纪律核查 → test 提交红端复跑 → 分支头/合并树绿端复跑 → diff 走查 → GitHub API 合并"，全程留证据于本片段。
2. **对照实验排除法**：全量回归出现偶发失败时，AI 用"分支头复跑 → 纯 main 对照 → 试合并树复跑"三轮对照把 flake 定性为存量时序敏感（而非新引入回归），避免误判打回或带病合并。
3. **合并操作**经 GitHub REST API（token 走 git credential fill）完成，无 gh CLI；JSON 正文避开中文以规避 Git Bash 编码问题。
