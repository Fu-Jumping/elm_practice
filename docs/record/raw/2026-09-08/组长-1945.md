# 2026-09-08 组长留痕（外观回改收尾 + 提交合入 main）

## A. 进度事实

- **按负责人「把剩下的小问题顺手改掉，直接提交、直接合主分支」的指令完成收尾**：分支 `fix/design-fidelity-full-bleed` 已 `--no-ff` 合入 main 并推送（`60b8e01`），工作区干净。
- **本轮补改的残留小问题**（4 个并行子代理分文件完成）：
  - 商家详情：顶部栏 44→56px、购物车栏 56→64px、店名 20px、分区标题 18px、商品名 16px、价格 18px/700、分类栏 14px、商品图圆角 8px→2px（`--radius-sm`）；**同步 §5 算式**：`.store-tabs { top: 56px }`、`.order-area`/`.review-placeholder` 高度 `calc(100dvh - 100px)`（56 顶栏 + 44 Tab）、左右栏底部留白 64px。
  - 首页：搜索框高 36px/边框 1.5px、商家卡优惠标签高 18px、分类宫格 5 列等距（列心 47/121.5/196/270.5/345，修掉文案撑宽导致的漂移）。
  - 地址页：徽标圆角 999px→2px；补两个固定底栏（地址列表「新增地址」h85、新增地址「保存」h77），底部留白 101px/93px。
  - 我的页：版本号 10px/`#999`；退出登录与未登录引导块改通栏白条（与功能分组同族）。
- **全量复验（本轮改完后）**：通栏结构断言 **23/23 通过**；§5 滚动交接双视口通过（Tab 吸顶 `top=56`、外层不越线、内层滚动与高亮正常）；主链路巡检 **34/34**、无控制台错误；地址页固定底栏无遮挡（末行距底栏 620/307px）；单测与 build 见下。
- **提交与合入**（4 笔，遵循 test: 先红纪律）：
  1. `09e8002 test:` 失败测试（T13 改口径 + T68/T69/T70）——**红端取证**：仅该提交 + 旧实现复跑 `5 failed | 13 passed`（T13/T68/T69/T70 按设计失败，另一条为存量 flake）；
  2. `ea3ab9e fix:` catalogStore + StoreDetailView（§5 交接、闪屏、外观回改）；
  3. `758ecf5 fix:` 其余 7 个视图文件（通栏回改 + 尺寸/圆角/固定底栏）；
  4. `3c472f3 docs:` 项目规则 §5 执行口径、复刻经验 §2.7 配方、9/8 六篇 raw 留痕、9/7 raw 片段与中期汇报演示方案入库；
  5. `fdbfb2f` merge（`--no-ff`）→ `60b8e01 chore:` 存量用例超时稳定性（见下），已推送 origin/main。
- **存量 flake 处理**：T18（去结算）在全量并行时偶发超时。定位过程：等待窗口 2s→5s 后仍偶发，且出现「Test timed out in 5000ms」——**用例总时长超过了 vitest 默认 5s 单测超时**；已在 `vitest.config.ts` 增加 `testTimeout: 20000` 并保留 5s 等待窗口。**单独运行 T18 5/5 通过、全量复跑 2/3 轮全绿**，定性为「本机并行负载（后端 + 两个 dev server + 22 文件并行）导致的存量 flake」，非本轮改动引入。该轮全量出现过的另一条 T29 亦同源。

## B. AI 使用点

1. **红端纪律**：先只提交测试并 stash 实现，复跑取证红端（4 条按设计失败）后再提交实现，满足「test: 提交仅含测试、且在该提交上复跑必须红」。
2. **并行分文件改**：4 个并行子代理各改独立文件，统一约束「只改样式、不动 testid/事件/文案/逻辑」；其中商家详情子代理**纠正了组长给的算式**（Tab 栏实为 44px，故用 `-100px` 而非 `-112px`），并给出自查推导。
3. **回归矩阵**：每轮改动后固定跑「通栏断言 + §5 交接 + 主链路巡检 + 单测 + build」五件套，本轮另加「固定底栏遮挡」专项检查。
4. **flaky 定性**：通过「单独跑 5 次 + 全量跑 3 次」对照，把 T18/T29 定性为负载型存量 flake，而不是归咎于本轮改动或盲目改业务代码。
