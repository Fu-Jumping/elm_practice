# 2026-09-08 用户端留痕（验收日前准备批次一：后端更新对齐 + IAB 复核定性 + 401 用例补录）

## A 当日进度

- **中期验收确认顺延至 9/9**（老师实际通知口径），今日为验收前最后一个准备日。
- **拉取后端更新**（origin/main 26a6ed4 → 316e6a3）：内存 Repository 删除，全部迁移 MyBatis（14 Mapper）+ 服务器 MySQL 8.0.46（82.157.137.114:3306，开发/测试库隔离），Service 事务化 + 行锁扣库存；红绿对 c1c50f6（test 红）→ a63e5ee（feat 绿）；`mvn clean test` 20 全绿 + 全链路冒烟 + 重启持久化验证通过（后端同学留痕 后端-2103）。
- **演示口径差距（待决策）**：后端种子仅 m002 肯德基 5 商品（p101-p105），其余 4 店无商品；用户端 mock 为 5 店 15 商品。real 模式下进非肯德基店铺为空列表，明日验收演示需知晓（补种子或演示主走肯德基）。
- **P0 real 全链路自测阻塞**：本机起后端需 DB_PASSWORD（不入库，线下分发）；用户提供的两个候选密码均 `Access denied for user 'elm'@'202.113.185.20'`（网络通，纯凭据错误），已停止尝试，待后端同学给准确密码。
- **IAB 两现象复核定性（详情页点击无效 / 首页卡片进错店铺）**：判定为 ZCode 内置浏览器（IAB）自动化环境缺陷，非应用缺陷。证据链：① 点击"老王小店"卡片 URL 不变，随后点底部"订单"link（href=/orders）URL 短暂变 `/stores/m002` 又弹回 `/`——事件延迟错位派发，与 9/7 用户端-1356 记录的 IAB 故障模式一致；② 代码无进错店路径（HomeView.vue:343 每卡绑定各自 storeId → onOpenStore push store-detail）；③ jsdom T60-T62 全绿；④ DOM 快照完整但视觉截图空白（渲染合成异常）。**终审方式：负责人用真实浏览器/真机点验**（dev server localhost:5173 mock 模式运行中）。
- **竞态修复静态确认在位**：catalogStore 三处 fetch 的 ERR_CANCELED 静默返回防护与 26a6ed4 一致；动态复核（快速切换店铺）归入真机清单。
- **401 公开页分支锁定用例补录（第三批任务完成）**：新增 `src/services/__tests__/http.spec.ts` 3 条——XA-06a 公开页 401 静默降级（不 toast 不跳登录）、XA-06b 受保护页 401 跳登录带 redirect、XA-06c 登录接口 401 仅 toast。**补录性质非红绿对**（实现 XA-06 于 9/7 联调先行，例外留痕 用户端-1356），预期直接绿；回归 **123 passed (20 files) 全绿**。提交 5801cb7 于特性分支 `feat/http-401-lock-tests-tdd`（未 push，待负责人指示）。
- **红绿序列演练取证（T64 批次）**：`10ac3ff`（test 红）复跑 **2 failed | 118 passed (120)**（T26 金额明细三行 + T65 滚动恢复断言失败）→ `af7d7a7`（feat 绿）复跑 **19 files 120 passed 全绿**。明日现场演示脚本：git log 看 test→feat 顺序 + 两端复跑。
- **留痕与杂项入库**：d228e04（9/7 用户端-1700 片段补录）、248017a（gitignore 忽略 .memorax-code 本地工具目录）。

## B AI 使用点

1. IAB 环境缺陷判定采用三角验证：交互对照实验（真链接点击产生错位导航，证明事件管线异常而非应用无响应）+ 源码证据（handler 绑定各自 storeId）+ jsdom 全绿；结论表述为"环境缺陷、真机终审"，不替负责人拍板。
2. 数据库密码仅试错两次（用户明示候选）即停，不暴力尝试；凭据只进进程环境变量，未落任何文件。
3. 提交纪律执行到位：每笔 commit 前先 `git branch --show-current` 校验（9/7 落 main 失误的预防措施首次落地，本批零失误）；测试用例提交走特性分支，与 docs/chore 分离。
4. 401 锁定用例的红绿纪律例外（补录直接绿）在测试文件头注释、提交信息、本片段三处如实留痕，供 PR 评审与次日汇总识别。
