# 组长 9/6 raw 片段（新机环境搭建 + 远端 PR #8/#9 评审 + TDD 规则补丁）

## 进度事实（A 表口径）

- **新开发机环境补齐**：git 全局身份此前为空，已配置 `user.name=Fu-Jumping` / `user.email=gc160514190803@163.com`；`frontend/user-h5` node_modules 缺失，已 `npm install`（399 包，npm registry 默认源在 sing-box 代理下速度可接受）。
- **网络诊断**：本机走 sing-box TUN（DNS 返回 198.18.x.x fake-ip），curl 到 github.com/api.github.com 正常；`git fetch` 首两次超时为偶发，第三次成功，暂无需额外配置。工具缺口备忘：`gh` CLI 未安装（本次用 GitHub API + 凭据管理器 token 替代）；JDK/Maven 未安装（后端尚无代码，暂不阻塞）。
- **远端 PR 评审（决定均未合并）**：
  - **PR #9（feat: 首页 exact 复刻静态层）→ 打回（CHANGES_REQUESTED）**。代码验收本身通过：分支尖端全量 vitest 实测 29/29 passed（4 文件）；无 daily/ 变更、raw 留痕齐全；素材提交（7dd0d81/5b13276/81fb433）实测未随 #7 进 main，retarget 到 main 合并后一并进入。打回唯一原因：**TDD 红绿在提交历史不成立**——`d63fe1a`（test: 先红）实测包含全部实现（TabBar/MainLayout/HomeView 共 +1095 行），在该提交上跑 HomeView.spec 为 4 passed 而非 4 failed；`b6fb523`（feat: 转绿）仅含 raw 记录文档（+35 行）。对照标准：#7 的 `f3058d4`（test 先红）实测 14 failed/6 passed，为真红。已在评审意见中给出修复方法：rebase 拆分，`d63fe1a` 只保留 `HomeView.spec.ts`（对 base 81fb433 的 42 行 Hello 占位页可复现 4 failed），实现三文件移入 `b6fb523`；force-push 后复验红绿，再把 base 改指 main 合并。
  - **PR #8（test: 首页 P0 行为测试）→ 关闭未合并**。head 是 main、base 是 `feat/user-h5-init`，实际为把 main 同步进已完全并入 main 的旧分支（47 文件全为 main 既有历史），标题所述 T1-T4 测试提交并不在其中（在 #9 分支）；文件清单另含 daily/ 变更（方向 main→分支、源自 main，不破坏 main，仅提醒后续避免）。已留说明评论，可重开。
- **TDD 规则补丁（负责人确认后落地，2026-09-06）**：根因分析认定原条款「`test:` 提交早于 `feat:` 提交」只约束顺序、未约束内容与可验证性，被 PR #9 字面满足；完成门槛「异常用例全绿」只查终点绿，同样检不出。补丁内容：
  1. `test:` 提交仅含测试代码与测试脚手架，业务实现一律进对应 `feat:` 提交；在 `test:` 提交上复跑测试必须失败（红），红绿两端证据（红 X failed / 绿 Y passed）写入 PR 描述或 raw/ 片段；
  2. PR 评审通用检查项新增「TDD 红绿真实性」抽查（`git checkout <test提交>` 复跑应红）；
  3. `AI协作:` 尾注口径统一为「AI 深度参与产生的提交必须追加」（`记录:` 尾注维持建议性），消除 AGENTS.md 两处「必须/可追加」打架；
  4. 拆分提交由人执行或交给 AI 时指令明确到文件级清单，push 前自检红绿。
  落点：`AGENTS.md`（TDD 纪律、Git 纪律）、`docs/project/TDD实施规划.md`（§2.2 标准循环与约束、§2.3 DoD 第 4 条、§3.1 规则、§3.3 自查表、§7 AI 边界）、`docs/project/小组协作与提交流程.md` §7、`docs/project/项目规则.md` §3 与 §5 台账；顺带修正 TDD实施规划 §2.2 ①「失败输出贴 daily A 表」的过时引用（与 9/5 记录机制修订冲突，改为 raw/ 片段）。
- 遗留：PR #9 作者按评审意见重排提交后需复审；规则补丁相关 4 文件改动待负责人确认后提交入库；`gh` CLI、JDK/Maven 待按需安装。

## 补充（同日 17:19 第二轮：PR #11 复验与合并）

- **作者回复落在 #8**（#9 已按 merged 关闭，无法重开）：已按打回方案重拆提交并 force-push（新 tip `c55ce39`），自测红绿通过，请复验后 retarget main 合并；备份分支 `backup/pre-rewrite-0542561` 暂留待复验。
- **重要发现**：作者 08:27 已把 #9 旧历史自行合并进 `feat/user-register-tdd` 侧分支（tip `945968f`），main 未受影响；重建历史范围同步扩大——新历史 6 提交两组红绿循环（T1-T4 首页静态层 + T5-T18 全局轻提示/商家卡数据驱动/商家详情页与购物车 mock），全量测试 29→43。
- **组长复验（AI 代跑，全部实测）**：两个 `test:` 提交 diff 均仅含测试代码（`2137d72` 仅 HomeView.spec.ts；`2220a43` 仅 3 个 spec 文件）；红① `2137d72` = 4 failed；红② `2220a43` 三文件全红（ToastHost/StoreDetailView 导入层红 + T7-T10 断言红）；绿 `682c973`→`c55ce39` 全量 **43/43 passed**（6 文件）+ `npm run build`（含 vue-tsc）通过；无 daily/ 变更；`记录:`/`AI协作:` 尾注齐全（符合当日新修规则）。
- **合并落地**：新开 **PR #11**（`feat/home-replica-tdd` → main，正文附红绿证据表，标注承 #9 评审脉络）并 merge 合并，main @ `57e4560`；素材提交（7dd0d81/5b13276/81fb433）一并进入 main。#8 已回复作者：复验通过、备份分支可删、**建议删除或重置被污染的 `feat/user-register-tdd`**（坏拆分旧历史在其上，防日后误合入 main）。
- 待办：作者清理两个分支；规则补丁 4 文件改动仍未提交入库；T5-T18 涉及的契约缺口清单（购物车/商品预览等 mock-only 字段）已由作者提交内标注，待与后端 A 对齐。

## AI 使用点（B 表口径）

| # | 任务 | 类别 | AI 做了什么 | 人工审核与修改 | 结果 |
| --- | --- | --- | --- | --- | --- |
| 1 | 新机环境搭建与诊断 | 环境配置 | 检查工具链/git 配置/网络/依赖；诊断 sing-box fake-ip 下的偶发连不通；配置 git 身份；安装前端依赖 | 是否补装 gh/JDK 由组长决定 | fetch 正常、测试可跑 |
| 2 | PR #8/#9 评审与测试验证 | 代码评审辅助 | 拉取 PR 变更与提交树分析；分支尖端全量测试（29/29 绿）；在 d63fe1a 复测证明"先红"不成立；对照复验 #7 红提交（14 failed 实证）；起草两条评审意见 | 合并/打回决策依据仓库 TDD 纪律由 AI 给出建议，组长本轮委托决定；评审意见已落 GitHub（#9 Request Changes、#8 关闭+评论） | 两 PR 均未合并；#9 待作者重排提交后复审 |
| 3 | TDD 规则补丁起草与落盘 | 文档初稿 | 根因分析（规则只限顺序未限内容/可验证性）；起草 4 条补丁条款并同步 4 份规则文档；发现并修正 TDD实施规划 §2.2 过时的 daily 引用 | 补丁方向与条款由 AI 提出、组长逐条确认后授权落盘；台账条目按 §5 格式追加 | 4 文件已改毕，待组长确认后随提交入库 |
| 4 | PR #11 复验与合并 | 代码评审辅助 | 拉取重建历史逐提交核验拆分与红绿（红① 4 failed / 红② 3 文件全红 / 绿 43 passed + build 通过）；起草新 PR 描述（含红绿证据表）与 #8 回复（含分支清理建议） | 合并动作依组长既定口径（测试通过即合并）执行；新历史范围扩大（T5-T18 详情页链路）由作者提交说明 | PR #11 已合并进 main（57e4560），#8 已回复，待作者清理两分支 |
