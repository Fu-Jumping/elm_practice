# 2026-09-09 组长留痕（第二阶段遗留 12 项拍板与回写，组长-2010）

## A. 进度事实

- **背景**：负责人对上一轮列出的 12 项遗留逐条拍板（**全部按建议执行**），本轮把结论回写文档与仓库配置。
- **拍板结论（12 项）**：① 订单状态机定稿——支付成功即 `PENDING`，`PROCESSING` 仅 P0 阶段；② 金额公式——`实付 = 小计 − 满减 − 新客 − 红包 − 会员折扣 + 配送费(默认 ¥3.00，店铺可配、未配置按 0) − 配送费优惠 + 打包费 ¥2.00` 且 ≥0，新增快照字段 `deliveryFee`，明细四行（配送费为 0 仍显示 ¥0.00）；③ 会员价与会员折扣不叠加；④ 优惠配置结构——`promotions` 扩列 `new_user_amount`/`free_delivery_threshold`/`member_discount` + 新表 `promotion_tiers`，旧单档数据重置种子 + 迁移脚本；⑤ P1 前端实现先走 PR 评审合并分支再开扩展；⑥ 图片上传参数——商品图 1 张/评价图 3 张、单张 ≤2MB、jpg/jpeg/png/webp、`backend/uploads/` + `/uploads/**`、不接对象存储、越权 403；⑦ 搜索——距离取种子 `distanceKm`、综合=销量优先评分次之、`page`/`size` 默认 1/10；⑧ 商家端导航九项平铺、评价管理与回复合并一页；⑨ 订单跟踪复刻真源 `index.jsx`、取消确认与配送信息页按设计系统新建；⑩ 缺陷台账 001=P1、002=P3 且 002 进变更窗口（正式生效待测试联调同学确认）；⑪ 老师变更未发布、本组决议不替代；⑫ 分批顺序 9 批（优惠→取消配送→评价→消息→搜索→收藏会员红包→规格→图片→统计），每批四类证据收口。
- **回写落盘**：`docs/project/项目规则.md` §5 新增第二条 2026-09-09 决议行；`docs/PRD.md` 补四行金额明细/配送费为 0 仍显示、图片与搜索参数、九项导航与评价管理合并、订单跟踪真源、§9 批次顺序、商家端页面数 7→6 页（合并评价回复）；`docs/backend/后端接口契约.md` §10.4 六条由"待确认"改为"决议（已确认）"并回写 §3.2/3.4/3.5/3.6/3.8/3.9/4.2/5.1/6.2/6.3/9/10.1（553→559 行）；`docs/project/TDD实施规划.md` 补状态机/金额/会员/搜索/图片口径 + §8 新增 9 行批次表 + §4.3 商家端新页面轻量覆盖；`docs/project/分工与里程碑安排.md` M5 改为 9 批次；`docs/project/运行说明.md` 新增库变更/上传配置/联调核对三节；`docs/design/设计系统-商家端.md` 与 `docs/frontend/前端架构设计与工程约定.md` 同步九项导航与合并页；`docs/testing/缺陷跟踪表.md` 补负责人认可备注（未改严重程度/状态）；`.gitignore` 新增 `backend/uploads/`。
- **主线收口冲突（子代理并行产生）**：统一分页参数为请求与响应均 `page`/`size`（`接口级联调用例` 2 处、`后端联调验收用例` 1 处、契约 §1.3/§3.6 响应示例与错误码）；商家端金额汇总由三行改四行（PRD MS-03-06 行）；PRD 商家端扩展页数 7→6 页并注明"评价回复不单独占页"。
- **分支现状核查（A5 相关，本轮新发现）**：远端存在**两条未合并分支**——`origin/wjy-9.8-zhongqi-demo`（12 提交 / 83 文件 / +4491−352，含用户端 `PaymentActions.vue`、商家端 App.tsx 868 行、`deploy/**`、`docs/presentation/**`）与 `origin/codex/merchant-p0-closeout`（33 提交 / 18 文件 / +2348−243，含商家端组件化、5 个商家端测试、SHOW-QA-003 修复链、`docs/testing/回归记录-2026-09-08.md`）；两者共同基线为 main（508889e），彼此各有独有提交（12 vs 33），重叠文件为 `App.tsx`/`App.css`/`services/merchantApi.ts`/`tests/App.test.tsx`/`tests/merchant-features.test.tsx`；codex 的 App.tsx 883 行、测试更全，建议以 codex 为商家端基线，再从 demo 摘用户端支付、后端店铺状态与种子、deploy 与演示材料。`deploy/release.py` 的 DB 凭据取自环境变量、`nginx.conf` 无凭据，未发现硬编码密钥。
- **验证**：契约 §10.4 已无"待确认"项（仅保留"实现完成后回写"提示）；全库无残留 `pageSize` 参数；`git status` 32 项（27 个已跟踪文件改动 +1301→1360 行 + 未跟踪 raw 片段），**未改动 `docs/record/daily/**`，未执行任何 git 写操作**。

## B. AI 使用点

1. **决策→回写闭环**：AI 先把 12 项拍板结论整理成统一口径分发给 3 个并行子代理（契约 / TDD+里程碑 / 运行说明+设计+前端+缺陷表），主线保留 PRD 与项目规则写权。
2. **冲突收敛**：子代理并行产生 9 处新冲突（商家端金额行数、分页命名、图片目录不一致、PRD 页面矩阵并列评价回复、`.gitignore` 缺规则、配送费为 0 的展示等），主线逐条裁决并回写，未让任一文档保留两套说法。
3. **分支考古**：AI 用 `git merge-base`/`rev-list --left-right`/`diff --name-status` 还原两条未合并分支的关系与重叠文件，并扫描 `deploy/**` 确认无硬编码凭据，把"该先合哪条分支"变成可核对的事实。
4. **机器可验证收口**：grep 复查 `pageSize`/待确认残留、`git status` 核对改动面与"未动 daily/未执行 git"。
5. **人负责判断**：12 项范围与数值均由负责人拍板；AI 未替负责人决定范围，缺陷台账严重程度与状态仍保留"待测试联调同学确认"。
