# 2026-09-09 组长留痕（结课汇报 · AI 前端 SOP 第二轮：证据体系与规划）

## A. 进度事实

- **任务**：承接第一轮现状调研与负责人四项决策（测试口径按第二阶段截止时定稿、商家端等落地、可补 320px 截图与像素比对脚本、数字差异可一定程度说明、中期汇报材料不再维护），做"进一步研究与规划"。
- **方法**：**5 个并行只读子代理**（R1 测试口径与冻结清单 / R2 保真度工具链 / R3 变更链路 / R4 评分卡与效率口径 / R5 逐页证据卡）+ 组长线自行做**像素比对可行性实测**（Playwright + 本机 Chrome + Python/Pillow，零新增依赖）。
- **产出**：`docs/_archive/2026-09-09-结课汇报-AI前端SOP-第二轮研究与规划.md`（12 节）；子代理报告 `tmp/结课汇报SOP调研/第二轮/R1…R5.md`（约 160KB）；实测脚本与产物 `tmp/结课汇报SOP调研/第二轮/原型-截图比对/`（shot.cjs / probe-geometry.cjs / pixel_diff.py / align_analysis.py / band_analysis.py + 截图与 diff 图）。
- **本轮 9 处口径修正（均已核实）**：① screen.png 实测 **15 种**尺寸（非 12）；② 导出根画布多为 **390px**，"393 vs 390 的 0.77% 缩放"只对首页/启动页精细版成立；③ 320px 截图**9/8 已用 Playwright 拍过**（raw/2026-09-08/用户端-1332.md:12），只是没进自动化；④ 外观回改"3h20m"实为下午窗口含 ≥4 批次，**回改本身 ≈81 分钟**（18:16→19:37 合入）；⑤ 测试数 **145** 为准（`^\s*it(` 计数；152 是 `mountEdit(` 等子串误命中）；⑥ 商家详情两个导出目录 `code.html` 与 `jsx/scss` **内容互换**（店名 20/18、Tab 32×2/32×3 的根因是上游资产不自洽，非实现乱改）；⑦ 结构断言 23/23 与巡检 34/34 的脚本 **未入库且已不在磁盘**；⑧ 首页差异排查 3 条尺寸问题中 2 条已回改、1 条为测量误读；⑨ **"183（148/10/25）"全部来自未合并分支** `origin/wjy-9.8-zhongqi-demo`，`main` 实测 **172 + 3 E2E**（用户端 145 / 商家端 4 / 后端 23）。
- **像素比对实测结论（本轮自行实验）**：首页 393×852@2x 截图与精细版基线 786×1704 尺寸完全一致；全图像素差 **30.147%**（超阈 30）→ 校正设计稿 53px 状态栏偏移后 **19.063%** → 最佳整体位移 dx=dy=0（布局对齐是准的）→ 分带看：分类宫格 7.2% / 定位栏 11.7% / 活动区 12.7% / 搜索区 15.0% / 筛选标签 20.9% / **商家卡（数据驱动）33.3%**。结论：像素 diff 必须做偏移校正 + 数据区掩膜，**不能当主门禁**；主门禁继续用几何/色值/结构断言，像素 diff 只做一次性取证与 before/after 素材。
- **规划结论**：推荐"**C 几何/色值清单做门禁 + A Playwright 快照做回归护栏 + B 设计稿像素 diff 做一次性取证**"（三者新增依赖均 0）；评分卡 7 维 100 分（首页示例 ≈93）；执行清单 P0（320 多视口断言 + 测试冻结命令集，≈2.5 人日）→ P1（静态值域/色值脚本固化 + 结构断言脚本重建，≈1.5 人日）→ P2（像素取证 + 评分卡填表）→ P3（快照基线入库，需拍板）。
- **本轮新增待拍板 5 项**：冻结基线是否先合并 wjy 分支、商家端以 wjy(10) 还是 codex(15) 为准、后端 DB 凭据注入、50 项接口检查是否进冻结口径、回归记录由谁新建。
- **未做**：未出正式讲稿/大纲；未改动任何入库文档（只写 docs/_archive/ 与 tmp/）；未做 git 写操作；未安装任何依赖。

## B. AI 使用点

| # | 任务 | 类别 | AI 做了什么 | 人工审核与修改 | 结果 |
| --- | --- | --- | --- | --- | --- |
| 1 | 五路并行二轮调研 | 其他 | 按五个自足提示词派只读子代理（测试口径/工具链/变更链路/评分卡/逐页证据卡），各自落报告到 tmp/ 并回 ≤400 字摘要 | 调研范围与产出格式由组长线指定 | 5 份报告（约 160KB） |
| 2 | 像素比对可行性实测 | 其他 | 自行写 Playwright 截图脚本 + Python/Pillow diff/偏移分析/分带分析脚本并**实跑**，得出 30.1%→19.1%→10.6%（静态外框）/33.3%（数据区）的量化结论 | 结论取舍由组长线判断；是否进汇报待拍板 | 脚本 + 截图 + diff 图落 tmp/ |
| 3 | 数字交叉校验 | 文档评审 | 复跑 R4 脚本验证 78.0%/96.1%/48.6%；实测验证"商家详情导出目录内容互换"；git 实测 wjy 分支 12 提交领先、23 vs 22 spec、7 vs 6 后端测试类 | 以实测为准，9 处口径写进规划文档 §1.1 | 口径全部可复跑 |
| 4 | 证据体系与内容结构规划 | 文档初稿 | 成文 12 节：评分卡、工具链方案、测试冻结清单、变更闭环、逐页证据卡、汇报内容结构（论点→证据→展示动作→追问应答）、执行清单、风险、待拍板 | 是否采用、档位选择待负责人拍板 | `docs/_archive/2026-09-09-结课汇报-AI前端SOP-第二轮研究与规划.md` |
| 5 | 诚实边界与禁用表述 | 文档初评 | 给出三个"别讲"（像素级一致/和饿了么一样/23-23 可复跑）与三个"必讲" | 待负责人确认 | 文档 §8.3 |
