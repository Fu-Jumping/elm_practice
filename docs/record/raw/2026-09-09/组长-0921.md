# 2026-09-09 组长留痕（分支合并、基线与四个 PR 落地，组长-0921）

## A. 进度事实

- **前置**：负责人授权"全做"——提交本轮文档、合并分支、push、开 PR；并确认**商家端以 codex 分支为基线**、**演示页进 main 但讲稿不进**。
- **main 提交 5 个**（4af3077 / 9f4bb30 / bcdd904 / 14d6017 / 7e08ea6）：第二阶段范围决议落盘与 PRD v1.1、SRS 两版与后端 6 件、测试与前端设计文档、项目入口与运行说明、9/8 与 9/9 raw 留痕补录；已 push（508889e..7e08ea6）。
- **分支考古结论**：远端 33 条分支中 3 条未合并——`wjy-9.8-zhongqi-demo`（+12 / 83 文件 / +4491−352）、`codex/merchant-p0-closeout`（+33 / 18 文件 / +2348−243）、`integration/verify-p0-fixes`（+4 / −34，携带 `docs/record/daily/**` 变更，判定不合并）；两条主分支共同基线为 main 且互不包含，重叠 6 个文件（App.tsx/App.css/merchantApi.ts/两个测试/缺陷跟踪表）。
- **四个 PR（串联堆叠，全部 MERGEABLE）**：
  - **#37** `merge/codex-merchant-p0-closeout` → main：商家端基线（App.tsx 883 行、5 测试、SHOW-QA-003 链、12 条 BUG 台账）+ 演示分支 `real-contract.test.ts`
  - **#38** `merge/wjy-user-payment` → #37：用户端支付动作与订单状态归一化（`test:` 30831ab 红 → `feat:` 5d0c63b 绿）
  - **#39** `merge/wjy-backend-store-status` → #38：后端营业状态写库与演示目录种子对齐
  - **#40** `merge/wjy-deploy-demo-materials` → #39：deploy/** 与演示页材料（**已排除 `答辩手册.html`**）
- **合并期冲突与修复**：契约保留 9/9 版并吸收 codex 的 sortOrder 一行；缺陷台账合并为 15 条；用户端 `vitest.config.ts` 适配 codex 的函数式 `vite.config.ts`（红：`Cannot merge config in form of callback`、0 用例执行 → 绿：23 文件 / 148 用例全绿）。
- **验证证据**：商家端 `npm test` 5 文件 / 15 用例全绿、`real-contract` 5/5 绿；用户端 `npx vitest run` 148/148 绿；后端 `mvn -DskipTests package` 通过，`mvn test` 因本机 127.0.0.1:3306 无 MySQL（`Communications link failure`）无法执行，已在 #39 描述中标注需在具备数据库的环境复跑。
- **历史 PR 交接**：#36（KikiDaydream，9/8 开，同 head 分支）已被 #37 取代，已在 #36 留言说明差异与本 PR 评审清单的处理情况；其中 ①店铺联系电话字段 ②测试文档工作说明位置 ③12 条缺陷的问题记录明细 ④raw 片段计数 四项仍未处理。
- **网络**：执行期间 `github.com:443` 一度不可达（HTTPS push 失败）、SSH 缺公钥；负责人补登记公钥并开启代理后，HTTPS 与 SSH 双通道恢复，推送与 `gh pr create` 均成功。

## B. AI 使用点

1. **分支考古与合并方案**：AI 用 `merge-base`/`rev-list --left-right`/`diff --name-status` 还原两条未合并分支的关系、重叠文件与凭据风险，产出 PR 拆分方案（含"以 codex 为商家端基线"的依据：883 行 + 5 个测试）。
2. **TDD 拆分与红绿取证**：摘取组员分支内容时按仓库纪律拆成 `test:` → `feat:`；用 `git stash --keep-index` 还原"仅测试提交"状态复跑取得红端证据（1 file failed / no tests），再提交实现取得绿端（148 全绿）。
3. **冲突收敛**：并行合并产生的 3 处冲突（契约、缺陷台账、vite 配置）按"以 9/9 决议文档为准 + 吸收分支事实性补充"逐条解决，并修复了 codex 配置改动导致的用户端测试无法启动。
4. **环境限制的诚实标注**：本机无 MySQL 导致后端测试无法复跑，AI 未把"构建通过"写成"测试通过"，而是在 PR 描述与留痕中明确标注红端证据来源与环境前提。
5. **人负责判断**：范围基线（codex 为准）、讲稿是否入库、PR 是否合入均由负责人决定；#36 是否关闭留给组长确认，AI 只留言交接。
