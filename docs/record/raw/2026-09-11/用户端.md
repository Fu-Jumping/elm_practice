# 2026-09-11 用户端留痕

> 命名口径：按 `docs/todo/README.md` §3 第 5 条与当日 `组长.md` 的既有做法，同日同角色只写本文件，后续轮次在末尾追加小节。

## 15:45 （本轮：TODO-USER-104 订单详情页（含订单跟踪时间线）TDD 落地）

### A. 进度事实

- **任务来源**：第二阶段开工后按 `docs/todo/用户端.md` 领批次⑩ `TODO-USER-104`；开工前回读出处置信源——PRD 7.6（时间线文案 ↔ 状态值映射表）+ PRD 7.16.1 订单详情页四行、契约 §3.5/§5.1、TC-ORD 组、CHG-003/CHG-004、设计系统-用户端「订单详情页」行，以及设计真源 `docs/design/exports/用户端/12-订单与支付/01-订单详情-含订单跟踪/`（index.jsx + index.module.scss + screen.png 逐件读完）。
- **负责人确认三项边界口径**：① 状态头「预计送达」**本地推算**（契约 §3.5 无 ETA 字段，按 `createdAt + 40 分钟`，代码注明课程演示口径）；② 「联系商家 / 去评价 / 再来一单」**渲染按钮 + toast 占位**（对应批次④/批次③/TODO-USER-008 完成后接真行为）；③ 取消确认弹层**归 TODO-USER-002 独立一批**，本批只做取消按钮的状态逻辑与置灰提示。
- **分支**：`feat/order-detail-timeline-tdd`（本地创建，**未提交、未推送**，提交时机待负责人决定）。
- **TDD 红端证据**：`npx vitest run src/services/__tests__/normalizers.spec.ts src/views/user/__tests__/OrderDetailView.spec.ts` → **15 failed | 16 passed**（红：OD-N1~N4 归一化/文案组 + OD-1~11 页面组；既有 T43/T44 等 16 条保持通过，脚手架零回归）。
- **TDD 绿端证据**：同命令 → **31 passed (31)**；全量 `npx vitest run` → **163 passed (163)**（原 148 + 新增 15，零回归）；`npm run build`（并联 vue-tsc 类型检查）→ **✓ built**。
- **实现内容**：
  - `src/services/normalizers.ts`：新增 `buildAmountLines`（CHG-004 定稿口径唯一出口：基础四行恒显示含配送费 0 显 ¥0.00 + 优惠项金额非 0 各占一行品牌橙负数 + 顺序固定）、`buildDiscounts`（满减→红包→新客→会员→配送费优惠，非 0 才出行）；`normalizeOrderDetail` 扩展透传 `deliveryFee/discounts/cancelReason/cancelledAt`；`statusText` 补 `CANCELLED → 已取消`。
  - `src/views/user/OrderDetailView.vue`：按真源重写——暖橙渐变状态横幅（状态头大字 + 餐具图标 + 预计送达胶囊 + 取消原因行）、**五节点时间线**（已下单/已支付/商家接单/配送中/已完成；已完成节点橙实心带勾、当前节点白底橙描边 + 光环、未达节点灰；已取消整体置灰）、商家与商品卡（店名可进店 + 「外卖自取/送达」纯展示标签 + 商品快照 + 金额明细 + 共 N 件商品与实付）、配送信息卡、订单信息卡（订单号 + 复制 + 下单时间 + 支付方式）、状态驱动底部操作区（取消按钮待支付/待接单可用、接单后置灰且点击提示「商家已接单，无法取消」；已完成给去评价/再来一单）。
  - `src/components/PaymentActions.vue`：新增 `variant` 支持（`inline` 默认形态不变以保住既有用例；`footer` 为主按钮品牌橙渐变形态，待支付时作为底部操作区主按钮，label 按 PRD 为「去支付」且保留 `pay-order` testid 与演示失败入口）。
  - `src/services/api/types.ts`：按契约 §3.5/§10.4 定稿字段名补 `deliveryFee` 与优惠各字段、`payDeadline`、`cancelReason/cancelledAt/cancelledBy`，`OrderStatus` 补 `CANCELLED`，新增 `OrderDiscountItem/OrderAmountLine` 视图模型。
- **按 PRD/复刻约定做的口径处理（提请复核）**：① 设计稿底部「**查看配送进度**」按钮**不实现**（PRD 7.16.1 明示删除）；② 「蜂鸟专送」「微信支付」替换为课程口径「**课程演示配送**」「**模拟支付**」；③ 卡片用 **8px 圆角 + 1px 边线**（依设计系统-用户端「订单详情页」行；导出 SCSS 未带 radius，属导出缺省）；④ **备注（remark）未在本页渲染**（设计稿与本页字段口径均未含，PRD 7.4 的备注展示要求落在确认订单页）；⑤ 置灰取消按钮用 `aria-disabled` + 灰态样式而非原生 `disabled`——原生 disabled 会吞掉点击，与「保留但置灰、点击提示」冲突。
- **定位到一处环境问题（非代码）**：本机 `node_modules` 缺 `@playwright/test`，导致 `npm run build` 并联的 type-check 报 `playwright.config.ts(1,30) TS2307`；`npm install` 补装 3 个包后 build 通过。该现象与本次改动无关，但会拦住本地 R1，已在此记录供其他同学参考。
- **待办更新**：`docs/todo/用户端.md` `TODO-USER-104` 证据列已补分支名与红绿/R1 结论；**状态保持「进行中」**（未 push/未合并、复刻对照截图待补，按待办规则未收口不得写已完成）。顺带覆盖了 `TODO-USER-009` 要求的 `CANCELLED → 已取消` 文案映射，但未改该行状态（待其回归全量订单状态展示后独立收口）。
- **本轮更新的 TODO 编号**：`TODO-USER-104`（证据：分支 + 红 15 failed/16 passed → 绿 31 passed + 全量 163 passed + build ✓）。

### B. AI 使用点

| # | 任务 | 类别 | AI 做了什么 | 人工审核与修改 | 结果 |
| --- | --- | --- | --- | --- | --- |
| 1 | 出处回读与实现清单 | 需求理解 | 读 PRD 7.6/7.16.1、契约 §3.5/§5.1、TC-ORD、CHG-003/004 与设计真源四件套，产出七区块实现清单与 12 条测试场景清单，并列出契约未定死的三处口径请负责人拍板 | 负责人拍板三项口径（ETA 本地推算 / 占位 toast / 弹层归 002） | 清单 1 份、口径 3 项 |
| 2 | 失败测试先行 | TDD | 按确认的场景清单写 OD-N1~N4（归一化与文案）与 OD-1~11（页面行为）用例，含状态参数化、金额明细顺序与边界（配送费 0 恒显示、优惠未发生不渲染）、禁词断言；复跑取得红端证据 | 场景清单由负责人确认 | 15 failed / 16 passed |
| 3 | exact 复刻实现 | 前端实现 | 逐值对照导出 SCSS 落地尺寸/色值/圆角/渐变（时间线节点三态、横幅渐变、卡片边线、底部浮动栏），金额明细走 normalizers 唯一出口，按钮可用性由状态机驱动 | 无 | 31 passed、build ✓ |
| 4 | 环境问题定位 | 排障 | build 的 type-check 失败时区分「本次改动」与「本机依赖缺失」，确认 `@playwright/test` 已在 package.json/lock 中而 node_modules 未装，补装后通过 | 无 | R1 可跑 |

## 15:56 （本轮：TODO-USER-105a 支付页 + 三处「去支付」入口接线 + mock 支付接口）

### A. 进度事实

- **任务与拆分**：按负责人决定把 `TODO-USER-105` 拆成 **105a（支付页 + 入口接线 + mock 支付接口）** 与 105b（支付成功页 + 失败页）；本分支 `feat/order-pay-chg003-tdd` **基于 104 分支叠加**（需改 104 落地的详情页底部入口），建议 104 先合、105 后合。
- **负责人拍板四项口径**：① 三页一起做（支付页 + 成功页 + 失败页）；② 创建订单成功后按 PRD 7.5 **改为进入支付页**（并同步修改 ConfirmOrderView 既有用例断言）；③ 支付页顶部应用标题用「**轻量外卖**」（设计稿英文 CampusBites 与「不出现英文界面」口径冲突，不作真源）；④ 详情页与列表页**两处「去支付」入口都接**。
- **TDD 红端**：`b3605c9`（test:）→ 6 个测试文件复跑 **17 failed | 45 passed**（TD-1~6、TD-8、TD-9、TD-10、TD-11、TD-12a~c、PaymentActions pay-page 三例）。
- **TDD 绿端**：实现后 **62 项中 61 通过**，其中 TD-3 首轮失败并暴露真实缺陷——`remainingSeconds(null)` 返回 0 被页面误判为「已失效」（应区分「payDeadline 未返回」与「已归零」）；修正为按 `payDeadline` 是否存在决定 `remain` 是否为 null 后全绿。
- **R1 矩阵**：全量 `npx vitest run` = **179 passed (179)**（104 后的 163 + 新增 16，零回归）；`npm run build`（含 vue-tsc）= **✓ built**。
- **实现内容**：
  - `OrderPayView.vue`（新）：顶部「← 轻量外卖」、倒计时卡（仅「支付剩余时间」+ 橙色大字 mm:ss，`payDeadline` 驱动；归零显示「已失效」并禁用支付；缺失显示 `--:--` 且禁用）、订单详情卡（标题行右侧「取消订单」文字入口 + 商品清单 + 金额明细复用 `buildAmountLines` + 商家/订单编号(复制)/下单时间/收货地址）、底部通栏「立即支付 ¥实付金额」；非待支付订单进入时直接转支付成功页（PRD：已支付订单再次进入不得重复扣款）。
  - `normalizers.ts`：实现 `remainingSeconds`（缺失/非法/过期一律 0）与 `formatCountdown`（mm:ss、负数按 0、分钟位不截断）；`normalizeOrderDetail` 透传 `payDeadline`。
  - `mocks/order.ts`：新增 `POST /orders/:orderId/payment`（成功 → `PENDING` + `paidAt`；失败 → 保持 `PENDING_PAYMENT`；超时 → `409`；非待支付幂等返回）；**创建订单对齐契约 §3.5 定稿**（`PENDING_PAYMENT` + `payDeadline` = 创建时间 + 15 分钟），使「下单 → 支付页 → 模拟支付」链路在 mock 下可完整走通（既有两个 mock 用例不校验创建后状态，已核对无回归）。
  - `PaymentActions.vue`：新增 `pay-page` 变体（「立即支付 ¥实付金额」+ 低权重「演示支付失败」文字入口）、`amountText`/`disabled` 属性与 `failed` 事件；`inline` 默认形态保持不变以保住 TODO-USER-101 的既有用例。
  - **入口接线三处**：确认订单页下单成功 → 支付页；订单详情页底部「去支付」→ 支付页（移除本页内联支付动作）；订单列表页待支付卡片「去支付」→ 支付页。
  - `router`：新增 `order-pay` / `pay-success` / `pay-fail` 三条路由；成功页与失败页本批为**可跳转占位骨架**（避免「立即支付」后死链），真稿随 105b。
- **口径偏差（提请复核）**：设计稿支付页用深橙 `#ae3200` 作倒计时与实付文字色，但 `设计系统-用户端`/`UI复刻经验与执行约定` 明示「令牌 `primary`（`#ae3200`）是深色锚点，**不直接用作界面主色**」→ 本批按文档口径统一用品牌橙 `#ff5a1f`；「立即支付」按钮按设计稿为品牌橙实心（非渐变），已照稿。
- **未收口**：① 105b（成功页/失败页真稿）未做；② 取消确认弹层仍归 `TODO-USER-002`（支付页内为占位提示）；③ 复刻对照截图未补。
- **本轮更新的 TODO 编号**：`TODO-USER-105`（证据补 105a：分支、红 17 failed/45 passed → 绿 61/62 全量 179 passed、build ✓）。

### B. AI 使用点

| # | 任务 | 类别 | AI 做了什么 | 人工审核与修改 | 结果 |
| --- | --- | --- | --- | --- | --- |
| 1 | 出处回读与清单 | 需求理解 | 读 PRD 7.5/7.16.1 支付三页、契约 §3.5、设计真源三套导出件与截图，产出三页实现清单 + 12 条测试场景，并就范围/跳转/标题/入口四处口径请负责人拍板 | 负责人拍板四项 | 清单 1 份 |
| 2 | 失败测试先行 | TDD | 新增 105a 的 TD 组（支付页 8 例 + 倒计时 3 例 + 组件 3 例 + 入口 2 例 + 改下单跳转断言），取得红端 17 failed/45 passed | 场景经负责人确认 | 红端已留证 |
| 3 | 实现与缺陷修正 | 前端实现 | 按导出 SCSS 逐值复刻支付页；发现并修正「缺失 payDeadline 被误判为已失效」的真实缺陷；把「去支付」从详情页内联动作改为跳转支付页并保住既有组件用例 | 无 | 全量 179 passed、build ✓ |
| 4 | mock 口径对齐 | 测试替身 | 新增支付接口替身并对齐契约创建状态（`PENDING_PAYMENT` + `payDeadline`），先核对既有 mock 断言确认不回归再改 | 无 | 链路可走通 |

## 16:01 （本轮：TODO-USER-105b 支付成功页与支付失败页真稿）

### A. 进度事实

- **任务**：105b = 把 105a 遗留的两个可跳转占位骨架换成设计真稿（真源 `07-支付/03-支付成功`、`04-支付失败`；口径 PRD 7.5 + 7.16.1 六行）。
- **TDD 红端**：`90acb91`（test:，仅两个用例文件）→ **11 failed | 0 passed**。绿端：**11 passed**；全量 `npx vitest run` = **190 passed (190)**（105a 后 179 + 新增 11，零回归）；`npm run build`（含 vue-tsc）= **✓ built**。
- **实现**：
  - `PaySuccessView`：成功横幅（64px 品牌橙圆角徽标 + 「支付成功」+ 金额来自接口）、订单摘要卡（订单编号 / **模拟支付** / 支付时间 `paidAt`）、会员提示（**固定课程文案**「会员红包已同步 / 下次下单可使用」，**纯展示无按钮**）、底部「查看订单」+「回到首页」；金额缺失显示「暂无金额」、时间缺失显示「暂无时间」、**状态未知（仍待支付）显示「支付结果待确认」而不显示成功**、接口失败保留结果并提供「重新查询」。
  - `PayFailView`：顶部栏（返回订单详情 + 「支付失败」）、语义红浅底徽标 + 失败原因（路由 `reason`，缺失显示通用提示「支付超时或余额不足，请尝试重新支付」）、订单卡（待支付金额 + 订单号）、「重新支付」（**先查订单状态**：仍待支付→回支付页；已成功→进成功结果；查询失败给重试提示）+「返回订单列表」。
  - `normalizers`/`types` 补 `paidAt` 透传（摘要卡支付时间数据源；首轮 TB-1 正是因此失败被暴露）。
- **课程口径替换（依 PRD 明示条款，均已提请复核）**：① 「获得 10 积分 / 查看积分」**不实现**（PRD：本期不建积分体系、不展示积分余额或兑换入口）；② 「支付宝」→「**模拟支付**」；③ 「联系客服」**不实现**（PRD：本期不提供联系客服入口），次按钮位按 PRD 7.5 改为「**返回订单列表**」。
- **未收口**：复刻对照截图未补（三个支付相关页面统一待补）；取消确认弹层仍归 `TODO-USER-002`。
- **本轮更新的 TODO 编号**：`TODO-USER-105`（补 105b 证据：真稿两页 + 红 11 failed → 绿 11 passed / 全量 190 passed / build ✓）。

### B. AI 使用点

| # | 任务 | 类别 | AI 做了什么 | 人工审核与修改 | 结果 |
| --- | --- | --- | --- | --- | --- |
| 1 | 结果两页实现 | 前端实现 | 按两套导出 SCSS 逐值与 PRD 六行口径实现成功页/失败页，并把设计稿的积分、真实支付渠道、联系客服按 PRD 明示条款替换为课程口径 | 口径变更待负责人复核 | 11 passed |
| 2 | 缺陷与缺口暴露 | 自检 | 首轮 4 例失败定位为「测试在 mock 延迟返回前断言」（data-testid 挂外层）与「详情视图模型缺 paidAt」两类问题，分别用订单就绪后再渲染与补透传修正 | 无 | 全量 190 passed |
| 3 | 状态机接线 | 流程串联 | 失败页「重新支付」按 PRD 先查订单状态再决定跳支付页或成功结果，并处理订单已支付时不再停留失败页 | 无 | 链路闭环 |

## 16:14 （本轮：CI 提交规范打回与历史重写修复）

### A. 进度事实

- **现象**：PR #41（104 分支）CI 的「提交规范检查」任务失败。**本地复现**（`bash .github/scripts/commit-hygiene.sh origin/main`）：❌ `test:` 提交改动了非测试文件——104 的 `2e75a3e` 带了 `src/services/api/types.ts`（类型声明）与 `src/services/normalizers.ts`（抛错桩）；105 的 `b3605c9` 另带 `src/views/user/OrderPayView.vue`（骨架）。判定依据为该脚本的路径白名单（只放行 `__tests__/`、`*.spec.ts`、`vitest.config` 等）。**另有一类 ⚠️**：尾注写成了列表项 `- 记录: …`，脚本要求行首直接是 `记录:`（不致命但一并修）。
- **修复（负责人拍板方案 A：拆出 chore: 脚手架）**：两个分支各自重写为「`test:`（纯测试文件）→ `chore:`（脚手架：类型声明 / 抛错桩 / 页面骨架）→ `feat:`（业务实现）」；尾注改为行首形式。**代码内容零改动**，用 `git diff <旧提交> <新分支>` 校验为空（104 对 `89d237e`、105 对 `2a191a2`/`32fde89` 均空）。
- **新历史与红绿复跑（逐提交实测）**：
  - 104：`0510a6f`(test) 红 **15 failed | 16 passed** → `979e2f8`(chore) 仍红 15/16 → `5fc25c0`(feat) 绿 31 passed，全量 **163 passed**、build ✓
  - 105：`c4c16a6`(test) 红 **10 failed | 44 passed**（页面骨架未入库 → `OrderPayView.spec.ts` 文件级加载失败）→ `6daa4f0`(chore) 断言级红 **17 failed | 45 passed** → `4549381`(feat) 绿，全量 **179 passed** → `901ebdf`(test) 红 **11 failed | 0 passed** → `d1fbf18`(feat) 绿 11 passed，全量 **190 passed**、build ✓
- **规范检查复核（按 CI 的两种 base 各跑一次）**：`origin/main`（104 的 PR）8 个提交 ✅；`origin/feat/order-detail-timeline-tdd`（105 的 PR）5 个提交 ✅。
- **推送**：两分支 `--force-with-lease` 强推成功（104 `89d237e→5fc25c0`、105 `32fde89→d1fbf18`），PR #41/#42 自动更新。
- **本轮更新的 TODO 编号**：无（提交结构修复，任务状态与口径未变）。

### B. AI 使用点

| # | 任务 | 类别 | AI 做了什么 | 人工审核与修改 | 结果 |
| --- | --- | --- | --- | --- | --- |
| 1 | CI 失败定位 | 排障 | 读 `.github/workflows/ci.yml` 与 `commit-hygiene.sh`，本地跑脚本复现两种 ❌ 与一种 ⚠️，给出精确到文件与规则的结论 | 负责人询问触发 | 定位完成 |
| 2 | 历史重写修复 | 流程整改 | 按方案 A 逐提交重建两个分支（test: 仅测试文件 → chore: 脚手架 → feat: 实现），逐提交复跑红绿并校验与旧末态零差异，最后 `--force-with-lease` 推送 | 负责人拍板方案 A 并授权强推 | 9 个提交合规 |

## 16:20 （本轮：TODO-USER-002 取消订单确认弹层）

### A. 进度事实

- **任务**：`TODO-USER-002`（批次⑩ 取消）。设计稿与红框图此前已入库，本批把它从「占位提示」做成真弹层，并把支付页卡内入口与订单详情页底部按钮两处接上（PRD 7.6 的两处取消入口）。
- **分支**：`feat/cancel-order-sheet-tdd`（基于 `feat/order-pay-chg003-tdd` 叠加，因为要改 105 落地的支付页与 104 落地的详情页）。
- **提交结构（按 CI 规范校正后的新写法）**：`chore:`（组件骨架，先于测试，保证红端是断言级）→ `test:`（红 10 failed | 21 passed）→ `feat:`（绿）。
- **实现**：
  - 新增 `CancelOrderSheet.vue`：40% 黑遮罩 + 白色底部弹层（顶部拖拽条、8px 圆角）、标题「取消订单」+ 右上关闭、说明「订单取消后不可恢复」、`* 取消原因` + 「必填」、四个快捷原因 chips（点选回填文本域并高亮）、文本域（placeholder + `maxlength=50` + `0/50` 计数）、「再想想」（品牌橙主按钮，按设计稿口径以主次对比给取消加摩擦）与「确定取消」（白底描边次按钮）；原因为空时次按钮 `aria-disabled` 置灰、点击给必填提示（原生 disabled 会吞点击）；提交中按钮 `disabled` + 「处理中…」且重复点击只发一次请求。
  - `services/api/order.ts` 新增 `cancelOrder(orderId, reason)` + `endpoints.order.cancel`（`POST /orders/{orderId}/cancel`，契约 §3.5）。
  - `mocks/order.ts` 新增取消接口替身：reason 缺失/空白/超 50 字 → 400；非 `PENDING_PAYMENT`/`PENDING` → 409「商家已接单，无法取消」；已取消幂等返回；成功置 `CANCELLED` + `cancelReason/cancelledAt/cancelledBy=USER`（库存回补在 mock 不维护，注释标注真实后端在取消事务内回补）。
  - 接线：支付页卡内入口打开弹层、取消成功提示后回订单列表、被拒则关弹层并刷新；**并修正 105 遗留的一处误判**——已取消订单进支付页原先会跳到「支付成功页」，现改为转订单详情（展示已取消 + 原因）。详情页可用态取消按钮打开弹层，取消成功/被拒均刷新详情（成功后详情显示「已取消」+ 取消原因、时间线整体置灰）。
- **TDD 红绿**：红 **10 failed | 21 passed**（TP-1~TP-10）→ 绿 31 passed（三文件）；全量 `npx vitest run` = **199 passed (199)**（105b 后 190 + 新增 9）；`npm run build`（含 vue-tsc）= **✓ built**。
- **实现中暴露并修正的问题**：`onRejected` 调用了不存在的 `loadOrder`，被 vue-tsc 拦下 → 把支付页取数逻辑抽成 `loadOrder()`（与成功页一致）。
- **未收口**：取消成功后的列表/详情「已取消 + 原因」已可演示；真实后端 `POST /orders/{id}/cancel` 与库存回补待后端按契约实现后联调（前端 mock 已就位）；复刻对照截图仍待补。
- **本轮更新的 TODO 编号**：`TODO-USER-002`（证据补：弹层实现 + 两处入口接线 + 红 10 failed → 绿 199 passed）。

### B. AI 使用点

| # | 任务 | 类别 | AI 做了什么 | 人工审核与修改 | 结果 |
| --- | --- | --- | --- | --- | --- |
| 1 | 弹层实现 | 前端实现 | 按 PRD 7.16.1 逐条与设计真源实现弹层（含必填校验、计数上限、提交中防重、关闭三路），并执行「删除优惠券退回文案」的拍板口径 | 无 | 10 例全绿 |
| 2 | 两处入口接线 | 流程串联 | 支付页与详情页的占位提示换成真弹层，并按取消成功/被拒分别处理（回列表、刷新详情） | 无 | 链路闭环 |
| 3 | 遗留误判修正 | 缺陷修复 | 发现并修正 105 遗留问题：已取消订单进支付页会被送去「支付成功页」，改为转订单详情 | 无 | 已修正 |
| 4 | 提交结构校正 | 流程整改 | 按 CI 的 R6 规范改用「chore: 骨架 → test: 红 → feat: 绿」顺序，使 test: 提交仅含测试文件且红端为断言级 | 无 | 规范检查通过 |

## 16:31 （本轮：TODO-USER-008 待支付倒计时（列表）与再来一单）

### A. 进度事实

- **任务**：`TODO-USER-008`（待支付倒计时 + 再来一单）。倒计时在支付页已于 105a 完成，本批补齐 PRD 订单列表页异常列要求的「列表侧倒计时与失效禁用」，并把「再来一单」从占位 toast 接成真行为。
- **分支**：`feat/reorder-countdown-tdd`（基于 002 分支叠加）。**过程留痕**：本轮头两个提交起初误落在 `feat/cancel-order-sheet-tdd` 上，已即时纠正——新建 008 分支并把 002 分支 `reset --hard` 回已推送的 `cf468c5`（002 分支远端未受影响，无需重推）。
- **负责人拍板三项口径**：① 再来一单复制完成后**跳商家详情页**（可核对购物车，不直接创建订单）；② 遇到下架/售罄/库存不足**能加尽加**、按商品名汇总提示；③ 倒计时**列表 + 支付页**生效（详情页不加）。
- **实现**：
  - `utils/reorder.ts`：`reorderToCart(orderId)` 按订单明细逐条 `POST /cart/items`（契约 §3.5 不新增接口），返回 `{added, failed, storeId}`。
  - `OrderListView`：待支付卡片显示「剩余 mm:ss」（每秒刷新，`payDeadline` 驱动）、到期显示「已失效」且「去支付」置灰（`aria-disabled`）并点击提示「订单已失效，请重新下单」；已完成卡片新增「再来一单」，按能加尽加汇总提示后跳商家详情页；组件卸载时清理计时器。
  - `OrderDetailView`：底部「再来一单」接真行为（同一编排函数）。
  - `normalizers`：`normalizeOrderSummary` 透传 `payDeadline`（列表倒计时数据源）。
- **TDD 红绿**：红 **6 failed | 19 passed**（TQ-1~TQ-5b）→ 绿 25 passed；全量 `npx vitest run` = **205 passed (205)**（002 后 199 + 新增 6）；`npm run build`（含 vue-tsc）= **✓ built**。
- **未收口**：再来一单仅用户端编排已就位，真实后端 `POST /cart/items` 的 §3.4 拒绝分支（跨店/关店/售罄）待联调覆盖；复刻对照截图仍待补。
- **本轮更新的 TODO 编号**：`TODO-USER-008`（证据补：列表倒计时与失效禁用 + 再来一单实现 + 红 6 failed → 绿 205 passed）。

### B. AI 使用点

| # | 任务 | 类别 | AI 做了什么 | 人工审核与修改 | 结果 |
| --- | --- | --- | --- | --- | --- |
| 1 | 口径回读与清单 | 需求理解 | 读 PRD 6.1 功能清单/列表页三行/契约 §3.4-§3.5，把「再来一单不新增接口」的编排落点与部分失败粒度整理成三问请负责人拍板 | 负责人拍板三项 | 清单 1 份 |
| 2 | 编排与页面实现 | 前端实现 | 用独立 utils 模块承载逐条加购与失败汇总（可单测），页面只负责提示与跳转；列表倒计时以单计时器 + 纯函数计算，避免每卡一计时器 | 无 | 25 passed |
| 3 | 分支归属纠错 | 流程整改 | 发现 008 的两个提交误落在 002 分支，即时新建分支并将 002 分支还原到已推送提交（远端不受影响） | 无 | 分支干净 |

## 16:41 （本轮：TODO-USER-003 评价订单页 + 评价 Tab 真实化）

### A. 进度事实

- **任务**：`TODO-USER-003` 批次③ 评价提交页与评价 Tab 真实化（含列表「待评价」文案与去评价入口）。分支 `feat/review-order-tdd`（基于 008 分支叠加）。
- **负责人拍板三项口径**：① 契约 §3.5 **新增 `reviewed` 布尔字段**（「待评价」不是独立存储状态，由 `status=COMPLETED` 与 `reviewed` 计算），已按 R7 同步契约；② 评价图片**按设计稿渲染但本批不接上传**（上传属 TODO-USER-007，依赖契约 §10.1），点击给批次⑧提示；③ **评价 Tab 本批一起真实化**。
- **实现**：
  - 新增 `ReviewOrderView`（设计真源 `08-评价/01-评价订单`）：顶部栏、商家与打分卡（商家徽标/商家名/「为本次服务打分」/五档星级与文案）、已购商品快照横排、四枚标签多选、文字区（占位按设计稿 + 200 字上限）、图片区占位、底部固定「提交评价」（未选星级置灰可点＋必填提示、提交中禁用防重）。
  - 进入校验（PRD 异常列）：订单不存在 / 未完成 / 已评价 → 提示原因并返回订单列表。
  - `reviewApi`（`POST /orders/{orderId}/review`、`GET /stores/{storeId}/reviews`）+ `mocks/review.ts` 替身：订单存在性与完成为前提，重复评价 409（TC-REV-004）、未完成 409（TC-REV-005）、评分 1–5 与正文上限 400（TC-REV-006）、成功标记 `reviewed=true` 并写入评价（昵称脱敏）；店铺评价按时间倒序并含商家回复（TC-REV-003）。
  - 入口接线：列表页「待评价」文案（`orderDisplayStatus`）与「去评价」入口（已评价则展示「已完成」+「再来一单」）、详情页「去评价」；`StoreDetailView` 评价页签接真实列表（含商家回复块、脱敏昵称、空态），移除 P1 占位文案。
- **TDD 红绿**：红 **14 failed | 62 passed**（TV-1~TV-7、TV-8~TV-10、TV-11、TV-12；其中 `mocks/review.ts` 未创建时评价替身用例为文件级失败）→ 绿全量 `npx vitest run` = **222 passed (222)**；`npm run build`（含 vue-tsc）= **✓ built**。
- **测试侧修正（并入同一 test: 提交）**：① 评价替身用例的夹具重置误把种子清空，改为按 `REVIEW_SEED` 重置；② 008 的「再来一单」两条用例夹具补 `reviewed: true`——003 引入 `reviewed` 判定后，待评价订单展示「去评价」、已评价订单展示「再来一单」，属需求驱动的夹具调整。
- **未收口**：评价图片上传（TODO-USER-007 / 契约 §10.1）；正文 200 字上限为**课程口径**（文档未定长度，前端 `maxlength` 与 mock 校验同值，后端实现时以契约为准）；商家端评价管理与回复（批次③ 商家端侧）不在用户端范围。
- **本轮更新的 TODO 编号**：`TODO-USER-003`（证据补：评价页与 Tab 真实化 + 红 14 failed → 绿 222 passed）。

### B. AI 使用点

| # | 任务 | 类别 | AI 做了什么 | 人工审核与修改 | 结果 |
| --- | --- | --- | --- | --- | --- |
| 1 | 缺口识别与口径提案 | 需求理解 | 读 PRD/契约/TC-REV 与设计真源时发现「待评价」无字段承载，提出加 `reviewed` 并说明对列表文案与入口的影响，连同图片范围与 Tab 范围一并请负责人拍板 | 负责人拍板三项 | 口径 3 项 |
| 2 | 页面与替身实现 | 前端实现 | 按设计稿与 PRD 三行实现评价页与进入校验；mock 按契约 §6.2 覆盖 400/409/幂等与店铺列表倒序含回复；评价 Tab 改为真实接口 + 空态 | 无 | 222 passed |
| 3 | 跨批次影响处理 | 回归修正 | 识别 `reviewed` 判定对 008 两条用例夹具的影响，作为测试侧调整并入同一 test: 提交并复跑红端 | 无 | 零回归 |
