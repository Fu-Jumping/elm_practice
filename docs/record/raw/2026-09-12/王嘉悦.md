# 2026-09-12 后端 A（王嘉悦）原始记录

## A. 当日进度事实

### 19:55 TODO-BE-002 订单状态机修正（开发完成待提交/PR）

- 任务出处：契约 §3.5 定稿流转（2026-09-09 拍板）——支付成功即 `PENDING`（待接单），`PROCESSING` 仅 P0 历史兼容；原实现 `OrderMapper.markPaid` 写 `PROCESSING` 属偏差。
- TDD 红绿闭环：
  - 红端（仅测试代码）：`OrderServiceTest` 支付用例改名 `unpaidOrderBecomesPendingAfterPaymentAndRepeatPayIsIdempotent`，支付后状态断言 `PROCESSING`→`PENDING`（2 处）；复跑 `-Dtest=OrderServiceTest` 结果 **Tests run: 7, Failures: 1, Errors: 0**，唯一失败即目标用例（`expected: <PENDING> but was: <PROCESSING>`）。
  - 绿端（实现）：
    - `OrderMapper.markPaid`：`SET status = 'PROCESSING'` → `'PENDING'`（注释同步 TODO-BE-002）
    - `OrderService.pay`：支付成功后内存对象状态同步 `PENDING`
    - `OrderService.advance`：删除 `PROCESSING`→`PENDING` 合法流转，商家推进自 `PENDING` 起（`PENDING`→`COOKING`→`DELIVERING`→`COMPLETED`）
    - 种子订单 `o1001`：`seed.sql` 与 `reset.sql` 两处 `PROCESSING`→`PENDING`（新造数据遵循新状态机；`PROCESSING` 仅作为 P0 存量数据兼容值保留，枚举不删，统计口径 `status != 'PENDING_PAYMENT'` 天然兼容）
    - `merchantStatusCannotJumpAndRepeatedTargetIsIdempotent` 无需改动：o1001 改 `PENDING` 后 `advance(o1001,"COMPLETED")` 仍 409 跳级、重复 `advance(o1001,"PENDING")` 仍幂等返回
  - R1+R8 证据：`mvn -B clean test` 连服务器测试库 `elm_practice_test`（未碰业务库）**Tests run: 55, Failures: 0, Errors: 0**；`mvn -B -DskipTests package` 成功。SSH 公钥仍未授权，沿外网直连测试库口径（elm 账号密码以环境变量注入，不入仓库）。
- 文档同步（R7 同 PR 口径）：契约 §3.5 状态机条目、§9"订单初始状态"、§10.4 决议 1 归属列三处回写"已修正实现（2026-09-12，TODO-BE-002）"；`docs/todo/后端.md` TODO-BE-002 状态改**已完成**并填证据列。
- 环境坑（再次踩中）：对 `OrderService.java` 两处并行 Edit，第二处落盘覆盖了第一处（149 行回退为旧值），grep 复核发现后单独重改——同文件多处修改必须串行 Edit + 落盘复核。
- 本轮新增/更新的 TODO：TODO-BE-002（已完成）。
- 待办：代码尚未提交，提交拆分建议 `test:`（红）→ `feat:`（绿）→ `docs:`（契约+台账+本 raw），由项目负责人确认提交时机。

## B. 本次工作 AI 使用点

- AI 辅助：影响面 grep 排查（markPaid/PROCESSING/advance/o1001 全量定位）、红端断言修改、绿端四处代码修改、契约三处回写、台账更新、红绿复跑与证据摘录。
- 人工判断口径：状态机定稿值（2026-09-09 负责人拍板，本轮仅按定稿实现）；种子 o1001 改 `PENDING` 而非保留 `PROCESSING` 的取舍（新造数据遵循新状态机、历史兼容只留给存量数据）。
