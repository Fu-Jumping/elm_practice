# 2026-09-12 后端 B（林晨）原始记录

## A. 当日进度事实

### 1. BUG-20260908-007 端口口径统一（TODO-OPS-003，开发完成待 PR）

- 分支 `fix/port-unify-4000`（基于 main）。
- 三处指定改动：
  - `backend/src/main/resources/application.properties`：`server.port=8080` → `4000`（加注释说明服务器命令行同值覆盖）
  - `frontend/user-h5/vite.config.ts`：代理默认值与注释统一为 `http://127.0.0.1:4000`
  - `backend/README.md`：默认监听 `127.0.0.1:8080` → `4000`
- R7 同 PR 同步的现行口径文件：`图片清单.md`（联调代理口径 b）、`docs/maps/PRD与SRS思维导图.html`（系统上下文/运行环境端口）。
- 未改动：`docs/record/**`、`docs/testing/**` 历史记录（历史事实）；`AuthControllerIntegrationTest` 的 CORS Origin `:8080`（保留为非标准端口通配匹配用例）；`merchant-admin/vite.config.ts` 注释本就写 4000。
- 部署影响评估：服务器 pm2 以 `--server.port=4000 --server.address=127.0.0.1` 启动（见 `deploy/release.py`），默认值改 4000 与命令行一致，无影响；nginx 代理本就是 4000。
- R1 证据：`mvn -B -DskipTests package` 成功（29.7M jar）；用户端 `npm run build`（run-p type-check + build-only，含 tsc）成功。
- grep 复查：跟踪文件中 8080 仅余历史记录、解释性注释、缺陷/todo 任务描述与 CORS 测试用例。

### 2. BUG-20260908-012 店铺设置联系电话（TODO-BE-019，开发完成待 PR）

- 分支 `fix/merchant-store-contact-phone`（基于 main），TDD 红绿。
- 红端（仅测试代码 `MerchantStoreContactPhoneIntegrationTest`，3 用例）：在绿端实现前复跑 **Tests run: 3, Failures: 3**，失败原因为 `No value at JSON path "$.data.contactPhone"`（PATCH 静默丢弃字段、GET 无回显；非法值未拦截）。
- 绿端实现：
  - `Requests.StorePatch` 加 `contactPhone`
  - `MerchantMapper.updatePhone`（UPDATE merchants.phone）
  - `StoreService.updateStore` 加商家与 contactPhone 参数，与店铺资料同一 `@Transactional`；不传=不改，传入校验 `^1\d{10}$`（与注册同口径），非法 400 不落库
  - `ViewMapper.merchantStore(store, phone)` 商家端视图额外回显 contactPhone；用户端 `store(s)` 不暴露商家电话
  - `MerchantController` GET/PATCH `/merchant/store` 改用商家端视图
  - 契约 §4.1 按 R7 同 PR 回写（字段清单、部分更新语义、校验规则、仅商家端可见）
- 绿端复跑：新测试 3/3；全量 **Tests run: 28, Failures: 0, Errors: 0**（连服务器测试库 `elm_practice_test`，未碰业务库 `elm_practice`）。
- 说明：R8 字面要求 SSH 到服务器跑测试；本机 ubuntu/fzp/dyh 公钥均未授权（Permission denied），本次从本机经外网以 `DB_HOST=82.157.137.114 DB_NAME=elm_practice_test DB_USER=elm` 连接执行，红线满足（仅测试库、未对业务库跑重置）；CI `backend-test` 将在 PR 上复核。
- 缺陷单状态：待测试联调同学（王馨琪）回归后在缺陷跟踪表关闭，后端未自行改台账状态。

### 3. 待办更新

- TODO-BE-019、TODO-OPS-003 证据列已更新（状态保持"进行中"，待 PR 合并与回归）。

## B. 本次工作 AI 使用点

- AI 辅助：grep 影响面排查、红测试用例设计脚手架（正常保存回显/部分更新保留/非法值 400 三场景由人确认）、绿端代码实现、契约回写、R1 构建验证、红绿复跑与证据摘录。
- 人工确认：缺陷口径回读缺陷跟踪表与契约 §4.1、提交分支拆分（两个缺陷两个 PR）、测试异常场景设计、是否修改历史记录文件的判断。
- 待负责人决策：是否授权提交并推送两个分支（`test:` 红端提交与 `fix:` 绿端提交分离）；SSH 公钥是否需要补登记以便后续严格按 R8 在服务器跑测试。

## 11:47 批次① 优惠计价七步接入下单（TODO-BE-001）红绿闭环

### A. 当日进度事实

- **红端**（上一轮）：`PricingIntegrationTest` 8 用例连 `elm_practice_test` 跑红（6F+2E，缺列缺表）。
- **绿端落盘**（本轮核对补齐）：
  - 两处 schema（`resources/db/schema.sql` 与 `database/schema/schema.sql`）：orders 补 6 个金额快照列（`delivery_fee`/`full_reduction_amount`/`new_customer_amount`/`member_discount_amount`/`coupon_amount`/`delivery_fee_discount`，DEFAULT 0）、promotions 扩 3 列、新增 `promotion_tiers`。
  - 迁移脚本 `database/migrations/20260912-promotion-tiers.sql`（幂等，information_schema+PREPARE 逐列判断）；另落运行时副本 `resources/db/upgrade-promotion-tiers.sql`（仅 schema 升级，步骤 1–4），由 `DatabaseInitializer` 启动幂等执行——演示促销种子（步骤 5）不随启动跑，避免每次重启覆盖商家配置。
  - `PricingService` 七步计价；`OrderService.create` 接入（新客判定 `OrderMapper.countByUserAndStore`，下单即算含待支付；会员/红包批次⑥接入前恒 false/0）；`PromotionMapper.findConfig/findTiers/upsert/insertTier`；`ViewMapper.order` 回写 6 个快照字段；`ExtensionService.savePromotion` 保存校验（门槛/减额非负、减额≤门槛、门槛不重复升序落库、折扣率 (0,1]、免配门槛非负，非法 400 不落库）。
  - 种子同步：两处 `seed.sql` orders 金额改按新口径（26.50/26.50/16.00）+ m002 演示促销；`reset.sql` 补 m002 促销基线（PricingIntegrationTest 期望金额依赖）。
- **本轮修复**：`OrderService` 补 promotions/pricing 字段构造器与 import（上轮编辑不完整）；`OrderMapper.insert` 补 6 快照列 + 新增 `countByUserAndStore`；既有断言按新口径修正——`OrderServiceTest` total 41→46 又按促销基线改 39.00（39−2+5−5+2）、`ExtensionServiceTest` 营收 43.00→53.00、`tcPrv004` 注册账号改 11 位手机号（用户注册校验 `^1\d{10}$`）且期望金额补算满减（首单 30.50、二单 33.50）。
- **R1+R8 证据**：`mvn -B clean test` 连服务器测试库 `elm_practice_test`（未碰业务库）**Tests run: 37, Failures: 0, Errors: 0**，其中 PricingIntegrationTest 8/8、PricingServiceTest 4/4、OrderServiceTest 7/7；`mvn -B -DskipTests package` 成功。SSH 公钥仍未授权，沿用上轮外网连测试库口径。
- **环境坑（规避决策）**：本机 IDE 的 JDT 构建器会在文件编辑后往 `target/` 写入源级不识别 `var` 的坏 class，Maven 增量编译复用导致运行期 `Unresolved compilation problems` 假失败；**后续跑测试一律 `mvn clean test`**。另发生过两次 IDE 缓冲区回写吞掉刚落盘的编辑（OrderService import、tcPrv004 期望值），编辑后须 grep 复核磁盘内容。
- **口径同步（R7）**：契约 §3.5 计价标题改"已实现（红包随批次⑥）"、§6.3 改"已实现"+保存校验口径（折扣率 (0,1]、减额≤门槛）+迁移交付证据、§10 优惠配置边界改"2026-09-12 接入计价"并回写快照字段与测试证据。
- **台账**：TODO-BE-001 证据列已更新（状态保持"进行中"待提交确认），阻塞表"旧单档数据兼容"标记已解除。
- 发现的存量文档债（不属本批，未动）：契约 §10"并发口径"仍写内存 Repository 监视器口径，与已合并的 MySQL 持久化不符，建议另行修正。

### B. 本次工作 AI 使用点

- AI 辅助：绿端缺漏核对与补齐（OrderService/OrderMapper/schema/seed）、失败归因（种子缺失、手机号校验、满减漏算）、契约 R7 回写、TODO 证据更新、红绿复跑与证据摘录。
- 人工确认事项（待拍板）：提交拆分建议 `test:` 红端先行 + `feat:` 绿端（复跑红端证据：迁移前 6F+2E）；是否授权提交；契约 §10 存量"并发口径"旧文是否由本 PR 顺带修正。
- 本轮新增/更新 TODO 编号：**TODO-BE-001**（证据列更新 + 阻塞解除）；无新增缺陷类任务。


## 14:18 批次① 入库闭环：提交拆分、PR #48 开启与合并

### A. 当日进度事实

- 提交拆分（TDD 纪律，分支 eat/pricing-batch1，基于 7692960——留痕文件耦合，从 main 直切会污染本文件）：
  - e958096 test: 红端（仅测试与脚手架；复跑 mvn clean test-compile 编译失败=红）
  - 7fc3ed2 feat: 绿端（实现 + schema + 迁移 + 种子）
  - cdd39a/15521b7/42180b0 docs: 契约 §3.5/§6.3/§10 回写（§3.5/§10 两处编辑被 IDE 缓冲区连续回吞 3 次，最终以 PowerShell 原子落盘→立即验证→提交方式补交）
- eset.sql 补 TRUNCATE promotion_tiers，修复阶梯数据在用例间残留的隔离隐患（此前全绿依赖用例顺序，属侥幸）。
- 推送并开 PR #48：描述含 R1/R8 证据表（红端编译失败+6F+2E；绿端 37/37 连 elm_practice_test）、TDD 红绿哈希、未收口项（红包步⑥/会员判定随批次⑥；连带 7692960 port fix 说明；契约 §10"并发口径"旧文档债）；CI clean。
- 经负责人授权通过 API 合并 PR #48 → main（merge commit cec86da），本地 main 已同步；TODO-BE-001 状态改"已完成"。
- 待收尾：服务器拉 main 重启后端（DatabaseInitializer 幂等升库，无需手工迁移）+ 冒烟验证（m002 下单满减/新客立减生效）。

### B. 本次工作 AI 使用点

- AI 辅助：提交拆分执行、PR 描述撰写、API 推送与合并、台账与留痕更新、IDE 吞改问题的原子化规避与 grep 复查。
- 人工确认：合并授权（负责人拍板"直接合并"）；port fix 连带处理方式在 PR 描述说明；部署收尾执行方式待定。
- 本轮新增/更新 TODO 编号：**TODO-BE-001**（状态与证据列更新为已完成）；无新增缺陷类任务。