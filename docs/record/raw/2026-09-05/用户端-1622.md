# 用户端 9/5 raw 片段（首页 exact 复刻启动前准备）

## 进度事实（A 表口径）

- 注册模块红-绿循环交付收口：PR #7 已开（feat/user-register-tdd → main），红 `test:` f3058d4（14 failed | 12 passed）→ 绿 `feat:`（26 passed），待组长 review。
- 首页 exact 复刻启动前准备完成（UI复刻约定 §3.1"启动前"）：
  - 真源定位：`02-首页-精细/首页-精细-项目化.svg`（页面级视觉真源）+ `screen.png` 截图比对基线；高于设计稿的既定取舍均有出处（色值映射/四 Tab 重绘/占位清单，见精细版 README）。
  - 素材先行：新增提取脚本 `frontend/user-h5/scripts/extract-design-assets.mjs`（解析 SVG 的 image 原件→pattern→rect 三级引用链），29 张内嵌位图落盘；24 张在用素材语义命名后落位 `frontend/user-h5/public/design-assets/首页-精细/`（channel ×5 / cat-grid ×10 / promo ×4 / merchant-cover / product-thumb ×3 / badge），5 张导出遗留图（疑似旧版底栏图标）判定不迁移。
  - 素材清单落 `docs/frontend/首页复刻素材清单.md`（原 id、位置、尺寸、用途、复用关系、实现注意）。
  - 素材经本人对 `screen.png` 抽查核对通过（16:22 前后）。
- 复刻工程策略已明确：长列表+固定底栏 → MainLayout 统一 `height:100dvh` 内容区滚动；底栏为共享组件；商家卡图片将来走 `GET /stores` 数据（mock 的 image 字段指向素材目录），运营字段不参与计价。

## AI 使用点（B 表口径）

| # | 任务 | 类别 | AI 做了什么 | 人工审核与修改 | 结果 |
| --- | --- | --- | --- | --- | --- |
| 3 | 首页复刻素材迁移 | 其他（工具脚本） | 编写 SVG 素材提取脚本（v1 误判渲染树、v2 误判 pattern 结构，靠输出数据异常自查后 v3 修正）；逐张查看图片提出语义命名草案与未使用素材剔除建议；起草素材清单文档 | 素材抽查核对（对 screen.png）由本人执行并确认；清单内容确认；提交与否及时机由本人定 | 素材先行完成，首页可进入静态转写阶段 |
