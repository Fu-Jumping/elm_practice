# 用户端 9/5 raw 片段（注册模块红阶段）

## 进度事实（A 表口径）

- 9/5 冲刺日一（账号模块红-绿循环）红阶段完成：新增 `frontend/user-h5/src/services/__tests__/validators.spec.ts`（C1-C7 字段校验 + D1-D4 表单校验，共 11 条）与 `sessionStore.spec.ts` E1-E3（注册链路，共 3 条），`vitest run` 结果 **14 failed | 12 passed**（新增 14 条全红，既有 A1-A6/B1-B5/HomeView 回归绿），14:51 输出。
- 口径拍板（人决策）：TC-ACC-007 密码复杂度暂不覆盖（待 SRS 固化后按"先补失败测试再实现"回填）；本期只做逻辑层、注册页 UI 归 exact 复刻任务；TC-MER-011 归后端集成（前端无商家接口调用）；重复注册业务码 40900（沿用 HTTP×100 惯例）；注册成功不建立会话（PRD 7.16.1 成功返回登录页）。
- 断言口径：字段级 toBe 严格锁定错误文案（错误文案表经本人确认）；表单级 toEqual 整体比对错误对象。
- 分支：feat/user-register-tdd（基于最新 origin/main 创建，feat/user-login-tdd 已由 PR #6 合并）。

## AI 使用点（B 表口径）

| # | 任务 | 类别 | AI 做了什么 | 人工审核与修改 | 结果 |
| --- | --- | --- | --- | --- | --- |
| 1 | 注册模块红阶段（红一/二/三） | 测试用例生成 | 讲解红-绿目的、Vitest 逐段语法与断言宽严权衡；起草 validators.ts 桩、sessionStore.register 桩与 E1/E2 骨架；review 出 D3、E3 各一处"断言与剧情不符"（D3 confirmPassword 残留 + 键名错、E3 误用 rejects） | 用例清单、函数签名、错误文案、断言严格度均由本人拍板（C 组选严格锁文案）；validators.spec 全部用例与 D3、E3 由本人亲手录入后修正 | 红阶段完成：14 failed \| 12 passed |
