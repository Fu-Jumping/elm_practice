# 王馨琪留痕（2026-09-14）

## 10:00–11:20 商家端总回归与二阶段真实验收（9/14 总回归日）

### A 进度事实

- **R1 必跑矩阵（商家端前端）**：`npm test` → 9 个测试文件 / **29 用例全绿**；`npm run build` → **0 个 TS 错误**（含类型检查）。分支 `test/merchant-total-regression-0914`，基于 `origin/main`（`fe0cf53`）。
- **真实链路验收（线上 4001 + 127.0.0.1:4010 隧道，真实模式）**：
  - `TODO-MER-008` 九项导航：左侧九项齐全且顺序符合 PRD → **已完成**。
  - `TODO-MER-001` 优惠配置：页面加载 + `PUT /merchant/promotions` 200 且落库（新客立减 3 / 免配送门槛 30 / 会员折扣 0.95）；**配置真实影响计价**——新客订单 `o1204` 39+2+5−3−5=实付 38 自洽 → **已完成**。
  - `TODO-MER-011` 金额明细：无优惠单 `o1157` 基础四行（39+2+0=41）；优惠单 `o1204`/`o1209` 优惠项按实际发生（新客立减 −3、配送费优惠 −5、红包抵扣 −5）金额自洽 → **已完成**。
  - `TODO-MER-012` 红包抵扣：红包订单 `o1209`（满 30 减 5）详情显示「红包抵扣 −¥5.00」，实付 36 自洽 → **已完成**。
  - `TODO-MER-002` 取消筛选：`?status=PENDING` 返回 o1209（筛选生效）、`?status=CANCELLED` 200 空数组；卡点：无 `CANCELLED` 样本。
  - `TODO-MER-003` 评价管理：页面（评分分布/未回复筛选/回复抽屉）渲染正常，`GET /merchant/reviews` 200 空数组；卡点：无评价样本。
  - `TODO-MER-004` 消息：`GET /conversations` 200（17 条真实会话含 orderId/merchantRead/messages）、详情 200、`PATCH /conversations/{id}/read` 200（merchantRead 置 true）；**卡点：`POST /conversations/{id}/messages` 返回 500**。
  - `TODO-MER-007` 概览+统计：概览页真实数据（今日营业额 ¥345.50 / 有效订单 10 / 商品 6，与接口一致）；统计页范围切换与口径说明正常；卡点：契约要求的 `avgOrderAmount`/`trend`/`channelDistribution`/`statusDistribution` 缺失（现仅 orderCount/salesAmount/productCount，daily=[] 且 range 不区分）。
  - `TODO-MER-005/006/009`：`/merchant/products/{id}/specifications`、`/files/images`、`/merchant/categories/{id}/products` 三接口线上**均 404「接口不存在」** → 阻塞（后端批次⑦⑧）。
  - `TODO-MER-010`：后端修复分支 `fix/merchant-store-contact-phone`（test `bb1d2cc` + fix `055408e`）与 `codex/merchant-stage2`（16 提交含后端二阶段）**均未并入 main** → 阻塞。
- **发现并登记 2 个新缺陷（已落问题记录明细 + 台账）**：
  - `BUG-20260914-001`：`GET /merchant/categories/{categoryId}` 对任意 id（含不存在）恒 **500**（同批未实现路径返回 404，说明属已映射但执行异常）；前端未调用该 GET，用户不可见 → P2。
  - `BUG-20260914-002`：`POST /conversations/{id}/messages` 返回 **500**（同会话标记已读 200）；商家端无法回复顾客，属批次④ 未实现但响应非契约语义 → P2。
  - 明细文件：`docs/testing/问题记录/2026-09-14-商家端二阶段验收接口异常.md`。
- **TODO 台账更新（`docs/todo/商家端.md`）**：`MER-001/008/011/012` 转「已完成」（附真实验收证据）；`MER-002/003/004/005/006/007/009/010` 保持「进行中」并写明卡点；§3 阻塞表刷新为 5 行（新增 BUG-20260914-001/002 与未部署批次引用）。
- **文档债收口（`docs/todo/文档与交付.md`）**：`DOC-002`（确认「以台账详情区块为唯一载体」豁免口径并回填）、`DOC-003`（说明文档已在 `docs/project/` 合规位置）、`DOC-004`（本文件上方 9/8 片段追加更正小节）、`DOC-105`（商家端 P0 4 类场景补入 `docs/backend/后端联调验收用例.md` §2.1）均转「已完成」。
- **数据副作用（如实记录）**：验收过程中保存了优惠配置（新客立减 3 元 / 免配送门槛 30 元 / 会员折扣 0.95，与契约 §6.3 演示口径一致），未回退；新建验收样本：用户 `13989353048`（u1200）、地址 `da1201`、订单 `o1204`（新客立减+配送减免）、`o1209`（红包抵扣），会话 `cv1008` 已标记已读。如需回到验收前状态（全部停用）可一键恢复。

### B 本轮 AI 使用点

| # | 任务 | 类别 | AI 做了什么 | 人工审核 | 结果 |
| --- | --- | --- | --- | --- | --- |
| 1 | R1 矩阵执行 | 测试执行 | 在 main 基线跑 `npm test` / `npm run build` 并汇总结果 | 王馨琪确认命令与结果 | 29/29 绿、构建 0 错误 |
| 2 | 真实链路验收 | 集成验收 | 经 SSH 隧道驱动浏览器完成 12 项 TODO 的页面与接口双证据采集，并造真实优惠/红包样本订单 | 王馨琪确认验收口径与样本真实性 | 4 项转已完成、8 项附卡点 |
| 3 | 接口异常定位 | 缺陷分析 | 逐接口探测并按前端真实路径复核（纠正了自身一次路径误判：`/merchant/conversations` → 实为 `/conversations`），定位两个 500 | 王馨琪确认后落盘 | BUG-20260914-001/002 |
| 4 | 文档收口 | 文档撰写 | 更新台账、问题记录明细、TODO 板块与验收用例 §2.1 | 王馨琪复核 | DOC-002/003/004/105 完成 |

### C 本轮新增/更新的 TODO 编号

- 更新状态：`TODO-MER-001`（→已完成）、`TODO-MER-008`（→已完成）、`TODO-MER-011`（→已完成）、`TODO-MER-012`（→已完成）
- 更新证据与卡点（保持进行中）：`TODO-MER-002`、`TODO-MER-003`、`TODO-MER-004`、`TODO-MER-005`、`TODO-MER-006`、`TODO-MER-007`、`TODO-MER-009`、`TODO-MER-010`
- 完成文档债：`TODO-DOC-002`、`TODO-DOC-003`、`TODO-DOC-004`、`TODO-DOC-105`
- 新增缺陷（登记台账 + 问题记录明细，后端侧任务编号需后端确认）：`BUG-20260914-001`、`BUG-20260914-002`

## 11:40 状态核对与待办修复（PR #62 合并后）

### A 进度事实

- **PR #62 已合并**：`05e52c9 Merge pull request #62 from Fu-Jumping/test/merchant-total-regression-0914`，本日成果（问题记录明细、TODO 状态流转、验收用例 §2.1、raw 片段）已全部进入 main。
- **main 前进**：`fe0cf53` → `a2f15b1`（45 提交），新增 PR #61（`codex/merchant-stage2` 后端二阶段）、#63（分类商家列表页）、#64（AI 点餐助手）。
- **线上仍是 9/13 版**：`/srv/elm-current → /srv/elm-releases/20260913-707f234`，落后 main 46 提交；外网 `4001` 实测 200（入口正常）。
- **后端实现核对（main 代码）**：消息发送（`ExtensionController`）、规格（`MerchantController` specifications）、分类绑定（`bindProducts`）、`contactPhone`、统计聚合（`OrderMapper`/`ExtensionService` 的 `avgOrderAmount`）、评价回复（`reviews/{reviewId}/reply`）**均已实现并随 PR #61 进入 main**；**取消订单（`orders/{orderId}/cancel`）与图片上传（`files/images`）在 main 后端仍无实现**。
- **待办修复**：发现并纠正自身失误——上一轮清理阻塞陈旧行时用前缀 `| TODO-MER-010 | **BUG-20260908-012**` 匹配，误删了「进行中」区的 `TODO-MER-010` 行（与阻塞行前缀相同）。已恢复该行，并把 `MER-004/005/007/009/010` 的卡点更新为「后端实现已随 PR #61 并入 main，待重新部署后验收」，`MER-006` 如实标注「后端未实现（非部署问题）」，`MER-002/003` 保持样本/实现缺失说明；§3 阻塞表压缩为 3 行。

### B 本轮 AI 使用点

| # | 任务 | 类别 | AI 做了什么 | 人工审核 | 结果 |
| --- | --- | --- | --- | --- | --- |
| 1 | 状态核对 | 集成复核 | fetch 全分支、比对 main 与线上版本、按后端源码逐接口核对实现是否存在 | 王馨琪确认结论 | 形成卡点转移结论 |
| 2 | 待办修复 | 文档修正 | 定位自身误删行、恢复并更新 8 行卡点 | 王馨琪复核 | 待办台账恢复一致 |

### C 本轮新增/更新的 TODO 编号

- 恢复：`TODO-MER-010`（进行中）
- 更新卡点与证据：`TODO-MER-002`、`TODO-MER-003`、`TODO-MER-004`、`TODO-MER-005`、`TODO-MER-006`、`TODO-MER-007`、`TODO-MER-009`
- 关联缺陷状态待部署后回归：`BUG-20260914-001`、`BUG-20260914-002`、`BUG-20260908-012`
## 12:10 店铺设置联系电话字段恢复（TDD 红转绿）

### A 进度事实

- **任务来源**：`TODO-MER-010`（缺陷 `BUG-20260908-012`）。核对发现后端 `StorePatch.contactPhone` 已随 PR #61（`2e28d3d`）落入 main、且 `Store`/`StoreDraft`/`normalizeStore` 早已支持该字段，**缺口仅在商家端 UI**（该字段此前按临时处置被前端移除）。
- **契约/PRD 口径回读**（按待办中心要求）：契约 §4.1（L364）「`PATCH /merchant/store` 的 `contactPhone` 为可选字段，传入时须为 11 位手机号 `^1\d{10}$`，空串或格式非法 `400` 且不落库」；PRD L993「店铺名称必填、联系电话非必填，格式不合法时提示并定位字段」。
- **TDD 红端**：新增 `tests/merchant-store-contact-phone.test.tsx`（3 用例：字段存在 / 保存提交 contactPhone / 格式非法提示且不提交），并反转 `tests/pr36-review-gaps.test.tsx` 中原临时断言；首次运行 **3 failed**（`Unable to find a label with the text of: /联系电话/`）。`test:` 提交 `4d85c9f`（仅测试代码）。
- **TDD 绿端**：`feat:` 提交 `f06ad90` —— 恢复表单输入项（非必填 + 11 位校验）、`saveStore` 提交 `contactPhone`（空值不发送，避免后端按空串判 400）、mock 种子电话由座机号改为 11 位 `13800000002`。
- **R1 必跑矩阵**：`npm test` **10 文件 / 32 用例全绿**（连续两次复跑结果一致）；`npm run build` **0 个 TS 错误**。期间一次全量运行出现 2 个偶发失败（`merchant-features`、`stage2-navigation` 在并发下超时），单独复跑与后续两次全量均通过，判定为时序性偶发、与本次改动无关。
- **缺陷单**：`BUG-20260908-012` 由「新建」转为「待回归」，补齐测试/修复提交与本轮口径；回归需在**按最新 main 重新部署后**执行（线上 9/13 版 jar 无该字段）。
- **注意**：本次代码改动只在重新部署后生效，届时后端字段与前端字段同批上线，不存在中间态。

### B 本轮 AI 使用点

| # | 任务 | 类别 | AI 做了什么 | 人工审核 | 结果 |
| --- | --- | --- | --- | --- | --- |
| 1 | 缺口定位 | 需求对齐 | 比对 main 后端能力与前端实现，回读契约 §4.1 与 PRD L993 确认字段可选性与手机号校验口径 | 王馨琪确认口径 | 缺口仅在前端 UI |
| 2 | TDD 实施 | 测试+实现 | 起草红测试（含异常用例）、实现恢复字段、跑通 R1 并识别偶发失败 | 王馨琪确认断言与实现 | 红 3 failed → 绿 32/32 |
| 3 | 缺陷台账 | 缺陷管理 | 更新 BUG-012 状态与提交证据 | 王馨琪复核 | 待回归 |

### C 本轮新增/更新的 TODO 编号

- 推进：`TODO-MER-010` —— 前端字段已恢复，卡点转为「待部署回归」；该待办行的状态/卡点改写与「恢复误删行」由配套分支 `fix/mer-todo-010-and-blockers` 提交（避免两个 PR 修改同一行造成冲突）
- 关联缺陷：`BUG-20260908-012`（新建 → 待回归）

## 13:20 部署后验收与统计页状态中文化（TDD 红转绿）

### A 进度事实

- **部署已更新**：`/srv/elm-current → 20260914-a2f15b1`（9/14 发布，含 PR #61 后端二阶段）。据此对原「卡在部署」的 6 项做部署后真实验收：
  - `TODO-MER-002` 已取消筛选：**通过**。建单 o1262 → `POST /orders/{id}/cancel` 200（`CANCELLED` + `cancelReason` + `cancelledAt`，重复取消幂等 200）；商家 `?status=CANCELLED` 返回 2 条含原因与时间。
  - `TODO-MER-004` 消息：**通过**。20 条会话、聊天详情可打开；UI 发送 → `POST /conversations/cv1008/messages` **200** 且消息可见；`PATCH /read` 200。**原 BUG-20260914-002（发送 500）已随本次部署修复**（缺陷待回归关闭）。
  - `TODO-MER-005` 规格：**通过**。商品详情返回 `specOptions`；`PUT /merchant/products/p101/specifications` 200（原值回写）。
  - `TODO-MER-006` 图片上传：**通过**。`POST /files/images` 200 返回 `/uploads/*.png`，该 url 可访问（200 / image/png）。
  - `TODO-MER-007` 运营概览与统计：**通过**（浏览器）。营业额 ¥473.50 / 有效订单 11 / 客单价 ¥43.05、每日趋势 7 天、渠道与状态分布均真实数据。
  - `TODO-MER-009` 分类绑定：**通过**。`PATCH /merchant/categories/c102/products` 200。
  - `TODO-MER-010` 联系电话：后端已上线（`GET /merchant/store` 返回 `contactPhone`）；前端字段恢复在本 PR 内，待合并部署后回归。
  - `TODO-MER-003` 评价：仍无样本（`GET /merchant/reviews` 0 条）→ 保持进行中。
- **新增 TDD 循环（SHOW-QA-006）**：部署后验收发现统计页「状态分布」显示英文枚举。核查契约只规定返回 `statusDistribution`（未规定文案语言），但全站状态文案均为中文（订单列表/详情用 `orderStatusLabel`），判定为展示一致性缺陷 P3。
  - 红端：`a7bc9a6`（新测试 3 条，首次 3 failed：`orderStatusLabel is not a function`、`localizeStatusDistribution is not a function`）
  - 绿端：`3ae7558`（映射下沉 `merchantRules`、`App.tsx` 引入并再导出、统计页 `localizeStatusDistribution`）；R1：`npm test` 10 文件 / 32 用例全绿、`npm run build` 0 类型错误、`npm run lint` 0 警告 0 错误
- **分支整合**：把 `feat/merchant-store-contact-phone-tdd` 合并进 `fix/merchant-analytics-status-label-tdd`（无冲突），本 PR 统一交付两条 TDD 循环与文档回填；原 `fix/mer-todo-010-and-blockers` 分支内容并入本 PR（该远端分支已删除，避免重复）。
- **遗留 500（未修）**：`GET /merchant/categories/{id}` 部署后仍恒 500（含不存在 id）；另发现 `PATCH /merchant/reviews/{id}/reply` 对不存在评价同样 500（应 404），同类签名，已在待办阻塞表登记并指向后端。

### B 本轮 AI 使用点

| # | 任务 | 类别 | AI 做了什么 | 人工审核 | 结果 |
| --- | --- | --- | --- | --- | --- |
| 1 | 部署后验收 | 集成验收 | 探测线上接口、浏览器驱动消息发送与统计页取页面证据、构造真实取消样本 | 王馨琪确认样本与结论 | 6 项通过、1 项缺样本 |
| 2 | TDD 实施 | 测试+实现 | 起草状态映射红测试、下沉映射模块、跑通 R1 与 lint | 王馨琪确认文案口径 | 红 3 failed → 绿 32/32 |
| 3 | 分支与文档整合 | 流程整理 | 合并两条 TDD 分支、回填台账与待办、追加留痕 | 王馨琪复核 | 单 PR 交付 |

### C 本轮新增/更新的 TODO 编号

- 转已完成：`TODO-MER-002`、`TODO-MER-004`、`TODO-MER-005`、`TODO-MER-006`、`TODO-MER-007`、`TODO-MER-009`
- 保持进行中：`TODO-MER-003`（缺评价样本）、`TODO-MER-010`（前端字段待合并部署）
- 新增展示缺陷：`SHOW-QA-006`（统计页状态分布英文枚举，已修复待回归）

## 14:05 同步 main 后履行台账确认职责（BUG-20260914-003/004/005）并关闭 002

### A 进度事实

- **状态核对**：main 由 `660dfbc` 前进至 `ac2b0fb`（14 提交，含 PR #65 AI 聊天页、`e64fa44` 消息中心真后端修复）；**线上再次发布为 `20260915-ac2b0fb-ai`**（2026-09-15 00:21）；本分支 `fix/merchant-analytics-status-label-tdd` 尚未合并。
- **履行台账确认职责**（`docs/project/项目规则.md` §1.1 硬约束 8：编号、严重程度与状态结论由测试联调同学确认）——用户端负责人名下 AI 代理代拟的三条缺陷已由本人逐条确认：
  - `BUG-20260914-003`（公开页刷新后加购被误判未登录）：编号有效、严重程度定为 **P1**（P0 主链路「浏览→加购→下单」在刷新/深链路径被打断，可重新登录绕过）、状态转「已确认」。
  - `BUG-20260914-004`（消息中心通知失败连带丢弃会话）：**抽检发现 `/me/notifications` 与 `/me/notifications/unread-count` 现已实现并返回 200**，原 404 触发条件消失；前端 `Promise.all` 健壮性缺陷仍在，按韧性修复。确认 **P2**、转「已确认」。
  - `BUG-20260914-005`（会话缺 `storeId` 致整页白屏）：**独立复核证实** `/conversations` 首条字段为 `conversationId/orderId/userId/userNickname/merchantId/userRead/merchantRead/unreadCount/lastMessage/updatedAt/messages`——确无 `storeId`，白屏根因成立。严重程度由建议值 P2 **上调为 P1**（台账分级「白屏」属严重症状；因消息中心非 P0 主链路且可不进入该页绕过，故不升 P0；如负责人认为影响面小可复议降 P2）。
- **关闭 `BUG-20260914-002`**（发送消息 500）：修复随 `20260914-a2f15b1` 部署生效，回归通过——商家端消息页 UI 发送 → `POST /conversations/cv1008/messages` **200**、消息在列表与详情可见、标记已读 200；`20260915-ac2b0fb-ai` 版本复测仍 200。状态「已确认 → 已关闭」。
- **`BUG-20260914-001`**（分类详情 GET 500）：`20260915-ac2b0fb-ai` 复测**仍恒 500**（含不存在 id），保持「已确认」并已在阻塞表登记，待后端修复。
- **合并处理**：`git merge origin/main` 后仅 `docs/testing/缺陷跟踪表.md` 冲突（双方在同一位置追加详情区块），按「双方详情均保留」解决，无内容丢失。
- **R1（合并后，商家端前端）**：`npm test` **11 文件 / 35 用例全绿**；`npm run build` **0 个 TS 错误**（合并带入 main 代码后重跑）。

### B 本轮 AI 使用点

| # | 任务 | 类别 | AI 做了什么 | 人工审核 | 结果 |
| --- | --- | --- | --- | --- | --- |
| 1 | 台账确认 | 缺陷管理 | 抽检代拟缺陷的关键证据（通知接口状态、会话字段清单），按台账分级规则给出严重程度与状态结论 | 王馨琪确认编号/级别/状态 | 003=P1、004=P2、005 上调 P1，均转已确认 |
| 2 | 回归关闭 | 集成验收 | 在两次发布版本上复测消息发送，给出关闭证据 | 王馨琪确认回归通过 | BUG-20260914-002 关闭 |
| 3 | 合并与验证 | 工程整理 | 解决台账冲突、重跑 R1 矩阵 | 王馨琪复核 | 无冲突残留、35/35 绿 |

### C 本轮新增/更新的 TODO 编号

- 本轮无新增 TODO；为缺陷台账状态流转（`BUG-20260914-001` 补复测、`-002` 关闭、`-003/004/005` 确认）与自建展示缺陷 `SHOW-QA-006` 的合并同步。
- 关联待办：`TODO-USER-114`、`TODO-USER-115`（用户端，责任人戴宇涵）；`TODO-MER-010`（联系电话，待本 PR 合并部署）
