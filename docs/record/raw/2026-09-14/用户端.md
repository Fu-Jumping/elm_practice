# 2026-09-14 用户端留痕（戴宇涵）

> 命名口径：按 `docs/todo/README.md` §3 第 5 条与 `docs/project/项目规则.md` §2 第 2 条 2.1–2.3，同日同角色只写本文件，后续轮次在文件末尾追加小节。

## 13:30–14:40 （本轮：跨板块改动外置交接 + TODO-USER-107 ② 分类商家列表页 + real 联调清单）

### A. 进度事实

- **本轮任务来源**：负责人（用户本人）指示三件事——① 把工作区里他人板块的改动导成 patch 存到仓库外、还原干净 main；② 开发 `TODO-USER-107 ②` 分类商家列表页；③ 写出 `TODO-USER-003`/`USER-004`/`USER-007` 三页的 real 联调步骤。
- **① 跨板块改动外置交接（已完成）**：把此前会话在 `docs/project/未完成模块与如实标注清单.md`、`docs/todo/后端.md`、`docs/todo/部署.md`、`docs/todo/文档与交付.md` 上的改动导出为 `D:\elm_practice-handoff\2026-09-14-跨板块台账与冻结前清单.patch`（46 insertions / 39 deletions），随后 `git checkout --` 还原为干净 main；**patch 经 `git apply --check` 正向校验通过**，可由组长直接 `git apply` 应用。（过程小插曲：首次误用 `--reverse --check` 得到「不适用」结论，随即改用正向校验得到正确结论，未把错误结论写入台账。）
- **② `TODO-USER-107 ②` 分类商家列表页（已完成实现，未提交）**：
  - 读真源：PRD 7.16.1「分类商家列表页-顶部栏与筛选栏 / 商家列表内容区」两行、契约 §3.2/§1.3、设计导出 `docs/design/exports/用户端/03-搜索与商家列表/02-商家列表搜索结果/`，并对照已完成并合并的 `SearchResultView.vue`（PR #59）复用卡片结构与样式口径。
  - 新增 `src/views/user/CategoryStoreListView.vue`：顶部栏（返回 + 标题）、定位行、排序栏、商家卡（名/评分/月售/时长/距离/起送/配送费/促销标签）、**休息中「不可购买」提示**（PRD 检查列）、四态（首次加载骨架 / 空态「该分类暂无商家」+ 返回 / 失败重试 / 已有结果时失败保留结果 + 重试）、请求序号守卫、切换排序后回到最近可滚容器顶部；路由新增 `category/:categoryId`（name `category-store-list`）。
  - 替身层补齐：`mocks/store.ts` 的 `GET /stores` 按契约支持 `keyword`/`categoryId`/`sort`（含非法 `sort` → 400）；排序实现抽到新文件 `mocks/storeSort.ts`，与搜索域**共用同一份**，避免两处口径漂移；**不传参数时保持种子顺序**，确保首页现有行为与用例不受影响。
  - **TDD 证据**：先写测试并复跑得红（`CategoryStoreListView.vue` 未解析 + 替身 5 failed），实现后转绿 **19/19**；全量 vitest **49 文件全绿**、`npm run build`（含 vue-tsc）✓、E2E（含保真度巡检）**21 项全绿**。红端原始输出存 `D:\elm_practice-handoff\2026-09-14-107②-红端.txt`。
  - **发现并登记的三处口径冲突**（已写入 `docs/todo/用户端.md` §3，未擅自改 PRD/契约）：
    1. **入口**：PRD「首页-分类宫格」行写「点击分类携带分类编号进入分类商家列表」，但同页「分类商家列表页」行与 CHG-001 又定「平台级 10 类不做、分类来源改为店铺自身 `categories`」→ 宫格现有栏目都是超范围占位或「天天爆红包」入口，**没有可传真实 `categoryId` 的入口**，页面已实现但当前不可达。
    2. **分页与筛选**：PRD 817/818 要求「分页参数按契约 §3.2」「下拉加载下一页」且设计稿含「筛选」按钮 **vs** 契约 §1.3「分页接口（§3.6 搜索）是列表约定的唯一例外」、§3.2 的 `GET /stores` 无 `page`/`size` 且返回裸数组。本页**按契约实现**（不分页、无筛选）。
    3. **排序取值**：PRD 817 写「销量、距离、评分」，契约 §3.2 写「综合/销量/距离」→ 本页按契约实现（与搜索结果页同一组选项）。
  - **演示数据观察（如实记录）**：种子中每个店铺的 `categories` 编号互不相同（m002 主食 c101、m001 招牌 c201…），故按 `categoryId` 过滤后**每个分类只对应 1 家店**；页面功能可用，但作为演示页观感有限，若要让「一个分类多家店」成立需调整演示种子（属数据口径，未擅自改）。
- **③ real 模式联调清单（已产出）**：新增 `docs/frontend/用户端-real模式联调清单.md`——含三项先决条件、三种环境（线上部署版 / 本地直连 / vite 代理）、评价「待评价」判定、消息收发与双端未读、评价图片上传三态与越权的分步操作表、失败时先看什么、记录表模板；同步 `docs/project/项目目录索引.md`。
- **本轮最重要的发现（部署侧缺口，已登记）**：`deploy/nginx.conf` **没有 `/uploads/` 映射**，而后端静态映射为 `/uploads/**` 且只监听 `127.0.0.1:4000` → 浏览器请求 `/uploads/xxx.png` 会落到 `location /` 的 SPA 回退（返回 HTML），**部署环境下评价图片与商家商品图必然显示不出来**。修法是 nginx 加 `location /uploads/ { proxy_pass http://127.0.0.1:4000/uploads/; }`；同一处 `client_max_body_size 2m` 偏紧（恰好 2MB 的图可能被 413 拒）。已记入 `docs/todo/用户端.md` §3，归口部署（林晨）/组长路由——**未越界修改部署板块文件**。
- **未做的事（遵守规则）**：**未提交、未推送任何改动**（按 Git 纪律等待负责人明确允许）；未改 PRD/契约（三处口径冲突只登记、待裁定后按 R7 同一 PR 同步）；未修改他人板块台账；未动演示种子数据。

### B. AI 使用点

| # | 任务 | 类别 | AI 做了什么 | 人工审核与修改 | 结果 |
| --- | --- | --- | --- | --- | --- |
| 1 | 跨板块改动外置 | 工程操作 | 生成 patch 并做正向 `git apply --check` 校验后还原工作区；误用反向校验时自行纠正结论 | 负责人指示 | patch 可在干净 main 应用 |
| 2 | 需求口径回读 | 事实核对 | 逐条回读 PRD 817/818、契约 §3.2/§1.3/§10.1 与设计导出，发现 PRD 与契约在三处（入口、分页/筛选、排序取值）互相矛盾，未凭猜测实现，而是按契约实现并把冲突清单化登记 | 待负责人裁定 | 3 处冲突登记 |
| 3 | TDD 实现 | 代码生成 | 先写 13 条页面用例 + 6 条替身用例并复跑取红，再实现页面、路由与替身过滤；把排序实现抽为共享模块避免与搜索域重复 | 负责人需复核页面口径与观感 | 红 → 绿 19/19 |
| 4 | 部署缺口发现 | 风险提示 | 从契约「`/uploads/**` 静态映射」与后端只监听 `127.0.0.1:4000` 推出公网不可达，再核对 `deploy/nginx.conf` 确认缺 `/uploads/` location，给出修法与 413 风险 | 归口部署同学 | 1 处阻塞已登记 |

- **本轮更新的 TODO 编号**：`TODO-USER-107`（② 分类商家列表页实现完成并补证据，仍保持「进行中」——入口未接线、PR 待开）；`TODO-USER-003/007`、`TODO-USER-002`、`TODO-USER-008/003` 三条阻塞行补 2026-09-14 进展（`reviewed` 已随 PR #61 入 main、取消与图片上传接口待发布）；`TODO-USER-107 ②` 新增 3 条阻塞行（入口 / 分页与筛选 / 排序取值）；`TODO-USER-007` 新增 1 条部署侧阻塞行（nginx 缺 `/uploads/` 与 413 风险）。**未替他人改状态**。

## 15:10 （本轮：分类商家列表页入口接线——方案 2）

### A. 进度事实

- **决策来源**：负责人就"入口怎么办"给出选择「2」＝从商家详情页分类区进入。此前已说明三方案（不做入口 / 商家详情页分类区 / 首页宫格）及各自代价，故本轮直接执行。
- **执行前的一次现场核实（改变了实现方式）**：读 `StoreDetailView.vue` 发现左侧**分类栏已绑定「点击分类 → 把外层滚到 Tab 吸顶线、再定位到该商品分区」**，这是项目规则 §5 的 P0 联动，且被 T13/T70 与 `e2e/store-detail-scroll-handoff.spec.ts` 锁着。**若照字面把分类栏做成跳转，会直接破坏 P0 交互**。因此改为把入口放在**商品分区标题行的右侧**（「同类商家 ›」），左分类栏保持原行为；并把"点分类栏不得跳路由"单独固化为 T76 守卫用例。
- **又一处约束**：分区标题有用例做**精确文本断言**（`[data-testid="product-section-c101"]`.text() === '主食'），故入口按钮不能放进 `h2` 内；实现为「标题行容器承载吸顶 + h2 文本不变 + 按钮右置」，并把 sticky 从 `h2` 移到容器（几何与高度不变）。
- **TDD 与回归证据**：先补 T75（入口跳转携带 `categoryId` 与分类名）/T76（分类栏不跳转）→ 复跑 **T75 红、T76 绿**；实现后 **T75/T76 绿**；全量 vitest **49 文件全绿**、`npm run build`（含 vue-tsc）✓、**E2E 21 项全绿**——其中专门跑过 `store-detail-scroll-handoff`（滚动交接）与保真度巡检，用来验证"吸顶由容器承担"这一结构调整没有回归。
- **台账同步**：`docs/todo/用户端.md` 的 `TODO-USER-107` 证据列改为"② 入口已按方案 2 接线"并保留未收口项；§3 的入口阻塞行由"需裁定三选一"改写为"入口已实现，剩余动作＝按 R7 同步 PRD 807 该行的入口位置"。
- **未做的事**：未改 PRD/契约（PRD 807 的入口表述与实现不一致，属口径同步，需裁定后按 R7 处理）；未动分类栏与吸顶的既有交互；未提交未推送。

### B. AI 使用点

| # | 任务 | 类别 | AI 做了什么 | 人工审核与修改 | 结果 |
| --- | --- | --- | --- | --- | --- |
| 1 | 实现前的冲突核实 | 风险提示 | 执行"改分类区"前先读页面代码，发现分类栏已被 P0 滚动联动占用、且分区标题有精确文本断言，主动改方案并把两处约束写成守卫用例，避免按字面实现造成 P0 回退 | 负责人已选方案 2 | 入口落在分区标题行 |
| 2 | TDD 接线 | 代码生成 | 先写 T75/T76 取红，再改模板/样式/脚本；把 sticky 从标题移到标题行容器以保证几何不变 | 负责人需真机瞄一眼入口观感 | 红 → 绿，全量 + E2E 全绿 |

- **本轮更新的 TODO 编号**：`TODO-USER-107`（补入口接线证据与未收口改写）；`TODO-USER-107 ②` 入口阻塞行改写（由"待裁定"改为"待按 R7 同步 PRD 807"）。

## 15:40 （本轮：TODO-USER-106 ③ 台账整理 + 106 收口）

### A. 进度事实

- **任务**：`TODO-USER-106 ③`——整理 `USER-006/028/029/030` 四行的「证据 / 更新」列（9/13 已注明"建议由主责人下次提交时顺手整理"）。
- **问题定性（先看清再改）**：四行的**更新列其实已是单日期**（2026-09-13），真正的混排是**证据列里粘着中间态工作日志**——如"（分支 `feat/confirm-amount-tdd`，待 push/PR）"，而这些分支**早已合并入 main**，这类描述已经失效且会误导读者。
- **处置**：四行证据列统一改为「**交付**（做了什么）/ **证据**（PR 与合并提交号、用例红绿数字、设计真源路径）/ **待拍板**（如 `USER-006` 的两条口径冲突）/ **遗留**（复刻对照截图、真实后端联调，依赖 `TODO-OPS-009`）」四段式；删除已失效的中间态描述；更新列统一 2026-09-14。**任务 / 类型 / 出处 / 负责人 / 状态五列一字未动**。
- **实现方式（吸取 9/13 教训）**：用脚本按「**分区 + 列数**」双条件定位（限定在 `## 2. 已完成` 与 `## 3.` 之间按 ID 精确匹配），并加守卫——分段数不符或证据列内容与预期不符即 **中止且不写入**。**守卫当场生效了一次**：我最初把"8 列表格按 ` | ` 拆分"误算成应为 9 段，脚本中止、文件未被改动；核对后确认 8 列表格拆分本就是 8 段，修正脚本重跑成功。事后三区结构自检（§1 8 列 / §2 8 列 / §3 5 列）全部通过。
- **`TODO-USER-106` 收口**：①②③ 三项子任务均已具备证据，按 `docs/todo/README.md`「完成即填证据并改状态」与「已完成任务保留在已完成区」，该行状态改为「已完成」并由 §1 移入 §2（同样用脚本按双条件定位，并在移动前校验状态确为「进行中」）。
- **整理后板块计数**：§1 进行中 **3 行**（`USER-107`、`USER-025`、`USER-027`）、§2 已完成 **32 行**、§3 阻塞与待确认 **13 行**。
- **未做的事**：未替他人改状态；未改任何行的任务/类型/出处/负责人/状态语义（仅 `USER-106` 自身按规则收口）；未提交未推送。

### B. AI 使用点

| # | 任务 | 类别 | AI 做了什么 | 人工审核与修改 | 结果 |
| --- | --- | --- | --- | --- | --- |
| 1 | 台账批量整理 | 待办维护 | 先判定"混排"的真实形态（更新列已合规、证据列粘工作日志），再用脚本按分区+列数双条件定位改写，保留 PR/提交号/红绿数字/设计真源与遗留项 | 负责人指示整理 | 4 行整理完成 |
| 2 | 守卫拦截自身错误 | 自检 | 脚本守卫按"预期段数"拦截了我自己数错的列数，文件未被写入；核对后修正并重跑 | 无 | 未产生错误写入 |
| 3 | 任务收口 | 待办维护 | 三项子任务均完成 → 改状态并移区，移动前校验原状态以防误移 | 无 | §1 3 行 / §2 32 行 |

- **本轮更新的 TODO 编号**：`TODO-USER-106`（③ 完成 → 整行状态改「已完成」并移入 §2）；`TODO-USER-006/028/029/030`（证据列结构与表述整理，内容与状态未改）。

## 16:10 （本轮：TODO-USER-025 / USER-027 核验与收口）

### A. 进度事实

- **任务**：负责人指示「做 `USER-025` / `USER-027`」。两条的台账状态都是「进行中」，但**证据列写的是"待主责人一句话确认收口"**——所以本轮先做**核验**（判断到底还有没有未交付的东西），再决定是补交付还是收口。
- **核验结论：两条都没有剩余交付物，只差确认**。
  - `USER-025`（PROTO-P2-001 取消订单原型）：设计稿与红框图**均已入库**（`docs/design/exports/用户端/12-订单与支付/03-取消确认弹窗/`、`docs/prd-assets/user/prototype-chg003-orderpay/27_2-01-cancelSheet.png`），清单 §6.2 状态已是「已提供（本期）」；功能实现查证为 `CancelOrderSheet.vue`，**成功与被拒两条分支都有实现**（`emit('cancelled')` / `catch` → 提示并 `emit('rejected')`）**且有用例**：`CancelOrderSheet.spec.ts` **TP-1~TP-7**（含 TP-4 提交成功、TP-5 被拒 409）、`OrderPayView.spec.ts` TP-8/TP-9、`OrderDetailView.spec.ts` OD-3（接单后置灰并提示）/OD-6（已取消展示）。
  - `USER-027`（PROTO-P2-004 图片上传原型）：口径已于 2026-09-11 由负责人拍板改为「内嵌评价订单页、不新增弹窗或页面、商家端商品图不需设计稿」→ 本行**出图交付物按口径取消**，功能实现查证见 `TODO-USER-007`，`ReviewOrderView.spec.ts` **IMG 组 7 条**（IMG-1 上传并预览、IMG-2b 上传中阻止提交、IMG-3a/3b 类型与 2MB 校验、IMG-4 失败保留预览并可重试、IMG-6 删除、IMG-7 三张上限）。
  - 2026-09-14 复跑 `CancelOrderSheet.spec.ts` + `ReviewOrderView.spec.ts` **两文件全绿**，全量 vitest 49 文件全绿。
- **收口依据（为何由本人确认即可）**：两条的「负责人」列都是戴宇涵，证据列写的也是「请**主责人**一句话确认」；且 `USER-027` 的口径变更本身是负责人 9/11 已拍板，本轮只是把账记平。
- **如实标注的两处口径差异（未粉饰、已写进证据列）**：
  1. `USER-025` 原范围写「取消确认、取消成功/失败」——**成功/失败不以独立页面表达**（PRD 7.6 无该页要求、设计稿亦无独立成功/失败屏），实现为「提交成功 → 提示并回订单列表；被拒（已接单/已取消，契约 §3.5 409）→ 弹层内提示原因并通知页面刷新」。是否认可该口径留给负责人复核。
  2. `USER-027` 原范围写「删除/替换」——本期**替换＝先删后加**（契约 §10.1 只提供上传、无删除接口，删除为本地操作）。
- **归属澄清**：`USER-025` 原范围里的「商家端取消状态」属商家端板块 `TODO-MER-002`，不在本行范围内（已在证据列写明，未越界改他人板块）。
- **结果**：`TODO-USER-025`、`TODO-USER-027` 状态改「已完成」并由 §1 移入 §2（脚本按「分区 + 列数 + 原状态」三重校验后执行）。用户端板块现为 **§1 进行中 1 行**（仅 `USER-107`）、§2 已完成 **34 行**、§3 阻塞 **13 行**，三区结构自检通过。
- **未做的事**：本轮**未改任何代码**（核验结论是无剩余工作量，不存在需要补的实现或用例）；未提交未推送。

### B. AI 使用点

| # | 任务 | 类别 | AI 做了什么 | 人工审核与修改 | 结果 |
| --- | --- | --- | --- | --- | --- |
| 1 | 交付物核验 | 事实核对 | 不凭台账措辞判断，逐项回读设计资产目录、组件实现（含失败分支）、清单 §6.2 状态与三份用例清单，再复跑相关用例取证 | 负责人指示「做这两条」 | 确认无剩余交付物 |
| 2 | 口径差异如实标注 | 文档修订 | 把两处与原范围表述不一致的地方（无独立成功/失败页、「替换」＝先删后加）明确写进证据列，并注明归属边界（商家端取消状态属 MER-002） | 待负责人复核该口径 | 2 处差异已标注 |
| 3 | 台账收口 | 待办维护 | 脚本按三重校验（分区 + 列数 + 原状态）改状态并移区，任一不符即中止 | 无 | §1 收至 1 行 |

- **本轮更新的 TODO 编号**：`TODO-USER-025`、`TODO-USER-027`（均 → 已完成并移入 §2，证据列写明交付物、用例编号与两处口径差异）。

## 17:00 （本轮：复刻对照截图补齐 + 阻塞区清理，用户端收尾）

### A. 进度事实

- **任务与授权**：负责人明确「我负责用户端，能做的你直接做」→ 本轮把用户端剩余的"只属于自己、无外部依赖"的收尾项做掉：**复刻对照截图**（§2 里 11 行挂着"待补"）与**阻塞区里那条"已定、不再阻塞"的历史行**。
- **新增截图脚本** `frontend/user-h5/scripts/shot-replica-compare.cjs`：复用 `e2e/design-fidelity.spec.ts` 的既有结论（**会话只在 SPA 内存里、整页刷新会丢会话**，故统一走 `/login?redirect=<目标>` → 点演示账号 → 由 LoginView 客户端跳转），在 mock 模式下对 13 个页面出图，视口 393×852@2x、关动画、等字体就绪；加购→确认订单→提交→支付→取消弹层这类多步流程在**同一个 SPA 会话内**串起来。
- **产出 13 张截图**至 `docs/prd-assets/user/replica-compare-2026-09-14/`（1.9MB）：首页、搜索结果页、分类商家列表页（本次新增页）、确认订单页、确认订单页-红包选择、支付页、取消确认弹层、订单列表、订单详情页、收藏页、会员权益页、红包页、爆红包浮层。抽验了 3 张（搜索结果/分类列表/取消弹层）确认渲染真实内容，不是空白或错误态。
- **未覆盖并如实记录**：**评价提交页与评价图片上传**（`TODO-USER-003`/`007`）——mock 种子只有 `PROCESSING` 订单，评价页需要**已完成订单**才可达，故这两行的"待补"保留并写明原因（待真实环境或发布后 real 模式补拍）。
- **台账回填**：§2 的 11 行证据列把"复刻对照截图待补"替换为"已补（路径 + 对应文件名）"；`TODO-USER-107` 行的两处过期表述一并修掉（"未提交"→"已提交并推送至 PR #63"、"PR 待开"→"PR #63 已开，CI 五项全绿"）。
- **阻塞区清理**：§3 移除 `TODO-USER-006`（红包品类适用范围，自注"已定，不再阻塞"）一行，阻塞行数 **13 → 12**；`USER-010`/`USER-026` 两条"已冻结"承载行按原样保留（属范围决议记录）。
- **两处脚本守卫再次拦住我自己**：① 第一版脚本按 ` | ` 拆列，被 `TODO-USER-008` 证据列内嵌的 ` | ` 拦下（列数 7≠8）→ 改为**整行文本替换 + 短语出现次数必须为 1** 的守卫；② 第二版把 `TODO-USER-107` 放进映射，但它位于 **§1** 而脚本只扫 §2 → 命中 0 次被拦下 → 从映射移除、该行单独用编辑处理。两次都是**中止且未写盘**，文件未产生错误内容。
- **未做的事**：未改任何业务代码（本轮为取证与台账）；未动他人板块。

### B. AI 使用点

| # | 任务 | 类别 | AI 做了什么 | 人工审核与修改 | 结果 |
| --- | --- | --- | --- | --- | --- |
| 1 | 截图脚本与取证 | 脚手架 | 复用 E2E 已有的登录/购物车驱动方式编写独立截图脚本，mock 模式出 13 张图并抽验渲染真实性；对不可达页面（需已完成订单）如实标注而非硬凑 | 负责人指示"能做的直接做" | 13 张截图入库 |
| 2 | 台账回填与清理 | 待办维护 | 11 行证据替换为截图路径、修正 `USER-107` 两处过期表述、移除阻塞区非阻塞行；全程用守卫式脚本（替换次数为 1 才写盘） | 无 | §3 13 → 12 行 |
| 3 | 自检拦截 | 自检 | 两版脚本的守卫分别拦下"内嵌分隔符"与"目标行不在扫描区间"两类错误，均中止未写盘 | 无 | 未产生错误写入 |

- **本轮更新的 TODO 编号**：`TODO-USER-002/005/006/008/028/029/030/104/105`（证据列补复刻对照截图路径）、`TODO-USER-107`（补截图证据 + 修正两处过期表述）、`TODO-USER-003/007`（保留待补并注明"需已完成订单"的原因）；`TODO-USER-006`（§3 非阻塞历史行移除）。

## 17:40 （本轮：负责人验收会话的 bug 复核 + 爆红包响应形状缺陷修复）

### A. 进度事实

- **任务来源**：负责人实际点检用户端后报了四条现象（爆红包点免费爆无红包、消息 Tab 无未读红点、下单后支付页无倒计时且显示"演示支付失败"、返回后又进支付页且无法取消订单），随后补充"购买之后也爆不出具体红包数额"。本轮逐条实测复现与定因。
- **环境与手段**：本机前端 mock 起在 5173；**线上真链路 `82.157.137.114:4001` 用浏览器实点复现**（后端不在本机——开发机无 MySQL/Maven，R8 口径）；接口层用页面内 XHR 抓包核对。
- **逐条定因（详见 `docs/todo/用户端.md` 证据列与 §3）**：
  1. **爆红包"没出红包"**：线上 `POST /me/coupons/blast` 返回 **409「今日免费次数已用完…」**（演示账号当天免费次数已用、且当时无可消耗券）→ 前端按设计进阻塞态引导购买；**非缺陷**，但活动卡按钮仍写「免费爆1次」，属已登记的 `TODO-USER-028`（前端查不到"今日免费次数是否可用"，需后端给字段）。
  2. **消息 Tab 无未读红点**：线上 `GET /me/notifications*` 属批次④、**未发布** → 未读数取不到 → 角标不显示；**本机 mock 下红点正常**（已截图取证）→ 部署问题。
  3. **支付页无倒计时**：后端未返回 `payDeadline`（契约 §3.5 要求）→ 前端按 9/11 定稿口径「未返回则禁用支付、显示 `--:--`」；**实测两个按钮均 disabled → 整页点不动**，P0 主链路断在支付这一步（订单 o1220 可复现）。
  4. **"演示支付失败"**：那是页面上的课程演示按钮（点它走模拟失败分支），非报错文案；因第 3 条同样被禁用。
  5. **返回后又进支付页**：支付页返回＝回订单详情，详情/列表对待支付订单均给「去支付」→ 又回到支付页；叠加禁用即"进得去、点不动、退出来又被引导回去"，属第 3 条的连带观感。
  6. **无法取消订单**：线上 `POST /orders/{id}/cancel` 返回 **404「接口不存在」**（取消是今天 PR #61 才合并、未发布）→ 前端关弹层并刷新订单。
- **一处自我纠正（未改代码，保留为回归锁）**：我最初判定第 6 条"被拒时前端无任何反馈"是缺陷并写了用例准备修，**用例一跑就绿**——核对后确认 `services/http` 在业务码非 0/HTTP 错误时**统一 toast 契约 message**，即提示本来就有（线上那次是我在 toast 自动消失后才查 DOM）。故**未做实现改动**，仅保留 `TP-11` 作为"关弹层 + 给出可见提示"的回归锁（该用例立即通过、非红端，已在提交说明中标注）。
- **真缺陷与修复（本轮实际改动）**：**爆红包在真实模式爆不出数额**——线上抓包证明 `POST /me/coupons/blast`（传 `couponId`）**返回 200 且含 `amount: 8.00`**，但页面渲染为失败态。根因：契约 §3.10 与真实后端返回的是**扁平对象**（券字段 + `tierIndex` + `freeBlast`），而前端类型与替身按 `{ coupon, tierIndex, free }` **嵌套**解析 → 结果卡拿不到金额（替身按前端形状返回，所以本机用例全绿、只有真实模式暴露）。
  - 修复：新增 `normalizeBlastResult`（`services/normalizers.ts`，唯一出口适配，已是嵌套形状则原样透传）并在 `services/api/coupon.ts:blastCoupon` 接入；新增类型 `CouponBlastRecord` 描述契约形状；替身形状不动（避免波及 RB/RBM 组用例），另登记技术债 `TODO-USER-110`。
  - 证据：BL-1~BL-3 红（`normalizeBlastResult is not a function`）→ 绿；全量 vitest **49 文件全绿**、`npm run type-check` ✓、`npm run build` ✓（类型检查拦下测试里 `scope: string` 未收窄为字面量联合类型，已修）；**真后端端到端**：本机 real 模式（5174 经 vite 代理到 `82.157.137.114:4001`）点「消耗一张红包再爆」→ 结果卡显示 `¥15 / 满25元可用 / 限今天 23:59 前使用`（修复前为失败态）。
- **另一处真缺陷（未修，已登记）**：**桌面宽度下整页按 vw 放大 ~3 倍**（见 `TODO-USER-109`）——窗口 1163px 宽时购物车栏高 **317px、盖住半屏**、加购按钮被遮挡点不动；430px 宽时正常（71px 贴底）。根因是 px→vw 按 390 设计宽换算而 `.app-shell` 只锁宽度不锁缩放（SHOW-QA-002 同类问题的残留面）。**需负责人定口径**：演示固定窗口宽度（零改动）或作为技术债修；同时提请测试联调同学登记缺陷台账。
- **工具经验（如实记录）**：第一次抓包用 `window.fetch` 补丁**抓不到任何请求**——本项目请求走 axios（XHR），改补丁 `XMLHttpRequest.prototype` 后才抓到 `POST /me/coupons/blast` 的请求体与响应，这是本次定位的关键一步。
- **未做的事**：未改契约（前端适配契约，未反向改真源）；未动替身形状与 RB/RBM 用例；未修 `payDeadline`（后端职责）与桌面缩放（待口径）。

### B. AI 使用点

| # | 任务 | 类别 | AI 做了什么 | 人工审核与修改 | 结果 |
| --- | --- | --- | --- | --- | --- |
| 1 | 现象实测定因 | 排障 | 在 mock 与线上两条链路分别复现同一操作，用 XHR 抓包核对请求/响应，把 6 条现象逐条归因到"未发布 / 后端缺字段 / 设计内降级 / 真缺陷"四类，避免把部署问题当代码 bug | 负责人提供现象 | 6 条定因 |
| 2 | 自我纠正 | 自检 | 先用 TDD 写"被拒需可见提示"的用例，用例立即通过 → 反查发现 http 层已有全局 toast，**据此撤回修复意图**并保留回归锁，未产生无意义改动 | 无 | 未误改 |
| 3 | 契约一致性修复 | 代码生成 | 抓包确认真实响应为扁平形状后，在 api/normalizer 层做归一化（兼容两种形状），并补 BL 组用例取红再转绿；真后端端到端验证 | 负责人需复核展示口径 | 红→绿 + 真实环境验证 |
| 4 | 缺陷登记 | 待办维护 | 新增 `TODO-USER-108`（已修复，含证据）、`TODO-USER-109`（桌面缩放，待口径）、`TODO-USER-110`（替身形状对齐技术债） | 无 | 三区结构自检通过 |

- **本轮更新的 TODO 编号**：新增 `TODO-USER-108`（爆红包响应形状缺陷 → 已完成）、`TODO-USER-109`（桌面缩放 → 阻塞区待口径）、`TODO-USER-110`（替身与契约 §3.10 形状对齐 → 进行中技术债）。

## 18:10 （本轮：负责人追问"消耗券爆失败是不是 bug" → 错误分类缺陷修复）

### A. 进度事实

- **追问与复现**：负责人在本机 real 模式（5174）看到「消耗一张红包再爆」显示**"爆红包失败，请重试"**，问"这里不是 bug 吗"。我先自己跑同一路径 → **成功**（结果卡 `¥8 / 满25元可用 / 限今天 23:59 前使用`，修复已生效）；再用接口核对后端语义：拿**已使用/不可爆**的券去爆 → **409「红包已使用」**，拿**不存在的券** → **404「红包不存在」**。
- **定因**：负责人那次是**那一次请求真的失败了**（页面持有的券已失效/会话或网络因素），但**前端把它显示成"网络异常，请重试 / 未消耗免费次数与红包"属于错误分类**——`burst()` 的 catch 只把 409 当阻塞态，其余一律当失败态，于是 4xx **业务拒绝**（券不存在、他人券、参数非法）被误报为网络异常，**且不刷新券列表**，用户只能拿同一张失效券反复重试。**这是真缺陷，已修**。
- **修复**：`BlastOverlay.burst()` 的 catch 改为**按状态码分类**——4xx → 阻塞态并 `emit('done')`（红包页重读列表，刷新可消耗券）；仅网络异常/服务端 5xx → 失败态可重试。
- **证据**：RB-8 红（`expected true to be false`：业务拒绝时不应出现失败态）→ 绿；RB-1~RB-8 全绿、全量 vitest **49 文件全绿**、`npm run type-check` ✓、`npm run build` ✓（本轮两次修复的回归合跑）。
- **台账**：新增 `TODO-USER-111`（爆红包业务拒绝被误报为网络异常 → 已完成，含证据与后端语义引用）。
- **未做的事**：未改后端与契约（后端语义正确，前端适配）；**本地提交与推送仍未成功**——本机代理未运行、直连 GitHub 被重置，待负责人恢复代理后推送。

### B. AI 使用点

| # | 任务 | 类别 | AI 做了什么 | 人工审核与修改 | 结果 |
| --- | --- | --- | --- | --- | --- |
| 1 | 现象定因 | 排障 | 先自测同一路径确认修复生效，再用接口分别验证"不可爆券/不存在券"的后端语义，据此判定"显示成网络异常"是前端错误分类而非后端问题 | 负责人提出质疑 | 1 处真缺陷 |
| 2 | 分类逻辑修复 | 代码生成 | 把 catch 从"只认 409"改为"4xx 全部按业务拒绝处理 + 触发列表刷新"，补 RB-8 取红再转绿 | 待负责人复核展示口径 | 红→绿，全量绿 |

- **本轮更新的 TODO 编号**：新增 `TODO-USER-111`（已完成）。

## 18:40 （本轮：入口位置调整——从食物分类标题旁挪到店名旁）

### A. 进度事实

- **负责人指示**：「同类商家不要放在具体的食物旁边，放在店名旁边」。
- **调整**：把 `TODO-USER-107 ②` 的入口从**商品分区标题行右侧**（`section-more-{categoryId}`，每个食物分类各一个）移到**店名右侧**（`store-similar-btn`，位于 `store-banner` 的店名行内、收藏 icon 之前）；每个食物分类旁的入口**全部移除**。参数改取**当前选中分类**（默认第一个有商品的分类），语义为"看当前这一类别的同类商家"；无分类时入口不渲染。
- **顺带回退**：入口搬走后，吸顶恢复为分区标题自身（`.product-list-title` sticky），此前为容纳按钮而引入的「分区标题行容器吸顶」结构一并撤销，几何回到调整前。
- **证据**：T75 改为断言「入口在店名之后且 banner 内、不再存在 `section-more-*`、点击携带当前分类（m002 → c101 主食）」→ 先红后绿；T76（点左分类栏不跳路由）保持绿；全量 vitest **49 文件全绿**、`npm run type-check` ✓、`npm run build` ✓、E2E **21 项全绿**（含滚动交接与保真度巡检，验证吸顶回退无回归）；5174 real 模式页面截图确认入口位于店名右侧。
- **台账**：`TODO-USER-107` 证据列与 §3 入口行按新位置改写（原「分区标题行」表述已过期）。
- **未做的事**：未改 PRD（PRD 807 的入口位置仍待按 R7 裁定同步）；未提交本轮的推送（待代理可用）。

### B. AI 使用点

| # | 任务 | 类别 | AI 做了什么 | 人工审核与修改 | 结果 |
| --- | --- | --- | --- | --- | --- |
| 1 | 入口位置调整 | 代码生成 | 按指示把入口搬到店名旁并撤销为容纳按钮而引入的吸顶容器改动；参数改用当前选中分类以保留语义 | 负责人提出位置要求 | T75 红→绿，全量 + E2E 全绿 |
| 2 | 口径同步 | 文档修订 | 同步台账中"入口在分区标题行"的过期表述（证据列 + §3 两处） | 无 | 表述与实现一致 |

## 19:00 （本轮：桌面缩放缺陷口径落定——选 1，修法转技术债）

### A. 进度事实

- **负责人裁定**：对 `TODO-USER-109`（桌面宽度下整页按 vw 放大 ~3 倍）明确选择 **方案 1「演示时固定窗口宽度」**，不修代码。
- **落盘**：
  - `docs/presentation/README.md` 新增「演示环境须知」段：用户端按移动 H5 演示，窗口保持 **390–430px**（或移动视口/已内嵌的 390px iframe）；宽窗口全屏时购物车栏会放大到约 317px、盖住半屏并遮挡加购按钮（实测 1163px 宽；430px 宽时 71px 正常）。
  - `docs/todo/用户端.md`：`TODO-USER-109` 由「待裁定（两方案）」改写为「**已定：不改代码、按演示口径规避**」，并记录实测依据与既有记录一致性；**新增 `TODO-USER-112`（§1 进行中，技术债）**承载"缩放上限"的正式修复，明确冻结后处理。
- **一致性核对（避免重复造轮子）**：`docs/presentation/engineering-audit.md` 第 66 行本就记录过同类已知限制（SHOW-QA-002 的 px→vw 缩放问题仍在、本轮演示以 390px iframe 与移动视口运行），本次裁定与该既有口径一致，仅补充了实测数字与"外壳已修但缩放未修"的边界说明。
- **未做的事**：**未改任何代码**（本口径即"不改代码"）；未动 PRD/契约。

### B. AI 使用点

| # | 任务 | 类别 | AI 做了什么 | 人工审核与修改 | 结果 |
| --- | --- | --- | --- | --- | --- |
| 1 | 口径落盘 | 文档修订 | 把负责人的选择写进演示材料与台账（含实测数字、既有记录引用），并把正式修复登记为技术债行，避免"裁定后无人修"的漏项 | 负责人裁定"就按 1" | 1 处演示须知 + 2 行台账 |

- **本轮更新的 TODO 编号**：`TODO-USER-109`（→ 已定：演示口径规避）；新增 `TODO-USER-112`（缩放上限技术债，冻结后处理）。

## 19:40 （本轮：payDeadline 两端修复——后端补字段 + 前端改为"缺失不阻断支付"）

### A. 进度事实

- **决策来源**：负责人就"要不要我直接帮后端改"选择**「都改」**——后端补 `payDeadline` 字段，前端同时改为"字段缺失不再禁用支付"，两头都做，发布一次即可让线上支付页恢复正常。
- **后端（契约 §3.5，填空式实现）**：
  - `OrderMapper.COLS` 已按 `DATE_FORMAT(created_at,'%Y-%m-%d %H:%i:%s')` 返回 `createdAt`，**SQL 无需改动**；
  - 新增 `Times.plusMinutes(time, minutes)`（同文本口径的加法）；
  - `ViewMapper.order` 增一行：`payDeadline = createdAt + 15 分钟`（派生于 createdAt、与订单状态无关）；
  - 契约 §3.5 去掉「（本期实现（待开发））」并补实现回写（R7 同批）。
- **后端 TDD 与 R8 验证（真在服务器测试库上跑）**：
  - 新增 `OrderServiceTest.orderViewCarriesPayDeadlineFifteenMinutesAfterCreatedAt`：断言订单视图**必须含** `payDeadline` 且等于 `createdAt` + 15 分钟；
  - **红端（服务器取证）**：把 `ViewMapper` 的该行临时去掉后跑 `mvn -B test -Dtest=OrderServiceTest` → `Tests run: 8, Failures: 1`，`payDeadline 不能缺失 ==> expected: not equal but was: <null>`；随后恢复该行；
  - **绿端**：`DB_NAME=elm_practice_test mvn -B test`（日志首行确认 `DB_NAME=elm_practice_test`，业务库未触碰）→ **77/77 通过、BUILD SUCCESS**（较修复前 +1 条）。
- **前端（口径修订 + 替身对齐）**：
  - `OrderPayView`：`payDisabled` 去掉 `remain === null` 条件 → 字段缺失只显示 `--:--`、**不禁用支付**，仅"已过期"才禁用；注释同步口径变更缘由；
  - 替身 `mocks/order.ts`：支付超时判定原用 `payDeadline`（缺失时 `remainingSeconds` 返回 0 → **误判超时**），改为与真实后端一致按 `createdAt` + 15 分钟派生；
  - 用例 `TD-3` 改写为「缺失 → 不禁用且能走到支付成功页」→ 红（仍禁用）→ 绿；
  - 前端回归：全量 vitest **49 文件全绿**、`npm run type-check` ✓、`npm run build` ✓。
- **台账**：`TODO-USER-008/003` 阻塞行的 `payDeadline` 部分改为「已修复（后端派生 + 前端不阻断），剩余＝发布后复验」；新增 `TODO-USER-113`（前端口径修订，已完成，含红绿与后端说明）。**后端板块的 `TODO-BE-*` 行由其后端负责人登记**（本轮只在我方板块记录与该阻塞行的解除）。
- **未做的事**：未改 PRD；未动其余接口；未发布（发布后需按 `docs/frontend/用户端-real模式联调清单.md` 复验支付页倒计时与支付按钮可用性）。

### B. AI 使用点

| # | 任务 | 类别 | AI 做了什么 | 人工审核与修改 | 结果 |
| --- | --- | --- | --- | --- | --- |
| 1 | 后端字段实现 | 代码生成 | 定位最小改动面（SQL 已含 createdAt，仅需视图派生），新增 `Times.plusMinutes` 与一行视图字段，并补一条断言性用例 | 负责人裁定"都改" | 77/77 全绿 |
| 2 | 红绿取证 | 测试执行 | 在服务器测试库上先移除实现跑出红（`Tests run: 8, Failures: 1`）再恢复跑绿，避免"只跑绿端" | 无 | 红绿证据齐 |
| 3 | 前端口径修订 | 代码生成 | 解除"字段缺失即禁用支付"，并把替身的超时判定对齐真实后端（原来按缺失误判超时） | 负责人裁定 | TD-3 红→绿，全量绿 |

- **本轮更新的 TODO 编号**：`TODO-USER-008/003`（阻塞行解除：payDeadline 已修复，待发布复验）；新增 `TODO-USER-113`（前端口径修订，已完成）。

## 20:10 （本轮：支付问题真后端复验）

### A. 进度事实

- **任务**：负责人要"检查支付的问题"，本轮先自验全链路，再交步骤给对方。
- **环境**：本机 real 模式 dev server（5174，经 vite 代理打到线上后端 `82.157.137.114:4001`），**后端仍是 9/13 版本、尚未返回 `payDeadline`**；线上站点（4001）仍是旧前端，故支付页在 4001 上依旧点不动——验证必须在 5174 做。
- **复验结果（下单 → 支付 → 列表）**：
  - 新建订单 o1252 → 支付页倒计时 `--:--`，**「立即支付 ¥97.00」与「演示支付失败」两个按钮均可点**（修复前两者皆 disabled）；
  - 点「立即支付」→ 进入**支付成功页**（¥97.00 / 模拟支付 / 支付时间 2026-09-14 20:03:32）；
  - 回订单列表：该单显示**「待接单」**，状态机与金额链路正常；
  - 倒计时待后端 `payDeadline` 随发布上线后自动显示（前端已按契约接好）。
- **未验到的一项**：「演示支付失败 → 支付失败页」此次未跑通，原因是复验中会话过期 + 我选到的标签页非登录态（**非应用缺陷**，该路径有 TD-4/TD-5 单测覆盖，且按钮已被证实可用）。
- **未做的事**：未改代码（本轮为验证与台账）；未在 4001 上做同样验证（旧前端，必然点不动）。

### B. AI 使用点

| # | 任务 | 类别 | AI 做了什么 | 人工审核与修改 | 结果 |
| --- | --- | --- | --- | --- | --- |
| 1 | 真后端复验 | 质量巡检 | 在 real 模式（5174 代理线上后端）走完下单→支付→列表，确认"字段缺失不再阻断支付"在真实后端上生效，并把证据写回台账 | 负责人要"检查支付" | 支付链路可用 |
| 2 | 失败路径如实记录 | 自检 | 未跑通的失败路径如实记为"会话过期+选错标签页、非应用缺陷"，并注明由单测覆盖 | 无 | 未夸大结论 |

- **本轮更新的 TODO 编号**：`TODO-USER-113`（证据列补真后端复验结果：支付链路恢复可用）。

## 22:35 （本轮：线上 22:00 发布后真后端联调复验）

### A. 进度事实

- **任务来源**：负责人指示「现在调用一下远程的后端吧，同学他们做好了」→ 本轮以**线上演示环境为真源**做用户端真后端复验（不再走本机代跑推测）。
- **环境与版本**：线上 `http://82.157.137.114:4001/user/`，发布 **`20260914-a2f15b1`**（`/srv/elm-current`，服务器时间戳 2026-09-14 21:59–22:00，即今日全部合并后的首次上线；后端 `127.0.0.1:4000` 经 Nginx 代理）。**注意**：本轮首两次探测（接口位点、分类页/消息页/支付页步进）跑在**切版之前**的 9/13 版 `707f234` 上（当时无 `payDeadline`、无取消/规格/图片接口），发现切版后**全部结论已重测**，本记录只保留新版本（`20260914-a2f15b1`）上的结果。
- **工具**：项目自带 Playwright（`channel: chrome`，视口 390×844）+ 接口级对照请求；脚本为一次性工具（`frontend/user-h5/scripts/real-*.cjs`），**跑完即删，不入库**。
- **验证通过（新版本）**：
  - **分类商家列表页（本分支 `TODO-USER-107 ②`，PR #63）线上可用**：商家详情店名旁「同类商家 ›」→ `/user/category/c101?name=主食`（标题「主食」）→ 真实 1 家店（肯德基宅急送 4.8 / 月售 3500 / 25 分钟 / 起送 ¥20.00 / 配送 ¥5.00）；排序切换带参重发（`categoryId=c101&sort=销量|距离`）；`/category/c999` → 空态「该分类暂无商家」+ 返回（接口返回空数组，非 404）。
  - **P0 下单→支付→取消全链路通过**：新单 `o1257`（加购 2 件）→ 结算页金额四行（商品小计 ¥39.00 / 打包费 ¥2.00 / 配送费 ¥5.00 / 实付金额 ¥46.00）→ 支付页倒计时 **14:58 → 14:53 真实走秒**（`payDeadline` 已生效）、「立即支付 ¥41.00」与「演示支付失败」**均可用** → 用支付页取消入口 + 快捷原因「不想要了」取消 → 列表显示「已取消」，接口核对 `status=CANCELLED`/`cancelReason=不想要了`/`cancelledBy=USER`/`cancelledAt=2026-09-14 22:17:59`。
  - **超时判定正确**：16:16 的旧待支付单显示「已失效」且支付按钮禁用（非缺陷）。
  - **排序实测（接口级，6 家店）**：`sort=销量` **生效**（m002 3500 → m003 2800 → m001 1200 → m005 950 → m004 800 → m1053 0）；`sort=距离`、`sort=综合`、`sort=评分` 均为默认 `store_id` 序，且 `/stores` 响应**无距离字段** → 「距离」项在页面上恒不显示（前端「缺失即隐藏」正确）。
  - **未实现接口（前端降级正常）**：`/api/v1/search`（批次⑤）、`/me/notifications` 与 `/unread-count`（批次④通知部分）、`/me/favorites`、`/me/member` 均 `404 40401 接口不存在`；搜索结果页显示失败态「接口不存在 + 重试」并保留关键词。
- **新发现 3 条缺陷（已代拟台账条目，编号/严重程度/状态待测试联调同学确认）**：
  - **BUG-20260914-003（建议 P1）公开页整页刷新后客户端会话未恢复** → 商家详情点「+」加购被误判未登录、提示「请先登录」并跳 `/login?redirect=/stores/m002`，**该次点击前后无任何 `/api/` 请求发出**；同刻 `GET /cart?storeId=m002` 返回 200（会话有效），商家详情加载期亦无 `/me` 探活。根因：`StoreDetailView.vue:381/178/435` 只读 `sessionStore.isLoggedIn`，而该状态仅在 `meta.auth` 路由由守卫 `checkLogin()` 恢复（`CouponView`/`OrderListView` 等已先 checkLogin，本页未做）。对照：连续 6 次整页打开 `/stores/m002` 均停留原页（导航本身正常，缺陷由点加购触发）。
  - **BUG-20260914-004（建议 P2）消息中心 `Promise.all` 连带失败**：`GET /conversations` 200/**19 条**、`/me/notifications` 404 → `loadAll` 整体抛出 → 页面显示「暂无消息」，19 条真实会话全部不可见且无错误提示。对照实验：把通知接口拦截为 200 空列表后 `isEmpty` 由 true 变 false，证明会话数据其实已拿到。
  - **BUG-20260914-005（建议 P2）会话对象缺 `storeId` 致整页白屏 + 前后端字段口径不一致**：线上返回 `merchantId`/`updatedAt`/`unreadCount`/`userNickname`，用户端按自拟 `storeId`/`lastMessageAt`/`unread` 渲染且 `listConversations` 未走归一化（架构约定 §3.3）→ `storeName(item.storeId).slice(0,1)` 抛 `TypeError: Cannot read properties of undefined (reading 'slice')`，页面只剩底部导航。契约 §6.1 未定义会话列表对象字段；商家端已按 `unreadCount` 归一化 → 需 R7 回写契约 + 一端对齐。
- **本轮落盘（工作区改动，未提交）**：
  - 新增 `docs/testing/问题记录/2026-09-14-用户端真后端联调发现.md`（三条缺陷 + 分类页真实行为 + 请求/响应摘要，AI 代拟待确认）；
  - `docs/testing/缺陷跟踪表.md`：§2 新增 3 行（BUG-20260914-003/004/005）+ §3 新增 3 个详情块（均标注 AI 代拟、状态「新建」待确认）；
  - `docs/todo/用户端.md`：**新增 `TODO-USER-114`（会话恢复/加购被拦）、`TODO-USER-115`（消息中心 004+005）**；补 `TODO-USER-113`（支付链路 22:00 发布后线上复验通过）、`TODO-USER-107 ②`（分类页线上复验 + 排序实测：销量生效/距离无效）、`TODO-USER-005`（搜索 real 404）、`TODO-USER-004`（消息中心线上不可用）；
  - `docs/frontend/用户端-real模式联调清单.md`：填 §5 记录表 + 新增 §6「2026-09-14 22:00 发布后的实测补充」（含「联调时勿用整页刷新进商家详情，否则加购必被拦」的规避提示）。
- **未做的事**：未改任何业务代码（本轮为验证与台账）；未提交、未推送（按 Git 纪律等负责人指示）；未改他人板块台账（`docs/todo/后端.md`、`部署.md` 均未动，后端侧缺口只在问题记录与本板块行内如实转述）；未做商家端验证（不在本板块范围）；评价提交/图片上传的真实联调未执行（前者需 `COMPLETED` 未评价订单，后者受 nginx 缺 `/uploads/` 代理阻塞）。

### B. AI 使用点

| # | 任务 | 类别 | AI 做了什么 | 人工审核与修改 | 结果 |
| --- | --- | --- | --- | --- | --- |
| 1 | 远端能力盘点 | 事实核对 | 以接口探测 + 源码/发布目录核对定位线上真实版本（发现首轮探测跑在切版前，识别出 `20260914-a2f15b1` 已上线并**把全部结论重跑**，避免把旧版行为写成新缺陷） | 负责人指示「调用远程后端」 | 版本与接口位点清楚 |
| 2 | 真后端端到端复验 | 质量巡检 | 用项目自带 Playwright 在**线上入口**跑真实登录/加购/结算/支付/取消与分类页排序，逐项留证据（倒计时 14:58→14:53、`CANCELLED` 字段、请求清单） | 无 | 5 项通过、1 项降级正常 |
| 3 | 缺陷定位 | 代码定位 | 对三条失败项做根因定位（会话恢复读 `isLoggedIn`、`Promise.all` 连带、未归一化缺字段抛 `.slice`）并用对照实验（拦截通知接口为 200 空列表）证明因果，而非停在现象 | 无 | 3 条缺陷可复现可定位 |
| 4 | 工具坑自我纠错 | 自检 | 初次用 Git Bash `curl --data-urlencode` 传中文参数（被按 GBK 编码）误判「排序全无效」，改用 Python 精确控制 UTF-8 后**推翻该结论**并纠正为「销量生效、距离无效」 | 无 | 结论已更正 |
| 5 | 台账与证据落盘 | 文档修订 | 按 §1.1/§1.2 规则代拟缺陷条目（编号递增、字段齐全、红绿待补）、补待办证据列与联调清单记录表，并如实标注「AI 代拟待确认」与「未做」项 | 无 | 4 份文档改动（未提交） |

- **本轮更新的 TODO 编号**：新增 `TODO-USER-114`、`TODO-USER-115`；更新 `TODO-USER-113`、`TODO-USER-107 ②`（主行 + 阻塞区新增复验行）、`TODO-USER-005`、`TODO-USER-004`。缺陷台账新增 `BUG-20260914-003/004/005`（代拟待确认）。

## 23:05 （本轮：AI 点餐助手前端开发——PRD 落地与 TDD 闭环）

### A. 进度事实

- **任务来源**：负责人「现在上传了一个PRD，是在页面加ai功能的，你找一下然后开始开发」→ 定位到刚推上来的 `docs/frontend/AI点餐助手前端PRD.md`（main `b4ff860`，251 行，验收项 AI-FE-01~12），对应 PRD 6.11.1 / SRS 5.15 / 契约 §10.6 的「AI 点餐助手」（P2 本期，PRD 明确「用户端对话入口（前端页面待实现）」，后端已由 #64 交付）。
- **分支**：从 `origin/main`（`660dfbc`）新建 `feat/ai-chat-page-tdd`（我本地此前的两个文档改动随分支安全带过——切换前已核对相关文件在两分支内容一致）。
- **交付内容（全部为新增文件 + 4 处接线）**：
  - `views/user/AiChatView.vue`：对话页——顶部栏（返回/「小饿 AI 点餐助手」/副标题「智能推荐 · 只做建议不下单」/清空）、消息区（用户右橙气泡、AI 左灰气泡带 🤖、流式逐字、「正在思考…」三点）、快捷提问栏（PRD §3.4 五词逐字）、输入区（空输入禁用、回车发送）、网络异常提示；**只做推荐，页面无任何下单/支付入口**（AI-FE-12）。
  - `utils/aiChatSession.ts`：`ai_session_id` + `ai_messages_{sessionId}`（上限 50 条），损坏/隐私模式静默降级。
  - `utils/aiReply.ts`：Markdown 轻解析为**结构化块**（加粗/列表/商家编号），渲染层用插值（不拼 HTML）→ 无 XSS 面。
  - `services/api/ai.ts` + `types/endpoints/barrel`：`chat`（非流式，30s 超时）+ `streamChat`（SSE `data:` 分片解析）；错误归一 `AiUnavailableError`(503/50301) 与 `AiTimeoutError`；mock 模式由替身回复按分片节流。
  - `mocks/ai.ts`：离线知识库**直接由演示种子构成**（STORES / ALL_PRODUCTS / CATEGORIES_BY_STORE），按店铺名（含 3 字滑窗，命中「肯德基→肯德基宅急送」）/辣味/便宜/饮品/优惠分流，多轮记忆按 `sessionId` 记住上一轮店铺；知识库没有的如实说「暂时没有」且不编出商家编号；另给 `setAiMockUnavailable()` 复现 503 错误态。
  - 三处入口：`HomeView`（右下角悬浮球 56px 亮橙、避开 TabBar；搜索框「AI 推荐」按钮带预填提示词且不触发搜索跳转）、`MineView`（常用功能新增「AI 点餐助手」条目带图标）、`router`（`ai-chat` 无底部导航、未登录可用）。
- **TDD 红绿**：
  - **红端**：5 个新测试文件（AS 会话状态 / AR 回复解析 / AM 替身 / AA 接口层 / AI 对话页）+ HomeView 2 条 + MineView 1 条 → `Test Files 5 failed (5)`（被测模块未创建）+ HomeView **2 failed** + MineView **1 failed**（入口不存在）。
  - **绿端**：AI 相关 **44 条**（AS 6 + AR 7 + AM 7 + AA 6 + AI 15 + 入口 3）全绿；全量 vitest **54 文件 / 426 用例全绿**、`npm run type-check` ✓、`npm run build` ✓、E2E **21 项全绿**（含保真度巡检与多视口断言，悬浮球未破坏首页结构断言）。
  - **一处实现期自查**：首版把消息对象直接改字段（绕过 Vue 响应式代理）→ 超时/降级路径 DOM 不刷新（3 条用例红）→ 改为**按 id 取代理再改**后全绿；该教训已写入代码注释。
- **浏览器实测（mock 模式 5173，Playwright）**：首页悬浮球 → `/ai-chat`；欢迎语 + 5 条快捷提问；点「我想吃辣的」→ 流式分片渲染「香辣鸡腿堡 ¥19.50 · 肯德基宅急送 [m002]」；点 `[m002]` → `/stores/m002`；整页刷新后 3 条消息与 `sessionId` 保持且不重复欢迎语；清空 → 仅欢迎语 + 新 `sessionId`；搜索框入口 `?prompt=…` 只预填不自动发送；**无控制台/页面错误**。
- **线上探测（重要发现）**：`POST http://82.157.137.114:4001/user/api/v1/ai/chat` → **503 `{code:50301}`「AI 点餐助手未配置 DEEPSEEK_API_KEY」**——线上后端未注入该环境变量（契约 §10.6 要求环境变量注入、不入仓库）→ **real 模式当前不可用**；前端已按 PRD §6.3 正确处理该错误态（顶部提示「AI 助手暂不可用，请稍后再试」）。属部署项，已登记 `TODO-USER-116 ③`。
- **口径差异（需 R7）**：① PRD §5.1 写「首次调用 `/chat` 不传 sessionId、后端返回」vs 实测 `/stream-chat` 不回 `sessionId`（按字面实现则首答无法流式）→ 实现改为**前端生成 sessionId 并随每次请求携带**（契约 §10.6 允许传入）；② PRD §4.2 写跳 `/store/m002`，工程实际路由为 `/stores/:storeId` → 实现按实际路由。两条已登记本板块 §3 待裁定。
- **本轮落盘（工作区改动，未提交）**：新增前端 10 个文件（含 5 个测试文件）；修改 `mocks/index.ts`、`router/index.ts`、`services/api/{endpoints,index,types}.ts`、`views/user/{HomeView,MineView}.vue` 及其两个 spec（仅加用例与路由）；`docs/todo/用户端.md` 新增 `TODO-USER-116` + 3 条阻塞行。
- **未做的事**：未提交、未开 PR（按 Git 纪律等负责人指示）；未改 PRD（口径差异只登记不擅改，等裁定）；未改他人板块台账（线上 key 缺失只在报告与 `TODO-USER-116 ③` 中如实转述）；未补复刻对照截图（该页无设计稿）；未加 E2E 用例。

### B. AI 使用点

| # | 任务 | 类别 | AI 做了什么 | 人工审核与修改 | 结果 |
| --- | --- | --- | --- | --- | --- |
| 1 | PRD 定位与缺口分析 | 事实核对 | 依据 `git fetch` 发现远端新增 `b4ff860`（前端 PRD）并读取全文，与后端已交付的 `AiChatController` 逐字段对齐，识别出「`/stream-chat` 不回 sessionId」这一影响实现的实测差异 | 负责人指示开发 | 实现路径明确 |
| 2 | 用例先行 | 测试设计 | 按 AI-FE-01~12 与 PRD §3~§7 设计 44 条用例（会话状态/解析/替身/接口层/页面），先跑出红端再实现 | 无 | 红端 5 文件 + 3 条入口用例 |
| 3 | 实现 | 代码生成 | 新增对话页、会话与解析工具、接口层与离线替身、三处入口与路由；替身知识库复用演示种子保证 mock 与真后端同口径 | 无 | 全绿 + 构建通过 |
| 4 | 自查纠错 | 自检 | 实现期用最小复现定位「绕过响应式代理导致 DOM 不刷新」，改为按 id 取代理改写并补注释；另修正类型检查暴露的 3 处类型错误 | 无 | type-check/build 通过 |
| 5 | 真机验证 | 质量巡检 | 在 mock 模式 dev server 上跑浏览器实测（入口/流式/跳转/刷新保持/清空/预填），并顺带探测线上接口发现 503（key 未配置） | 无 | 全流程通过 + 1 项部署缺口 |
| 6 | 台账与留痕 | 文档修订 | 新增 `TODO-USER-116`（含红绿/R1/浏览器证据）与 3 条阻塞行（两处口径 R7 + 线上 key），并按规则标注未收口项 | 无 | 1 份台账改动（未提交） |

- **本轮更新的 TODO 编号**：新增 `TODO-USER-116`（AI 点餐助手前端页面，进行中）及其 §3 阻塞行 ①②③。

## 23:15 （本轮：AI 对话页顶部间距走查修复）

### A. 进度事实

- **来源**：负责人走查「小饿 AI 点餐助手太靠近屏幕上面了，往下移一点」。
- **改动**：`views/user/AiChatView.vue` 的 `.ai-page` 增加 `padding-top: max(12px, env(safe-area-inset-top))`（桌面/普通浏览器 12px，与设计令牌 `--spacing-page` 同值；刘海屏独立打开时取安全区高度，`index.html` 已 `viewport-fit=cover`，与底部栏 `env(safe-area-inset-bottom)` 同口径）。**顶部栏本体仍按 PRD §3.2 保持 48px**，只把整页内容下移，不新增页面结构。
- **实测（mock 模式 5173，Playwright，390×844）**：改前顶部栏顶端 `y=0`（贴屏幕最上沿）；改后 `y=12`，标题/副标题/欢迎语整体下移 12px，截图对照 `ai-02-entry.png`。
- **回归**：全量 vitest **54 文件 / 426 用例全绿**、`npm run build` ✓（纯样式改动，无用例口径变化）。
- **留痕联动**：`docs/todo/用户端.md` 的 `TODO-USER-116` 证据列补该条走查与实测几何。

### B. AI 使用点

| # | 任务 | 类别 | AI 做了什么 | 人工审核与修改 | 结果 |
| --- | --- | --- | --- | --- | --- |
| 1 | 视觉走查修复 | 代码生成 | 按负责人口径加顶部间距，并对齐工程既有安全区写法与设计令牌；用浏览器量取改前/改后顶部几何作为证据 | 负责人走查提出 | 顶部下移 12px，回归全绿 |

- **本轮更新的 TODO 编号**：`TODO-USER-116`（证据列补顶部间距走查修复）。

## 23:30 （本轮：AI 对话页改为三行固定布局——顶部栏/输入框不动，只滚聊天记录）

### A. 进度事实

- **来源**：负责人走查「ai助手最上面的横框和最下面的聊天框不动，滑动页面只是中间的聊天记录在动」。
- **改动（`views/user/AiChatView.vue`，纯样式）**：
  - `.ai-page`：`min-height: 100vh` → `height: 100%; min-height: 0; overflow: hidden`——高度取外层滚动容器 `MainLayout .app-main`（`flex: 1` 的确定高度）的 100%，页面自身不再滚动；
  - `.ai-list`：改为**唯一滚动容器**（`flex: 1 1 auto; min-height: 0; overflow-y: auto; overscroll-behavior: contain`），并按设计稿口径隐藏常驻滚动条（与 `.app-main` 同口径）；
  - `.ai-appbar` / `.ai-quick` / `.ai-network-tip` / `.ai-composer`：`flex: none`，不参与伸缩、保持原位。
- **浏览器实测**（mock 5173，390×844，向 localStorage 注入 41 条消息构成长对话）：进入页面时 `.ai-list` 已自动贴到最新消息（`scrollTop = 3490 = max`）；分别在「列表滚到顶」「列表滚到底」两次测量：顶部栏 `top=12`、快捷提问栏底 `788`、输入区底 `844`（贴视口底）**三处完全不变**；外层 `.app-main` 可滚高度 `0`（整页不滚）；无控制台错误。截图 `ai-07-fixed-layout.png`（进入态）/ `ai-08-scrolled-top.png`（滚到顶）。
- **回归**：全量 vitest **54 文件 / 426 用例全绿**、`npm run type-check` ✓、`npm run build` ✓。
- **说明**：布局类断言按工程约定 §6 只能在浏览器/E2E 层做（jsdom 无布局引擎），故本项无单测断言，证据为上述实测数值与截图；`TODO-USER-116` 证据列已同步。

### B. AI 使用点

| # | 任务 | 类别 | AI 做了什么 | 人工审核与修改 | 结果 |
| --- | --- | --- | --- | --- | --- |
| 1 | 固定布局改造 | 代码生成 | 先读外层 `MainLayout` 的高度模型（壳 `100dvh` + `app-main` 独立滚动）再改本页高度与滚动归属，避免「页面不滚但内部也无处可滚」或双滚动条 | 负责人走查提出 | 三行固定、仅中间滚动 |
| 2 | 实测取证 | 质量巡检 | 注入 41 条长对话后在浏览器量取顶部栏/输入框位置（滚动前后不变）与外层可滚高度（0），并按约定说明布局断言不入单测 | 无 | 数值 + 截图证据 |

- **本轮更新的 TODO 编号**：`TODO-USER-116`（证据列补三行固定布局改造与实测数据）。

## 23:45 （本轮：AI 对话页顶栏副标题位置走查修复）

### A. 进度事实

- **来源**：负责人走查「智能推荐，只建议不下单 字太靠下了，往上一点」。
- **根因（先量后改）**：`<p>` 的浏览器默认外边距未重置——`.ai-title` 上下各 15px、`.ai-subtitle` 上下各 11px → 标题块总高 **83.2px**，被塞进 48px 的顶栏后垂直居中出现溢出：标题 top 8.9（栏顶之上被裁）、**副标题 top 52.9**（栏底 60 之下、视觉上"贴在分隔线下方"）。
- **改动（`views/user/AiChatView.vue`，纯样式）**：`.ai-title`、`.ai-subtitle` 补 `margin: 0`；顺带把同页另外两个 `<p>`（`.ai-thinking`、`.ai-network-tip`）的默认外边距一并重置，避免同类隐患。
- **实测（mock 5173，390×844，Playwright 量取 getBoundingClientRect）**：副标题 top **52.9 → 37.9**（上移 15px）；标题块高 **83.2 → 31.2px**，在顶栏（12–60，仍 48px）内垂直居中（上间隙 7.9 / 下间隙 8.9）；标题 top 8.9 → 19.9（不再溢出到栏外）。
- **回归**：全量 vitest **54 文件 / 426 用例全绿**、`npm run build` ✓。
- **同类自检**：其它页面顶栏标题类均显式写了 `margin: 0`（如 `MessageCenterView .msg-title`），本页实现时漏写 → 已作为"新增页面自检项"记入台账本行；布局类断言按工程约定 §6 不入单测，证据为上述实测数值与截图。

### B. AI 使用点

| # | 任务 | 类别 | AI 做了什么 | 人工审核与修改 | 结果 |
| --- | --- | --- | --- | --- | --- |
| 1 | 先量后改 | 质量巡检 | 不靠印象改数值：先用浏览器量出 83.2px 标题块与副标题 top 52.9，定位到 `<p>` 默认外边距这一根因，再改 | 负责人走查提出 | 副标题上移 15px |
| 2 | 同类隐患排查 | 自检 | 顺手把同页其余 `<p>` 的默认外边距一并重置，并与其它页面（`.msg-title { margin: 0 }`）对齐 | 无 | 无同类遗留 |

- **本轮更新的 TODO 编号**：`TODO-USER-116`（证据列补副标题位置走查与实测数值）。

## 23:55 （本轮：修两条缺陷——刷新后加购被拦（P1）+ 消息中心真后端不可用（P2））

### A. 进度事实

- **任务来源**：负责人「那我要做的事情以此开工吧，被卡着的事情就不做」→ 按上一轮按人归类的清单开工，只做**用户端自己能动**的两条缺陷修复（被口径/他人依赖卡住的项不做，理由与对接人另列）。
- **修复 1｜BUG-20260914-003（P1）公开页整页刷新后加购被误判未登录**：
  - 改动（`views/user/StoreDetailView.vue`）：`onAdd` / `onToggleFavorite` / `onCheckout` 三处改为「**未登录才探活**」两步式——`if (!sessionStore.isLoggedIn) await sessionStore.checkLogin()` 后再判定；
    **关键取舍**：已登录路径保持完全同步（不在前面加 await），避免把加购的**抛球动画即时反馈**推迟一个微任务（T73 依赖该同步性）；`onCheckout` 相应改为 async。
  - **红端**：新增 T77（服务端会话有效仍被跳登录页）、T79（收藏/去结算同根因）失败 → `2 failed / 25 passed`；T78（真未登录仍引导登录带 redirect）为回归锁，修复前即通过。
  - **绿端**：`StoreDetailView.spec.ts` **27/27 通过**。既有用例 T45/T18 因该路径变为异步探活（替身 200–500ms），断言改为 `vi.waitFor` 等待重定向（用例语义未变、口径未放宽）。
- **修复 2｜BUG-20260914-004 / 005（P2）消息中心在真后端不可用**：
  - 改动（`views/user/MessageCenterView.vue` + `services/normalizers.ts`）：
    ① `loadAll` 由 `Promise.all` 改为 `Promise.allSettled([loadNotifications(), loadConversations()])`，两个数据源**各自独立失败标记**，并在通知区/会话区加**局部失败态与「重试」入口**（`notification-error`/`notification-retry`、`conversation-error`/`conversation-retry`）；空态口径收紧（有失败时不显示「暂无消息」）。
    ② 新增 `normalizeConversation`（架构约定 §3.3「归一化唯一出口」）：`storeId` 缺失退回 `merchantId`、`lastMessageAt` 退回 `updatedAt`、`unread` 退回 `unreadCount`，全缺给确定默认值；头像首字改用 `avatarText()`（恒返回字符串）、店名缺失给「未知商家」占位。
  - **红端**：新增 `MessageCenterView.realShape.spec.ts` 的 MC-R1（通知 404 → 会话渲染 0 条）与 MC-R2/MC-R3 **在单测里复现了线上同一条报错** `TypeError: Cannot read properties of undefined (reading 'slice')` → `3 failed / 1 passed`；MC-R4（替身标准形状）为回归锁，修复前即通过。
  - **绿端**：该文件 4/4 通过、`MessageCenterView.spec.ts`（TW 组）不变全绿。
- **R1 全量回归（两处修复后）**：vitest **55 文件 / 433 用例全绿**、`npm run type-check` ✓、`npm run build` ✓、E2E **21 项全绿**（含加购链路与保真度巡检）。
- **台账同步（本板块）**：`TODO-USER-114`、`TODO-USER-115` 证据列补红绿与「已修（工作区，待提交）」；§3 两条阻塞行标注**已解除**——payDeadline（线上已返回，倒计时真走秒）与取消接口（线上实测 `o1257` 取消成功）。
- **口径纪律**：BUG-20260914-005 的修复是**别名兼容**（让页面在真后端形状下不崩、字段可用），**不是字段口径定稿**；契约 §6.1 仍需 R7 裁定后可能收敛实现，已在台账与问题记录写明，避免"用兼容冒充口径已定"。
- **未做的事**：未提交、未开 PR（按 Git 纪律等负责人明确允许）；未改他人板块台账（缺陷台账 3 条的状态流转归测试联调同学）；被卡的项一律未动（清单见当轮回复）。

### B. AI 使用点

| # | 任务 | 类别 | AI 做了什么 | 人工审核与修改 | 结果 |
| --- | --- | --- | --- | --- | --- |
| 1 | 缺陷复现 | 测试设计 | 先用「伪造服务端会话有效 / 伪造线上响应形状」的用例把两条缺陷在单测层复现（含原样复现线上 `reading 'slice'` 报错），再动实现 | 负责人指示开工 | 红端证据齐（2 + 3 条） |
| 2 | 修复 | 代码生成 | 会话探活做成「未登录才探活」两步式以保住已登录路径的同步性；消息中心把双数据源拆开并把字段兼容收敛到 `normalizers.ts` 单一出口 | 无 | 绿端全过 + R1 全绿 |
| 3 | 既有用例适配 | 测试执行 | 识别出 T45/T18 因异步探活而失败，仅调整断言等待方式（不改语义、不放宽口径），并逐一复核 TW 组未受影响 | 无 | 55 文件 433 用例全绿 |
| 4 | 台账与留痕 | 文档修订 | 补红绿证据、标注两条阻塞已解除、明确「别名兼容 ≠ 口径定稿」的边界，并复核表格结构（修掉证据里未转义竖线导致的列错位） | 无 | 台账结构自检 0 异常 |

- **本轮更新的 TODO 编号**：`TODO-USER-114`（补修复红绿证据）、`TODO-USER-115`（补修复红绿证据）、§3 两条阻塞行（payDeadline / 取消接口）标注已解除。

## 23:59 （本轮：AI 会话「接数据库」核查 + 两条缺陷修复提交与 PR #65）

### A. 进度事实

- **任务来源**：负责人「同学说我的AI点餐助手没接数据库，你接上；然后把该…PR」→ 本轮先做接库核查与能做的硬化，再按 §1.2/§3 规则提交并开 PR。
- **接库核查（先查证再动手）**：
  - **写入链路是通的**：后端 `MysqlChatMemoryStore` 按 `sessionId` 落库 `ai_chat_sessions` / `ai_chat_messages`（`INSERT ... ON DUPLICATE KEY UPDATE`），线上业务库实测 **`ai_chat_sessions` 13 行 / `ai_chat_messages` 49 行**（SSH 到服务器查 `elm_practice`，只读）；知识库 `ProductKnowledgeService` 启动时从 MySQL 读 stores/products 拼入系统提示词。前端每条消息都经 `/ai/chat` 或 `/stream-chat` 上报，localStorage 只是展示缓存（PRD §5.2 明写「后端只存不查、前端自管理展示」）。
  - **前端硬化**：新增 `writeAiSessionId` + 视图 `adoptSessionId`——`/chat` 回显的 sessionId 与本地不一致时切到后端 id 并在新 key 下保存当前消息，避免「数据库一段会话、本地展示另一段」分叉。用例 AS-7 / AI-15 红→绿。
  - **另发现两处真实缺口（非本板块可修，已登记）**：① `ProductKnowledgeService.refresh()` 在 main 全库**无调用点**——商品/商家变更后 AI 仍按启动快照回答（契约 §10.6 明写可刷新）；② 契约 §10.6 **无历史查询接口**——会话消息落库但前端读不回（清 localStorage/换设备即丢展示），若验收要求「从数据库读历史」需后端新增接口 + R7。
- **提交与 PR（本轮授权范围内执行）**：按 `test:` 早于 `feat:`/`fix:` 拆成 6 个提交，逐条在对应提交上复跑取证：
  1. `0cbaca7` test: AI 前端用例（红端：`7 failed (7)` + 入口 `3 failed / 22 passed`）
  2. `5f9d4ea` feat: AI 点餐助手前端页面与三处入口（绿端：AI 7 文件 68 用例）
  3. `070ae9e` test: 会话探活与消息中心真后端形状用例（红端：`5 failed`，其中 MC-R2/R3 复现线上同一 TypeError）
  4. `a8f871e` fix: BUG-20260914-003（绿端 27/27）
  5. `c1c369d` fix: BUG-20260914-004/005（绿端 realShape 4/4）
  6. `77ab1bb` docs: 真后端复验证据与台账
  - 取证方式：提交后 `git stash push -u` 把实现挪开、在 `test:` 提交的干净树上复跑拿红端，`git stash pop` 恢复后提交实现；`bash .github/scripts/commit-hygiene.sh origin/main` **✅ 通过（6 提交）**。
  - 干净工作区全量复跑：vitest **55 文件 / 435 用例全绿**、`type-check` ✓、`build` ✓、E2E **21 项全绿**。
  - 分支 `feat/ai-chat-page-tdd` 已推送，**PR #65** 已开（https://github.com/Fu-Jumping/elm_practice/pull/65），PR 描述按 `.github/pull_request_template.md` 填全（含每条红绿与未收口项）。
- **本轮追加落盘（随同一 PR 推送）**：`TODO-USER-116` 证据列补接库核查结论（含线上库行数与前端硬化）；§3 新增 `TODO-USER-116 ④`（历史读接口缺口）、`TODO-USER-116 ⑤`（知识库不刷新），归口后端 + 负责人。
- **未做的事**：未改 PRD/契约（两处口径与实测不符只登记待 R7）；未动后端代码（`refresh()` 与历史接口归后端板块）；未替他人改状态（缺陷台账 3 条状态流转归测试联调同学）；未把 pycache 等他人产物纳入提交。

### B. AI 使用点

| # | 任务 | 类别 | AI 做了什么 | 人工审核与修改 | 结果 |
| --- | --- | --- | --- | --- | --- |
| 1 | 接库核查 | 事实核对 | 不停在"同学说没接"的转述：SSH 只读查线上库确认表与行数、读后端持久化与知识库实现，分清「写入已通 / 读回缺口 / 知识库不刷新」三件事 | 负责人指示「接上」 | 结论有据（13/49 行） |
| 2 | 前端硬化 | 代码生成 | 补「会话以后端为准」（`writeAiSessionId` + `adoptSessionId`），用例先行红→绿 | 无 | 23/23 |
| 3 | 提交拆分与红绿取证 | 部署操作 | 按 `test:`→`feat:`/`fix:` 拆 6 个提交，用 stash 在 `test:` 提交的干净树上复跑拿红端后再提交实现，并过提交规范脚本 | 负责人授权提交与开 PR | 6 提交全过规范 |
| 4 | 开 PR | 部署操作 | 用本机已存凭据走 GitHub API 建 PR #65（不打印 token），描述按仓库模板填红绿、命令结果与未收口项 | 负责人授权 | PR #65 已开 |

- **本轮更新的 TODO 编号**：`TODO-USER-116`（补接库核查与前端硬化证据）、新增 §3 阻塞行 `TODO-USER-116 ④`、`TODO-USER-116 ⑤`。
