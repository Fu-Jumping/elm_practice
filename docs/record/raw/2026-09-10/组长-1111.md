# 2026-09-10 组长留痕（R8 定稿、分支清零、待办中心 docs/todo 建立，组长-1111）

## A. 进度事实

- **三项决定落地**：① R8 定稿为**统一使用服务器测试库**——实测服务器已有独立测试库 `elm_practice_test`，账号 `elm_local@localhost` 对业务库与测试库均有 ALL PRIVILEGES 且**仅限服务器本机连接**；后端测试一律 SSH 到服务器执行并强制 `DB_NAME=elm_practice_test`，禁止对业务库跑重置类测试，本机不装 MySQL（运行说明 §4.5 已改写并标红线）。② **CI 必需检查无法配置**——GitHub 私有仓库 + 免费账号，分支保护/rulesets API 返回 403（提示升级 Pro 或公开仓库），已登记为**已知限制**，以"PR 描述自检 + 组长核对 CI 全绿"替代。③ **讲稿分支删除**——`wjy-9.8-zhongqi-demo` 已删，**远端未合并分支清零**（31 条分支）。
- **待办中心建成**：新增 `docs/todo/`（6 个文件）——README（机制、智能体工作协议 6 条、与其他文档分工、全局总览与已知限制）+ **用户端.md / 商家端.md / 后端.md / 部署.md / 文档与交付.md**。按负责人拍板口径：**前端拆成用户端与商家端两个文件**；**去掉"待认领"与"待验证"**，任务一登记即**必须指定唯一主责人**，状态只保留 `进行中/已完成/已冻结`；**所有待办只在文档内部改写更新、不设归档目录**；ID 前缀 `TODO-USER/MER/BE/OPS/DOC`；类型含 `缺陷/打磨/未完成模块/技术债`，出处必须可回溯，缺陷类与 `docs/testing/缺陷跟踪表.md` 互引编号。
- **迁移内容**：本期 9 个批次按端别拆入各板块（批次① 含商家端优惠配置页，主责王馨琪）；PR #36 评审遗留 4 项 → 后端/商家端/文档与交付；未关闭缺陷（BUG-005/007/012、SHOW-QA-001/002）按端别落位；打磨项（smoke.py 支付断言改 PENDING、订单跟踪页复刻真源、九项导航、金额四行复查等）与阻塞项（等后端接口/等老师变更）分别标注。
- **规则与入口同步**：`docs/project/项目规则.md` 新增 **§1.2 待办台账规则**（载体与写权表 + 8 条硬约束，含强制 AI 代理协议）并在 §5 登记；`AGENTS.md` 入口清单新增第 10 项 `docs/todo/README.md`，工作过程段落补"待办台账联动 + 收工留痕写明 TODO 编号"；`docs/project/项目目录索引.md` 与 `docs/README.md` 同步为 `docs/todo/**`；**原 `docs/project/待办台账.md` 迁移后删除**，避免两份口径。提交 ed607df 已推送 main。
- **验证**：CI（push 触发）在本次提交上运行；文档类改动不影响三端构建与测试任务的通过基线。

## B. AI 使用点

1. **方案先行**：先给结构方案（目录、板块、字段、状态、协议、迁移清单、工时）交负责人审，按反馈（拆前端两文件、去两态、不归档、必填负责人）修订后再执行，避免边做边改。
2. **规则与载体同步**：把机制写进 `项目规则` §1.2（与既有 §1.1 缺陷台账体例一致），并在 AGENTS.md 入口与记录机制段落挂接，使"文档可被智能体执行"这一诉求落到入口清单与每条提交纪律上。
3. **收敛口径**：删除旧台账文件而不是并存，防止出现两套任务状态；缺陷类强制与缺陷跟踪表互引，避免绕过既有唯一台账。
4. **环境实测优先**：R8 的结论基于在服务器上实测数据库清单与授权（而非推断），红线（禁止对业务库跑重置测试）在文档中显式标注。
5. **人负责判断**：板块划分、状态收敛、归档策略、讲稿去留、仓库是否公开等均由负责人决定；CI 必需检查不可用的限制如实登记，未用流程话术掩盖。
