# 2026-09-10 组长留痕（分支清理、任务台账建立、协作规范 R1/R2/R5/R6/R7/R8/R9 落地与 CI 接入，组长-1043）

## A. 进度事实

- **分支清理**：远端删除 7 条（`merge/*` 4 条、`codex/merchant-p0-closeout`、`integration/verify-p0-fixes`、`feat/demo-images`），本地删除 10 条已并入分支；远端分支降至 32 条，**唯一未合并分支为 `wjy-9.8-zhongqi-demo`**（独有内容仅 `docs/presentation/答辩手册.html` 讲稿，按负责人决定不入 main，分支保留）。
- **规范修订（负责人拍板采纳 R1/R2/R5/R6/R7/R8/R9；R3/R4/R10 暂不启用）**：
  - **R1 合并前必跑矩阵**：改前端必须 `npm run build`（含 tsc）+ `npm test`，改后端 `mvn -DskipTests package`（有库补 `mvn test`），改文档口径 grep 复查，改脚本语法检查；写入 `AGENTS.md` 与 `docs/project/小组协作与提交流程.md` §7，触发教训为今日"只跑 npm test 漏掉 tsc"。
  - **R2 PR 模板**：新增 `.github/pull_request_template.md`（改动范围 / 验证证据表 / TDD 红绿 / 未收口项 / 合并前自检）。
  - **R5 CI**：新增 `.github/workflows/ci.yml`，四个任务：用户端（构建+单测）、商家端（构建+单测）、后端（构建）、后端集成测试（MySQL 服务容器 + 建表种子 + `mvn test`）。
  - **R6 提交规范检查**：新增 `.github/scripts/commit-hygiene.sh`（标题格式、正文 `- ` 列表、`记录:`/`AI协作:` 尾注、`test:` 提交纯度、不得携带 `docs/record/daily/**`），由 CI 在 PR 上执行。
  - **R7 口径变更同一 PR 同步**、**R9 材料入库边界**（演示页与证据进 main、讲稿与个人材料不进）写入 `AGENTS.md`。
  - **R8 环境规范**：运行说明新增 §4.5（本机 MySQL 为推荐路径、服务器测试库为替代；本机 3306 不可达属环境缺失），并在项目规则 §5 登记整批修订。
- **CI 首跑与修复**：首次运行 **0 秒失败**——步骤名含 `test: ` 的未加引号 YAML 标量被解析为嵌套映射；加引号并本地 js-yaml 复验后重跑。**第二次运行全绿**（用户端/商家端/后端构建与单测 + 后端集成测试 23 条 + 提交规范检查在 push 时按设计跳过）。据此把 `backend-test` 由"观察期 continue-on-error"**提升为必需检查**（第三次运行确认全绿），并在流程与运行说明中同步表述。
- **任务台账建立**：新增 `docs/project/待办台账.md` 作为**唯一任务台账**（本期 9 批次表 + 按人视图 + PR #36 后续任务 4 项 + 冻结/不做项 + 使用方式），`docs/project/项目目录索引.md` 与 `docs/README.md` 已收录。负责人已把**批次① 优惠与统一计价（TC-PRV 11 条）指派给王馨琪**，台账中登记为"待认领（组长 09-10 指派）"，协作方为后端 B 林晨（计价与库）、后端 A 王嘉悦（契约回写）、前端主力 戴宇涵（四行明细）。
- **提交**：ad6f8ec（台账与规范）、7f60e84（CI YAML 修复）、3836bed（提升必需检查），均已推送 main；工作区干净。

## B. AI 使用点

1. **规范落地为可执行物**：AI 把 7 条口头规则转成 CI 工作流、检查脚本、PR 模板与文档条款，其中 R6/R1 直接取自今日实测教训（类型检查漏跑、提交格式不合规）。
2. **CI 故障定位**：从"run 0 秒失败、无 job、无 check-run"三个特征判定为 YAML 解析期失败，用 js-yaml 定位到未加引号的 `test: ` 标量并修复，再据首跑结果把观察期任务提升为必需检查（有证据才提级）。
3. **台账结构化**：把散落在 5 份文档里的任务收敛为单一台账，含状态取值、认领方式、证据列与冻结项，便于组员与其 AI 代理直接读取与更新。
4. **分支决策留痕**：删除前先核对"相对 main 是否还有需要的内容"，仅保留承载讲稿的分支，并在留痕中写明保留原因。
5. **人负责判断**：规则取舍（R3/R4/R10 暂不启用）、批次① 指派、讲稿不入 main 均由负责人决定；AI 只执行与留痕。
