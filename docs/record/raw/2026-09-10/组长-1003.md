# 2026-09-10 组长留痕（发布合并后 main 到演示环境 + 真实接口与页面验证，组长-1003）

## A. 进度事实

- **授权与前置**：负责人同意（1 发布、2 不跑种子、3 参数化 release.py）。服务器探测：`ubuntu` 免密 sudo、Node 22.23.2 / Java 17.0.20 / Maven 3.6.3 / Python 3.10.12 / pm2 7.0.3 齐全，回滚资产（pm2-before/restore-*/source-before/数据库转储）完整，磁盘 109G 可用；服务器源码目录非 git 仓库（tarball 解压），故采用"上传源码包"而非 git pull。
- **脚本改造**：`deploy/release.py` 参数化（`--source/--backup/--srv-root/--tag/--release-sha`，默认值 = 2026-09-08 首次发布），新增发布前前置校验（源目录、nginx 模板、回滚资产、后端 jar 缺失即退出），结束打印改为输出实际发布目录（提交 ed38825）。
- **构建阻塞与修复**：服务器构建在商家端 `tsc -b` 阶段失败——`real-contract.test.ts` 传 `{onSale:false}` 与 codex 版 `Pick<ProductDraft,'onSale'|'stock'>` 必填 stock 冲突（TS2345）。根因是本次合并把演示分支用例并入 codex 基线时未跑 type-check。核查后端 `CatalogService.availability`：`stock == null` 时不修改库存，说明"只提交上下架"是契约支持且更安全的行为；遂按最小修复改前端：`merchantApi.updateProductAvailability` 参数改 `Partial<Pick<...>>`、mock 只应用显式字段、`App.tsx` 列表行只提交 `{ onSale }`，避免回写旧库存覆盖顾客下单后的实时变化（提交 99a646d）。红端证据：`tsc -b` TS2345 构建中断；绿端证据：`npm run build` 通过 + `npm test` 6 文件 / 20 用例全绿。
- **发布**：`RELEASE_OK 99a646d 20260910-99a646d`；`/srv/elm-current` 指向 `/srv/elm-releases/20260910-99a646d`，旧版本 20260908 保留；`nginx -t` 通过；`/`、`/user/`、`/demo/`、`/user/api/v1/stores` 全 200；**`/demo/答辩手册.html` 返回 404**（讲稿按决定不入 main）；只重启 `elm-backend`，服务器上其他项目（sw-rag、student-profile、jt-textbook-pipeline 等）未受影响。
- **真实接口冒烟**：`python deploy/smoke.py` **50/50 通过**（`businessAssertionsPassed=true`、`testStoreClosed=true`，合成订单 o1178 / 测试店 m1172 / 账号 u1170），`deploy/smoke-result.json` 已更新。
- **浏览器级验证**（Playwright 打线上地址）：用户端首页渲染、`/user/stores/m002` 显示真实店铺与商品（香辣鸡腿堡 ¥19.50 等）；商家端 `merchant-a` 登录后订单管理显示真实订单（o1157/o1152/o1143… 含金额与状态）；用户端 `13800000001` 登录后订单页状态显示中文（"待接单"等），证明合并后的状态映射生效。
- **留痕**：`docs/project/运行说明.md` 新增 §4.4 发布记录（在线版本、构建与验证证据、冒烟结果、回滚点、讲稿 404 口径）；`deploy/README.md` 更新当前 release 与参数化用法。

## B. AI 使用点

1. **发布链路自动化**：AI 完成"本地打包 → scp → 服务器构建 → 参数化发布 → 冒烟"全流程，并把长耗时构建放进后台作业，失败即时定位。
2. **构建失败根因定位**：AI 从截断日志中识别 `tsc` 类型错误，追溯到"合并时未跑 type-check"，再对照后端 `CatalogService` 的 null 语义判断"用例意图正确、实现需修"，避免把测试改成迁就错误实现。
3. **多层验证**：接口级冒烟（50 项）+ 浏览器级双端登录核对 + HTTP 入口与讲稿 404 检查，三层证据互相印证。
4. **风险控制**：发布前新增脚本前置校验、备份 `restore-backend.json`、保留旧 release 与 static release 作回滚点，并只操作 `elm-backend` 与 elm 的 nginx 配置，不影响同机其他项目。
5. **人负责判断**：是否发布、是否跑种子、脚本改造方式均由负责人拍板；AI 未擅自改课程硬节点或演示数据。
