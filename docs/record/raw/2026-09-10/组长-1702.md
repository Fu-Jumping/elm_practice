# 2026-09-10 组长留痕（结课汇报 · AI 前端 SOP 板块：演示页面大纲 v2 + 材料归位 + 两条待办落给用户端）

## A. 进度事实

- **负责人本轮的三点修正（已执行）**：① **时间口径**——本板块讲的是"全部开发完成之后的终态"，不得出现"某模块仍在进行中/尚未收口/还有多少待办"这类当前时点表述；② **投入档位**——同意投入（"加断言脚本和像素比对"）；③ **商家端不进本板块**，只用用户端举例。
- **材料归位**：新建 `docs/_archive/presentation/`（本地、gitignore），把本板块"之前与后续"材料全部归入：
  - 4 份规划文档（第一轮现状、第二轮研究与规划、大纲设计待确认项、演示页面大纲）由 `docs/_archive/` 根层移入；
  - `tmp/结课汇报SOP调研/` 整目录移入 `presentation/调研报告/第二轮与第一轮原始报告/`（10 份子代理报告 + R4 脚本原型 + 像素比对脚本与截图）；
  - 已修正规划文档内 13 处失效路径引用（`tmp/` 与旧 `_archive` 路径），grep 复查为 0。
- **演示页面大纲 v2**（`docs/_archive/presentation/2026-09-10-结课汇报-AI前端SOP-演示页面大纲.md`，204 行）：按修正重写——开头新增"时间口径"条款；全部数字改为**用户端口径**（32 导出目录 / 120 导出文件 / 88 红框图 / 72 区行 / 11 视图 / 148 单测）；删除商家端屏（原可选屏 2b）与商家端证据（20/20 SHA256、85 区行、17 端点、AntD 注入）；屏 7 的边界改为**完成态下依然成立的 3 条**（像素比对是取证非门禁 / 设计稿只对有导出的页面生效 / 复刻度是值域命中率而非像素相似度）；备用页与 §待办同步更新。
- **两条待办落给用户端（写入 `docs/todo/用户端.md`）**：
  - **TODO-USER-023 保真度巡检断言脚本（结构 + 多视口）**：把 23/23 通栏结构断言恢复为"仓库内可复跑"（判定式三条件、8 类 23 项受检区块清单、类名映射处理），并补 320/360/375/390/430 五视口断言（横向溢出、壳宽=视口宽、固定栏贴底、文字不裁切）；
  - **TODO-USER-024 设计稿像素比对脚本与一次性取证**：截图（393×852@2x）+ Python/PIL 比对（零 npm 依赖），写明三条必须遵守的口径（53px 状态栏偏移校正、数据驱动区掩膜、整图比对只对 393 画布精细版）与三条验收标准；
  - 两条均附**可执行细节**：文件落位、运行命令、验收标准、参考实现路径（`presentation/调研报告/…/原型-截图比对/` 已有可改写的 shot.cjs / pixel_diff.py / align_analysis.py / band_analysis.py），以及通用纪律（R1 合并前必跑 `npm test` + `npm run build`、TDD 提交顺序、产物落 `test-results/`）。
- **落地方式**：按待办中心规则"所有待办只在所属文件内部改写更新"，明细写入 `docs/todo/用户端.md` 新增的 §4「任务实施说明」，未另建清单；两条状态为 `进行中`、主责人 戴宇涵。
- **未做**：未写讲稿；未改任何前端代码；未开始脚本实现；未做 git 写操作。

## B. AI 使用点

| # | 任务 | 类别 | AI 做了什么 | 人工审核与修改 | 结果 |
| --- | --- | --- | --- | --- | --- |
| 1 | 材料归位与引用修正 | 文档修订 | 新建 archive 子目录、移动 4 份规划文档与整个调研报告目录，并全量修正规划文档内的失效路径（13 处），grep 复查 0 残留 | 目录方案由负责人指定 | `docs/_archive/presentation/**` |
| 2 | 大纲 v2（时间口径 + 去商家端） | 文档初稿 | 按"完成态口径"重写全部屏内容，替换为用户端数字、删除商家端证据与可选屏、重写边界三条 | 口径与取舍待负责人复核 | 大纲 v2（204 行） |
| 3 | 待办落盘（两条 + 实施说明） | 技术债任务登记 | 在 `docs/todo/用户端.md` 新增 TODO-USER-023/024 两行，并附 §4 实施说明（目标、落位、判定口径、断言清单、三条像素口径、验收标准、参考实现、通用纪律） | 负责人指定执行人（戴宇涵）与投入档位 | 待办文件 104 行 |

---

## 11:50 追加（本轮：原始记录片段改为当天单文件规则）

### A. 进度事实

- **新规则（负责人拍板）**：同一天内同一成员或角色的多轮工作留痕**只写一个原始记录文件**，后续轮次在文件末尾追加小节（`## HH:mm <本轮主题>`），**不得一轮新开一个文件**；只有跨天（进入新的 `YYYY-MM-DD/` 目录）才新开。规范文件名 `docs/record/raw/YYYY-MM-DD/<成员或角色>.md`。
- **落点**：`docs/project/项目规则.md` §2 第 2 条新增 2.1（同日单文件）/2.2（过渡口径：生效当天追加到该成员当天**最后一个**已有片段，次日起用新命名；**既有片段不改名不移动不删除**）/2.3（追加写法：保留原内容、写清本轮进度事实与 AI 使用点、写明 TODO 编号）；`AGENTS.md` 工作过程段落同步；`docs/record/raw/README.md` 命名说明同步；§5 长期约束台账登记本次修订。
- **触发动因**：2026-09-10 组长名下当天产生 10 个片段文件（0921/0926/1003/1043/1111/1115/1144/1459/1636/1702），碎片化影响次晨汇总与检索；本条即本次修订的过渡示例——本轮不再新建文件，而是追加到当天最后一个组长片段（本文件）。
- **本轮更新的 TODO 编号**：无（本轮为记录机制规则修订，未改动任何 `TODO-*` 行）。

### B. AI 使用点

1. **规则三处同步**：AI 把负责人的一句话要求落成"项目规则细则 + AGENTS 入口约束 + raw/README 命名说明"三处一致表述，避免只在单处可查。
2. **过渡设计**：考虑"片段落盘即永久留档、不得移动"的既有硬约束，设计"追加到当天最后一个文件、次日起换新命名"的过渡口径，既不改写历史片段又立即生效。
3. **自证执行**：本轮留痕直接按新规则追加而非新建文件，以自身动作验证规则可执行。
4. **人负责判断**：规则内容与生效方式由负责人决定，AI 只负责表述、落点与自证执行。

---

## 11:55 追加（补充说明：本轮提交意外带上并行会话的待提交文件）

### A. 进度事实

- 本次提交 `3ad89b3`（原始记录片段改为当天单文件）使用 `git add -A docs` 暂存，**连带把并行会话（结课汇报 · AI 前端 SOP 主题）当时尚未提交的文件一起提交**：
  - `docs/record/raw/2026-09-10/组长-1459.md`（+22）、`组长-1636.md`（+19）：该会话当天两轮留痕；
  - `docs/todo/用户端.md`（+81）：新增 TODO-USER-023/024 两条技术债任务与 §4.1/§4.2 实施说明。
- **影响评估**：内容完整、格式正常（已核对 §4.1/§4.2 结构），**没有丢失或半成品**；唯一问题是提交信息未体现这部分来源（归属写成了本次规则修订）。
- **处理**：历史已推送、不做改写；本条以留痕方式补充说明。**后续规则：提交时使用显式路径 `git add <文件>`，不再用 `git add -A` 扫全目录**，避免替他会话代提交。
- **本轮更新的 TODO 编号**：无（仅记录说明）。

### B. AI 使用点

1. **副作用自查**：提交后核对 `git show --stat`，发现提交范围超出本意，主动回溯定位来源（并行会话未提交文件）并如实留痕，未掩盖。
2. **流程纠偏**：把"用 -A 暂存"改为"显式路径暂存"，从操作层面避免同一工作目录多会话并行时的代提交风险。
3. **人负责判断**：是否改写已推送历史由负责人决定；AI 默认选择不改写并留痕说明。
