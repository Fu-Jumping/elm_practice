# 2026-09-10 组长留痕（四个 PR 合并、main 验证与真实接口冒烟，组长-0926）

## A. 进度事实

- **PR #36 关闭**：组长决定关闭同源的 `codex/merchant-p0-closeout` → main（王馨琪 9/8 开），已留言说明 #37 取代关系与差异；其评审清单未闭环的 4 项已在 `docs/project/分工与里程碑安排.md` 新增 **§2.1 后续任务登记**（联系电话字段、测试文档位置、问题记录明细、raw 计数修正），逐条给出责任人与验收口径（提交 c0f348c）。
- **合并前复查**：#37 CLEAN / 20 文件 / +2396−244；#38 10 文件；#39 11 文件；#40 43 文件且**讲稿 0 命中**（`docs/presentation/答辩手册.html` 未入库）；仓库允许 merge commit。
- **四个 PR 顺序合并（均已 MERGED）**：#37 → 重定基线 #38 → 合并 → 重定基线 #39 → 合并 → 重定基线 #40 → 合并；远端 main 最新为 `00204d7 Merge pull request #40`。
- **合并后 main 结构与验证**：商家端 `App.tsx` 883 行、6 个测试文件（含 `real-contract.test.ts`）；用户端 `PaymentActions.vue` 在位；`deploy/smoke.py` 与 `docs/presentation/index.html` 在位；`答辩手册.html` 不存在。三项验证全绿：用户端 **23 文件 / 148 用例**、商家端 **6 文件 / 20 用例**、后端 `mvn -DskipTests package` 构建通过。
- **真实接口冒烟**：`python deploy/smoke.py` 对部署环境 `http://82.157.137.114:4001` 执行，**50/50 项检查全过**（`deploy/smoke-result.json`：`passed=50 / total=50 / businessAssertionsPassed=true / testStoreClosed=true`，退出码 0，订单 `o1168` / 测试店 `m1162` / 账号 `u1160`），脚本按设计在结束时关闭测试店；产物 `deploy/smoke-result.json` 已更新。
- **冒烟口径说明（重要）**：本次冒烟针对**部署环境当前构建（合并前、演示分支产物）**，其支付后状态仍为 `PROCESSING`（脚本按
该口径断言并通过）；**合并后 main 的真实模式端到端冒烟尚未执行**，原因是本机无 MySQL（127.0.0.1:3306 不可达）且无 docker，而部署服务器 SSH 免密登录被拒（`Permission denied (publickey,password)`），无法把合并后构建发布上去。
- **待办建议**：提供服务器 SSH 权限或在具备 MySQL 的环境执行 `mvn test` 与 `deploy/release.py` 发布合并后构建，再复跑 `smoke.py`；届时脚本中 `payment` 断言需按 PRD 7.6 定稿改为支付成功即 `PENDING`。

## B. AI 使用点

1. **合并流水线自动化**：AI 用 `gh pr merge` + `gh pr edit --base` 完成串联 PR 的"合并—重定基线—再合并"，并在每次前复查 `mergeable/mergeStateStatus/changedFiles`。
2. **口径核对**：合并前用 `gh pr view --json files` 逐项核对 4 个 PR 的文件清单，确认讲稿 0 命中、商家端基线文件到位。
3. **环境限制的诚实标注**：Smoke 只能打到部署环境（旧构建），AI 明确区分"部署环境冒烟通过"与"合并后 main 的真实模式冒烟未执行"，未把前者写成后者。
4. **变更留痕与复核**：合并后结构核对（App.tsx 行数、测试文件数、关键文件存在性与讲稿缺席）与三项测试结果均以命令输出为证据。
5. **人负责判断**：关闭 #36、登记后续任务、直接合并与自动冒烟均由负责人拍板，AI 只执行并如实回报未达成项。
