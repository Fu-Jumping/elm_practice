# 2026-09-13 组长留痕

> 命名口径：按 `docs/todo/README.md` §3 第 5 条与 `docs/project/项目规则.md` §2 第 2 条 2.1–2.3，同日同角色只写本文件，后续轮次在文件末尾追加小节。

## 19:40 （本轮：补录 9/11 与 9/12 两日正式记录）

### A. 进度事实

- **任务来源**：负责人指示「先把记录补齐，再 push，再开始今天的开发」。开工核对发现 `docs/record/daily/` 只到 2026-09-10，**9/11 与 9/12 两天的正式记录缺失**（按规则由次日上午汇总智能体或组长撰写），故本轮补录两份。
- **素材归集与事实核对**（不凭印象写）：
  - 读 `docs/record/raw/2026-09-11/` 的 2 个片段（`组长.md` 20 个小节、`用户端.md` 12 个小节）与当日提交：`git log --since/--until` 得 **63 笔**，作者分布 Fu-Jumping 32 / qianhan 28 / qanhan 3。
  - 读 `docs/record/raw/2026-09-12/` 的 3 个片段（`用户端.md` 10 节、`林晨.md` 5 节、`王嘉悦.md` 2 节）与当日提交：**58 笔**，作者分布 qianhan 30 / linchen 16 / qanhan 9 / 花绿青 3；当日 `Merge pull request` 提交计 **12 个 PR**（#45–#56）。
- **一处时间标注偏差的核对（保留原文、只作说明）**：`组长.md` 有 **8 个小节**标注为「次日 00:40–04:40」，但按提交时间逐条比对，其内容对应的是 `1aabbe7`(13:24) → `2cd3be7`(13:38) → `2e5d7e4`(13:43) → `8d3be99`(13:46) → `e610f12`(13:47) → `e83583a`(13:49) → `815f67a`(13:51)，即**当日 13:24–13:51 的连续工作被误标为「次日」**。按「raw 落盘即永久留档、不改写」的规定未动原文，在 9/11 记录开头以引用块如实说明。
- **产出**：
  - 新增 `docs/record/daily/2026-09-11.md`（标题 `# 2026-09-11（周五）`，沿用既有 daily 结构：依据引用块 + A 当日进度 4 列表 + B AI 使用点 6 列表 + 当日要点）。内容含：CHG-003/004 同日定稿与 13 份文档 R7 同步、批次⑩ 五项 + 批次④ 两步经 PR #41/#42/#43/#44 合入 main（main 复跑 242 passed）、6 域审计 142 条 + 6 域复核 49 条、**PRD 的 CHG-002 内容丢失事故**（外部编辑器回退）定位与修复、评审与写作两条口径成文、9/9 与 9/10 两日记录补录。
  - 新增 `docs/record/daily/2026-09-12.md`（标题 `# 2026-09-12（周六）`）。内容含：**12 个 PR 同日合入 main**、CHG-001 前端闭环（红包页/浮窗/爆红包浮层/首页入口/确认订单红包选择）与批次⑥ 三项落地、**替身与真实后端七步计价逐分一致**（PR #52/#53）、后端批次①/⑥/状态机（PR #48/#49/#51，37-55 条测试全绿）与 CI UTC 时区 bug 修复、3 项缺口登记（`payDeadline`/`reviewed`/免费爆可用性）、**演示环境入口 404 实测**（仅报告未处置）、端口与联系电话两分支待 PR。
  - **未采用被更正的错误时间**：`用户端.md` 的 20:40 节为更正说明（原「次日 00:30–03:20」实为当日 20:0x–20:3x），记录按更正后的时间归集。
- **如实标注**：9/11 与 9/12 两天，后端 A/后端 B 与商家端/测试联调**存在无片段、无提交的情形**（后端成果集中在 9/12；王馨琪两日均未见片段与提交），两份记录均按「未见当日片段/提交」如实书写，未虚构其当日工作。
- **未做的事（遵守规则）**：本轮只写 `docs/record/daily/**`（写权属组长/次晨汇总智能体）与本人的 raw 片段，**未改动任何他人板块的待办行、未改缺陷台账、未提交任何业务代码**。
- **本轮更新的 TODO 编号**：无（记录补录属规则内的记录维护，不新增也不改任何任务行）。

### B. AI 使用点

| # | 任务 | 类别 | AI 做了什么 | 人工审核与修改 | 结果 |
| --- | --- | --- | --- | --- | --- |
| 1 | 两日素材归集与事实核对 | 文档初稿 | 读 9/11 的 2 个与 9/12 的 3 个 raw 片段（共 49 个小节）与当日 121 笔提交，按「成员 × 今日完成/明日计划/阻塞」与「AI 使用点」两表归集；对无片段无提交的成员如实写「未见当日片段/提交」 | 负责人指示补录；daily 写权经授权 | 2 份 daily |
| 2 | 时间标注偏差核对 | 事实核对 | 对 `组长.md` 8 个自称「次日」的小节逐条比对提交时间，确认实为当日 13:24–13:51，按「不改写留痕原文」的规定只在 daily 引用块中说明 | 无 | 偏差 1 处已说明 |
| 3 | 提交归属与 PR 计数 | 事实核对 | 用 `git log --since/--until` 按日拉提交并按作者分组（63 / 58 笔），按 `Merge pull request` 计数得 12 个 PR，避免把跨日成果误归当日 | 无 | 归日与计数准确 |

## 12:10 （本轮：待办中心收口 + R1 基线验证 + VIS-001/VIS-004 系统说明图）

### A. 进度事实

- **本轮任务**：负责人指示「先把记录补齐，再 push，再开始今天的开发」。记录已补齐并推送（`5e072de`，push `80c883a..5e072de`）；本轮开始今日开发，范围＝**本人（组长/文档与交付板块）名下的未收口项**，不触碰他人板块的任务状态与业务代码。
- **① 待办中心收口（`TODO-DOC-106` + `TODO-DOC-009`）**：
  - `docs/todo/文档与交付.md`：`TODO-DOC-010` 由「进行中」区移入「已完成」区（`TODO-DOC-106` 的第一项要求）；**修正 2 处区/状态不一致**——`TODO-DOC-105`（内容未完成、状态进行中）原误置于「已完成」区，已归位「进行中」区；`TODO-DOC-011` 复核确认已在正确区。收口后本板块：§1 进行中 12 行、§2 已完成 8 行、§3 阻塞 2 行，**两表均 8 列、无残缺行**（按管道符计数核对）。
  - `TODO-DOC-009` 证据列补本轮全量核对结论；**对其他板块的处置**：`用户端.md` 中 `TODO-USER-004/028/029/030`（PR #45/#46/#54/#55/#56 已合并）与 `后端.md` 中 `TODO-BE-002`（PR #51 已合并）等「代码已合并但仍在进行中区」的行，**未替他人改状态**，以清单形式报给各端主责人，留待其本人或其代理按规则收口。
- **② R1 基线验证（为 9/14 总回归提供基线）**：
  - 用户端 `frontend/user-h5`：`npx vitest run` = **301 passed（40 文件）**、`npm run build`（含 vue-tsc）= **✓ built in 663ms**；
  - 商家端 `frontend/merchant-admin`：依赖缺失，先 `npm ci`（新增 178 包）后 `npm test` = **20 passed（6 文件）**、`npm run build` = **✓ built in 475ms**（仅有 chunk >500kB 的体积提示，非错误）；
  - 后端：**本机无 Maven（`mvn: command not found`，Java 17 存在）**，按 R8 口径后端构建与测试在 CI / 服务器执行，本机不跑，故本轮不做后端 R1。
- **③ VIS-001 + VIS-004 系统说明图（`TODO-DOC-012`，已完成）**：
  - 产出**可编辑源文件**（白底、中文、1260px 画布，HTML+CSS 自包含）：`docs/maps/VIS-001-全流程产品结构图.html`、`docs/maps/VIS-004-全流程泳道图.html`；用 Playwright（chromium）从 HTML 渲染为**交付图片** `docs/maps/product-structure-fullstack-v3.png`（1260×1538）、`docs/maps/core-flow-fullstack-v3.png`（1260×2244），并以 2 倍像素密度导出。
  - **VIS-001** 六层齐全（用户端 / 商家端 / 前端公共能力 / 后端服务 / 数据库 / 工程交付），第④层按「P0 六类能力 + 本期扩展能力均已实施」表述（不把接口预留当终点），第⑥层含测试、联调、部署、报告 PPT 演示脚本；**未收口项如实标注**（收藏/通知/会员相关表待建）。
  - **VIS-004** 六泳道（用户 / 用户端前端 / Java 后端 / MySQL 数据层 / 商家端前端 / 商家）三段（登录与下单、商家处理、扩展闭环），实线 = P0 主线、虚线 = 本期 P1/P2（已实施）、红色虚线 = 异常与回滚；含 P0 四类必画异常分支（未登录与越权 401/403、价格变化以后端重算为准、事务失败整体回滚、重复请求靠幂等键与条件更新只生效一次）。
  - **事实核对（不臆造）**：图中接口路径取自契约全文抽取（`POST /orders`、`GET /merchant/orders`、`PATCH /merchant/orders/{id}/status`、`PUT /merchant/promotions`、`/me/coupons*`、`/me/favorites`、`/me/member`、`/me/notifications*`、`/search`、`/orders/{id}/review` 等）；表名取自 `backend/database/schema/schema.sql` 实际建表（含 `cart_lines`、`order_items`、`promotion_tiers`、`coupon_packs` 等），**未使用推测名称**；计价七步、状态机（支付成功即「待接单」）、事务与幂等口径按契约 §3.5/§7 核对。
  - **R7 同批同步**：`docs/project/用户待补充资料与原型清单.md` §4 的 VIS-001/VIS-004 状态改「已提供」并写明文件名与源文件；并按定稿口径**更正 VIS-004 第 2 段的过期表述**（原文「P0 下单 — `PROCESSING` 不流转」与状态机定稿及已实施的推进不符，改写为「支付成功即待接单 + 跨端读同一份订单数据」）；`docs/project/项目目录索引.md` 的 `docs/maps/**` 行补 4 个新文件；`docs/todo/文档与交付.md` `TODO-DOC-012` 移入已完成区并填证据。
  - **过程事实（工具与踩坑）**：Playwright 1.63 自带浏览器版本与 `ms-playwright/chromium-1234` 不匹配导致 `chromium.launch()` 报「请运行 playwright install」，改用 `executablePath` 指向已安装的 `chrome-win64/chrome.exe` 后正常；渲染脚本与核对用裁剪图均写入临时文件并在用后**立即删除**（不留在仓库）；渲染中修正一处重复 `grid-column` 的单元格错误。
- **未做的事（遵守规则）**：未改他人板块任务状态与业务代码；未新增 CHG（本轮为文档与图，未改需求口径）；**改动尚未提交**——按 Git 纪律「未经负责人明确允许不得 commit/push」，等待负责人授权。
- **本轮更新的 TODO 编号**：`TODO-DOC-106`（→ 已完成，证据为 3 处不一致修正 + 表结构核对）、`TODO-DOC-012`（→ 已完成，证据为两张图 + 清单/索引同步 + R7 口径更正）、`TODO-DOC-009`（证据列补本轮全量核对与「他人板块清单化上交」的处置方式）。

### B. AI 使用点

| # | 任务 | 类别 | AI 做了什么 | 人工审核与修改 | 结果 |
| --- | --- | --- | --- | --- | --- |
| 1 | 待办中心结构收口 | 待办维护 | 按 `docs/todo/README.md` 三态与分区规则核对本板块全部 22 行，移区 1 行、归位 1 行，并用管道符计数复核两张表列数一致；对他人的过时行只做清单化上报 | 负责人指示「开始今天的开发」 | 3 处不一致修正 |
| 2 | R1 基线验证 | 质量巡检 | 按 R1 矩阵逐端执行：用户端单测 + 构建、商家端补装依赖后单测 + 构建；如实报告后端因本机无 Maven 无法本地执行（R8 走 CI/服务器） | 无 | 301 + 20 全绿、两次构建 ✓ |
| 3 | 系统说明图设计与实现 | 资产制作 | 按清单 §4 的六层与六泳道三段要求设计 HTML（自包含 CSS，箭头落于泳道间隙、异常用红色虚线），接口与表名逐项回读契约与建表脚本对齐，避免图示与实现脱节 | 无 | 2 张图（源文件 + PNG） |
| 4 | 图 → PNG 渲染链路 | 脚手架 | 用 Playwright 从 HTML 渲染 2 倍密度 PNG；遇到浏览器版本不匹配时改用 `executablePath` 指向已安装 chromium，避免额外下载；脚本用后即删 | 无 | 2 个 PNG（1260×1538 / 1260×2244） |
| 5 | 口径过期项发现与更正 | 文档修订 | 发现清单 §4 VIS-004 第 2 段仍写「`PROCESSING` 不流转」，与状态机定稿及已实施的商家推进不符，按 R7 同批更正并注明依据 | 无 | 1 处口径更正 |

## 14:10 （本轮：演示环境 404 只读排查 + 第三方品牌字样全库扫描）

### A. 进度事实

- **授权与边界**：负责人「先做 a 再 c 再 b d」明确授权；本轮对服务器**只读**（未改任何配置、未重启任何进程），写入只在仓库。
- **演示环境 404 的根因（已定位）**：
  - `:4001` 的 nginx 站点配置被改名为 `/etc/nginx/conf.d/elm-release.conf.disabled-for-classroom`（不以 `.conf` 结尾 → nginx 不再加载），同时新增 `classroom-vote.conf`（同为 `listen 4001`、`server_name 82.157.137.114`、root `/srv/classroom-vote/current/public`，PHP-FPM）。两者时间戳均为 **2026-09-10 15:39**。
  - 我们的配置 `server_name _`（默认）+ 对方按 IP 精确匹配 → 用 IP 访问 4001 全部落到 classroom-vote：`/` 302 `/login`、`/user/` 与 `/demo/` 404，与 2026-09-12 实测一致。
  - **纠正 9/12 的一处判断**：443 上返回 401 的 Express 是另一个项目（`sites-enabled/students-way`），**与本项目无关**——我们的 API 设计上就走 `:4001` 的 `/api/`、`/user/api/` 两条代理，不需要 443。
  - **发布与后端其实都是健康的**：`pm2 elm-backend` online（uptime 3D，参数 `-jar ... --server.port=4000 --server.address=127.0.0.1`）；服务器本机 `curl 127.0.0.1:4000/api/v1/stores` = **200**；静态产物完整（`/srv/elm-current/{user,merchant,demo}`，`elm-current → /srv/elm-releases/20260910-99a646d`）。即：**不是发布坏了、也不是后端挂了，是入口被占用**。
  - **端口全景（只读 `ss -tln`）**：3306 MySQL、8080 sw-rag、4001 classroom-vote、4000 本项目后端（仅 127.0.0.1）、80/443 students-way、3000（127.0.0.1）另一服务。
- **对发布链路的影响（重要）**：`deploy/release.py` 会把配置 `sudo cp` 回 `elm-release.conf` 并探活 `http://127.0.0.1:4001/…`。但在 `conf.d` 的字母序里 `classroom-vote.conf` 在前、且它按 IP 精确匹配，nginx 会把 4001 的默认 server 判给 classroom-vote → **即使恢复我们的配置，IP 访问与默认 server 仍落到对方，探活会失败并触发发布回滚**。所以**端口冲突必须先解决，再谈发布**。
- **三个候选方案与影响面（未执行，待负责人决定）**：
  - **A 让 classroom-vote 让出 4001**（它换端口或换路径）：不动腾讯云安全组（4001 已放行），我们零改动；需其负责人/课程方同意。**注意**：该项目的 `shared/` 目录 2026-09-12 17:46 仍有改动 → **仍在活跃使用，不能单方面顶掉**。
  - **B 我们换到新端口**：需改 `deploy/nginx.conf`（listen + 302 目标）、`deploy/release.py`（探活 URL）、`运行说明` §4.4/§4.5、`deploy/README.md`，且**新端口必须在腾讯云安全组放行**（需控制台权限，本机 SSH 无权改）。
  - **C 时间窗切换**：演示/验收窗口前启用我们的配置、其余时间保持对方（反向复刻 9/10 那次操作），零端口冲突但要每次协调“谁在用”。
- **本轮未做**：未改服务器任何配置、未重启进程、未联系对方项目负责人（不知归属，需负责人出面）。
- **全库第三方品牌字样扫描（本轮 c）**：
  - **UI 会渲染的残留：仅 `frontend/user-h5/src/views/user/StoreDetailView.vue:407` 一处**（顶部栏 `.detail-brand` 仍写第三方品牌名），**已在本分支修掉**（`test:` `666bfb3` → `fix:` `007befc`）。
  - 其余命中均为**解释性注释**（OrderDetailView 两处注明“不出现微信支付”、MemberView、PaySuccessView、OrderPayView、StoreDetailView:1498 的措辞）与**测试守卫**（`MemberView.spec.ts:122` 正向断言不含该词）；`frontend/merchant-admin/src` **零命中**；两个 `index.html` 与 `public/` 无命中。
  - **待决项（未擅自改）**：**演示店铺名**用真实快餐品牌（肯德基宅急送、麦当劳等）出现在用户端 mocks 3 处、后端 `seed.sql` 4 处、`catalog-alignment.sql` 2 处、商家端 `merchantApi.ts` 2 处——这是 P0 起就存在的固定演示数据且被大量用例断言（T7/T46 等）。是否算「第三方品牌字样」需口径拍板；若替换为虚构名，改动面包含三端种子与受影响断言。
- **本轮更新的 TODO 编号**：无新增；相关证据分别写入 `TODO-USER-023`（同分支的脚本）与 `TODO-USER-024`（本轮 024 部分由用户端留痕记录），服务器结论待负责人决定后落入 `docs/todo/部署.md`。

### B. AI 使用点

| # | 任务 | 类别 | AI 做了什么 | 人工审核与修改 | 结果 |
| --- | --- | --- | --- | --- | --- |
| 1 | 演示环境只读排查 | 排障 | 用 `pm2 list` / `readlink` / `ls -lt` / `ss -tln` / `cat` 两份 nginx 配置 / 服务器本机 curl 逐项取证，把「404」定位到「站点配置被禁用 + 4001 被另一项目占用」，并纠正 443 归属的旧判断 | 负责人已授权只读排查 | 根因 1 组 + 端口全景 |
| 2 | 影响面与方案评估 | 风险提示 | 从 `release.py` 的写入与探活逻辑推出「恢复配置也仍会被抢端口 → 发布回滚」，给出 A/B/C 三方案及各自影响面（含安全组与控制台权限约束），未擅自改服务器 | 待负责人决定与协调 | 3 方案 |
| 3 | 品牌字样全库扫描 | 质量巡检 | 分「渲染代码 / 注释 / 测试守卫 / 演示种子」四类扫描三端与两个 index.html，区分出唯一一处真实残留（已修）与需拍板的演示店铺名口径 | 演示店铺名口径待拍板 | 残留 1 处（已修） |

## 21:00 （本轮：PR #59 合并核实 + 用户端台账收口 + 未完成模块清单）

### A. 进度事实

- **PR #59 合并核实（负责人已合并）**：`origin/main` = **`d034c48`**（Merge pull request #59 from Fu-Jumping/test/design-fidelity-gate）；本地 main `--ff-only` 同步后**在 main 上复跑 R1** = 全量 vitest **356 passed（47 文件）** + `npm run build`（含 vue-tsc）✓，合并健康。本轮把所有未推送提交补齐（`37d6625..51c8bf2`，共 35 个提交在远端）。
- **用户端台账收口（`docs/todo/用户端.md`）**：把「交付物已完成且已合入 main」的行由「进行中」移入「已完成」区并补合并证据——
  - 本批新增/修复的 9 行：`USER-005`（搜索结果页）、`USER-007`（评价图片上传）、`USER-011`（SHOW-QA-001 桌面侧 + 安全区修复）、`USER-012`（SHOW-QA-002）、`USER-013`（SHOW-QA-004 迟滞）、`USER-014`（SHOW-QA-005，PR #58）、`USER-016`（加购抛球，上一轮已移）、`USER-023`/`USER-024`（巡检脚本与像素工具）；
  - 既有已合并的 6 行：`USER-001`/`004`/`006`/`028`/`029`/`030`（PR #45/#46/#50/#54/#55/#56），统一补「已合入 main，遗留=复刻对照截图 + 真实后端联调（依赖 4001 恢复与后端批次）」的收口说明；
  - 收口后：**§1 进行中 4 行**（`USER-107` 分类列表页待入口、`USER-025`/`027` 待主责人确认、`USER-106` ③ 台账整理）、**§2 已完成 31 行**、**§3 阻塞与待确认 9 行**；结构自检 8 列 × 39 行 + 5 列 × 9 行，无残缺行。
  - **两次误匹配如实记录**：脚本按 `^\| TODO-USER-xxx \|` 匹配时，**阻塞区同 ID 的 5 列行也被命中**（005/006/028 各有一次）→ 被误改成 8 列并移入已完成区；两次都当场核对结构与分区计数发现并修回（恢复阻塞行、删除误移行）。**教训**：跨分区批量改行必须按「分区 + 列数」双条件匹配，不能只按 ID 前缀。
- **新增交付物 `docs/project/未完成模块与如实标注清单.md`（组长主责）**：六节——① 一句话现状（含「线上仍是 9/10 版本、`:4001` 被 classroom-vote 占用」）；② 可演示（真实链路：P0 主链路 + 红包 + 商家端已实现部分）；③ **只有替身**（搜索/评价图片/消息/收藏会员/统计，并写明 `VITE_MOCK_FALLBACK=false` 下 real 模式不会回退而是直接失败）；④ **未实现（须如实标注「本期实现（待开发）」）**（后端批次②③④⑤⑥⑦⑧⑨ + `BE-019/020/021`、用户端 `107②`/`022`、商家端 `005/006/010`、部署 4 项、文档 5 项，逐条挂台账编号）；⑤ 环境与验收阻塞 4 条；⑥ 演示建议（若 9/16 前无法恢复真实环境该怎么讲）。并在 `项目目录索引.md` 补该行（R7 同步）。
- **本轮更新的 TODO 编号**：`USER-005/007/011/012/013/014/023/024`（移入已完成并补合并证据）、`USER-001/004/006/028/029/030`（同批收口）、`USER-107/025/027/106`（保持进行中并保留说明）。

### B. AI 使用点

| # | 任务 | 类别 | AI 做了什么 | 人工审核与修改 | 结果 |
| --- | --- | --- | --- | --- | --- |
| 1 | 合并核实与 main 复跑 | 质量巡检 | 拉取远端确认 PR #59 已并入、在 main 上复跑全量单测与构建，作为合并健康的证据 | 无 | 356 passed + build ✓ |
| 2 | 台账批量收口 | 待办维护 | 按「交付物完成且已合入 main」的判定把 15 行移入已完成区并逐行补 PR/提交证据，保留全部遗留项描述 | 无 | §1 4 行 / §2 31 行 |
| 3 | 误匹配自查与修复 | 自检 | 两次发现脚本把阻塞区同 ID 行误改（按 ID 前缀匹配所致），均以分区计数与列数扫描定位并修回，固化「分区 + 列数」双条件匹配的教训 | 无 | 两处已修复 |
| 4 | 冻结前口径清单 | 文档初稿 | 依据五块台账与 main 实际合并情况，产出「可演示 / 只有替身 / 未实现 / 环境阻塞 / 演示建议」六节清单，明确 real 模式不会回退替身，并同步目录索引 | 无 | 1 份新文档 |
