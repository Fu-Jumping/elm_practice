# 2026-09-13 商家端与测试联调（王馨琪）原始记录

## A. 当日进度事实

### 1. 最新 main 干净基线与 TDD 红绿

- 从 `origin/main` 的 `80c883a` 建立独立工作树 `/home/ubuntu/elm-worktrees/merchant-stage2` 和分支 `codex/merchant-stage2`，未改动原脏工作区。
- 保留 main 的 `frontend/merchant-admin/.env.example`、`tests/merchant-category-sortorder.test.ts` 和 `vite.config.ts`（4001 端口与 `/api` 代理）；与 `origin/main` 的这三项差异为空。
- 红端提交 `c8b73ef` 只含 3 个测试文件；首次执行 3 个文件失败、5 项失败，确认 main 缺少二阶段规则、接口与页面入口。
- 绿端分批：`64a39bc` 完成服务与规则，规则/契约 6/6；`9b714ce`、`32c071d` 修正测试控件定位且均先于页面实现；`69400ac` 完成经营页面，导航与页面入口 3/3。
- 覆盖范围：优惠配置、已取消订单与金额明细、评价管理、消息聊天、规格与图片、分类绑定、概览与统计、九项导航；联系电话仍不显示，等待后端 TODO-BE-019。

### 2. R1 本地与真实模式验证

- 商家端全量：`npm test -- --run`，9 个测试文件、29 项测试全部通过。
- 静态检查：`npm run lint`，0 warning、0 error。
- 关闭 Mock 的生产构建：`VITE_API_MODE=real VITE_API_BASE_URL=/api/v1 VITE_MOCK_FALLBACK=false npm run build` 通过；仅有既存的大包体积提示。
- 当前服务器进程来自 `/home/ubuntu/releases/elm-20260910-e41444c/backend`，jar 修改时间为 2026-09-10 09:59，晚于它的新版后端尚未部署。
- 对该部署做只读冒烟：商家登录、店铺、分类、商品、`CANCELLED` 订单、优惠、评价、会话、概览、统计共 10 个请求均 HTTP 200。
- 真实契约缺口：优惠仍返回 `enabled/threshold/amount`；概览仅 `orderCount/productCount/salesAmount`；统计仍为旧聚合字段；商品无 `specOptions`；取消订单与评价均为空样本。故真实写接口与页面验收未收口。

### 3. PR、CI 与 TODO

- 已推送 PR #57：`https://github.com/Fu-Jumping/elm_practice/pull/57`。
- 首轮提交规范检查因两条提交正文使用了字面 `\\n` 失败；已重写为仓库要求的正文列表、`记录:` 与 `AI协作:` 尾注，本地 `commit-hygiene.sh origin/main` 已通过；PR #57 最终代码与证据提交的五项 CI 均成功（提交规范、商家端、用户端、后端构建、后端 MySQL 集成）。
- 商家端 TODO-MER-001～012 的证据列已更新，状态全部保持“进行中”。在新版后端部署、CI 全绿、真实接口写入/样本与页面证据齐全前不改为“已完成”。

## B. 本次工作 AI 使用点

- AI 辅助：读取 PRD/SRS/接口契约/TDD 与仓库规范；隔离工作树；测试先行与红绿复跑；迁移并校正商家端实现；全量测试、lint、真实模式构建；服务器只读接口验收；PR 与证据整理。
- 人工责任：王馨琪需复核 PR 页面行为和接口口径；后端新版部署后在服务器测试库执行优惠保存、评价回复、消息发送/已读、规格保存、图片上传、分类绑定及真实取消/红包订单页面验收。
- 本轮更新 TODO：TODO-MER-001～012（仅补证据与阻塞，不改完成状态）；无新增 TODO。
