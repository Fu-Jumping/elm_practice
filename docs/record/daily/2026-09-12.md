# 2026-09-12（周六）

> 本文件为**补录**（原计划次日上午汇总），依据 `docs/record/raw/2026-09-12/` 的 **3 个片段**（`用户端.md` 10 个小节、`林晨.md` 5 个小节、`王嘉悦.md` 2 个小节）与当日 **58 笔 Git 提交**（qianhan 30 / linchen 16 / qanhan 9 / 花绿青 3）整理；经组长授权在 main 上直接维护。当日 **12 个 PR** 合并入 main。
>
> **留痕口径提示**：`用户端.md` 20:40 小节为一条**更正说明**——该文件 19:45 之后几轮的初始时间曾被误写成「次日 00:30–03:20」，经核对实为当日 20:0x–20:3x，已按实测时间更正（原文保留更正说明）。本记录不采用被更正的错误时间。

## A 当日进度

| 成员 | 今日完成 | 明日计划 | 阻塞 / 需要谁配合 |
| --- | --- | --- | --- |
| 用户端 戴宇涵（AI 协助） | ① **批次① 确认订单页金额明细**（基础四行 + 优惠项按实际发生，走 `buildAmountLines` 唯一出口）与**批次⑥ 收藏页 + 会员权益页**（含商家详情收藏入口），经 **PR #50** 合并；② **替身镜像后端七步计价**（逐行对照 `PricingService`/`OrderService`/`PromoConfig`/`seed.sql`，PR-1~PR-5 锁定），并修正一处真实模式缺陷——`reviewed` 缺失不再补 `false`（否则已评价订单被误标「待评价」并给出必然 409 的入口），**PR #52** 合并；③ **与后端集成测试的交叉验证**（PR-6/PR-7 复现 `PricingIntegrationTest` 原始数字，替身与真实后端逐分一致），**PR #53** 合并；④ **CHG-001 前端三批**：红包页 + 买红包浮窗（**PR #54**）、爆红包全屏浮层三态（**PR #55**）、首页「天天爆红包」真入口 + 确认订单页红包选择（`couponId` 下单选用，**PR #56**）；⑤ **PR #45/#46/#47**（批次⑩ 消息中心、聊天详情、打磨批次）合并收口，其中 #45→#46→#47 为叠加链，按「先合 #45、再把后续 PR base 重定向到 main」处理；⑥ 合并后 main 复跑 R1 = **301 passed** + `npm run build`（含 vue-tsc）✓ | 批次⑤ 搜索结果页、⑧ 评价图片上传；SHOW-QA-001/002/004/005 与打磨 016/022；保真度巡检与像素比对脚本（023/024） | 批次⑤/⑧ 依赖后端接口；**确认订单页预览只能给「预估金额」**（用户端无优惠/计价查询接口）；真实后端联调待后端批次就绪；复刻对照截图待补 |
| 后端 B 林晨 | ① **端口口径统一 8080→4000**（`7692960`，三处代码/配置 + 现行口径文档，TODO-OPS-003，R1 已过，**分支待 PR**）；② **批次① 优惠计价七步接入下单**（红 `e958096` → 绿 `7fc3ed2`，**PR #48 合并** `cec86da`，37/37）：orders 补 6 个金额快照列、promotions 扩列与 `promotion_tiers` 新表、迁移脚本幂等 + `DatabaseInitializer` 启动升库、保存校验（减额 ≤ 门槛、折扣率 (0,1] 等）；③ **批次⑥ 红包与爆红包（CHG-001）后端**（红 `67e61f4` → 绿 `6278ee1`，**PR #49 合并** `fae1f3a`，55/55）：`coupons`/`coupon_packs`/`users.free_blast_date` 建表、列表/可用/买套餐/爆红包四接口、`FOR UPDATE` 行锁 + 条件更新核销、10 档权重池（契约双写、随机源可注入）；④ **修 CI 时区 bug**：有效期判断统一东八区（写券东八区、判断用 JVM 默认时区 + MySQL `NOW()` 导致 UTC 环境下券被误判过期）；⑤ **BUG-20260908-012 店铺联系电话**（红 3 用例 → 绿 28/28，**分支 2 提交待 PR**） | 为端口与联系电话两个分支开 PR；批次⑥ 剩余（收藏、会员标识/会员价/计价第⑤步）；批次② 取消订单 | R8 要求的服务器 SSH 公钥仍未授权，只能经外网连服务器测试库 `elm_practice_test`（未碰业务库）；服务器部署按负责人要求暂缓，待 #48/#49/#51 合并为一次升级 + 冒烟 |
| 后端 A 王嘉悦 | **TODO-BE-002 订单状态机修正**（红 `64241ed` → 绿 `2c1dea7`，**PR #51 合并** `71db6d5`，55/55）：`markPaid` 由写 `PROCESSING` 改为写 `PENDING`（待接单），`PROCESSING` 退为 P0 存量兼容值；商家推进自 `PENDING` 起；种子订单 `o1001` 两处同步；契约 §3.5/§9/§10.4 三处按 R7 回写 | 批次② 取消订单（前置状态机已就绪） | 服务器部署暂缓；直连 github.com 443 超时，需走本地代理 7890（API 连接不受影响） |
| 商家端 / 测试联调 王馨琪 | 当日**未见留痕片段与提交** | 商家端扩展页与 P0 验收场景补充（TODO-DOC-105） | SHOW-QA 编号与严重程度待确认；商家端 12 项进行中任务尚无证据 |
| 部署 / 环境（林晨，仅报告未处置） | 按 `运行说明` §4.4/§4.5 记录的演示入口实测**与文档不一致**：`http://82.157.137.114:4001/user/`、`/user/api/v1/stores`、`/demo/` 均 **404**；`:4001/` 根路径 302 到 `/login`；`https://82.157.137.114/api/v1/**` 返回的是**另一个项目的 Express 服务** 401。即**本项目后端当前未按文档路径对外提供接口**，本轮联调改为后端**代码级**核对替代 | 待负责人决定发布时机，做一次合并部署升级 + 冒烟（TODO-OPS-002/003/004） | 部署时机由负责人拍板；端口 4000/4001/3306/80/443 均可达 |
| AI 代理 | 逐行读后端代码完成「替身 ↔ 真实后端」计价镜像与交叉验证（不照抄文档概述）；发现并登记 2 项后端实现缺口（`payDeadline`、`reviewed` 契约已定稿但 `ViewMapper.order` 不返回）与 1 项契约缺口（「今日免费爆是否可用」无查询来源）；把审定的两处口径按 R7 落盘（TC-MBR-005 重写、契约 §3.7 收藏对象补 3 字段）；如实记录 4 处过程教训 | 按批次推进用户端实现 | 缺口路由与是否新增计价预览接口待负责人决定 |

## B AI 使用点

| # | 任务 | 类别 | AI 做了什么 | 人工审核与修改 | 结果 |
| --- | --- | --- | --- | --- | --- |
| 1 | 批次① 金额明细与批次⑥ 两页实现 | 前端实现 | 复用 `buildAmountLines` 唯一出口（不新写第三套算法），按导出稿逐值复刻收藏页与会员权益页（暖色渐变状态卡、无壳商家卡、标签胶囊），并对照 PRD 688 行新建商家详情收藏入口 | 无 | PR #50（红 9 failed/34 passed → 绿 263 passed） |
| 2 | 替身镜像后端七步计价 | 前端实现 + 质量巡检 | 不照抄文档概述，逐行读 `PricingService`/`OrderService`/`PromoConfig` 默认值与 `seed.sql`，把七步编码为替身 `priceOrder` 并逐字段落快照，保证 mock 与真实后端同金额 | 负责人指示「按组长规定的文档做，和真实的后端对接起来」 | PR #52（269 passed + build ✓） |
| 3 | 与后端的交叉验证 | 质量巡检 | 新增 PR-6/PR-7 复现后端 `PricingIntegrationTest` 的原始数字（23.00→28.00、48.00→45.00），使后端计价漂移时前端先红 | 无 | PR #53（逐分一致） |
| 4 | CHG-001 前端三批 | 前端实现 | 按导出 SCSS 逐值提取渐变/圆角/分隔线/徽标并读图核对区块顺序；红包列表「只展示接口返回项、前端不自判可选性」；爆红包动效以 Web Audio 合成音效并 try/catch 静默降级 | 负责人拍板档位、套餐与入口范围 | PR #54/#55/#56（301 passed） |
| 5 | 真实模式缺陷发现与修正 | Bug 修复辅助 | 对照后端代码时识别 `reviewed` 缺失被补 `false`，会让已评价订单误标「待评价」并给出必然 409 的入口；按「字段缺失只隐藏字段」改为 `undefined` 并补用例 TV-13 | 无 | 已合并（PR #52） |
| 6 | 缺口识别与登记 | 风险提示 | 发现 `payDeadline`/`reviewed` 契约已定稿但后端不返回且后端待办未登记，**未擅自用假数据糊过去**，登记到用户端阻塞区并提请负责人路由；另登记「免费爆可用性」无查询来源 | 待负责人路由 | 阻塞区 2 行 |
| 7 | 环境实测与如实报告 | 排障 | 按文档记录实测线上入口，发现 404 与「443 为其他项目 Express」的不一致，如实记录并说明改为代码级核对，**不擅自处置部署** | 无 | 环境结论 1 条 |
| 8 | 口径冲突处置 | 文档修订 | TC-MBR-005 与 PRD 871 行冲突（「不提供开通/续费入口」vs「续费按钮给演示反馈」），按「PRD 为真源 + 实现为原型说明超集」的既有评审口径重写 TC 并注明依据；契约 §3.7 收藏对象补 PRD 要求但契约缺失的 3 字段，并在后端实现前补齐以避免返工 | 按 R7 同批同步 | 2 处口径 |
| 9 | 过程失误如实记录与自查 | 事故处理 | ① 为构造「test: 红端为断言级」而先降级实现文件再 `stash`，顺序缺陷导致三文件停留在骨架版本，用 sha1/diff 与备份比对发现并恢复；② `TODO-USER-030` 两个提交误提交到 main，立即改以特性分支承载并回退 main；③ 批量替换脚本自报「成功数」掩盖一处锚点未命中（18 条用例全红），改为逐项 grep 关键定义作为验收；④ PR #52 描述中凭印象写错提交哈希，随后用 API 更正 | 无 | 均已恢复 / 已纠正 |

## 当日要点

1. **12 个 PR 同日合入 main**：#45/#46/#47（批次⑩ 消息中心、聊天详情、打磨批次）、#48（后端批次① 优惠计价）、#49（后端批次⑥ 红包与爆红包）、#50（用户端批次① 金额明细 + 批次⑥ 收藏/会员）、#51（订单状态机修正）、#52（替身镜像七步计价）、#53（计价交叉验证）、#54/#55/#56（CHG-001 前端三批）。
2. **CHG-001「爆红包」前端全部落地，红包链路闭环**：红包页 → 买红包浮窗 → 爆红包浮层三态 → 首页真入口 → 确认订单页红包选择 → 下单 `couponId` 抵扣，前后端（PR #49 + #54/#55/#56）齐备；批次⑥ 三项（收藏页 / 会员权益页 / 确认订单红包选择）亦全部落地。
3. **替身与真实后端计价逐分一致**：先按后端代码逐行镜像七步计价，再用后端集成测试的原始数字做交叉验证（PR-6/PR-7），使后端计价漂移时前端用例先红——这是「mock 与真实后端对齐」的可复跑证据。
4. **后端批次① 与批次⑥ 交付且回归充分**：PR #48（37/37）、PR #49（55/55）、PR #51（55/55），并修掉一处只在 CI（UTC 环境）暴露的时区 bug；R8 口径下测试全程连服务器测试库 `elm_practice_test`，未碰业务库。
5. **发现 3 项缺口并按规则登记而非掩盖**：后端未返回 `payDeadline`/`reviewed`（真实模式下倒计时失效、已完成订单无法判定「待评价」）、「今日免费爆是否可用」无查询来源——均登记阻塞区、归口后端并提请负责人路由；本组不得用演示值补齐。
6. **演示环境与文档记录不一致（仅报告未处置）**：线上 `:4001/user/`、`/user/api/v1/stores`、`/demo/` 均 404，443 端口返回的是另一个项目的 Express 服务；因此本轮「联调」以后端代码级核对替代。该现状直接关系到 9/16 交叉验收能否演示，需在冻结前处理。
7. **两处收口项留到下一工作日**：端口统一（`TODO-OPS-003`）与店铺联系电话（`TODO-BE-019`）两个分支均**已开发完成、R1 已过，但尚未开 PR**；服务器部署继续暂缓，待 #48/#49/#51 等改动合并为一次部署升级 + 冒烟。
