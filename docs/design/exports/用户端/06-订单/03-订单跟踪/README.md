# 03-订单跟踪（已并入订单详情）

> **2026-09-11 起并入订单详情**：新设计稿 `docs/design/exports/用户端/12-订单与支付/01-订单详情-含订单跟踪/` 已包含状态时间线，订单跟踪页不再单独实现（PRD 7.6 已登记时间线文案 ↔ 状态值映射表）。
>
> 本目录仅有 `index.jsx` + `index.module.scss`（缺 `code.html`/`screen.png`），仅供历史对照，**不得作为实现真源**。
>
> 变更记录见 `docs/project/需求变更台账.md` CHG-003。
