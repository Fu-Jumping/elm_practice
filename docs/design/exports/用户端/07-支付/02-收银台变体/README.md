# 02-收银台变体（已被取代）

> **2026-09-11 起被取代**：支付页新设计稿 `docs/design/exports/用户端/12-订单与支付/02-支付页/` 已提供完整四件套，并明确取消订单入口位于卡内（不与「立即支付」并排）。
>
> 本目录仅有 `code.html` + `screen.png`（缺 Figma 的 `index.jsx`/`index.module.scss`），且画布宽高非标准（706×1600），**不得作为实现真源**。
>
> 变更记录见 `docs/project/需求变更台账.md` CHG-003。
