# 未完成模块与如实标注清单（冻结前口径）

日期：2026-09-13（基于 `main` `d034c48`，即 PR #59 合并后）
用途：**9/14 总回归、9/15 冻结、9/16 交叉验收、9/17–18 最终验收**时统一口径——哪些可演示、哪些只有替身、哪些必须如实写成「本期实现（待开发）」。

> 规则依据：`docs/PRD.md` §9（取舍顺序与如实标注义务）、`docs/project/项目规则.md` §5、`docs/todo/README.md`。
> **不得把替身（mock）当真实接口结果展示，也不得把未实现模块写成已完成。**

## 1. 一句话现状

- **用户端**：PRD 里能凭「契约 + 替身」推进的页面**已全部实现并合入 main**（356 单测 / 47 文件、21 条 E2E 全绿）。
- **商家端**：二阶段页面与规则已合入 main（PR #57），**待部署后做真实写入验收**。
- **后端**：P0 六类 + 批次① 计价（PR #48）+ 批次⑥ 红包（PR #49）+ 订单状态机修正（PR #51）已实现；**批次②③④⑤⑥（收藏/会员）⑦⑧⑨ 已于 2026-09-15 全部补齐**（搜索、收藏、通知、会员标识/会员价/计价第⑤步为本轮新增实现，证据见 `docs/todo/后端.md` 各证据列与 `docs/backend/后端接口契约.md` §3.6–§3.9 实现回写）——**后端"未实现"项已清零，剩余缺口是"线上未部署"而不是"未实现"**。
- **环境**：线上仍是 **9/10 的版本**（`/srv/elm-current → 20260910-99a646d`），原入口 `:4001` 曾被同服务器另一项目 `classroom-vote` 占用（我们的 nginx 配置 9/10 15:39 被改名禁用）；**2026-09-13 已在服务器恢复：停用对方站点（经确认、可回退）并把本项目入口恢复到 `:4001`，本机与外网 `4001/user/`、`4001/user/api/v1/stores`、`4001/demo/` 均 200**。真实链路**已可对外访问**，但**线上内容仍是 9/10 版本**，9/11–9/13 的合并需重新发布一次。

## 2. 可演示（真实链路，real 模式）

| 模块 | 依据 |
| --- | --- |
| 注册/登录/会话与角色权限 | P0 后端已实现 |
| 店铺列表/详情、商品与分类浏览 | P0 后端已实现 |
| 购物车（同店限制、增删改） | P0 后端已实现 |
| 地址管理 | P0 后端已实现 |
| 下单 → 计价（七步优惠）→ 金额快照 → 清空购物车 | 后端批次①（PR #48） |
| 支付（模拟）与订单状态流转 | P0 + 状态机修正（PR #51） |
| 取消订单 | 后端批次② **已实现**（`TODO-BE-003`，2026-09-15 复核 OrderCancellationStage2IntegrationTest 4 用例绿） |
| 搜索（商家组 + 商品组 + 排序 + 分页） | 后端批次⑤ **已实现**（`TODO-BE-007`，SearchIntegrationTest 8 用例绿） |
| 收藏页 / 会员权益页 | 后端批次⑥ 剩余项 **已实现**（`TODO-BE-008`，FavoriteIntegrationTest 6 + MemberIntegrationTest 6 用例绿） |
| 消息中心 / 聊天详情 / 通知 | 后端批次④ **已实现**（`TODO-BE-006`，NotificationIntegrationTest 8 用例绿） |
| 评价提交与评价图片上传 | 后端批次③⑧ **已实现**（`TODO-BE-005/010`） |
| 商家端运营概览 / 数据统计 | 后端批次⑨ **已实现**（`TODO-BE-011`，MerchantAnalyticsStage2IntegrationTest 3 用例绿） |
| 红包：列表/可用券/买套餐/爆红包/下单选用 | 后端批次⑥ 红包子项（PR #49） |
| 商家端：登录、商品/分类、上下架、订单推进、店铺设置 | 商家端已实现（PR #57） |

## 3. 只有替身（等真实后端，演示时必须说明）

| 用户端页面 | 替身落点 | 依赖的后端批次 |
| --- | --- | --- |
| —— | **2026-09-15 起本表为空**：上表各模块依赖的后端批次均已在 main 实现（搜索、评价与图片上传、消息与通知、收藏与会员、商家端统计），不再是"只有替身"；但这些实现尚未部署到线上，切 real 复验前仍按"待发布"标注 | 见下方 2026-09-15（本轮收口）块 |

> 注：`VITE_MOCK_FALLBACK=false`（2026-09-07 起关闭回退）→ 上表模块在 real 模式下**不会自动回退到替身，而是直接失败**。演示时只有两种选择：① 用 mock 模式演示并明确说明；② 等后端批次就绪后切 real。

## 4. 未实现（必须如实标注「本期实现（待开发）」）

| 端 | 项 | 台账 |
| --- | --- | --- |
| 后端 | **2026-09-15 起后端"未实现"项清零**：批次②③④⑤⑥（含收藏与会员）⑦⑧⑨ 均已实现并复核（服务器测试库 elm_practice_test 全量 mvn test 105/105 绿），证据列见 docs/todo/后端.md | TODO-BE-003/005/006/007/008/009/010/011 |
| 后端 | 店铺设置 `contactPhone`（分支已就绪，未合并） | `TODO-BE-019` |
| 后端 | VIS-002 系统技术架构图、VIS-003 数据库 ER 图 | `TODO-BE-020/021` |
| 用户端 | `USER-107 ②` 分类商家列表页（`GET /stores?categoryId=&sort=` 分类条件与排序；**不分页、不筛选**，2026-09-15 裁定 A5） | `TODO-USER-107` |
| 用户端 | `USER-022` 启动页横滑（**前提不成立**：仓库无启动页实现、PRD 无对应行，待负责人指明） | `TODO-USER-022` |
| 商家端 | 商品规格维护、商品图片上传、店铺联系电话 回归 | `TODO-MER-005/006/010` |
| 部署 | 演示数据重置脚本、`uploads/` 持久化、发布记录、CI 扩充 | `TODO-OPS-002/004/005/006` |
| 文档 | EVID-001~012 证据包、交叉验收包匿名化、报告/PPT、商家端 P0 验收场景、TC-RBP 用例确认 | `TODO-DOC-005/007/013/105/014` |

> **2026-09-15 更新（本轮口径变化，按 R7 同步）**：
> - 上表「用户端 `USER-107 ②` 分类商家列表页」一行**已完成**并随发布上线（PR #63；2026-09-14 线上复验通过）——本轮同时补齐了**首页宫格的分类入口**（`TODO-USER-121`，宫格课程分类项按 PRD 826 行的「课程固定分类数据」承载并进入分类列表）。
> - **本轮新增交付（此前在"用户端前端功能缺失"清单中被判未落地，现均已实现并有红绿证据）**：设置页 + FAQ 页 + 个人资料页（`TODO-USER-117`）、订单列表六项状态筛选（`TODO-USER-118`）、商品规格弹层与会员价高亮（`TODO-USER-119`）、搜索结果页商品组与分页（`TODO-USER-120`）。
> - **本轮新增未完成项（数据侧，须如实标注；2026-09-15 下一轮已修复，见本文件末尾「本轮收口」块，此处保留为过程记录）**：① **平台级课程分类未落库** → 首页宫格分类入口在真实后端下恒落「该分类暂无商家」空态（`TODO-USER-121`，归口负责人 + 后端）；② **规格弹层与会员价在 real 模式不可见**，需后端批次⑦ 部署（线上 `/merchant/products/{id}/specifications` 实测仍 404，`TODO-MER-005`）并由商家端配置规格/会员价，或补种子数据（`TODO-USER-119`）。
> - **前端目录状态**：本轮后 `frontend/user-h5` 全量单测 **59 文件 / 471 用例**、`npm run build`（含 vue-tsc）✓、E2E **21 项全绿**。
>
> **2026-09-15（本轮收口）**：
> - ① **平台级课程分类已落库**：新增 `platform_categories` + `platform_category_stores`（`pc01`~`pc09` 与用户端 `COURSE_CATEGORIES` 一致），`reset.sql`、`upgrade-stage3.sql`、`backend/database/migrations/20260915-stage3.sql` 三处同步、可重复执行；`GET /stores` 与 `GET /search` 的分类条件同时接受店铺自身分类与平台课程分类 → `TODO-USER-121` **已修复**，且**未用前端假数据兜底**。
> - ② **规格与会员价种子已补**：`upgrade-stage3.sql` 与 `reset.sql` 为 p102/p103/p106/p205/p208 补 `member_price`、为 p103/p106 补 `spec_options`（仅补空值、不覆盖运行期配置）；`TODO-USER-119` 的 real 模式可见性**剩"一次重新发布 + 商家端配置"**。
> - ③ **后端四组缺口已实现**：搜索（§3.6）、收藏（§3.7）、会员与会员价与计价第⑤步（§3.8）、通知（§3.9），契约同 PR 回写；`TODO-BE-006/007/008` 状态更新为已完成。
> - ④ **部署侧**：`deploy/nginx.conf` 补 `location /uploads/` 反代并把 `client_max_body_size` 由 2m 放宽到 4m（`TODO-USER-007`）。
> - ⑤ **仍未完成**：线上仍是 9/10 版本之前的口径，**以上全部需要一次重新发布**才能进入 real 复验；规格与会员价的线上可见性同时依赖商家端配置。

## 5. 环境与验收阻塞（不是代码问题，但会挡住演示）

1. **入口端口（已恢复 4001，2026-09-13）**：`classroom-vote` 停用、本项目配置恢复（备份 `/home/ubuntu/backups/nginx-20260913-4001switch/`），并移除会拖垮 nginx reload 的 5174 旧跳转块（该端口被 `banwei-vote` 占用）；外网验证 200。**剩余**：重新发布一次（含 9/11–9/13 全部合并）并跑 `smoke.py` 冒烟。
2. **部署版本落后**：线上仍是 `20260910-99a646d`（9/10），9/11–9/13 的全部合并未上线；即使入口恢复，也只是 9/10 的功能。
3. **真机待确认项**：`SHOW-QA-001` 的底部安全区（购物车栏内容不被压扁、抽屉贴底、列表不被遮挡）需在带 Home Indicator 的真机上人工确认——代码侧已修（安全区不再压缩内容）。
4. **待人工的口径**：`SHOW-QA-004` 迟滞死区暂定 8px（建议区间 8–16px）；`USER-025/027` 两条原型收口待主责人确认。

## 6. 演示建议（若 9/16 前仍无法恢复真实环境）

- 以 **mock 模式**演示用户端全流程（页面与交互完整），并在演示稿中**明确标注**「后端批次②③④⑤⑥⑦⑧⑨ 本期未交付、当前为前端替身（契约已定稿）」；
- 真实链路只演示 **P0 主链路 + 红包**（后端已具备）；
- 把本文档 §3/§4 两张表直接作为交叉验收时的「未完成模块说明」附件，避免现场口径不一。
