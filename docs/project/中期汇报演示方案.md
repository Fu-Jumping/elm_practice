# 中期汇报演示方案（2026-09-09）

> 定稿依据：2026-09-07 与组长的决策讨论，过程记录见 `docs/_archive/2026-09-07-中期汇报演示方案讨论.md`。
> 本文包含三部分：演示主线脚本表（§2）、种子数据需求清单（§3）、彩排检查单（§4），另附结课汇报预留素材清单（§5）。

## 1. 已确认口径（2026-09-07 组长拍板）

| 决策点 | 结论 |
| --- | --- |
| 总时长 | 老师未定，不锁死。脚本按"主线约 4:30–5:00、每步标参考耗时、可裁剪"设计，现场按老师给的口径伸缩 |
| 故事线 | 用户/商家双角色、双窗口闭环；模拟支付与接单状态推进两个加分点**视今晚（9/7）完成情况**决定是否演示，脚本含条件分支 |
| 页面取舍 | 只走主线页面；商家端其余 P0 页面与用户端次要页面用"已实现"一句话带过，不做现场操作 |
| 降级预案 | 不做三层兜底体系（演示前会反复验证）。仅保留零成本动作：9/8 彩排后录一遍完整演示视频；现场单步失败一律"重试一次 → 口头带过" |
| PPT | 不做。若 9/8 晚老师临时通知需要，用本文素材（分工表、里程碑、截图）半天内可拼最低版本，不提前投入 |
| 中期/结课分配 | 中期只讲结果与证据（演示 + 进度 + 一句工程化钩子）；工作流 SOP、AI 协作体系、TDD 红绿证据全部留给结课汇报（见 §5） |

## 2. 演示主线脚本表

### 2.1 现场布局与人员

- **讲解者**（建议组长成员E（组长））：面向大屏和老师讲，不看键盘；负责开场、全部讲解要点、收尾。
- **操作者**（建议成员B（用户端）或成员D（商家端/测试），**待定**）：只操作电脑，不背台词；提前把脚本实际点过三遍。
- **双窗口布局**（提前摆好并保存）：左侧用户端 H5 定宽约 420px（模拟手机屏，保证投屏字号可读）；右侧商家端桌面后台占余下宽度。全程**不切换窗口、不用 Alt+Tab**，只在两个窗口间点选。
- 演示浏览器提前准备：三端入口收藏夹、测试账号 13800000001 已登录、关闭系统通知与弹窗。

### 2.2 脚本表

> 失败兜底统一口径：**单步失败 = 操作者静默重试一次 → 仍失败则讲解者按该步"兜底话术"带过，直接进下一步，不停顿不解释技术原因**。整体不可用（服务器/环境异常）= 直接播放 9/8 彩排录制的完整演示视频。
> 标注【加分项】的步骤含条件分支，按今晚实际完成情况在 9/8 彩排时定稿去留。

| 编号 | 操作者 | 动作 | 预期结果 | 失败兜底 | 讲解要点 | 参考耗时 |
| --- | --- | --- | --- | --- | --- | --- |
| S0 | 讲解 | 开场白（不动电脑）：一句话项目定位 + 技术栈一句话 + "以下全部是真实前后端接口、服务器数据库现场运行，不是前端假数据" | — | — | "我们做的是一个饿了么式外卖平台，用户端 H5 加商家端桌面后台，Java 后端加 MySQL 数据库，三端独立工程。" | 0:30 |
| S1 | 操作 | 指向左侧窗口：用户端首页已在服务器数据加载完成态 | 首页展示 5 家店铺卡片（老王小店、肯德基宅急送、麦当劳、老胖烧烤、元盛居火锅），老胖烧烤带"临时闭店"标识 | 兜底话术："数据正在实时从服务器加载，请稍等"，同时讲 S1 要点拖时间 | "店铺、商品、状态全部来自服务器数据库。注意老胖烧烤是临时闭店状态——这个状态由商家端控制、双端实时同步，后面商家端会看到。" | 0:30 |
| S2 | 操作 | 点击"肯德基宅急送"卡片进入商家详情 → 依次选 2 个商品加入购物车 | 进入商家详情页，商品分类与商品列表正常展示；加购后底部购物车栏出现数量与金额 | 换一家店铺重试（麦当劳）；仍失败则讲解者带过："商家详情页按设计稿 1:1 复刻，包含分类、商品、购物车能力"，直接跳 S3 话术 | "这个页面是按高保真设计稿逐像素复刻的，购物车实时算价走后端接口。" | 0:45 |
| S3 | 操作 | 点击购物车栏进入确认订单页 | 展示收货地址（测试账号预置地址）、商品明细、金额合计（配送费等口径正确） | 兜底话术："确认订单页包含地址、明细与金额计算，规则由后端统一计价兜底"，跳下一步 | "金额计算在后端统一兜底，前端只是展示层——这是我们定的业务规则。" | 0:30 |
| S4 | 操作 | 【加分项·条件分支】点击提交订单 → 模拟支付页 → 点击支付 | 生成订单成功，支付成功提示，跳转订单详情/列表，状态"待接单" | **若模拟支付未完成**：直接点"提交订单"创建订单，讲解者带过"订单创建接口实时落库"；**若整步失败**：讲解者带过"订单创建链路已完成开发"，直接跳 S5 | "这里是模拟支付——真实支付需要商户资质，我们用模拟支付打通了完整支付状态机。" | 0:30 |
| S5 | 操作 | 【加分项·条件分支】鼠标移到右侧窗口：商家端后台 → 订单列表应出现刚创建的新订单（必要时刷新）→ 点击接单 → 状态推进（已接单/备餐中） | 右侧订单列表新增一条"待接单"订单（与 S4 下单一致）；点接单后状态变为"已接单" | **若接单推进未完成**：只展示订单列表中新订单出现（这本身就是双端同步证据），讲解者带过"商家端订单处理页已实现，状态推进在第二阶段扩展"；**若新订单未出现**：刷新一次；仍失败则讲解者指预置订单讲"商家端订单列表支持按状态筛选处理" | "注意右边——用户刚下的单已经实时出现在商家端了，这就是真实接口加数据库的效果。商家点接单，状态立刻推进。" | 0:45 |
| S6 | 操作 | 鼠标移回左侧窗口：用户端订单页（或下拉刷新） | 订单状态已同步为"商家已接单/备餐中" | 兜底话术："用户端订单状态页支持查看全部状态流转，双端由同一份数据驱动"，不强求画面刷新 | "用户这边不用做任何操作，状态自动跟上——双端同步靠数据，不靠人。" | 0:30 |
| S7 | 讲解 | 收尾（不动电脑）：进度对照 + 亮点 + 钩子 + 计划 | — | — | ① 进度："按里程碑，后端接口层、用户端主流程、商家端 P0 页面已全部完成并接入真实数据库。" ② 亮点三条："真实三端闭环、设计稿 1:1 复刻的工作流、TDD 测试先行。" ③ 钩子："我们还有一套从 PRD 到原型的完整工程化流程，细节留到结课汇报展开。" ④ 计划："下周进入第二阶段需求变更响应。" | 0:45 |

**合计**：全开约 4:45（含两个加分项）；需压缩时先压 S6 停留时间和 S7 亮点条数，主线 S1–S3+S5 为不可裁剪骨架。

## 3. 种子数据需求清单（9/7 晚冻结前核对，责任人：后端 B 成员A（后端B））

| # | 项目 | 要求 | 用途 |
| --- | --- | --- | --- |
| 1 | 店铺 5 家 | 老王小店、肯德基宅急送、麦当劳、老胖烧烤（**状态=临时闭店**）、元盛居火锅；营业中 4 家；配送费/起送价有值；店铺图片齐全（用 demo-images 素材包） | S1 首页展示 |
| 2 | 肯德基宅急送商品 | ≥6 个商品、≥2 个分类、价格覆盖两位数、商品图片齐全 | S2 加购演示 |
| 3 | 商品库存 | S2 所点商品库存 >0（建议 ≥10）；**不得为 0**（会触发加购拦截） | 避免 S2 翻车 |
| 4 | 测试账号 | 13800000001 可正常登录，会话在演示机上保持有效 | 全程 |
| 5 | 收货地址 | 该账号预置 1 条**默认地址**（姓名、电话、地址完整，建议用校区真实感地址） | S3 确认订单 |
| 6 | 购物车 | 演示账号购物车**预置为空**，无历史残留商品 | S2 干净起步 |
| 7 | 预置订单 | 建议挂在肯德基宅急送名下 3–5 条，覆盖不同状态：待接单 ×1、已接单/备餐中 ×1–2、已完成 ×1–2；创建时间为演示当日午前，避免"昨天下单"的违和感 | S5 商家端列表不空 |
| 8 | 计价口径 | 商品金额、配送费、合计与 PRD 计价规则一致，S3 页面数字肉眼可对账 | S3 讲解可信度 |
| 9 | 闭店可见性 | 老胖烧烤"临时闭店"状态在用户端首页有可见标识（置灰/角标/文案，按设计稿） | S1 亮点 |
| 10 | 订单状态文案 | 用户端与商家端对同一状态的文案一致（如"待接单""商家已接单"），避免双端叫法不同 | S6 同步说服力 |

## 4. 彩排检查单（2026-09-08 部署联调彩排）

### 4.1 环境与部署
- [ ] 服务器上三端可访问：用户端 H5、商家端后台、后端接口；MySQL 连接正常
- [ ] 教室网络之外可访问（手机热点实测一遍）；不依赖教室网络
- [ ] 演示笔记本定机：所有服务可一键启动，浏览器收藏夹含三端入口

### 4.2 种子数据
- [ ] 按 §3 清单 1–10 逐项核对打勾

### 4.3 全链路彩排
- [ ] 按脚本 S1–S6 实际走通一遍（含加分项则走加分项分支）
- [ ] 确定加分项去留：模拟支付、接单状态推进今晚（9/7）完成情况 → 9/8 彩排定稿脚本
- [ ] 计时走表一遍，记录实际总耗时，确认 ≤5 分钟

### 4.4 录屏（零成本兜底，务必做）
- [ ] 彩排通过后立即录制完整演示视频：1080p、按脚本节奏、带真实鼠标动作、时长 ≈ 脚本节奏
- [ ] 视频存演示笔记本本地 + 一份云/群备份
- **用途**：① 若老师不安排演示环节，播此视频替代现场操作；② 现场整体故障时的唯一兜底

### 4.5 投屏与演示机
- [ ] HDMI 投屏实测：分辨率、字体在教室尺寸下后排可读；左 H5 定宽窗口 + 右商家后台布局保存
- [ ] 演示机关闭系统通知/更新弹窗/输入法候选干扰；电源接通、息屏关闭

### 4.6 人员与台词
- [ ] 讲解者、操作者人选定稿；各自把脚本过一遍
- [ ] 两人合练 ≥2 遍：操作与讲解节奏对齐（讲解等操作，操作等讲解）

## 5. 结课汇报预留素材清单（中期不展开，随手归档防散失）

| 素材 | 出处/时机 | 结课用法 |
| --- | --- | --- |
| 工作流 SOP before/after 案例页 | 任选一个真实页面：设计稿（`docs/design/exports/**`）vs 上线代码截图对照 | 结课主讲页：PRD→原型→设计稿导出→1:1 复刻全程 |
| AI 协作与留痕体系 | `docs/project/项目规则.md`、`docs/record/raw/**`、TDD 红绿提交、PR #9 打回案例 | 结课讲方法论与质量保障 |
| 第二阶段需求变更响应包 | 9/8–9/14 的变更单、影响分析、新增测试、回归记录（中期时还不存在） | 结课独有素材，体现流程闭环 |
| 完整功能演示 | 9/15 冻结后：评价、消息、优惠、更多商家端页面 | 结课完整版演示（区别于中期主线） |
| 量化数据 | 提交数、PR 数、测试用例数、文档篇数（冻结后统计） | 结课收尾数据页 |
