# TDD 实施规划（测试先行）

日期：2026-09-01。适用范围：2026-09-04（第一批失败测试）至材料归档（第 4 周，具体日期以老师通知为准）的课程开发期。

2026-09-09 修订：同步第二阶段范围决议（除平台管理员外全部 P1/P2 纳入本期），更新第 5、6、8、10 章；老师下发的阶段 2 变更内容尚未发布（2026-09-10 起按「待定」跟踪：负责人判断大概率不发布，不作为开发前置阻塞），本规划只落本组范围决策，不视为老师变更已发布。

2026-09-09 二次修订：按负责人第二阶段遗留项逐条拍板结论回写——第 5 章同步订单状态机定稿（支付成功即 `PENDING`，`PROCESSING` 仅 P0 基线）、定稿金额公式（新增 `deliveryFee` 快照、金额明细「基础四行 + 优惠项按实际发生展示」（2026-09-11 扩展））、会员价与会员折扣不叠加、优惠配置结构、图片上传参数、搜索排序与分页口径；第 8 章按 10 个批次顺序推进并新增批次表。

**文档定位**：本文件是小组"测试先行（TDD）"工作方式的执行口径，回答三个问题——每个功能怎么按"红-绿-重构"推进、Git 里要留下什么证据、各 P0 模块要测哪些正常与异常用例。用例矩阵由测试联调同学（成员D（商家端/测试））牵头维护，各模块负责人维护自己模块的行；SRS 与接口契约变化时同步回填。

**前提（工程背景）**：单仓库多目录组织——`frontend/user-h5/`（用户端 Vue 3 + Vite + Pinia）、`frontend/merchant-admin/`（商家端 React 18 + Vite + Ant Design 5）、`backend/`（Java Spring Boot + MyBatis，数据库定为 MySQL 8（见 §10.1））。后端测试框架 JUnit 5，用户端 Vitest。关键时间口径：**9/8（或 9/9）进行第一阶段中期验收，P0 基础功能须于 9/7 晚完成并于当晚验收演练**；9/5–9/6 为周末冲刺日（9/5 注册登录/权限与商品分类管理，9/6 购物车与店铺浏览链路）；验收后立即进入第二阶段需求变更。

**关联文档**：课程口径依据课程要求；需求与排期见 `docs/PRD.md`；接口与错误码见 `docs/backend/后端接口契约.md`；分工与里程碑见 `docs/project/分工与里程碑安排.md`；AI 使用记录见 `docs/project/项目规则.md`。本文不重复这些文档的内容，只做引用和落地。

---

## 1. 为什么测试先行（课程要求摘录）

以下均为课程要求原文摘录，是本规划的直接依据：

> 今年强制强调**测试先行**。基本要求是：1. 先理解需求。2. 先设计单元测试。3. 明确正常情况和异常情况。4. 再编写业务代码。5. 业务代码通过测试后，才能认为这个功能基本完成。

> 老师特别强调，测试的重要价值就是主动验证这些**异常场景**。

（课程要求以"注册用户"为例，要求不能只测"正确手机号 + 正确密码 → 注册成功"，还要测：手机号只有 10 位、手机号有 12 位、手机号包含字母、手机号已经存在、密码长度不足、密码不符合复杂度要求。这六条已完整纳入第 5.1 节用例矩阵，编号 TC-ACC-002~007。）

> 第二周注意事项：2. 不能只测试正常情况，要重点测试异常情况。

关于业务规则归属：

> **前端校验：主要为了用户体验。** **后端校验：真正保证业务规则。**"手机号是否已经注册"这种需要查询数据库的逻辑，更必须放在后端。

因此本规划的用例矩阵以后端 Service/接口层为主战场，前端用例只覆盖归一化、状态逻辑和核心交互。

关于交叉验收与自测的边界（课程要求）：

> 不能因为第三周有人帮你测试，就不做自己的测试。1. 自己开发过程中必须做**单元测试**。2. 项目内部也应该进行必要的集成测试。3. 交叉验收时，其他组主要从完整系统角度做集成和验收测试。

关于 Git 可见性（课程要求）：

> 必须持续提交，不能前两周仓库为空，最后一天一次性上传。不能通过强制覆盖等方式把历史提交记录抹掉，因为老师会通过仓库记录观察项目过程和个人贡献。

即：测试先行不只是开发习惯，还必须能从提交历史里**被看见**（第 3 章）。

---

## 2. TDD 工作流（红-绿-重构）

### 2.1 什么是一个"功能单元"

- 后端：一个接口的一条业务规则，或一个 Service 方法。例如"注册时重复账号返回 409"是一个单元，"整个注册模块"不是。
- 用户端：一个 store action、一个 normalizer/工具函数，或一个核心组件交互（如购物车弹层减到 0 移除该行）。
- 商家端：一个表单校验规则或一个状态标签渲染分支。

### 2.2 标准循环（每个功能单元）

| 步骤 | 动作 | 完成标志 |
| --- | --- | --- |
| ① 红 | 从第 5 章矩阵挑本单元用例，写测试并运行。失败原因必须是"功能未实现"（断言不匹配、404 等），不是测试自身写错 | 测试红；首次失败的关键输出贴入 raw/ 片段（`docs/record/raw/YYYY-MM-DD/`，禁止直接写 daily，次日汇总进 A 表） |
| ② 绿 | 写**最小**业务代码让测试通过，不预做矩阵之外的功能 | 测试全绿 |
| ③ 重构 | 消除重复、抽取公共校验；重跑本单元及受影响用例保持绿 | 无行为变化，测试仍绿 |
| ④ 提交 | `test:` 提交（红）在前，`feat:` 提交（绿）在后；**`test:` 提交仅含测试代码与测试脚手架，业务实现一律进 `feat:` 提交**（见第 3 章） | `git log` 可查顺序；在 `test:` 提交上复跑测试为红 |

约束：

- 重构改动原则上并入本次 `feat:` 提交（`分工与里程碑安排.md` 的类型前缀集合为 `feat:` `fix:` `test:` `docs:` `chore:`，无 `refactor:`）；确需独立提交时用 `chore:` 并在提交信息正文说明。
- 禁止先写实现再补测试提交；禁止用无断言的测试凑数；禁止用 `@Disabled` / `skip` 让红测试"看起来绿"。
- 提交粒度：一个功能单元一批（红 + 绿），当天收工前提交，不攒批。
- **拆分提交（哪些文件进 `test:`、哪些进 `feat:`）由人执行，或交给 AI 时指令明确到文件级清单**；push 前自检：切到 `test:` 提交复跑测试必须红，`feat:` 后全量转绿（2026-09-06 修订，源自 PR #9 打回教训）。

### 2.3 "功能完成"的门槛（Definition of Done）

一个功能单元满足以下全部条件才算完成：

1. 该单元在用例矩阵中的用例（正常 + 至少 1 条异常）全部执行且通过——**异常用例全绿 + 业务代码通过测试**；
2. 该单元实际会出现的错误码语义（400/401/403/404/409）至少各有一条用例覆盖；
3. 响应符合统一结构（`code`/`message`/`data`，错误返回 JSON 而非 HTML 错误页）；
4. Git 中 `test:`（红）提交早于对应 `feat:`（绿）提交，且 `test:` 提交仅含测试代码与脚手架（在该提交上复跑测试必须失败，红）；
5. 原有测试没有被删除、跳过或放宽断言；
6. 当日 `docs/record/daily/` A 表有一行记录（含提交号）。

模块级门槛：该模块矩阵内全部编号用例通过，且按接口契约第 8 节模板填写至少一条模块联调记录。

---

## 3. Git 证据规范

### 3.1 规则

依据 `docs/project/分工与里程碑安排.md` 第 3 节与课程要求：

- 每人用自己的账号提交，禁止代提交、禁止最后一天整包上传。
- **TDD 证据靠提交内容与顺序共同体现：`test:` 提交仅含测试代码与测试脚手架，业务实现一律进对应 `feat:` 提交；同一功能的测试先提交（在该提交上复跑必须红），实现后提交（绿），不事后补测试提交。红绿两端证据（红 X failed / 绿 Y passed）写入 PR 描述或 raw/ 片段。**
- 提交信息用中文，类型前缀用英文：`feat:` `fix:` `test:` `docs:` `chore:`。
- 禁止 force push 或任何抹除历史的操作；改代码走分支 + PR，review 规则见 `小组协作与提交流程.md`。
- AI 深度参与产生的提交，按 `docs/project/项目规则.md` 第 3 节在提交信息正文末尾追加 `AI协作: <一句话说明>`。

### 3.2 提交序列示例（符合本项目业务，中文信息 + 英文前缀）

序列一：账号模块（9/5，后端 A）

```text
test: 用户注册失败测试：10位/12位/含字母手机号、重复注册、密码过短（预期红）
feat: 实现注册接口字段校验与重复账号 409 拦截
fix: 修复密码长度下限校验边界（5 位密码未被拦截）
```

序列二：购物车模块（9/6，后端 B）

```text
test: 购物车异常用例：数量为0、加购超库存、未登录返回401（预期红）
feat: 实现购物车加入、同商品数量合并与删除接口
fix: 修复同商品重复加购未合并数量的问题
```

序列三：订单创建模块（9/7，后端 B + 前端主力）

```text
test: 订单创建失败测试：库存不足、店铺关闭、地址归属校验（预期红）
feat: 实现订单创建事务与商品、地址快照保存
test: 购物车数量合并与金额归一化测试（用户端 stores/normalizers）
feat: 用户端确认订单页接入创建订单接口与金额回显
```

说明：每对 `test:`→`feat:`构成一次红-绿循环；`fix:`提交是回归发现的缺陷修复，修复时若有新用例，先补 `test:` 再 `fix:`。老师课堂示例（`feat: add user registration` / `test: add invalid phone tests` / `fix: reject duplicated users`）体现的就是这个粒度；本项目在此基础上额外要求 `test:` 早于 `feat:`。

### 3.3 老师会查什么（对应自查方式）

| 老师关注点（课程要求） | 本项目自查方式 |
| --- | --- |
| 持续真实提交，不是最后一天整包上传 | `git log` 按日期分布检查；每日记录 A 表逐日可对 |
| 每人用自己的账号、贡献真实可查 | `git log --author=<本人账号>`（分工文档第 4 节答辩自查项） |
| 历史不被抹掉 | 禁止 force push；分支合并保留历史 |
| 测试先于实现 | `git log --follow <测试文件路径>` 首次提交早于对应实现提交；抽查在 `test:` 提交上复跑测试应失败（红），`test:` 提交 diff 中不含业务实现文件 |
| 能否解释自己提交的代码 | 答辩前按分工文档第 4 节"每人贡献自查"过一遍 |
| 原有测试没有被无故删除 | 冻结前对比矩阵用例数与现存测试数；PRD 埋点方案的 TDD 行要求"测试先于实现，原有测试没有被无故删除" |

---

## 4. 测试分层与工具

### 4.1 后端（JUnit 5，`backend/src/test/java`）

| 层 | 内容 | 工具 | 说明 |
| --- | --- | --- | --- |
| Service 单元测试 | 业务规则：注册校验、金额计算、状态机、资源归属 | JUnit 5 + Mockito 打桩 DAO | 不连数据库，跑得快，是第 2 章循环的主战场；400/409 类规则在此层先红 |
| 接口集成测试 | Controller + 统一响应结构 + 会话/权限 | MockMvc（首选，同进程快）或 TestRestTemplate | 断言 HTTP 状态码 + `code`/`message`/`data` 结构；401/403/404 与 Session 相关用例主要在此层验证 |
| 数据层 | 建表脚本可重复执行、种子数据 | 测试环境数据库（见下） | 验证事务回滚、唯一约束 |

**数据库测试环境（已于 2026-09-10 结案，见 §10.1）**：

- ~~方案 A：H2 内存库 MySQL 兼容模式~~（**不采用**：§10.1 已结案数据库定为 MySQL 8，不使用 H2 兼容模式）。
- 方案 B（**采用**）：测试统一走服务器独立测试库 `elm_practice_test`（规则 R8，见 `docs/project/运行说明.md` §4.5）；每用例前以 `reset.sql` 复位，禁止对业务库执行重置类测试。
- 结论：Service 单测一律不碰库；接口集成测试统一连服务器测试库 `elm_practice_test`，不采用 H2。测试数据在各用例内自建，用例之间不依赖执行顺序。

### 4.2 用户端（Vitest，`frontend/user-h5`）

| 优先测 | 内容 | 说明 |
| --- | --- | --- |
| services/normalizers | 后端字段 → 页面字段归一化：缺字段不产生 `undefined` 展示、金额两位小数、时间 `yyyy-MM-dd HH:mm:ss` | 纯函数，最易测、收益最高 |
| stores（Pinia） | 购物车同商品数量合并、合计金额、跨商家隔离提示、订单状态到展示文案的映射 | 状态逻辑不等价于后端规则，只测前端职责部分 |
| 工具函数 | 手机号/密码/11 位电话校验、格式化 | 与后端规则保持同一口径，测试数据从契约推导 |
| 核心组件交互 | 购物车弹层（步进加减、减到 0 移除、售罄灰化）、确认订单页（提交禁用条件、金额以后端为准） | 用 @vue/test-utils + jsdom；**只挑这两个核心交互，不做全页面组件测试** |
| 布局与关键路径（E2E） | 布局类断言（贴底、遮挡、间距等几何关系）与 3–5 条关键路径 | Playwright，用例 `frontend/user-h5/e2e/`，`npm run test:e2e`（2026-09-08 新增分层：jsdom 无布局引擎且不加载 SFC scoped CSS，几何断言只能落在此层） |

不设覆盖率百分比指标（见第 10 章）。

### 4.3 商家端（React Testing Library，`frontend/merchant-admin`）

轻量覆盖即可，不追求页面全覆盖：

- 表单校验：商品表单必填、价格 ≥0、库存非负的提示是否出现（Vitest + RTL + jsdom）；
- 状态渲染：店铺三态（OPEN/CLOSED/TEMPORARILY_CLOSED）与售罄标签的文案和语义色正确渲染；
- **本期新增页面（2026-09-09 决议）同样按轻量覆盖**：优惠配置表单保存与校验、评价管理（含回复抽屉）提交与回显、消息会话未读、统计页空数据与查询范围提示、规格编辑与图片上传校验；接口契约未收口前只测前端渲染与表单校验，不测真实取数。
- AntD 组件行为本身不重复测试。

### 4.4 证据留痕

- 测试运行结果以命令输出/测试报告为证据，关键节点（首批失败测试、模块全绿、冻结前回归）以 raw/ 片段留痕（或提交信息"记录:"行），随次晨汇总进入每日记录 A 表；**同一人当天只写一个片段文件**（`docs/record/raw/YYYY-MM-DD/<成员或角色>.md`，后续轮次追加小节，2026-09-10 起，见 `docs/project/项目规则.md` §2）；
- 后端构建与核心测试通过是 PRD 7.18 的交付要求；用户端、商家端 `npm run build` 与后端构建在 9/7 全链路联调和 9/15 冻结前各跑一次并留日志。

---

## 5. 测试用例矩阵（P0 核心 + 本期扩展，核心章节）

使用说明：

- 本矩阵是**代表性用例清单**，不是穷举；执行中发现的缺陷必须回补成用例（先 `test:` 后 `fix:`）。
- "层"取值：后端单测 / 后端集成 / 双端 / 前端（用户端或商家端）。
- 错误码依据 `docs/backend/后端接口契约.md` 第 1.3 节：`400` 格式/必填/范围不合法，`401` 未登录或 Session 失效，`403` 角色不匹配或无权访问，`404` 资源不存在或归属不符时按安全需要统一返回不存在，`409` 重复注册/状态冲突/库存金额版本已变化，`500` 未预期错误。
- 契约未逐条固定状态码的少数场景（店铺关闭加购、有商品分类删除等）在表中标"口径 SRS 固化"，SRS 定稿后回填，不得凭空写码。
- 带有"（P1 扩展）"的用例与第 5.9~5.20 节的扩展模块用例组，对应 2026-09-09 第二阶段范围决议纳入本期的全部 P1/P2（平台管理员除外）；在 P0 用例全绿后按模块分批实施红-绿，未收口模块标注“本期实现（待开发）”，证据字段写“待补”。

### 5.1 账号注册/登录（TC-ACC）

依据：契约 §3.1；PRD 7.1。口径：账号为手机号格式；密码长度 6-20 位（PRD 7.16.1），复杂度规则由 SRS 固化后回填 TC-ACC-007。

| 编号 | 场景 | 类型 | 预期结果 | 层 |
| --- | --- | --- | --- | --- |
| TC-ACC-001 | 合法手机号 + 合法密码注册 | 正常 | 成功，返回用户摘要；响应与日志不含密码 | 后端集成 |
| TC-ACC-002 | 手机号只有 10 位（课程示例） | 异常 | `400`，message 指出账号格式错误 | 后端单测+集成 |
| TC-ACC-003 | 手机号有 12 位（课程示例） | 异常 | `400` | 后端单测+集成 |
| TC-ACC-004 | 手机号包含字母（课程示例） | 异常 | `400` | 后端单测+集成 |
| TC-ACC-005 | 手机号已注册，重复注册（课程示例） | 异常 | `409`，提示账号已存在 | 后端单测+集成 |
| TC-ACC-006 | 密码长度不足（课程示例，少于 6 位） | 异常 | `400` | 后端单测+集成 |
| TC-ACC-007 | 密码不符合复杂度要求（课程示例；规则按 SRS 固化） | 异常 | `400` | 后端单测+集成 |
| TC-ACC-008 | 缺少昵称等必填字段 / 密码超过 20 位 | 异常 | `400` | 后端单测 |
| TC-ACC-009 | 前端注册表单校验（必填、两次密码一致、协议勾选） | 异常 | 不发请求，逐项提示 | 前端 |
| TC-ACC-010 | 正确账号 + 密码登录 | 正常 | 成功，`Set-Cookie` 建立会话 | 后端集成 |
| TC-ACC-011 | 密码错误 | 异常 | `401`，统一提示账号或密码错误 | 后端集成 |
| TC-ACC-012 | 账号不存在（按契约与密码错误同口径） | 异常 | `401`，不泄露"账号不存在"细节 | 后端集成 |
| TC-ACC-013 | 未登录访问 `GET /api/v1/me` | 异常 | `401` | 后端集成 |
| TC-ACC-014 | 注销后（Session 失效）访问受保护接口 | 异常 | `401` | 后端集成 |
| TC-ACC-015 | 修改昵称/性别后查询回显最新 | 正常 | 成功 | 后端集成 |
| TC-ACC-016 | 前端登录失效处理：`401` 后保留当前地址跳登录，登录后回原地址 | 异常 | 跳转与回跳正确 | 前端 |

### 5.2 商家注册与开店/关店（TC-MER）

依据：契约 §4.1；PRD 7.1、6.15。口径：注册即自动建店，初始营业状态固定 `CLOSED`；店铺归属以 Session 为准，不信任请求体 `storeId`。

| 编号 | 场景 | 类型 | 预期结果 | 层 |
| --- | --- | --- | --- | --- |
| TC-MER-001 | 合法信息注册商家 | 正常 | 成功；自动建店且状态 `CLOSED` | 后端集成 |
| TC-MER-002 | 商家账号重复注册 | 异常 | `409` | 后端 |
| TC-MER-003 | 店铺名称为空 / 联系电话格式不合法 | 异常 | `400` | 后端 |
| TC-MER-004 | 商家登录成功，会话绑定店铺 | 正常 | `GET /merchant/me` 返回店铺归属 | 后端集成 |
| TC-MER-005 | 商家密码错误 | 异常 | `401` | 后端集成 |
| TC-MER-006 | 开店：`CLOSED`→`OPEN` | 正常 | 成功；用户端可查询到该店并可下单 | 双端 |
| TC-MER-007 | 关店/临时闭店：→`CLOSED`/`TEMPORARILY_CLOSED` | 正常 | 成功；用户端展示不可购买 | 双端 |
| TC-MER-008 | 传入非法状态值（如 `PAUSED`） | 异常 | `400` | 后端 |
| TC-MER-009 | 未登录修改营业状态 | 异常 | `401` | 后端集成 |
| TC-MER-010 | 请求体携带他人 `storeId` 试图改别家店铺 | 异常 | 归属以 Session 为准，返回 `404`（防资源枚举） | 后端集成 |
| TC-MER-011 | 用户身份调用商家接口 | 异常 | `403` 角色不匹配 | 后端集成 |
| TC-MER-012 | 商家端状态标签渲染（三态文案与语义色） | 正常 | 三态显示正确 | 前端（商家端） |

### 5.3 店铺状态与查询（TC-STO）

依据：契约 §3.2。口径：`status` 取 `OPEN`/`CLOSED`/`TEMPORARILY_CLOSED`；空列表返回空数组不是错误。

| 编号 | 场景 | 类型 | 预期结果 | 层 |
| --- | --- | --- | --- | --- |
| TC-STO-001 | 查询店铺列表 | 正常 | 成功；无数据返回 `data: []` | 后端集成 |
| TC-STO-002 | `keyword`/`categoryId`/`sort`（综合/销量/距离）过滤排序 | 正常 | 结果与参数一致 | 后端集成 |
| TC-STO-003 | 非法 `sort` 取值 | 异常 | `400` | 后端 |
| TC-STO-004 | 店铺详情返回三态 status | 正常 | 成功 | 后端集成 |
| TC-STO-005 | `storeId` 不存在 | 异常 | `404` | 后端集成 |
| TC-STO-006 | 闭店店铺：加购/结算入口禁用并提示；直连接口被后端拒绝 | 异常 | 前端禁用 + 后端拒绝（`409` 状态冲突，口径 SRS 固化） | 双端 |
| TC-STO-007 | 店铺分类/商品查询只返回该店铺数据 | 正常 | 不串店 | 后端集成 |

### 5.4 分类管理（TC-CAT）

依据：契约 §4.2。口径：分类名称不能在同一店铺重复；有商品的分类不能静默删除；分类批量绑定商品为本期实现（待开发）。

| 编号 | 场景 | 类型 | 预期结果 | 层 |
| --- | --- | --- | --- | --- |
| TC-CAT-001 | 新增分类成功 | 正常 | 成功 | 后端 |
| TC-CAT-002 | 同一店铺分类名重复 | 异常 | `409` | 后端 |
| TC-CAT-003 | 分类名为空 / 超长 | 异常 | `400` | 后端 |
| TC-CAT-004 | 修改分类成功 | 正常 | 成功 | 后端 |
| TC-CAT-005 | 删除仍有商品的分类 | 异常 | `409`（状态冲突），提示先处理商品归属，不能静默删除 | 后端 |
| TC-CAT-006 | 删除空分类成功 | 正常 | 成功 | 后端 |
| TC-CAT-007 | 修改/删除不存在的分类 | 异常 | `404` | 后端 |
| TC-CAT-008 | 商家 A 会话操作商家 B 的分类 | 异常 | `404`（归属不存在） | 后端集成 |
| TC-CAT-009 | 未登录调用分类管理接口 | 异常 | `401` | 后端集成 |
| TC-CAT-010 | 分类批量绑定商品（本期实现·待开发） | 正常 | 绑定成功，商品归属目标分类；重复绑定幂等；不改变商品价格与库存 | 后端 |
| TC-CAT-011 | 绑定他人店铺商品、绑定到不存在分类（本期实现·待开发） | 异常/权限 | `404`；不改变任何商品归属 | 后端集成 |

### 5.5 商品管理（TC-PRD）

依据：契约 §4.2、§3.2；PRD 7.12。口径：价格 ≥0 保留两位小数；库存为非负整数；库存为 0 售罄禁止加购；下架商品用户端不得展示为可购买；删除商品仅限下架状态，在售商品删除返回 `409` 并提示先下架（TC-PRD-012~014）。

| 编号 | 场景 | 类型 | 预期结果 | 层 |
| --- | --- | --- | --- | --- |
| TC-PRD-001 | 新增商品（名称/分类/价格/库存/上下架） | 正常 | 成功 | 后端 |
| TC-PRD-002 | 价格为负或非法 | 异常 | `400` | 后端 |
| TC-PRD-003 | 库存为负数或非整数 | 异常 | `400` | 后端 |
| TC-PRD-004 | 名称/分类缺失 | 异常 | `400` | 后端 |
| TC-PRD-005 | 修改商品信息成功 | 正常 | 成功 | 后端 |
| TC-PRD-006 | 上下架切换（availability） | 正常 | 成功；下架商品用户端不可购买 | 双端 |
| TC-PRD-007 | 库存改为 0 | 正常 | 用户端展示售罄并禁止加购 | 双端 |
| TC-PRD-008 | 修改/查询不属于本店的商品 | 异常 | `404` | 后端集成 |
| TC-PRD-009 | `productId` 不存在 | 异常 | `404` | 后端集成 |
| TC-PRD-010 | 商品列表按分类筛选 | 正常 | 成功 | 后端集成 |
| TC-PRD-011 | 商家端商品表单校验提示（必填/价格/库存） | 异常 | 逐项提示，不发非法请求 | 前端（商家端） |
| TC-PRD-012 | 删除下架状态商品 | 正常 | 成功；商品从商品列表移除（物理删除） | 后端 |
| TC-PRD-013 | 删除在售状态商品 | 异常 | `409`，提示先下架再删除 | 后端 |
| TC-PRD-014 | 删除商品后历史订单商品快照不变 | 正常 | 旧订单详情仍完整展示商品名称、单价与金额快照 | 后端集成 |

### 5.6 购物车（TC-CRT）

依据：契约 §3.4。口径：购物车行唯一范围是"当前用户 + 店铺 + 商品"，重复加购合并数量；数量必须大于 0；后端重新读取商品价格与上下架状态，客户端单价不作计价依据。

| 编号 | 场景 | 类型 | 预期结果 | 层 |
| --- | --- | --- | --- | --- |
| TC-CRT-001 | 加入购物车成功 | 正常 | 成功；购物车栏数量金额刷新 | 双端 |
| TC-CRT-002 | 同一商品重复加购合并数量 | 正常 | 单行数量累加 | 后端 |
| TC-CRT-003 | 查询指定店铺购物车 | 正常 | 只含当前用户该店行 | 后端集成 |
| TC-CRT-004 | 修改数量（PATCH cartLineId） | 正常 | 成功 | 后端 |
| TC-CRT-005 | 数量为 0 或负数 | 异常 | `400`（契约：数量必须大于 0）；前端减到 0 转删除请求 | 双端 |
| TC-CRT-006 | 删除购物车行 | 正常 | 成功 | 后端 |
| TC-CRT-007 | 加购数量超过当前库存 | 异常 | 非成功（`400`/`409`，口径 SRS 固化），message 含库存上限（对齐契约 details 示例） | 后端 |
| TC-CRT-008 | 加购已下架商品 | 异常 | 非成功（`400`/`409`，口径 SRS 固化） | 后端 |
| TC-CRT-009 | 店铺关闭时加购/结算 | 异常 | 前端禁用 + 后端拒绝（`409` 状态冲突，口径 SRS 固化） | 双端 |
| TC-CRT-010 | 未登录操作购物车 | 异常 | `401` | 后端集成 |
| TC-CRT-011 | 操作他人 `cartLineId` | 异常 | `404` | 后端集成 |
| TC-CRT-012 | 跨商家隔离：含 A 店商品时进入 B 店结算给出提示 | 异常 | 按商家隔离展示与提示 | 前端 |
| TC-CRT-013 | 后端重读价格计价 | 正常 | 计价以数据库当前价为准 | 后端 |

### 5.7 订单创建与查询（TC-ORD）

依据：契约 §3.5、§5、§7；PRD 7.16、7.6。口径：订单状态机按 2026-09-09 定稿——P0 基线创建订单成功后状态为 `PROCESSING`（可查询、不流转），**支付成功即写 `PENDING`（待接单），`PROCESSING` 不再作为支付后状态**（PRD 7.6），TC-ORD 相关用例按此断言；订单头、明细快照、地址快照与购物车清空在同一事务；`expectedTotal` 只用于提示；实付金额 = 商品小计 − 满减 − 新客立减 − 红包 − 会员折扣 + 配送费（默认 ¥3.00，店铺可配、未配置按 0） − 配送费优惠 + 打包费（¥2.00），且不小于 0（P0 无优惠时退化为商品小计 + 打包费）；金额快照含 `packagingFee` 与新增 `deliveryFee`（TC-ORD-021~022）；待支付倒计时与再来一单为本期实现（待开发）。

| 编号 | 场景 | 类型 | 预期结果 | 层 |
| --- | --- | --- | --- | --- |
| TC-ORD-001 | 从购物车创建订单成功 | 正常 | 成功；P0 基线状态 `PROCESSING`（支付成功后按定稿为 `PENDING`）；返回订单号 | 后端集成 |
| TC-ORD-002 | 订单保存商品快照、地址快照、金额、创建时间 | 正常 | 详情与创建时一致；商品改价不影响旧订单快照 | 后端集成 |
| TC-ORD-003 | 创建成功清空该用户该店购物车 | 正常 | 事务提交后该店购物车行为空 | 后端集成 |
| TC-ORD-004 | 缺少 `addressId`/`storeId` | 异常 | `400` | 后端 |
| TC-ORD-005 | `addressId` 不属于当前用户 | 异常 | `404` | 后端集成 |
| TC-ORD-006 | 店铺已关闭时下单 | 异常 | `409`（状态冲突，口径 SRS 固化） | 后端 |
| TC-ORD-007 | 库存不足 | 异常 | `409`，message 指出具体商品与可用数量；不创建半成品订单 | 后端 |
| TC-ORD-008 | 商品已下架时下单 | 异常 | `409`（状态冲突） | 后端 |
| TC-ORD-009 | 不满足起送条件 | 异常 | 非成功并提示起送金额（`400`/`409`，口径 SRS 固化） | 后端 |
| TC-ORD-010 | 未登录创建订单 | 异常 | `401` | 后端集成 |
| TC-ORD-011 | `expectedTotal` 与后端计算不一致 | 正常 | 以后端计算为准 | 后端 |
| TC-ORD-012 | 事务回滚：任一步失败时库存、订单、购物车保持原状 | 异常 | 无半成功数据 | 后端集成 |
| TC-ORD-013 | 订单列表按创建时间倒序、支持 `status` 筛选 | 正常 | 成功 | 后端集成 |
| TC-ORD-014 | 查询他人订单详情 | 异常 | `404` | 后端集成 |
| TC-ORD-015 | `orderId` 不存在 | 异常 | `404` | 后端集成 |
| TC-ORD-016 | 订单明细查询（含在详情或独立接口） | 正常 | 成功 | 后端集成 |
| TC-ORD-017 | （P1 扩展）跳级推进：待接单直接跳已完成 | 异常 | `409`，拒绝跳级 | 后端 |
| TC-ORD-018 | （P1 扩展）重复推进/重复支付幂等 | 正常 | 返回当前结果，状态不二次变化、不重复扣库存 | 后端 |
| TC-ORD-019 | （P1 扩展）待支付超时（15 分钟）后支付 | 异常 | `409`；库存不自动回补（课程简化规则） | 后端 |
| TC-ORD-020 | （P1 扩展）商家推进他人店铺订单 | 异常 | `404` | 后端集成 |
| TC-ORD-021 | 创建订单实付金额按定稿公式计算 | 正常 | 实付 = 商品小计 − 满减 − 新客立减 − 红包 − 会员折扣 + 配送费（默认 ¥3.00，店铺可配、未配置按 0） − 配送费优惠 + 打包费（¥2.00），且不小于 0；由后端计价（P0 无优惠时退化为商品小计 + 打包费） | 后端集成 |
| TC-ORD-022 | 订单金额快照含 `packagingFee` 与新增 `deliveryFee` | 正常 | 订单详情返回商品小计、`packagingFee`、`deliveryFee` 与实付金额，且与定稿公式一致；确认订单页、订单详情与支付页金额明细为「**基础四行 + 优惠项按实际发生展示**」（基础四行 / 优惠项按实际发生展示，2026-09-11 定稿，CHG-004）（基础四行：商品小计 / 打包费 / 配送费 / 实付金额；优惠项金额非 0 时各占一行、未发生不显示） | 后端集成 |
| TC-ORD-023 | 再来一单：按历史订单重建购物车（本期实现·待开发） | 正常 | 按历史订单商品与数量重建购物车；商品下架、售罄或库存不足时给出提示并按页面规则处理，不直接生成订单 | 双端 |
| TC-ORD-024 | 待支付倒计时与超时（本期实现·待开发） | 边界 | 倒计时起点为订单创建时间、时长 15 分钟且不由前端延长；超时后支付被拒绝（`409`）且库存不自动回补 | 双端 |

> 注：商家订单查询接口（`GET /api/v1/merchant/orders`、`GET /api/v1/merchant/orders/{orderId}`）已定 P0（2026-09-02 负责人拍板，免评审），其用例随 9/4 首批失败测试由测试联调同学补充，不在本矩阵中预先编造。

### 5.8 地址管理（TC-ADR）

依据：契约 §3.3；PRD 7.9。口径：联系人必填、电话 11 位、地址必填；删除默认地址后由后端把剩余地址中最近更新的一条设为默认，无剩余时默认为空。

| 编号 | 场景 | 类型 | 预期结果 | 层 |
| --- | --- | --- | --- | --- |
| TC-ADR-001 | 新增地址成功 | 正常 | 成功 | 后端 |
| TC-ADR-002 | 联系人缺失 / 电话非 11 位 / 地址缺失 | 异常 | `400` | 后端 |
| TC-ADR-003 | 设为默认后其他地址取消默认 | 正常 | 仅一条默认 | 后端 |
| TC-ADR-004 | 删除默认地址后自动指定最近更新一条为默认 | 正常 | 成功；无剩余地址时默认为空 | 后端集成 |
| TC-ADR-005 | 编辑地址回显与保存 | 正常 | 成功 | 后端 |
| TC-ADR-006 | 查询只返回当前用户地址 | 正常 | 成功；空列表返回 `[]` | 后端集成 |
| TC-ADR-007 | 访问/删除他人地址 | 异常 | `404` | 后端集成 |
| TC-ADR-008 | 未登录查询地址 | 异常 | `401` | 后端集成 |
| TC-ADR-009 | 前端表单校验（必填/电话格式/保存中禁用） | 异常 | 逐项提示；失败保留输入 | 前端 |

### 5.9 优惠与后端统一计价（TC-PRV，本期实现·待开发）

依据：PRD 7.4、7.13、7.17；契约 §6 优惠配置接口（`GET /merchant/promotions`、`PUT /merchant/promotions`）。口径：**实付 = 商品小计 − 满减 − 新客立减 − 红包 − 会员折扣 + 配送费（默认 ¥3.00，店铺可配、未配置按 0） − 配送费优惠 + 打包费（¥2.00），且不小于 0**（2026-09-09 定稿）；优惠配置数据结构为 `promotions` 扩列 `new_user_amount`、`free_delivery_threshold`、`member_discount`，满减阶梯新建 `promotion_tiers(store_id, threshold, amount, sort_order)`，开发库旧单档数据重置种子并提供迁移脚本、不做静默兼容；演示数值为满 20 减 2、满 40 减 5、新客立减 3 元、免配送费门槛 30 元、会员 95 折，红包门槛与适用范围由种子数据定义、一单一红包；金额一律以后端计算结果为准。

| 编号 | 场景 | 类型 | 预期结果 | 层 |
| --- | --- | --- | --- | --- |
| TC-PRV-001 | 商品小计 25 元命中满 20 减 2 | 正常 | 最终金额按固定顺序计算并由后端返回；金额随订单快照保存，前端只展示 | 后端单测+集成 |
| TC-PRV-002 | 小计 45 元同时满足满 20 与满 40 阶梯 | 正常 | 只取最大满足阶梯（减 5），两档不叠加 | 后端单测 |
| TC-PRV-003 | 小计恰好等于门槛 20 元 | 边界 | 按“满足门槛”命中满减，边界值不因取整或浮点误差漏减 | 后端单测 |
| TC-PRV-004 | 店铺新客首单与第二单 | 正常 | 首单立减 3 元；第二单不再享受；同一用户重复请求不重复立减 | 后端单测+集成 |
| TC-PRV-005 | 小计达 30 元免配送费门槛 | 正常 | 配送费优惠只作用于配送费，不改变商品小计与满减基数 | 后端单测 |
| TC-PRV-006 | 会员用户与非会员用户同单对比 | 正常 | 会员按 95 折作用于商品小计（配置了 `memberPrice` 的商品按会员价计价、不再打折）；非会员不享受；两者订单快照金额可复算 | 后端单测+集成 |
| TC-PRV-007 | 优惠合计超过应付基数（负金额边界） | 边界 | 最终金额不小于 0，超出部分被截断而不是返回负数 | 后端单测 |
| TC-PRV-008 | 优惠配置为负数、折扣率越界（超出 0~1）、优惠已过期 | 异常 | 配置被拒绝（`400`）或限制到合法范围，不落库；用户端计价不出现负金额 | 后端单测+集成 |
| TC-PRV-009 | 前端篡改 `expectedTotal` 与商品改价后再提交 | 异常 | 以后端计算结果为准；订单金额与快照一致，页面金额与接口一致 | 双端 |
| TC-PRV-010 | 商家 A 读取/保存商家 B 的优惠配置；未登录或用户身份调用 | 权限 | 他店 `404`、未登录 `401`、用户身份 `403`；配置不串店 | 后端集成 |
| TC-PRV-011 | 保存优惠配置后用户端确认订单计价同步生效 | 正常 | 配置改动前后的两笔订单计价分别与配置一致（配置证据 + 订单快照证据） | 双端 |

### 5.10 评价与商家回复（TC-REV，本期实现·待开发）

依据：PRD 7.7、7.14、7.17；契约 §6（`GET /stores/{storeId}/reviews`、`POST /orders/{orderId}/review`、`GET /merchant/reviews`、`PATCH /merchant/reviews/{reviewId}/reply`）。口径：只有已完成且属于当前用户的订单可评价，一单一评；评分 1~5；评价图片可选，走图片上传能力；商家只能回复本店评价，重复回复更新原记录。

| 编号 | 场景 | 类型 | 预期结果 | 层 |
| --- | --- | --- | --- | --- |
| TC-REV-001 | 已完成订单提交星级、标签与文字评价 | 正常 | 成功；订单展示为已评价；评价出现在店铺评价列表 | 后端集成 |
| TC-REV-002 | 评价携带上传成功的图片（可选字段） | 正常 | 提交成功并回显图片；不传图片同样可提交 | 双端 |
| TC-REV-003 | 用户端店铺评价列表展示商家回复 | 正常 | 按时间倒序；回复内容与时间可见 | 后端集成 |
| TC-REV-004 | 同一订单重复评价 | 异常 | 拒绝（`409`，口径 SRS 固化），不新增第二条评价 | 后端单测+集成 |
| TC-REV-005 | 未完成/未支付订单评价、评价他人订单 | 异常 | 状态不符 `409`、归属不符 `404`；不落库 | 后端集成 |
| TC-REV-006 | 评分 0 或 6 或非整数、正文裁剪后为空、正文超长、非法标签或图片 | 异常 | `400`；正文首尾空白在服务端统一裁剪；不落库 | 后端单测 |
| TC-REV-007 | 商家回复本店评价，随后再次回复 | 正常 | 首次回复成功；重复回复更新原回复，不新增记录；用户端可见最新回复 | 后端单测+集成 |
| TC-REV-008 | 商家 A 查询/回复商家 B 的评价；用户身份调用商家评价接口；未登录 | 权限 | `404`、`403`、`401`；不返回他店评价内容 | 后端集成 |
| TC-REV-009 | 商家端评价筛选（全部/未回复/已回复）与评分分布 | 正常 | 筛选结果与评分分布与数据库一致；无评价时返回空数组并展示空态 | 后端集成+前端（商家端） |

### 5.11 用户与商家消息（TC-MSG，本期实现·待开发）

依据：PRD 7.8、7.17；契约 §6（`GET /conversations`、`GET /conversations/{conversationId}`、`POST /conversations/{conversationId}/messages`、`PATCH /conversations/{conversationId}/read`）。口径：会话以订单维度组织并关联用户与商家；空消息拒绝；用户端 `unread` 与商家端 `merchantUnread` 分别记录；平台客服不提供。

| 编号 | 场景 | 类型 | 预期结果 | 层 |
| --- | --- | --- | --- | --- |
| TC-MSG-001 | 用户从订单详情进入会话并发送消息 | 正常 | 发送成功；消息时间线置底刷新；商家端可见同一会话 | 双端 |
| TC-MSG-002 | 标记已读只影响当前角色 | 正常 | 当前角色未读清零，另一端未读数不变；反向操作同理 | 后端集成 |
| TC-MSG-003 | 订单状态更新、红包到账、会员权益提醒通知列表 | 正常 | 三类通知按时间倒序；无通知时空态 | 后端集成 |
| TC-MSG-004 | 发送空消息或仅空白消息、超长消息 | 异常 | `400`；不落库；页面保留输入可重试 | 后端单测+集成 |
| TC-MSG-005 | 会话不存在、会话不属于当前角色 | 异常 | `404`；不返回消息内容 | 后端集成 |
| TC-MSG-006 | 用户访问他人会话、商家访问非本店订单会话 | 权限 | `404`；未登录 `401`；不泄露消息与会话归属 | 后端集成 |
| TC-MSG-007 | 发送失败后重试、无会话时展示 | 边界 | 失败保留输入可重试；无会话返回空数组并展示空态 | 前端 |

### 5.12 搜索、排序与分页（TC-SRC，本期实现·待开发）

依据：PRD 6.3、7.17；契约 §3.2 店铺/商品查询参数。口径：后端支持商家名、商品名与分类条件查询；排序取值综合/销量/距离，**综合排序 = 销量优先、评分次之（不做加权公式）**，距离取种子数据固定 `distanceKm`（不引入地图与定位）；分页参数 `page`/`size`，默认 `page=1&size=10`；不在页面层自行过滤真实数据。

| 编号 | 场景 | 类型 | 预期结果 | 层 |
| --- | --- | --- | --- | --- |
| TC-SRC-001 | 按商家名关键词查询 | 正常 | 结果包含匹配商家，参数由后端处理 | 后端集成 |
| TC-SRC-002 | 按商品名关键词查询 | 正常 | 返回匹配商品及其所属商家 | 后端集成 |
| TC-SRC-003 | 分类条件查询（分类宫格入口） | 正常 | 只返回该分类下商家 | 后端集成 |
| TC-SRC-004 | 排序取值综合/销量/距离 | 正常 | 综合排序按销量优先、评分次之；距离取种子固定 `distanceKm`（不引入地图与定位）；结果顺序与参数一致 | 后端集成 |
| TC-SRC-005 | 分页参数 `page`/`size`（默认 `page=1&size=10`），连续翻页 | 正常 | 两页数据不重复、总数与总页数正确 | 后端集成 |
| TC-SRC-006 | 关键词无命中 | 边界 | 返回 `data: []` 并展示空态，不视为错误 | 后端集成+前端 |
| TC-SRC-007 | 页码越界、`size` 为 0/负数/超上限 | 边界 | 按契约返回空页或 `400`；不得返回全量数据 | 后端单测 |
| TC-SRC-008 | 非法 `sort` 取值 | 异常 | `400` | 后端单测 |
| TC-SRC-009 | 关键词含特殊字符或超长 | 异常 | 安全处理、不拼接 SQL、不返回 `500` | 后端集成 |

### 5.13 商家收藏（TC-FAV，本期实现·待开发）

依据：PRD 7.10。口径：收藏为“当前用户 + 商家”唯一；重复收藏幂等；收藏列表只返回当前用户数据。

| 编号 | 场景 | 类型 | 预期结果 | 层 |
| --- | --- | --- | --- | --- |
| TC-FAV-001 | 收藏商家 | 正常 | 成功；icon 状态切换；收藏页可见该商家 | 双端 |
| TC-FAV-002 | 取消收藏 | 正常 | 成功；列表移除；重复取消幂等 | 后端单测 |
| TC-FAV-003 | 重复收藏同一商家 | 正常 | 幂等，不产生重复记录 | 后端单测+集成 |
| TC-FAV-004 | 收藏不存在或已关闭店铺 | 异常 | 按契约拒绝（`404`/`400`），不落库 | 后端集成 |
| TC-FAV-005 | 未登录收藏、查询他人收藏 | 权限 | `401`；只返回当前用户收藏，不泄露他人数据 | 后端集成 |
| TC-FAV-006 | 无收藏时的列表展示 | 边界 | 返回 `[]` 并展示空态 | 后端集成+前端 |

### 5.14 会员权益（TC-MBR，本期实现·待开发）

依据：PRD 7.10、7.4。口径：会员标识由种子数据/后台标记，**本期不做开通与续费**；会员折扣 95 折按 PRD 7.4 顺序作用于商品小计；**会员价与会员折扣不叠加**——商品配置了 `memberPrice` 的按会员价计价、不再打折，未配置的才应用会员折扣（2026-09-09 定稿）。

| 编号 | 场景 | 类型 | 预期结果 | 层 |
| --- | --- | --- | --- | --- |
| TC-MBR-001 | 会员标识 `memberOpened` 展示与会员价高亮 | 正常 | 标识与会员价按种子数据展示 | 双端 |
| TC-MBR-002 | 会员下单计价 | 正常 | 95 折作用于商品小计，金额由后端计算并写入订单快照 | 后端单测+集成 |
| TC-MBR-003 | 非会员下单计价 | 正常 | 不享受会员折扣；与会员订单金额差异可复算 | 后端单测 |
| TC-MBR-004 | 会员折扣与满减、红包叠加 | 边界 | 按定稿公式计算，不重复打折、不重复减免 | 后端单测 |
| TC-MBR-005 | 开通/续费能力 | 异常 | 本期**不提供开通与续费能力**：不出现真实开通/续费流程、不调用任何开通或续费接口、不产生订单或状态变化；**演示反馈入口不计为偏差**（PRD 7.16.1 会员权益页权益内容区行要求「续费按钮只给出『权益已生效』的演示反馈并明确提示为演示操作」；评审口径「实现为原型说明的超集」见 `项目规则.md` §5；2026-09-12 与 PRD 口径对齐，原表述「页面不出现对应入口」作废） | 前端 |
| TC-MBR-006 | 会员状态查询归属 | 权限 | 只返回当前用户会员状态；伪造 `userId` 无效 | 后端集成 |
| TC-MBR-007 | 会员价与会员折扣叠加场景 | 边界 | 配置了 `memberPrice` 的商品按会员价计价且不再打折；未配置的才应用会员折扣；同一商品不重复减免 | 后端单测+集成 |

### 5.15 红包（TC-CPN，本期实现·待开发）

依据：PRD 7.4、7.10。口径：红包门槛与适用范围由种子数据定义，一单一红包；`available`/`expired` 两态；过期红包灰化不可选。

| 编号 | 场景 | 类型 | 预期结果 | 层 |
| --- | --- | --- | --- | --- |
| TC-CPN-001 | 红包列表展示金额、门槛、适用范围、有效期 | 正常 | 状态取值正确，过期红包灰化 | 双端 |
| TC-CPN-002 | 确认订单选择一个满足门槛且在适用范围的红包 | 正常 | 计价扣减正确；订单快照含红包信息；一单一红包 | 后端集成 |
| TC-CPN-003 | 不满足门槛或超出适用范围的红包 | 异常 | 页面不可选；直连接口提交被拒绝，金额不变 | 后端单测+集成 |
| TC-CPN-004 | 一单提交两个红包 | 异常 | `400`；只生效一个或不生效，不重复抵扣 | 后端单测 |
| TC-CPN-005 | 使用已过期红包 | 异常 | 拒绝；金额与订单不变化 | 后端单测+集成 |
| TC-CPN-006 | 使用他人红包、查询他人红包列表 | 权限 | `404`；未登录 `401`；不泄露红包信息 | 后端集成 |
| TC-CPN-007 | 红包金额大于应付金额 | 边界 | 最终金额不小于 0（与 TC-PRV-007 同口径，不重复计价） | 后端单测 |

### 5.16 取消订单与库存回补（TC-CAN，本期实现·待开发）

依据：PRD 7.6（取消与配送规则）、7.16。口径：仅 `PENDING_PAYMENT` 与 `PENDING` 可取消，取消原因必填，订单进入 `CANCELLED` 并保留订单与明细快照；取消在同一事务内回补库存；不接真实退款；`COOKING` 及之后不可取消；待支付超时不自动回补（课程简化口径），两种规则不得混用。

| 编号 | 场景 | 类型 | 预期结果 | 层 |
| --- | --- | --- | --- | --- |
| TC-CAN-001 | `PENDING_PAYMENT` 订单携带取消原因取消 | 正常 | 状态 `CANCELLED`；原因落库；订单与明细快照保留 | 后端集成 |
| TC-CAN-002 | `PENDING`（已支付未接单）订单取消 | 正常 | 状态 `CANCELLED`；库存回补成功 | 后端集成 |
| TC-CAN-003 | 取消后逐条核对库存回补数量 | 正常 | 回补数量与创建订单时扣减数量一致（按商品与规格核对数据库） | 后端集成 |
| TC-CAN-004 | `COOKING`/`DELIVERING`/`COMPLETED` 订单取消 | 异常 | `409` 拒绝；订单状态与库存不变 | 后端单测+集成 |
| TC-CAN-005 | 取消原因为空或仅空白、原因超长 | 异常 | `400`；订单状态不变 | 后端单测 |
| TC-CAN-006 | 重复取消同一订单 | 异常 | 幂等返回当前订单，不二次回补库存 | 后端单测+集成 |
| TC-CAN-007 | 取消后的退款处理 | 异常 | 不接真实退款：不产生资金流水，订单与支付记录按契约更新 | 后端集成 |
| TC-CAN-008 | 取消他人订单、取消不存在订单 | 权限 | `404`；不泄露订单存在性 | 后端集成 |
| TC-CAN-009 | 用户端取消二次确认；商家端展示已取消 | 正常 | 页面二次确认后才发请求；商家端列表与详情展示“已取消”与取消原因 | 双端 |
| TC-CAN-010 | 待支付超时未支付订单 | 边界 | 不自动回补库存（课程简化口径），与取消回补规则不混用 | 后端单测 |

### 5.17 简化配送管理（TC-DLV，**已砍掉，CHG-002**，2026-09-11）

**结论：整组用例已废止。**「简化配送管理」整块已砍（CHG-002）——不建 `deliveries` 表、不提供契约 §10.2 三个接口、不设用户端配送页；配送进度由订单状态（`DELIVERING`＝配送中）与订单详情时间线、配送信息卡表达；商家端订单状态推进已实现。原 TC-DLV-001~006 用例组不再执行、不作为交付内容。

### 5.18 商品规格扩展（TC-SPC，本期实现·待开发）

依据：PRD 7.12、7.4；契约 `PUT /merchant/products/{productId}/specifications`。口径：购物车与订单按“商品 + 规格组合”区分（`cartLineId`）；单价含规格价差；无规格商品沿用基础计价。

| 编号 | 场景 | 类型 | 预期结果 | 层 |
| --- | --- | --- | --- | --- |
| TC-SPC-001 | 商家保存规格选项与价差 | 正常 | 保存成功并回显一致 | 后端集成 |
| TC-SPC-002 | 用户选择规格加购 | 正常 | 购物车按“商品 + 规格组合”分行，不同规格不合并 | 双端 |
| TC-SPC-003 | 规格价差计入订单金额 | 正常 | 单价含价差；订单金额与快照可复算 | 后端单测+集成 |
| TC-SPC-004 | 价差非数字或超出契约范围、规格选项为空 | 异常 | `400`；不落库 | 后端单测 |
| TC-SPC-005 | 不同规格被错误合并为同一行 | 异常 | 视为失败：数量与金额应按规格分行统计 | 后端单测 |
| TC-SPC-006 | 商家 A 修改商家 B 商品规格；未登录 | 权限 | `404`；`401` | 后端集成 |
| TC-SPC-007 | 无规格商品、下架或售罄规格 | 边界 | 无规格沿用基础计价；不可售规格不可选 | 双端 |

### 5.19 商家数据统计与概览（TC-STA，本期实现·待开发）

依据：PRD 7.14、7.17；契约 `GET /merchant/overview`、`GET /merchant/analytics?range={range}`。口径：只统计数据库中的有效订单，数值必须注明查询范围；不使用固定演示数值作为完成证据。

| 编号 | 场景 | 类型 | 预期结果 | 层 |
| --- | --- | --- | --- | --- |
| TC-STA-001 | 概览返回营业额、有效订单、预计收入、客单价（2026-09-10 删去「访客」：无数据源，见 PRD §8） | 正常 | 数值与数据库有效订单一致，并注明查询范围 | 后端集成 |
| TC-STA-002 | `range` 取合法值查询趋势 | 正常 | 趋势数据与查询范围一致 | 后端集成 |
| TC-STA-003 | 非法 `range` 取值 | 异常 | `400` | 后端单测 |
| TC-STA-004 | 无订单时的统计结果 | 边界 | 返回 0 或空数组，不是错误，也不得返回 Mock 演示数值 | 后端集成 |
| TC-STA-005 | 统计范围与归属 | 权限 | 只统计当前店铺；他店数据不可见；未登录 `401`、用户身份 `403` | 后端集成 |
| TC-STA-006 | 已取消订单是否计入 | 边界 | 只计入有效订单；已取消订单不计入营业额与有效订单数 | 后端集成 |

### 5.20 图片上传（TC-IMG，本期实现·待开发）

依据：PRD 7.12（商品图片）、7.7（评价图片可选）；上传接口路径以接口契约为准（当前待补）。口径（2026-09-09 定稿）：商品图 1 张、评价图最多 3 张，单张 ≤2MB，类型限 jpg/jpeg/png/webp；存后端本地目录 `backend/uploads/`（不入 Git）并经 `/uploads/**` 静态映射访问，不接对象存储；商家只能传本店商品图、用户只能传自己评价的图，越权 `403`；商品表单出现图片字段；用户端评价图片可选并走上传；上传失败可重试，不静默切回 Mock。

| 编号 | 场景 | 类型 | 预期结果 | 层 |
| --- | --- | --- | --- | --- |
| TC-IMG-001 | 上传合法图片并用于商品表单 | 正常 | 返回可访问地址；表单预览成功；保存后商品详情回显图片 | 后端集成+前端（商家端） |
| TC-IMG-002 | 评价图片可选上传后随评价提交 | 正常 | 上传成功并随评价提交回显；不上传图片同样可提交评价 | 双端 |
| TC-IMG-003 | 非 jpg/jpeg/png/webp 类型、单张超过 2MB、0 字节文件 | 异常 | `400`；不落盘 | 后端单测+集成 |
| TC-IMG-004 | 上传失败（后端 `500`、网络中断） | 异常 | 页面显示可理解错误并支持重试；不静默切回 Mock；不产生半成功数据 | 双端 |
| TC-IMG-005 | 未登录上传、商家上传他人店铺商品图、用户上传他人评价图、上传可执行脚本类型 | 权限 | `401`；越权 `403`；脚本类型拒绝 | 后端集成 |
| TC-IMG-006 | 删除或替换图片 | 边界 | 商品表单与评价展示同步更新，旧图不再被引用 | 前端（商家端） |
| TC-IMG-007 | 张数上限：商品图第 2 张、评价图第 4 张 | 边界 | 拒绝并提示上限（商品图 1 张、评价图最多 3 张）；已上传图片不受影响 | 后端单测+集成 |

### 5.21 爆红包（TC-RBP，CHG-001 2026-09-10 新增·第二次修订·本期实现·待开发）

依据：契约 §3.10、PRD 7.10 与 7.4；档位池与权重见契约 §10.5。

| 编号 | 场景 | 类型 | 预期 | 层面 |
| --- | --- | --- | --- | --- |
| TC-RBP-001 | 购买 ¥4.9 套餐 | 正常 | 生成 **4 张**（3×满30减5 + 1×无门槛减5，面额 ¥20）与 `packId`；**不产生支付记录**；有效期 **7 天** | 后端集成 |
| TC-RBP-002 | 购买 ¥9.9 套餐 | 正常 | 生成 **8 张**（6×满30减5 + 1×满40减10 + 1×无门槛减5），面额合计 **¥45** | 后端集成 |
| TC-RBP-003 | 每日首次免费爆（不传 `couponId`） | 正常 | 走**免费次数**：新增一张爆出的券，**不消耗已购买红包**；当日免费次数置为已用 | 后端集成 |
| TC-RBP-004 | 同日再次免费爆 | 异常 | 免费次数已用完 → `409`，前端引导「消耗红包爆」或「去购买」 | 后端单测+集成 |
| TC-RBP-005 | 消耗券爆（传 `couponId`） | 正常 | **替换式**：该券 `threshold` 与 `amount` 同时更新为新档位，`can_blast=0`，不新增行 | 后端单测+集成 |
| TC-RBP-006 | 爆出来的券再爆 | 异常 | `409`（`can_blast=0` 为终态） | 后端单测 |
| TC-RBP-007 | 档位命中与权重 | 正常 | 固定随机种子下命中确定档位，落在 **10 档**内且权重生效 | 后端单测 |
| TC-RBP-008 | 有效期规则 | 正常 | **爆出来的券 `validTo` = 当天 23:59:59**；**购买所得券 `validTo` = 购买时刻 + 7 天** | 后端单测 |
| TC-RBP-009 | 无免费次数且无可爆券 | 异常 | `409` + 明确提示「去购买」；不产生脏数据 | 后端集成 |
| TC-RBP-010 | 用爆出来的券下单 | 正常 | 计价第 ⑥ 步扣减，快照 `couponAmount` 正确，券置 `used`，**一单一红包** | 后端集成 |
| TC-RBP-011 | 未登录 / 越权 | 权限 | `401` / `404`，不泄露信息 | 后端集成 |
| TC-RBP-012 | 占位券不可用 | 边界 | 品类券与「限非外卖配送」券**不入库**，前端纯展示、点击提示不可用；`scope` 仅 `ALL`/`STORE` | 前端 |
| TC-RBP-013 | 爆红包全屏动效与音效降级 | 正常/异常 | 动效可跳过；音频不可用时功能与验收不受影响 | 前端 |

注：本组用例为 CHG-001 新增，按 `TDD实施规划.md` §9 先补失败测试（`test:` 仅含测试代码，在该提交上复跑必须红）再实现；用例清单由模块负责人与测试联调同学人工确认后才允许 AI 落地。

---

## 6. 重点异常场景清单（跨模块）

以下场景横跨多个模块，单靠某一模块的用例覆盖不了，由测试联调同学在集成/回归阶段统一组织验证：

| 编号 | 场景 | 涉及模块 | 预期行为 | 依据 |
| --- | --- | --- | --- | --- |
| XA-01 | 并发修改冲突：商品/店铺状态/订单被另一端修改后再提交 | 商品、店铺、订单（P1 推进） | 后端返回 `409`，message 为"数据已变化，请刷新后重试"；页面重新读取相关数据 | 契约 §7；PRD 原型统一规则第 7 条 |
| XA-02 | 库存为 0 / 库存不足 | 商品、购物车、订单 | 售罄灰化禁止加购；下单返回具体商品与可用数量，不创建半成品订单 | PRD 7.4；契约错误响应示例 |
| XA-03 | 非法状态流转：跳级（待接单→已完成）、重复推进、重复支付、超时支付 | 订单（P1 扩展） | 一律 `409` 拒绝或幂等返回当前结果；不能二次扣库存或跨越状态 | PRD 7.6；契约 §5 |
| XA-04 | 跨角色越权：用户身份调商家接口、商家访问他人订单/商品、请求体伪造 `storeId`/`userId` | 全部受保护模块 | 角色不符 `403`；归属不符 `404`；身份只从 Session 读取 | 契约 §1.2、§4、§7 |
| XA-05 | 重复提交：同一按钮连点 | 加购、下单、支付、状态推进 | 前端禁用只发一次请求；后端仍以唯一约束/状态条件/幂等键防重复写入 | 契约 §7 |
| XA-06 | Mock 关闭后的回退行为 | 用户端、商家端 | `VITE_MOCK_FALLBACK=false` 后接口失败**不得**静默切回演示数据；空数组是空态不是错误；HTTP 200 但返回 HTML 错误页不算联调通过 | 契约 §8；PRD 数据与接口策略 |
| XA-07 | 未登录 / 登录失效 | 全部受保护模块 | `401` 统一处理：保留当前页面地址跳登录，登录成功后回原流程 | PRD 7.15 |
| XA-08 | 服务端未预期错误（500） | 后端全局 | 必须返回 JSON，不返回服务器默认 HTML 错误页；不泄露密码、数据库细节或堆栈 | 契约 §1.3 |
| XA-09 | 金额与库存以提交时后端结果为准 | 购物车、订单 | 不信任前端金额与库存；商品改价、下架、店铺关闭后旧购物车结算时必须重新检查 | 契约 §7；PRD 7.16 |

### 6.1 高频兜底校验清单（异常用例取材优先级）

以下条目是订单类业务系统公认的高频兜底校验模式，设计异常用例时优先从这里取材，逐条映射到第 5 章矩阵或新增编号；其共同背景是：**页面校验只做用户体验层，业务规则必须由服务端在每个调用入口统一兜底**（课程要求原文见第 1 章）。

| 编号 | 边界类别 | 最小用例集 |
| --- | --- | --- |
| XA-10 | 入口一致性 | 每个写接口至少一条"绕过页面直接调用"的负向用例：必填缺失、归属不符、状态非法在服务层全部拒绝；页面禁用、trim、`min/max` 不能作为唯一防线 |
| XA-11 | 数值边界 | 数量为正整数；金额/价格/库存/门槛非负；优惠合计不超过应付基数且最终金额不小于 0；折扣率限定约定区间（如 0~1）；评分限定 1~5 |
| XA-12 | 内容边界 | 评价正文、商家回复、聊天消息在服务端统一裁剪首尾空白，裁剪后为空即拒绝；标签、图片等列表载荷逐项清洗，不接受非法元素 |
| XA-13 | 状态联动 | 评价提交成功与订单"已评价"是同一闭环（列表、详情、商家端统计一致，重复评价拒绝）；未支付订单对商家端不可见、不可接单、不可发起会话（P1 扩展期生效） |
| XA-14 | 无效标识 | 访问不存在的订单/评价/分类/商品等标识返回明确 `404` 或空态；前端不得沿用上一次查询的详情数据（后端 `404` + 前端空态各一条用例） |

### 6.2 本期扩展异常场景（2026-09-09 决议纳入本期，待开发）

以下场景随本期扩展范围一并纳入，编号接续 XA-14；对应接口与实现尚未收口，执行证据待补。

| 编号 | 场景 | 涉及模块 | 预期行为 | 依据 |
| --- | --- | --- | --- | --- |
| XA-15 | 取消与回补库存：非 `PENDING_PAYMENT`/`PENDING` 状态取消、重复取消、取消原因为空 | 取消订单、订单、库存 | 状态不符 `409`；原因为空 `400`；重复取消幂等且不二次回补；`CANCELLED` 保留订单与明细快照；回补在同一事务内完成 | PRD 7.6、7.16 |
| XA-16 | ~~配送状态非法流转：跳级、逆向回退、重复推进、已取消订单推进配送~~（**已砍，随 CHG-002 移除**） | 配送、订单（不适用） | 配送单与配送状态机不实现；订单侧非法流转由 XA-03 覆盖 | CHG-002（原 PRD 7.6） |
| XA-17 | 图片上传失败：非图片类型、超大或空文件、后端 `500`、网络中断 | 图片上传、商品表单、评价 | `400` 拒绝且不落盘；页面显示可理解错误并支持重试，不静默切回 Mock；商品与评价不产生半成功数据 | PRD 7.12、7.7 |
| XA-18 | 优惠金额边界：优惠为负数、折扣率越界、优惠过期、优惠合计超过应付基数 | 优惠计价、红包、会员 | 非法配置 `400` 或限制到合法范围；最终金额不小于 0；金额以后端结果为准 | PRD 7.4、7.17 |
| XA-19 | 搜索无结果与分页越界：关键词无命中、页码超出总页数、`size` 非法 | 搜索、分页 | 无结果返回 `data: []` 并展示空态（不是错误）；越界返回空页或按契约 `400`，不返回全量数据 | PRD 7.17 |
| XA-20 | 消息空消息与未读：空或仅空白消息、超长消息、重复标记已读、双端未读串号 | 消息、通知 | 空消息 `400` 不落库；已读只影响当前角色；用户端 `unread` 与商家端 `merchantUnread` 分离 | PRD 7.8、7.17 |

---

## 7. AI 与测试的边界

课程要求的规则与老师引语：

> 不允许的做法：……4. 让 AI 同时完整生成业务代码和全部测试代码。
>
> > AI 不能既当运动员，又当裁判。
>
> 如果 AI 自己生成注册业务代码，然后再让同一个 AI 针对自己的实现生成测试，它很可能写出一套天然能够通过自己代码的测试，因此人必须参与测试标准和异常情况的设计。

本项目执行细则：

1. **禁止**"AI 生成业务代码 + 同一 AI 生成全部测试"，也禁止测试代码与业务代码由同一代理一次性生成后一起提交。物理上必须拆成 `test:`（红）与 `feat:`（绿）两次提交，且用例清单（正常/异常场景与断言口径）先于代码设计确定；`test:` 提交中不得混入业务实现文件，拆分提交按 §2.2 约束执行并在 push 前复跑自检。
2. **人负责测试标准**：第 5 章矩阵和变更单里的"新增测试清单"由模块负责人与测试联调同学人工确定后，才允许借助 AI 落地测试代码。异常场景（错误码、边界值）的选取必须有人的判断，不能由 AI 从实现反推。
3. **AI 可辅助的范围**：测试类骨架、测试数据工厂/fixture、Mock 样板、断言模板等脚手架性内容；生成的测试必须逐条可解释，经人审核修改后才能合入。
4. **记录**：AI 使用点按 `docs/project/项目规则.md` 第 3 节落 raw/ 片段留痕（类别取"测试用例生成"），随次晨汇总进入每日记录 B 表；AI 深度参与的提交在提交信息末尾追加 `AI协作: <一句话说明>`。
5. **红线**：无法解释逻辑的 AI 产出不得并入交付；答辩被抽查到测试用例时，作者本人必须能说明该用例为什么这样设计。

---

## 8. 里程碑挂钩（9/4 → 材料归档）

日期对齐 `docs/project/分工与里程碑安排.md` 声明的课程硬节点（**9/8（更可能）或 9/9 第一阶段中期验收、P0 基础功能 9/7 晚完成并于当晚验收演练**、功能冻结 9/15、材料归档在第 4 周），日常动作对应 `docs/PRD.md` 项目排期逐日表。两文档个别日期存在差异时，以分工文档的优先级声明和老师群实际通知为准，**只调整日期不改门槛**；阶段 2 需求变更于 9/8 验收后立即接收，走第 9 章同一套流程。2026-09-09 第二阶段范围决议已落定（除平台管理员外全部 P1/P2 纳入本期），扩展开发按 2026-09-09 决议的 10 个批次顺序分批红-绿推进（批次表见本节末）；老师下发的阶段 2 变更内容尚未发布（2026-09-10 起按「待定」跟踪：负责人判断大概率不发布，不作为开发前置阻塞），到达后仍走第 9 章流程。

| 日期 | TDD 动作 | 产出证据 |
| --- | --- | --- |
| 9/4 | 提交 SRS；按接口契约为登录、商品查询、购物车、订单核心场景写**第一批失败测试**并提交；建立开发分支；三方并行开工（当天有功能代码） | 仓库首个 `test:`（红）提交；失败运行输出以 raw/ 片段留痕，随次晨汇总进每日记录 |
| 9/5 | 周末冲刺日一：账号模块红-绿循环——注册六条课程异常用例（TC-ACC-002~007）+ 登录/会话/越权（TC-MER-011）；商家注册、商品与分类管理 TDD（TC-MER、TC-CAT、TC-PRD） | TC-ACC 主干全绿、TC-MER/TC-CAT/TC-PRD 全绿；`test:`→`feat:` 提交对 |
| 9/6 | 周末冲刺日二：购物车模块 TDD（合并、数量边界、超库存、隔离，TC-CRT）+ 店铺浏览链路 TDD（店铺状态与查询，TC-STO） | TC-CRT、TC-STO 全绿；联调记录一条 |
| 9/7 | 地址 + 订单创建/查询 + 商家端订单页 TDD（TC-ADR、TC-ORD-001~016）：快照、事务回滚、归属、金额；关闭 Mock 回退（`VITE_MOCK_FALLBACK=false`）做 P0 全链路联调，XA-06 场景现场验证；**晚 P0 用例矩阵核心路径全绿**；当晚验收演练并执行商家端回退检查点 | TC-ADR 全绿、TC-ORD 创建与查询侧全绿、P0 核心路径全绿；P0 联调记录；演练记录 |
| 9/8 | **第一阶段中期验收**：现场演示“测试先失败再通过”的红绿序列与 TDD 证据；验收后立即接收第二阶段需求变更，当天出变更单与影响分析，先补失败测试锁定新行为 | `git log` 红绿顺序可查；变更单 + 新增 `test:`（红）提交 |
| 9/9–9/14 | **按 2026-09-09 决议的 10 个批次顺序推进（批次⑩为 CHG-003 追加）**（9/10–9/14：① 优惠与统一计价 → ② 取消 → ③ 评价与回复 → ④ 消息 → ⑤ 搜索 → ⑥ 收藏·会员·红包 → ⑦ 规格 → ⑧ 图片上传 → ⑨ 统计 → ⑩ 订单详情与支付页（CHG-003），批次表见本节末）：每批先补失败测试锁定新行为再最小实现，含模拟支付与库存一致性、商家订单推进与跨端同步（TC-ORD-017~020）；异常、并发、重复提交（XA-01~05、XA-15~20）完善 | 变更单 + 新增测试（红）+ 回归记录；每批红-绿证据 + 异常用例全绿 + 接口/数据库/页面证据齐；未收口模块标注“本期实现（待开发）”、证据待补 |
| 9/14 | 总回归：全量既有测试与变更新增用例重跑；前后端与后端构建 | 全量测试绿；构建与测试日志 |
| 9/15 | 冻结前全量回归；**功能冻结** | 全量测试绿；冻结 tag |
| 9/15–9/16 | 9/15 整理并备妥交叉验收包，9/16 组内组间双盲交叉验收执行：按对方组 SRS 补验收测试记录 | 交叉验收测试记录 |
| 9/17–9/18 | 老师最终验收；三端构建部署与部署验证、手机访问验证 | 最终验收记录；部署与访问验证日志 |
| 归档周（第 4 周） | 材料归档：汇总测试报告、红绿提交序列、AI 使用点（每日记录 B 表）与演示材料 | 归档材料与冻结 tag 一致 |

**9/10–9/14 批次表**（2026-09-09 决议顺序，不得跳批；每批完成门槛 = 红-绿证据 + 异常用例全绿 + 接口/数据库/页面证据齐）

| 批次 | 模块 | 用例组 | 完成门槛 | 负责人建议 |
| --- | --- | --- | --- | --- |
| ① | 优惠与统一计价 | TC-PRV | 红-绿证据 + 异常用例全绿 + 接口/数据库/页面证据齐 | 后端 B 主责（计价与优惠配置），前端主力（确认订单金额明细）、商家端（优惠配置页） |
| ② | 取消 | TC-CAN（**TC-DLV 已砍，CHG-002**） | 红-绿证据 + 异常用例全绿 + 接口/数据库/页面证据齐 | 后端 B 主责，前端主力（取消确认页） |
| ③ | 评价与回复 | TC-REV | 红-绿证据 + 异常用例全绿 + 接口/数据库/页面证据齐 | 后端 B 主责，前端主力（评价提交）、商家端（评价管理 + 回复抽屉，与回复合并一页） |
| ④ | 消息 | TC-MSG | 红-绿证据 + 异常用例全绿 + 接口/数据库/页面证据齐 | 后端 A 主责，前端主力（用户端会话）、商家端（消息列表/聊天详情） |
| ⑤ | 搜索 | TC-SRC | 红-绿证据 + 异常用例全绿 + 接口/数据库/页面证据齐 | 后端 A 主责，前端主力（搜索结果与分页） |
| ⑥ | 收藏·会员·红包 | TC-FAV、TC-MBR、TC-CPN；**CHG-001 追加 TC-RBP（§5.21）** | 红-绿证据 + 异常用例全绿 + 接口/数据库/页面证据齐；**爆红包须与 `coupons` 建表一次设计到位** | 后端 B 主责，前端主力（收藏页、会员权益、红包选择、爆红包全屏过渡态） |
| ⑦ | 规格 | TC-SPC | 红-绿证据 + 异常用例全绿 + 接口/数据库/页面证据齐 | 后端 B 主责，商家端（规格维护）、前端主力（规格选择与加购） |
| ⑧ | 图片上传 | TC-IMG | 红-绿证据 + 异常用例全绿 + 接口/数据库/页面证据齐 | 后端 A 主责（上传接口 + `/uploads/**` 静态映射），商家端（商品图）、前端主力（评价图） |
| ⑨ | 统计 | TC-STA | 红-绿证据 + 异常用例全绿 + 接口/数据库/页面证据齐 | 后端 A 主责，商家端（运营概览/数据统计） |
| ⑩ | 订单详情与支付页（CHG-003） | TC-ORD（订单详情含跟踪、支付页与取消入口） | 红-绿证据 + 异常用例全绿 + 接口/数据库/页面证据齐 | 前端主力主责（exact 复刻 + 取消入口），后端 B（订单详情与取消接口） |

说明：批次顺序与用例组依据 2026-09-09 决议（扩展分批顺序：优惠与计价→取消→评价与回复→消息→搜索→收藏/会员/红包→规格→图片上传→统计→订单详情与支付页（CHG-003））；“负责人建议”列为拟承接人建议，模块认领以 `docs/project/分工与里程碑安排.md` 与开工评审结果为准。每批收口需同时具备红-绿证据（红 X failed / 绿 Y passed）、异常用例全绿与接口/数据库/页面三类实现证据；未收口的模块在本矩阵与验收材料中如实标注“本期实现（待开发）”，不得写成已完成。

---

## 9. 需求变更时的测试流程

阶段 2 需求变更（**9/8 第一阶段中期验收后立即接收**；若验收顺延至 9/9，则在验收当天接收）同样执行"先补失败测试锁定新行为，再最小实现并回归"的红-绿流程；变更登记、影响分析与归档由组长组织。变更各步骤与 TDD 的对应关系：

| 变更流程步骤 | TDD 对应 |
| --- | --- |
| 先写新增测试（红） | 红：失败测试**锁定新行为**，提交早于实现 |
| 最小实现（绿） | 绿：只改影响分析列出的范围，新测试全过 |
| 回归测试 | 全部既有测试重跑通过；失败项修复后重跑并记录 |

补充约束：

- 原有用例不得因变更被删除或放宽断言；行为确实变化的用例在变更单中说明后修改，而不是静默改测试。
- 变更新增的用例同步登记进第 5 章矩阵（编号沿用模块前缀，在变更单中标注对应 CHG 编号），保证矩阵始终是全组用例的唯一索引。
- 禁止趁机重构与本次变更无关的代码；想改的记入"冻结后重构清单"。

---

## 10. 本规划的局限与待定项

以下事项如实标注，待小组确认后回填本文，不作为已定事实（库存扣减归属已于 2026-09-09 随范围决议结案：统一在创建订单事务内扣减、支付不重复扣减、用户取消在事务内回补，见 PRD 7.16，不再列为待定项；第 1 条数据库与测试环境、第 2 条后端框架版本已于 2026-09-10 结案，见下文）：

1. **数据库测试环境选型（2026-09-10 结案）**：数据库定为 **MySQL 8**；测试统一走服务器独立测试库 `elm_practice_test`（规则 R8，见 `docs/project/运行说明.md` §4.5），**不使用 H2 兼容模式**；每用例前以 `reset.sql` 复位，禁止对业务库执行重置类测试。
2. **后端框架精确版本（2026-09-10 结案）**：**Spring Boot 3.5.5** + **MyBatis Spring Boot Starter 3.0.4**（Java 17，以 `backend/pom.xml` 为准）；集成测试统一用 `@SpringBootTest` + `@AutoConfigureMockMvc` + `MockMvc`（现有 7 个测试文件即此口径），不使用 `TestRestTemplate`。
3. **仓库组织口径**：本规划按"单仓库多目录（`frontend/user-h5/`、`frontend/merchant-admin/`、`backend/`）"编写；该口径已于 2026-09-01 依据课程要求"每组必须建立一个代码仓库"确认，并在运行说明等文档中固化；各端工程独立、独立进程，不影响本文的测试分层与用例矩阵。
4. **前端组件测试深度**：只做第 4.2/4.3 节列出的核心交互，不做全页面组件测试；深度是否加大视 9/8 中期验收结果决定。
5. **覆盖率不设硬指标**：不以百分比覆盖率为完成标准，以 PRD 目标 G3"TDD 过程完整率"（核心任务留有需求分析、测试先行、首次失败、实现通过和回归记录）和矩阵执行率为准。
6. **个别异常场景错误码未逐条固定**：店铺关闭加购、有商品分类删除、不满足起送条件等场景的 `400`/`409` 归属需在 SRS 固化后回填矩阵，当前标注"口径 SRS 固化"。
7. **矩阵为代表性清单**：不是全量穷举；缺陷回补用例、交叉验收反馈的问题都会追加编号，矩阵版本随文档更新。
8. **成员与矩阵的认领对应表**：成员姓名已于 2026-09-02 确定（成员E（组长）/成员B（用户端）/成员D（商家端/测试）/成员C（后端A）/成员A（后端B），见 `docs/project/分工与里程碑安排.md`）；模块编号（TC-ACC 等）认领到人于 9/3 开工评审确定，认领结果回填分工文档，本文不重复维护。
9. **变更到达日期**：已确认 9/8（或 9/9）进行第一阶段中期验收，**验收后立即接收第二阶段需求变更**；原“9/12–9/14 与 9/16 两种排期口径”废止。若老师现场调整验收时间，以老师群实际通知为准，“先补失败测试再改代码”的流程不变。本组第二阶段范围决议已于 2026-09-09 落定（除平台管理员外全部 P1/P2 纳入本期）；老师下发的阶段 2 变更内容尚未发布（2026-09-10 起按「待定」跟踪：负责人判断大概率不发布，不作为开发前置阻塞），到达后仍按同一流程执行。
10. **扩展模块用例的执行证据**：第 5.9~5.20 节与第 6.2 节为 2026-09-09 决议纳入本期的模块用例设计，接口、实现与执行证据均未收口，本规划统一标注“本期实现（待开发）”、证据字段写“待补”；未收口模块不得写成已完成。
