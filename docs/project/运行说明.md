# 运行说明

日期：2026-09-01；2026-09-02 新增第 4 节"服务器与部署"框架（具体方案由后端同学填写）；2026-09-07 后端同学填表 §4.2，MyBatis + 服务器 MySQL 已接入；2026-09-09 同步第二阶段范围决议（§3），并按负责人当日拍板补图片上传运行配置、优惠配置结构与订单金额快照的库变更/联调口径（§3、§4.3）

## 1. 当前状态：后端 MyBatis 持久化已接入

- Java 后端已在 `backend/` 初始化 Spring Boot 工程；持久化已从内存 Repository 迁移到 MyBatis + 云服务器 MySQL 8，启动、测试和固定演示账号见 `backend/README.md`。前端工程仍按各自目录 README 启动。
- 仓库结构（单仓库口径，依据课程要求"每组必须建立一个代码仓库"）：全组共用一个 Git 仓库，顶层规划 `frontend/user-h5/`（用户端移动 H5，Vue 3 + Vite + Pinia）、`frontend/merchant-admin/`（商家端桌面 Web 后台，React 18 + Vite + Ant Design 5）、`backend/`（Java 后端）、`docs/`（文档）；前后端分离指各端独立工程、独立进程、通过 RESTful 接口通信，不要求分仓库。
- 用户端 Vue 工程已初始化（`frontend/user-h5/`）；安装、开发、构建命令见该目录自己的 README（`frontend/user-h5/README.md`），本文件不再重复。
- 商家端为桌面 Web 后台（React 18 + Vite + Ant Design 5），位于 `frontend/merchant-admin/`，运行方式见该目录自己的 README。
- Java 后端位于 `backend/`；接口与联调口径以 `docs/backend/后端接口契约.md`、`docs/backend/后端任务分配与联调清单.md`、`docs/backend/后端联调验收用例.md` 为准。

## 2. 文档阅读与协作入口

- 阅读入口：`docs/README.md`（分角色阅读路径）、`docs/project/项目目录索引.md`（目录职责索引）。
- 唯一有效 PRD：`docs/PRD.md`；视觉与交互基线：`docs/design/`（`设计系统-用户端.md`、`设计系统-商家端.md`）；实现转写以 `docs/design/exports/**` 设计稿代码导出为准。
- 小组协作、问题单与 PR 流程：`docs/project/小组协作与提交流程.md`。
- 分工与里程碑：`docs/project/分工与里程碑安排.md`；每日进度：`docs/record/daily/YYYY-MM-DD.md`（记录规则见 `docs/project/项目规则.md`）。

## 3. 范围说明

- 用户端 Vue 工程已在 `frontend/user-h5/` 初始化，按 PRD 与接口契约开发。
- 真实 Java 后端、数据库脚本和后端测试在 `backend/` 目录完成，完成状态以实际源码、测试和联调记录为准。
- 商家端为桌面 Web 后台（React 18 + Vite + Ant Design 5），在 `frontend/merchant-admin/` 实现；桌面优先 ≥1280px，不做移动端。
- 课程基础 P0 共六类：用户、商家（含商家注册）、店铺、商品、购物车、基础订单；P0 订单状态为单一状态字段，创建后仅 `PROCESSING`、不流转（`PROCESSING` 仅作为 P0 阶段状态保留）；本期扩展在此基础上新增待支付（`PENDING_PAYMENT`）、支付成功即 `PENDING`（待接单）、商家推进状态（`COOKING`/`DELIVERING`/`COMPLETED`）与取消（`CANCELLED`）；后端现行 `markPaid` 写 `PROCESSING`、再接单转 `PENDING` 属偏差，需按 PRD 7.6 定稿修正（修正归后端；联调按定稿状态核对，前端文案映射以定稿为准）。
- 库存一致性、商家订单推进与跨端同步（细分状态）、模拟支付属本期实施范围（2026-09-09 决议），不是 P0。
- **2026-09-09 决议后范围**：除平台管理员外全部 P1/P2 本期实施——优惠与后端统一计价、评价与商家回复、用户与商家消息、搜索排序与分页、商家收藏、会员权益、红包、商品规格扩展、待支付倒计时、再来一单、评价订单、分类绑定商品，以及简化取消订单、~~简化配送管理~~、图片上传、设置与 FAQ；**平台管理员已砍掉**，不制作页面、接口、数据与测试。商家端扩展经营与消息 7 页、用户端扩展服务 13 页同在交付范围。
- 扩展接口的运行影响：真实接口模式 baseURL 与 §4.3 的联调切换口径不变；图片上传的存放目录与访问路径已于 2026-09-09 定稿——存后端本地目录 `backend/uploads/`（不入 Git）、经 `/uploads/**` 静态映射访问，运行配置见 §4.3；优惠配置结构与订单金额快照（`deliveryFee`）的库变更与联调口径同样见 §4.3。
- 平台客服已移除；订单中的“联系商家”保留为用户到商家的会话。
- 支付为前端/系统模拟反馈，不接第三方支付；支付失败页只保留重新支付，支付成功页只保留查看订单和回首页。

## 4. 服务器与部署（已定稿，2026-09-10）

> **填写责任人**：后端同学（部署脚本与局域网演示环境归属见 `docs/project/分工与里程碑安排.md`）。本节方案已于 2026-09-10 定稿并随 §4.4 发布记录更新；后续调整直接改本节，并在 §4.4 保留上一版本回滚点。

### 4.1 既有约束（无论选哪种方案都成立）

- 代码在各成员本地电脑编写，通过 Git 分支 + PR 协作（见 `docs/project/小组协作与提交流程.md`）；服务器只承担"运行"（数据库、后端服务、演示访问），不承担"写代码"。
- 建表 SQL 与种子数据跟随后端工程维护（规划路径 `backend/database/schema/`），须可重复执行并可重置演示数据（PRD §7.18）。
- 数据库密码、服务器密码与密钥不写入提交代码（PRD §7.18），也不发群聊。

### 4.2 待后端同学拍板的问题清单

| # | 问题 | 结论 |
| --- | --- | --- |
| S1 | 服务器信息：主机地址、登录方式（口令/密钥）、负责人与使用权限分配 | 主机 `82.157.137.114`（SSH 22），账号 `wjy/wxq/lc/fzp/dyh` 均 sudo；账号与密码见负责人私聊，**密码不入库、不入群、不写入文档**（PRD §7.18） |
| S2 | 启用阶段与用途：开发期是否使用；联调期承载什么；演示期是否托管前端构建产物 | 开发期即启用，承载 MySQL 8（3306）；联调期后端服务可在成员本机或服务器跑；**演示期方案已定（2026-09-10）**：服务器以 pm2 承载三端构建产物与后端，入口为用户端 `http://82.157.137.114:4001/user/`、商家端 `:4001/`、互动汇报 `:4001/demo/`（2026-09-13 曾短暂评估改为 4100，最终保留 4001，见 §4.4 注记），后端仅监听 `127.0.0.1:4000` 并由 Nginx 代理 API，见 §4.4 |
| S3 | 数据库策略：开发期各人本地库还是共用服务器库；本地库如何初始化、表结构变更如何同步全组 | 开发期共用服务器库 `elm_practice`；表结构变更经 `backend/database/schema/schema.sql` + `DatabaseInitializer` 启动幂等建表，全组拉代码后重启即同步；测试用独立库 `elm_practice_test`，每用例前 `reset.sql` 复位 |
| S4 | 环境配置：开发环境与演示环境的配置划分与切换方式 | `application.properties` 默认指向本机；通过 `DB_HOST/DB_USER/DB_PASSWORD/DB_NAME` 环境变量覆盖指向服务器库，密码不入仓库 |
| S5 | 联调指向：联调期用户端、商家端请求 baseURL 如何切换到演示环境 | 用户端/商家端 baseURL 默认 `http://localhost:4000/api/v1`；联调时改为 `http://82.157.137.114:4000/api/v1`（2026-09-10 统一为 4000，见 BUG-20260908-007）（后端跑服务器时）或保持本机（后端跑本机连服务器库） |
| S6 | 部署步骤：三端构建、启动、重启与演示数据重置的可复制命令清单 | 见 §4.3 |

结论直接写入上表"结论"列；单项内容较多时在下方 §4.3 展开小节。

### 4.3 方案展开区（后端同学填写）

**数据库部署（已完成）**
- MySQL 8.0.46 已装在 `82.157.137.114`，监听 `0.0.0.0:3306`；时区表已导入（`Asia/Shanghai` 可用）。
- 开发库 `elm_practice`、测试库 `elm_practice_test`（utf8mb4）；业务账号 `elm`（仅授权这两个库）；密码与服务器登录密码一致，**由负责人私聊分发、不入仓库**，经 `DB_PASSWORD` 环境变量注入（2026-09-08 全组统一口径，改密码必须周知全组并更新本节，禁止单方面改动）。
- 建表与种子：`backend/database/schema/schema.sql` + `backend/database/seed/seed.sql`，由 `DatabaseInitializer` 启动时幂等执行（users 表为空才插种子，避免重启重置运行期库存/销量）。
- 腾讯云安全组已放行 TCP 3306（来源 `0.0.0.0/0`，建议后续收紧到团队出口 IP）。

**数据库变更：优惠配置与订单金额快照（2026-09-09 定稿，需重置种子/迁移）**
- 优惠配置：`promotions` 扩列 `new_user_amount`、`free_delivery_threshold`、`member_discount`；满减阶梯改用新表 `promotion_tiers(store_id, threshold, amount, sort_order)`（不再用单档字段）。
- 开发库旧单档数据**不做静默兼容**：按"重置种子 + 迁移脚本"处理——`backend/database/seed/seed.sql` 重置为多档阶梯（演示满 20 减 2、满 40 减 5）；迁移脚本由后端同学在 `backend/database/` 下落盘后回写本节路径（**本期实现（待开发）**，未落盘前以"重置种子"为准）。
- 订单金额快照新增 `deliveryFee`（配送费）；计价公式：实付 = 商品小计 − 满减 − 新客立减 − 红包 − 会员折扣 + 配送费 − 配送费优惠 + 打包费（¥2.00），且不小于 0；配送费默认 ¥3.00、店铺可配、未配置按 0。
- 表结构变更仍走 `backend/database/schema/schema.sql` + `DatabaseInitializer` 启动幂等建表（§4.2 S3）；测试库 `reset.sql` 同步新表与新列后重跑，未同步前的测试结果不作为证据。

**后端启动（连服务器库）**
```powershell
cd backend
$env:JAVA_HOME="<本机 JDK 17 路径>"
$env:DB_HOST="82.157.137.114"; $env:DB_USER="elm"; $env:DB_PASSWORD="<密码>"; $env:DB_NAME="elm_practice"
mvn spring-boot:run
```

**测试（连服务器独立测试库）**
```powershell
cd backend
$env:DB_HOST="82.157.137.114"; $env:DB_USER="elm"; $env:DB_PASSWORD="<密码>"; $env:DB_NAME="elm_practice_test"
mvn clean test
```
每用例前 `reset.sql` 复位种子（TRUNCATE + 重插），测试间互不污染，不污染开发库。

**演示数据重置**
```bash
ssh wjy@82.157.137.114
mysql elm_practice < backend/database/schema/schema.sql
mysql elm_practice < backend/database/seed/seed.sql
```

**图片上传运行配置（2026-09-09 定稿）**
- 存储：后端本地目录 `backend/uploads/`，**不入 Git**；现行 `.gitignore` 只有 `.uploads/`，需补 `backend/uploads/` 规则（待仓库维护同学补，未补前不得提交上传产物）。
- 访问：后端静态映射 `/uploads/**`（与接口前缀 `/api/v1` 分开，前端拼 URL 时不要带 `/api/v1`）；不接对象存储、不接 CDN。
- 如需可配置基址：由后端把目录与静态映射前缀的配置项定名后回写 `docs/backend/后端接口契约.md` 与本节，前端不写死服务器地址，直接使用接口返回的 `url`。
- 部署要求：`backend/uploads/` 需可写并在服务器持久化（安全组只影响端口，不额外放行路径）。
- 与契约的现行差异：契约 §10.1 写 `file.upload-dir` 默认 `backend/uploads/`（2026-09-11 已统一）、读取接口 `GET /api/v1/files/images/{fileName}`，与本次定稿的 `backend/uploads/` + `/uploads/**` 不一致，待后端同学回写契约；本文件以定稿为准。

**联调金额与状态核对（2026-09-09 定稿）**
- 金额明细：确认订单页、订单详情与支付页的金额明细为「**基础四行 + 优惠项按实际发生展示**」（基础四行 / 优惠项按实际发生展示，2026-09-11 定稿，CHG-004）（基础四行：商品小计 / 打包费 / 配送费 / 实付金额；满减优惠、红包优惠等优惠项金额非 0 时各占一行、未发生不显示），金额以后端返回的快照（含 `deliveryFee`）为准，前端只展示不计算。商家端订单详情金额汇总同口径，契约 §5.2 已同步。
- 种子数据需含店铺配送费配置（默认 ¥3.00、未配置按 0）与多档满减阶梯；联调金额用例同时覆盖"未配置配送费按 0"与"实付不小于 0"。
- 状态核对：按 PRD 7.6 定稿，支付成功即 `PENDING`（待接单），`PROCESSING` 仅作 P0 阶段状态；联调发现后端仍返回 `PROCESSING` 时按缺陷登记，不以"能跑通"放行。

### 4.4 发布记录（按时间倒序，最新在上）

> **⚠️ 2026-09-13 入口处置（已生效）**：4001 自 2026-09-10 15:39 起被同服务器另一项目 `classroom-vote` 占用、本项目站点配置被改名为 `elm-release.conf.disabled-for-classroom`（不再加载），对外入口一度全部 404。**处置**：曾评估改为 4100（模板/脚本一度同步，见 git 历史），最终**经负责人确认后保留 4001**——停用 `classroom-vote` 的站点配置（改名保留、可回退，备份 `/home/ubuntu/backups/nginx-20260913-4001switch/`）并恢复本项目配置；**同时移除配置中过时的 5174 旧地址跳转块**（5174 现由同服务器项目 `banwei-vote` 的 node 服务占用，该块 `bind()` 失败会导致**整次 nginx reload 中止**，从而出现「配置改了但线上行为不变」——本次实测踩到并修复）。

#### 2026-09-13 发布：合并后 main（`707f234`，当前在线版本）

- **在线版本**：`/srv/elm-current → /srv/elm-releases/20260913-707f234`；源码与构建产物在 `/home/ubuntu/releases/elm-20260913-707f234/`（源码以 `git archive` 打包上传，服务器侧非 git 仓库）
- **入口**：用户端 `http://82.157.137.114:4001/user/`、商家端 `http://82.157.137.114:4001/`、互动汇报 `http://82.157.137.114:4001/demo/`、后端仅本机 `127.0.0.1:4000`（经 Nginx 两条 API 路径代理）
- **本次上线内容**：9/11–9/13 的全部合并——后端批次① 优惠计价七步（PR #48）、批次⑥ 红包与爆红包（PR #49）、订单状态机修正（PR #51）；用户端批次⑩ 订单/支付/评价/消息/打磨、批次① 金额明细、批次⑥ 收藏·会员·红包前端三批（PR #45–#56）、搜索结果页与搜索页、评价图片上传、SHOW-QA-002/004/005 修复、加购抛球、保真度巡检与像素比对脚本；商家端二阶段页面与规则（PR #57）
- **构建与验证证据**（服务器 Node 22.23.2 / Java 17.0.20 / Maven 3.6.3）：
  - 用户端：`npm ci` → `npx vitest run`（**47 文件全绿**）→ `npm run type-check` → `VITE_API_MODE=real VITE_API_BASE_URL=/user/api/v1 npx vite build --base=/user/`
  - 商家端：`npm ci` → `npm test`（**9 文件 / 29 用例全绿**）→ `VITE_API_MODE=real VITE_API_BASE_URL=/api/v1 npm run build`
  - 后端：`mvn -B -DskipTests package` 生成 29M jar（集成测试需独立测试库，本次未跑）
  - 汇集 `www/{user,merchant,demo}` 后执行 `python3 deploy/release.py --source /home/ubuntu/releases/elm-20260913-707f234 --tag 20260913-707f234 --release-sha 707f234 --backup /home/ubuntu/backups/elm-20260913-707f234` → **RELEASE_OK**；`pm2 save` 已执行
- **真实接口冒烟**：`python3 deploy/smoke.py` **50/50 通过**（`businessAssertionsPassed=true`、`testStoreClosed=true`，合成订单 `o1198` / 临时店 `m1192`）
  - **冒烟脚本本次一并修正（`TODO-OPS-001`）**：原断言写死 `total == 60`（旧口径「小计 58 + 打包费 2」）且支付后状态断言 `PROCESSING` → 与批次① 七步计价、PR #51 状态机修正不符，**首次冒烟因此失败**；已改为**按契约 §3.5 公式核对金额**（实付 = 小计 − 满减 − 新客 − 红包 − 会员折扣 + 配送费 − 配送费优惠 + 打包费，且不小于 0）与「支付成功即 `PENDING`」，并复跑全绿
- **外网与端到端核对**（发布后）：`4001/user/`、`4001/user/api/v1/stores`、`4001/demo/`、`4001/`（商家端）均 **200**；演示账号登录成功且 `GET /me/coupons` 返回**真实券数据**（cp001/cp002，含 `status`/`used`/`source`/`canBlast`）→ 新后端 + 新建表 + 种子生效；订单接口返回新金额快照字段（`deliveryFee`/`fullReductionAmount`）；`GET /me/notifications` 返回 **404**（批次④ 未实现，与《未完成模块与如实标注清单》一致）
- **静态产物核对**：线上用户端入口 JS 引用 `SearchEntryView-*.js`、`SearchResultView-*.js` 且可 200 取到（今日新增页面确在线上）；商家端入口 JS 含「优惠配置 / 数据统计 / 评价管理」（二阶段构建确在线上）。**注**：本机与服务器构建的同提交产物哈希不同（平台原生打包器差异），故以「功能性子资源存在性」而非哈希比对作为核对口径
- **回滚点**：上一版本 `/srv/elm-releases/20260910-99a646d` 与源码目录 `/home/ubuntu/releases/elm-20260910-e41444c` 保留；本次回滚资产 `/home/ubuntu/backups/elm-20260913-707f234/`（含发布前 `pm2-before.json`、`restore-frontends.json` 与 `rollback-note.txt`）
- **数据库**：**无需手工迁移**——后端启动时 `DatabaseInitializer` 幂等建表/升级（本批新增 `promotion_tiers`、`coupons`、`coupon_packs`、`users.free_blast_date` 与订单金额快照列）；**未对业务库执行任何重置类脚本**
- **注意**：服务器上同时运行其他项目（`banwei-vote`、`students-way`、`sw-rag` 等），本次只重启 `elm-backend`、只替换 `/etc/nginx/conf.d/elm-release.conf` 与 `/srv/elm-current` 软链

#### 2026-09-10 发布：合并后 main（`99a646d`，上一版本）


> **⚠️ 2026-09-13 入口恢复（已生效）**：4001 自 2026-09-10 15:39 起被同服务器另一项目 `classroom-vote` 占用、本项目站点配置被改名为 `elm-release.conf.disabled-for-classroom`（不再加载），对外入口一度全部 404。**处置**：曾评估改为 4100（模板/脚本一度同步，见 git 历史），最终**经负责人确认后保留 4001**——停用 `classroom-vote` 的站点配置（改名保留、可回退，备份 `/home/ubuntu/backups/nginx-20260913-4001switch/`）并恢复本项目配置；**同时移除配置中过时的 5174 旧地址跳转块**（5174 现由同服务器项目 `banwei-vote` 的 node 服务占用，该块 `bind()` 失败会导致**整次 nginx reload 中止**，从而出现「配置改了但线上行为不变」——本次实测踩到并修复）。服务器已于 2026-09-13 21:44 生效：本机与外网 `4001/user/`、`4001/user/api/v1/stores`、`4001/demo/` 均 200（标题为「轻量外卖」）。**注意**：线上内容仍是 9/10 版本，9/11–9/13 的合并需重新发布一次方可见。

- **在线版本**：`/srv/elm-current → /srv/elm-releases/20260910-99a646d`；源码与构建产物在 `/home/ubuntu/releases/elm-20260910-e41444c/`（对应提交 `99a646d`：合并后 main + 上下架接口修复）
- **入口**：用户端 `http://82.157.137.114:4001/user/`、商家端 `http://82.157.137.114:4001/`、互动汇报 `http://82.157.137.114:4001/demo/`、后端仅本机 `127.0.0.1:4000`（经 Nginx 两条 API 路径代理）
- **构建与验证证据**（服务器 Node 22.23.2 / Java 17.0.20 / Maven 3.6.3）：
  - 用户端：`npm ci` → `npx vitest run` → `VITE_API_MODE=real VITE_API_BASE_URL=/user/api/v1 npx vite build --base=/user/`
  - 商家端：`npm ci` → `npm test`（6 文件 / 20 用例全绿）→ `VITE_API_MODE=real VITE_API_BASE_URL=/api/v1 npm run build`
  - 后端：`mvn -B -DskipTests package` 生成 29M jar（后端集成测试需独立测试库，本次未跑，见 §4.3）
  - `nginx -t` 通过；`/`、`/user/`、`/demo/`、`/user/api/v1/stores` 均 200
- **真实接口冒烟**：`python deploy/smoke.py` **50/50 通过**（`businessAssertionsPassed=true`、`testStoreClosed=true`，合成订单 `o1178` / 测试店 `m1172`，证据见 `deploy/smoke-result.json`）
- **浏览器级核对**（Playwright 对线上地址）：用户端首页与 `/user/stores/m002` 渲染真实店铺商品；商家端登录后订单管理显示真实订单（含状态与金额）；用户端登录后订单页状态显示中文（`待接单` 等），确认合并后的状态映射生效
- **讲稿口径**：`/demo/答辩手册.html` 返回 **404** —— 按负责人决定，讲稿不入 main、不再随发布提供
- **发布脚本**：`deploy/release.py` 已参数化（`--source/--tag/--release-sha/--backup`，默认值与 2026-09-08 首次发布一致）；本次命令：`python3 deploy/release.py --source /home/ubuntu/releases/elm-20260910-e41444c --tag 20260910-99a646d --release-sha 99a646d --backup /home/ubuntu/backups/elm-20260908`
- **回滚点**：上一版本 `/srv/elm-releases/20260908-508889e` 与源码目录均保留；PM2/nginx 回滚资产在 `/home/ubuntu/backups/elm-20260908/`（本次已把 `restore-backend.json` 另存为 `restore-backend.json.bak-20260910`）。失败回滚按 `deploy/README.md` 步骤执行，不回灌数据库
- **注意**：服务器上同时运行其他项目（`sw-rag`、`student-profile`、`jt-textbook-pipeline` 等），本次只重启 `elm-backend`、只替换 `/etc/nginx/conf.d/elm-release.conf`

### 4.5 开发环境与后端测试路径（R8，2026-09-10）

**结论（负责人 2026-09-10 拍板）：统一使用服务器测试库，本机不需要装 MySQL。** 服务器实测：业务库 `elm_practice`，**独立测试库 `elm_practice_test` 已存在**；数据库账号 `elm_local@localhost` 对两个库均有 ALL PRIVILEGES，**该账号仅限服务器本机连接**（远程连不上属正常安全配置）。

**执行方式：在服务器上跑**（不是从本机远程连库）。步骤：SSH 登录 `ubuntu@82.157.137.114`（需本机公钥已登记）→ 进入当前 release 源码的 `backend/` → 从私有 PM2 备份取 DB 连接参数（不落仓库）→ **强制 `DB_NAME=elm_practice_test`** → `mvn -B test`。测试前如需重建结构，只对测试库执行 `database/schema/schema.sql` 与 `database/seed/seed.sql`。

**红线**：`DB_NAME` 必须为 `elm_practice_test`；**禁止对业务库 `elm_practice` 执行任何会重置数据的测试或脚本**。

补充路径：`.github/workflows/ci.yml` 的 `backend-test` 任务用 MySQL 服务容器自动跑 `mvn test`（2026-09-10 首跑通过 23 条后端测试，已提升为必需任务），PR 与 main 推送都会执行——**不开服务器也能拿到后端测试结论**，日常优先看 CI。本机 `mvn test` 因 `127.0.0.1:3306` 不可达会报 `Communications link failure`，属环境缺失、不是代码缺陷，不要据此改代码。

服务器发布路径见 §4.4；发布脚本 `deploy/release.py` 已参数化，发布前会校验源目录、nginx 模板、回滚资产与后端 jar。

**联调 baseURL 切换**
- 后端跑本机连服务器库：前端 baseURL 保持 `http://localhost:4000/api/v1`。
- 后端跑服务器：前端 baseURL 改 `http://82.157.137.114:4000/api/v1`（需安全组放行 4000）。
