# 需求变更台账（自拟变更与老师变更统一登记）

> **用途**：登记每一次需求变更的变更单、影响分析、新增测试与回归要求，作为结课汇报「需求变更响应」的正式素材（来源：`docs/project/中期汇报演示方案.md` 结课独有素材清单）。
> **规则**：按 `AGENTS.md` R7，变更涉及 PRD / 契约 / TDD 用例 / 范围决议时，必须在**同一个 PR 内**同步全部受影响文档；测试按 `docs/project/TDD实施规划.md` §9 先红后绿，`test:` 提交仅含测试代码。
> **建立**：2026-09-10。老师下发的阶段 2 变更按「待定」跟踪（负责人判断大概率不发布），本台账先登记本组自拟变更。

## 变更索引

| 编号 | 日期 | 来源 | 变更主题 | 状态 | 负责人 |
| --- | --- | --- | --- | --- | --- |
| CHG-001 | 2026-09-10 | **本组自拟**（非老师下发） | 新增「爆红包」模块（需求确认阶段曾称「开红包」，2026-09-11 起统一为「爆红包」） | **已定稿**（2026-09-10 负责人批准基线；同日完成**第二次修订**并按 R7 同步文档） | 成员E（组长） |
| CHG-002 | 2026-09-11 | **本组自拟**（砍范围） | 砍掉「简化配送管理」整块（配送页 + 配送单层） | **已定稿**（配送状态改由订单状态表达、订单详情零改动；配送单表/接口/用例一并砍） | 成员E（组长） |
| CHG-003 | 2026-09-11 | **本组自拟**（削范围 + 口径细化） | 订单跟踪页并入订单详情；取消入口/按钮口径；新稿取代旧稿 | **已定稿**（PRD/设计系统/清单/待办已同步；新两页已入库） | 成员E（组长） |
| CHG-004 | 2026-09-11 | **本组自拟**（口径扩展） | 金额明细由「固定四行」扩展为「基础四行 + 优惠项按实际发生展示」 | **已定稿**（PRD/契约/SRS/TDD/设计系统/运行说明/待办已同步） | 成员E（组长） |
| CHG-005 | 2026-09-15 | **本组自拟**（口径变更） | 首页定位栏「无地址」口径改三态（未登录=演示地址；已登录无地址=占位「选择收货地址」） | **已定稿**（PRD/SRS/清单/决策记录/台账同批同步；实现与测试已落地） | 成员E（组长） |

---

# CHG-001 「开红包 / 爆红包」模块

## 1. 变更单

| 项目 | 内容 |
| --- | --- |
| 变更编号 | CHG-001 |
| 提出人 / 日期 | 成员E（组长）（负责人），2026-09-10 |
| 变更来源 | **本组自拟**。老师阶段 2 变更内容截至 2026-09-10 未发布，按「待定」跟踪；本变更用于补齐「需求变更响应」流程闭环并增加演示视觉亮点 |
| 变更类型 | 新增功能模块（扩范围） |
| 需求确认记录 | `docs/project/爆红包需求确认记录.md` |
| 现状 | 全库无任何开红包 / 爆红包界面；`coupons` 表、红包接口与红包种子均不存在（契约 §3.8 已定字段） |
| 优先级与冻结影响 | 与批次⑥（收藏·会员·红包）共用后端窗口；**不跨过 9/15 功能冻结** |

### 1.1 变更内容（第 1 轮已确认部分） **【已废止（被第二次修订取代，见 PRD v1.3 与契约 §3.10/§10.5）】**

- 入口：首页活动区入口 → 红包页；**在红包页内开红包**（第 1 轮问题 1：方案 A + B）。
- 获得方式：**红包页提供“购买红包”入口**，购买后可开红包（第 1 轮问题 2；具体含义待第 2 轮澄清）。
- 金额规则：购买所得红包为固定金额 / 满减档位，**开红包后金额增长**，预设多组金额与满减档位（第 1 轮问题 3；成长规则与上限待第 2 轮澄清）。
- 动效：**方案 B（中等）**——红包卡片翻转 + 金额弹出 + 首屏“爆”效果（红包雨 / 粒子）+ 金额数字滚动（第 1 轮问题 4）。
- 与既有红包体系：**完全打通**——开出红包写入 `coupons`，可在确认订单页选用，走契约 §3.8 的 `couponId`（第 1 轮问题 5）。
- 门槛与有效期：大多数为「有门槛 + 7 天有效」，少部分按概率开出「无门槛 + 7 天有效」（第 1 轮问题 6；概率待定稿）。
- 异常：未登录 / 已领过 / 活动结束 / 接口失败 / 网络慢**逐一明确提示且可重试**（第 1 轮问题 7）。
- 页面数字：负责人要求**直接显示、不标注占位**（第 1 轮问题 8）——⚠️ 与 PRD §8 / 7.17「不得用固定演示数值冒充真实数据」存在冲突，处理方式待第 2 轮确认。
- 端与角色：**用户端 + 商家端只读**（第 1 轮问题 9）。
- 验收口径：**四类证据齐全**（接口 + 数据库 + 测试 + 页面，走 TDD 红绿 + 契约新增章节）（第 1 轮问题 10 前半）。

### 1.2 第 2 轮确认后的范围（结论：收敛） **【已废止（被第二次修订取代，见 PRD v1.3 与契约 §3.10/§10.5）】**

1. **购买 = 买一组红包**（一次购买生成多张，示例 ¥2 买几张「满 30 减 5」），不是买一张。
2. **不落支付记录**：付费只做前端模拟，不复用模拟支付接口、不新增支付表。
3. **购买次数不限**（可无限次购买）。
4. 档位门槛与减免均为**整数**；档位集合预设并**写在代码常量**。
5. **商家端零新增**：只在订单详情显示「本单红包抵扣金额」，复用订单金额快照 `couponAmount`，不新增页面与接口。
6. **显示口径**：真实实现的数据显示真实值；未真实实现的显示占位（化解了与 PRD §8/7.17 红线的冲突）。
7. 首页只把「天天爆红包」做成真入口，其余保持不可点装饰（另有 3 个零成本候选待确认，见记录 §7 问题 7）。
8. 负责人要求**全做完、不砍不降级**（与 9/15 功能冻结的冲突见记录 §7 问题 9，待定）。

### 1.3 第 3 轮确认后的补充（2026-09-10） **【已废止（被第二次修订取代，见 PRD v1.3 与契约 §3.10/§10.5）】**

9. **套餐**：¥2 = 5 张「满 30 减 5」；**购买次数完全不限**。
10. **升档 = 固定升一档**，阶梯中**门槛与减免同时可变**；档位池采纳建议档位并扩充（含满 25 / 满 40），**无门槛档定额减 5**。
11. **一单一红包保持**（PRD 7.4 计价第 ⑥ 步与契约 §3.8 均不用改）。
12. **购买入口** = 红包页顶部按钮；**首页真入口** = 宫格分类（按分类过滤商家列表）+ 宫格「天天爆红包」（进红包页）。
13. **占位视觉** = 与真实内容完全同款，仅点击提示「暂未开放」。
14. ✅ **9/15 功能冻结为硬约束不破**（解决上一轮冲突）；演示主线 = 全链路。

### 1.4 第 4 轮确认后的补充（2026-09-10） **【已废止（被第二次修订取代，见 PRD v1.3 与契约 §3.10/§10.5）】**

15. **开箱模型由「固定升一档」反转为「7 档随机命中」**——第 3 轮的递增阶梯作废，无最高档概念。
16. 一次购买生成的 5 张红包**初始档位相同**（满 30 减 5）。
17. **首页候选 1（定位 → 地址列表页）经核实在库**（`HomeView.vue` 已有 `goAddressList()` → `address-list`，PRD 806 已定义），不排任务。
18. **占位口径**：有真实统计用真实值，无真实统计用占位（按此写 PRD）。
19. 开箱承载＝红包页**上方覆盖式动效浮窗**（含红包形状与开箱按钮，结果留在浮窗内），**零新路由**；动效分镜按建议执行（≤3.5 秒，可连点）。
20. 开箱失败：浮窗保留 + 原地重试；**不预置待开红包**；验收清单 8 条全部采纳。

### 1.5 第 5 轮确认后的补充（2026-09-10） **【已废止（被第二次修订取代，见 PRD v1.3 与契约 §3.10/§10.5）】**

21. **开箱一次性 + 替换式**：每张只能开一次，开出的新券**替换**原券（不新增行）；**开出来的红包不能再被开**。
22. **纯随机**且不存在降档问题（可开对象只有「满 30 减 5」，池内 8 档均不低于它）。
23. **档位池 8 档**：满 30 减 5 / 满 30 减 8 / 满 25 减 8 / 满 40 减 10 / 满 25 减 15 / 满 40 减 20 / 满 50 减 25 / 无门槛减 5；概率＝**高额度低概率**（权重表数值待定）。
24. **要有开红包音效**（单个、简单）；演示账号**什么都不预置**。
25. 浮窗：竖版红包卡片居中、简约写实、半透明暗化、**先点「开」再播动画**；**开箱后重新计时 7 天**。

### 1.6 三项最终确认（2026-09-10，负责人） **【已废止（被第二次修订取代，见 PRD v1.3 与契约 §3.10/§10.5）】**

26. **档位权重表定稿**：满30减5 30% / 满30减8 20% / 满25减8 15% / 满40减10 12% / 满25减15 10% / 满40减20 7% / 满50减25 3% / 无门槛减5 3%。
27. **音效**：同意由脚本**合成原创短音效**（0.6–1.0 秒，前端工程内，加载失败静默降级）。
28. **首页宫格分类过滤不做**（负责人原话：商品、商家分类都不做，只把红包这块儿变成真实入口）→ **平台级「点餐分类 10 类」登记为后续变更候选**（对应 PRD 第 408 行 P0 既有缺口）。
29. **变更文档落实形式确认**：采用「过程证据层（本台账）+ 唯一真源层（PRD/SRS/契约/TDD/设计系统原位更新）」两层方案；本台账不复制口径。

> **收敛结论（更新，最终）**：新增面 = **1 张新表 + 2 个接口 + 用户端动效浮窗与音效 + 首页红包入口真跳转**；商家端零新增（仅订单详情加一行）；不新增支付实体；首页宫格分类与平台级分类**本期不做**。

### 1.7 第二次修订（2026-09-10，按已产出设计稿对齐）

30. **命名**：交互由「开红包」改称**「爆红包」**，UI 文案以设计稿为准（天天必爆 / 免费爆1次 / 爆）。
31. **入口重构**：「待开红包区」口径**作废**；改为顶部**「加量红包省更多 / 去购买」通栏 → 买红包浮窗**，以及**「天天必爆」活动卡 → 免费爆1次**。
32. **免费次数**：每个用户**每天 1 次免费爆**（0 点重置、不消耗已购券）；用完后爆一次**消耗一张已购券**；都没有则 `409` + 提示去购买。
33. **档位池 10 档**：原 8 档 + **满30减18.8（5%）** + **满40减18.8（5%）**，权重同步重排（见契约 §10.5）。
34. **有效期拆分**：**爆出来的券当天 23:59:59 到期**；**购买未爆的券 7 天**。
35. **套餐**：`pack49` = 4 张（3×满30减5 + 1×无门槛减5，面额 ¥20）；`pack99` = 8 张（6×满30减5 + 1×满40减10 + 1×无门槛减5，面额 ¥45）。
36. **占位券**：品类券与「限非外卖配送」券**纯展示、不入库、不可选用**（`scope` 仅 `ALL`/`STORE`）。
37. **视觉例外**：红包 / 爆红包模块**保留红金渐变**（活动场景例外）；其余页面仍守单一亮橙 `#ff5a1f`。
38. **删除退款条款**：买红包浮窗的「30 天内未使用一键退款」权益条目**删除**（不接真实退款）。
39. **设计真源入库**：四页设计稿由 `docs/_archive/new_page` 迁入 `docs/design/exports/用户端/11-天天必爆/**`（红包页 / 爆红包过渡态 / 爆出结果 / 买红包浮窗），资源引用已改为 `shared-assets` 相对路径；**红框标注图为待办**（TODO-USER-031）。

> 第二次修订的落地范围：`docs/PRD.md` **v1.3**、`docs/backend/后端接口契约.md` §3.10/§10.5、`docs/project/TDD实施规划.md` §5.21、`docs/design/设计系统-用户端.md`、`docs/project/用户待补充资料与原型清单.md` §6.2、`docs/todo/**`。

## 2. 影响分析（截至第 1 轮）

| 影响面 | 影响内容 | 处理方式 |
| --- | --- | --- |
| 产品范围（PRD） | 新增「开红包」页面与交互；红包来源从“种子发放”扩展为“可购买 + 可开” | 定稿后按 R7 同步 PRD §6.11 / 7.10 / §6.1 交付范围总表 |
| 接口契约 | 需新增 **2 个接口**：红包购买（买一组，生成待开记录）、开红包（升档并写入 `coupons`）；沿用 §3.8 的 `couponId` 选用（**不新增支付接口**） | 定稿后新增契约章节并在其中标注“本期实现（待开发）” |
| 数据库 | 新增 **1 张表**：红包购买 / 待开记录（用户 + 购买批次 + 张数 + 档位 + 状态）；开箱结果写入 `coupons`；`scope` 维持 `ALL`/`STORE`；**无支付表** | 与批次⑥ 的 `coupons` 建表一并设计，避免两次改表 |
| 服务端业务规则 | 购买事务、开箱增长规则、并发与重复提交防护、红包归属与有效期校验 | 复用订单事务与库存一致性的既有口径 |
| 用户端前端 | 首页活动区真入口、红包页开红包交互与动效（方案 B ≈1 天）、确认订单页选用 | 动效不占后端工期，但需在 9/13 前留出回归时间 |
| 商家端 | **零新增**：仅在订单详情显示「本单红包抵扣金额」 | 复用订单金额快照 `couponAmount`，无需新接口与新页面 |
| 测试用例 | 需在 TDD §5 矩阵追加新前缀用例组（建议 `TC-RBP`），并按“先红后绿”提交 | 用例清单由模块负责人 + 测试联调同学人工确定后才允许 AI 落地（TDD §9） |
| 工期与冻结 | 与批次① 计价第 ⑥ 步、批次⑥ 红包共用同一后端窗口；**9/15 冻结不改** | 若 9/13 晚未收口，按第 2 轮问题 10 的取舍执行 |
| 报告与演示 | 提供“需求变更响应闭环”素材（变更单 + 影响分析 + 新增测试 + 回归），以及一个视觉效果突出的演示页面 | 结课汇报素材清单 |

## 3. 新增测试清单（**旧草案，已废止；定稿见 TDD §5.21**）

> 按 `TDD实施规划.md` §9：测试标准与异常场景由模块负责人与测试联调同学人工确定，AI 只可辅助生成脚手架。以下为骨架草案，编号前缀建议 `TC-RBP`，最终以第 2 轮定稿后的清单为准。 **本清单为旧模型（购买红包/开红包升档）草案，已废止**：CHG-001 第二次修订后的定稿为 `TDD实施规划.md` §5.21 的 13 条 TC-RBP（爆红包、替换式、免费爆、当天 23:59 到期），下表仅作历史留档、不得作为用例真源。

| 建议编号 | 场景 | 类型 | 预期 |
| --- | --- | --- | --- |
| TC-RBP-001 | 购买红包成功 | 正常 | 生成待开红包记录，金额与档位符合配置 |
| TC-RBP-002 | 开红包金额增长 | 正常 | 命中预设档位且不超过上限 |
| TC-RBP-003 | 未登录购买 / 开红包 | 权限 | `401`，不产生记录 |
| TC-RBP-004 | 开出的红包写入 `coupons` | 正常 | `status=available`、`validTo` 符合规则、归属正确 |
| TC-RBP-005 | 用开出的红包下单 | 正常 | 计价按第 ⑥ 步扣减，红包置 `used`，一单一红包 |
| TC-RBP-006 | 使用已用 / 已过期红包 | 异常 | `409`，金额不变 |
| TC-RBP-007 | 并发开红包 / 重复提交 | 边界 | 不重复生成、不重复入账 |
| TC-RBP-008 | 他人红包 / 他人购买记录 | 权限 | `404`，不泄露信息 |
| TC-RBP-009 | 开箱接口失败与重试 | 异常 | 无脏数据，重试可成功 |
| TC-RBP-010 | 商家端只读列表越权 | 权限 | 用户身份调用 `403`；商家只能看本店数据 |

## 4. 回归与冻结要求

- 回归范围：红包列表、确认订单计价（含 `couponAmount` 快照）、订单金额明细展示（基础四行 + 优惠项按实际发生）、商家端订单详情。
- 冻结纪律：**不跨过 9/15 功能冻结**；未收口一律写“本期实现（待开发）”，不得写成已完成。
- 提交纪律：先补失败测试（`test:` 仅含测试代码，在该提交上复跑必须红）→ 再改实现（`feat:`）→ 回归；红绿证据写入 PR 描述或 raw/ 片段。

## 5. 未定项（第 2 轮问题待回答）

- **无未决产品口径**：第 1–5 轮 50 问全部作答，三项最终确认已落 §1.6；**方案基线见 `docs/project/爆红包需求确认记录.md` §13**。
- 待办事项：负责人确认方案基线 → 按 R7 落地文档同步 → 登记实现任务。

> 自 2026-09-10 起，需求确认阶段的问题改为**对话确认**，不再写入 `爆红包需求确认记录.md`。

## 6. 变更历程

| 日期 | 事件 | 产出 |
| --- | --- | --- |
| 2026-09-10 | 负责人提出要做「爆红包 / 开红包」界面；第 1 轮需求确认（10 问）完成 | 本变更单 + `docs/project/爆红包需求确认记录.md` |
| 2026-09-10 | **第 2 轮需求确认（10 问）完成**：范围收敛为 1 表 + 2 接口 + 前端动效，商家端零新增，不落支付记录 | 记录 §6 分析、§7 第 3 轮问题；本单 §1.2 与影响分析同步 |
| 2026-09-10 | **第 3 轮需求确认（10 问）完成**：套餐 ¥2/5 张、不限次数、固定升一档且门槛可变、一单一红包保持、顶部购买按钮、首页分类+红包入口、9/15 冻结不破 | 记录 §8 分析、§9 收敛轮问题；本单 §1.3 与未定项同步 |
| 2026-09-10 | **第 4 轮需求确认（10 问）完成**：开箱口径反转为「7 档随机命中」、初始档统一满 30 减 5、占位口径按「有无真实统计」、开箱为覆盖式动效浮窗、验收清单 8 条采纳；候选 1 经核实在库 | 记录 §10 分析、§11 终轮问题；本单 §1.4 与未定项同步 |
| 2026-09-10 | **第 5 轮需求确认（10 问）完成**：开箱＝一次性替换、纯随机、8 档池、高额度低概率、要音效、不预置任何红包、浮窗细节、开箱后重置 7 天；问题 10 转为「变更文档落实形式」流程问题 | 记录 §12 分析；本单 §1.5 与未定项同步；**自本轮起问题改为对话确认** |
| 2026-09-10 | **方案基线产出**（记录 §13）：定位、范围边界、页面清单、核心流程、数据组织、权限、首版范围、原型要求、验收清单、风险与未决、附 A 权重表 / 附 B 音效规格 / 附 C R7 文档同步清单 | 本单 §1.6 三项最终确认；状态置为「方案基线待确认」 |
| 2026-09-10 | **第二次修订**：负责人产出 4 页设计稿并调整口径（爆红包命名、免费爆、10 档池、当天 23:59、两档套餐、占位券、红金例外、删退款条款） | 本单 §1.7；PRD 升 v1.3；契约 §3.10/§10.5、TDD §5.21、设计系统、清单、待办同步；设计稿入库 `docs/design/exports/用户端/11-天天必爆/**` |
| 2026-09-10 | **负责人批准方案基线 → CHG-001 定稿**；按 R7 完成文档同步：`docs/PRD.md` 升 **v1.2**（版本记录 + 7.10 开箱链路 + 7.17 占位口径 + 6.1 页面数说明；**该版本已由同日第二次修订的 PRD v1.3 取代，当前 v1.3**）、`docs/SRS.md` 与 `docs/SRS-工程版.md` 新增 **FR-RED-02**、`docs/backend/后端接口契约.md` 新增 **§3.10** 与 **§10.5**、`docs/project/TDD实施规划.md` 新增 **§5.21 TC-RBP** 组与批次⑥说明、`docs/design/设计系统-用户端.md` 新增购买浮层与开箱浮窗约定；任务登记 TODO-BE-022/023、TODO-USER-028~031、TODO-MER-012、TODO-DOC-014；原型 PROTO-RED-01~03 登记；音效已合成 `frontend/user-h5/public/sounds/open-coupon.wav` | 全部落在同一工作区内，待负责人提交 |

---

# CHG-002 砍掉用户端「配送信息/状态页」

## 1. 变更单

| 项目 | 内容 |
| --- | --- |
| 变更编号 | CHG-002 |
| 提出人 / 日期 | 成员E（组长）（负责人），2026-09-11 |
| 变更来源 | **本组自拟**（砍范围） |
| 变更类型 | 删除功能模块（缩范围） |
| 变更原因 | ① 该页信息量极小（不建骑手端、不做地图，只剩三态标签 + 收件人快照）；② 与订单详情/订单跟踪高度重叠；③ **配送状态已由订单状态覆盖**——商家端 `PATCH /merchant/orders/{orderId}/status` 已实现，订单详情 `statusText(order.status)` 已能显示「配送中/已完成」，**订单详情零改动** |
| 影响 | 用户端少 1 页、少 1 套原型（PROTO-P2-002）、少 1 张红框图；批次② 联调面缩小 |

## 2. 已落地

- `docs/PRD.md`：§6.1 页面清单去掉该页；7.6 新增「配送状态的展示口径」说明（`DELIVERING`→配送中、`COMPLETED`→已完成，订单详情现有状态区即可覆盖，**无需新增字段/接口/页面**）；7.16.1 删除该页行。
- `docs/design/设计系统-用户端.md`：该页样式约定行标注**已砍掉**。
- `docs/project/用户待补充资料与原型清单.md`：`PROTO-P2-002` 状态改为**已砍掉，不制作**。
- `docs/todo/用户端.md`：`TODO-USER-026`（配送状态原型）置为**已冻结**。

## 3. 待决

- **配送单数据层已一并砍掉（2026-09-11 负责人拍板）**：`deliveries` 表、契约 §10.2 三个接口、TDD `TC-DLV-001~006`、`TODO-BE-004`（已冻结）与 `TODO-MER-002` 中的配送推进部分全部不实现；SRS `FR-DLV-01`、契约、用例、架构设计、后端清单、前端架构、级联用例与验收用例均已标注为已砍掉；**保留**订单状态推进里的 `DELIVERING`（配送中）与商家端「出餐/完成」按钮。

---

# CHG-003 订单跟踪页并入订单详情（附取消入口 / 按钮口径与旧稿取代）

## 1. 变更单

| 项目 | 内容 |
| --- | --- |
| 变更编号 | CHG-003 |
| 提出人 / 日期 | 成员E（组长）（负责人），2026-09-11 |
| 变更来源 | **本组自拟**（削范围 + 口径细化） |
| 变更类型 | 删除页面（跟踪页并入详情页）+ 交互口径细化 |
| 变更原因 | ① 新导出的订单详情设计稿一页内已含**状态时间线与商品/费用/配送/订单信息**，跟踪页与详情页内容 90% 重复；② 跟踪页旧导出稿本身残缺（缺 `code.html`/`screen.png`） |
| 影响 | 用户端少 1 页、少 1 张红框图、少 1 组断言；`TODO-USER-010` 冻结 |

## 2. 已落地

- `docs/PRD.md`：**7.6 新增「用户端订单进度时间线：文案 ↔ 状态值映射表」**（已下单/已支付/商家接单/配送中/已完成 ↔ `PENDING_PAYMENT`/`PENDING`/`COOKING`/`DELIVERING`/`COMPLETED`，`CANCELLED` 不进时间线）；**7.16.1 删除「订单跟踪页-跟踪内容区」行**，新增「订单详情页-状态头与时间线」行，并更新「订单详情内容」（补四行金额、配送信息卡、订单信息卡、第三方字样替换要求）与「底部操作区」（**取消按钮接单后保留但置灰 + 点击提示**、**删除「查看配送进度」按钮**）、「支付页-支付内容区」（倒计时卡 + 卡内取消入口 + 立即支付）。
- `docs/design/设计系统-用户端.md`：支付页行补 CHG-003 形态；新增「订单详情页（含订单进度时间线）」样式行。
- `docs/project/用户待补充资料与原型清单.md`：新增 `PROTO-CHG003-01`（订单详情）、`PROTO-CHG003-02`（支付页）为**已提供**；`PROTO-P2-001` 补注「入口已在支付页设计稿中确定，弹窗设计稿与红框图已入库（`12-订单与支付/03-取消确认弹窗/`、`27_2-01-cancelSheet.png`）」。
- `docs/todo/用户端.md`：`TODO-USER-010` 冻结；`TODO-USER-008`（待支付倒计时）、`TODO-USER-017`（支付页版式）、`TODO-USER-002`（取消确认弹层）补设计真源证据。
- **入库**：`docs/design/exports/用户端/12-订单与支付/01-订单详情-含订单跟踪/`、`02-支付页/`（各含 `index.jsx`/`index.module.scss`/`code.html`/`screen.png`），素材 8 个入 `shared-assets/用户端/12-订单与支付/**`，页内资源引用已重写（0 处残留）；红框图 4 张入 `docs/prd-assets/user/prototype-chg003-orderpay/` 并回填 PRD 7.16.1。
- **旧稿取代**：`06-订单/02-订单详情`、`06-订单/03-订单跟踪`、`07-支付/02-收银台变体` 各加 `README.md` 标注已被 `12-订单与支付/**` 取代（旧稿问题：02-订单详情内容整块重复、03-订单跟踪缺 Stitch 两件、02-收银台变体缺 Figma 两件）。

## 3. 待决 / 未做

- ~~取消订单确认弹窗~~ **已落地（2026-09-11）**：新稿入库 `docs/design/exports/用户端/12-订单与支付/03-取消确认弹窗/`（**底部弹层** + 取消原因 chips 必填 + 0/50 文本域，形态与设计系统一致），红框图 `27_2-01-cancelSheet.png` 已产出并回填 PRD 7.16.1；**设计稿中的「如有已使用的优惠券将原路退回」按负责人拍板删除**。
- ~~`07-支付/01-收银台`~~ **已标注为被取代（2026-09-11 负责人拍板）**：支付页真源由 `12-订单与支付/02-支付页` 取代，该目录已加 `README.md` 说明不得作为实现真源。
- ~~待补（负责人重新导出）~~ **已落地（2026-09-11）**：支付页补充版（Figma `28_2` + Stitch）已入库并**覆盖** `12-订单与支付/02-支付页/`，订单详情卡已含**商品清单 + 金额明细（商品小计 / 打包费 / 配送费 / 满减优惠 / **红包优惠** / 实付金额）**；红框图 2 张（`28_2-01-payContent`、`28_2-02-itemsAndAmount`）已重做并回填 PRD 7.16.1；旧素材 `26_170-*` 已清理。
- ~~新增待决~~ **已定稿（2026-09-11 负责人拍板，登记 CHG-004）**：金额明细口径扩展为「**基础四行 + 优惠项按实际发生展示**」——基础四行恒显示（商品小计 / 打包费 / 配送费 / 实付金额），优惠项（满减优惠、红包优惠等）在金额非 0 时各占一行、未发生不显示；已按 R7 同批同步 PRD 7.4/7.16.1、契约 §3.5/§5.1/§5.2、SRS 两版、TDD `TC-ORD-022`、前端架构、设计系统、运行说明与待办。

---

# CHG-004 金额明细口径扩展为「基础四行 + 优惠项按实际发生展示」

## 1. 变更单

| 项目 | 内容 |
| --- | --- |
| 变更编号 | CHG-004 |
| 提出人 / 日期 | 成员E（组长）（负责人），2026-09-11 |
| 变更来源 | **本组自拟**（口径扩展） |
| 变更类型 | 展示口径变更（不改计价与快照） |
| 变更原因 | 支付页补充版设计稿的金额明细为 6 行（含「满减优惠」「红包优惠」两行），与 PRD 7.4 / 契约 §5.2 原先的「固定四行」不一致；负责人拍板**扩展口径**而非回改设计稿 |
| 新口径 | **基础四行恒显示**：商品小计 / 打包费 / 配送费（为 0 时仍显示 ¥0.00）/ **实付金额**；**优惠项按实际发生展示**：满减优惠、红包优惠、新客立减、会员折扣、配送费优惠等**金额非 0 时各占一行**、以品牌橙负数展示（如 −¥5.00），**未发生则整行不显示**；顺序固定为 商品小计 → 打包费 → 配送费 → 满减优惠 → 红包优惠 → 其他优惠 → 实付金额 |
| 边界 | **只影响展示层**：后端计价公式、金额快照字段（契约 §3.5/§5.1）与前端「只展示不计算」的口径均不变 |

## 2. 已落地（R7 同批同步）

- `docs/PRD.md`：**7.4** 金额规则改写为新口径；**7.16.1** 订单详情内容行与支付页内容区行同步（支付页行原先的「待拍板」注记改为**已按定稿口径确认**）。
- `docs/backend/后端接口契约.md`：**§3.5**（定稿公式与明细行数）与 **§5.2**（订单详情金额构成）两处「固定四行」表述改为新口径，并注明取代 2026-09-09 的旧表述。
- `docs/SRS.md`（金额规则、跨端一致性两处、验收步骤）与 `docs/SRS-工程版.md`（跨端一致性第 2 条）同步。
- `docs/project/TDD实施规划.md`：`TC-ORD-022` 断言改为新口径；二次修订说明与批次①说明同步。
- `docs/design/设计系统-用户端.md`（订单详情页行）、`docs/frontend/前端架构设计与工程约定.md`（购物车与下单行）、`docs/project/运行说明.md`（联调金额核对）同步。
- `docs/todo/用户端.md` `TODO-USER-001` 与 `docs/todo/商家端.md` `TODO-MER-011` 的口径描述同步。

## 3. 影响与回归

- **对实现**：确认订单页、订单详情页、支付页三处明细渲染改为「基础四行 + 动态优惠行」；商家端订单详情金额汇总同口径；**无需改计价与快照**。
- **回归范围**：确认订单计价展示（含「满减与红包同时发生」「仅发生其一」「均未发生」三种场景）、订单详情金额、支付页金额、商家端订单详情金额汇总；`TC-ORD-021/022`。
- **待补证据**：前端三处明细的「有优惠项 / 无优惠项」两种截图（`EVID-*` 待收口）。

# CHG-005 「首页定位栏无地址」口径变更（演示地址 → 三态）

## 1. 变更单

| 项目 | 内容 |
| --- | --- |
| 变更编号 | CHG-005 |
| 提出人 / 日期 | 成员E（组长）（负责人），2026-09-15 |
| 变更来源 | **本组自拟**（口径变更；由负责人走查「新建账号地址为空、首页定位却仍显示天津大学北洋园校区」引发） |
| 变更类型 | 展示口径 + 可测行为变更（不改接口、不改契约、不改后端与数据库） |
| 变更原因 | PRD 7.16.1 原口径「右侧定位文字来自当前用户默认地址，**无地址时使用课程演示地址「天津大学北洋园校区」**」未区分「未登录访客」与「已登录但未填地址」两种场景；后者会让新账号看到一个并不属于它的地址，语义上冒充了用户真实收货地址。原检查列的兜底手段「保留默认演示地址**并标记为演示数据**」在实现上仅由 `title` 属性承载（`HomeView.vue` 的 `:title`），H5 移动端不可见，等于未落实。 |
| 新口径（方案 C·三态） | ① **未登录访客**：显示课程演示地址「天津大学北洋园校区」并标记为演示数据（首页必须可浏览，且承载课程演示）；② **已登录且有地址**：取默认地址 `region`（缺省 `detail`），不标记；③ **已登录但无任何地址**：显示占位「**选择收货地址**」，不标记，**不得用演示地址冒充真实地址**；④ **读取地址失败**：仍按原口径保留演示地址并标记。 |
| 边界 | 定位文案唯一出口为 `frontend/user-h5/src/utils/location.ts`（`resolveLocationState`）；点击定位仍进地址列表、登录引导由地址列表页负责（原口径不变）；**不改后端、接口契约、数据库与商家端**。 |

## 2. 已落地（R7 同批同步）

- `docs/PRD.md`：**7.16.1** 行「首页-定位与频道栏」（字段列与检查列）与行「搜索结果页-搜索头部」（定位来源句）改为三态口径。
- `docs/SRS.md`：**§6.2.1** 两处同源副本同步（与 PRD 逐字同口径）。
- `docs/frontend/首页复刻素材清单.md`（定位栏说明）、`docs/frontend/用例设计决策记录.md`（T61 修订，并在 §3「已知简化口径（答辩主动说明项）」补「定位栏未登录态」一行）。
- `docs/project/需求变更台账.md`：本变更单。
- 实现：`frontend/user-h5/src/utils/location.ts` 新增 `ADDRESS_PLACEHOLDER`、`resolveLocationState`、`LOCATION_LOAD_FAILED`；`frontend/user-h5/src/views/user/HomeView.vue`、`SearchResultView.vue`、`CategoryStoreListView.vue` 三页共用（消除 `DEMO_LOCATION` 的重复定义，收口 `TODO-USER-005` 遗留）。
- TDD 证据：新增 `frontend/user-h5/src/utils/__tests__/location.spec.ts`（LC-1~LC-7）；改 `frontend/user-h5/src/views/user/__tests__/HomeView.spec.ts` T61（改为「已登录无地址 → 占位且不标记」，并在 T1 补未登录演示标记断言）、`SearchResultView.spec.ts` SR-14、`CategoryStoreListView.spec.ts` CL-14。**红：4 文件 10 failed / 47 passed → 绿：4 文件 57 passed**；全量 `npx vitest run` 与 `npm run build` 见当日 raw 留痕。

## 3. 影响与回归

- **影响面**：用户端三页定位栏（首页 / 搜索结果页 / 分类商家列表页）的文案与演示标记；未登录访客与已有地址用户的行为不变。
- **回归范围**：`HomeView.spec.ts` T1/T60/T61/T62、`SearchResultView.spec.ts` SR-14、`CategoryStoreListView.spec.ts` CL-3/CL-14、`utils/__tests__/location.spec.ts` LC-1~LC-7；全量单测与构建。
- **待补证据**：真实模式（real）下用**新建账号**走一遍首页 / 搜索结果页 / 分类商家列表页三处定位栏的截图（未登录态与无地址态各一张）；`docs/testing/测试计划.md` 的固定演示数据账号不受影响。


