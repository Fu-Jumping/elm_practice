# 后端任务分配与联调清单

日期：2026-09-01

> **说明**：本文件只保留里程碑 deadline 与联调节点；内部任务拆分由后端同学自行建表维护，文档归属见 `docs/backend/README.md`。

## 1. 当前边界（摘要）

单仓库多目录：`frontend/user-h5/`（用户端 Vue 3）、`frontend/merchant-admin/`（商家端 React 18 + AntD 5 桌面后台）、`backend/`（Java 后端）、`docs/`。后端在 `backend/` 创建 Spring Boot 工程，按 `docs/backend/后端接口契约.md` 实现 `/api/v1` RESTful 接口，与前端真实接口模式联调。工程初始化、文档归属与写作纪律见 `docs/backend/README.md`。

## 2. 里程碑 deadline（后端线摘要）

> 口径以 `docs/project/分工与里程碑安排.md` 为准，此处只列后端线节点，冲突时以上述文档为准。

| 时间 | 节点 | 后端线完成门槛 |
| --- | --- | --- |
| 9/2–9/3 | M1 契约打底 | 契约定稿推进、最小架构设计与数据库实体方案（后端 A）；9/3 `backend/` Spring Boot 骨架初始化 |
| 9/4 | 并行开工 | 第一批失败测试提交（`test:` 先于 `feat:`）；后端账号模块当天有功能提交 |
| 9/5–9/6 | 周末冲刺 | 9/5 注册登录/权限与商品分类管理；9/6 购物车与店铺浏览链路 |
| 9/7 晚 | P0 完成截止 | 地址/确认订单/订单创建与查询、商家端订单页完成，关闭 Mock 全链路联调，当晚验收演练 |
| 9/8–9/9 | 第一阶段中期验收 | 验收后立即进入第二阶段需求变更（先补失败测试再改代码）；**9/9 负责人拍板第二阶段范围决议**：除平台管理员外全部 P1/P2 纳入本期 |
| 9/10–9/14 | 本期扩展模块分批交付 | 按模块收口，每模块以“接口 + 数据库 + 测试 + 页面”四类证据交付（顺序与负责人见 §6）；未收口模块如实标注“本期实现（待开发）” |
| 9/15 | 功能冻结 | 冻结 tag；**本期新增模块的 deadline 即 9/15 冻结前**，冻结后不再新增功能 |
| 9/17–9/18 | 老师最终验收 | 三端构建部署与部署验证 |
| 第 4 周 | 材料归档（具体日期以老师通知为准） | 全部材料引用同一冻结版本 |

## 3. P0 接口交付顺序（联调节点）

后端按以下顺序交付，前端逐段关闭 Mock 回退；字段、请求体、状态机以契约为准：

1. 用户注册/登录/注销/当前用户：`/api/v1/users`、`/api/v1/auth/login`、`/api/v1/auth/logout`、`/api/v1/me`
2. 店铺和商品查询：`/api/v1/stores`、`/api/v1/stores/{storeId}`、`/api/v1/stores/{storeId}/products`
3. 地址维护：`/api/v1/me/addresses`
4. 商家登录和店铺管理：`/api/v1/merchant/auth/login`、`/api/v1/merchant/store`
5. 商家分类和商品管理：`/api/v1/merchant/categories`、`/api/v1/merchant/products`
6. 购物车：`/api/v1/cart`、`/api/v1/cart/items`
7. 创建订单：`/api/v1/orders`
8. 订单列表/详情/明细：`/api/v1/orders/**`
9. 商家订单查询（P0，2026-09-02 负责人拍板，免评审）：`/api/v1/merchant/orders`、`/api/v1/merchant/orders/{orderId}`
10. P1 模拟支付：`/api/v1/orders/{orderId}/payment`——**已实现**（2026-09-06 修复轮，见契约 §9），本期继续维护并纳入回归

创建订单必须写入订单头、明细快照、地址快照和金额字段，不得只返回订单号。

§3 为 P0 顺序；2026-09-09 决议新增模块（取消、搜索分页、收藏、会员、红包、通知、优惠计价、评价图片、规格、分类绑定、图片上传、统计）的交付顺序与负责人见 §6。

## 4. 联调 Checklist（按页面）

| 顺序 | 页面 | 验证点 |
| --- | --- | --- |
| 1 | `/login` | 真实登录成功进入首页 |
| 2 | `/` | 首页商家来自真实接口 |
| 3 | `/merchants/m002` | 商家详情和商品来自真实接口 |
| 4 | 商家详情购物车 | 加购、改数量、删除后接口同步 |
| 5 | `/checkout?storeId=m002` | 地址、商品和基础金额能显示 |
| 6 | `/orders` | 新订单出现在列表 |
| 7 | `/orders/:orderId` | 订单明细、地址和基础状态正确 |
| 8 | 支付环节（Mock 演示页，无固定路由） | 选定支付扩展后，模拟支付状态正确更新 |
| 9 | 订单取消 | 仅 PENDING_PAYMENT/PENDING 可取消、原因必填、取消后库存回补且状态为 CANCELLED |
| 10 | 搜索结果页 | 商家名/商品名/分类条件查询，排序 综合/销量/距离，分页 10 条/页 |
| 11 | 收藏页 | 收藏/取消收藏、收藏列表与商家状态 |
| 12 | 会员与红包 | 会员标识与会员价、红包列表与确认订单选用（一单一红包） |
| 13 | 优惠影响结算 | 满减/新客立减/配送费优惠/会员折扣/红包按 PRD 7.4 顺序，金额以后端为准 |
| 14 | 评价（含图片） | 一单一评、标签与图片上传、商家回复双端回显 |
| 15 | 消息与通知 | 双端未读分离、通知列表与已读 |
| 16 | 商家端规格与分类绑定 | 规格保存（specOptions/priceDelta）、批量绑定商品到分类 |
| 17 | ~~配送单~~（已砍，CHG-002） | 商家推进 PENDING_DELIVERY→DELIVERING→DELIVERED，用户端只读（**2026-09-11 已砍掉，CHG-002**） |
| 18 | 商家端统计 | 只统计有效订单，字段与口径见契约 §6.3 |

P0 通过后再测：用户评价、用户消息、商家端订单推进、商家端商品上下架、商家端优惠影响用户结算。**9–18 为 2026-09-09 决议新增模块，标注“待实现后执行”**，未实现前不得作为联调通过证据；平台管理员已砍掉，不做页面与接口联调。

## 5. 后端交付物

后端项目源码、数据库建表 SQL、种子数据 SQL、运行说明、接口自测记录、已联调通过的接口列表、未完成接口及原因说明、与契约不一致处的处理说明。

逐项验收动作与固定数据见 `docs/backend/后端联调验收用例.md`；接口自测记录格式见 `docs/backend/后端接口契约.md` 第 8 章。本期新增模块需额外交付**契约 §10.4 定稿口径的落地结果**（六条已于 2026-09-09 拍板，非待确认）与**未收口模块清单（含原因）**，未实现模块的证据字段写“待补”。

## 6. 本期新增模块任务与负责人（2026-09-09 决议，deadline：9/15 功能冻结前）

分工沿用现有口径：后端 A（王嘉悦）负责架构、契约回写与商家订单相关接口；后端 B（林晨）负责业务接口、数据库与部署；用户端页面由用户端前端线负责、商家端页面由商家端前端线负责（本表只列后端线认领部分，前端任务以各自排期为准）。

| # | 模块 | 接口/数据交付 | 负责人 | 完成门槛（9/15 前） |
| --- | --- | --- | --- | --- |
| 1 | 取消订单与 CANCELLED | `POST /api/v1/orders/{orderId}/cancel`、订单状态机与取消字段 | 后端 A（接口与状态机）+ 后端 B（回补库存事务） | 正常/异常/权限用例通过；取消与支付并发只生效一次 |
| 2 | 搜索、排序与分页 | `GET /api/v1/search`、`GET /api/v1/stores` 分页参数 | 后端 A | 商家名/商品名/分类条件 + 三种排序 + 10 条/页 |
| 3 | 商家收藏 | `/api/v1/me/favorites` 三个接口 + favorite 表 | 后端 B | 唯一约束、幂等收藏、归属校验 |
| 4 | 会员与会员价 | `GET /api/v1/me/member`、商品 `memberPrice` 字段与计价 | 后端 B（数据）+ 后端 A（计价） | 会员标识来自种子数据，会员价影响商品小计 |
| 5 | 红包 | `/api/v1/me/coupons`、可用红包查询、下单 `couponId` | 后端 B | 一单一红包、门槛与适用范围校验、并发锁定 |
| 6 | 通知 | `/api/v1/me/notifications` 列表与已读、未读数 | 后端 B | 三类通知、未读幂等、归属校验 |
| 7 | 优惠配置影响下单计价 | `promotions` 字段扩展 + 下单七步计价与金额快照 | 后端 A（计价）+ 后端 B（种子数据） | PRD 7.4 顺序与演示数值一致；金额以后端为准 |
| 8 | 评价图片与标签 | `/orders/{orderId}/review` 新增 `tags`/`images`；商家回复回显 | 后端 B | 一单一评、图片地址校验、回复更新原记录 |
| 9 | 规格启用与分类绑定商品 | `PUT /merchant/products/{id}/specifications`、`PATCH /merchant/categories/{id}/products` | 后端 A | 规格价差进入购物车与订单行，批量绑定校验归属 |
| 10 | 图片上传 | `POST /api/v1/files/images`（本地目录存储，不接对象存储） | 后端 B | 类型/大小限制、角色校验、失败重试口径 |
| 11 | ~~简化配送~~（已砍，CHG-002） | 配送单查询与状态推进三个接口 + delivery 表 | 后端 B | 只顺序推进、幂等与 409、用户端只读（**2026-09-11 已砍掉，CHG-002**） |
| 12 | 统计口径扩展 | `overview`/`analytics` 字段与“有效订单”口径 | 后端 A | 只统计有效订单，范围注明，空数据返回 0/空数组 |
| 13 | 契约与架构同步 | 契约 §10.4 定稿口径的回写、架构设计与数据库文档同步 | 后端 A | 口径有结论并回写，不留两套口径 |
| 14 | 数据库与部署 | 新增表建表/种子/迁移脚本、演示数据重置 | 后端 B | 脚本可重复执行，重置后演示数据可复现 |
| 15 | 爆红包（购买套餐 + 爆一次） | `POST /api/v1/me/coupon-packs`、`POST /api/v1/me/coupons/blast`、`coupon_packs` 表、`users.free_blast_date`、`coupons.source/can_blast/pack_id` | 后端 A（契约与接口）+ 后端 B（表、种子与权重池） | 每日 1 次免费爆（0 点重置）、10 档权重池、爆出券当天 23:59:59 到期、购买券 7 天；正常/异常/权限用例通过（CHG-001，契约 §3.10/§10.5） |

说明：**平台管理员已砍掉**，不排任务、不建表、不做接口与测试；上表模块如 9/15 前未收口，按 PRD 与项目规则如实标注“本期实现（待开发）”，不得写成已完成。

---

## 需求编号 ↔ 后端契约章节映射（原 SRS 工程版 §2 迁移，2026-09-15）

> 迁移说明：本表取自原 `docs/SRS-工程版.md` §2 需求追踪矩阵的「接口契约映射」列，**只迁移映射关系，不复制 §4 需求正文**；**TC 编号列不迁移**——测试用例编号真源为 `docs/project/TDD实施规划.md`，需求编号真源为 `docs/SRS.md`。`docs/SRS-工程版.md` 于 2026-09-15 拆分归档。共 36 条 FR 映射。

| FR 编号 | 需求名称 | PRD 来源 | 契约章节 |
| --- | --- | --- | --- |
| FR-ACC-01 | 用户注册 | PRD 7.1 | 契约 3.1 `POST /users` |
| FR-ACC-02 | 用户登录、注销和当前用户 | PRD 7.1 | 契约 3.1 `POST /auth/login`、`POST /auth/logout`、`GET /me`、`PATCH /me` |
| FR-MER-01 | 商家注册并创建店铺 | PRD 7.1 | 契约 4.1 `POST /merchants` |
| FR-MER-02 | 商家登录和店铺信息维护 | PRD 7.1、7.13 | 契约 4.1 |
| FR-STO-01 | 店铺列表、详情、分类和商品查询 | PRD 7.3 | 契约 3.2 `GET /stores/**`、`GET /products/{id}` |
| FR-ADR-01 | 地址查询、新增、修改和删除 | PRD 7.9 | 契约 3.3 `GET/POST/PATCH/DELETE /me/addresses/**` |
| FR-CAT-01 | 商家分类增删改查 | PRD 7.12 | 契约 4.2 `/merchant/categories` |
| FR-PRD-01 | 商家商品增删改查 | PRD 7.12 | 契约 4.2 `/merchant/products` |
| FR-CART-01 | 购物车查询、加购、改数量和删除 | PRD 7.3 | 契约 3.4 `/cart`、`/cart/items` |
| FR-ORD-01 | 从购物车创建基础订单 | PRD 7.4、7.6 | 契约 3.5 `POST /orders` |
| FR-ORD-02 | 用户订单列表、详情和明细 | PRD 7.6 | 契约 3.5 `GET /orders/**` |
| FR-MORD-01 | 商家订单列表和详情查询 | PRD 7.16.2 | 契约 5.1 `GET /merchant/orders/**` |
| FR-PAY-01 | 模拟支付结果更新与待支付倒计时 | PRD 7.5 | 契约 3.5 `POST /orders/{id}/payment` |
| FR-OST-01 | 商家订单状态推进与跨端同步 | PRD 7.6、7.11 | 契约 5.2 |
| FR-PRM-01 | 优惠与后端统一计价 | PRD 7.4 | 契约 6.3 + §3.5 优惠计价接入下单（已定稿） |
| FR-SKU-01 | 商品规格 | PRD 7.3、7.12 | 契约 4.2 `PUT /merchant/products/{productId}/specifications` |
| FR-REV-01 | 评价与商家回复 | PRD 7.7、7.14 | 契约 6.2 |
| FR-MSG-01 | 消息与联系商家 | PRD 7.8、6.10 | 契约 6.1 |
| FR-SEA-02 | 关键词搜索与历史 | PRD 7.2 | 契约 3.2 |
| FR-SEA-03 | 搜索排序与分页 | PRD 7.2、7.17 | 契约 3.2（分页与排序参数待回写） |
| FR-FAV-01 | 商家收藏 | PRD 7.10 | 契约 §3.7 商家收藏 |
| FR-MBR-01 | 会员权益 | PRD 7.10 | 契约 §3.8 会员与红包 |
| FR-RED-01 | 红包 | PRD 7.10、7.4 | 契约 §3.8 会员与红包 |
| FR-RED-02 | 爆红包（CHG-001） | PRD 7.10、7.4 | 契约 §3.10 爆红包（购买套餐 / 爆红包）、§3.8（`couponId` 选用） |
| FR-CXL-01 | 取消订单 | PRD 7.6 | 契约 §3.5 用户取消订单 |
| FR-DLV-01 | 配送单与配送状态 | PRD 7.6 | —（已砍，CHG-002；契约 §10.2 同节亦已标注） |
| FR-IMG-01 | 图片上传 | PRD 7.7、7.12 | 契约 §10.1 图片上传 |
| FR-STA-01 | 商家经营概览与数据统计 | PRD 6.12、7.14 | 契约 6.3 `GET /merchant/analytics` |
| FR-SET-01 | 设置与 FAQ | PRD 7.10 | 契约 3.1 `GET /me`、`PATCH /me` |
| FR-MPRM-01 | 商家端优惠配置 | PRD 6.16、7.13 | 契约 6.3 |
| FR-MREV-01 | 商家端评价管理与回复 | PRD 6.17、7.14 | 契约 6.2 |
| FR-MMSG-01 | 商家端消息 | PRD 6.17、7.8 | 契约 6.1 |
| FR-MSKU-01 | 商家端规格维护 | PRD 6.14、7.12 | 契约 4.2 |
| FR-MCAT-02 | 分类绑定商品 | PRD 6.14、7.12 | 契约 4.2（待补） |
| FR-MORD-02 | 联系顾客 | PRD 7.11、6.13 | 契约 6.1 |
| FR-AI-01 | AI 点餐助手 | PRD 6.11.1 | 契约 §10.6 AI 点餐助手 |

未收口模块仍按「本期实现（待开发）」标注；模块级的交付顺序与负责人见本文件 §6，测试用例编号与用例正文见 `docs/project/TDD实施规划.md`。
