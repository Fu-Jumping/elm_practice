# 后端文档入口

日期：2026-09-01。本文件是后端同学（后端 A、后端 B）与测试联调同学的文档工作入口：先看这里，再进各文档。

## 1. 文档地图

| 文档 | 状态 | 归属 | 用途 |
| --- | --- | --- | --- |
| `docs/PRD.md` | 定稿（唯一需求源） | 组长（成员E（组长）） | 产品范围、优先级、页面与验收口径的唯一来源；下游只引用不修改 |
| `docs/SRS.md` | 骨架（章节已认领 2026-09-02） | 正文主笔后端 B（成员A（后端B））；组长（成员E（组长））保留引言、追踪矩阵与终审；第 8 章与测试联调同学（成员D（商家端/测试））共创 | 系统行为与验收条件；写作纪律与章节分工见该文件头 |
| `docs/backend/后端接口契约.md` | 草案（组长起草）；**2026-09-09 增补本期新增章节 §3.5–§3.10、§10（含 CHG-001 的 §3.10/§10.5）** | 组长（成员E（组长））保留总规则与资源清单；后端 A（成员C（后端A））确认扩充字段与错误码；细节口径以后端实现为准（见契约头部弱化声明） | RESTful 接口唯一契约；本期新增接口统一标注“本期实现（待开发）”，平台管理员已砍掉（契约 §10.3） |
| `docs/backend/架构设计.md` | v1.2（2026-09-09，第二阶段范围同步） | 后端 A（成员C（后端A）） | 模块划分、下单与取消数据流、实体约束（含本期新增表）、数据库选型与框架版本、Session 安全边界、§7 事务与并发口径 |
| `docs/backend/后端任务分配与联调清单.md` | 已删减；**2026-09-09 增补 §6 本期新增模块任务与负责人** | 组长（成员E（组长））保留里程碑与联调节点 | 里程碑 deadline、P0 交付顺序、联调 Checklist 与本期模块分工（deadline 9/15 冻结前） |
| `docs/backend/后端联调验收用例.md` | 已删减；**2026-09-09 增补 §3 本期新增模块验收场景（待实现后执行）** | 组长（成员E（组长））保留 P0 场景与固定数据 | P0 场景清单、固定演示数据与本期新增模块的正常/异常/权限场景；接口级用例由测试联调同学从契约生成 |
| `docs/backend/后端B任务清单.md` | 已重新启用（2026-09-09） | 后端 B（成员A（后端B）） | 后端 B 个人任务清单：M1/M2 状态回填 + 本期新增模块任务（模块 17–28） |
| 数据库建表 SQL / 种子数据 | 初版已提供（2026-09-06） | 后端 A/B 协作 | `backend/database/schema/schema.sql` 与 `backend/database/seed/seed.sql`；后端 B 接入真实 DAO 时继续维护并与 SRS §5 对齐 |

## 2. 接口契约怎么接手（后端 A：成员C（后端A））

- **组长保留区**：第 1 章接口总规则，以及第 3–6 章各接口的"方法/路径/说明"表（资源清单）。
- **后端 A 负责区**：每个接口的请求/响应字段明细、错误码明细、种子数据扩充，以及商家订单接口（契约第 5 章）的优先级归属与设计定夺。对现有草案逐节**确认、修订、扩充**，以自己的 Git 提交落痕，由组长评审定稿。
- **写作纪律**：PRD 已写死的规则（如优惠计算顺序 PRD 7.4、订单状态表 PRD 7.6）只引用章节号，不复述；发现 PRD 未定义或两处冲突，报小组修订，不得自行拍板写入。
- **弱化口径**：契约头部已声明"以实现为准"——后端认为草案细节过严或有误的，直接改并回写即可，不需要事先逐条请示。

## 3. 架构设计怎么写（后端 A：成员C（后端A），9/3 前出可开发版本）

创建 `docs/backend/架构设计.md`，至少包含：

- 模块划分：Controller/Service/DAO 分层与各模块职责（账号、店铺、商品、地址、购物车、订单）。
- 数据流：一次下单请求从 Controller 到数据库的完整路径。
- ER 图与实体方案：表结构、主外键、唯一约束；固定演示数据引用契约第 2 章。落点由后端 A、后端 B 商定（写进架构设计或独立数据库文档均可，不重复做两份）。
- 数据库选型与框架版本（**2026-09-10 结案，不再列为待确认项**）：MySQL 8；Spring Boot 3.5.5 + MyBatis Spring Boot Starter 3.0.4（Java 17）。以 `backend/pom.xml` 为准。
- 端间边界：接口细节一律引用契约章节号，不重复契约内容。

## 4. SRS 认领（已于 2026-09-02 确认到人）

SRS 按章节拆分共创，分工与写作纪律见 `docs/SRS.md` 文件头：**后端 B（成员A（后端B））主笔**"总体描述、功能需求、数据需求、接口需求、非功能需求"各章并主笔"测试与验收"章（与测试联调同学成员D（商家端/测试）共创确认），组长（成员E（组长））负责引言、需求追踪矩阵和全文终审。SRS §5 数据需求与数据库实体的最终对齐由后端 B 在成稿时自行完成。

## 5. TDD 与 Git 纪律

- 9/4 提交第一批失败测试（`test:` 提交早于对应 `feat:` 实现）；异常场景设计由后端同学自己做，SRS 的"异常边界全集"就是失败测试的用例来源。
- 改代码走特性分支 + PR，后端 PR 由后端 A review；提交信息中文，类型前缀英文（feat/fix/test/docs/chore）。
- 每人 Git 记录里要有自己的提交（`git log --author` 可查），答辩时能解释。

## 6. 关键时间节点

| 日期 | 节点 |
| --- | --- |
| 9/2 晚 | SRS 章节分工发出；契约与清单归属确认到人 |
| 9/3 | SRS / 契约 / 架构设计完成可开发版本；三端工程初始化（`backend/` Spring Boot 骨架） |
| 9/4 | 提交 SRS + 第一批失败测试；三方并行开工 |
| 9/7 晚 | P0 基础功能完成截止并验收演练 |
| 9/8–9/9 | 第一阶段中期验收，验收后立即进入需求变更；**9/9 第二阶段范围决议**：除平台管理员外全部 P1/P2 纳入本期（平台管理员砍掉） |
| 9/10–9/14 | 本期扩展模块分批交付：每模块以“接口 + 数据库 + 测试 + 页面”四类证据收口（分工见《后端任务分配与联调清单》§6） |
| 9/15 | 功能冻结：本期新增模块 deadline；冻结后不再新增功能，未收口模块如实标注“本期实现（待开发）” |

## 7. 本期范围与标注口径（2026-09-09 决议）

- **范围**：除平台管理员外全部 P1/P2 纳入本期开发；平台管理员**已砍掉**，不制作页面、接口、数据与测试（契约 §10.3）。范围真源为 `docs/PRD.md` **v1.3**，约束台账见 `docs/project/项目规则.md` §5。
- **标注规则**：未实现的功能一律写“本期实现（待开发）”“待实现”，证据类字段写“待补”；已实现但缺联调/测试证据的写“代码在库、证据待补”。不得把未实现写成已完成。
- **统一演示数值**：满 20 减 2、满 40 减 5、新客立减 3 元、免配送费门槛 30 元、会员 95 折、红包门槛与适用范围由种子数据定义（一单一红包）；调整须同步契约、种子数据与测试用例。
- **待确认项：无**。契约 §10.4 六条遗留口径已于 2026-09-09 全部拍板（含金额公式、快照字段、图片参数、搜索分页、红包/通知种子、规格命名，见 `docs/project/项目规则.md` §5），**不再是“待确认”项**；其中订单状态机 `PROCESSING` 属**待实现修正**项（`docs/todo/后端.md` TODO-BE-002）。实现完成后由后端 A 回写契约与架构设计，不留两套口径。
- **老师变更**：阶段 2 老师下发的需求变更内容尚未发布（2026-09-10 起按「待定」跟踪：负责人判断大概率不发布，不作为开发前置阻塞），本组范围决议不替代老师变更；变更到达时先补失败测试锁定新行为，再改代码并回归。
