# 项目AI Agent代理协作约束

本仓库为课程项目唯一工作仓库。任何AI Agent代理在本仓库工作前，必须先阅读以下入口文件：

1. `docs/PRD.md`
2. `docs/design/`：设计系统（`设计系统-用户端.md`、`设计系统-商家端.md`）与 `exports/` 设计稿代码导出——用户端 exact 复刻的实现真源
3. `docs/README.md`
4. `docs/project/项目目录索引.md`
5. `docs/project/运行说明.md`
6. `docs/project/小组协作与提交流程.md`
7. `docs/backend/后端接口契约.md`
8. `docs/backend/后端任务分配与联调清单.md`
9. `docs/backend/后端联调验收用例.md`

`docs/PRD.md` 是唯一有效 PRD，负责产品目标、范围、优先级、业务流程、页面和产品验收口径。

## 当前项目边界

- 项目处于**从零开发、文档先行**阶段：功能完成度以真实接口、数据库记录和测试证据为准，不以页面数量判断完成。
- **商家端为桌面 Web 后台**：React 18 + Vite + Ant Design 5，桌面优先 ≥1280px，不做移动端；界面实现以 PRD 的字段列、规则列和接口契约为准。
- **单仓库多目录（2026-09-01 确定）**：用户端放 `frontend/user-h5/`（Vue 3 + Vite + Pinia + Vue Router + Axios，用 `npm create vue` 在该目录从零初始化），商家端放 `frontend/merchant-admin/`（React 18 + Vite + Ant Design 5），Java 后端放 `backend/`。各端独立工程、独立进程、RESTful 通信；不得把后端或商家端代码写进用户端前端工程目录。用户端 UI 修改必须遵守 `docs/frontend/UI复刻经验与执行约定.md` 中的 exact 复刻经验，不得先用通用组件简化搭建。
- 平台客服不在本项目范围；"联系商家"仅表示订单用户与该订单商家之间的会话。
- 普通红色按钮、图标、logo、线条等品牌强调元素统一使用亮橙色 `#ff5a1f`（商家端经 AntD `ConfigProvider` 配置主题色）；只有错误、危险、未读角标等明确语义红色可以保留红色。
- 后端同学按 `docs/backend/后端接口契约.md`、`docs/backend/后端任务分配与联调清单.md` 和 `docs/backend/后端联调验收用例.md` 实现与验收。
- 展示同学发现问题先按 `docs/project/小组协作与提交流程.md` 提交问题单；如需改代码，必须走分支和 PR，由组长 review 后合并。
- `docs/` 根层保留核心文档和阅读入口；面向分工的文档放入 `docs/project/**`、`docs/backend/**`。
- 原型图资产目录：用户端移动原型与红框标注图在 `docs/prd-assets/user/**`，商家端 Stitch 高保真设计稿与红框逐区标注图在 `docs/prd-assets/merchant-stitch/**`；PRD 引用的资产文件不得覆盖。

## 工作过程与 AI 使用记录

- 规则唯一口径：`docs/project/项目规则.md`；记录实体：`docs/record/daily/YYYY-MM-DD.md`（一天一个文件，A 当日进度 + B AI 使用点）。
- **AI Agent 代理每轮工作结束必须起草/更新当天记录**（进度事实 + 本次工作 AI 使用点），交成员确认补充后定稿；不得虚构成员工人写的记录。
- 成员随 Git 提交附带记录（提交信息中写"记录: …"行，或把片段放入 `docs/record/raw/YYYY-MM-DD/` 按天文件夹，落盘即永久留档）；次日上午由智能体汇总前一天提交与片段，整合成当天正式记录文件；raw/ 只读取不清理，任何人不得删除或移动其中片段。
- AI 深度参与产生的提交在提交信息末尾追加 `AI协作: <说明>`；业务代码与全部测试代码不得由同一 AI 一次性生成（见 `docs/project/TDD实施规划.md`）。

## Git 纪律

- **未经项目负责人（用户本人）明确允许，代理不得执行 `git init`、`git commit`、`git push` 或新建仓库**；只做工作区修改，提交时机与拆分由项目负责人决定。
- 提交信息必须使用中文；只有 `feat:`、`fix:`、`chore:` 等开头类型标识可以使用英文。
- 提交信息格式：`<类型>: <中文概要>`，类型用英文小写（feat / fix / test / chore / docs / refactor），概要与正文用中文；**正文用无序列表逐条列出提交的具体内容，每一点一条 `- `**。测试先行要求**同一功能的 `test:`（失败测试）提交早于对应的 `feat:` 实现提交**；提交信息末尾可追加尾注：`记录: <当日工作一句话>` 与 `AI协作: <AI 参与说明>`（供次日记录汇总识别）。
- 主分支为 `main`；改代码走特性分支 + PR，由组长 review 后合并。

## TDD 纪律

- 实施规划唯一口径：`docs/project/TDD实施规划.md`。先理解需求 → 先设计单元测试（正常 + 异常场景）→ 再写业务代码；功能完成的门槛是异常用例全绿，不是"能跑就行"。
- 业务代码与全部测试代码不得由同一 AI 一次性生成后一起提交；测试标准与异常场景由人设计，AI 只可辅助生成脚手架/样板。
- 业务规则必须由后端兜底校验，前端校验只做用户体验层。
- 需求变更（阶段 2）到达时：先补失败测试锁定新行为，再改业务代码并回归。

## 执行规则

- 按计划书任务顺序推进，不跳过前置任务。
- 每完成一个任务，更新计划书状态和必要执行记录。
- 能形成独立回退点的里程碑可以提交一次（须经项目负责人确认）。
- 如用户提出新的特殊要求，追加记录到 `docs/project/项目规则.md` §5 长期约束台账，必要时同步当前入口文档。
- 如新增目录、关键文件或职责边界变化，同步更新 `docs/project/项目目录索引.md`。
- 如修复缺陷、遇到阻塞或做出容易遗忘的规避决策，同步记入当日 `docs/record/daily/` 记录（原问题与修复记录台账已停用）。
