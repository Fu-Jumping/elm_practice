# 项目AI Agent代理协作约束

本仓库为课程项目唯一工作仓库。任何AI Agent代理在本仓库工作前，必须先阅读以下入口文件：

1. `docs/PRD.md`
2. `docs/design/`：设计系统（`设计系统-用户端.md`、`设计系统-商家端.md`）与 `exports/` 设计稿代码导出——用户端 exact 复刻的实现真源
3. `docs/README.md`
4. `docs/project/项目目录索引.md`
5. `docs/project/运行说明.md`
6. `docs/project/小组协作与提交流程.md`
7. `docs/backend/后端接口契约.md`
8. `docs/backend/后端任务分配与联调清单.md`
9. `docs/backend/后端联调验收用例.md`
10. `docs/todo/README.md`：**待办中心入口**——任务台账机制与智能体工作协议；开工前还须读自己负责的板块文件（`用户端.md` / `商家端.md` / `后端.md` / `部署.md` / `文档与交付.md`）

`docs/PRD.md` 是唯一有效 PRD，负责产品目标、范围、优先级、业务流程、页面和产品验收口径。

## 当前项目边界

- 项目处于**从零开发、文档先行**阶段：功能完成度以真实接口、数据库记录和测试证据为准，不以页面数量判断完成。
- **商家端为桌面 Web 后台**：React 18 + Vite + Ant Design 5，桌面优先 ≥1280px，不做移动端；界面实现以 PRD 的字段列、规则列和接口契约为准。
- **本期范围（2026-09-09 第二阶段范围决议，负责人拍板）**：除平台管理员外，全部 P1 与 P2 纳入本期开发（P1 全项 + 简化取消订单、简化配送管理、图片上传、设置与 FAQ）；**平台管理员已砍掉**，不制作页面、接口、数据与测试，不作为交付内容。商家端原标注"二期不做"的 7 页（运营概览、优惠配置、评价管理、评价回复、消息列表、聊天详情、数据统计）与规格维护、联系顾客一并纳入本期，用户端扩展服务 13 页同为交付范围；未实现项一律写"本期实现（待开发）/待实现"，不得写成已完成。老师下发的阶段 2 需求变更内容尚未发布（**2026-09-10 起按「待定」跟踪：负责人判断大概率不发布，不作为开发前置阻塞**），本组范围决议不替代老师变更。范围真源为 `docs/PRD.md` v1.1，约束台账见 `docs/project/项目规则.md` §5。
- **单仓库多目录（2026-09-01 确定）**：用户端放 `frontend/user-h5/`（Vue 3 + Vite + Pinia + Vue Router + Axios，用 `npm create vue` 在该目录从零初始化），商家端放 `frontend/merchant-admin/`（React 18 + Vite + Ant Design 5），Java 后端放 `backend/`。各端独立工程、独立进程、RESTful 通信；不得把后端或商家端代码写进用户端前端工程目录。用户端 UI 修改必须遵守 `docs/frontend/UI复刻经验与执行约定.md` 中的 exact 复刻经验，不得先用通用组件简化搭建。
- 平台客服不在本项目范围；"联系商家"仅表示订单用户与该订单商家之间的会话。
- 普通红色按钮、图标、logo、线条等品牌强调元素统一使用亮橙色 `#ff5a1f`（商家端经 AntD `ConfigProvider` 配置主题色）；只有错误、危险、未读角标等明确语义红色可以保留红色。
- 后端同学按 `docs/backend/后端接口契约.md`、`docs/backend/后端任务分配与联调清单.md` 和 `docs/backend/后端联调验收用例.md` 实现与验收。
- 展示同学发现问题先按 `docs/project/小组协作与提交流程.md` 提交问题单，并**正式落盘到 `docs/testing/问题记录/`**（群聊只作通知，不作证据）；如需改代码，必须走分支和 PR，由组长 review 后合并。
- `docs/` 根层保留核心文档和阅读入口；面向分工的文档放入 `docs/project/**`、`docs/backend/**`。
- **材料入库边界（2026-09-10 负责人拍板，R9）**：课程演示页与证据（`docs/presentation/**`、`deploy/**`）进 main；**讲稿类材料（如 `docs/presentation/答辩手册.html`）与个人材料不进 main**，保留在对应分支或本地。新增材料入库前先确认属于哪一类。
- 原型图资产目录：用户端移动原型与红框标注图在 `docs/prd-assets/user/**`，商家端 Stitch 高保真设计稿与红框逐区标注图在 `docs/prd-assets/merchant-stitch/**`；PRD 引用的资产文件不得覆盖。

## 工作过程与 AI 使用记录

- 规则唯一口径：`docs/project/项目规则.md`；正式记录实体：`docs/record/daily/YYYY-MM-DD.md`（一天一个文件，A 当日进度 + B AI 使用点），**唯一写权归次日上午汇总智能体（或组长在 main 上直接维护）**。
- **AI Agent 代理每轮工作结束必须把留痕片段写入 `docs/record/raw/YYYY-MM-DD/`**（内容为进度事实 + 本次工作 AI 使用点，字段口径沿用 A/B 表）；**同一天同一成员或角色只写一个文件**（规范文件名 `<成员或角色>.md`，后续轮次在文件末尾追加 `## HH:mm <本轮主题>` 小节，不得一轮新开一个文件；只有跨天才新开）；**禁止创建或修改 `docs/record/daily/**` 下的任何文件**；不得虚构成员工人写的记录。规则细则（含过渡口径）见 `docs/project/项目规则.md` §2 第 2 条 2.1–2.3。
- **待办台账（2026-09-10 起）**：`docs/todo/` 是「谁在做什么、做到哪一步」的唯一来源，规则见 `docs/project/项目规则.md` §1.2；AI 代理只做负责人属于自己的 `进行中` 任务，按「出处」回读 PRD/契约/TC，有进展即更新证据列，发现新问题直接追加任务行（缺陷类同时登记 `docs/testing/缺陷跟踪表.md`），**每轮收工的 raw 片段写明本轮新增/更新了哪些 TODO 编号**；不得替他人改状态、不得虚构进度。
- 成员随 Git 提交附带记录（提交信息中写"记录: …"行，或把片段放入 `docs/record/raw/YYYY-MM-DD/` 按天文件夹，落盘即永久留档）；次日上午由汇总智能体读取前一天提交与 raw/ 片段，独立撰写当天正式记录文件；raw/ 只读取不清理，任何人不得删除或移动其中片段。
- **问题记录与缺陷跟踪正式落盘**：唯一正式台账 `docs/testing/缺陷跟踪表.md`，明细 `docs/testing/问题记录/YYYY-MM-DD-<主题>.md`，均由 Git 跟踪、写权归测试联调同学（王馨琪）；不得只放群聊、共享文档或 `docs/_archive/**`（已 gitignore）。规则见 `docs/project/项目规则.md` §1.1。
- **特性分支与 PR 不得携带 `docs/record/daily/**` 的变更**（PR 评审检查项，携带即打回）。
- AI 深度参与产生的提交在提交信息末尾追加 `AI协作: <说明>`；业务代码与全部测试代码不得由同一 AI 一次性生成（见 `docs/project/TDD实施规划.md`）。

## Git 纪律

- **未经项目负责人（用户本人）明确允许，代理不得执行 `git init`、`git commit`、`git push` 或新建仓库**；只做工作区修改，提交时机与拆分由项目负责人决定。
- 提交信息必须使用中文；只有 `feat:`、`fix:`、`chore:` 等开头类型标识可以使用英文。
- 提交信息格式：`<类型>: <中文概要>`，类型用英文小写（feat / fix / test / chore / docs / refactor），概要与正文用中文；**正文用无序列表逐条列出提交的具体内容，每一点一条 `- `**。测试先行要求**同一功能的 `test:`（失败测试）提交早于对应的 `feat:` 实现提交，且 `test:` 提交仅含测试代码（在该提交上复跑必须红，见 TDD 纪律）**；提交信息末尾追加尾注：`记录: <当日工作一句话>`（建议）与 `AI协作: <AI 参与说明>`（AI 深度参与产生的提交必须追加，供次日记录汇总识别）。
- 主分支为 `main`；改代码走特性分支 + PR，由组长 review 后合并。

## TDD 纪律

- 实施规划唯一口径：`docs/project/TDD实施规划.md`。先理解需求 → 先设计单元测试（正常 + 异常场景）→ 再写业务代码；功能完成的门槛是异常用例全绿，不是"能跑就行"。
- 业务代码与全部测试代码不得由同一 AI 一次性生成后一起提交；测试标准与异常场景由人设计，AI 只可辅助生成脚手架/样板。
- **`test:`（失败测试）提交仅含测试代码与测试脚手架**，业务实现一律进入对应的 `feat:` 提交；合并前在 `test:` 提交上复跑测试必须失败（红），`feat:` 后转绿，红绿两端证据（红 X failed / 绿 Y passed）写入 PR 描述或 raw/ 片段（2026-09-06 修订，源自 PR #9 打回教训）。
- 业务规则必须由后端兜底校验，前端校验只做用户体验层。
- 需求变更（阶段 2）到达时：先补失败测试锁定新行为，再改业务代码并回归。
- **合并前必跑矩阵（R1，2026-09-10）**：改前端必须跑 `npm run build`（含类型检查）**和** `npm test`；改后端必须跑 `mvn -B -DskipTests package`（有测试库时补 `mvn test`）；改文档必须做口径 grep 复查；改脚本必须做语法检查。只跑单测不跑构建**不算通过**（2026-09-10 实测：只跑 `npm test` 漏掉 `tsc` 类型错误，直到服务器构建才暴露）。命令与结果写入 PR 描述（模板 `.github/pull_request_template.md`），CI 见 `.github/workflows/ci.yml`。

## 执行规则

- 按计划书任务顺序推进，不跳过前置任务。
- 每完成一个任务，更新计划书状态和必要执行记录。
- 能形成独立回退点的里程碑可以提交一次（须经项目负责人确认）。
- 如用户提出新的特殊要求，追加记录到 `docs/project/项目规则.md` §5 长期约束台账，必要时同步当前入口文档。
- **口径变更同一 PR 同步（R7，2026-09-10）**：凡改动需求口径（PRD）、接口（契约）、用例（TDD 规划/验收用例）或范围决议，必须在**同一个 PR 内**把受影响的文档一起改完，并在 PR 描述里列出改动清单；不允许两套口径并存，也不允许“先改代码、文档下个 PR 补”。
- 如新增目录、关键文件或职责边界变化，同步更新 `docs/project/项目目录索引.md`。
- 如修复缺陷、遇到阻塞或做出容易遗忘的规避决策：正式条目落 `docs/testing/问题记录/**` 并登记 `docs/testing/缺陷跟踪表.md`（测试联调同学维护，规则见 `docs/project/项目规则.md` §1.1），同时以 raw/ 片段留痕（`docs/record/raw/YYYY-MM-DD/`）随次晨汇总进入当日记录；原问题与修复记录台账已停用。
