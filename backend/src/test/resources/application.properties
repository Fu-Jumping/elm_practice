# 测试连服务器独立测试库 elm_practice_test（与开发库隔离）；密码经 DB_PASSWORD 环境变量注入，不入仓库。
spring.datasource.url=jdbc:mysql://${DB_HOST:82.157.137.114}:3306/${DB_NAME:elm_practice_test}?useUnicode=true&characterEncoding=utf8&connectionTimeZone=Asia/Shanghai&forceConnectionTimeZoneToSession=true&allowPublicKeyRetrieval=true&useSSL=false
spring.datasource.username=${DB_USER:elm}
spring.datasource.password=${DB_PASSWORD:}
mybatis.configuration.map-underscore-to-camel-case=true
