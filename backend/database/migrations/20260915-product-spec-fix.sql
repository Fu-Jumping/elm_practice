-- 香辣鸡腿堡（p101）不应有规格（2026-09-15 验收快修）。
-- 口径：p101 是「点加号直接按基础价加购」的商品；PRD §7 商品详情里的「中杯/大杯」只是**饮品类**规格示例，
-- 被误配到 p101 上（线上业务库实测 spec_options = [{"name":"大杯","priceDelta":3.00}]），
-- 导致用户端点加号先弹规格弹层而不是直接加购。
-- 幂等：可重复执行；只清 p101，不触碰 p103（标准/大份）、p106（5块/10块）等确需规格的商品。
UPDATE products SET spec_options = NULL WHERE product_id = 'p101';
