# E2E 专用模式（2026-09-13，TODO-USER-023）：Playwright 的 webServer 以 `vite --mode mock` 启动，
# 只加载 .env.mock、.env 与 .env.local，**不会**读到只按 mode 名匹配的 .env.development / .env.development.local。
# 原因：开发者本机会常驻 .env.development.local（VITE_API_MODE=real，见 BUG-20260908-001 的联调口径），
# 它会让 E2E 跑到真实后端上（本机不装 MySQL → 代理 ECONNREFUSED），保真度巡检必须有确定的数据源。
VITE_API_MODE=mock
VITE_API_BASE_URL=http://127.0.0.1:4000/api/v1
VITE_MOCK_FALLBACK=false
